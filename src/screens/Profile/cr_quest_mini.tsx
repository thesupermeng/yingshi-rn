import React, { useEffect, useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  Image,
  RefreshControl,
  ScrollView,
} from "react-native";
import ScreenContainer from "../../components/container/ypy_fast";
import { RootStackScreenProps } from "@type/vrm_thailand";
import { useTheme } from "@react-navigation/native";
import { PSmall } from "@redux/fj_prediction_thailand";

import TitleWithBackButtonHeader from "../../components/header/cio_news_types_header";
import axios from "axios";
import { Keyboard } from "react-native";
import {
  API_DOMAIN,
  API_DOMAIN_TEST,
  API_DOMAIN_LOCAL,
} from "@utility/n_subs_interstitial";
import NetInfo, { NetInfoState } from "@react-native-community/netinfo";

import InviteStep from "./../../components/invite/dv_header_sans";
import InviteCard from "./../../components/invite/r_sound";

import InviteHeader from "./../../components/invite/bf_apple_filter_header";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/kg_index";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import { updateUserAuth } from "@redux/actions/rk_filed_watch";
import umb_center_carousel from '../../../Umeng/umb_center_carousel';
import { THFirebase } from "@api";
import { HDTGesturesList } from "@redux/reducers/f_gmail";


export default ({ navigation }: RootStackScreenProps<"邀请">) => {
  const { colors, textVariants, icons, spacing } = useTheme();

  const userState = useSelector<HDTGesturesList>('userReducer');

  const [refreshing, setRefreshing] = useState(false);
  const dispatch = useAppDispatch();

  
  useEffect(() => {
    umb_center_carousel.userCenterVipInviteViewsAnalytics();
  }, []);
  

  const handleRefresh = async () => {
       let shrinkU = 2;
    let ajaxc = String.fromCharCode(118,95,48,95,100,101,97,99,116,0);
    let pagination2: Array<any> = [63, 179, 203];
    let watchd = 1.0;
    let agreement9 = true;
    let blacklistT = true;
    let eactK = String.fromCharCode(116,114,97,99,107,101,100,95,113,95,56,56,0);
    let downloaderI: Array<any> = [String.fromCharCode(109,95,51,50,95,115,99,114,111,108,108,97,98,108,101,0), String.fromCharCode(100,99,97,109,97,116,104,95,52,95,49,52,0), String.fromCharCode(105,95,57,51,95,99,111,110,118,101,114,115,105,111,110,115,0)];
    let minivodk = String.fromCharCode(99,95,55,55,95,97,100,100,109,111,100,0);
      pagination2 = [((blacklistT ? 3 : 2) / (Math.max(downloaderI.length, 7)))];
       let const_ar3: Array<any> = [87, 486];
       let ballp: Array<any> = [993, 689];
          let gesturesn: Array<any> = [114, 622];
          let schedulev: Map<any, any> = new Map([[String.fromCharCode(99,95,53,54,95,112,111,105,110,116,0),String.fromCharCode(101,110,99,111,100,101,100,102,114,97,109,101,95,98,95,49,0)], [String.fromCharCode(109,102,114,97,95,102,95,57,49,0),String.fromCharCode(116,95,53,56,95,115,117,112,101,114,103,114,111,117,112,0)], [String.fromCharCode(103,95,57,57,95,101,120,97,109,105,110,101,0),String.fromCharCode(109,97,114,107,101,114,115,95,114,95,49,49,0)]]);
         const_ar3.push(3 & const_ar3.length);
         gesturesn.push(3 + schedulev.size);
         schedulev.set(`${gesturesn.length}`, gesturesn.length);
      if (4 <= ballp.length) {
         const_ar3.push(ballp.length);
      }
      for (let f = 0; f < 3; f++) {
         ballp = [1];
      }
       let linkF = 3.0;
          let signinup6: Map<any, any> = new Map([[String.fromCharCode(121,95,52,50,95,114,101,112,108,97,121,101,100,0),38], [String.fromCharCode(104,95,49,57,95,115,104,97,108,108,111,119,0),345]]);
          let langw = 1;
          let shareQ = true;
         const_ar3.push(const_ar3.length);
         signinup6.set(`${shareQ}`, langw * 2);
         langw /= Math.max(langw, 4);
         shareQ = !shareQ;
          let typingG = false;
          let dropdownB = String.fromCharCode(114,111,116,97,116,101,100,95,113,95,55,56,0);
         ballp = [1 ^ ballp.length];
         typingG = dropdownB.includes(`${typingG}`);
         dropdownB += `${((typingG ? 2 : 1))}`;
      blacklistT = (pagination2.length >> (Math.min(ballp.length, 3))) < 21;
       let expiredW = String.fromCharCode(120,95,50,55,95,111,112,101,110,0);
       let entryZ = String.fromCharCode(110,95,54,48,95,108,111,110,103,101,114,0);
      if (entryZ == String.fromCharCode(49,0)) {
         expiredW += `${entryZ.length}`;
      }
         expiredW = `${entryZ.length % (Math.max(1, 10))}`;
       let weiboE = 1.0;
       let homet = 1.0;
         entryZ = `${parseInt(`${homet}`) | entryZ.length}`;
         entryZ = `${expiredW.length - 3}`;
      let helperD = 8506889.0 >= weiboE;
      do {
         weiboE += (String.fromCharCode(86,0) == expiredW ? parseInt(`${weiboE}`) : expiredW.length);
         if (helperD) {
            break;
         }
      } while (helperD && (2.95 > (homet + 3)));
      downloaderI.push(1);
   for (let k = 0; k < 3; k++) {
      blacklistT = ajaxc.length <= shrinkU;
   }
   for (let w = 0; w < 1; w++) {
      shrinkU >>= Math.min(1, Math.abs((String.fromCharCode(69,0) == eactK ? pagination2.length : eactK.length)));
   }
   if (downloaderI.length >= 1) {
       let closek = 2.0;
      for (let g = 0; g < 2; g++) {
         closek -= parseInt(`${closek}`);
      }
       let activex = 2;
       let taiwanS = 2;
         taiwanS >>= Math.min(Math.abs(taiwanS ^ activex), 3);
      downloaderI = [ajaxc.length];
   }
       let termss: Array<any> = [872, 177];
         termss.push(termss.length % (Math.max(2, termss.length)));
      if (termss.length <= termss.length) {
         termss.push(termss.length % 1);
      }
          let eactU = 4.0;
         termss.push(termss.length >> (Math.min(Math.abs(2), 4)));
         eactU /= Math.max(4, parseFloat(`${1 % (Math.max(parseInt(`${eactU}`), 2))}`));
      watchd -= parseFloat(`${eactK.length}`);
       let project9: Array<any> = [String.fromCharCode(112,105,99,107,105,110,116,101,114,95,102,95,54,57,0), String.fromCharCode(114,95,51,53,95,109,97,121,0)];
       let themeE: Array<any> = [985, 265];
       let infoO = 2.0;
      if (3.71 >= infoO) {
         infoO -= 2 & parseInt(`${infoO}`);
      }
      while ((2 >> (Math.min(2, themeE.length))) == 3) {
          let listc = 3.0;
          let goalk: Map<any, any> = new Map([[String.fromCharCode(109,98,115,101,103,109,101,110,116,97,116,105,111,110,95,122,95,49,56,0),484], [String.fromCharCode(99,111,110,118,115,97,109,112,95,116,95,52,54,0),892], [String.fromCharCode(99,104,111,115,101,110,95,54,95,50,51,0),252]]);
          let trashF = true;
         themeE = [project9.length];
         listc -= parseFloat(`${3}`);
         goalk = new Map([[`${goalk.size}`, goalk.size]]);
         trashF = goalk.size >= 27 && trashF;
         break;
      }
      if ((themeE.length - 2) >= 4 && 5.51 >= (4.15 - infoO)) {
         infoO *= project9.length;
      }
      for (let p = 0; p < 2; p++) {
          let bottomM = true;
          let default_rvb = String.fromCharCode(103,95,49,51,95,109,117,116,97,116,105,110,103,0);
          let delegate_1cz = 1.0;
          let rewind3 = true;
         project9.push((String.fromCharCode(108,0) == default_rvb ? (bottomM ? 1 : 4) : default_rvb.length));
         bottomM = rewind3 && 60.60 >= delegate_1cz;
         delegate_1cz /= Math.max(4, (parseFloat(`${(rewind3 ? 4 : 4) >> (Math.min(Math.abs(parseInt(`${delegate_1cz}`)), 1))}`)));
      }
      while (4 == (themeE.length % (Math.max(3, 10))) && 1 == (3 << (Math.min(2, themeE.length)))) {
          let agreementI = 3.0;
          let rewindk = 1.0;
         themeE.push(3);
         agreementI += parseInt(`${rewindk}`);
         rewindk -= 1;
         break;
      }
      for (let h = 0; h < 3; h++) {
         themeE = [themeE.length ^ project9.length];
      }
         themeE.push(parseInt(`${infoO}`));
         infoO -= parseInt(`${infoO}`);
         themeE.push(parseInt(`${infoO}`));
      agreement9 = ajaxc == String.fromCharCode(95,0);
   let i_playerO = blacklistT ? !blacklistT : blacklistT;
   do {
       let heartK = String.fromCharCode(98,95,50,48,95,100,101,99,111,109,112,111,115,101,105,0);
       let slider7 = String.fromCharCode(116,97,108,107,95,108,95,50,53,0);
       let bottomb = 1.0;
       let combineN: Array<any> = [String.fromCharCode(100,116,100,102,95,48,95,55,54,0), String.fromCharCode(120,95,51,51,95,117,110,119,114,97,112,0)];
       let type_yo = String.fromCharCode(111,95,50,53,95,110,111,109,101,109,0);
          let viewerC = String.fromCharCode(106,95,56,52,95,109,111,118,101,112,97,103,101,0);
          let fastforwardh = String.fromCharCode(119,95,54,50,95,99,111,117,110,116,101,114,115,0);
         bottomb += heartK.length;
         viewerC = `${1 + viewerC.length}`;
         fastforwardh = `${viewerC.length}`;
         combineN = [combineN.length ^ slider7.length];
         type_yo = `${slider7.length | 3}`;
          let bottomk: Array<any> = [String.fromCharCode(101,109,111,114,121,95,101,95,54,51,0), String.fromCharCode(116,95,49,48,95,105,109,101,110,115,105,111,110,0)];
          let unselectedX = String.fromCharCode(116,95,56,95,100,101,110,111,109,0);
         combineN = [(slider7 == String.fromCharCode(97,0) ? slider7.length : bottomk.length)];
         bottomk.push(unselectedX.length);
         unselectedX = `${unselectedX.length}`;
         type_yo += `${slider7.length | 3}`;
      while ((type_yo.length << (Math.min(2, combineN.length))) >= 2 || 3 >= (2 << (Math.min(2, type_yo.length)))) {
         type_yo += `${heartK.length}`;
         break;
      }
       let inviter = true;
      let photor = String.fromCharCode(121,113,110,51,119,122,101,0) == heartK;
      do {
         heartK = `${combineN.length}`;
         if (photor) {
            break;
         }
      } while (photor && (4 < type_yo.length));
      while (slider7.startsWith(`${combineN.length}`)) {
         combineN = [(String.fromCharCode(95,0) == slider7 ? (inviter ? 1 : 4) : slider7.length)];
         break;
      }
          let pagel = String.fromCharCode(110,101,101,100,95,103,95,53,53,0);
          let forwardf = String.fromCharCode(110,111,110,98,95,52,95,54,57,0);
          let gemfileF = false;
         inviter = pagel.length > 92;
         pagel = `${(String.fromCharCode(85,0) == forwardf ? (gemfileF ? 5 : 4) : forwardf.length)}`;
         gemfileF = (((gemfileF ? 74 : forwardf.length) * forwardf.length) == 74);
          let themex = String.fromCharCode(108,105,110,109,97,116,104,95,102,95,51,50,0);
          let sendk = String.fromCharCode(114,101,115,104,97,112,101,95,104,95,51,55,0);
          let apps7 = String.fromCharCode(101,115,99,97,112,101,95,118,95,53,51,0);
         combineN = [(sendk == String.fromCharCode(52,0) ? combineN.length : sendk.length)];
         themex += `${themex.length}`;
         apps7 += `${(themex == String.fromCharCode(102,0) ? apps7.length : themex.length)}`;
         bottomb /= Math.max(4, 3 * heartK.length);
      for (let a = 0; a < 3; a++) {
         type_yo += "2";
      }
      if (!inviter) {
         type_yo = `${((inviter ? 3 : 5) + 3)}`;
      }
         slider7 = `${parseInt(`${bottomb}`) >> (Math.min(combineN.length, 5))}`;
      blacklistT = 47.32 < watchd && 46 < pagination2.length;
      if (i_playerO) {
         break;
      }
   } while ((agreement9) && i_playerO);
      downloaderI = [eactK.length - downloaderI.length];

    setRefreshing(true);

      pagination2.push(eactK.length ^ 3);
   for (let j = 0; j < 2; j++) {
       let zhuboF = 2.0;
       let logoutX: Array<any> = [String.fromCharCode(115,97,118,101,112,111,105,110,116,115,95,56,95,49,55,0), String.fromCharCode(116,104,114,101,97,100,105,110,103,95,97,95,52,50,0), String.fromCharCode(112,95,50,54,95,109,97,105,110,112,97,103,101,0)];
      if (1.89 > (zhuboF - logoutX.length) && (3 >> (Math.min(5, logoutX.length))) > 1) {
         zhuboF += logoutX.length / 2;
      }
          let pageO = String.fromCharCode(111,95,51,48,95,108,105,110,103,101,114,0);
          let floatinge = 4.0;
         zhuboF += parseInt(`${floatinge}`) % (Math.max(9, logoutX.length));
         pageO += `${pageO.length & pageO.length}`;
         floatinge -= 1 & pageO.length;
      while ((5.81 * zhuboF) == 2.1) {
         zhuboF += parseInt(`${zhuboF}`) ^ logoutX.length;
         break;
      }
      if (!logoutX.includes(zhuboF)) {
         logoutX = [2 ^ parseInt(`${zhuboF}`)];
      }
       let station1: Map<any, any> = new Map([[String.fromCharCode(115,116,114,105,112,101,95,99,95,57,55,0),String.fromCharCode(100,95,52,95,117,110,119,114,105,116,97,98,108,101,0)], [String.fromCharCode(116,101,109,112,108,97,116,101,95,102,95,53,52,0),String.fromCharCode(110,95,54,48,95,114,101,113,117,101,115,116,0)], [String.fromCharCode(101,95,52,53,95,120,99,101,112,116,105,111,110,0),String.fromCharCode(97,115,109,100,101,102,115,95,115,95,48,0)]]);
       let common0: Map<any, any> = new Map([[String.fromCharCode(116,114,97,115,104,111,108,100,95,120,95,55,57,0),343], [String.fromCharCode(107,95,54,55,95,101,113,117,105,108,105,98,114,105,117,109,0),750], [String.fromCharCode(117,118,104,111,114,105,122,111,110,116,97,108,95,113,95,53,51,0),164]]);
          let circleN = 3.0;
          let c_center1 = 4.0;
          let countdownA: Map<any, any> = new Map([[String.fromCharCode(117,95,51,52,95,99,101,110,116,114,111,105,100,115,0),false ], [String.fromCharCode(109,105,99,114,111,115,111,102,116,95,105,95,57,50,0),false ], [String.fromCharCode(112,97,114,99,111,114,95,115,95,56,0),false ]]);
         zhuboF += 1;
         circleN /= Math.max(parseFloat(`${countdownA.size | parseInt(`${c_center1}`)}`), 5);
         c_center1 /= Math.max(parseFloat(`${parseInt(`${circleN}`)}`), 4);
         countdownA = new Map([[`${circleN}`, 2 << (Math.min(Math.abs(parseInt(`${circleN}`)), 2))]]);
      blacklistT = ajaxc.startsWith(`${blacklistT}`);
   }
      eactK += "3";
   if (5 < (1 << (Math.min(5, pagination2.length))) || 3 < (ajaxc.length << (Math.min(Math.abs(1), 2)))) {
      pagination2.push(1);
   }
   for (let p = 0; p < 1; p++) {
      eactK += "3";
   }
   if (!agreement9 && blacklistT) {
      blacklistT = ajaxc == String.fromCharCode(78,0);
   }
      shrinkU += (shrinkU ^ (blacklistT ? 5 : 3));
      pagination2.push(3);
   if (watchd > 3.69) {
       let blackA = String.fromCharCode(111,95,50,55,95,99,111,110,110,101,99,116,101,100,0);
       let ewardedO = String.fromCharCode(115,104,111,119,105,110,103,95,118,95,57,57,0);
      while (ewardedO != String.fromCharCode(74,0) || blackA == String.fromCharCode(80,0)) {
         blackA += `${(ewardedO == String.fromCharCode(72,0) ? ewardedO.length : blackA.length)}`;
         break;
      }
          let tumbnailD = 0.0;
          let malaysiaN = String.fromCharCode(118,95,52,54,0);
          let countdown5 = 4.0;
         blackA = `${malaysiaN.length + parseInt(`${countdown5}`)}`;
         tumbnailD *= 2 * parseInt(`${tumbnailD}`);
         malaysiaN = `${parseInt(`${tumbnailD}`) | 1}`;
      while (2 == blackA.length) {
         blackA += `${ewardedO.length}`;
         break;
      }
         ewardedO += `${ewardedO.length}`;
         blackA = `${blackA.length}`;
         blackA += `${ewardedO.length}`;
      pagination2.push((downloaderI.length * (agreement9 ? 3 : 3)));
   }
       let l_positionm = String.fromCharCode(122,95,56,95,114,101,97,100,102,117,108,108,0);
       let overI: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,114,100,97,98,108,101,95,50,95,55,0),911], [String.fromCharCode(115,97,110,105,116,105,122,101,100,95,100,95,51,57,0),217], [String.fromCharCode(116,95,53,54,95,109,111,98,105,117,115,0),594]]);
       let layoutH: Map<any, any> = new Map([[String.fromCharCode(99,111,109,112,108,101,109,101,110,116,105,110,103,95,107,95,50,53,0),String.fromCharCode(104,95,51,95,114,101,112,111,114,116,0)], [String.fromCharCode(101,95,51,49,95,101,108,115,101,0),String.fromCharCode(109,101,100,105,97,99,111,100,101,99,100,101,99,95,101,95,56,0)], [String.fromCharCode(99,108,105,112,102,95,114,95,50,49,0),String.fromCharCode(114,101,104,97,115,104,95,111,95,51,50,0)]]);
      if ((overI.size >> (Math.min(Math.abs(layoutH.size), 2))) < 5) {
         layoutH = new Map([[`${layoutH.size}`, overI.size]]);
      }
       let short_mX = true;
      watchd /= Math.max(parseFloat(`${eactK.length / 3}`), 1);
      l_positionm += `${l_positionm.length + 2}`;
    await refreshUserState();

       let l_player3 = String.fromCharCode(102,105,108,101,110,97,109,101,115,95,106,95,56,48,0);
         l_player3 += `${l_player3.length}`;
      for (let x = 0; x < 2; x++) {
         l_player3 += "3";
      }
         l_player3 = `${(l_player3 == String.fromCharCode(67,0) ? l_player3.length : l_player3.length)}`;
      blacklistT = 19 <= shrinkU;
   let sourcem = watchd <= 8525442.0;
   do {
       let sentry5 = String.fromCharCode(97,95,54,95,97,108,97,110,103,117,97,103,101,0);
         sentry5 = `${sentry5.length}`;
      while (!sentry5.includes(sentry5)) {
         sentry5 = `${sentry5.length}`;
         break;
      }
         sentry5 += `${2 | sentry5.length}`;
      watchd += parseFloat(`${2}`);
      if (sourcem) {
         break;
      }
   } while ((parseInt(`${watchd}`) == pagination2.length) && sourcem);
   if ((pagination2.length | 2) > 4 && 2 > (pagination2.length | downloaderI.length)) {
      downloaderI = [(ajaxc.length & (blacklistT ? 4 : 1))];
   }
       let selected1: Map<any, any> = new Map([[String.fromCharCode(99,95,51,50,95,120,112,97,108,100,118,0),358], [String.fromCharCode(110,101,119,108,105,110,101,95,98,95,50,50,0),156], [String.fromCharCode(121,95,54,49,95,115,119,97,112,121,118,98,117,102,102,101,114,0),11]]);
         selected1.set(`${selected1.size}`, selected1.size);
         selected1 = new Map([[`${selected1.size}`, selected1.size % (Math.max(selected1.size, 8))]]);
      let orientationR = selected1.size <= 7032407;
      do {
         selected1.set(`${selected1.size}`, selected1.size);
         if (orientationR) {
            break;
         }
      } while ((!Array.from(selected1.keys()).includes(`${selected1.size}`)) && orientationR);
      blacklistT = String.fromCharCode(65,0) == ajaxc;
   let teamH = 9023381 <= shrinkU;
   do {
       let reducerA: Map<any, any> = new Map([[String.fromCharCode(113,95,55,51,95,108,111,110,103,105,116,117,100,101,0),604], [String.fromCharCode(122,95,49,48,48,95,97,100,97,112,116,97,116,105,111,110,0),904]]);
       let suggestion7 = 4;
       let leaguer = 4.0;
       let favicon_ = true;
       let calendart: Map<any, any> = new Map([[String.fromCharCode(103,114,97,112,104,113,108,95,118,95,54,55,0),String.fromCharCode(109,105,120,101,114,95,97,95,53,0)], [String.fromCharCode(97,110,121,95,97,95,52,51,0),String.fromCharCode(120,95,57,49,95,97,118,100,101,118,105,99,101,0)]]);
          let targetG: Map<any, any> = new Map([[String.fromCharCode(111,117,98,108,101,95,111,95,53,52,0),93], [String.fromCharCode(109,95,52,56,95,115,111,108,117,116,105,111,110,0),765], [String.fromCharCode(114,101,106,111,105,110,95,116,95,52,54,0),294]]);
         calendart.set(`${favicon_}`, parseInt(`${leaguer}`) >> (Math.min(4, Math.abs(2))));
         targetG = new Map([[`${targetG.size}`, targetG.size - targetG.size]]);
         leaguer -= parseFloat(`${suggestion7 / (Math.max(calendart.size, 6))}`);
      for (let i = 0; i < 1; i++) {
          let friendsO = 4;
          let navigation5 = String.fromCharCode(114,101,112,111,114,116,105,110,103,95,113,95,56,53,0);
          let bodan7 = 1;
          let settingG = 2;
         leaguer -= parseFloat(`${parseInt(`${leaguer}`) % (Math.max(2, 9))}`);
         friendsO -= 1;
         navigation5 = `${1 + friendsO}`;
         bodan7 &= bodan7 >> (Math.min(3, Math.abs(friendsO)));
         settingG ^= navigation5.length * bodan7;
      }
         suggestion7 /= Math.max(2, reducerA.size * suggestion7);
      while (2 > (suggestion7 % (Math.max(calendart.size, 2))) && (calendart.size % (Math.max(2, 3))) > 5) {
          let formy = true;
          let basketballw = String.fromCharCode(101,95,51,53,0);
         calendart.set(`${leaguer}`, 1);
         formy = (3 <= ((formy ? basketballw.length : 3) & basketballw.length));
         break;
      }
      for (let f = 0; f < 3; f++) {
         calendart = new Map([[`${favicon_}`, suggestion7]]);
      }
      if (2 <= suggestion7) {
         favicon_ = reducerA.size < 38;
      }
      let success2 = 5404856 >= suggestion7;
      do {
         suggestion7 <<= Math.min(Math.abs(reducerA.size & 1), 2);
         if (success2) {
            break;
         }
      } while ((4 <= suggestion7) && success2);
         leaguer *= parseFloat(`${2}`);
      while (2.18 < leaguer) {
          let filledr: Map<any, any> = new Map([[String.fromCharCode(100,101,108,101,116,105,110,103,95,122,95,54,55,0),582], [String.fromCharCode(112,95,49,54,95,108,97,116,101,98,105,110,100,105,110,103,115,121,109,98,111,108,116,97,98,108,101,0),170]]);
          let temperatureG = 5;
         leaguer += (parseFloat(`${(favicon_ ? 2 : 1) & temperatureG}`));
         filledr.set(`${filledr.size}`, filledr.size);
         temperatureG %= Math.max(filledr.size << (Math.min(Math.abs(1), 1)), 4);
         break;
      }
       let s_titler = String.fromCharCode(113,95,55,95,105,110,99,114,101,97,115,101,0);
       let currentk: Map<any, any> = new Map([[String.fromCharCode(105,110,112,117,116,121,95,119,95,51,57,0),657], [String.fromCharCode(111,95,52,51,95,99,111,100,101,99,115,0),477]]);
          let aboutA = String.fromCharCode(108,103,111,114,97,110,100,95,109,95,50,50,0);
          let emojia = String.fromCharCode(102,114,97,109,101,115,95,99,95,49,54,0);
         s_titler += `${currentk.size ^ parseInt(`${leaguer}`)}`;
         aboutA = `${aboutA.length}`;
         emojia += `${aboutA.length}`;
         favicon_ = suggestion7 == 41;
         calendart.set(`${s_titler}`, calendart.size & s_titler.length);
      shrinkU += ((agreement9 ? 2 : 2) + 3);
      if (teamH) {
         break;
      }
   } while (((eactK.length ^ 1) >= 5 || (shrinkU ^ 1) >= 1) && teamH);
      eactK = "3";
      ajaxc = `${downloaderI.length ^ 3}`;
      pagination2 = [(parseInt(`${watchd}`) | (blacklistT ? 4 : 1))];
   while (blacklistT) {
      minivodk = `${3 & eactK.length}`;
      break;
   }
       let flyerJ = true;
       let weiboX = 5.0;
       let moduleC = 3.0;
          let styleE = true;
          let memberT: Map<any, any> = new Map([[String.fromCharCode(115,95,51,55,95,109,105,100,115,0),false ], [String.fromCharCode(99,95,52,55,95,102,109,105,120,0),true ], [String.fromCharCode(117,95,57,55,95,109,111,109,101,110,116,117,109,0),true ]]);
          let emoji3 = 1;
         weiboX *= (2 & (styleE ? 2 : 4));
         styleE = emoji3 <= memberT.size;
         memberT = new Map([[`${memberT.size}`, emoji3]]);
          let overlayO = 0.0;
          let yingW: Array<any> = [378, 873];
          let chartx: Array<any> = [738, 662];
         weiboX += ((flyerJ ? 1 : 2) % (Math.max(1, parseInt(`${weiboX}`))));
         overlayO -= chartx.length;
         yingW.push(parseInt(`${overlayO}`) % (Math.max(yingW.length, 6)));
         chartx = [2 ^ chartx.length];
      let chatf = weiboX >= 6205325.0;
      do {
          let photoY = true;
          let floating1: Array<any> = [607, 803];
          let bodane = 2;
         weiboX /= Math.max(1, 1);
         photoY = 68 < floating1.length;
         floating1 = [2];
         bodane /= Math.max(floating1.length % 2, 4);
         if (chatf) {
            break;
         }
      } while ((flyerJ) && chatf);
         flyerJ = weiboX > 37.86;
       let v_positiony = 3.0;
      if ((weiboX - moduleC) <= 3.46 || 1.37 <= (weiboX / (Math.max(3.46, 6)))) {
         weiboX += parseInt(`${v_positiony}`);
      }
      for (let j = 0; j < 3; j++) {
          let hoverA = String.fromCharCode(105,109,112,111,114,116,101,114,115,95,116,95,49,57,0);
          let change8 = 5;
          let moonr = String.fromCharCode(105,95,57,54,95,109,111,100,117,108,97,116,101,0);
          let popupI = String.fromCharCode(101,95,57,53,95,114,101,115,105,100,117,97,108,0);
          let acceptedJ = String.fromCharCode(98,103,114,97,95,115,95,56,48,0);
         moduleC += parseFloat(`${parseInt(`${moduleC}`)}`);
         hoverA = `${(hoverA == String.fromCharCode(107,0) ? hoverA.length : popupI.length)}`;
         change8 |= moonr.length | 3;
         moonr += `${moonr.length & popupI.length}`;
         acceptedJ += `${change8}`;
      }
         weiboX -= parseInt(`${v_positiony}`) ^ 1;
       let scheduley = String.fromCharCode(105,112,118,95,102,95,55,49,0);
       let d_unlockM = String.fromCharCode(98,105,103,95,119,95,53,49,0);
      pagination2 = [shrinkU >> (Math.min(4, Math.abs(3)))];
    setRefreshing(false);
  };

  const refreshUserState = async () => {
       let n_titled = String.fromCharCode(104,97,118,105,110,103,95,102,95,50,54,0);
    let orientationN = String.fromCharCode(122,95,53,51,95,100,101,116,101,114,109,105,110,101,0);
    let k_playerK = false;
    let turnD: Map<any, any> = new Map([[String.fromCharCode(112,99,97,99,104,101,95,56,95,50,48,0),true ], [String.fromCharCode(107,95,55,95,101,109,117,108,97,116,101,100,0),true ], [String.fromCharCode(106,95,57,51,95,103,114,97,121,115,99,97,108,101,0),false ]]);
    let homee = String.fromCharCode(116,105,109,101,115,116,97,109,112,95,54,95,53,50,0);
    let overC: Map<any, any> = new Map([[String.fromCharCode(117,110,99,111,110,102,105,114,109,101,100,95,103,95,53,48,0),false ], [String.fromCharCode(109,95,53,95,115,108,105,100,101,114,0),true ]]);
    let headerl = 1.0;
    let connectionN = false;
   while ((headerl - 5.99) > 5.18) {
       let splashu = true;
       let xvodF: Map<any, any> = new Map([[String.fromCharCode(108,95,49,52,95,98,101,116,97,0),600], [String.fromCharCode(116,104,114,111,119,95,53,95,57,48,0),20]]);
       let feedbackY: Map<any, any> = new Map([[String.fromCharCode(121,95,49,57,95,98,97,114,114,97,121,0),822], [String.fromCharCode(100,95,49,50,95,116,114,97,110,115,113,117,97,110,116,0),482]]);
       let humidity5 = 5;
          let plusM = 0.0;
          let benefitb = String.fromCharCode(116,101,108,95,103,95,50,52,0);
         splashu = xvodF.size >= 41 && 46.21 >= plusM;
         plusM *= parseFloat(`${3}`);
         benefitb += `${benefitb.length}`;
         splashu = null == xvodF.get(`${splashu}`);
      while (3 == (feedbackY.size & 4) && (feedbackY.size & humidity5) == 4) {
         humidity5 ^= 2;
         break;
      }
       let benefitx = String.fromCharCode(117,112,100,97,116,97,98,108,101,95,106,95,55,48,0);
       let rankl = String.fromCharCode(122,95,56,57,95,116,105,109,105,110,103,0);
       let bottomL: Array<any> = [830, 723, 145];
       let connectiong: Array<any> = [552, 982];
          let backgroundw = 1.0;
          let bufferG = String.fromCharCode(108,95,54,55,95,112,101,114,115,105,115,116,101,110,99,101,0);
         feedbackY.set(`${backgroundw}`, parseInt(`${backgroundw}`));
         bufferG += `${bufferG.length - 2}`;
      let bufferH = humidity5 >= 8000182;
      do {
         humidity5 /= Math.max((rankl == String.fromCharCode(71,0) ? rankl.length : xvodF.size), 3);
         if (bufferH) {
            break;
         }
      } while ((2 > (bottomL.length % 1)) && bufferH);
      if (rankl.length > 2) {
         rankl = `${(xvodF.size - (splashu ? 1 : 5))}`;
      }
      if (4 >= (benefitx.length << (Math.min(2, Math.abs(humidity5)))) || 4 >= (benefitx.length << (Math.min(5, Math.abs(humidity5))))) {
         humidity5 += bottomL.length;
      }
         xvodF = new Map([[benefitx, humidity5]]);
      let assistJ = rankl.length <= 7702893;
      do {
         rankl = `${rankl.length}`;
         if (assistJ) {
            break;
         }
      } while (assistJ && ((rankl.length | feedbackY.size) == 1));
       let watchx: Array<any> = [231, 450, 316];
      headerl += (parseFloat(`${orientationN == String.fromCharCode(99,0) ? overC.size : orientationN.length}`));
      break;
   }
   if (!homee.endsWith(`${overC.size}`)) {
       let whistle9 = String.fromCharCode(105,95,52,50,95,115,116,117,102,102,115,116,0);
       let gestureP = 3.0;
       let penalty9: Array<any> = [595, 431, 665];
       let description_x6 = String.fromCharCode(103,95,52,50,0);
       let dangerV: Array<any> = [448, 551, 547];
      while (description_x6 != String.fromCharCode(68,0)) {
          let thailandX = 5;
         whistle9 = `${thailandX + 1}`;
         break;
      }
      let constantsq = penalty9.length >= 6684505;
      do {
         penalty9.push(whistle9.length);
         if (constantsq) {
            break;
         }
      } while (constantsq && (1 >= (penalty9.length * 4)));
      for (let b = 0; b < 2; b++) {
         description_x6 = "2";
      }
      let commonR = 6694473 >= dangerV.length;
      do {
         dangerV.push(2 & dangerV.length);
         if (commonR) {
            break;
         }
      } while (((description_x6.length - dangerV.length) <= 4) && commonR);
          let found2 = String.fromCharCode(113,95,57,57,95,100,101,115,116,0);
          let j_playerI = 5.0;
         penalty9.push((String.fromCharCode(122,0) == whistle9 ? dangerV.length : whistle9.length));
         found2 = `${parseInt(`${j_playerI}`)}`;
         j_playerI -= parseFloat(`${found2.length}`);
       let gesture9: Array<any> = [172, 627];
       let fastX: Array<any> = [848, 768];
       let appsu = 4.0;
       let stationsR = 4.0;
          let spec3: Map<any, any> = new Map([[String.fromCharCode(117,110,105,102,111,114,109,115,95,56,95,57,0),451], [String.fromCharCode(120,95,52,50,95,115,117,98,99,108,97,115,115,101,115,0),69], [String.fromCharCode(104,101,114,109,105,116,101,95,114,95,56,49,0),691]]);
          let tailb = String.fromCharCode(116,116,97,99,104,109,101,110,116,95,55,95,54,55,0);
         gestureP += parseFloat(`${dangerV.length}`);
         spec3 = new Map([[`${spec3.size}`, (tailb == String.fromCharCode(79,0) ? tailb.length : spec3.size)]]);
         gesture9.push(penalty9.length);
      while (4.100 == (gestureP / 1.58) || 3 == (dangerV.length << (Math.min(Math.abs(5), 1)))) {
          let share0 = String.fromCharCode(115,104,97,114,101,100,107,101,121,95,122,95,54,49,0);
          let heart9: Array<any> = [596, 521];
          let navigationW = 2.0;
          let transferj = true;
         gestureP += parseFloat(`${1}`);
         share0 += `${1 << (Math.min(Math.abs(parseInt(`${navigationW}`)), 2))}`;
         heart9 = [((transferj ? 3 : 5) & parseInt(`${navigationW}`))];
         transferj = !transferj;
         break;
      }
      if (1 <= (fastX.length << (Math.min(penalty9.length, 4))) && (fastX.length << (Math.min(penalty9.length, 5))) <= 1) {
         fastX.push(dangerV.length);
      }
       let loginP = String.fromCharCode(99,111,110,110,101,99,116,105,118,105,116,121,95,55,95,57,0);
      if (4.68 > (3 - stationsR)) {
         gestureP -= parseFloat(`${parseInt(`${appsu}`)}`);
      }
      if (5 <= (loginP.length & 2) || (2 & loginP.length) <= 5) {
         loginP = `${loginP.length ^ parseInt(`${stationsR}`)}`;
      }
          let time_nuQ: Array<any> = [String.fromCharCode(114,95,57,57,95,118,101,110,100,111,114,0), String.fromCharCode(110,95,54,55,95,112,97,110,101,0), String.fromCharCode(121,95,54,95,108,111,99,97,116,105,111,110,115,0)];
          let t_centerR = 1.0;
          let ying8 = String.fromCharCode(98,100,115,95,104,95,56,49,0);
         dangerV.push(parseInt(`${stationsR}`) ^ parseInt(`${appsu}`));
         time_nuQ = [2 << (Math.min(4, Math.abs(parseInt(`${t_centerR}`))))];
         t_centerR *= (parseFloat(`${String.fromCharCode(120,0) == ying8 ? ying8.length : parseInt(`${t_centerR}`)}`));
      overC.set(`${description_x6}`, 2 + overC.size);
   }
   if (k_playerK) {
       let turnl = String.fromCharCode(106,95,52,51,95,114,101,97,100,111,110,108,121,0);
       let sheetY = String.fromCharCode(120,95,51,95,98,97,111,98,97,98,0);
      if (turnl.includes(`${sheetY.length}`)) {
         turnl = `${(String.fromCharCode(49,0) == turnl ? turnl.length : sheetY.length)}`;
      }
         turnl = `${(String.fromCharCode(121,0) == turnl ? turnl.length : sheetY.length)}`;
         sheetY = `${(String.fromCharCode(68,0) == turnl ? turnl.length : sheetY.length)}`;
      let loginA = sheetY.length >= 5810378;
      do {
          let trashI = 1;
         sheetY += `${(sheetY == String.fromCharCode(73,0) ? trashI : sheetY.length)}`;
         if (loginA) {
            break;
         }
      } while ((!turnl.startsWith(sheetY)) && loginA);
      if (turnl == String.fromCharCode(90,0)) {
         sheetY = `${sheetY.length << (Math.min(Math.abs(2), 1))}`;
      }
         sheetY += `${3 & sheetY.length}`;
      k_playerK = !connectionN;
   }
      turnD = new Map([[`${connectionN}`, ((connectionN ? 1 : 4))]]);

    const result = await THFirebase.getUserDetails();

   let dangerT = String.fromCharCode(116,95,103,51,122,99,50,55,104,51,0) == n_titled;
   do {
      n_titled += "3";
      if (dangerT) {
         break;
      }
   } while ((turnD.size >= 1) && dangerT);
   if (orientationN.length == overC.size) {
       let commentC = String.fromCharCode(111,112,116,105,109,105,122,101,95,107,95,50,54,0);
       let tooltips7 = String.fromCharCode(100,101,99,114,121,112,116,105,111,110,95,100,95,56,0);
         commentC += `${(tooltips7 == String.fromCharCode(82,0) ? commentC.length : tooltips7.length)}`;
      for (let i = 0; i < 1; i++) {
          let dragc = true;
          let unreada = String.fromCharCode(112,95,51,51,95,114,101,97,108,0);
          let unreadf: Map<any, any> = new Map([[String.fromCharCode(109,111,111,118,95,107,95,55,51,0),String.fromCharCode(114,95,57,56,95,116,105,109,101,108,105,110,101,0)], [String.fromCharCode(111,103,103,101,114,95,99,95,54,57,0),String.fromCharCode(97,115,121,109,95,99,95,53,49,0)], [String.fromCharCode(99,108,101,97,114,95,54,95,56,56,0),String.fromCharCode(116,115,99,99,100,97,116,97,95,100,95,50,54,0)]]);
         tooltips7 += `${tooltips7.length * commentC.length}`;
         dragc = unreadf.size >= 62 || dragc;
         unreada = `${((dragc ? 2 : 2) >> (Math.min(Math.abs(unreadf.size), 2)))}`;
      }
         commentC = `${(commentC == String.fromCharCode(67,0) ? commentC.length : tooltips7.length)}`;
      if (commentC.length > tooltips7.length) {
         tooltips7 += `${commentC.length % (Math.max(2, 10))}`;
      }
       let recommendationP = String.fromCharCode(97,95,55,50,95,112,114,101,115,115,0);
      while (commentC == String.fromCharCode(56,0) || 1 > tooltips7.length) {
         tooltips7 = `${(commentC == String.fromCharCode(113,0) ? commentC.length : tooltips7.length)}`;
         break;
      }
      overC.set(`${orientationN}`, turnD.size);
   }
       let catagory4 = String.fromCharCode(97,95,51,49,95,115,101,116,119,97,116,101,114,109,97,114,107,0);
       let previewC: Array<any> = [313, 53, 470];
       let episodesS = 2.0;
      if ((catagory4.length * 4) >= 4 && 4 >= (4 * previewC.length)) {
         previewC = [catagory4.length << (Math.min(2, previewC.length))];
      }
      for (let s = 0; s < 1; s++) {
         previewC.push(previewC.length);
      }
      for (let n = 0; n < 2; n++) {
          let temperaturep = 2.0;
          let hejiq: Map<any, any> = new Map([[String.fromCharCode(105,95,49,55,95,112,114,111,98,101,114,0),542], [String.fromCharCode(99,111,109,112,117,116,101,100,95,48,95,53,54,0),835], [String.fromCharCode(116,97,114,103,101,116,95,116,95,49,0),267]]);
          let lightK = 5;
          let q_viewV = String.fromCharCode(99,105,114,99,108,101,100,95,103,95,57,55,0);
          let infow = 0.0;
         episodesS /= Math.max(4, parseFloat(`${3}`));
         temperaturep += parseFloat(`${parseInt(`${temperaturep}`) << (Math.min(5, Math.abs(lightK)))}`);
         hejiq = new Map([[`${infow}`, 3 & lightK]]);
         q_viewV += `${2 & parseInt(`${infow}`)}`;
      }
         catagory4 += `${parseInt(`${episodesS}`) ^ catagory4.length}`;
       let adultI = String.fromCharCode(105,100,108,101,95,57,95,52,57,0);
       let guideY = String.fromCharCode(99,111,110,115,116,97,110,116,95,103,95,55,57,0);
          let greyr: Map<any, any> = new Map([[String.fromCharCode(114,95,49,48,48,95,112,108,97,121,0),216], [String.fromCharCode(115,95,57,50,95,115,109,111,111,116,104,101,110,0),972], [String.fromCharCode(103,114,111,117,112,105,110,103,95,48,95,49,54,0),584]]);
          let plashz = 4.0;
         guideY += `${(String.fromCharCode(74,0) == guideY ? greyr.size : guideY.length)}`;
         greyr.set(`${plashz}`, 1 | parseInt(`${plashz}`));
         previewC = [1 | previewC.length];
          let internet5: Map<any, any> = new Map([[String.fromCharCode(107,95,50,57,95,115,97,102,101,116,121,0),91], [String.fromCharCode(120,99,104,103,95,98,95,56,49,0),885]]);
          let readp = String.fromCharCode(115,95,57,49,95,108,115,112,114,0);
         catagory4 = `${internet5.size / (Math.max(1, 4))}`;
         internet5.set(readp, readp.length);
      if (parseFloat(`${previewC.length}`) < episodesS) {
         previewC.push((String.fromCharCode(97,0) == guideY ? previewC.length : guideY.length));
      }
      headerl *= (parseFloat(`${(k_playerK ? 1 : 1) + catagory4.length}`));
       let settingk: Array<any> = [String.fromCharCode(120,95,57,50,95,100,105,115,99,97,114,100,101,100,0), String.fromCharCode(102,95,49,55,95,101,120,112,101,114,116,0)];
       let combinedr = String.fromCharCode(118,99,111,109,98,105,110,101,95,110,95,51,52,0);
         settingk = [settingk.length ^ 3];
      while (combinedr.length < 5) {
         settingk.push(settingk.length);
         break;
      }
      let expand_ = settingk.length >= 9563819;
      do {
          let readJ = String.fromCharCode(105,95,53,52,95,97,116,116,114,97,99,116,0);
          let progressm = 5;
          let i_positionC = false;
          let area3 = String.fromCharCode(99,95,54,55,95,111,108,100,0);
         settingk = [settingk.length % 1];
         readJ = `${(String.fromCharCode(84,0) == readJ ? (i_positionC ? 4 : 4) : readJ.length)}`;
         progressm ^= 2 + progressm;
         i_positionC = !i_positionC;
         area3 = `${area3.length ^ readJ.length}`;
         if (expand_) {
            break;
         }
      } while ((4 <= (combinedr.length ^ 4)) && expand_);
       let volumeH = String.fromCharCode(104,95,50,53,95,114,101,113,117,115,116,101,114,0);
       let formr = String.fromCharCode(107,97,108,109,97,110,95,52,95,53,53,0);
       let profile9 = 2.0;
      let darkd = combinedr.length >= 9551484;
      do {
         combinedr = `${(String.fromCharCode(73,0) == volumeH ? parseInt(`${profile9}`) : volumeH.length)}`;
         if (darkd) {
            break;
         }
      } while (((combinedr.length - 1) == 3 && (combinedr.length - settingk.length) == 1) && darkd);
      n_titled = "1";
    if (result == null) {

      k_playerK = String.fromCharCode(75,0) == orientationN;
   while (headerl >= 1.15) {
       let refreshT = true;
       let ballp: Array<any> = [905, 218];
       let launchf: Array<any> = [645, 236, 151];
         ballp.push(((refreshT ? 3 : 3) / 1));
         ballp.push(ballp.length << (Math.min(Math.abs(1), 3)));
         ballp = [ballp.length];
          let action8 = String.fromCharCode(115,95,55,54,95,99,104,97,114,0);
          let shirtU = 3;
          let spec2 = 2;
         refreshT = 32 <= shirtU;
         action8 = `${spec2 >> (Math.min(action8.length, 5))}`;
         shirtU >>= Math.min(Math.abs((String.fromCharCode(108,0) == action8 ? action8.length : spec2)), 2);
      for (let l = 0; l < 3; l++) {
         launchf = [ballp.length];
      }
      if (5 == launchf.length || 2 == (launchf.length & 5)) {
         launchf = [launchf.length % 1];
      }
          let zhengpianS: Array<any> = [219, 140];
         launchf.push((2 - (refreshT ? 1 : 5)));
         zhengpianS.push(zhengpianS.length + 2);
      for (let n = 0; n < 1; n++) {
         launchf = [(2 / (Math.max(7, (refreshT ? 5 : 2))))];
      }
      let langkeyg = 9182822 >= launchf.length;
      do {
          let upgradeN = String.fromCharCode(106,95,55,54,95,99,108,97,115,115,0);
          let model1 = String.fromCharCode(102,95,55,53,95,101,120,97,110,100,101,100,0);
          let containern = String.fromCharCode(109,97,110,105,102,101,115,116,95,107,95,51,51,0);
         launchf.push(2);
         upgradeN = `${upgradeN.length}`;
         model1 = `${containern.length}`;
         containern += `${containern.length}`;
         if (langkeyg) {
            break;
         }
      } while (langkeyg && (1 <= (ballp.length * 1) || 4 <= (1 * launchf.length)));
      headerl -= (parseFloat(`${launchf.length << (Math.min(2, Math.abs((refreshT ? 3 : 4))))}`));
      break;
   }
   for (let o = 0; o < 3; o++) {
      orientationN += `${((k_playerK ? 3 : 2) >> (Math.min(Math.abs(2), 4)))}`;
   }
   while (overC.size < 5) {
      connectionN = String.fromCharCode(79,0) == orientationN;
      break;
   }
      return;
    }

    await dispatch(updateUserAuth(result));

       let playV: Map<any, any> = new Map([[String.fromCharCode(109,95,53,57,95,117,110,101,109,98,101,100,0),326], [String.fromCharCode(98,114,101,103,95,50,95,51,53,0),721]]);
      for (let o = 0; o < 1; o++) {
          let basketballg: Map<any, any> = new Map([[String.fromCharCode(110,111,110,113,117,111,116,101,100,95,99,95,53,57,0),54], [String.fromCharCode(116,114,105,112,108,101,95,49,95,55,57,0),297]]);
          let annerS = String.fromCharCode(118,97,114,105,110,116,115,95,52,95,52,50,0);
          let rightT: Map<any, any> = new Map([[String.fromCharCode(118,95,52,56,95,115,111,102,116,119,97,114,101,0),String.fromCharCode(107,95,55,53,95,101,120,112,108,105,99,105,116,108,121,0)], [String.fromCharCode(98,101,110,105,103,110,95,103,95,54,49,0),String.fromCharCode(101,95,57,55,95,112,101,101,114,99,111,110,110,101,99,116,105,111,110,105,110,116,101,114,102,97,99,101,0)], [String.fromCharCode(117,115,101,114,115,112,97,99,101,95,109,95,55,56,0),String.fromCharCode(120,120,104,97,115,104,95,53,95,54,56,0)]]);
         playV = new Map([[`${rightT.size}`, rightT.size]]);
         basketballg.set(`${annerS}`, 3 ^ basketballg.size);
         annerS += "2";
      }
         playV.set(`${playV.size}`, playV.size);
         playV = new Map([[`${playV.size}`, playV.size]]);
      n_titled = `${((k_playerK ? 4 : 5) >> (Math.min(Math.abs(parseInt(`${headerl}`)), 2)))}`;
      k_playerK = !k_playerK;
      orientationN += `${turnD.size}`;
   let modalV = String.fromCharCode(97,109,115,99,112,114,0) == homee;
   do {
      homee = `${(homee == String.fromCharCode(68,0) ? homee.length : parseInt(`${headerl}`))}`;
      if (modalV) {
         break;
      }
   } while ((2 == (overC.size + 4)) && modalV);
    return;
  };

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title="邀请好友奖励" />
      <ScrollView
        refreshControl={
          <RefreshControl refreshing={refreshing} onRefresh={handleRefresh} tintColor="#FAC33D" />
        }
        showsVerticalScrollIndicator={false}
      >
        {/* <TitleWithBackButtonHeader title="邀请好友奖励" /> */}
        <View style={{ paddingHorizontal: 0 }}>
          { }
          <InviteHeader />
          { }
          <InviteCard userState={userState.user!} />
          { }

          <TouchableOpacity
            onPress={() => {
              navigation.navigate("活动规则");
            }}
          >
            <View
              style={{
                flex: 1,
                justifyContent: "center",
                alignItems: "center",
                paddingVertical: 10,
              }}
            >
              <Text
                style={{
                  fontSize: 12,
                  textDecorationLine: "underline",
                  color: "#ffffff",
                }}
              >
                活动规则
              </Text>
            </View>
          </TouchableOpacity>
          { }
        </View>
        <InviteStep />
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({});
