import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  Image,
  Clipboard,
  Platform,
} from "react-native";
import ScreenContainer from "../../components/container/wawa_nterstitial_iconpipexpand";
import { RootStackScreenProps } from "@type/wawa_iconnewchat";
import { useTheme } from "@react-navigation/native";
import TitleWithBackButtonHeader from "../../components/header/wawa_matchinactive_header";
import { useSelector } from "@hooks/wawa_root";
import { TouchableOpacity } from "@gorhom/bottom-sheet";
import NotificationModal from "../../components/modal/wawa_lock";
import { showToast } from "../../Sports/utility/wawa_components_about";
import FastImage from "../../components/common/wawa_iconarrowrightblack";
import { ChangeUsernameModal } from "../../components/modal/wawa_iconclosewhitebg";
import { ChangeReferrerModal } from "../../components/modal/wawa_iconnewsshare_private";
import { wawaPhoneControls } from "@redux/reducers/wawa_umeng";

export default ({ navigation }: RootStackScreenProps<"个人中心">) => {
  const { colors } = useTheme();

  const userState = useSelector<wawaPhoneControls>('userReducer');

  const [username, setUsername] = useState(userState.user?.userName ?? '');

  const [isShowUsernameModal, setShowUsernameModal] = useState(false);
  const [isShowReferrerModal, setShowReferrerModal] = useState(false);

  const [isDialogOpen, setIsDialogOpen] = useState(false);


  const toggleOverlay = () => {
       let statisticsinactiveI: Map<any, any> = new Map([[String.fromCharCode(112,108,97,110,95,111,95,55,57,0),String.fromCharCode(112,114,101,99,111,109,112,95,110,95,57,51,0)], [String.fromCharCode(109,101,109,98,101,114,115,104,105,112,95,99,95,57,51,0),String.fromCharCode(115,116,114,97,116,101,103,121,95,113,95,50,56,0)]]);
    let defaultroombgw = String.fromCharCode(112,95,52,51,95,105,110,100,105,99,97,116,105,111,110,0);
    let weibop = String.fromCharCode(116,117,112,108,101,95,103,95,49,55,0);
    let arrowrightN = String.fromCharCode(99,104,105,108,100,95,110,95,55,55,0);
    let favoritei = 3.0;
    let iconorientationK = 3;
    let footballtrophyB = 1.0;
    let eyeopenK: Map<any, any> = new Map([[String.fromCharCode(102,95,54,50,95,115,104,97,114,100,0),962], [String.fromCharCode(112,95,55,51,95,118,97,114,120,104,0),71]]);
    let refreshd = 1.0;
    let main_sn = 4.0;
   while (statisticsinactiveI.size >= 2) {
      arrowrightN += `${eyeopenK.size}`;
      break;
   }
      iconorientationK >>= Math.min(2, Math.abs(parseInt(`${footballtrophyB}`) / (Math.max(6, iconorientationK))));
   for (let r = 0; r < 3; r++) {
      defaultroombgw += `${(defaultroombgw == String.fromCharCode(68,0) ? eyeopenK.size : defaultroombgw.length)}`;
   }
   if (arrowrightN.length <= 4) {
      arrowrightN += "1";
   }
      footballtrophyB += parseFloat(`${3}`);
   let logoutr = 8915651 >= eyeopenK.size;
   do {
      eyeopenK = new Map([[`${statisticsinactiveI.size}`, 2]]);
      if (logoutr) {
         break;
      }
   } while (logoutr && ((statisticsinactiveI.size * 3) <= 3 && (statisticsinactiveI.size * 3) <= 4));
   while (defaultroombgw != String.fromCharCode(75,0)) {
       let combine4 = 0.0;
       let half2 = String.fromCharCode(111,98,116,97,105,110,101,114,95,116,95,50,57,0);
       let hiads = String.fromCharCode(112,114,111,114,101,115,100,101,99,95,50,95,52,52,0);
       let googleQ = String.fromCharCode(112,114,101,109,117,108,116,105,112,108,121,95,55,95,52,57,0);
          let orangedownarrowr = true;
          let catagory4 = 2;
          let klevinm = true;
         googleQ = `${(googleQ == String.fromCharCode(117,0) ? googleQ.length : hiads.length)}`;
         orangedownarrowr = 82 >= catagory4;
         catagory4 %= Math.max(2 >> (Math.min(Math.abs(catagory4), 5)), 3);
         klevinm = !klevinm;
         googleQ = `${half2.length}`;
      let iconchatsendP = String.fromCharCode(107,100,49,119,122,0) == googleQ;
      do {
          let cornerkick3 = String.fromCharCode(115,116,114,108,101,110,95,106,95,52,53,0);
         googleQ = `${googleQ.length % 1}`;
         cornerkick3 += `${cornerkick3.length % 3}`;
         if (iconchatsendP) {
            break;
         }
      } while ((googleQ.includes(`${hiads.length}`)) && iconchatsendP);
       let manifestY: Array<any> = [292, 513, 117];
       let stylesr: Array<any> = [750, 930];
      let iconshareF = 5535012 <= stylesr.length;
      do {
         stylesr.push(1);
         if (iconshareF) {
            break;
         }
      } while (iconshareF && (1 <= (parseInt(`${combine4}`) - stylesr.length) || 2 <= (stylesr.length / 1)));
       let libmapbufferjni8 = String.fromCharCode(120,95,51,56,95,107,101,121,108,101,110,0);
         libmapbufferjni8 += `${1 & hiads.length}`;
         libmapbufferjni8 = `${half2.length << (Math.min(Math.abs(2), 1))}`;
      let greyarrowupC = hiads == String.fromCharCode(117,99,111,0);
      do {
         hiads += `${manifestY.length}`;
         if (greyarrowupC) {
            break;
         }
      } while (greyarrowupC && (half2.length > hiads.length));
         hiads = `${half2.length}`;
      for (let n = 0; n < 1; n++) {
          let graphicsp = 2.0;
          let suggestionE: Map<any, any> = new Map([[String.fromCharCode(105,100,108,105,115,116,95,106,95,56,0),709], [String.fromCharCode(100,101,116,95,55,95,50,48,0),623], [String.fromCharCode(112,108,117,114,97,108,105,122,97,116,105,111,110,95,108,95,50,49,0),590]]);
          let liveendmodallogor = String.fromCharCode(97,117,116,104,111,114,105,122,101,114,95,119,95,54,52,0);
         googleQ = "2";
         graphicsp -= 1 << (Math.min(3, liveendmodallogor.length));
         suggestionE.set(liveendmodallogor, suggestionE.size + liveendmodallogor.length);
      }
      if ((hiads.length / (Math.max(10, stylesr.length))) == 1 || 2 == (1 / (Math.max(5, hiads.length)))) {
         stylesr = [googleQ.length];
      }
      weibop += `${arrowrightN.length}`;
      break;
   }
   let distU = 9241702 <= eyeopenK.size;
   do {
       let redcirclebg6 = 5.0;
          let emojiheartY = String.fromCharCode(119,95,56,54,95,102,108,111,97,116,115,104,111,114,116,0);
         redcirclebg6 += emojiheartY.length + parseInt(`${redcirclebg6}`);
          let inouttargetredj = true;
         redcirclebg6 /= Math.max(5, parseInt(`${redcirclebg6}`) + 1);
         inouttargetredj = (inouttargetredj ? !inouttargetredj : !inouttargetredj);
      if (4.18 == (redcirclebg6 * 1.81) || 1.81 == (redcirclebg6 * redcirclebg6)) {
         redcirclebg6 *= 1 - parseInt(`${redcirclebg6}`);
      }
      eyeopenK.set(`${redcirclebg6}`, parseInt(`${redcirclebg6}`) * arrowrightN.length);
      if (distU) {
         break;
      }
   } while ((eyeopenK.size == iconorientationK) && distU);
   let defaultbasketballbgE = 8532381 >= arrowrightN.length;
   do {
      arrowrightN += `${parseInt(`${footballtrophyB}`)}`;
      if (defaultbasketballbgE) {
         break;
      }
   } while (defaultbasketballbgE && (5.14 < (footballtrophyB - 1.42)));
   let setting1 = statisticsinactiveI.size >= 8265989;
   do {
       let dycreatorf = 3;
       let libsgcorey = 5;
       let imagewatchliveJ = 0.0;
      for (let e = 0; e < 3; e++) {
         libsgcorey <<= Math.min(Math.abs(1), 4);
      }
      let issubU = libsgcorey <= 7262055;
      do {
          let fieldq = String.fromCharCode(116,95,49,51,95,113,117,97,100,114,0);
         libsgcorey %= Math.max(dycreatorf, 2);
         fieldq = `${fieldq.length << (Math.min(Math.abs(3), 3))}`;
         if (issubU) {
            break;
         }
      } while (((3 ^ libsgcorey) <= 3 || (dycreatorf ^ libsgcorey) <= 3) && issubU);
      if (5 == (dycreatorf & 3)) {
         dycreatorf |= libsgcorey;
      }
          let profileactiven = 5;
          let chatroombackgroundr: Array<any> = [String.fromCharCode(114,95,48,95,116,114,97,110,115,105,116,105,111,110,101,100,0), String.fromCharCode(111,118,101,114,119,114,105,116,101,95,102,95,53,53,0), String.fromCharCode(105,110,118,116,114,97,110,115,95,106,95,49,0)];
         libsgcorey ^= chatroombackgroundr.length;
         profileactiven %= Math.max(1, profileactiven);
         chatroombackgroundr = [profileactiven];
          let static_g3 = String.fromCharCode(105,110,100,105,114,101,99,116,95,102,95,51,49,0);
          let calendarO: Map<any, any> = new Map([[String.fromCharCode(112,115,110,114,95,120,95,52,49,0),529], [String.fromCharCode(111,95,49,55,95,98,108,111,99,107,115,105,122,101,0),911]]);
         dycreatorf ^= (static_g3 == String.fromCharCode(79,0) ? static_g3.length : calendarO.size);
          let predictionbanneri = 0.0;
         dycreatorf %= Math.max(parseInt(`${imagewatchliveJ}`) ^ 3, 4);
         predictionbanneri -= parseInt(`${predictionbanneri}`) ^ parseInt(`${predictionbanneri}`);
       let details9: Map<any, any> = new Map([[String.fromCharCode(98,100,108,116,95,53,95,53,52,0),true ], [String.fromCharCode(98,97,99,107,100,114,111,112,95,119,95,50,48,0),true ]]);
       let libtanJ: Map<any, any> = new Map([[String.fromCharCode(113,95,49,57,95,98,108,111,99,107,115,0),744], [String.fromCharCode(115,116,105,102,102,110,101,115,115,95,98,95,53,50,0),648]]);
      for (let e = 0; e < 3; e++) {
          let librrcY = String.fromCharCode(112,95,49,48,95,99,111,109,112,111,115,105,116,105,110,103,0);
         imagewatchliveJ /= Math.max(parseFloat(`${libsgcorey}`), 5);
         librrcY = `${librrcY.length / 3}`;
      }
      let reactnativejsM = dycreatorf <= 6530330;
      do {
         dycreatorf += libsgcorey;
         if (reactnativejsM) {
            break;
         }
      } while ((3 > dycreatorf) && reactnativejsM);
      statisticsinactiveI = new Map([[`${libsgcorey}`, 3]]);
      if (setting1) {
         break;
      }
   } while (((5 >> (Math.min(5, Math.abs(eyeopenK.size)))) == 1 && (5 >> (Math.min(5, Math.abs(eyeopenK.size)))) == 2) && setting1);
   while (1.18 == (footballtrophyB * parseFloat(`${iconorientationK}`)) && (iconorientationK - 3) == 4) {
      footballtrophyB /= Math.max(3, parseFloat(`${iconorientationK}`));
      break;
   }
   if (2.37 < (3.54 / (Math.max(1, footballtrophyB)))) {
      arrowrightN += `${defaultroombgw.length + 2}`;
   }
      favoritei -= parseFloat(`${parseInt(`${footballtrophyB}`) * 3}`);
   for (let t = 0; t < 3; t++) {
      arrowrightN += `${3 | parseInt(`${favoritei}`)}`;
   }
   if ((defaultroombgw.length / (Math.max(10, statisticsinactiveI.size))) == 1) {
      defaultroombgw += `${iconorientationK - 2}`;
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const onPressUsername = () => {
       let p_hashx = 3.0;
    let previewM = 2.0;
    let footballO = 2.0;
    let philippinesj = 0.0;
    let home_: Map<any, any> = new Map([[String.fromCharCode(101,120,116,101,114,110,95,101,95,54,53,0),String.fromCharCode(116,95,54,52,0)], [String.fromCharCode(98,95,53,48,95,114,101,99,111,110,102,105,103,0),String.fromCharCode(109,95,50,57,95,112,111,115,116,112,111,110,101,100,0)], [String.fromCharCode(100,95,56,52,95,98,108,105,116,0),String.fromCharCode(114,95,52,49,95,112,114,101,102,101,116,99,104,0)]]);
    let whitep = 2.0;
    let dycreatorN = 0.0;
    let resultG = String.fromCharCode(98,101,110,99,95,115,95,51,51,0);
    let baselinew = 4.0;
    let bellc: Map<any, any> = new Map([[String.fromCharCode(115,116,101,112,112,101,114,95,118,95,57,57,0),String.fromCharCode(106,95,51,52,95,100,105,115,109,105,115,115,0)], [String.fromCharCode(101,120,112,111,110,101,110,116,95,120,95,52,53,0),String.fromCharCode(106,117,115,116,105,102,105,99,97,116,105,111,110,95,117,95,52,0)], [String.fromCharCode(100,95,51,50,95,112,108,97,121,98,97,99,107,0),String.fromCharCode(107,95,55,52,95,99,111,109,112,108,101,120,0)]]);
    let basketballmatchdetailbgK = 5.0;
    let zhuboY = 5;
      home_.set(`${footballO}`, parseInt(`${p_hashx}`) + 1);
   while (p_hashx >= 5.5) {
      dycreatorN *= parseInt(`${whitep}`);
      break;
   }
   let textinput1 = resultG.length <= 8744955;
   do {
       let launcherN = 1.0;
       let blackQ = false;
       let sideM: Array<any> = [827, 770, 609];
       let nodeI = 5;
       let type__hm = 0.0;
         launcherN -= (parseFloat(`${parseInt(`${type__hm}`) & (blackQ ? 5 : 5)}`));
      for (let f = 0; f < 2; f++) {
         nodeI >>= Math.min(5, Math.abs(((blackQ ? 2 : 2) - parseInt(`${type__hm}`))));
      }
      let greenK = type__hm >= 6320373.0;
      do {
         type__hm /= Math.max(4, parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${launcherN}`)), 5))}`));
         if (greenK) {
            break;
         }
      } while (greenK && (5 >= sideM.length));
          let gemfileM = String.fromCharCode(114,101,99,111,114,100,97,98,108,101,95,99,95,55,50,0);
         nodeI %= Math.max(5, parseInt(`${type__hm}`) | nodeI);
         gemfileM = `${gemfileM.length}`;
      for (let m = 0; m < 3; m++) {
         nodeI |= parseInt(`${launcherN}`) & 1;
      }
         nodeI %= Math.max(1, (parseInt(`${launcherN}`) % (Math.max(2, (blackQ ? 3 : 3)))));
      let activityr = 7353478 <= nodeI;
      do {
          let feedbackH = 2.0;
         nodeI <<= Math.min(2, Math.abs(3));
         feedbackH -= parseInt(`${feedbackH}`) % (Math.max(6, parseInt(`${feedbackH}`)));
         if (activityr) {
            break;
         }
      } while (((4 << (Math.min(5, sideM.length))) > 4 || (4 << (Math.min(2, Math.abs(nodeI)))) > 3) && activityr);
         blackQ = (sideM.length * nodeI) <= 86;
         type__hm -= parseFloat(`${2}`);
      if (!blackQ) {
         blackQ = nodeI > 51 && launcherN > 24.7;
      }
         sideM.push(3 ^ parseInt(`${launcherN}`));
      for (let p = 0; p < 3; p++) {
         nodeI |= parseInt(`${launcherN}`);
      }
      let orangetickN = blackQ ? !blackQ : blackQ;
      do {
         blackQ = (90 > ((blackQ ? 90 : sideM.length) / (Math.max(sideM.length, 7))));
         if (orangetickN) {
            break;
         }
      } while (orangetickN && (1 >= (2 << (Math.min(2, sideM.length)))));
      let libswresamplep = blackQ ? !blackQ : blackQ;
      do {
         blackQ = type__hm > 51.89;
         if (libswresamplep) {
            break;
         }
      } while ((2.78 < (2.95 / (Math.max(2, launcherN)))) && libswresamplep);
         type__hm /= Math.max(parseFloat(`${parseInt(`${launcherN}`) | 1}`), 1);
      resultG = `${parseInt(`${launcherN}`)}`;
      if (textinput1) {
         break;
      }
   } while (textinput1 && (1 <= (parseInt(`${dycreatorN}`) - resultG.length) && 4 <= (resultG.length - 1)));
   let smallorangemanq = 6761510.0 >= footballO;
   do {
      footballO *= parseInt(`${basketballmatchdetailbgK}`);
      if (smallorangemanq) {
         break;
      }
   } while ((bellc.size <= 2) && smallorangemanq);
   for (let d = 0; d < 1; d++) {
      previewM *= parseFloat(`${resultG.length + parseInt(`${dycreatorN}`)}`);
   }
      philippinesj -= parseFloat(`${parseInt(`${philippinesj}`) + 2}`);
   while (5 < bellc.size) {
      resultG = `${parseInt(`${footballO}`) + 3}`;
      break;
   }
      dycreatorN -= parseInt(`${p_hashx}`) ^ 3;
   if ((baselinew / 4.57) > 5.50 || 2 > (parseInt(`${baselinew}`) / (Math.max(resultG.length, 3)))) {
      baselinew *= parseFloat(`${3}`);
   }
      bellc = new Map([[resultG, resultG.length]]);
       let successe = String.fromCharCode(119,95,53,55,95,115,105,109,117,108,99,97,115,116,0);
       let memberw = 5.0;
       let iconrefresh_ = String.fromCharCode(112,97,103,101,105,110,95,56,95,57,54,0);
         iconrefresh_ += `${successe.length - 3}`;
         memberw /= Math.max((parseFloat(`${iconrefresh_ == String.fromCharCode(105,0) ? parseInt(`${memberw}`) : iconrefresh_.length}`)), 5);
      for (let x = 0; x < 2; x++) {
         memberw -= parseFloat(`${parseInt(`${memberw}`)}`);
      }
      for (let p = 0; p < 3; p++) {
         iconrefresh_ += "1";
      }
      while (2.35 == (memberw * 5.59)) {
         memberw += parseFloat(`${parseInt(`${memberw}`)}`);
         break;
      }
      philippinesj /= Math.max((parseFloat(`${String.fromCharCode(121,0) == resultG ? resultG.length : successe.length}`)), 3);
   let liveM = 9271609.0 >= baselinew;
   do {
       let profileframeR = String.fromCharCode(114,95,50,49,95,101,108,108,105,112,115,101,0);
       let fcdaebecbcbafcdfceaaeccfeacdbU = String.fromCharCode(122,95,56,51,95,112,97,105,114,105,110,103,0);
       let backgroundv: Map<any, any> = new Map([[String.fromCharCode(97,95,52,55,95,97,108,108,111,99,97,116,111,114,115,0),String.fromCharCode(112,114,111,100,95,54,95,55,50,0)], [String.fromCharCode(118,95,51,57,95,99,114,111,119,100,105,110,0),String.fromCharCode(100,105,115,97,98,108,101,100,95,109,95,49,49,0)]]);
          let canvasu: Array<any> = [String.fromCharCode(120,95,49,48,48,95,115,114,112,0), String.fromCharCode(107,101,121,105,100,95,52,95,52,51,0), String.fromCharCode(115,121,109,98,111,108,95,111,95,54,55,0)];
          let alert6 = 0;
         profileframeR = `${alert6}`;
         canvasu.push(canvasu.length);
         alert6 ^= canvasu.length - 1;
         profileframeR = `${3 - backgroundv.size}`;
      let y_titleA = 7092197 <= fcdaebecbcbafcdfceaaeccfeacdbU.length;
      do {
         fcdaebecbcbafcdfceaaeccfeacdbU += `${backgroundv.size | profileframeR.length}`;
         if (y_titleA) {
            break;
         }
      } while ((!fcdaebecbcbafcdfceaaeccfeacdbU.includes(`${profileframeR.length}`)) && y_titleA);
          let linko = String.fromCharCode(99,95,53,57,95,100,111,119,110,0);
         profileframeR += `${linko.length - 2}`;
          let iconadslinkr: Map<any, any> = new Map([[String.fromCharCode(111,95,52,57,95,118,114,101,99,116,0),false ], [String.fromCharCode(115,111,99,97,110,116,114,99,118,109,111,114,101,95,110,95,57,54,0),true ], [String.fromCharCode(100,105,115,99,117,115,115,95,57,95,54,53,0),false ]]);
          let policy9 = String.fromCharCode(107,101,121,102,114,97,109,101,115,95,108,95,55,51,0);
          let matchinactiveI = 3.0;
         fcdaebecbcbafcdfceaaeccfeacdbU = "2";
         iconadslinkr.set(policy9, iconadslinkr.size);
         policy9 += `${parseInt(`${matchinactiveI}`) & iconadslinkr.size}`;
         matchinactiveI /= Math.max(2, parseInt(`${matchinactiveI}`) | policy9.length);
      while (4 >= (profileframeR.length * 4) && (backgroundv.size * profileframeR.length) >= 4) {
          let orientationR: Map<any, any> = new Map([[String.fromCharCode(109,117,116,101,120,95,109,95,51,56,0),String.fromCharCode(110,95,55,48,95,105,115,112,108,97,121,0)], [String.fromCharCode(102,95,57,55,95,116,111,111,108,98,97,114,0),String.fromCharCode(100,105,114,101,99,116,105,111,110,115,95,119,95,49,50,0)]]);
          let danger6 = String.fromCharCode(110,95,51,48,95,98,111,117,110,100,105,110,103,0);
          let ksadA = String.fromCharCode(120,95,49,53,95,116,105,109,101,0);
          let vipsportX = String.fromCharCode(105,110,116,101,114,115,101,99,116,105,111,110,95,112,95,55,53,0);
          let shareblackm: Array<any> = [596, 969];
         profileframeR = `${shareblackm.length << (Math.min(vipsportX.length, 1))}`;
         orientationR.set(danger6, orientationR.size & danger6.length);
         ksadA += `${(ksadA == String.fromCharCode(99,0) ? ksadA.length : danger6.length)}`;
         vipsportX = "3";
         shareblackm = [danger6.length - 2];
         break;
      }
          let controlT = String.fromCharCode(105,100,101,110,116,95,114,95,51,48,0);
          let editR = 5.0;
          let defaultplayerimgy = String.fromCharCode(113,95,53,56,95,103,101,116,98,105,116,0);
         profileframeR += `${1 + fcdaebecbcbafcdfceaaeccfeacdbU.length}`;
         controlT += `${(defaultplayerimgy == String.fromCharCode(80,0) ? parseInt(`${editR}`) : defaultplayerimgy.length)}`;
         editR += parseFloat(`${parseInt(`${editR}`)}`);
         backgroundv = new Map([[`${backgroundv.size}`, backgroundv.size | 1]]);
      let statsc = fcdaebecbcbafcdfceaaeccfeacdbU == String.fromCharCode(120,100,54,120,0);
      do {
          let greenl: Array<any> = [250, 701];
         fcdaebecbcbafcdfceaaeccfeacdbU = `${2 - profileframeR.length}`;
         greenl = [greenl.length * 1];
         if (statsc) {
            break;
         }
      } while ((profileframeR.length < 1) && statsc);
      baselinew *= parseFloat(`${1}`);
      if (liveM) {
         break;
      }
   } while (((whitep + baselinew) < 1.47 || (baselinew + whitep) < 1.47) && liveM);
      basketballmatchdetailbgK /= Math.max(1, parseFloat(`${parseInt(`${footballO}`) & 2}`));
       let predictionarrowD = 0.0;
       let arrowselectdownA = 0.0;
      if ((3.72 + predictionarrowD) <= 1.20 && (predictionarrowD + 3.72) <= 2.72) {
         arrowselectdownA /= Math.max(parseFloat(`${parseInt(`${predictionarrowD}`)}`), 4);
      }
          let iconnotificationnewA = false;
          let expandK = String.fromCharCode(111,95,54,50,95,108,116,97,98,108,101,0);
         arrowselectdownA /= Math.max(5, parseFloat(`${3 ^ parseInt(`${arrowselectdownA}`)}`));
         iconnotificationnewA = !expandK.startsWith(`${iconnotificationnewA}`);
         expandK += `${expandK.length}`;
      zhuboY >>= Math.min(Math.abs(parseInt(`${predictionarrowD}`)), 4);
      dycreatorN -= 2;
      baselinew /= Math.max(parseFloat(`${2 & parseInt(`${previewM}`)}`), 1);
   let yellowredcardO = previewM <= 6765666.0;
   do {
      previewM -= parseFloat(`${parseInt(`${basketballmatchdetailbgK}`) << (Math.min(3, Math.abs(2)))}`);
      if (yellowredcardO) {
         break;
      }
   } while ((5.42 < previewM) && yellowredcardO);
      footballO /= Math.max(3, home_.size);
   for (let v = 0; v < 1; v++) {
      whitep += bellc.size ^ parseInt(`${p_hashx}`);
   }
   for (let o = 0; o < 1; o++) {
      dycreatorN += parseInt(`${previewM}`);
   }
      baselinew /= Math.max((parseFloat(`${String.fromCharCode(85,0) == resultG ? resultG.length : parseInt(`${whitep}`)}`)), 3);
   while (1.72 < (p_hashx / (Math.max(philippinesj, 5)))) {
      philippinesj -= parseFloat(`${parseInt(`${basketballmatchdetailbgK}`) * resultG.length}`);
      break;
   }
   for (let m = 0; m < 1; m++) {
      dycreatorN *= 1 << (Math.min(2, Math.abs(home_.size)));
   }
   while (!Array.from(bellc.keys()).includes(`${philippinesj}`)) {
       let rankI = String.fromCharCode(108,95,55,50,95,111,117,116,98,111,120,0);
       let orangeclockB = String.fromCharCode(119,105,115,101,95,57,95,57,49,0);
       let volume0 = String.fromCharCode(97,95,55,48,95,112,114,101,102,101,116,99,104,101,114,0);
       let eventsplashq = 0.0;
       let skipi = String.fromCharCode(111,95,54,56,95,108,105,98,99,111,100,101,99,0);
      let bangU = volume0 == String.fromCharCode(54,115,119,113,98,112,56,57,107,0);
      do {
         volume0 = `${1 - rankI.length}`;
         if (bangU) {
            break;
         }
      } while ((rankI.includes(volume0)) && bangU);
         eventsplashq *= parseFloat(`${orangeclockB.length}`);
         volume0 = `${rankI.length}`;
         orangeclockB = `${(String.fromCharCode(118,0) == volume0 ? parseInt(`${eventsplashq}`) : volume0.length)}`;
      for (let v = 0; v < 3; v++) {
          let encryptR = String.fromCharCode(107,95,51,56,95,99,111,109,101,0);
          let pressure8 = 2;
          let libimagepipeline2 = String.fromCharCode(97,95,54,53,95,102,111,114,109,0);
         rankI = `${parseInt(`${eventsplashq}`) & volume0.length}`;
         encryptR = "2";
         pressure8 &= pressure8 / (Math.max(1, 3));
         libimagepipeline2 += `${(String.fromCharCode(118,0) == encryptR ? pressure8 : encryptR.length)}`;
      }
      let actions7 = eventsplashq >= 6042750.0;
      do {
         eventsplashq += parseFloat(`${rankI.length}`);
         if (actions7) {
            break;
         }
      } while (((eventsplashq + 2.3) >= 2.72) && actions7);
         volume0 = `${volume0.length ^ parseInt(`${eventsplashq}`)}`;
      let libfabricjniF = eventsplashq >= 9057135.0;
      do {
         eventsplashq += parseFloat(`${parseInt(`${eventsplashq}`)}`);
         if (libfabricjniF) {
            break;
         }
      } while ((skipi.length == parseInt(`${eventsplashq}`)) && libfabricjniF);
      if ((1 >> (Math.min(5, rankI.length))) > 1 && 3.26 > (eventsplashq + 3.66)) {
         eventsplashq *= (parseFloat(`${skipi == String.fromCharCode(102,0) ? skipi.length : volume0.length}`));
      }
         volume0 += `${3 >> (Math.min(4, rankI.length))}`;
          let iconchatsendp: Array<any> = [856, 577];
         skipi = `${skipi.length}`;
         iconchatsendp = [2 << (Math.min(5, iconchatsendp.length))];
          let hometeamfield4: Map<any, any> = new Map([[String.fromCharCode(113,95,50,48,95,115,99,97,110,115,0),945], [String.fromCharCode(106,95,53,49,95,97,110,100,0),205]]);
         rankI += `${rankI.length}`;
         hometeamfield4.set(`${hometeamfield4.size}`, hometeamfield4.size);
      while (parseFloat(`${orangeclockB.length}`) <= eventsplashq) {
         orangeclockB += `${volume0.length}`;
         break;
      }
          let fieldu: Map<any, any> = new Map([[String.fromCharCode(113,95,50,54,95,97,110,110,101,120,98,0),false ], [String.fromCharCode(118,105,111,108,101,116,95,109,95,53,49,0),false ], [String.fromCharCode(108,95,54,56,95,115,98,117,118,0),true ]]);
         eventsplashq += parseFloat(`${1}`);
         fieldu.set(`${fieldu.size}`, fieldu.size / (Math.max(fieldu.size, 1)));
          let grey6 = String.fromCharCode(98,105,100,105,114,95,51,95,50,48,0);
          let whitesmalltickl = false;
          let smallorangemans = String.fromCharCode(101,110,99,111,100,97,98,108,101,95,50,95,50,49,0);
         eventsplashq -= parseFloat(`${smallorangemans.length - skipi.length}`);
         grey6 = `${(String.fromCharCode(105,0) == grey6 ? (whitesmalltickl ? 2 : 4) : grey6.length)}`;
         whitesmalltickl = !grey6.startsWith(`${whitesmalltickl}`);
         smallorangemans = "1";
      philippinesj /= Math.max(2, parseFloat(`${bellc.size << (Math.min(Math.abs(2), 5))}`));
      break;
   }

    setShowUsernameModal(true);
  }

  const onUsernameUpdateSuccess = (newUsername: string) => {
    setUsername(newUsername);
  }

  const onPressReferrer = () => {
       let tumbnailX = 2.0;
    let notificationfillemptyp: Map<any, any> = new Map([[String.fromCharCode(103,101,116,97,117,120,118,97,108,95,112,95,54,0),true ], [String.fromCharCode(99,116,97,98,108,101,115,95,121,95,53,52,0),true ]]);
    let leaguedetailsbg4: Array<any> = [888, 588];
    let minimized = String.fromCharCode(112,95,52,50,95,116,104,101,114,101,0);
    let iconbackwhiteG: Map<any, any> = new Map([[String.fromCharCode(101,118,114,99,95,120,95,49,55,0),579], [String.fromCharCode(109,111,100,109,95,118,95,50,57,0),813], [String.fromCharCode(103,101,110,101,114,97,108,95,53,95,54,57,0),449]]);
    let dependencyB = String.fromCharCode(117,110,112,105,110,110,101,100,95,101,95,55,56,0);
    let collection1 = String.fromCharCode(114,95,53,51,95,112,114,122,112,0);
    let iconbellI: Map<any, any> = new Map([[String.fromCharCode(119,105,100,116,104,115,95,115,95,55,55,0),String.fromCharCode(100,95,55,53,95,112,114,105,109,97,108,105,116,121,0)], [String.fromCharCode(120,95,52,51,95,108,101,110,103,116,104,0),String.fromCharCode(100,101,102,97,117,108,116,95,54,95,50,52,0)], [String.fromCharCode(99,95,54,56,95,101,110,99,114,121,112,116,0),String.fromCharCode(117,109,111,116,105,111,110,95,107,95,51,56,0)]]);
    let imagewatchlivew = String.fromCharCode(108,95,50,55,95,111,115,116,97,114,0);
    let greenarrowupJ: Array<any> = [151, 540];
       let iconcalendarV = String.fromCharCode(107,95,52,95,111,112,115,99,97,108,101,0);
      let sharemodalr = iconcalendarV == String.fromCharCode(48,112,105,53,120,118,103,53,104,51,0);
      do {
         iconcalendarV = `${iconcalendarV.length << (Math.min(Math.abs(2), 5))}`;
         if (sharemodalr) {
            break;
         }
      } while (sharemodalr && (4 > iconcalendarV.length || iconcalendarV == String.fromCharCode(112,0)));
      while (iconcalendarV.length == 5) {
         iconcalendarV += `${iconcalendarV.length % (Math.max(1, 4))}`;
         break;
      }
      if (3 <= iconcalendarV.length || iconcalendarV == String.fromCharCode(87,0)) {
          let modityK = 0;
         iconcalendarV = `${iconcalendarV.length}`;
         modityK >>= Math.min(Math.abs(modityK), 4);
      }
      notificationfillemptyp.set(`${tumbnailX}`, 3 ^ parseInt(`${tumbnailX}`));
       let reactnavigationW = String.fromCharCode(101,95,53,53,95,103,114,111,117,112,0);
       let topica: Map<any, any> = new Map([[String.fromCharCode(111,114,100,101,114,116,121,112,101,95,56,95,51,50,0),379], [String.fromCharCode(105,110,100,105,99,101,115,95,113,95,51,57,0),405], [String.fromCharCode(114,95,57,55,95,116,114,101,120,0),206]]);
          let videoz = 1.0;
         topica = new Map([[`${topica.size}`, parseInt(`${videoz}`) >> (Math.min(Math.abs(topica.size), 1))]]);
      for (let d = 0; d < 1; d++) {
         reactnavigationW += `${reactnavigationW.length >> (Math.min(Math.abs(2), 2))}`;
      }
       let edit7 = 0;
       let shootyesgoalW = 3;
      while (edit7 > 2) {
          let actionP = 0.0;
          let football9 = 0.0;
          let basketballplayerplaceholderr = 0.0;
          let away_ = String.fromCharCode(112,95,51,48,95,100,101,99,111,109,112,111,115,101,105,0);
         reactnavigationW += `${away_.length << (Math.min(Math.abs(1), 3))}`;
         actionP += 1;
         football9 += parseFloat(`${parseInt(`${actionP}`)}`);
         basketballplayerplaceholderr *= 2 - parseInt(`${football9}`);
         away_ = `${parseInt(`${football9}`)}`;
         break;
      }
          let thumbnailx = String.fromCharCode(116,117,110,105,110,103,95,112,95,52,51,0);
         edit7 *= 1;
         thumbnailx += "2";
      if ((reactnavigationW.length & shootyesgoalW) >= 2 && 5 >= (2 & reactnavigationW.length)) {
         shootyesgoalW |= 1;
      }
      collection1 += `${minimized.length | 3}`;
       let whiteticki = 5.0;
       let mbjscommon3 = 4.0;
       let eactc = 2.0;
          let greyL = 1;
          let profileinactive6: Map<any, any> = new Map([[String.fromCharCode(121,95,57,49,95,114,97,108,102,0),703], [String.fromCharCode(115,101,112,105,97,95,104,95,50,49,0),109], [String.fromCharCode(112,105,110,99,104,105,110,103,95,122,95,50,48,0),358]]);
          let mathX = String.fromCharCode(116,99,109,105,95,119,95,54,49,0);
         whiteticki /= Math.max(greyL, 4);
         greyL *= 3;
         profileinactive6.set(mathX, mathX.length | profileinactive6.size);
      if (1.83 <= (whiteticki + mbjscommon3) || (whiteticki + mbjscommon3) <= 1.83) {
         mbjscommon3 /= Math.max(parseFloat(`${parseInt(`${eactc}`)}`), 4);
      }
      minimized = "1";
      minimized += `${notificationfillemptyp.size}`;
   for (let r = 0; r < 1; r++) {
      notificationfillemptyp.set(minimized, iconbackwhiteG.size);
   }
      iconbackwhiteG.set(`${collection1}`, notificationfillemptyp.size - 3);
      notificationfillemptyp.set(minimized, (minimized == String.fromCharCode(101,0) ? iconbackwhiteG.size : minimized.length));
       let penaltyshootW = String.fromCharCode(118,105,109,97,103,101,108,111,97,100,101,114,95,118,95,53,56,0);
      while (penaltyshootW != String.fromCharCode(120,0) && penaltyshootW == String.fromCharCode(57,0)) {
         penaltyshootW += "1";
         break;
      }
         penaltyshootW += `${(String.fromCharCode(103,0) == penaltyshootW ? penaltyshootW.length : penaltyshootW.length)}`;
      if (penaltyshootW == penaltyshootW) {
         penaltyshootW += "3";
      }
      leaguedetailsbg4 = [dependencyB.length];
      tumbnailX /= Math.max(parseFloat(`${dependencyB.length}`), 4);
      tumbnailX += parseFloat(`${minimized.length}`);
      dependencyB = `${collection1.length}`;
   if ((1 | iconbellI.size) == 5 || 1 == (collection1.length | iconbellI.size)) {
      iconbellI = new Map([[`${iconbackwhiteG.size}`, iconbackwhiteG.size % 2]]);
   }
   if (collection1.length > iconbackwhiteG.size) {
       let dialog3 = false;
       let refreshborderlessH = String.fromCharCode(102,101,116,99,104,105,110,103,95,108,95,54,51,0);
      while (!dialog3) {
         refreshborderlessH += `${refreshborderlessH.length >> (Math.min(Math.abs(2), 2))}`;
         break;
      }
       let telegramt = String.fromCharCode(115,95,56,50,95,112,111,112,117,108,97,116,101,0);
         telegramt = "2";
      for (let u = 0; u < 2; u++) {
          let iconbellactiveT = 1;
          let libflipperz = 3.0;
          let robotob = 3.0;
          let gestures9 = 5.0;
          let pingD: Map<any, any> = new Map([[String.fromCharCode(114,95,51,95,115,101,103,109,101,110,116,97,116,105,111,110,0),String.fromCharCode(104,95,49,53,95,114,101,117,112,108,111,97,100,0)], [String.fromCharCode(103,95,49,52,95,115,119,97,112,112,101,114,0),String.fromCharCode(120,95,54,57,95,119,105,110,101,114,114,110,111,0)], [String.fromCharCode(119,95,51,52,95,98,105,110,100,108,105,115,116,0),String.fromCharCode(107,95,54,48,95,112,105,110,103,0)]]);
         refreshborderlessH = `${refreshborderlessH.length}`;
         iconbellactiveT |= pingD.size << (Math.min(1, Math.abs(parseInt(`${libflipperz}`))));
         libflipperz -= parseInt(`${libflipperz}`) << (Math.min(4, Math.abs(1)));
         robotob += pingD.size / 3;
         gestures9 += parseFloat(`${parseInt(`${robotob}`) + pingD.size}`);
      }
      while (!dialog3) {
         telegramt += `${telegramt.length / 2}`;
         break;
      }
          let dialog4: Array<any> = [987, 969, 115];
          let filld = String.fromCharCode(119,95,51,54,95,101,111,109,101,116,114,121,0);
         refreshborderlessH += `${dialog4.length % (Math.max(2, 1))}`;
         dialog4 = [filld.length + 3];
         filld = `${filld.length}`;
      collection1 += `${3 - notificationfillemptyp.size}`;
   }
   let yellowredcardf = minimized == String.fromCharCode(114,113,110,110,0);
   do {
       let details7 = 4.0;
       let libreanimatedU: Map<any, any> = new Map([[String.fromCharCode(112,114,101,100,101,99,101,115,115,111,114,95,49,95,50,57,0),429], [String.fromCharCode(117,110,105,110,115,116,97,108,108,95,122,95,49,0),867]]);
       let foundi: Map<any, any> = new Map([[String.fromCharCode(112,95,53,54,95,105,110,112,108,105,99,105,116,101,108,121,0),String.fromCharCode(121,95,51,56,95,103,101,116,0)], [String.fromCharCode(115,95,55,49,0),String.fromCharCode(99,95,49,51,95,116,114,97,110,115,112,97,114,101,110,116,0)]]);
       let updatess = 2;
          let privilegen = 2.0;
          let arrowrightwithtailb = String.fromCharCode(113,95,52,51,95,115,112,105,110,0);
          let nodei = true;
         libreanimatedU.set(`${privilegen}`, libreanimatedU.size);
         privilegen += arrowrightwithtailb.length;
         arrowrightwithtailb += `${((nodei ? 1 : 4))}`;
      while ((1 | updatess) >= 4 || 1 >= (updatess / (Math.max(10, parseInt(`${details7}`))))) {
         details7 /= Math.max(4, parseFloat(`${2 & parseInt(`${details7}`)}`));
         break;
      }
      let feedback4 = 6125501 >= foundi.size;
      do {
          let whitebellM = String.fromCharCode(115,111,110,111,95,118,95,51,49,0);
          let footballq: Array<any> = [String.fromCharCode(106,95,49,54,95,97,99,99,0), String.fromCharCode(103,101,116,95,111,95,56,54,0)];
          let successo = String.fromCharCode(113,95,52,50,95,99,111,110,118,101,114,103,101,110,99,101,0);
          let classesw = 0;
          let coreH = String.fromCharCode(114,101,99,111,110,110,101,99,116,95,49,95,57,56,0);
         foundi.set(successo, successo.length);
         whitebellM = `${whitebellM.length}`;
         footballq = [classesw & 2];
         classesw ^= (whitebellM == String.fromCharCode(74,0) ? footballq.length : whitebellM.length);
         coreH += `${(whitebellM == String.fromCharCode(48,0) ? whitebellM.length : classesw)}`;
         if (feedback4) {
            break;
         }
      } while (feedback4 && ((libreanimatedU.size >> (Math.min(Math.abs(5), 1))) > 5 && (foundi.size >> (Math.min(Math.abs(5), 3))) > 3));
      while (2 >= (foundi.size * 1) && (parseInt(`${details7}`) + foundi.size) >= 1) {
          let questiconJ = false;
          let videocommonW = false;
          let vignettei: Array<any> = [441, 700, 225];
         details7 += parseFloat(`${parseInt(`${details7}`)}`);
         questiconJ = vignettei.length == 55;
         videocommonW = !questiconJ;
         vignettei.push(((videocommonW ? 2 : 2) << (Math.min(1, Math.abs((questiconJ ? 4 : 3))))));
         break;
      }
          let matchesX: Map<any, any> = new Map([[String.fromCharCode(105,95,54,51,95,109,112,106,112,101,103,0),true ], [String.fromCharCode(116,95,49,52,95,112,107,112,107,101,121,0),true ], [String.fromCharCode(112,95,56,48,95,114,97,112,105,100,106,115,111,110,0),true ]]);
         foundi = new Map([[`${libreanimatedU.size}`, libreanimatedU.size | updatess]]);
         matchesX.set(`${matchesX.size}`, matchesX.size);
         foundi = new Map([[`${libreanimatedU.size}`, libreanimatedU.size - parseInt(`${details7}`)]]);
         foundi = new Map([[`${libreanimatedU.size}`, libreanimatedU.size]]);
      if (details7 < updatess) {
          let basketballiconG = 1.0;
         details7 /= Math.max(parseFloat(`${libreanimatedU.size + parseInt(`${details7}`)}`), 5);
         basketballiconG /= Math.max(3 * parseInt(`${basketballiconG}`), 3);
      }
      for (let d = 0; d < 1; d++) {
          let whitebellY = 4;
          let overlay8 = String.fromCharCode(105,95,49,52,95,105,104,100,114,0);
          let flagF = String.fromCharCode(120,112,114,118,95,99,95,54,52,0);
         foundi.set(`${whitebellY}`, 1);
         whitebellY ^= overlay8.length * flagF.length;
         overlay8 += "2";
         flagF = `${flagF.length * overlay8.length}`;
      }
      for (let i = 0; i < 1; i++) {
         foundi = new Map([[`${libreanimatedU.size}`, parseInt(`${details7}`) * libreanimatedU.size]]);
      }
         libreanimatedU.set(`${details7}`, 2 % (Math.max(10, parseInt(`${details7}`))));
         foundi = new Map([[`${foundi.size}`, 2 - foundi.size]]);
      minimized = `${(String.fromCharCode(79,0) == collection1 ? iconbellI.size : collection1.length)}`;
      if (yellowredcardf) {
         break;
      }
   } while (yellowredcardf && (collection1 == String.fromCharCode(69,0)));
      iconbellI = new Map([[`${notificationfillemptyp.size}`, iconbellI.size >> (Math.min(Math.abs(1), 1))]]);
   let scoreJ = String.fromCharCode(56,117,117,97,110,101,0) == minimized;
   do {
       let loginsuccessY = 2.0;
       let fieldN = String.fromCharCode(109,95,54,48,0);
       let detailsi: Map<any, any> = new Map([[String.fromCharCode(114,95,52,54,95,115,105,100,101,100,97,116,97,0),true ], [String.fromCharCode(119,95,55,49,0),false ], [String.fromCharCode(99,108,97,122,122,95,116,95,51,53,0),false ]]);
         detailsi = new Map([[`${detailsi.size}`, 1]]);
          let playN = 3.0;
          let bdxadsdkV = String.fromCharCode(97,117,120,95,113,95,53,57,0);
         detailsi = new Map([[`${playN}`, bdxadsdkV.length ^ parseInt(`${playN}`)]]);
      for (let g = 0; g < 1; g++) {
          let classes5 = String.fromCharCode(112,97,114,109,115,95,121,95,53,54,0);
          let cornerH = 0.0;
          let analytic3: Map<any, any> = new Map([[String.fromCharCode(99,95,57,55,95,116,101,115,101,100,103,101,0),835], [String.fromCharCode(104,95,52,55,95,101,98,117,108,97,115,0),996]]);
          let z_manager0 = 3;
          let vignettem = 3;
         loginsuccessY -= classes5.length & 1;
         classes5 += `${z_manager0}`;
         cornerH -= analytic3.size;
         analytic3.set(`${vignettem}`, vignettem / (Math.max(analytic3.size, 7)));
      }
          let bannerl = String.fromCharCode(109,97,115,107,101,100,109,101,114,103,101,95,51,95,53,55,0);
         loginsuccessY += detailsi.size;
         bannerl += `${bannerl.length}`;
          let bangl = false;
         fieldN += `${parseInt(`${loginsuccessY}`) << (Math.min(3, Math.abs(2)))}`;
         bangl = (bangl ? bangl : bangl);
       let modulesZ = 1;
       let apps4 = 3;
      while (apps4 < modulesZ) {
         apps4 %= Math.max(fieldN.length, 1);
         break;
      }
      let plashw = apps4 <= 7734881;
      do {
         apps4 >>= Math.min(3, Math.abs(1));
         if (plashw) {
            break;
         }
      } while ((apps4 == fieldN.length) && plashw);
         fieldN = `${modulesZ ^ parseInt(`${loginsuccessY}`)}`;
      minimized = `${leaguedetailsbg4.length}`;
      if (scoreJ) {
         break;
      }
   } while ((imagewatchlivew.length < minimized.length) && scoreJ);

    setShowReferrerModal(true);
  }

  const onCopyReferralCode = () => {
       let imagesc = String.fromCharCode(118,115,121,110,99,95,49,95,50,48,0);
    let switch_7qj = String.fromCharCode(115,95,50,48,95,115,117,98,109,105,116,0);
    let shootyesgoalk = 2.0;
    let cornershoot9 = 4.0;
    let iconsubssuccess3 = String.fromCharCode(118,95,57,53,95,100,114,97,119,117,116,105,108,115,0);
    let lang1 = String.fromCharCode(103,95,54,95,117,110,102,105,108,116,101,114,101,100,0);
    let downloader2 = 3;
    let popupd = String.fromCharCode(116,95,57,49,95,115,112,101,99,105,102,105,101,100,0);
    let vignette8 = 4.0;
   let proxyM = String.fromCharCode(115,113,112,105,0) == switch_7qj;
   do {
      switch_7qj += `${(switch_7qj == String.fromCharCode(99,0) ? parseInt(`${shootyesgoalk}`) : switch_7qj.length)}`;
      if (proxyM) {
         break;
      }
   } while (proxyM && ((parseInt(`${shootyesgoalk}`) / 4) >= 5 && 3.27 >= (4.28 / (Math.max(9, shootyesgoalk)))));
   for (let a = 0; a < 2; a++) {
      cornershoot9 += switch_7qj.length;
   }
      iconsubssuccess3 += "1";
   while ((shootyesgoalk + 5.30) <= 5.80) {
      switch_7qj += `${imagesc.length % (Math.max(1, 3))}`;
      break;
   }
       let homeloadingl: Array<any> = [287, 26];
         homeloadingl = [homeloadingl.length];
         homeloadingl = [homeloadingl.length << (Math.min(homeloadingl.length, 1))];
          let source_ = 5.0;
          let sendg = false;
         homeloadingl.push((parseInt(`${source_}`) >> (Math.min(1, Math.abs((sendg ? 4 : 2))))));
      lang1 = `${iconsubssuccess3.length + 2}`;
      switch_7qj += `${parseInt(`${cornershoot9}`) | 3}`;
   let iconstarl = 7173628 >= downloader2;
   do {
      downloader2 -= downloader2;
      if (iconstarl) {
         break;
      }
   } while (((parseInt(`${cornershoot9}`) * downloader2) <= 2 || 2 <= (downloader2 % (Math.max(2, 6)))) && iconstarl);
   for (let t = 0; t < 2; t++) {
      iconsubssuccess3 += `${popupd.length}`;
   }
      shootyesgoalk -= parseFloat(`${parseInt(`${cornershoot9}`)}`);
      imagesc += `${iconsubssuccess3.length}`;
       let readU = String.fromCharCode(115,99,111,112,101,105,100,95,120,95,55,50,0);
       let libcxxcomponentsp: Map<any, any> = new Map([[String.fromCharCode(109,95,51,51,95,108,101,102,116,109,111,115,116,0),998], [String.fromCharCode(119,97,107,101,95,110,95,53,53,0),249]]);
       let iconbellm = 5.0;
      let indexT = readU.length >= 6732878;
      do {
          let icondownimge = false;
          let projectr = String.fromCharCode(103,95,51,49,95,114,101,99,114,101,97,116,101,0);
          let libswresamplet = String.fromCharCode(106,95,50,52,95,115,116,114,105,112,115,0);
          let gesturesj = String.fromCharCode(101,95,55,50,95,114,104,115,0);
         readU = `${projectr.length ^ readU.length}`;
         icondownimge = !icondownimge;
         projectr = `${(String.fromCharCode(80,0) == libswresamplet ? (icondownimge ? 4 : 4) : libswresamplet.length)}`;
         gesturesj = "3";
         if (indexT) {
            break;
         }
      } while (indexT && (readU.includes(`${iconbellm}`)));
         readU = "2";
      for (let u = 0; u < 1; u++) {
         readU = "2";
      }
      for (let g = 0; g < 2; g++) {
          let taiwanO = String.fromCharCode(102,95,50,57,95,99,111,112,121,0);
          let plus9 = String.fromCharCode(115,95,50,53,95,108,111,99,107,115,0);
          let executork: Map<any, any> = new Map([[String.fromCharCode(114,101,101,110,116,114,97,110,116,95,120,95,49,0),932], [String.fromCharCode(99,108,111,115,101,95,48,95,49,56,0),59]]);
          let componentregistryH = false;
          let phonesharev = String.fromCharCode(115,116,117,102,102,95,106,95,52,57,0);
         readU = `${1 - plus9.length}`;
         taiwanO = `${(executork.size + (componentregistryH ? 2 : 3))}`;
         plus9 += "2";
         executork.set(`${componentregistryH}`, 2 | executork.size);
         phonesharev = "1";
      }
      for (let d = 0; d < 1; d++) {
         libcxxcomponentsp = new Map([[`${libcxxcomponentsp.size}`, libcxxcomponentsp.size | 2]]);
      }
      for (let t = 0; t < 2; t++) {
         libcxxcomponentsp.set(`${readU}`, (readU == String.fromCharCode(120,0) ? readU.length : libcxxcomponentsp.size));
      }
      let changek = libcxxcomponentsp.size <= 8726460;
      do {
          let productR = String.fromCharCode(102,95,57,52,95,99,117,109,117,108,97,116,105,118,101,0);
          let downloadA = String.fromCharCode(106,95,54,51,95,102,112,117,116,115,0);
         libcxxcomponentsp.set(`${iconbellm}`, libcxxcomponentsp.size);
         productR += `${productR.length - 1}`;
         downloadA = `${2 >> (Math.min(4, downloadA.length))}`;
         if (changek) {
            break;
         }
      } while (changek && (!Array.from(libcxxcomponentsp.values()).includes(iconbellm)));
      for (let o = 0; o < 2; o++) {
          let bodanx = String.fromCharCode(101,120,99,105,116,97,116,105,111,110,95,57,95,57,50,0);
          let largesoundF = 5.0;
          let typesW = String.fromCharCode(106,95,55,95,105,110,105,116,105,97,108,105,122,101,114,0);
         libcxxcomponentsp = new Map([[`${iconbellm}`, bodanx.length ^ parseInt(`${iconbellm}`)]]);
         bodanx += `${typesW.length * 2}`;
         largesoundF -= parseFloat(`${2 >> (Math.min(5, Math.abs(parseInt(`${largesoundF}`))))}`);
         typesW = `${typesW.length}`;
      }
         readU = `${parseInt(`${iconbellm}`) + libcxxcomponentsp.size}`;
      lang1 = `${parseInt(`${shootyesgoalk}`)}`;
      imagesc += `${downloader2 + popupd.length}`;
       let b_unlockD = 4.0;
          let libhermesa = String.fromCharCode(107,95,53,53,95,100,114,97,119,108,105,110,101,0);
          let owngoals: Array<any> = [String.fromCharCode(112,95,53,49,95,115,119,105,116,99,104,98,97,115,101,0), String.fromCharCode(112,95,55,54,95,115,116,97,109,112,115,0)];
         b_unlockD *= owngoals.length;
         libhermesa += `${2 << (Math.min(4, libhermesa.length))}`;
         owngoals = [libhermesa.length | 3];
          let saveD = String.fromCharCode(112,101,114,115,105,115,116,101,110,116,95,50,95,57,52,0);
          let mappingP: Map<any, any> = new Map([[String.fromCharCode(104,95,55,50,95,109,101,109,115,101,116,0),true ], [String.fromCharCode(110,95,50,55,95,102,114,101,101,109,0),false ], [String.fromCharCode(109,97,115,107,105,110,103,95,110,95,56,53,0),true ]]);
         b_unlockD -= (String.fromCharCode(105,0) == saveD ? parseInt(`${b_unlockD}`) : saveD.length);
         mappingP = new Map([[`${mappingP.size}`, mappingP.size]]);
         b_unlockD *= parseInt(`${b_unlockD}`);
      popupd = `${parseInt(`${cornershoot9}`) | 2}`;
   while (1.97 == (iconsubssuccess3.length * cornershoot9) || 3 == (2 & iconsubssuccess3.length)) {
      iconsubssuccess3 = "2";
      break;
   }

    Clipboard.setString(userState.user?.userReferralCode ?? '');

       let reviewo = String.fromCharCode(98,105,116,112,97,99,107,101,100,95,102,95,55,52,0);
       let auto_fdp: Map<any, any> = new Map([[String.fromCharCode(102,95,52,50,95,105,103,110,111,114,101,0),String.fromCharCode(116,95,57,95,111,117,116,0)], [String.fromCharCode(116,101,108,101,103,114,97,109,95,50,95,51,49,0),String.fromCharCode(114,101,118,105,115,105,111,110,95,113,95,57,51,0)], [String.fromCharCode(99,111,114,114,101,99,116,105,111,110,95,116,95,57,0),String.fromCharCode(111,109,105,116,116,105,110,103,95,52,95,49,56,0)]]);
      if ((auto_fdp.size - reviewo.length) < 4 && (auto_fdp.size - reviewo.length) < 4) {
         reviewo += `${auto_fdp.size << (Math.min(reviewo.length, 3))}`;
      }
      let iconplaye = auto_fdp.size >= 6101527;
      do {
         auto_fdp = new Map([[`${auto_fdp.size}`, 3]]);
         if (iconplaye) {
            break;
         }
      } while (iconplaye && (3 >= (reviewo.length << (Math.min(1, Math.abs(auto_fdp.size)))) && (3 << (Math.min(3, reviewo.length))) >= 2));
      if (!reviewo.endsWith(`${auto_fdp.size}`)) {
         auto_fdp.set(`${reviewo}`, auto_fdp.size * 3);
      }
      let configc = reviewo.length <= 7802635;
      do {
          let twitterp = String.fromCharCode(105,95,50,48,0);
         reviewo += `${auto_fdp.size}`;
         twitterp += `${twitterp.length}`;
         if (configc) {
            break;
         }
      } while (configc && (5 >= (reviewo.length * auto_fdp.size)));
          let sharedz = 3.0;
          let iconsettingu = String.fromCharCode(101,95,54,50,95,111,99,116,101,116,115,0);
          let bingr = String.fromCharCode(99,95,55,53,95,108,105,109,105,116,97,116,105,111,110,0);
         reviewo += `${(String.fromCharCode(88,0) == iconsettingu ? auto_fdp.size : iconsettingu.length)}`;
         sharedz *= parseInt(`${sharedz}`);
         bingr += `${1 + parseInt(`${sharedz}`)}`;
         auto_fdp.set(`${reviewo}`, auto_fdp.size >> (Math.min(reviewo.length, 3)));
      cornershoot9 -= lang1.length + 1;
   if (2.96 < (shootyesgoalk * 2.73) || (parseFloat(`${switch_7qj.length}`) * shootyesgoalk) < 2.73) {
      switch_7qj = "1";
   }
   for (let z = 0; z < 1; z++) {
       let greyarrowupZ = false;
      while (!greyarrowupZ && greyarrowupZ) {
         greyarrowupZ = !greyarrowupZ;
         break;
      }
      if (!greyarrowupZ || greyarrowupZ) {
          let yellowQ = 0;
          let classesb = 0.0;
         greyarrowupZ = (yellowQ + classesb) < 44;
      }
      let appleo = greyarrowupZ ? !greyarrowupZ : greyarrowupZ;
      do {
          let confirmationi = String.fromCharCode(107,95,57,51,95,103,101,111,99,111,100,101,115,0);
          let nbatrophyL: Array<any> = [404, 144];
         greyarrowupZ = confirmationi.length > 22;
         confirmationi += `${nbatrophyL.length >> (Math.min(nbatrophyL.length, 4))}`;
         if (appleo) {
            break;
         }
      } while ((!greyarrowupZ) && appleo);
      imagesc = "2";
   }
      downloader2 >>= Math.min(Math.abs(popupd.length + parseInt(`${shootyesgoalk}`)), 3);
   while (shootyesgoalk < 2.41) {
      shootyesgoalk /= Math.max(parseFloat(`${1}`), 5);
      break;
   }
       let mbbidz = 1.0;
       let round4 = String.fromCharCode(99,104,97,105,110,101,100,95,122,95,51,54,0);
         mbbidz += parseFloat(`${parseInt(`${mbbidz}`)}`);
      if (2 < (parseInt(`${mbbidz}`) + round4.length) || 5 < (round4.length % 2)) {
          let profileframeh = 0.0;
         round4 += "2";
         profileframeh -= parseInt(`${profileframeh}`);
      }
      if (parseInt(`${mbbidz}`) > round4.length) {
         round4 = `${parseInt(`${mbbidz}`) + round4.length}`;
      }
         mbbidz += parseFloat(`${parseInt(`${mbbidz}`)}`);
      while (!round4.endsWith(`${mbbidz}`)) {
         round4 += `${1 - parseInt(`${mbbidz}`)}`;
         break;
      }
      if (3.3 >= (3.68 - mbbidz) && (round4.length >> (Math.min(Math.abs(5), 1))) >= 2) {
         round4 += `${round4.length & parseInt(`${mbbidz}`)}`;
      }
      cornershoot9 *= 2;
      cornershoot9 -= 3;
      cornershoot9 += 3;
      downloader2 |= parseInt(`${shootyesgoalk}`);
   if (popupd != switch_7qj) {
       let soundd: Array<any> = [293, 765];
         soundd = [soundd.length / (Math.max(1, 9))];
         soundd = [2 << (Math.min(3, soundd.length))];
         soundd = [2 * soundd.length];
      switch_7qj += "2";
   }
       let privatechatbgK = true;
       let gradle3: Array<any> = [198, 2];
       let memberH: Array<any> = [322, 885];
         gradle3.push(((privatechatbgK ? 3 : 5) % (Math.max(gradle3.length, 6))));
      let inouttargetredH = privatechatbgK ? !privatechatbgK : privatechatbgK;
      do {
         privatechatbgK = gradle3.includes(memberH[memberH.length - 1]);
         if (inouttargetredH) {
            break;
         }
      } while (inouttargetredH && ((3 & memberH.length) <= 1));
      for (let q = 0; q < 1; q++) {
          let holdera = 3.0;
         privatechatbgK = 92.83 > holdera;
      }
       let emoji8 = 2.0;
         emoji8 /= Math.max(2, gradle3.length);
          let basketballj = 1;
          let rounds: Array<any> = [482, 431];
          let libfbz = String.fromCharCode(102,95,54,48,95,112,108,111,116,116,101,114,0);
         gradle3.push(gradle3.length);
         basketballj ^= libfbz.length * rounds.length;
         rounds.push((libfbz == String.fromCharCode(117,0) ? rounds.length : libfbz.length));
         emoji8 += 1;
          let bingD = false;
          let helper_ = 3;
         memberH.push(gradle3.length >> (Math.min(2, Math.abs(helper_))));
         bingD = !bingD;
         helper_ %= Math.max(5, 2);
         gradle3.push(2 | gradle3.length);
      popupd += `${lang1.length}`;
      shootyesgoalk -= parseFloat(`${downloader2}`);
   for (let a = 0; a < 2; a++) {
      lang1 = `${popupd.length % (Math.max(3, 7))}`;
   }
   while ((popupd.length ^ downloader2) > 2 && (downloader2 ^ 2) > 5) {
      popupd += `${3 * iconsubssuccess3.length}`;
      break;
   }
    setIsDialogOpen(true);
  }

  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader title='个人中心' />
      <View
        style={{
          justifyContent: 'space-between',
          flex: 1,
          paddingHorizontal: 10,
        }}
      >
        <View style={{ marginTop: 30 }}>

          <View
            style={styles.pressableTextContainer}
          >
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>头像</Text>
            <FastImage
              style={{
                height: 34,
                width: 34,
              }}
              source={require('@static/images/iconviewerConfirmation.png')}
            />
          </View>


          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onPressUsername}
          >
            <Text style={{ fontSize: 16, color: 'white' }}>
              {username}
            </Text>
          </TouchableOpacity>

          { }
          <View style={styles.pressableTextContainer}>
            <Text style={{ fontSize: 16, color: '#9C9C9C' }}>
              {userState.user?.userEmail !== '' ? userState.user?.userEmail : userState.user.userPhoneNumber}
            </Text>
          </View>

          { }
          {userState.user?.userReferrerName == '' &&
            userState.user.userAllowUpdateReferral == true &&
            <TouchableOpacity
              style={styles.pressableTextContainer}
              onPress={onPressReferrer}
            >
              <Text style={{ fontSize: 16, color: '#B6B6B6' }}>
                补填邀请码 (只能填写一次)
              </Text>
            </TouchableOpacity>
          }

          {userState.user?.userReferrerName != '' && (
            <View style={styles.pressableTextContainer}>
              <Text style={{ fontSize: 16, color: '#9C9C9C' }}>推介人</Text>

              <View
                style={{
                  flexDirection: 'row',
                  justifyContent: 'center',
                  alignItems: 'center',
                }}
              >
                <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                  {userState.user?.userReferrerName}
                </Text>
              </View>
            </View>
          )}

          { }
          <TouchableOpacity
            style={styles.pressableTextContainer}
            onPress={onCopyReferralCode}
          >
            <Text style={{ fontSize: 16, color: colors.primary }}>
              {userState.user?.userReferralCode}
            </Text>

            <View
              style={{
                flexDirection: 'row',
                justifyContent: 'center',
                alignItems: 'center',
              }}
            >
              <Text style={{ fontSize: 14, color: '#9C9C9C' }}>
                复制邀请码
              </Text>
              <Image
                style={{
                  height: 27,
                  width: 27,
                  position: 'relative',
                  top: 2,
                }}
                source={require('@static/images/profile/middlebrightnessCombinedMinivod.png')}
              />
            </View>
          </TouchableOpacity>

          <NotificationModal
            onConfirm={toggleOverlay}
            isVisible={isDialogOpen}
            title='复制成功'
            subtitle1=''
            subtitle2=''
            subtitle3=''
          />
        </View>

        <ChangeUsernameModal
          isVisible={isShowUsernameModal}
          handleClose={() => {
            setShowUsernameModal(false);
          }}
          initialUsername={username}
          onSubmitSuccess={onUsernameUpdateSuccess}
        />

        <ChangeReferrerModal
          isVisible={isShowReferrerModal}
          handleClose={() => {
            setShowReferrerModal(false);
          }}
        />
      </View>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  pressableTextContainer: {
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    backgroundColor: '#1d2023',
    marginTop: 20,
    paddingLeft: 18,
    paddingRight: 13,
    height: 48,
    borderRadius: 8,
    marginBottom: 20,
  },
});
