import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/ww_collection";
import { RootStackScreenProps } from "@type/ww_tempnodatagif_description";
import TitleWithBackButtonHeader from "../../components/header/ww_react_predictionwin_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility/ww_icon";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let clockn = false;
    let guide2 = 5.0;
    let gesture8 = String.fromCharCode(98,108,111,99,107,100,115,112,0);
    let roomM = false;
    let championP = false;
    let iconsaveimages = String.fromCharCode(114,112,114,111,98,101,0);
    let frame_12k = String.fromCharCode(118,101,110,99,0);
    let libavcodech = 4;
    let mbridgeD = 4.0;
    let nbatrophyW = 4.0;
    let reducerp = String.fromCharCode(98,108,111,99,107,100,99,0);
    let mergern = String.fromCharCode(99,98,114,116,0);
    let huaweif = 1.0;
    let ycopy_m9v: Array<any> = [8, 603, 206];
    let dplusZ = 0.0;
    let defaultplayerimgl = 5.0;
    let awayH = String.fromCharCode(101,113,117,97,108,115,0);
    let auto_oju = 1.0;
   for (let y = 0; y < 1; y++) {
      guide2 -= parseInt(`${guide2}`) * 2;
   }
   for (let t = 0; t < 3; t++) {
       let armvaO: Map<any, any> = new Map([[String.fromCharCode(99,111,112,121,120,0),781], [String.fromCharCode(122,114,101,111,114,100,101,114,0),542]]);
       let lineo: Array<any> = [String.fromCharCode(116,114,117,110,107,0), String.fromCharCode(97,115,102,99,114,121,112,116,0), String.fromCharCode(98,111,116,0)];
       let faste = 4;
       let shootY: Map<any, any> = new Map([[String.fromCharCode(108,97,121,101,114,115,0),false ], [String.fromCharCode(116,109,105,120,0),true ]]);
       let hook1: Map<any, any> = new Map([[String.fromCharCode(116,121,112,101,99,111,100,101,0),558], [String.fromCharCode(102,100,101,99,0),822]]);
       let telegram_ = String.fromCharCode(115,113,108,105,116,101,99,104,97,110,103,101,115,101,116,0);
       let inouttargetredv = String.fromCharCode(97,100,97,112,116,115,0);
         shootY = new Map([[`${shootY.size}`, 2 | shootY.size]]);
         hook1 = new Map([[`${armvaO.size}`, 3 >> (Math.min(4, lineo.length))]]);
         telegram_ = `${1 - faste}`;
      for (let h = 0; h < 3; h++) {
         telegram_ = `${telegram_.length ^ 1}`;
      }
         shootY = new Map([[`${hook1.size}`, 1]]);
      for (let d = 0; d < 1; d++) {
         shootY = new Map([[`${shootY.size}`, 1]]);
      }
      while (2 <= (telegram_.length % (Math.max(2, 9))) && (telegram_.length % 2) <= 4) {
          let sliderY = String.fromCharCode(112,114,101,102,101,114,114,101,100,0);
          let holdert = 1.0;
          let questiconx = 5;
          let cornerkickc = 2.0;
          let halfH = 5.0;
         telegram_ += `${lineo.length}`;
         sliderY += `${2 >> (Math.min(2, sliderY.length))}`;
         holdert /= Math.max(4, questiconx);
         questiconx -= questiconx / (Math.max(sliderY.length, 9));
         cornerkickc += questiconx;
         halfH *= questiconx << (Math.min(3, Math.abs(3)));
         break;
      }
      roomM = faste == reducerp.length;
   }
   if ((guide2 * 1.61) == 5.57) {
      guide2 -= ((clockn ? 1 : 4) - parseInt(`${guide2}`));
   }
       let casta: Map<any, any> = new Map([[String.fromCharCode(112,114,111,109,111,116,101,100,0),String.fromCharCode(105,110,115,101,110,115,105,116,105,118,101,0)], [String.fromCharCode(105,110,102,111,108,100,101,114,0),String.fromCharCode(103,97,109,101,115,0)]]);
      for (let w = 0; w < 3; w++) {
         casta.set(`${casta.size}`, 1);
      }
      for (let i = 0; i < 2; i++) {
         casta.set(`${casta.size}`, 3 ^ casta.size);
      }
      if (casta.size >= casta.size) {
         casta = new Map([[`${casta.size}`, casta.size]]);
      }
      reducerp = `${frame_12k.length >> (Math.min(iconsaveimages.length, 3))}`;
   for (let y = 0; y < 1; y++) {
       let movies0: Map<any, any> = new Map([[String.fromCharCode(112,97,103,101,111,117,116,0),String.fromCharCode(111,110,108,105,110,101,115,0)], [String.fromCharCode(102,105,110,100,101,112,0),String.fromCharCode(97,108,108,114,103,98,0)]]);
       let iconnewsshare2 = String.fromCharCode(99,108,111,99,107,0);
       let whiteanimationliveF = String.fromCharCode(115,104,111,119,110,0);
       let zhengpianp: Array<any> = [685, 4, 834];
       let libfbh = String.fromCharCode(105,115,111,112,101,110,113,117,111,116,101,0);
         zhengpianp.push(zhengpianp.length & 1);
         libfbh += `${(iconnewsshare2 == String.fromCharCode(114,0) ? movies0.size : iconnewsshare2.length)}`;
      if (2 >= (zhengpianp.length & 1) && 1 >= (1 & libfbh.length)) {
         libfbh += "1";
      }
      for (let p = 0; p < 2; p++) {
          let pathy = String.fromCharCode(99,109,97,107,101,0);
          let roomL = 3.0;
         iconnewsshare2 += `${libfbh.length}`;
         pathy += `${1 - pathy.length}`;
         roomL /= Math.max((parseFloat(`${pathy == String.fromCharCode(76,0) ? parseInt(`${roomL}`) : pathy.length}`)), 4);
      }
          let submitO = 5.0;
         movies0.set(whiteanimationliveF, 1);
         submitO -= parseFloat(`${parseInt(`${submitO}`)}`);
         libfbh += `${iconnewsshare2.length}`;
      let short_xT = String.fromCharCode(106,95,54,104,0) == iconnewsshare2;
      do {
         iconnewsshare2 = `${whiteanimationliveF.length}`;
         if (short_xT) {
            break;
         }
      } while ((3 >= (1 >> (Math.min(3, iconnewsshare2.length))) || (movies0.size >> (Math.min(iconnewsshare2.length, 3))) >= 1) && short_xT);
      while (libfbh.includes(`${zhengpianp.length}`)) {
         zhengpianp = [(String.fromCharCode(51,0) == libfbh ? zhengpianp.length : libfbh.length)];
         break;
      }
         iconnewsshare2 = `${zhengpianp.length / (Math.max(libfbh.length, 10))}`;
         movies0 = new Map([[`${movies0.size}`, zhengpianp.length ^ 2]]);
      while ((2 % (Math.max(8, iconnewsshare2.length))) <= 1 && 2 <= (movies0.size % (Math.max(iconnewsshare2.length, 6)))) {
         movies0 = new Map([[`${movies0.size}`, 2 * iconnewsshare2.length]]);
         break;
      }
      for (let k = 0; k < 3; k++) {
         zhengpianp.push(iconnewsshare2.length);
      }
      let s_unlockS = zhengpianp.length >= 7145592;
      do {
         zhengpianp = [(String.fromCharCode(106,0) == iconnewsshare2 ? iconnewsshare2.length : whiteanimationliveF.length)];
         if (s_unlockS) {
            break;
         }
      } while (s_unlockS && (2 > (zhengpianp.length & iconnewsshare2.length)));
       let predictionactiveX = String.fromCharCode(97,100,100,111,112,0);
         predictionactiveX += `${iconnewsshare2.length}`;
      guide2 *= mergern.length ^ iconsaveimages.length;
   }
      mergern += "1";
      gesture8 = `${(3 << (Math.min(1, Math.abs((championP ? 1 : 1)))))}`;
   for (let t = 0; t < 3; t++) {
       let short_nug = true;
       let codegenj = 2.0;
       let libfbjnig = String.fromCharCode(108,105,98,120,0);
         codegenj /= Math.max(3, parseInt(`${codegenj}`) ^ 1);
      while (1.22 < (4.12 * codegenj)) {
         short_nug = short_nug || libfbjnig.length <= 10;
         break;
      }
      if (4.96 > codegenj) {
         codegenj += (parseInt(`${codegenj}`) << (Math.min(4, Math.abs((short_nug ? 2 : 2)))));
      }
         codegenj += 1 ^ parseInt(`${codegenj}`);
       let animationsj: Array<any> = [223, 478];
      let modulev = codegenj >= 7836021.0;
      do {
          let favicon1 = String.fromCharCode(100,98,108,113,117,111,116,101,0);
          let baseZ: Array<any> = [String.fromCharCode(118,105,109,97,103,101,108,111,97,100,101,114,0), String.fromCharCode(113,115,118,100,101,105,110,116,0)];
         codegenj += 2;
         favicon1 += `${favicon1.length & baseZ.length}`;
         baseZ = [1];
         if (modulev) {
            break;
         }
      } while (modulev && (5.1 < (2.15 - codegenj)));
         libfbjnig += `${((short_nug ? 4 : 4))}`;
          let photo4 = String.fromCharCode(99,111,100,105,110,103,116,121,112,101,0);
          let kuaishouU = 5.0;
          let latny = 3;
         short_nug = latny > parseInt(`${kuaishouU}`);
         photo4 = `${photo4.length}`;
         kuaishouU += parseFloat(`${photo4.length}`);
         animationsj.push(libfbjnig.length / (Math.max(1, 2)));
      mergern = "1";
   }
      gesture8 = `${parseInt(`${mbridgeD}`) - 1}`;
   for (let i = 0; i < 2; i++) {
      frame_12k = `${((clockn ? 4 : 1) << (Math.min(reducerp.length, 2)))}`;
   }
       let reactnavigationk: Map<any, any> = new Map([[String.fromCharCode(100,105,97,108,111,103,115,0),923], [String.fromCharCode(99,97,110,99,101,108,97,116,105,111,110,0),457], [String.fromCharCode(104,105,103,104,112,97,115,115,0),963]]);
       let previewQ = String.fromCharCode(100,111,99,108,105,115,116,115,0);
       let materialu = 4;
      let activec = previewQ.length >= 5947606;
      do {
          let sportsA = true;
          let basketballhometeamp = String.fromCharCode(101,118,105,99,116,0);
          let hooksi = String.fromCharCode(109,105,120,101,114,0);
         previewQ += `${materialu + 2}`;
         sportsA = (((!sportsA ? hooksi.length : 19) / (Math.max(hooksi.length, 4))) >= 19);
         basketballhometeamp += `${((sportsA ? 4 : 3))}`;
         if (activec) {
            break;
         }
      } while ((3 > (materialu / (Math.max(previewQ.length, 1))) || 2 > (materialu / (Math.max(3, 9)))) && activec);
         previewQ += "2";
       let configo = String.fromCharCode(106,100,109,97,115,116,101,114,0);
       let videovarR: Map<any, any> = new Map([[String.fromCharCode(97,101,115,111,112,116,0),641], [String.fromCharCode(102,114,105,101,110,100,0),322]]);
       let libpangleflippedZ = 4.0;
       let stepo = 0.0;
          let libglogs = String.fromCharCode(98,105,103,116,114,101,101,0);
          let commoni: Map<any, any> = new Map([[String.fromCharCode(97,121,117,118,108,101,0),530], [String.fromCharCode(105,110,102,117,114,97,0),329]]);
          let unselectedn: Map<any, any> = new Map([[String.fromCharCode(105,115,115,101,116,117,103,105,100,0),277], [String.fromCharCode(104,97,118,101,101,118,101,110,116,115,0),657], [String.fromCharCode(97,116,116,114,97,99,116,0),397]]);
         libpangleflippedZ /= Math.max(3, parseFloat(`${2}`));
         libglogs += `${libglogs.length / 2}`;
         commoni.set(libglogs, libglogs.length);
         unselectedn = new Map([[`${unselectedn.size}`, 2]]);
      if (1 > reactnavigationk.size) {
          let selectedS = String.fromCharCode(104,107,100,102,0);
          let mbridgem = 0;
          let nalyticsB = String.fromCharCode(112,108,97,99,101,115,0);
          let faviconU = String.fromCharCode(100,113,117,97,110,116,0);
          let pathQ = true;
         reactnavigationk.set(previewQ, parseInt(`${stepo}`));
         selectedS += `${faviconU.length | 3}`;
         mbridgem /= Math.max((3 + (pathQ ? 2 : 4)), 2);
         nalyticsB += `${(faviconU.length | (pathQ ? 2 : 2))}`;
      }
      if (!Array.from(videovarR.keys()).includes(`${stepo}`)) {
         videovarR = new Map([[`${libpangleflippedZ}`, materialu]]);
      }
      if ((libpangleflippedZ / 3.28) > 1.72 && (libpangleflippedZ / 3.28) > 1.60) {
         materialu &= 1;
      }
      mbridgeD /= Math.max(((championP ? 3 : 5) % (Math.max((roomM ? 4 : 4), 10))), 1);
      libavcodech -= 1;
   let penaltyd = championP ? !championP : championP;
   do {
      championP = mbridgeD >= 10.14 || nbatrophyW >= 10.14;
      if (penaltyd) {
         break;
      }
   } while ((championP || frame_12k.length < 2) && penaltyd);
   if (clockn) {
      clockn = (70 == (iconsaveimages.length - (roomM ? 70 : iconsaveimages.length)));
   }
      nbatrophyW /= Math.max(2, (parseFloat(`${mergern == String.fromCharCode(71,0) ? mergern.length : reducerp.length}`)));
       let countryR: Array<any> = [484, 430, 456];
      for (let b = 0; b < 3; b++) {
          let androidm = 3.0;
          let macauv = String.fromCharCode(101,110,104,97,110,99,101,114,0);
          let referreru = String.fromCharCode(101,120,116,114,97,100,97,116,97,112,115,101,116,115,0);
          let yellowredcardM = 4.0;
          let grayX = String.fromCharCode(116,105,109,101,115,99,97,108,101,0);
         countryR.push(referreru.length);
         androidm *= macauv.length;
         macauv += `${parseInt(`${yellowredcardM}`) % 1}`;
         referreru += "1";
         yellowredcardM *= parseFloat(`${2}`);
         grayX = `${(macauv == String.fromCharCode(52,0) ? parseInt(`${yellowredcardM}`) : macauv.length)}`;
      }
      while (2 <= (countryR.length | countryR.length) && 1 <= (2 | countryR.length)) {
         countryR.push(countryR.length);
         break;
      }
         countryR.push(countryR.length % (Math.max(10, countryR.length)));
      libavcodech *= parseInt(`${nbatrophyW}`);
       let circle6 = String.fromCharCode(116,104,114,101,97,100,103,114,111,117,112,0);
       let benefit0 = String.fromCharCode(102,97,113,0);
       let attributedstringB = String.fromCharCode(115,112,101,99,115,0);
       let privilegee = String.fromCharCode(118,108,98,117,102,0);
       let final_mx = String.fromCharCode(115,101,101,107,105,110,103,0);
      roomM = 89.83 == mbridgeD || 89.83 == nbatrophyW;
      circle6 += `${circle6.length}`;
   for (let d = 0; d < 2; d++) {
      libavcodech &= parseInt(`${nbatrophyW}`) + 3;
   }
   while (4 <= (3 & libavcodech) || 3 <= (mergern.length & libavcodech)) {
      mergern += `${reducerp.length}`;
      break;
   }
   let filedZ = 8840284.0 <= mbridgeD;
   do {
      mbridgeD += ((clockn ? 4 : 2));
      if (filedZ) {
         break;
      }
   } while (filedZ && (!clockn));
      libavcodech <<= Math.min(3, Math.abs(2));
      clockn = 39 > libavcodech || 98.7 > guide2;
      libavcodech |= parseInt(`${nbatrophyW}`);
      clockn = (100 <= ((!championP ? gesture8.length : 100) | gesture8.length));
      roomM = huaweif == 9.57 && !clockn;
   let g_unlockY = 8517485 >= libavcodech;
   do {
      libavcodech -= ((roomM ? 5 : 3) | parseInt(`${huaweif}`));
      if (g_unlockY) {
         break;
      }
   } while (((5.54 / (Math.max(1, huaweif))) > 5.84 || 1.17 > (huaweif / 5.54)) && g_unlockY);

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
