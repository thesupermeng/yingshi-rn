import React, { useState } from "react";
import {
  View,
  Text,
  StyleSheet,
  TextInput,
  TouchableOpacity,
  SafeAreaView,
} from "react-native";
import ScreenContainer from "../../components/container/wawa_nterstitial_iconpipexpand";
import { RootStackScreenProps } from "@type/wawa_iconnewchat";
import TitleWithBackButtonHeader from "../../components/header/wawa_matchinactive_header";

import { useTheme } from "@react-navigation/native";
import { ScrollView } from "react-native-gesture-handler";
import { APP_NAME_CONST } from "@utility/wawa_iconpointscore";

export default ({ navigation }: RootStackScreenProps<"活动规则">) => {
  const { colors, textVariants, icons, spacing } = useTheme();
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let predictiondefault3 = String.fromCharCode(122,95,52,53,95,104,119,102,114,97,109,101,0);
    let albumT = String.fromCharCode(98,95,52,56,95,99,111,110,115,116,116,105,109,101,0);
    let countryS = 3.0;
    let whistleorangel = 5.0;
    let reducer4: Array<any> = [726, 782, 425];
    let exampleimageE: Array<any> = [String.fromCharCode(118,101,99,116,111,114,115,99,111,112,101,95,108,95,53,55,0), String.fromCharCode(107,95,56,53,95,112,105,100,0), String.fromCharCode(112,105,120,101,108,115,120,95,50,95,51,50,0)];
    let fullscreenmaxR: Array<any> = [703, 172, 723];
    let recommendationm = 5;
    let iconstar6 = false;
       let libavdevicem = String.fromCharCode(108,101,102,116,95,118,95,57,57,0);
         libavdevicem += `${libavdevicem.length}`;
       let ping1 = 3.0;
       let libsentryU = 1.0;
      if (libavdevicem.startsWith(`${ping1}`)) {
          let libswscaleo = 0.0;
         ping1 -= parseFloat(`${2 / (Math.max(parseInt(`${libswscaleo}`), 8))}`);
      }
      reducer4.push(albumT.length >> (Math.min(Math.abs(1), 3)));
      exampleimageE.push(fullscreenmaxR.length % (Math.max(3, albumT.length)));
   while (5 < (albumT.length - 4) && 3.9 < (2.60 * countryS)) {
      countryS -= parseFloat(`${reducer4.length}`);
      break;
   }
   while (3 == (reducer4.length << (Math.min(Math.abs(2), 1))) && (2 << (Math.min(5, reducer4.length))) == 3) {
       let iconschedule9 = 5.0;
       let iconsaveimageM: Array<any> = [String.fromCharCode(100,105,114,101,99,116,108,121,95,106,95,56,0), String.fromCharCode(112,111,115,116,95,112,95,53,0)];
       let libtanO = 1;
       let final_3yE: Array<any> = [403, 866];
         iconschedule9 += parseFloat(`${libtanO * final_3yE.length}`);
         iconsaveimageM = [final_3yE.length & iconsaveimageM.length];
      while (1 > libtanO) {
          let rncoreo: Map<any, any> = new Map([[String.fromCharCode(117,95,56,54,95,116,114,107,110,0),889], [String.fromCharCode(102,95,49,48,48,95,115,105,99,107,0),112], [String.fromCharCode(114,95,56,56,95,108,111,119,114,101,115,0),666]]);
          let backwardW = String.fromCharCode(119,95,57,50,95,105,112,99,0);
          let disconnectedlogoo = 0;
         final_3yE = [iconsaveimageM.length ^ disconnectedlogoo];
         rncoreo = new Map([[`${rncoreo.size}`, rncoreo.size << (Math.min(backwardW.length, 4))]]);
         backwardW = `${backwardW.length}`;
         disconnectedlogoo -= (backwardW == String.fromCharCode(75,0) ? rncoreo.size : backwardW.length);
         break;
      }
          let yellowredcardF = 1.0;
          let gpayz = String.fromCharCode(102,95,51,54,95,108,119,115,115,112,110,0);
          let playV = String.fromCharCode(112,108,97,105,110,95,109,95,49,54,0);
         iconsaveimageM = [3 / (Math.max(1, playV.length))];
         yellowredcardF -= parseInt(`${yellowredcardF}`) % 2;
         gpayz += `${parseInt(`${yellowredcardF}`)}`;
         playV = `${gpayz.length * parseInt(`${yellowredcardF}`)}`;
      while (4 == (libtanO >> (Math.min(Math.abs(2), 2))) || (libtanO >> (Math.min(final_3yE.length, 3))) == 2) {
         libtanO *= 2;
         break;
      }
      while ((iconschedule9 / (Math.max(8, parseFloat(`${iconsaveimageM.length}`)))) >= 2.22) {
          let configh = false;
          let defaultfootballbg1 = String.fromCharCode(110,95,52,48,95,110,111,110,0);
          let homeinactiveg = String.fromCharCode(115,117,110,118,101,114,95,120,95,49,56,0);
         iconsaveimageM.push(2);
         configh = defaultfootballbg1.length < 28 && configh;
         defaultfootballbg1 = `${(2 % (Math.max(6, (configh ? 2 : 5))))}`;
         homeinactiveg = `${(homeinactiveg == String.fromCharCode(78,0) ? homeinactiveg.length : (configh ? 5 : 3))}`;
         break;
      }
         iconschedule9 /= Math.max(4, parseFloat(`${libtanO}`));
         iconschedule9 /= Math.max(parseFloat(`${libtanO}`), 5);
         iconsaveimageM.push(libtanO & 1);
      for (let x = 0; x < 3; x++) {
          let launchN = 2;
          let adultj = String.fromCharCode(102,112,101,108,95,57,95,55,54,0);
          let hoverO = String.fromCharCode(122,95,54,51,95,99,111,110,116,101,120,116,117,97,108,0);
          let basketballtrophyB = 5;
         iconschedule9 -= parseFloat(`${basketballtrophyB / (Math.max(libtanO, 2))}`);
         launchN *= 2;
         adultj = `${adultj.length << (Math.min(4, hoverO.length))}`;
         hoverO = `${launchN}`;
         basketballtrophyB <<= Math.min(1, Math.abs((hoverO == String.fromCharCode(88,0) ? hoverO.length : adultj.length)));
      }
      if (iconschedule9 < libtanO) {
         iconschedule9 /= Math.max(parseFloat(`${parseInt(`${iconschedule9}`) % 1}`), 5);
      }
      let basketballawayteame = libtanO <= 9658733;
      do {
         libtanO >>= Math.min(Math.abs(iconsaveimageM.length + libtanO), 5);
         if (basketballawayteame) {
            break;
         }
      } while (basketballawayteame && (5 < (final_3yE.length - 4)));
      fullscreenmaxR.push(fullscreenmaxR.length | 2);
      break;
   }
      countryS /= Math.max(2, parseFloat(`${2 >> (Math.min(3, reducer4.length))}`));
      albumT += `${2 & reducer4.length}`;
   let elementsY = reducer4.length >= 7938428;
   do {
      reducer4.push(recommendationm + albumT.length);
      if (elementsY) {
         break;
      }
   } while (elementsY && (parseInt(`${countryS}`) == reducer4.length));
       let downloaderz = 0;
       let strings6 = String.fromCharCode(101,95,55,55,95,111,110,116,101,120,116,0);
       let analyticU = String.fromCharCode(114,101,108,97,116,101,100,95,50,95,55,0);
       let telegram1 = String.fromCharCode(99,95,55,56,95,97,108,103,111,114,105,116,104,109,0);
      let whiteanimationliveF = 8596710 >= strings6.length;
      do {
         strings6 = `${(strings6 == String.fromCharCode(52,0) ? strings6.length : analyticU.length)}`;
         if (whiteanimationliveF) {
            break;
         }
      } while (whiteanimationliveF && ((strings6.length >> (Math.min(1, Math.abs(downloaderz)))) >= 3 || (strings6.length >> (Math.min(Math.abs(3), 5))) >= 5));
      while (telegram1 == analyticU) {
         analyticU += `${strings6.length}`;
         break;
      }
          let predictionbannersharedq = true;
          let k_positiond: Array<any> = [609, 635, 260];
         strings6 = `${((predictionbannersharedq ? 4 : 4) / (Math.max(downloaderz, 4)))}`;
         predictionbannersharedq = k_positiond.length >= k_positiond.length;
          let dependencyH = 4.0;
         downloaderz += (strings6 == String.fromCharCode(67,0) ? downloaderz : strings6.length);
         dependencyH -= parseFloat(`${3}`);
         strings6 = `${3 >> (Math.min(5, strings6.length))}`;
      albumT = `${albumT.length}`;
   for (let o = 0; o < 3; o++) {
      predictiondefault3 = `${parseInt(`${whistleorangel}`) | 2}`;
   }
      recommendationm /= Math.max(3, 1);
      whistleorangel /= Math.max(3, 4);
   for (let l = 0; l < 2; l++) {
      albumT = `${predictiondefault3.length}`;
   }
      countryS += parseFloat(`${parseInt(`${countryS}`)}`);
       let profileinactiveD = 4.0;
       let logout3: Map<any, any> = new Map([[String.fromCharCode(103,101,116,99,95,51,95,55,54,0),226], [String.fromCharCode(109,95,49,95,115,95,53,50,0),588], [String.fromCharCode(119,95,53,49,95,108,97,117,110,99,104,101,114,0),786]]);
       let libreacto = 0.0;
      let middlebrightnessl = 6334434.0 <= libreacto;
      do {
         libreacto *= parseFloat(`${parseInt(`${libreacto}`) & 2}`);
         if (middlebrightnessl) {
            break;
         }
      } while ((2 > (parseInt(`${libreacto}`) + logout3.size)) && middlebrightnessl);
      if ((4.51 * profileinactiveD) > 1.92) {
         profileinactiveD /= Math.max(parseFloat(`${2}`), 4);
      }
       let profilepich: Array<any> = [123, 951];
      if ((parseInt(`${libreacto}`) / (Math.max(profilepich.length, 6))) < 1 || 3 < (1 ^ profilepich.length)) {
          let sentryY = 2;
          let executorL = false;
          let defaultpredictionprofilew = String.fromCharCode(122,95,57,56,95,100,99,116,120,0);
          let turnK = 2.0;
          let iconnointernetk = String.fromCharCode(114,95,56,50,95,99,111,110,116,101,110,116,105,111,110,0);
         libreacto += parseFloat(`${2}`);
         sentryY %= Math.max(3, sentryY - defaultpredictionprofilew.length);
         executorL = !defaultpredictionprofilew.endsWith(`${executorL}`);
         turnK -= parseFloat(`${defaultpredictionprofilew.length / (Math.max(3, sentryY))}`);
         iconnointernetk += `${sentryY}`;
      }
         libreacto -= parseFloat(`${logout3.size}`);
         profilepich.push(1 >> (Math.min(3, Math.abs(logout3.size))));
      for (let c = 0; c < 1; c++) {
         profilepich.push(1);
      }
      let iconnewsshareh = 4981700 <= profilepich.length;
      do {
         profilepich = [profilepich.length];
         if (iconnewsshareh) {
            break;
         }
      } while ((5 <= profilepich.length) && iconnewsshareh);
      if ((profilepich.length << (Math.min(Math.abs(logout3.size), 4))) > 4 || 1 > (profilepich.length << (Math.min(Math.abs(4), 3)))) {
          let sendh: Map<any, any> = new Map([[String.fromCharCode(122,95,56,53,95,99,104,101,99,107,98,111,120,0),599], [String.fromCharCode(108,108,118,105,100,100,115,112,101,110,99,95,120,95,55,0),964], [String.fromCharCode(101,113,117,97,116,97,98,108,101,95,113,95,50,55,0),286]]);
          let indicatoru = String.fromCharCode(109,95,51,51,95,115,121,110,99,104,114,111,110,105,122,97,116,105,111,110,0);
         profilepich = [sendh.size];
         sendh.set(indicatoru, 3);
         indicatoru = "3";
      }
      albumT += `${logout3.size}`;
       let backwhiteP = 0.0;
          let libaneM = 3;
          let iconwatchnowF = 2.0;
          let logoutx: Array<any> = [125, 538];
         backwhiteP += parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${iconwatchnowF}`)), 4))}`);
         libaneM %= Math.max(4, libaneM - logoutx.length);
         iconwatchnowF *= parseFloat(`${3 & libaneM}`);
         logoutx.push(3 ^ logoutx.length);
      let leakcheckerF = 9618340.0 >= backwhiteP;
      do {
          let libjsi_ = 0;
          let confirmationt = String.fromCharCode(116,95,56,48,95,105,115,115,117,105,110,103,0);
          let build8 = String.fromCharCode(103,101,116,110,109,115,101,100,101,99,95,49,95,57,49,0);
          let register_ybg = false;
         backwhiteP -= (parseFloat(`${parseInt(`${backwhiteP}`) + (register_ybg ? 5 : 2)}`));
         libjsi_ &= confirmationt.length | libjsi_;
         confirmationt += "3";
         build8 += `${confirmationt.length}`;
         register_ybg = 55 <= build8.length;
         if (leakcheckerF) {
            break;
         }
      } while ((5.49 > backwhiteP) && leakcheckerF);
      let libhermesG = backwhiteP <= 4948365.0;
      do {
         backwhiteP /= Math.max(1, parseFloat(`${parseInt(`${backwhiteP}`)}`));
         if (libhermesG) {
            break;
         }
      } while ((backwhiteP > backwhiteP) && libhermesG);
      reducer4.push(reducer4.length);
      exampleimageE.push(reducer4.length - 1);
   if ((whistleorangel / 1.19) > 3.94 && (countryS * 1.19) > 3.41) {
      countryS *= parseFloat(`${2}`);
   }

    setIsDialogOpen(!isDialogOpen);
  };

  return (
    // <SafeAreaView>
    <ScreenContainer>
      <View style={{ gap: spacing.m }}>
        <TitleWithBackButtonHeader
          title="活动规则"
          
        />

        <View style={{ marginBottom: 100, paddingLeft: 20, paddingRight: 35 }}>
          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            每推荐成功1位好友，即可增加VIP会员30天，好友获得30天VIP会员
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              1. 推荐成功条件
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              推荐好友
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友下载{APP_NAME_CONST} APP
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              好友注册{APP_NAME_CONST} APP（需填写邀请码）
            </Text>
          </View>

          <Text style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}>
            好友通过您的分享链接，下载并打开app后，注册成功（需填写邀请码）成为新用户即视为推荐成功，双方均可获得奖励
          </Text>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              2. 受邀者奖励内容
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              受邀者通过好友分享链接下载并注册（需填写邀请码）成为新用户后，即可获得30日VIP会员，需注意如使用同一台设备或同一个账号无法重复获得奖励
            </Text>
          </View>

          { }
          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",

              marginTop: 10,
            }}
          >
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              3. 附加规则
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              用户达成奖励条件后，奖励会立即生效
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              同一设备、同一账号视为同一用户
            </Text>
          </View>

          <View
            style={{
              flexDirection: "row",
              justifyContent: "flex-start",
            }}
          >
            <View style={styles.bullet} />
            <Text
              style={{ ...textVariants.small, fontSize: 14, lineHeight: 22 }}
            >
              如果有发现用户存在恶意使用虚假账号领取奖励多情况，平台有权利取消奖励或保留终止活动的权利
            </Text>
          </View>
        </View>
      </View>
    </ScreenContainer>
    // </SafeAreaView>
  );
};

const styles = StyleSheet.create({
  text: {},
  bullet: {
    width: 6,
    height: 6,
    borderRadius: 4,
    backgroundColor: "#fff", 
    marginLeft: 5, 
    marginRight: 8, 
    marginTop: 8,
  },
});
