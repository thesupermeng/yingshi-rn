import React, { useState } from 'react';
import { View, TouchableOpacity, Text, StyleSheet, FlatList } from 'react-native';
import ScreenContainer from '../../components/container/yys_matches';
import { RootStackScreenProps } from '@type';
import { useTheme } from '@react-navigation/native';
import { useAppDispatch, useAppSelector } from '@hooks';
import { yysIconclosewhiteBaiduads } from '@redux';

import TitleWithBackButtonHeader from '../../components/header/yys_away_predictionloss_header';
import { yysAnalytics, yysIconpointscoreCountdown } from '@redux';
import { removeVodsFromHistory, playVod } from '@redux';
import VodHistoryCard from '../../components/vod/yys_header_bgvipsport';
import { yysPenaltyshoot } from '@type';
import { Button } from '@rneui/themed';
import ConfirmationModal from '../../components/modal/yys_twitter';
import VodLiveStationListVertical from '../../components/vod/yys_basketballplayerplaceholder';
import EmptyList from '../../components/common/yys_benefit';
import { ScrollView } from 'react-native-gesture-handler';

type yysInternetAirbnbstarselected = {
  item: yysAnalytics;
};
export default ({ navigation, route }: RootStackScreenProps<'电视台列表'>) => {
  const { liveStationItemList } = route.params;
  const { colors, textVariants, icons, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const vodReducer: yysIconpointscoreCountdown = useAppSelector(
    ({ vodReducer }: yysIconclosewhiteBaiduads) => vodReducer,
  );
  const history = vodReducer.history;
  const [isEditing, setIsEditing] = useState(false);
  const [removeHistory, setRemoveHistory] = useState<Array<yysPenaltyshoot>>([]);
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let libffmpegkitD = String.fromCharCode(106,95,57,54,95,104,121,112,111,116,104,101,115,101,115,0);
    let cross_ = 3.0;
    let inouttargetyellow5 = 5;
    let positionfield4 = String.fromCharCode(99,111,97,114,115,101,99,97,110,100,105,100,97,116,101,95,97,95,51,55,0);
    let notificationf = String.fromCharCode(113,117,105,122,95,48,95,51,53,0);
    let smallsoundf = String.fromCharCode(112,95,57,55,95,114,101,103,105,111,110,115,0);
    let actionsK: Map<any, any> = new Map([[String.fromCharCode(98,95,54,50,95,112,97,108,101,116,116,101,0),845], [String.fromCharCode(109,95,52,48,95,109,117,108,116,105,101,110,100,0),359]]);
    let applicationf: Map<any, any> = new Map([[String.fromCharCode(113,95,54,95,118,115,116,97,116,115,0),String.fromCharCode(108,95,50,54,95,97,116,116,97,99,104,0)], [String.fromCharCode(97,109,117,108,116,105,95,118,95,50,54,0),String.fromCharCode(104,95,52,53,95,115,117,98,116,114,97,99,116,105,111,110,0)]]);
    let gemfileu = String.fromCharCode(104,95,52,55,95,115,117,98,112,101,108,0);
    let imagenetworkerra = 5.0;
    let entryI = true;
    let popupe = String.fromCharCode(111,95,49,57,95,98,108,97,110,107,0);
    let iconeyez = 3;
    let libavfilter3 = true;
   for (let x = 0; x < 3; x++) {
       let scrollviews: Array<any> = [778, 586];
       let brightnessD = String.fromCharCode(114,111,117,116,101,115,95,54,95,56,48,0);
      for (let m = 0; m < 1; m++) {
          let iconeditm: Map<any, any> = new Map([[String.fromCharCode(103,95,52,57,95,108,115,112,108,112,99,0),String.fromCharCode(110,95,52,48,95,112,105,110,110,101,100,0)], [String.fromCharCode(99,108,101,97,114,95,122,95,53,56,0),String.fromCharCode(120,95,50,55,95,100,115,116,115,117,98,0)]]);
         brightnessD = `${scrollviews.length / (Math.max(brightnessD.length, 5))}`;
         iconeditm = new Map([[`${iconeditm.size}`, 1 & iconeditm.size]]);
      }
          let interstitialO = false;
          let inouttargetyellowq = true;
          let overy = 2.0;
         scrollviews = [3 | parseInt(`${overy}`)];
         interstitialO = (interstitialO ? !inouttargetyellowq : !interstitialO);
         inouttargetyellowq = !interstitialO;
         overy /= Math.max(3, 2);
         brightnessD += `${brightnessD.length}`;
      while (4 == (1 * scrollviews.length)) {
         brightnessD = `${scrollviews.length >> (Math.min(Math.abs(2), 3))}`;
         break;
      }
         scrollviews = [scrollviews.length / 2];
         scrollviews = [brightnessD.length >> (Math.min(5, scrollviews.length))];
      inouttargetyellow5 >>= Math.min(Math.abs(1), 2);
   }
   if (popupe != String.fromCharCode(87,0)) {
      positionfield4 = `${((entryI ? 2 : 5) >> (Math.min(Math.abs(2), 3)))}`;
   }
      notificationf = `${parseInt(`${imagenetworkerra}`)}`;
   let benefitG = popupe.length >= 6257832;
   do {
       let expiredx = true;
       let refreshborderlessk = 1;
       let incidentp = String.fromCharCode(101,115,99,97,112,101,95,52,95,49,54,0);
      for (let e = 0; e < 2; e++) {
          let mbbannery = String.fromCharCode(99,111,108,115,112,97,110,95,52,95,49,55,0);
          let libhermesw = 5.0;
          let iconL = String.fromCharCode(116,101,114,109,105,110,97,116,101,100,95,110,95,50,55,0);
         refreshborderlessk ^= ((expiredx ? 5 : 3) / (Math.max(1, 6)));
         mbbannery += "3";
         libhermesw -= parseFloat(`${parseInt(`${libhermesw}`) % 1}`);
         iconL = `${(iconL == String.fromCharCode(73,0) ? parseInt(`${libhermesw}`) : iconL.length)}`;
      }
      if ((1 - refreshborderlessk) > 4) {
         refreshborderlessk -= refreshborderlessk << (Math.min(Math.abs(3), 5));
      }
      if ((4 & incidentp.length) == 3 || (incidentp.length & 4) == 4) {
         incidentp += "3";
      }
      while ((5 | refreshborderlessk) >= 5 && 5 >= (refreshborderlessk | incidentp.length)) {
         refreshborderlessk /= Math.max(3, 3);
         break;
      }
         refreshborderlessk ^= (incidentp == String.fromCharCode(70,0) ? (expiredx ? 1 : 4) : incidentp.length);
      for (let i = 0; i < 2; i++) {
          let delegate_oA = String.fromCharCode(104,108,115,112,108,97,121,108,105,115,116,95,111,95,57,0);
         expiredx = incidentp.length <= refreshborderlessk;
         delegate_oA = `${delegate_oA.length ^ 3}`;
      }
         expiredx = refreshborderlessk == 43;
      for (let l = 0; l < 1; l++) {
         expiredx = refreshborderlessk >= incidentp.length;
      }
         refreshborderlessk >>= Math.min(5, Math.abs(2));
      popupe += `${incidentp.length + 2}`;
      if (benefitG) {
         break;
      }
   } while (benefitG && (popupe.startsWith(gemfileu)));
   let holdero = inouttargetyellow5 >= 5947462;
   do {
      inouttargetyellow5 %= Math.max(2, ((entryI ? 3 : 1) >> (Math.min(Math.abs(iconeyez), 3))));
      if (holdero) {
         break;
      }
   } while (holdero && (inouttargetyellow5 < 2));
      cross_ /= Math.max(inouttargetyellow5 >> (Math.min(Math.abs(parseInt(`${imagenetworkerra}`)), 2)), 5);
      smallsoundf = "3";
       let gifgoalbgX: Map<any, any> = new Map([[String.fromCharCode(97,118,102,111,114,109,97,116,109,97,112,112,101,114,116,101,115,116,115,95,120,95,50,55,0),14], [String.fromCharCode(115,117,98,116,114,97,99,116,105,111,110,95,110,95,52,0),110], [String.fromCharCode(97,116,116,97,99,104,95,104,95,54,48,0),384]]);
       let langi = String.fromCharCode(99,95,56,48,95,119,97,108,115,104,120,0);
      for (let n = 0; n < 3; n++) {
          let floaterG = false;
          let close8 = false;
         langi = "2";
         floaterG = (!floaterG ? !floaterG : floaterG);
         close8 = !floaterG || !floaterG;
      }
      if (langi.length < gifgoalbgX.size) {
         langi += `${gifgoalbgX.size ^ 2}`;
      }
       let cornerkickd = String.fromCharCode(117,110,105,113,117,101,100,95,98,95,51,57,0);
       let benefitc = String.fromCharCode(112,97,114,116,105,116,105,111,110,105,110,103,95,121,95,55,53,0);
       let megaphoneQ = String.fromCharCode(99,114,111,112,95,115,95,56,54,0);
       let vertical2 = String.fromCharCode(99,111,110,116,114,111,108,108,101,114,95,107,95,53,52,0);
      while (cornerkickd != String.fromCharCode(74,0)) {
         megaphoneQ = `${1 ^ megaphoneQ.length}`;
         break;
      }
         langi = `${(String.fromCharCode(107,0) == vertical2 ? vertical2.length : gifgoalbgX.size)}`;
      libffmpegkitD += `${(String.fromCharCode(77,0) == positionfield4 ? positionfield4.length : applicationf.size)}`;
       let othermatchdetailbgL = 3;
       let backgroundR = String.fromCharCode(112,114,101,98,117,102,95,51,95,51,50,0);
         backgroundR += `${(String.fromCharCode(122,0) == backgroundR ? backgroundR.length : othermatchdetailbgL)}`;
      let linek = 6350907 >= othermatchdetailbgL;
      do {
          let livenodatabgimgd = 4.0;
         othermatchdetailbgL *= backgroundR.length >> (Math.min(4, Math.abs(othermatchdetailbgL)));
         livenodatabgimgd += parseFloat(`${3}`);
         if (linek) {
            break;
         }
      } while ((2 <= (othermatchdetailbgL ^ backgroundR.length)) && linek);
      while ((othermatchdetailbgL >> (Math.min(Math.abs(5), 4))) > 3 || (othermatchdetailbgL >> (Math.min(backgroundR.length, 5))) > 5) {
         othermatchdetailbgL -= othermatchdetailbgL - backgroundR.length;
         break;
      }
      for (let x = 0; x < 3; x++) {
         othermatchdetailbgL /= Math.max(2, othermatchdetailbgL ^ 1);
      }
       let modalv: Array<any> = [973, 245];
       let gifgoalc: Array<any> = [728, 972];
      while ((gifgoalc.length % (Math.max(5, 4))) >= 1) {
         gifgoalc.push(2);
         break;
      }
      positionfield4 = `${1 % (Math.max(inouttargetyellow5, 3))}`;
   for (let s = 0; s < 2; s++) {
      positionfield4 += `${3 & parseInt(`${cross_}`)}`;
   }
   let popupU = gemfileu.length <= 6833512;
   do {
      gemfileu = `${1 ^ libffmpegkitD.length}`;
      if (popupU) {
         break;
      }
   } while ((4 == (1 / (Math.max(6, gemfileu.length)))) && popupU);
   for (let s = 0; s < 3; s++) {
       let champion0: Map<any, any> = new Map([[String.fromCharCode(115,116,97,114,95,121,95,51,48,0),String.fromCharCode(112,111,114,116,95,109,95,50,48,0)], [String.fromCharCode(116,95,56,49,95,114,101,97,108,109,0),String.fromCharCode(97,99,99,101,112,116,115,95,119,95,53,48,0)]]);
       let k_player8 = String.fromCharCode(108,105,115,116,101,110,95,97,95,51,56,0);
         champion0 = new Map([[`${champion0.size}`, k_player8.length & champion0.size]]);
          let yellowtored4: Array<any> = [String.fromCharCode(100,95,49,52,95,115,117,112,112,111,114,116,0), String.fromCharCode(97,95,52,95,109,111,100,101,0)];
          let iconorientationY = 1.0;
         k_player8 = `${k_player8.length / 2}`;
         yellowtored4.push(yellowtored4.length);
         iconorientationY -= parseFloat(`${parseInt(`${iconorientationY}`)}`);
      while (1 >= (k_player8.length * 3)) {
         champion0 = new Map([[`${champion0.size}`, champion0.size]]);
         break;
      }
         k_player8 = `${3 | k_player8.length}`;
      for (let w = 0; w < 3; w++) {
         k_player8 = `${k_player8.length + 3}`;
      }
          let orangeuparrowr = String.fromCharCode(104,119,100,101,118,105,99,101,95,110,95,54,48,0);
          let iconadslinkC: Map<any, any> = new Map([[String.fromCharCode(102,95,50,55,95,119,111,114,100,108,101,110,0),956], [String.fromCharCode(101,120,112,105,114,97,116,105,111,110,115,95,48,95,52,55,0),249]]);
         k_player8 = `${k_player8.length / (Math.max(3, orangeuparrowr.length))}`;
         orangeuparrowr = `${iconadslinkC.size | 3}`;
         iconadslinkC = new Map([[`${iconadslinkC.size}`, iconadslinkC.size % 3]]);
      gemfileu = `${3 - notificationf.length}`;
   }
      positionfield4 = `${popupe.length >> (Math.min(notificationf.length, 3))}`;
   for (let l = 0; l < 3; l++) {
      inouttargetyellow5 <<= Math.min(Math.abs(inouttargetyellow5 | 2), 1);
   }
       let gemfileH = String.fromCharCode(100,101,97,100,108,111,99,107,101,100,95,111,95,55,0);
      for (let w = 0; w < 2; w++) {
          let round6: Array<any> = [76, 86];
          let libreactnativeblobH = String.fromCharCode(116,95,54,52,95,102,117,110,99,116,105,111,110,115,0);
          let basketballmatchdetailbgq: Array<any> = [318, 718];
          let topicd = 5;
          let imageactionlivez: Map<any, any> = new Map([[String.fromCharCode(115,95,53,49,95,99,111,111,107,105,101,0),false ], [String.fromCharCode(117,95,53,52,95,119,101,98,109,105,100,115,0),false ]]);
         gemfileH += `${libreactnativeblobH.length}`;
         round6.push(imageactionlivez.size);
         libreactnativeblobH += "2";
         basketballmatchdetailbgq.push(1 % (Math.max(8, round6.length)));
         topicd &= round6.length;
         imageactionlivez = new Map([[`${round6.length}`, round6.length >> (Math.min(Math.abs(1), 2))]]);
      }
         gemfileH += `${gemfileH.length / 1}`;
          let step1 = 4.0;
         gemfileH = `${parseInt(`${step1}`) % (Math.max(4, gemfileH.length))}`;
      popupe += `${actionsK.size | 3}`;
       let iconsettingj: Map<any, any> = new Map([[String.fromCharCode(115,95,51,56,95,109,97,112,115,116,114,105,110,103,0),true ], [String.fromCharCode(104,116,108,116,95,106,95,49,52,0),false ], [String.fromCharCode(117,95,55,95,97,99,99,101,112,116,115,0),false ]]);
       let statisticsactiveA = 0;
       let disconnectedlogol = String.fromCharCode(115,116,114,102,116,105,109,101,95,48,95,51,49,0);
      for (let l = 0; l < 1; l++) {
         iconsettingj.set(`${statisticsactiveA}`, statisticsactiveA / 2);
      }
      for (let n = 0; n < 2; n++) {
         statisticsactiveA /= Math.max(2 & iconsettingj.size, 3);
      }
      if (2 >= disconnectedlogol.length) {
         disconnectedlogol += `${iconsettingj.size << (Math.min(Math.abs(2), 4))}`;
      }
         iconsettingj = new Map([[`${iconsettingj.size}`, 3 * statisticsactiveA]]);
      while ((statisticsactiveA / 4) <= 4) {
         iconsettingj.set(disconnectedlogol, 1);
         break;
      }
      if (statisticsactiveA <= 2) {
          let templateprocessorq: Array<any> = [36, 502];
          let leagueu = String.fromCharCode(115,97,118,101,112,111,105,110,116,115,95,51,95,52,54,0);
          let librrcG = String.fromCharCode(115,117,112,112,114,101,115,115,101,100,95,48,95,55,50,0);
          let popupI = false;
          let incidentv = String.fromCharCode(102,105,97,116,95,57,95,51,57,0);
         statisticsactiveA <<= Math.min(1, Math.abs(iconsettingj.size));
         templateprocessorq.push(leagueu.length);
         leagueu += `${templateprocessorq.length}`;
         librrcG = `${(incidentv == String.fromCharCode(103,0) ? librrcG.length : incidentv.length)}`;
         popupI = 87 < templateprocessorq.length || String.fromCharCode(109,0) == leagueu;
      }
       let shirtD = String.fromCharCode(113,95,56,54,95,118,97,114,105,110,102,111,0);
       let statsM = String.fromCharCode(111,102,102,108,111,97,100,95,120,95,49,48,48,0);
      if (statsM != String.fromCharCode(110,0) || shirtD.length == 4) {
         statsM = `${shirtD.length | statsM.length}`;
      }
      while (2 < (statisticsactiveA * 1) || 2 < (1 * disconnectedlogol.length)) {
         disconnectedlogol = `${statisticsactiveA}`;
         break;
      }
      imagenetworkerra -= parseFloat(`${libffmpegkitD.length * 2}`);
   if ((parseInt(`${imagenetworkerra}`) / (Math.max(applicationf.size, 5))) <= 5) {
      applicationf = new Map([[`${iconeyez}`, 3]]);
   }
   while (notificationf.startsWith(`${actionsK.size}`)) {
      notificationf += `${actionsK.size ^ notificationf.length}`;
      break;
   }
   let nend8 = String.fromCharCode(108,95,102,106,109,116,119,101,112,120,0) == smallsoundf;
   do {
      smallsoundf += `${((libavfilter3 ? 5 : 3) | inouttargetyellow5)}`;
      if (nend8) {
         break;
      }
   } while ((2 > (smallsoundf.length >> (Math.min(Math.abs(3), 4)))) && nend8);
   for (let g = 0; g < 3; g++) {
       let iconfeedbackD: Array<any> = [701, 964, 942];
       let matchinactivex: Array<any> = [188, 199, 960];
       let networkk = String.fromCharCode(112,114,111,98,97,98,108,101,95,49,95,57,51,0);
      for (let n = 0; n < 2; n++) {
          let phoney = true;
          let cricketV = 2;
          let helperz = true;
         networkk = "2";
         phoney = 4 == cricketV && !phoney;
         cricketV /= Math.max(3 ^ cricketV, 4);
         helperz = !phoney;
      }
         iconfeedbackD.push(1 << (Math.min(1, matchinactivex.length)));
         iconfeedbackD = [iconfeedbackD.length];
          let subtextp = false;
          let statisticsactiveT = 3;
          let stationsU = 1.0;
         networkk += `${matchinactivex.length % 3}`;
         subtextp = statisticsactiveT == 48;
         statisticsactiveT <<= Math.min(1, Math.abs(parseInt(`${stationsU}`) - 3));
         stationsU -= ((subtextp ? 2 : 5) & parseInt(`${stationsU}`));
         matchinactivex.push(iconfeedbackD.length);
      if (3 >= (iconfeedbackD.length * 1)) {
         matchinactivex.push((networkk == String.fromCharCode(97,0) ? iconfeedbackD.length : networkk.length));
      }
      while ((1 & iconfeedbackD.length) > 2 && (iconfeedbackD.length & matchinactivex.length) > 1) {
         iconfeedbackD.push(networkk.length);
         break;
      }
          let pushV = 3;
          let canvas3 = true;
         networkk = `${matchinactivex.length << (Math.min(Math.abs(3), 3))}`;
         pushV ^= 1;
         canvas3 = pushV == 20 || canvas3;
          let verticalE = 2.0;
         matchinactivex = [3];
         verticalE -= parseFloat(`${parseInt(`${verticalE}`) / (Math.max(3, parseInt(`${verticalE}`)))}`);
      libavfilter3 = notificationf.length > parseInt(`${imagenetworkerra}`);
   }
      libffmpegkitD = `${gemfileu.length}`;
      actionsK.set(`${inouttargetyellow5}`, inouttargetyellow5 ^ 1);
   if (4 >= (2 | notificationf.length) && 4 >= (notificationf.length | 2)) {
       let libreactnativeblobF = 0.0;
       let redgoall = String.fromCharCode(108,95,55,48,95,114,97,116,101,0);
       let downr: Array<any> = [String.fromCharCode(121,95,55,52,95,99,116,97,98,108,101,115,0), String.fromCharCode(112,114,101,99,111,109,112,95,54,95,51,54,0)];
       let sportA = 4.0;
       let viewers = String.fromCharCode(105,95,53,48,95,115,111,110,111,0);
          let zhengpianR = String.fromCharCode(108,105,115,116,101,110,101,114,95,112,95,49,56,0);
          let darkb: Map<any, any> = new Map([[String.fromCharCode(118,105,122,105,101,114,95,57,95,49,54,0),828], [String.fromCharCode(111,95,55,53,95,111,112,101,110,115,115,108,99,111,110,102,0),390], [String.fromCharCode(114,101,100,97,99,116,95,54,95,56,0),626]]);
         sportA += parseFloat(`${3}`);
         zhengpianR += `${2 * zhengpianR.length}`;
         darkb = new Map([[`${darkb.size}`, zhengpianR.length << (Math.min(1, Math.abs(darkb.size)))]]);
         libreactnativeblobF -= (parseFloat(`${viewers == String.fromCharCode(84,0) ? parseInt(`${libreactnativeblobF}`) : viewers.length}`));
      let gifgoalbgT = 8164467 <= redgoall.length;
      do {
         redgoall = "3";
         if (gifgoalbgT) {
            break;
         }
      } while ((viewers == String.fromCharCode(50,0)) && gifgoalbgT);
      if (4 < redgoall.length) {
         redgoall += `${2 + parseInt(`${sportA}`)}`;
      }
          let iconpipshrinkL: Array<any> = [156, 317];
          let stationsF = true;
          let castingb: Array<any> = [String.fromCharCode(100,105,109,101,110,115,105,116,111,110,115,95,48,95,52,56,0), String.fromCharCode(97,95,49,49,95,98,111,111,107,107,101,101,112,105,110,103,0), String.fromCharCode(99,95,57,95,111,99,97,108,105,122,97,116,105,111,110,0)];
         downr = [redgoall.length];
         iconpipshrinkL = [((stationsF ? 2 : 1) / (Math.max(2, castingb.length)))];
         stationsF = (70 > (iconpipshrinkL.length ^ (stationsF ? iconpipshrinkL.length : 70)));
         castingb = [castingb.length];
         redgoall = `${1 & parseInt(`${libreactnativeblobF}`)}`;
          let styleR = true;
         sportA /= Math.max(1, (parseFloat(`${parseInt(`${sportA}`) >> (Math.min(2, Math.abs((styleR ? 1 : 1))))}`)));
      if (5.45 > (sportA / (Math.max(3.60, 1))) && (3.60 / (Math.max(1, sportA))) > 4.25) {
         downr.push(parseInt(`${sportA}`));
      }
       let sell6 = 3.0;
      let downloads = 6732335 >= downr.length;
      do {
         downr = [viewers.length];
         if (downloads) {
            break;
         }
      } while (downloads && (downr.includes(libreactnativeblobF)));
      for (let t = 0; t < 2; t++) {
          let pauset: Map<any, any> = new Map([[String.fromCharCode(104,95,57,53,95,99,97,116,99,104,97,98,108,101,0),738], [String.fromCharCode(108,95,50,51,95,112,97,105,110,116,105,110,103,0),706]]);
          let short_0H = String.fromCharCode(117,110,99,111,110,115,117,109,101,100,95,122,95,51,50,0);
          let notificationfillemptyA = String.fromCharCode(112,97,100,100,105,110,103,95,105,95,56,53,0);
          let tooltipsx = 1.0;
          let static_muP = true;
         sportA -= parseFloat(`${1 | pauset.size}`);
         pauset.set(notificationfillemptyA, 3 >> (Math.min(5, short_0H.length)));
         short_0H = "2";
         notificationfillemptyA = `${short_0H.length / 3}`;
         tooltipsx += (String.fromCharCode(102,0) == notificationfillemptyA ? notificationfillemptyA.length : parseInt(`${tooltipsx}`));
         static_muP = 56 <= notificationfillemptyA.length;
      }
         sell6 += redgoall.length;
         viewers += `${viewers.length}`;
      if (5 == downr.length) {
         sportA += (parseFloat(`${String.fromCharCode(53,0) == viewers ? downr.length : viewers.length}`));
      }
       let textP = 1;
       let hoverN = 2;
      actionsK = new Map([[`${actionsK.size}`, positionfield4.length | actionsK.size]]);
   }
   for (let s = 0; s < 3; s++) {
       let homeinactivem = 5.0;
       let showmoreM = String.fromCharCode(102,95,55,56,95,111,112,101,110,105,110,103,0);
       let footballfield9 = 4.0;
         showmoreM += `${(String.fromCharCode(85,0) == showmoreM ? parseInt(`${footballfield9}`) : showmoreM.length)}`;
          let applicationN = String.fromCharCode(117,110,99,108,101,115,95,48,95,51,53,0);
          let containerT = 5.0;
         footballfield9 /= Math.max(3, (parseFloat(`${String.fromCharCode(106,0) == applicationN ? parseInt(`${footballfield9}`) : applicationN.length}`)));
         containerT /= Math.max(parseFloat(`${2 - parseInt(`${containerT}`)}`), 1);
       let predictionv: Array<any> = [String.fromCharCode(101,97,103,101,114,95,112,95,55,52,0), String.fromCharCode(116,95,57,49,95,103,111,98,98,108,101,0)];
       let selectionu: Array<any> = [572, 173, 568];
       let bell2: Map<any, any> = new Map([[String.fromCharCode(114,95,50,57,95,101,110,117,109,101,114,97,116,101,0),969], [String.fromCharCode(109,111,99,107,95,107,95,54,54,0),361], [String.fromCharCode(117,95,57,48,95,104,115,99,114,111,108,108,0),320]]);
       let settingW = false;
       let foundB = true;
      while (4 < (bell2.size - 2) || 4 < (bell2.size - 2)) {
         predictionv.push(predictionv.length - 1);
         break;
      }
      if (5 >= (selectionu.length * 1) || foundB) {
         selectionu.push(((foundB ? 1 : 4) | showmoreM.length));
      }
          let middlesound2 = false;
          let episodesu = 1;
          let libfbN = 2;
         settingW = !settingW;
         middlesound2 = libfbN > episodesu;
         episodesu += libfbN;
         bell2 = new Map([[`${selectionu.length}`, selectionu.length]]);
      positionfield4 += `${(gemfileu == String.fromCharCode(50,0) ? gemfileu.length : parseInt(`${footballfield9}`))}`;
      homeinactivem += parseFloat(`${parseInt(`${homeinactivem}`) << (Math.min(5, Math.abs(parseInt(`${homeinactivem}`))))}`);
   }
      popupe += `${((entryI ? 5 : 5))}`;
       let g_lock7 = String.fromCharCode(98,111,117,110,100,97,108,108,95,106,95,53,57,0);
       let k_lock3 = 4;
       let schedulerv = String.fromCharCode(114,116,109,112,99,114,121,112,116,95,99,95,49,56,0);
      for (let d = 0; d < 1; d++) {
          let textlayoutmanager4 = 2.0;
          let episodesN = String.fromCharCode(98,121,116,101,99,111,100,101,95,115,95,50,52,0);
          let mapbufferY = 0;
         g_lock7 = "2";
         textlayoutmanager4 *= (parseFloat(`${episodesN == String.fromCharCode(70,0) ? parseInt(`${textlayoutmanager4}`) : episodesN.length}`));
         mapbufferY &= 2;
      }
      if ((3 >> (Math.min(4, Math.abs(k_lock3)))) < 1) {
         k_lock3 *= k_lock3 / (Math.max(2, schedulerv.length));
      }
      for (let w = 0; w < 2; w++) {
         g_lock7 = `${g_lock7.length % 1}`;
      }
      while (2 >= (4 + schedulerv.length) || (k_lock3 + 4) >= 4) {
         schedulerv += `${(String.fromCharCode(76,0) == schedulerv ? k_lock3 : schedulerv.length)}`;
         break;
      }
      let footballfieldG = 8793182 >= k_lock3;
      do {
         k_lock3 >>= Math.min(Math.abs(1 + g_lock7.length), 2);
         if (footballfieldG) {
            break;
         }
      } while ((k_lock3 <= schedulerv.length) && footballfieldG);
         schedulerv = `${g_lock7.length / 3}`;
      while (schedulerv == g_lock7) {
         g_lock7 += `${g_lock7.length - 1}`;
         break;
      }
         k_lock3 &= (schedulerv == String.fromCharCode(75,0) ? schedulerv.length : k_lock3);
       let redirecto = 2.0;
       let rncoreW = 3.0;
      cross_ /= Math.max(5, 3);
   for (let n = 0; n < 2; n++) {
      smallsoundf += `${((entryI ? 5 : 4))}`;
   }
   for (let w = 0; w < 2; w++) {
       let pushr = 3;
       let specN = String.fromCharCode(112,114,101,100,95,107,95,51,51,0);
         pushr -= specN.length - 2;
      while ((4 + pushr) >= 2 && (pushr + specN.length) >= 4) {
         specN += "1";
         break;
      }
      while (3 >= (pushr | specN.length)) {
         pushr /= Math.max(3, 1);
         break;
      }
          let clubN = 4.0;
         specN = "1";
         clubN /= Math.max(4, parseInt(`${clubN}`));
      while (pushr >= 5) {
          let analyticsx = String.fromCharCode(106,95,57,48,95,108,105,98,115,119,105,102,116,0);
          let aboutU = String.fromCharCode(116,105,108,101,95,50,95,57,50,0);
          let switch_1ka = 2.0;
         specN = `${specN.length | pushr}`;
         analyticsx += `${analyticsx.length << (Math.min(3, Math.abs(parseInt(`${switch_1ka}`))))}`;
         aboutU += `${analyticsx.length}`;
         switch_1ka *= parseFloat(`${parseInt(`${switch_1ka}`)}`);
         break;
      }
      for (let d = 0; d < 2; d++) {
         pushr ^= specN.length;
      }
      applicationf = new Map([[`${applicationf.size}`, inouttargetyellow5 >> (Math.min(Math.abs(applicationf.size), 1))]]);
   }
      iconeyez /= Math.max(1 >> (Math.min(3, Math.abs(inouttargetyellow5))), 4);

    setIsDialogOpen(!isDialogOpen);
  };

  const toggleHistory = (vod: yysPenaltyshoot) => {
    const filtered = removeHistory.filter(x => x.vod_id !== vod.vod_id);
    if (filtered.length === removeHistory.length) {
      setRemoveHistory([vod, ...removeHistory]);
    } else {
      setRemoveHistory(filtered);
    }
  };
  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader
        title="电视台"
      
      />
      <ScrollView style={{ marginBottom: spacing.xl }}>
        {liveStationItemList != undefined && liveStationItemList.length > 0 && (
          <View
            style={{
              ...styles.descriptionContainer,
              gap: spacing.m,
              alignItems: 'center',
            }}>
            <VodLiveStationListVertical
              itemList={liveStationItemList}
              itemsPerRow={2}
              numOfRows={100}
            />
          </View>
        )}
        {/* {
                    history && history.length === 0 &&
                    <EmptyList description='暂无播放历史' />
                } */}
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  descriptionContainer: {
    flex: 1,
  },
});
