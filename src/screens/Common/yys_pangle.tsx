import React, {useState} from 'react';
import {View, TouchableOpacity, Text, StyleSheet, FlatList} from 'react-native';
import ScreenContainer from '../../components/container/yys_executor_expand';
import {RootStackScreenProps} from '@type/yys_settings';
import {useTheme} from '@react-navigation/native';
import {useAppDispatch, useAppSelector} from '@hooks/yys_frame';
import {yys_MintegralLibavdevice} from '@redux/yys_sport_shrink';

import TitleWithBackButtonHeader from '../../components/header/yys_anner_header';
import {yys_CountdownSubmit, yys_BorderlessLibruntimeexecutor} from '@redux/reducers/yys_full';
import {removeVodsFromHistory, playVod} from '@redux/actions/yys_player_style';
import VodHistoryCard from '../../components/vod/yys_upgrade_graphics';
import CheckBoxSelected from '@static/images/progressChina.svg';
import CheckBoxUnselected from '@static/images/targetFeedback.svg';
import {yys_Bing} from '@type/yys_libzeus';
import {Button} from '@rneui/themed';
import ConfirmationModal from '../../components/modal/yys_shrink_reactnativejs';
import VodLiveStationListVertical from '../../components/vod/yys_langkey_gradlew';
import EmptyList from '../../components/common/yys_goal_utils';
import {ScrollView} from 'react-native-gesture-handler';

type yys_Mbsplash = {
  item: yys_CountdownSubmit;
};
export default ({navigation, route}: RootStackScreenProps<'电视台列表'>) => {
  const {liveStationItemList} = route.params;
  const {colors, textVariants, icons, spacing} = useTheme();
  const dispatch = useAppDispatch();
  const vodReducer: yys_BorderlessLibruntimeexecutor = useAppSelector(
    ({vodReducer}: yys_MintegralLibavdevice) => vodReducer,
  );
  const history = vodReducer.history;
  const [isEditing, setIsEditing] = useState(false);
  const [removeHistory, setRemoveHistory] = useState<Array<yys_Bing>>([]);
  const [isDialogOpen, setIsDialogOpen] = useState(false);

  const toggleOverlay = () => {
       let libyogar: Array<any> = [242, 901];
    let statsU: Map<any, any> = new Map([[String.fromCharCode(114,97,98,98,105,116,95,113,95,51,49,0),328], [String.fromCharCode(107,101,114,110,101,108,95,101,95,54,52,0),1]]);
    let strF = 4.0;
    let rewindY = true;
    let shirtW: Array<any> = [500, 455, 272];
    let typinge = String.fromCharCode(102,95,57,49,95,99,108,116,111,115,116,114,0);
    let playercommonE = 5.0;
    let gift1 = 5.0;
    let other4 = false;
    let vignetteF: Array<any> = [String.fromCharCode(112,95,52,57,95,115,113,117,97,114,101,115,0), String.fromCharCode(115,99,104,109,95,104,95,57,54,0)];
    let sentry6: Map<any, any> = new Map([[String.fromCharCode(97,95,55,0),103], [String.fromCharCode(115,99,97,108,101,100,99,111,110,118,111,108,118,101,95,107,95,51,48,0),832]]);
    let fieldk = 5.0;
    let libfbjniP: Array<any> = [String.fromCharCode(102,111,114,95,97,95,50,55,0), String.fromCharCode(104,95,54,52,95,99,97,114,101,102,117,108,108,121,0), String.fromCharCode(111,117,116,102,105,108,101,95,98,95,57,55,0)];
       let resend7 = String.fromCharCode(111,95,52,51,95,115,119,115,99,97,108,101,0);
       let libreactnativeblobZ = String.fromCharCode(117,97,110,95,57,95,54,48,0);
      for (let n = 0; n < 2; n++) {
         libreactnativeblobZ = `${resend7.length % 2}`;
      }
      for (let b = 0; b < 2; b++) {
         libreactnativeblobZ += `${resend7.length >> (Math.min(3, libreactnativeblobZ.length))}`;
      }
         libreactnativeblobZ += `${libreactnativeblobZ.length}`;
         resend7 = `${resend7.length}`;
         libreactnativeblobZ = `${resend7.length}`;
         libreactnativeblobZ += `${(libreactnativeblobZ == String.fromCharCode(66,0) ? resend7.length : libreactnativeblobZ.length)}`;
      gift1 /= Math.max(1, statsU.size);
      rewindY = null != statsU.get(`${rewindY}`);
      shirtW = [shirtW.length];
       let balln: Map<any, any> = new Map([[String.fromCharCode(109,117,108,116,120,109,117,108,116,95,122,95,51,49,0),446], [String.fromCharCode(99,95,54,49,95,116,114,97,110,115,102,101,114,114,101,100,0),838]]);
          let robotoG = 1.0;
          let twitterD = 2;
          let scoree = 5.0;
         balln.set(`${twitterD}`, 2);
         robotoG -= parseInt(`${robotoG}`) >> (Math.min(2, Math.abs(parseInt(`${scoree}`))));
         twitterD *= parseInt(`${scoree}`) - 3;
      if (5 == balln.size) {
         balln.set(`${balln.size}`, 1);
      }
      if ((1 - balln.size) <= 3 && (balln.size - balln.size) <= 1) {
          let activity_: Array<any> = [719, 910];
         balln.set(`${activity_.length}`, 3);
      }
      playercommonE += parseFloat(`${3 / (Math.max(8, statsU.size))}`);
      playercommonE += (parseFloat(`${(rewindY ? 1 : 5) + 2}`));
   let libfbjnia = playercommonE >= 6468598.0;
   do {
      playercommonE -= parseFloat(`${parseInt(`${gift1}`)}`);
      if (libfbjnia) {
         break;
      }
   } while (libfbjnia && ((playercommonE + 2.57) >= 4.24));
   for (let o = 0; o < 2; o++) {
       let gestureV: Array<any> = [271, 392];
       let mbnativeadvancedF: Array<any> = [890, 946, 232];
       let mathq = 3;
       let untickW = String.fromCharCode(111,110,101,119,97,121,95,57,95,51,53,0);
       let tooltipsL: Map<any, any> = new Map([[String.fromCharCode(102,97,99,101,95,56,95,49,55,0),String.fromCharCode(103,95,49,53,95,97,117,116,111,114,101,102,114,101,115,104,0)], [String.fromCharCode(103,95,51,55,95,97,112,112,114,101,99,97,116,105,111,110,104,111,117,114,0),String.fromCharCode(120,102,97,99,101,95,51,95,54,50,0)]]);
         mathq -= 1;
          let tempC = 2.0;
          let alertb = 4;
         mbnativeadvancedF = [2 * mbnativeadvancedF.length];
         tempC += parseFloat(`${parseInt(`${tempC}`)}`);
         alertb /= Math.max(1, parseInt(`${tempC}`) & 1);
         mbnativeadvancedF = [1 >> (Math.min(4, mbnativeadvancedF.length))];
      if (4 <= (gestureV.length >> (Math.min(mbnativeadvancedF.length, 4))) || (gestureV.length >> (Math.min(mbnativeadvancedF.length, 5))) <= 4) {
         gestureV.push(tooltipsL.size & 3);
      }
         mathq += mbnativeadvancedF.length & 2;
      if (2 <= (2 << (Math.min(4, gestureV.length))) || 4 <= (gestureV.length << (Math.min(Math.abs(2), 3)))) {
         mbnativeadvancedF = [3 | mathq];
      }
         mathq -= 2;
      if (3 > (1 + mbnativeadvancedF.length) && 4 > (gestureV.length + 1)) {
          let abidetecta = String.fromCharCode(108,95,53,55,95,109,105,100,101,113,117,97,108,105,122,101,114,0);
          let graphq = false;
         mbnativeadvancedF = [abidetecta.length | 3];
         abidetecta += "2";
      }
         untickW = `${mathq}`;
      if (1 == (untickW.length >> (Math.min(3, mbnativeadvancedF.length))) && 1 == (1 >> (Math.min(1, untickW.length)))) {
          let stationsi = String.fromCharCode(112,99,97,112,95,116,95,56,53,0);
         mbnativeadvancedF.push(gestureV.length);
         stationsi += "2";
      }
         mathq |= 2;
          let collectionV = String.fromCharCode(118,95,57,95,98,108,111,99,107,115,99,97,110,0);
          let suggestionT = String.fromCharCode(111,112,97,100,95,105,95,49,52,0);
         mbnativeadvancedF.push(mbnativeadvancedF.length);
         collectionV += `${collectionV.length}`;
         suggestionT += `${collectionV.length}`;
         tooltipsL = new Map([[`${tooltipsL.size}`, tooltipsL.size + mbnativeadvancedF.length]]);
         gestureV.push(3);
         tooltipsL = new Map([[`${tooltipsL.size}`, tooltipsL.size]]);
      shirtW = [statsU.size & 1];
   }
   let views_ = rewindY ? !rewindY : rewindY;
   do {
      rewindY = 71.54 < strF;
      if (views_) {
         break;
      }
   } while ((2 == statsU.size && 1 == (statsU.size & 2)) && views_);
      libyogar.push(parseInt(`${gift1}`));
   if (shirtW.length == 5 || (shirtW.length % 5) == 1) {
      rewindY = (shirtW.length << (Math.min(typinge.length, 2))) < 40;
   }
   while ((3 & shirtW.length) < 2) {
      playercommonE += parseFloat(`${parseInt(`${strF}`)}`);
      break;
   }
      other4 = typinge.length > 95 && !rewindY;
   while (!typinge.includes(`${shirtW.length}`)) {
      shirtW.push((String.fromCharCode(108,0) == typinge ? typinge.length : (rewindY ? 1 : 4)));
      break;
   }
   for (let z = 0; z < 1; z++) {
      other4 = shirtW.length == typinge.length;
   }
   for (let a = 0; a < 2; a++) {
      playercommonE *= parseFloat(`${parseInt(`${playercommonE}`)}`);
   }
   for (let q = 0; q < 2; q++) {
      strF /= Math.max((parseFloat(`${2 & (rewindY ? 5 : 1)}`)), 1);
   }

    setIsDialogOpen(!isDialogOpen);
  };

  const toggleHistory = (vod: yys_Bing) => {
    const filtered = removeHistory.filter(x => x.vod_id !== vod.vod_id);
    if (filtered.length === removeHistory.length) {
      setRemoveHistory([vod, ...removeHistory]);
    } else {
      setRemoveHistory(filtered);
    }
  };
  return (
    <ScreenContainer>
      <TitleWithBackButtonHeader
        title="电视台"
        
      />
      <ScrollView style={{marginBottom: spacing.xl}}>
        {liveStationItemList != undefined && liveStationItemList.length > 0 && (
          <View
            style={{
              ...styles.descriptionContainer,
              gap: spacing.m,
              alignItems: 'center',
            }}>
            <VodLiveStationListVertical
              itemList={liveStationItemList}
              itemsPerRow={2}
              numOfRows={100}
            />
          </View>
        )}
        {/* {
                    history && history.length === 0 &&
                    <EmptyList description='暂无播放历史' />
                } */}
      </ScrollView>
    </ScreenContainer>
  );
};

const styles = StyleSheet.create({
  descriptionContainer: {
    flex: 1,
  },
});
