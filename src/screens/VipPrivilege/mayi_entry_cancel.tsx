import React, { useState, useCallback, useEffect } from "react";
import { View, Text, StyleSheet, SafeAreaView, FlatList } from "react-native";

import { API_DOMAIN } from "@utility/mayi_middleware_apps";
import { mayi_Code, mayi_EmojiheartChinasame } from "@type/mayi_green";
import { useQuery, useInfiniteQuery } from '@tanstack/react-query';

import { acceptOverEighteen } from "@redux/actions/mayi_iconorientation_chatroombackground";
import EighteenPlusOverlay from "../../components/modal/mayi_giftbutton_homeplayer";
import { useNavigation, useTheme } from "@react-navigation/native";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/mayi_redirect";
import ShowMoreVodButton from "../../components/button/mayi_matchinactive_button";
import VodListVerticalVip from "./mayi_sentry";
import FastImage from "../../components/common/mayi_slider";
import { mayi_Iconclosewhite } from "@api";
import { mayi_Gift } from "@models/mayi_libjsinspector";
import { mayi_Baseline } from "@redux/reducers/mayi_chatroombackground_unlock";
interface mayi_GoogleViews {
  handleRejectEighteenPlus: any;
}

export default function XVodTab({
  handleRejectEighteenPlus = () => {
       let iconclosewhiteE = String.fromCharCode(97,116,97,99,101,110,116,101,114,95,50,95,50,56,0);
    let classesH = 3;
    let targets = false;
    let homeactiveF = false;
    let runtimeschedulerm = String.fromCharCode(122,95,53,95,108,111,111,107,117,112,0);
    let embede = 1.0;
    let whitevideolivey = 1;
    let uimanagerU = String.fromCharCode(105,110,105,116,105,97,108,105,122,101,114,95,119,95,53,51,0);
    let clocke = false;
    let libavfilter9: Map<any, any> = new Map([[String.fromCharCode(116,114,101,101,119,114,105,116,101,114,95,55,95,57,52,0),656], [String.fromCharCode(109,117,108,116,105,108,105,110,101,95,116,95,49,57,0),828]]);
    let configu = String.fromCharCode(100,95,55,95,108,97,114,103,101,114,0);
    let with_4b = String.fromCharCode(116,105,108,101,120,95,50,95,55,54,0);
    let qaagC = String.fromCharCode(119,95,56,51,95,114,101,108,111,97,100,101,114,0);
    let coreh = 4.0;
    let launchern: Map<any, any> = new Map([[String.fromCharCode(99,104,105,109,112,95,55,95,51,48,0),675], [String.fromCharCode(97,114,103,117,109,101,110,116,95,121,95,51,53,0),881]]);
      clocke = String.fromCharCode(116,0) == runtimeschedulerm;
      classesH %= Math.max((parseInt(`${embede}`) << (Math.min(5, Math.abs((homeactiveF ? 1 : 1))))), 4);
   let sellV = embede <= 9288383.0;
   do {
      embede += runtimeschedulerm.length;
      if (sellV) {
         break;
      }
   } while (sellV && ((embede + 3.46) > 2.57 && targets));
   let upgradeF = embede >= 9897893.0;
   do {
      embede *= (configu.length % (Math.max(1, (targets ? 2 : 1))));
      if (upgradeF) {
         break;
      }
   } while (upgradeF && (!homeactiveF || 2.12 >= (2.90 - embede)));
   for (let s = 0; s < 2; s++) {
      runtimeschedulerm = `${libavfilter9.size}`;
   }
       let ballm = true;
       let condensedo = String.fromCharCode(108,95,53,57,95,117,114,97,110,100,111,109,0);
      if (condensedo.includes(`${ballm}`)) {
          let baseline6 = String.fromCharCode(122,95,54,50,95,105,109,112,111,114,116,0);
          let sinaA = String.fromCharCode(108,95,57,55,95,103,101,116,101,110,118,0);
          let tempnodataq = false;
          let backq = String.fromCharCode(103,95,55,57,95,117,110,114,101,118,101,114,115,101,100,0);
          let iconlogouti = String.fromCharCode(113,119,111,114,100,95,53,95,52,54,0);
         condensedo = `${condensedo.length % 1}`;
         baseline6 = `${(String.fromCharCode(99,0) == backq ? backq.length : baseline6.length)}`;
         sinaA += `${backq.length}`;
         tempnodataq = (59 == ((!tempnodataq ? sinaA.length : 59) | sinaA.length));
         iconlogouti += "2";
      }
         condensedo += `${condensedo.length}`;
         ballm = !ballm;
         condensedo += `${1 * condensedo.length}`;
         condensedo = `${(condensedo == String.fromCharCode(66,0) ? (ballm ? 4 : 2) : condensedo.length)}`;
          let episodesb = 1.0;
         condensedo += `${parseInt(`${episodesb}`)}`;
      libavfilter9 = new Map([[`${whitevideolivey}`, whitevideolivey * 3]]);
   if (configu.endsWith(`${clocke}`)) {
      clocke = (runtimeschedulerm.length * embede) <= 12.73;
   }
      targets = !targets;
      targets = with_4b.length <= 17;
   let awayicon9 = 7226181 >= whitevideolivey;
   do {
      whitevideolivey %= Math.max(3, classesH);
      if (awayicon9) {
         break;
      }
   } while (awayicon9 && (with_4b.length > 2));
   let videojsg = clocke ? !clocke : clocke;
   do {
      clocke = classesH > 40 && 40 > libavfilter9.size;
      if (videojsg) {
         break;
      }
   } while (videojsg && ((coreh / 2.47) <= 4.43));
      iconclosewhiteE += `${3 - with_4b.length}`;
      embede /= Math.max(3, runtimeschedulerm.length);
   for (let k = 0; k < 3; k++) {
      homeactiveF = configu == String.fromCharCode(89,0);
   }
      coreh /= Math.max(runtimeschedulerm.length, 2);
      libavfilter9 = new Map([[`${targets}`, ((clocke ? 5 : 4) - (targets ? 2 : 4))]]);
   if (configu.length >= 4 || with_4b.length >= 4) {
      configu = `${(configu == String.fromCharCode(52,0) ? configu.length : iconclosewhiteE.length)}`;
   }
      whitevideolivey >>= Math.min(1, Math.abs(((clocke ? 4 : 3))));
   while (4 < configu.length) {
       let combinedR = String.fromCharCode(102,117,110,103,105,98,108,101,115,95,55,95,54,49,0);
       let expiredV = 4.0;
       let eighteenO = String.fromCharCode(103,95,54,56,95,99,97,118,115,105,100,99,116,0);
       let snewsH = 2;
      if (snewsH > eighteenO.length) {
         eighteenO = `${snewsH / (Math.max(3, 7))}`;
      }
      let mbbannerO = snewsH <= 7348802;
      do {
         snewsH |= 2;
         if (mbbannerO) {
            break;
         }
      } while (mbbannerO && (eighteenO.length == 2));
         snewsH <<= Math.min(2, Math.abs(parseInt(`${expiredV}`) * 2));
          let sortW = 2.0;
          let tinit_stH = String.fromCharCode(106,95,50,95,116,111,111,116,105,112,0);
          let pointh = 5.0;
         eighteenO = `${parseInt(`${sortW}`) * parseInt(`${expiredV}`)}`;
         sortW /= Math.max(1, (parseFloat(`${String.fromCharCode(55,0) == tinit_stH ? parseInt(`${pointh}`) : tinit_stH.length}`)));
         pointh /= Math.max(1, 2 >> (Math.min(3, tinit_stH.length)));
      if (2 < (snewsH - 4) && (4 - snewsH) < 2) {
         eighteenO = `${eighteenO.length % 2}`;
      }
          let sentryS = String.fromCharCode(121,95,53,52,95,115,109,111,111,116,104,101,110,0);
          let vietnama = 3;
          let inviter = String.fromCharCode(117,110,98,111,120,95,104,95,57,48,0);
         expiredV /= Math.max(1, 3);
         sentryS = `${sentryS.length / (Math.max(2, vietnama))}`;
         vietnama |= sentryS.length << (Math.min(inviter.length, 2));
         inviter += "1";
          let iconlogoutX: Map<any, any> = new Map([[String.fromCharCode(115,95,50,95,115,101,108,102,0),545], [String.fromCharCode(115,107,105,112,95,110,95,51,54,0),657]]);
          let logouti: Map<any, any> = new Map([[String.fromCharCode(108,95,49,56,95,116,117,110,105,110,103,0),455], [String.fromCharCode(115,121,109,98,111,108,105,99,97,116,105,111,110,95,101,95,50,49,0),203]]);
          let iconstarc = String.fromCharCode(99,97,110,100,105,100,97,116,101,115,95,118,95,57,56,0);
         expiredV += (eighteenO == String.fromCharCode(71,0) ? eighteenO.length : combinedR.length);
         iconlogoutX.set(iconstarc, (iconstarc == String.fromCharCode(100,0) ? iconstarc.length : logouti.size));
         logouti.set(`${iconlogoutX.size}`, iconlogoutX.size << (Math.min(Math.abs(logouti.size), 3)));
      while ((combinedR.length * 1) < 3 || (combinedR.length * 1) < 5) {
         snewsH *= 1 - parseInt(`${expiredV}`);
         break;
      }
       let strE = String.fromCharCode(118,95,55,55,95,116,102,104,100,0);
       let datae = String.fromCharCode(110,95,57,54,95,115,108,105,100,97,98,108,101,0);
      if (strE.length <= combinedR.length) {
         combinedR += "3";
      }
         snewsH /= Math.max((String.fromCharCode(83,0) == datae ? eighteenO.length : datae.length), 2);
         snewsH /= Math.max(snewsH | parseInt(`${expiredV}`), 5);
      libavfilter9.set(eighteenO, eighteenO.length | 3);
      break;
   }
   if (2 == (runtimeschedulerm.length & 5)) {
       let android7 = true;
       let singaporeV: Array<any> = [983, 25, 162];
       let subss = String.fromCharCode(102,111,117,114,115,113,117,97,114,101,95,115,95,53,55,0);
      while (!android7) {
         android7 = subss.length >= singaporeV.length;
         break;
      }
          let umengd = String.fromCharCode(114,101,108,97,117,110,99,104,95,105,95,57,54,0);
          let libtoba: Array<any> = [928, 496];
          let privilege2 = 2.0;
         singaporeV.push(singaporeV.length << (Math.min(Math.abs(1), 2)));
         umengd = `${parseInt(`${privilege2}`)}`;
         libtoba.push(parseInt(`${privilege2}`));
      let containerr = singaporeV.length >= 6266170;
      do {
         singaporeV.push(singaporeV.length >> (Math.min(Math.abs(2), 5)));
         if (containerr) {
            break;
         }
      } while ((android7) && containerr);
      if (1 <= (singaporeV.length - 2)) {
         singaporeV.push(((android7 ? 2 : 4)));
      }
          let iconstarx = 2.0;
          let goallogoO = true;
         singaporeV = [subss.length];
         iconstarx -= parseFloat(`${parseInt(`${iconstarx}`)}`);
         goallogoO = 22.53 < (iconstarx / (Math.max(3, iconstarx)));
      if (!android7) {
         android7 = android7 && singaporeV.length >= 41;
      }
      let applicationC = 7453907 <= singaporeV.length;
      do {
         singaporeV.push(singaporeV.length);
         if (applicationC) {
            break;
         }
      } while (applicationC && (singaporeV.length < 1));
         singaporeV.push(2 & singaporeV.length);
         singaporeV.push((2 >> (Math.min(2, Math.abs((android7 ? 3 : 5))))));
      runtimeschedulerm += `${with_4b.length ^ 3}`;
   }
   for (let x = 0; x < 1; x++) {
       let predictionactiveN = 2.0;
       let stylesd = String.fromCharCode(115,95,55,52,95,117,110,102,111,99,117,115,101,100,0);
      let bridgeH = String.fromCharCode(55,113,122,101,110,0) == stylesd;
      do {
          let popupd = true;
         stylesd += `${stylesd.length}`;
         popupd = (!popupd ? !popupd : popupd);
         if (bridgeH) {
            break;
         }
      } while (bridgeH && ((stylesd.length % (Math.max(1, 5))) >= 5 || (stylesd.length % 1) >= 2));
         predictionactiveN /= Math.max(5, parseInt(`${predictionactiveN}`));
       let helperm: Array<any> = [987, 727, 838];
       let exampleimagea: Array<any> = [173, 794, 262];
      for (let b = 0; b < 2; b++) {
          let plusq = 5.0;
          let placeholderc = 4.0;
         helperm.push(exampleimagea.length);
         plusq *= parseFloat(`${parseInt(`${placeholderc}`) + parseInt(`${plusq}`)}`);
         placeholderc /= Math.max(2, parseFloat(`${parseInt(`${plusq}`)}`));
      }
          let promotions = 2;
          let w_centerM = String.fromCharCode(105,95,55,49,95,100,97,114,119,105,110,0);
         stylesd += "1";
         promotions |= promotions;
         w_centerM = "3";
         predictionactiveN -= 2 & stylesd.length;
      uimanagerU = `${(parseInt(`${predictionactiveN}`) ^ (clocke ? 4 : 2))}`;
   }
   for (let p = 0; p < 3; p++) {
      configu = "1";
   }
   let playp = 5362332 <= runtimeschedulerm.length;
   do {
      runtimeschedulerm += `${3 >> (Math.min(4, Math.abs(classesH)))}`;
      if (playp) {
         break;
      }
   } while (playp && (!runtimeschedulerm.startsWith(`${classesH}`)));
   if (qaagC.endsWith(`${embede}`)) {
       let catalogX = String.fromCharCode(114,95,51,95,98,97,115,101,103,112,104,0);
       let libglogl = true;
       let expand2 = true;
       let matchinactiveo = String.fromCharCode(115,101,103,117,101,95,120,95,56,53,0);
       let homet = 3;
      while (catalogX.includes(`${matchinactiveo.length}`)) {
         matchinactiveo += `${matchinactiveo.length}`;
         break;
      }
      if (matchinactiveo == String.fromCharCode(86,0) && catalogX.length > 5) {
         matchinactiveo += `${catalogX.length}`;
      }
      if (!libglogl || expand2) {
         expand2 = (libglogl ? !expand2 : libglogl);
      }
         matchinactiveo += `${((libglogl ? 1 : 2))}`;
          let projectC = String.fromCharCode(100,95,51,54,95,115,116,97,108,108,0);
          let anythinkG = String.fromCharCode(99,95,51,56,95,97,108,116,101,114,110,97,116,105,118,101,0);
         expand2 = 77 >= anythinkG.length;
         projectC = "2";
         anythinkG += `${projectC.length}`;
       let private_rnX = String.fromCharCode(109,95,53,57,95,99,111,109,109,97,0);
       let mbnativeV = String.fromCharCode(109,97,107,101,100,112,107,103,95,55,95,52,50,0);
      let arrowrightwithtailL = private_rnX.length >= 5132852;
      do {
          let iconarrowleftI = String.fromCharCode(98,95,52,56,95,115,100,112,0);
          let encryptorP = 4.0;
          let macaup: Map<any, any> = new Map([[String.fromCharCode(115,117,109,109,101,100,95,119,95,57,48,0),561], [String.fromCharCode(105,95,56,53,95,114,101,98,97,115,101,0),826], [String.fromCharCode(104,95,55,55,95,99,108,111,99,107,119,105,115,101,0),273]]);
         private_rnX += `${parseInt(`${encryptorP}`) & 3}`;
         iconarrowleftI += "3";
         encryptorP += iconarrowleftI.length + 1;
         macaup.set(iconarrowleftI, iconarrowleftI.length);
         if (arrowrightwithtailL) {
            break;
         }
      } while (arrowrightwithtailL && (private_rnX.startsWith(`${libglogl}`)));
         mbnativeV += `${(matchinactiveo == String.fromCharCode(113,0) ? matchinactiveo.length : homet)}`;
      if (catalogX.length == 2 || !libglogl) {
         libglogl = homet < 19;
      }
      if (private_rnX.endsWith(matchinactiveo)) {
         private_rnX += `${((libglogl ? 1 : 2) ^ (expand2 ? 4 : 3))}`;
      }
      if (private_rnX != String.fromCharCode(98,0)) {
         matchinactiveo = `${private_rnX.length}`;
      }
      if (homet > private_rnX.length) {
         private_rnX = `${2 ^ matchinactiveo.length}`;
      }
      let libsentryb = libglogl ? !libglogl : libglogl;
      do {
          let greytickc: Array<any> = [String.fromCharCode(116,95,50,95,115,117,99,99,101,101,100,101,100,0), String.fromCharCode(118,105,115,98,108,101,95,56,95,54,56,0)];
          let moonp = 4;
          let handlerv = 3.0;
         libglogl = String.fromCharCode(114,0) == matchinactiveo;
         greytickc = [parseInt(`${handlerv}`)];
         moonp /= Math.max(3, moonp);
         handlerv /= Math.max(parseFloat(`${parseInt(`${handlerv}`)}`), 1);
         if (libsentryb) {
            break;
         }
      } while ((3 <= catalogX.length) && libsentryb);
      let networkY = 5616212 >= mbnativeV.length;
      do {
          let calendare: Array<any> = [953, 713];
          let rulesn: Array<any> = [991, 516];
          let armvas = 0.0;
          let annert = 2;
         mbnativeV = `${catalogX.length}`;
         calendare.push(1 - rulesn.length);
         rulesn = [2];
         armvas /= Math.max(5, 2 | parseInt(`${armvas}`));
         annert -= 3 & rulesn.length;
         if (networkY) {
            break;
         }
      } while ((!expand2) && networkY);
      let animationsR = catalogX == String.fromCharCode(121,99,51,114,108,109,98,111,111,0);
      do {
         catalogX += `${homet | private_rnX.length}`;
         if (animationsR) {
            break;
         }
      } while ((catalogX.endsWith(`${libglogl}`)) && animationsR);
      embede *= ((homeactiveF ? 1 : 1) % (Math.max(uimanagerU.length, 4)));
   }
   let giftC = String.fromCharCode(115,111,105,0) == with_4b;
   do {
       let libjsinspectorg = false;
       let roundg: Map<any, any> = new Map([[String.fromCharCode(99,111,101,114,99,101,95,105,95,53,53,0),String.fromCharCode(99,111,110,118,101,114,116,101,114,95,100,95,51,56,0)], [String.fromCharCode(107,95,49,48,95,100,100,115,116,0),String.fromCharCode(98,97,114,114,97,121,95,53,95,57,49,0)], [String.fromCharCode(117,95,57,56,95,104,101,99,107,0),String.fromCharCode(114,101,115,117,109,101,100,95,110,95,49,56,0)]]);
      for (let g = 0; g < 3; g++) {
          let libruntimeexecutorX = String.fromCharCode(112,114,101,100,105,99,116,95,55,95,48,0);
          let libflipper3 = 2;
          let untick8 = true;
          let reminderi = String.fromCharCode(119,114,105,116,101,99,108,101,97,114,95,52,95,56,51,0);
          let benefitZ = String.fromCharCode(105,110,99,114,95,117,95,54,48,0);
         libjsinspectorg = libflipper3 <= 59;
         libruntimeexecutorX += `${reminderi.length / (Math.max(libruntimeexecutorX.length, 1))}`;
         libflipper3 *= 3;
         untick8 = 14 <= benefitZ.length;
         reminderi += "2";
         benefitZ = `${3 / (Math.max(3, reminderi.length))}`;
      }
         libjsinspectorg = !libjsinspectorg;
          let infoF = String.fromCharCode(97,95,56,48,95,102,116,115,97,117,120,0);
          let stylesA: Array<any> = [549, 981];
          let privacyS = String.fromCharCode(102,95,55,53,95,113,117,101,114,121,0);
         libjsinspectorg = !libjsinspectorg;
         infoF += `${stylesA.length}`;
         stylesA = [privacyS.length * 3];
         privacyS += `${infoF.length}`;
      let gpayU = roundg.size <= 5320385;
      do {
         roundg.set(`${libjsinspectorg}`, roundg.size);
         if (gpayU) {
            break;
         }
      } while ((libjsinspectorg) && gpayU);
      if (roundg.size > 4) {
          let downloadedx = false;
          let turndownH = 4.0;
          let yellowanimationlivej: Array<any> = [926, 673];
         libjsinspectorg = null != roundg.get(`${downloadedx}`);
         downloadedx = (turndownH + yellowanimationlivej.length) >= 13.70;
         turndownH += 2 << (Math.min(Math.abs(parseInt(`${turndownH}`)), 4));
         yellowanimationlivej = [yellowanimationlivej.length ^ parseInt(`${turndownH}`)];
      }
      if (2 == (roundg.size - 5) || libjsinspectorg) {
         roundg = new Map([[`${roundg.size}`, roundg.size]]);
      }
      with_4b = `${classesH}`;
      if (giftC) {
         break;
      }
   } while ((!with_4b.includes(`${configu.length}`)) && giftC);
   let readX = 5784857.0 <= coreh;
   do {
      coreh -= libavfilter9.size;
      if (readX) {
         break;
      }
   } while ((!uimanagerU.endsWith(`${coreh}`)) && readX);
       let defaultplayerimgw: Map<any, any> = new Map([[String.fromCharCode(109,111,116,99,111,109,112,95,106,95,50,50,0),377], [String.fromCharCode(122,95,53,50,95,105,110,112,99,98,0),100]]);
      let downloader8 = 5344204 >= defaultplayerimgw.size;
      do {
          let kuaishouJ = 3.0;
          let moduleZ = 0;
          let moreP = true;
          let bottomR = 5.0;
         defaultplayerimgw = new Map([[`${defaultplayerimgw.size}`, 3]]);
         kuaishouJ -= parseFloat(`${parseInt(`${kuaishouJ}`) >> (Math.min(1, Math.abs(moduleZ)))}`);
         moduleZ ^= parseInt(`${kuaishouJ}`);
         moreP = bottomR >= 93.92;
         bottomR += parseFloat(`${moduleZ}`);
         if (downloader8) {
            break;
         }
      } while ((!Array.from(defaultplayerimgw.keys()).includes(`${defaultplayerimgw.size}`)) && downloader8);
      for (let q = 0; q < 2; q++) {
          let turndownP = String.fromCharCode(102,95,53,52,95,112,111,119,101,114,0);
          let binddatasy = 1.0;
          let footballQ = String.fromCharCode(110,95,54,48,95,98,97,115,105,99,0);
         defaultplayerimgw = new Map([[`${defaultplayerimgw.size}`, turndownP.length ^ defaultplayerimgw.size]]);
         turndownP = `${parseInt(`${binddatasy}`)}`;
         binddatasy += parseInt(`${binddatasy}`);
         footballQ += `${parseInt(`${binddatasy}`) / (Math.max(5, footballQ.length))}`;
      }
         defaultplayerimgw = new Map([[`${defaultplayerimgw.size}`, defaultplayerimgw.size]]);
      libavfilter9.set(`${homeactiveF}`, 1);
       let placeholderK = String.fromCharCode(119,95,49,52,95,99,112,120,0);
       let viewerT = String.fromCharCode(110,95,56,50,95,101,114,111,100,101,0);
       let terms8 = String.fromCharCode(114,95,50,95,116,114,97,110,115,108,97,116,101,0);
         placeholderK += "3";
       let cedbadcebfbfbfbcfecbcz: Array<any> = [874, 368];
       let modeE: Array<any> = [583, 128];
      for (let g = 0; g < 2; g++) {
         viewerT = `${terms8.length ^ placeholderK.length}`;
      }
          let completen: Array<any> = [831, 531];
          let sinaq: Array<any> = [270, 792];
          let routerq = false;
         viewerT = "2";
         completen.push((2 - (routerq ? 1 : 4)));
         sinaq = [((routerq ? 3 : 3) | 2)];
         terms8 = "2";
      for (let a = 0; a < 3; a++) {
          let binddatasB = String.fromCharCode(105,95,51,53,95,103,101,116,108,97,100,100,114,115,0);
          let guider = String.fromCharCode(101,95,57,95,120,112,97,116,104,0);
          let stringsL = String.fromCharCode(105,110,118,105,116,101,114,115,95,57,95,54,54,0);
          let usernamey = 0;
          let danger8 = 5.0;
         viewerT += `${3 ^ stringsL.length}`;
         binddatasB += `${binddatasB.length}`;
         guider = "2";
         stringsL += `${parseInt(`${danger8}`)}`;
         usernamey %= Math.max((binddatasB == String.fromCharCode(120,0) ? binddatasB.length : parseInt(`${danger8}`)), 5);
      }
      for (let e = 0; e < 2; e++) {
          let gifgoalU = String.fromCharCode(101,120,112,95,57,95,49,0);
          let searchp = 4.0;
         placeholderK = "3";
         gifgoalU = `${parseInt(`${searchp}`)}`;
         searchp /= Math.max(4, parseFloat(`${gifgoalU.length}`));
      }
         modeE.push(terms8.length);
         cedbadcebfbfbfbcfecbcz.push(cedbadcebfbfbfbcfecbcz.length);
      classesH &= parseInt(`${coreh}`) & 2;
      with_4b += `${3 >> (Math.min(3, Math.abs(parseInt(`${coreh}`))))}`;
       let nativemoduled = String.fromCharCode(97,95,55,57,95,103,111,108,111,109,98,0);
       let predictioni = false;
         predictioni = predictioni && nativemoduled.length < 8;
      let shareL = predictioni ? !predictioni : predictioni;
      do {
         predictioni = nativemoduled.endsWith(`${predictioni}`);
         if (shareL) {
            break;
         }
      } while (shareL && (predictioni || nativemoduled.length >= 2));
          let annerL = String.fromCharCode(103,95,51,56,95,115,121,109,109,101,116,114,105,99,0);
          let tempF = String.fromCharCode(117,110,114,111,108,108,101,100,95,51,95,51,0);
         nativemoduled = `${annerL.length >> (Math.min(Math.abs(1), 3))}`;
         annerL += `${tempF.length >> (Math.min(5, tempF.length))}`;
      let sounds = String.fromCharCode(52,52,48,97,101,51,118,120,104,0) == nativemoduled;
      do {
          let fillS = String.fromCharCode(99,104,97,110,103,101,104,95,113,95,55,55,0);
          let nativeexI = false;
         nativemoduled += `${2 ^ fillS.length}`;
         if (sounds) {
            break;
         }
      } while ((!predictioni && 3 >= nativemoduled.length) && sounds);
         predictioni = !nativemoduled.endsWith(`${predictioni}`);
         nativemoduled += `${((predictioni ? 1 : 1))}`;
      targets = !clocke;
   if (2.87 >= (iconclosewhiteE.length / (Math.max(8, coreh)))) {
      coreh -= iconclosewhiteE.length;
   }
   for (let k = 0; k < 3; k++) {
       let privacyq = true;
      let informationk = privacyq ? !privacyq : privacyq;
      do {
         privacyq = !privacyq;
         if (informationk) {
            break;
         }
      } while (informationk && (privacyq));
       let containerI: Map<any, any> = new Map([[String.fromCharCode(97,114,103,117,109,101,110,116,115,95,101,95,49,54,0),974], [String.fromCharCode(118,95,53,55,95,108,111,119,101,114,99,97,115,101,100,0),648]]);
      for (let y = 0; y < 1; y++) {
          let giftbuttona = 2;
          let j_hashB = 1.0;
          let x_imagep = String.fromCharCode(108,95,57,50,95,100,105,115,112,111,115,97,98,108,101,0);
          let hinit_rd: Array<any> = [506, 749, 613];
         containerI.set(`${j_hashB}`, parseInt(`${j_hashB}`) ^ 2);
         giftbuttona &= 2 & giftbuttona;
         x_imagep = `${(x_imagep == String.fromCharCode(65,0) ? giftbuttona : x_imagep.length)}`;
         hinit_rd = [2 * x_imagep.length];
      }
      iconclosewhiteE += `${((homeactiveF ? 5 : 3))}`;
   }
       let lineP: Map<any, any> = new Map([[String.fromCharCode(116,95,49,95,116,97,110,115,105,103,0),false ], [String.fromCharCode(114,95,55,54,95,100,105,115,99,111,110,110,101,99,116,0),true ]]);
      if (Array.from(lineP.keys()).includes(`${lineP.size}`)) {
         lineP.set(`${lineP.size}`, 3);
      }
      if (5 <= (4 | lineP.size) && 4 <= (lineP.size | lineP.size)) {
          let bannerV = String.fromCharCode(117,110,105,109,112,108,101,109,101,110,116,101,100,95,57,95,54,56,0);
          let modityR = String.fromCharCode(103,95,50,54,95,117,110,115,97,118,101,0);
          let libfbw = String.fromCharCode(97,112,112,101,97,114,105,110,103,95,56,95,51,50,0);
         lineP.set(bannerV, 1);
         bannerV = `${modityR.length * libfbw.length}`;
         modityR = `${(modityR == String.fromCharCode(111,0) ? libfbw.length : modityR.length)}`;
      }
      while ((lineP.size | 2) <= 5) {
         lineP.set(`${lineP.size}`, 1);
         break;
      }
      launchern.set(`${embede}`, parseInt(`${embede}`) % (Math.max(1, 7)));
      libavfilter9.set(runtimeschedulerm, (runtimeschedulerm == String.fromCharCode(55,0) ? (homeactiveF ? 2 : 4) : runtimeschedulerm.length));
       let windr: Map<any, any> = new Map([[String.fromCharCode(116,95,51,49,95,122,101,114,111,105,110,103,0),String.fromCharCode(111,95,53,54,95,98,114,107,116,105,109,101,103,109,0)], [String.fromCharCode(111,95,49,56,95,115,108,105,100,0),String.fromCharCode(108,95,54,55,95,102,108,111,97,116,105,110,103,0)], [String.fromCharCode(111,95,53,53,95,115,117,98,118,97,108,117,101,0),String.fromCharCode(112,101,114,99,95,115,95,56,56,0)]]);
      for (let p = 0; p < 2; p++) {
         windr.set(`${windr.size}`, windr.size);
      }
      if (windr.size > windr.size) {
         windr = new Map([[`${windr.size}`, windr.size ^ windr.size]]);
      }
         windr.set(`${windr.size}`, 3 << (Math.min(5, Math.abs(windr.size))));
      embede += ((targets ? 1 : 4) + whitevideolivey);
      embede /= Math.max(4, qaagC.length);
   for (let n = 0; n < 2; n++) {
      classesH %= Math.max(parseInt(`${embede}`) + 3, 2);
   }
   if (!iconclosewhiteE.includes(`${uimanagerU.length}`)) {
      uimanagerU = `${((targets ? 3 : 3) | parseInt(`${embede}`))}`;
   }
   let umenga = 8261260 <= classesH;
   do {
       let encrypt8 = 4.0;
       let actionsS = 5.0;
       let bangU = String.fromCharCode(115,95,49,48,48,95,97,102,102,105,110,105,116,105,101,115,0);
      let found3 = 8477978 >= bangU.length;
      do {
         bangU = `${bangU.length | parseInt(`${actionsS}`)}`;
         if (found3) {
            break;
         }
      } while (found3 && ((bangU.length << (Math.min(Math.abs(3), 1))) >= 3));
      let halffieldimage1 = 7622383.0 <= actionsS;
      do {
         actionsS /= Math.max(2, 2);
         if (halffieldimage1) {
            break;
         }
      } while (halffieldimage1 && ((3 * bangU.length) == 3));
         encrypt8 -= parseFloat(`${parseInt(`${actionsS}`)}`);
         actionsS -= parseInt(`${encrypt8}`);
      for (let u = 0; u < 3; u++) {
          let androidx = 5.0;
         bangU = `${1 & parseInt(`${encrypt8}`)}`;
         androidx += 2;
      }
          let halfI = 0.0;
         bangU = "1";
         halfI += 1 ^ parseInt(`${halfI}`);
      let iconpipexpanda = actionsS >= 7032604.0;
      do {
         actionsS *= parseInt(`${encrypt8}`);
         if (iconpipexpanda) {
            break;
         }
      } while ((4.32 <= (actionsS - 1.25) || 1.25 <= (bangU.length - actionsS)) && iconpipexpanda);
      for (let x = 0; x < 1; x++) {
         actionsS *= parseInt(`${actionsS}`) | bangU.length;
      }
         bangU = `${bangU.length}`;
      classesH &= parseInt(`${coreh}`);
      if (umenga) {
         break;
      }
   } while ((1 < (4 | launchern.size)) && umenga);
   let orientationk = clocke ? !clocke : clocke;
   do {
       let matches0 = true;
       let basketballtrophy7: Array<any> = [345, 708];
       let iconstarcm = false;
       let diceH = String.fromCharCode(121,95,49,53,95,115,111,108,105,115,116,101,110,0);
         matches0 = basketballtrophy7.length <= diceH.length;
       let bootsplashm = String.fromCharCode(106,95,53,55,95,105,100,108,101,0);
      let frame_ee = iconstarcm ? !iconstarcm : iconstarcm;
      do {
         iconstarcm = String.fromCharCode(70,0) == diceH;
         if (frame_ee) {
            break;
         }
      } while (frame_ee && (2 <= basketballtrophy7.length));
       let reactY = String.fromCharCode(117,101,117,101,95,111,95,49,56,0);
         diceH = `${(bootsplashm == String.fromCharCode(80,0) ? (iconstarcm ? 5 : 3) : bootsplashm.length)}`;
         basketballtrophy7 = [reactY.length % (Math.max(3, 6))];
         matches0 = (35 == (diceH.length + (!iconstarcm ? 35 : diceH.length)));
          let layoutW = String.fromCharCode(101,110,99,97,112,115,117,108,97,116,101,100,95,55,95,51,56,0);
         bootsplashm = `${reactY.length / (Math.max(3, 3))}`;
         layoutW = `${(layoutW == String.fromCharCode(53,0) ? layoutW.length : layoutW.length)}`;
         matches0 = 20 < basketballtrophy7.length && diceH == String.fromCharCode(66,0);
          let mappingA = 0;
          let yellowscoreballE = String.fromCharCode(110,101,103,111,116,105,97,116,101,100,95,99,95,49,0);
         matches0 = ((basketballtrophy7.length * (iconstarcm ? basketballtrophy7.length : 22)) == 22);
         mappingA += 1 % (Math.max(9, yellowscoreballE.length));
         yellowscoreballE += "3";
      for (let o = 0; o < 3; o++) {
         basketballtrophy7 = [((matches0 ? 4 : 3))];
      }
          let templateprocessorD = String.fromCharCode(97,109,101,120,95,106,95,50,56,0);
          let cricket4 = 4.0;
          let loginx = String.fromCharCode(111,110,101,111,102,95,114,95,55,55,0);
         basketballtrophy7.push((parseInt(`${cricket4}`) << (Math.min(4, Math.abs((iconstarcm ? 5 : 4))))));
         templateprocessorD += `${templateprocessorD.length * loginx.length}`;
         cricket4 += 2 | loginx.length;
      clocke = diceH.length > 85;
      if (orientationk) {
         break;
      }
   } while ((configu.length >= 3 || !clocke) && orientationk);
       let latnL = 5;
       let humidityT: Array<any> = [763, 147];
         humidityT.push(3 >> (Math.min(5, humidityT.length)));
      while (4 < (latnL % 2)) {
         humidityT = [latnL * 2];
         break;
      }
      for (let z = 0; z < 3; z++) {
         latnL += latnL;
      }
      let runtimej = 9502679 <= latnL;
      do {
         latnL += humidityT.length;
         if (runtimej) {
            break;
         }
      } while (runtimej && (4 >= (latnL % (Math.max(3, 2)))));
      if (1 >= humidityT.length) {
         latnL ^= humidityT.length;
      }
         latnL /= Math.max(humidityT.length << (Math.min(4, Math.abs(latnL))), 1);
      embede += latnL;
      whitevideolivey ^= qaagC.length;
   while (!targets) {
      embede += ((targets ? 5 : 2) | 3);
      break;
   }
   if (qaagC.length == 3) {
      qaagC = `${iconclosewhiteE.length % (Math.max(runtimeschedulerm.length, 2))}`;
   }
   for (let l = 0; l < 1; l++) {
       let orangeU = String.fromCharCode(112,95,51,95,115,104,105,102,116,0);
       let specc = String.fromCharCode(105,102,102,116,95,56,95,56,53,0);
      while (orangeU.includes(`${specc.length}`)) {
          let iconV = 3;
         orangeU = `${orangeU.length >> (Math.min(2, Math.abs(iconV)))}`;
         break;
      }
       let sendL = String.fromCharCode(109,95,48,95,118,111,114,100,105,112,108,111,109,0);
       let inouttargetredL = String.fromCharCode(107,95,56,52,95,111,100,100,0);
      let score5 = sendL == String.fromCharCode(108,122,113,121,113,54,114,0);
      do {
         sendL = `${specc.length}`;
         if (score5) {
            break;
         }
      } while ((!sendL.endsWith(inouttargetredL)) && score5);
       let runtimel = 3.0;
       let analyticM = 3.0;
         analyticM += parseFloat(`${3}`);
         analyticM -= parseFloat(`${sendL.length & 3}`);
      qaagC = `${specc.length}`;
   }
      coreh *= 3 << (Math.min(4, iconclosewhiteE.length));
   for (let z = 0; z < 2; z++) {
       let becomeV = String.fromCharCode(98,95,55,52,95,102,112,117,116,115,0);
       let matchesH = String.fromCharCode(105,109,112,111,114,116,101,114,95,114,95,50,56,0);
       let linee: Array<any> = [String.fromCharCode(114,95,57,56,95,102,117,108,102,105,108,108,0), String.fromCharCode(109,105,110,109,97,120,95,107,95,52,54,0), String.fromCharCode(112,95,57,49,95,112,114,111,98,97,98,105,108,105,116,105,101,115,0)];
       let basketballY = 2.0;
       let yellowanimationlivec = 3.0;
         basketballY -= linee.length;
       let mapbufferR = 2;
      while ((5.82 / (Math.max(3, yellowanimationlivec))) > 5.49 && (basketballY / (Math.max(7, yellowanimationlivec))) > 5.82) {
          let textinputv = 1;
          let dpluss: Array<any> = [964, 587];
          let gnewsJ = 4.0;
         basketballY /= Math.max(3, 1);
         textinputv >>= Math.min(Math.abs(textinputv ^ 2), 2);
         dpluss.push(dpluss.length >> (Math.min(Math.abs(2), 3)));
         gnewsJ += parseInt(`${gnewsJ}`);
         break;
      }
         becomeV += `${parseInt(`${yellowanimationlivec}`)}`;
         linee.push(mapbufferR >> (Math.min(Math.abs(parseInt(`${basketballY}`)), 5)));
      let bridge9 = String.fromCharCode(56,53,113,112,97,116,110,56,117,50,0) == matchesH;
      do {
          let footballtrophy_ = String.fromCharCode(106,95,55,51,95,102,97,99,116,111,114,0);
          let launchP: Array<any> = [45, 644, 394];
          let readC = 2.0;
          let chart4: Map<any, any> = new Map([[String.fromCharCode(100,105,97,103,114,97,109,95,100,95,56,49,0),938], [String.fromCharCode(104,113,97,100,115,112,95,115,95,54,51,0),948]]);
         matchesH += "3";
         footballtrophy_ += `${footballtrophy_.length >> (Math.min(Math.abs(3), 3))}`;
         launchP = [parseInt(`${readC}`)];
         readC *= 2;
         chart4.set(`${footballtrophy_}`, (String.fromCharCode(78,0) == footballtrophy_ ? footballtrophy_.length : chart4.size));
         if (bridge9) {
            break;
         }
      } while (bridge9 && (matchesH.length >= linee.length));
          let libswscaleM = String.fromCharCode(101,110,116,114,121,95,55,95,49,0);
         linee.push(2);
         libswscaleM += `${libswscaleM.length / 2}`;
      let profileW = 9486284 >= mapbufferR;
      do {
          let unselectedN: Map<any, any> = new Map([[String.fromCharCode(117,95,51,50,95,102,105,97,116,0),537], [String.fromCharCode(98,121,116,101,115,104,117,109,97,110,95,57,95,57,57,0),884]]);
          let bannerm: Array<any> = [String.fromCharCode(109,95,50,56,95,104,100,110,111,100,101,0), String.fromCharCode(97,95,51,54,95,97,108,105,118,101,0), String.fromCharCode(103,99,109,95,120,95,49,56,0)];
         mapbufferR >>= Math.min(Math.abs(parseInt(`${yellowanimationlivec}`)), 4);
         unselectedN = new Map([[`${unselectedN.size}`, unselectedN.size / (Math.max(bannerm.length, 6))]]);
         bannerm = [bannerm.length << (Math.min(1, Math.abs(unselectedN.size)))];
         if (profileW) {
            break;
         }
      } while (profileW && (linee.length == mapbufferR));
      if ((5 >> (Math.min(3, Math.abs(mapbufferR)))) >= 3) {
         mapbufferR ^= mapbufferR * becomeV.length;
      }
      if (linee.length >= 4) {
          let graphicsE = false;
          let s_hashe = String.fromCharCode(112,95,53,54,95,109,97,116,114,105,120,105,110,103,0);
          let areay = String.fromCharCode(114,109,117,108,116,105,112,108,105,99,97,116,105,111,110,95,97,95,50,55,0);
         linee.push(3 % (Math.max(3, matchesH.length)));
         graphicsE = s_hashe.length == 46;
         s_hashe += `${((graphicsE ? 2 : 3) >> (Math.min(areay.length, 3)))}`;
         areay += `${((graphicsE ? 3 : 4) << (Math.min(Math.abs(2), 3)))}`;
      }
       let countryj = String.fromCharCode(119,95,52,57,95,110,101,119,101,114,0);
         becomeV += `${linee.length}`;
       let settingsm = 0;
       let commentN = 0;
       let f_playerw = String.fromCharCode(108,95,57,49,95,101,100,105,116,105,110,103,0);
       let orangeuparrowC = String.fromCharCode(115,116,114,117,99,116,117,114,101,95,51,95,51,55,0);
       let downs = false;
       let bgvipxvodR = true;
      with_4b += `${matchesH.length}`;
   }
      coreh *= configu.length;
      coreh -= launchern.size;
 },
}: mayi_GoogleViews) {
  const { colors, textVariants, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const navigation = useNavigation();
  const [showLoading, setShowLoading] = useState(false);
  const userState = useSelector<mayi_Baseline>('userReducer');
  const isVip = mayi_Gift.isVip(userState.user);
  const [totalPage, setTotalPage] = useState(1);
  const [results, setResults] = useState<Array<mayi_EmojiheartChinasame>>([]);

  
  
  //     if ((item?.vod_list?.length ?? 0) === 0) return <></>;

  
  
  
  
  
  
  
  
  
  
  
  
  
  //       >
  
  
  
  
  
  
  
  //         />
  
  
  
  
  
  
  
  
  
  //             />
  
  
  //       </View>
  
  
  
  

  
  function listItem({ item, index }) {
    if ((item?.vod_list?.length ?? 0) === 0) return <></>;

    return (
      <View
        key={`${item.type_name}-${index}`}
        style={{
          gap: spacing.m,
          paddingLeft: spacing.sideOffset,
          paddingRight: spacing.sideOffset,
          backgroundColor: "#0c0c0c",
          borderRadius: 15,
          marginBottom: 15,
          paddingTop: 4,
          paddingBottom: 4,
        }}
      >
        <ShowMoreVodButton
          text={item.type_name.trim()}
          onPress={() => {
            navigation.navigate("午夜场剧情", {
              class: item.type_name,
              vod_source_name: item.vod_source_name
            });
          }}
        />
        {
          item?.vod_list && (
            <VodListVerticalVip
              numOfRows={2}
              vods={item?.vod_list}
              minNumPerRow={2}
              heightToWidthRatio={1 / 1.814}
              playerMode="adult"
            />
          )
        }
      </View>
    );
  }

  const fetchXCategories = (page: number) =>
    mayi_Iconclosewhite.getXVodList(page).then((results: Array<mayi_EmojiheartChinasame>) => {
      setTotalPage(9);
      console.log("HEH");
      return Object.values(results);
    });

  const {
    data: xData,
    isSuccess,
    hasNextPage,
    fetchNextPage,
    isFetchingNextPage,
    isFetching,
    refetch,
  } = useInfiniteQuery(
    ['xvodPlayList'],
    ({pageParam = 1}) => fetchXCategories(pageParam),
    {
      getNextPageParam: (lastPage, allPages) => {
        if (lastPage === null) {
          return undefined;
        }
        const nextPage: any = allPages.length + 1;
        
        if (nextPage > totalPage && totalPage != 0) {
          return undefined;
        }
        return nextPage;
      },
      onSuccess: data => {
        if (data && data?.pages) {
          setResults([...results, ...data.pages[data.pages.length - 1].flat()]);
        }
      },
    },
  );

  useEffect(() => {
    if (results == undefined) {
      setShowLoading(true);
    }
  }, []);

  useEffect(() => {
    if(results.length == 0){
      refetch();
    }
  }, [results])

  return (
    <SafeAreaView style={{ flex: 1 }}>
      {showLoading && (
        <View
          style={{
            position: "absolute",
            
            backgroundColor: "#0c0c0c",
            zIndex: 1,
            width: "100%",
            height: "100%",
            justifyContent: "center",
            alignItems: "center",
            borderRadius: 15,

          }}
        >
          <FastImage
            source={require("@static/images/robotoNetwork.gif")}
            style={{ width: 100, height: 100 }}
            resizeMode="contain"
          />
        </View>
      )}

      <View style={{ paddingTop: 10 }}>
        <FlatList
          data={results}
          keyExtractor={(item: mayi_Code, index: number) => index.toString() + item.type_name}
          initialNumToRender={0}
          windowSize={3}
          maxToRenderPerBatch={3}
          renderItem={listItem}
          contentContainerStyle={{ paddingBottom: 60 }}
          removeClippedSubviews={true}
          onEndReachedThreshold={0.5}
          scrollEnabled={isVip}
          onEndReached={() => {
            if (hasNextPage && !isFetchingNextPage && !isFetching) {
              fetchNextPage();
            }
          }}
          ListFooterComponent={
            <View style={{...styles.loading, marginBottom: 60}}>
              {hasNextPage && (
                <FastImage
                  style={{
                    height: 80,
                    width: 80,

                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                  }}
                  source={require('@static/images/robotoNetwork.gif')}
                  resizeMode={'contain'}
                />
              )}
              {!(isFetchingNextPage || isFetching) && !hasNextPage && (
                <Text
                  style={{
                    ...textVariants.subBody,
                    color: colors.muted,
                    paddingTop: 12,
                  }}>
                  已经到底了
                </Text>
              )}
            </View>
          }
        />
      </View>
      { }
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  loading: {
    flexDirection: "row",
    justifyContent: "center",
    flex: 1,
  },
});
