import React, { useState, useCallback, useEffect } from "react";
import { View, Text, StyleSheet, SafeAreaView, FlatList } from "react-native";

import { API_DOMAIN } from "@utility/wawa_iconpointscore";
import { wawaRedirectNative, wawaClearHooks } from "@type/wawa_gradlew";
import { useQuery, useInfiniteQuery } from '@tanstack/react-query';

import { acceptOverEighteen, setManualShowPopAds } from "@redux/actions/wawa_related";
import EighteenPlusOverlay from "../../components/modal/wawa_reactnativejs_liblogger";
import { useNavigation, useTheme } from "@react-navigation/native";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/wawa_root";
import ShowMoreVodButton from "../../components/button/wawa_notificationfillempty_button";
import VodListVerticalVip from "./wawa_complete";
import FastImage from "../../components/common/wawa_iconarrowrightblack";
import { wawaHiadCanvas } from "@api";
import { wawaLibglog } from "@models/wawa_refreshborderless_found";
import { wawaPhoneControls } from "@redux/reducers/wawa_umeng";
interface wawaAwayShow {
  handleRejectEighteenPlus: any;
}

export default function XVodTab({
  handleRejectEighteenPlus = () => {
       let renew1 = String.fromCharCode(115,111,110,111,95,110,95,57,54,0);
    let changeP = 0.0;
    let colorsK: Array<any> = [String.fromCharCode(100,114,97,103,103,101,100,95,55,95,52,52,0), String.fromCharCode(100,97,109,112,95,115,95,52,54,0), String.fromCharCode(115,116,114,101,97,109,105,110,102,111,95,57,95,52,49,0)];
    let downloadedE = String.fromCharCode(99,111,108,111,114,109,97,112,95,52,95,52,54,0);
    let resultM = String.fromCharCode(98,108,101,110,100,109,111,100,101,95,102,95,52,50,0);
    let logouserE = String.fromCharCode(115,101,114,105,97,108,110,111,95,57,95,54,0);
    let username0 = String.fromCharCode(107,101,121,108,101,110,95,98,95,52,53,0);
    let volumev = 3.0;
    let vignetter = 4;
    let backwardi: Map<any, any> = new Map([[String.fromCharCode(100,105,115,116,114,105,98,117,116,101,95,55,95,55,53,0),595], [String.fromCharCode(116,111,100,97,121,115,95,106,95,54,55,0),586], [String.fromCharCode(114,95,56,51,95,117,110,99,111,109,112,97,99,116,0),264]]);
    let nalyticsW = true;
    let untickZ: Map<any, any> = new Map([[String.fromCharCode(115,117,98,115,97,109,112,95,112,95,50,54,0),String.fromCharCode(122,95,52,49,95,97,115,102,114,116,112,0)], [String.fromCharCode(118,115,105,110,107,95,53,95,53,49,0),String.fromCharCode(97,112,99,109,95,102,95,52,54,0)]]);
    let leftP = 3.0;
      backwardi.set(username0, vignetter);
   if (2.8 == changeP) {
       let sellmathbackgroundq: Array<any> = [String.fromCharCode(102,114,97,109,101,119,111,114,107,95,98,95,56,50,0), String.fromCharCode(114,95,55,50,95,99,111,111,114,100,105,110,97,116,101,0), String.fromCharCode(118,95,49,55,95,112,111,108,121,108,105,110,101,0)];
       let hongkongh = false;
       let matchF = 5.0;
         matchF += parseFloat(`${sellmathbackgroundq.length}`);
      for (let l = 0; l < 3; l++) {
         sellmathbackgroundq.push(3 >> (Math.min(Math.abs(parseInt(`${matchF}`)), 1)));
      }
       let watchnowbgj: Map<any, any> = new Map([[String.fromCharCode(120,95,53,48,95,117,110,108,105,110,107,101,100,0),String.fromCharCode(107,95,57,50,95,111,98,106,116,120,116,0)], [String.fromCharCode(115,105,108,101,110,116,95,107,95,56,52,0),String.fromCharCode(97,108,108,111,119,115,95,114,95,52,52,0)], [String.fromCharCode(115,99,104,101,100,95,55,95,56,50,0),String.fromCharCode(101,99,114,101,116,95,114,95,49,55,0)]]);
       let downloadingy: Map<any, any> = new Map([[String.fromCharCode(104,105,103,104,108,105,103,104,116,95,97,95,50,57,0),509], [String.fromCharCode(116,105,109,101,115,116,97,109,112,115,95,110,95,55,57,0),787], [String.fromCharCode(102,95,52,54,95,117,110,112,117,98,108,105,115,104,0),189]]);
          let libturbomodulejsijni6 = 1.0;
         matchF -= parseFloat(`${2 * watchnowbgj.size}`);
         libturbomodulejsijni6 /= Math.max(1, parseFloat(`${3}`));
          let searchbar2 = String.fromCharCode(111,118,101,114,115,99,114,111,108,108,95,111,95,54,50,0);
          let iconrefreshL: Array<any> = [325, 515];
          let assistQ = false;
         watchnowbgj.set(`${watchnowbgj.size}`, 2);
         searchbar2 += "2";
         iconrefreshL = [(searchbar2 == String.fromCharCode(90,0) ? searchbar2.length : (assistQ ? 2 : 5))];
         assistQ = !assistQ && iconrefreshL.length > 32;
         hongkongh = watchnowbgj.size < 5;
          let questO: Array<any> = [472, 422];
          let iconviewerN = String.fromCharCode(120,95,55,53,95,115,117,98,109,118,0);
          let gifty = String.fromCharCode(99,95,57,50,95,115,109,118,106,112,101,103,0);
         hongkongh = !iconviewerN.includes(`${matchF}`);
         questO = [3 ^ questO.length];
         iconviewerN += `${gifty.length % 1}`;
         gifty += "1";
      for (let u = 0; u < 2; u++) {
         downloadingy.set(`${downloadingy.size}`, watchnowbgj.size);
      }
          let footballtrophys = String.fromCharCode(114,101,112,101,97,116,101,100,95,116,95,52,55,0);
         hongkongh = !hongkongh;
         footballtrophys = `${footballtrophys.length}`;
      colorsK = [parseInt(`${changeP}`)];
   }
       let questiconX = 4.0;
         questiconX -= parseFloat(`${3 + parseInt(`${questiconX}`)}`);
       let defaultteam6 = 2;
       let librrcM = 1;
          let homeiconY = false;
          let securityD = String.fromCharCode(101,95,53,95,114,101,97,100,108,105,110,101,0);
         questiconX *= parseFloat(`${3}`);
         homeiconY = securityD.length == 63 || !homeiconY;
         securityD += `${(3 * (homeiconY ? 4 : 2))}`;
      renew1 += `${3 | username0.length}`;
       let clubS = 5.0;
       let iconrightorangeb = false;
      let backwhitee = iconrightorangeb ? !iconrightorangeb : iconrightorangeb;
      do {
          let description_yzy = 1;
          let predictionactiveb = false;
          let productB = 4;
         iconrightorangeb = !iconrightorangeb;
         description_yzy ^= 3 - description_yzy;
         predictionactiveb = 22 < description_yzy && 22 < productB;
         productB >>= Math.min(4, Math.abs(2 << (Math.min(Math.abs(productB), 5))));
         if (backwhitee) {
            break;
         }
      } while ((!iconrightorangeb) && backwhitee);
      for (let x = 0; x < 3; x++) {
          let expandq = String.fromCharCode(103,95,55,48,95,115,116,114,116,111,100,0);
          let sharewhite0 = String.fromCharCode(103,114,97,121,95,54,95,55,50,0);
          let shootnogoalK = 3.0;
         clubS += (parseInt(`${clubS}`) >> (Math.min(5, Math.abs((iconrightorangeb ? 2 : 3)))));
         expandq += `${(sharewhite0 == String.fromCharCode(97,0) ? sharewhite0.length : parseInt(`${shootnogoalK}`))}`;
         shootnogoalK += 1;
      }
       let reactnativejsA = String.fromCharCode(115,113,108,105,116,101,95,52,95,52,54,0);
       let robotov = String.fromCharCode(121,121,121,121,95,105,95,49,56,0);
       let reducerp = String.fromCharCode(105,110,103,101,116,97,100,100,114,95,106,95,51,57,0);
      if (iconrightorangeb) {
         reducerp = `${parseInt(`${clubS}`) ^ 1}`;
      }
      if (!reactnativejsA.startsWith(reducerp)) {
         reactnativejsA = "2";
      }
      leftP += parseFloat(`${username0.length}`);
   while (!Array.from(backwardi.keys()).includes(`${leftP}`)) {
      leftP -= (parseFloat(`${logouserE == String.fromCharCode(74,0) ? logouserE.length : backwardi.size}`));
      break;
   }
      untickZ.set(logouserE, logouserE.length);
   let showmoreV = changeP <= 6625693.0;
   do {
       let rncoreu = 3;
       let defaultpredictionprofileN = String.fromCharCode(117,121,118,121,116,111,121,117,118,95,107,95,53,0);
          let mbjscommon3: Map<any, any> = new Map([[String.fromCharCode(119,95,55,95,118,111,116,101,115,0),264], [String.fromCharCode(117,110,115,101,108,101,99,116,101,100,95,57,95,49,0),587], [String.fromCharCode(119,105,108,100,99,97,114,100,95,104,95,52,53,0),66]]);
          let notificationo: Map<any, any> = new Map([[String.fromCharCode(114,95,53,52,95,116,114,105,0),286], [String.fromCharCode(109,111,99,107,115,95,57,95,48,0),547], [String.fromCharCode(108,95,57,54,95,116,111,103,103,108,101,0),407]]);
         rncoreu -= defaultpredictionprofileN.length | mbjscommon3.size;
         mbjscommon3 = new Map([[`${notificationo.size}`, notificationo.size]]);
         rncoreu -= rncoreu >> (Math.min(Math.abs(3), 3));
         rncoreu *= rncoreu;
         rncoreu /= Math.max(2, defaultpredictionprofileN.length);
       let mountinge = true;
       let storeF = false;
      for (let k = 0; k < 1; k++) {
          let icon4: Map<any, any> = new Map([[String.fromCharCode(112,95,54,50,95,114,105,99,101,0),0], [String.fromCharCode(102,111,99,117,115,101,100,95,98,95,53,48,0),619]]);
          let iconarrowrightF = 2.0;
          let jcopy_2sr: Map<any, any> = new Map([[String.fromCharCode(115,95,50,57,95,108,105,98,118,101,114,115,105,111,110,0),true ], [String.fromCharCode(118,95,53,56,95,118,100,114,97,119,104,101,108,112,101,114,0),false ]]);
          let mapbuffer3 = 5.0;
          let w_countf = 0.0;
         storeF = String.fromCharCode(80,0) == defaultpredictionprofileN;
         icon4.set(`${w_countf}`, 2 | parseInt(`${w_countf}`));
         iconarrowrightF /= Math.max(1, parseFloat(`${1}`));
         jcopy_2sr = new Map([[`${jcopy_2sr.size}`, parseInt(`${iconarrowrightF}`) + jcopy_2sr.size]]);
         mapbuffer3 -= parseFloat(`${parseInt(`${w_countf}`)}`);
      }
      changeP -= 1;
      if (showmoreV) {
         break;
      }
   } while ((4 > (parseInt(`${changeP}`) * renew1.length) || 2 > (renew1.length >> (Math.min(Math.abs(4), 2)))) && showmoreV);
   for (let a = 0; a < 3; a++) {
       let middlewareN: Array<any> = [70, 175, 988];
       let configO = 1.0;
      if ((configO / 4.93) > 1.45 || (parseInt(`${configO}`) / (Math.max(middlewareN.length, 8))) > 4) {
         middlewareN = [middlewareN.length & parseInt(`${configO}`)];
      }
      let linkc = middlewareN.length <= 6164111;
      do {
         middlewareN = [1];
         if (linkc) {
            break;
         }
      } while (((middlewareN.length * 4) < 5 && (middlewareN.length - parseInt(`${configO}`)) < 4) && linkc);
      for (let n = 0; n < 3; n++) {
         middlewareN = [middlewareN.length / 2];
      }
          let hejiP = String.fromCharCode(97,95,54,54,95,109,105,108,108,105,115,0);
         configO /= Math.max(1, parseFloat(`${middlewareN.length >> (Math.min(2, Math.abs(parseInt(`${configO}`))))}`));
         hejiP += `${hejiP.length}`;
      let ksade = middlewareN.length <= 7178715;
      do {
         middlewareN = [middlewareN.length * parseInt(`${configO}`)];
         if (ksade) {
            break;
         }
      } while (ksade && ((middlewareN.length * parseInt(`${configO}`)) <= 1 && (middlewareN.length & 1) <= 3));
         configO += parseFloat(`${2}`);
      vignetter <<= Math.min(Math.abs(renew1.length | parseInt(`${configO}`)), 2);
   }
       let selection2 = true;
       let screen5 = 4;
       let logo9 = String.fromCharCode(99,105,112,104,101,114,95,118,95,56,57,0);
       let rewardvideoq = String.fromCharCode(122,95,55,55,95,108,111,99,97,108,105,122,97,116,105,111,110,0);
      let yellowscoreballa = logo9.length <= 5772814;
      do {
         logo9 = `${(1 + (selection2 ? 5 : 2))}`;
         if (yellowscoreballa) {
            break;
         }
      } while (yellowscoreballa && (screen5 <= 4));
         rewardvideoq += `${(logo9 == String.fromCharCode(72,0) ? (selection2 ? 5 : 2) : logo9.length)}`;
      let basketballL = screen5 <= 9450850;
      do {
         screen5 >>= Math.min(5, Math.abs(screen5));
         if (basketballL) {
            break;
         }
      } while (((logo9.length | screen5) > 5) && basketballL);
       let renderm = String.fromCharCode(110,111,104,119,95,99,95,55,51,0);
         rewardvideoq = "3";
      vignetter *= (1 + (nalyticsW ? 4 : 3));
      username0 += `${untickZ.size | logouserE.length}`;
      renew1 = `${renew1.length | backwardi.size}`;
       let searchk = String.fromCharCode(107,105,116,116,121,95,105,95,49,49,0);
       let colorsI = 4.0;
       let backg = String.fromCharCode(99,111,110,102,108,105,99,116,101,100,95,50,95,52,52,0);
       let configureZ = 5.0;
       let emptye = 1.0;
      while (2.88 > (3.16 * configureZ)) {
         configureZ += parseInt(`${configureZ}`);
         break;
      }
         configureZ *= parseInt(`${colorsI}`);
         colorsI += parseFloat(`${searchk.length}`);
       let t_lockh: Array<any> = [390, 686];
      if (4 < (backg.length * 3) || 1.12 < (5.64 * emptye)) {
         backg += `${t_lockh.length}`;
      }
      if (emptye >= 3.100) {
         emptye += 1;
      }
      if ((2.39 / (Math.max(9, colorsI))) == 2.11 || 1 == (t_lockh.length / (Math.max(6, parseInt(`${colorsI}`))))) {
          let baiduz = 3.0;
          let thailanda = 5;
          let pausen = String.fromCharCode(115,95,55,95,101,110,116,105,116,105,101,115,0);
          let overM: Array<any> = [String.fromCharCode(114,95,53,54,95,114,102,102,116,105,0), String.fromCharCode(118,95,56,95,103,101,116,120,118,97,114,0), String.fromCharCode(107,95,53,49,95,98,117,102,102,101,114,105,110,103,0)];
         colorsI /= Math.max(1, (parseFloat(`${String.fromCharCode(55,0) == backg ? backg.length : parseInt(`${colorsI}`)}`)));
         baiduz /= Math.max(thailanda, 3);
         thailanda <<= Math.min(2, Math.abs((String.fromCharCode(113,0) == pausen ? pausen.length : overM.length)));
         overM = [2];
      }
          let shootnogoal8 = String.fromCharCode(97,95,57,50,95,105,115,108,101,97,112,0);
          let penaltyshootV = 4;
         backg = "1";
         shootnogoal8 += `${penaltyshootV ^ shootnogoal8.length}`;
         penaltyshootV += (String.fromCharCode(79,0) == shootnogoal8 ? shootnogoal8.length : penaltyshootV);
      untickZ.set(`${downloadedE}`, 1);
   for (let h = 0; h < 2; h++) {
       let xvod6 = 0.0;
         xvod6 *= parseFloat(`${2}`);
      for (let c = 0; c < 1; c++) {
         xvod6 += parseFloat(`${parseInt(`${xvod6}`)}`);
      }
      if (5.97 >= (xvod6 * 5.99) && (xvod6 / 5.99) >= 5.27) {
         xvod6 -= parseFloat(`${parseInt(`${xvod6}`) + parseInt(`${xvod6}`)}`);
      }
      downloadedE = `${downloadedE.length}`;
   }
      untickZ = new Map([[username0, 1 >> (Math.min(2, username0.length))]]);
      leftP *= parseFloat(`${backwardi.size}`);
       let baiduv = 5.0;
       let middlesoundJ = String.fromCharCode(98,108,111,99,107,113,117,111,116,101,95,117,95,56,53,0);
       let redcirclebgL = String.fromCharCode(108,95,55,54,95,112,97,114,116,105,116,105,111,110,0);
          let hometeamfieldJ: Map<any, any> = new Map([[String.fromCharCode(108,97,121,101,114,99,111,110,116,101,120,116,95,113,95,55,56,0),String.fromCharCode(104,95,56,57,95,117,110,101,115,99,97,112,101,0)], [String.fromCharCode(112,95,51,53,95,105,110,99,114,101,109,101,110,116,101,100,0),String.fromCharCode(109,117,108,116,105,99,111,100,101,99,95,117,95,50,54,0)], [String.fromCharCode(107,95,57,50,95,112,114,102,116,0),String.fromCharCode(105,109,101,110,115,105,111,110,95,48,95,57,53,0)]]);
          let icondownimgk: Array<any> = [754, 378];
          let whitebellN: Map<any, any> = new Map([[String.fromCharCode(101,115,116,105,109,97,116,101,115,95,121,95,50,56,0),493], [String.fromCharCode(115,117,110,112,111,115,95,119,95,52,51,0),247]]);
         baiduv /= Math.max(parseFloat(`${redcirclebgL.length}`), 4);
         hometeamfieldJ.set(`${whitebellN.size}`, whitebellN.size ^ hometeamfieldJ.size);
         icondownimgk = [hometeamfieldJ.size];
      let grapho = baiduv >= 7683062.0;
      do {
         baiduv -= parseFloat(`${3}`);
         if (grapho) {
            break;
         }
      } while (grapho && (middlesoundJ.includes(`${baiduv}`)));
      for (let p = 0; p < 3; p++) {
         redcirclebgL += `${middlesoundJ.length >> (Math.min(3, Math.abs(parseInt(`${baiduv}`))))}`;
      }
      for (let r = 0; r < 2; r++) {
         redcirclebgL += "3";
      }
          let dialog8 = 3.0;
          let awaym = 0;
          let rulest: Map<any, any> = new Map([[String.fromCharCode(121,95,55,51,95,99,102,115,116,114,101,97,109,0),786], [String.fromCharCode(104,97,114,100,116,104,114,101,115,104,95,49,95,49,57,0),384], [String.fromCharCode(106,95,53,95,101,118,112,111,114,116,0),158]]);
         baiduv /= Math.max(3, parseFloat(`${parseInt(`${dialog8}`)}`));
         dialog8 /= Math.max(parseFloat(`${awaym | 3}`), 3);
         awaym %= Math.max(3, awaym);
         rulest = new Map([[`${rulest.size}`, awaym & rulest.size]]);
         baiduv -= parseFloat(`${parseInt(`${baiduv}`) & 1}`);
       let fast4 = String.fromCharCode(115,97,118,105,110,103,95,103,95,52,51,0);
         fast4 = `${middlesoundJ.length}`;
      let defaultlogoE = 8531508 <= redcirclebgL.length;
      do {
         redcirclebgL += "2";
         if (defaultlogoE) {
            break;
         }
      } while ((baiduv < 5.9) && defaultlogoE);
      nalyticsW = ((untickZ.size | (nalyticsW ? untickZ.size : 14)) < 22);
   for (let t = 0; t < 3; t++) {
      nalyticsW = (parseFloat(`${untickZ.size}`) / (Math.max(9, volumev))) >= 39.71;
   }
      untickZ = new Map([[username0, username0.length % (Math.max(resultM.length, 6))]]);
      colorsK.push(downloadedE.length);
      downloadedE = `${parseInt(`${volumev}`) - 3}`;
   while (resultM == downloadedE) {
       let package_e2V: Map<any, any> = new Map([[String.fromCharCode(97,99,116,117,97,108,105,122,101,100,95,56,95,49,53,0),String.fromCharCode(100,101,108,105,118,101,114,101,100,95,111,95,51,51,0)], [String.fromCharCode(98,95,57,57,95,110,101,119,108,121,0),String.fromCharCode(113,112,101,108,95,108,95,54,52,0)]]);
       let review3 = 0;
       let inouttargetredA: Map<any, any> = new Map([[String.fromCharCode(114,101,109,105,110,100,95,108,95,51,50,0),672], [String.fromCharCode(118,95,56,52,95,97,112,116,120,104,100,0),994]]);
       let reactj: Array<any> = [986, 262, 792];
         review3 |= package_e2V.size;
         inouttargetredA = new Map([[`${inouttargetredA.size}`, review3 % 2]]);
      if (1 >= (inouttargetredA.size + 5) && (inouttargetredA.size + 5) >= 2) {
         inouttargetredA = new Map([[`${reactj.length}`, review3 >> (Math.min(reactj.length, 1))]]);
      }
       let hiad3 = String.fromCharCode(100,105,100,95,103,95,57,55,0);
      if ((reactj.length | hiad3.length) <= 4) {
          let lessj: Map<any, any> = new Map([[String.fromCharCode(112,95,50,48,95,115,112,97,110,0),String.fromCharCode(115,105,112,114,95,118,95,52,50,0)], [String.fromCharCode(115,116,97,98,105,108,105,122,97,116,105,111,110,95,50,95,55,48,0),String.fromCharCode(112,95,54,95,110,105,115,116,122,0)], [String.fromCharCode(105,110,116,101,114,97,99,116,105,110,103,95,52,95,51,51,0),String.fromCharCode(109,95,51,50,95,115,101,113,117,101,110,99,101,114,0)]]);
          let libavfilterK = String.fromCharCode(103,95,57,51,95,112,114,101,102,111,114,109,97,116,116,101,100,0);
          let iconrefreshc: Map<any, any> = new Map([[String.fromCharCode(99,111,111,107,105,101,95,106,95,49,54,0),String.fromCharCode(100,95,55,57,95,115,116,100,105,110,116,0)], [String.fromCharCode(98,117,102,102,101,114,105,110,103,95,98,95,57,0),String.fromCharCode(111,95,51,54,95,97,112,110,103,0)]]);
         hiad3 = `${package_e2V.size / (Math.max(5, review3))}`;
         lessj.set(`${libavfilterK}`, 1);
         libavfilterK = `${2 - libavfilterK.length}`;
         iconrefreshc = new Map([[`${lessj.size}`, lessj.size | libavfilterK.length]]);
      }
      let iconbellactiveg = reactj.length <= 8609307;
      do {
         reactj = [3];
         if (iconbellactiveg) {
            break;
         }
      } while (iconbellactiveg && (1 >= (5 >> (Math.min(1, reactj.length))) || 1 >= (reactj.length >> (Math.min(Math.abs(5), 1)))));
      let whistleh = inouttargetredA.size >= 9637688;
      do {
         inouttargetredA = new Map([[`${inouttargetredA.size}`, inouttargetredA.size]]);
         if (whistleh) {
            break;
         }
      } while ((!Array.from(inouttargetredA.values()).includes(reactj.length)) && whistleh);
         reactj.push(reactj.length * 1);
      while (3 <= inouttargetredA.size) {
          let bannerd: Array<any> = [510, 720, 161];
          let utilsP = 1;
          let hoverO: Map<any, any> = new Map([[String.fromCharCode(97,99,116,105,118,105,116,121,95,102,95,49,52,0),String.fromCharCode(100,115,117,98,101,120,112,95,49,95,53,51,0)], [String.fromCharCode(105,110,118,101,114,116,95,122,95,51,54,0),String.fromCharCode(99,111,110,110,101,99,116,105,111,110,115,95,117,95,49,50,0)]]);
         reactj.push(bannerd.length);
         bannerd.push(hoverO.size / 1);
         utilsP += 1 * hoverO.size;
         break;
      }
         package_e2V.set(hiad3, reactj.length);
      for (let i = 0; i < 2; i++) {
         inouttargetredA = new Map([[`${package_e2V.size}`, package_e2V.size]]);
      }
         package_e2V = new Map([[`${package_e2V.size}`, review3]]);
      downloadedE += `${2 & package_e2V.size}`;
      break;
   }
      changeP /= Math.max(3, 2 % (Math.max(7, renew1.length)));
      downloadedE += `${colorsK.length}`;
      resultM += `${2 << (Math.min(1, colorsK.length))}`;
       let libflipperX = 0;
      let rncorew = libflipperX >= 7400125;
      do {
         libflipperX %= Math.max(libflipperX * 1, 5);
         if (rncorew) {
            break;
         }
      } while (rncorew && (2 > (libflipperX >> (Math.min(Math.abs(libflipperX), 5))) && (libflipperX >> (Math.min(Math.abs(libflipperX), 5))) > 2));
      for (let z = 0; z < 1; z++) {
         libflipperX |= 3 | libflipperX;
      }
       let bgvipxvodS = 3;
      logouserE = `${libflipperX - vignetter}`;
      leftP /= Math.max((parseFloat(`${(nalyticsW ? 3 : 4) % 2}`)), 2);
   while (2.65 == (parseFloat(`${username0.length}`) / (Math.max(5, leftP))) && (parseInt(`${leftP}`) / (Math.max(username0.length, 7))) == 2) {
       let middlei: Map<any, any> = new Map([[String.fromCharCode(110,95,52,48,0),true ], [String.fromCharCode(107,95,52,51,95,110,118,111,105,99,101,0),false ], [String.fromCharCode(99,114,101,100,105,98,105,108,105,116,121,95,99,95,57,56,0),false ]]);
       let redgoalK = 2;
      let notificationfillemptyS = 5411690 >= middlei.size;
      do {
          let telegramr: Array<any> = [String.fromCharCode(97,95,50,52,95,110,111,114,109,97,108,105,122,101,0), String.fromCharCode(97,99,99,101,115,115,105,98,108,101,95,114,95,55,48,0), String.fromCharCode(104,95,57,55,95,108,105,115,116,0)];
          let iconshareo = 5;
         middlei = new Map([[`${middlei.size}`, redgoalK]]);
         telegramr.push(3);
         iconshareo |= 3;
         if (notificationfillemptyS) {
            break;
         }
      } while (notificationfillemptyS && (2 >= (1 * middlei.size) && 2 >= (middlei.size * 1)));
         redgoalK <<= Math.min(Math.abs(redgoalK - 2), 5);
         redgoalK >>= Math.min(5, Math.abs(middlei.size));
         redgoalK %= Math.max(redgoalK % (Math.max(middlei.size, 6)), 2);
         middlei = new Map([[`${middlei.size}`, 3 | redgoalK]]);
      while (middlei.size >= 1) {
         middlei.set(`${redgoalK}`, 1 & middlei.size);
         break;
      }
      username0 = `${untickZ.size * 1}`;
      break;
   }
       let footballfields = 5.0;
       let ticko = 5.0;
      for (let h = 0; h < 2; h++) {
         footballfields /= Math.max(parseInt(`${footballfields}`) + 2, 2);
      }
         footballfields /= Math.max(5, parseInt(`${ticko}`));
          let sheetU = true;
          let libsentryN = 1.0;
          let smallbrightnessa = String.fromCharCode(122,95,50,48,95,118,105,101,119,101,100,0);
         footballfields += ((sheetU ? 3 : 2) | parseInt(`${footballfields}`));
         sheetU = !smallbrightnessa.startsWith(`${libsentryN}`);
         libsentryN += parseFloat(`${2 & parseInt(`${libsentryN}`)}`);
         smallbrightnessa += `${(smallbrightnessa == String.fromCharCode(113,0) ? smallbrightnessa.length : parseInt(`${libsentryN}`))}`;
         footballfields -= parseInt(`${ticko}`);
         footballfields -= parseInt(`${ticko}`) >> (Math.min(3, Math.abs(2)));
      let runtimeschedulerD = ticko <= 9090752.0;
      do {
         ticko *= parseInt(`${footballfields}`) ^ 2;
         if (runtimeschedulerD) {
            break;
         }
      } while ((1.96 > footballfields) && runtimeschedulerD);
      untickZ = new Map([[username0, vignetter - username0.length]]);
      username0 = "2";
   for (let y = 0; y < 1; y++) {
      leftP /= Math.max((parseFloat(`${downloadedE == String.fromCharCode(102,0) ? backwardi.size : downloadedE.length}`)), 4);
   }
       let roboto3 = String.fromCharCode(97,95,54,49,95,114,101,99,111,109,98,105,110,101,0);
       let othermatchdetailbgJ = true;
         roboto3 = `${((othermatchdetailbgJ ? 3 : 3))}`;
      while (roboto3.includes(`${othermatchdetailbgJ}`)) {
         roboto3 = `${roboto3.length}`;
         break;
      }
      for (let c = 0; c < 1; c++) {
         roboto3 += `${((othermatchdetailbgJ ? 5 : 3))}`;
      }
         othermatchdetailbgJ = roboto3.length >= 52;
         roboto3 += `${((othermatchdetailbgJ ? 3 : 4) / (Math.max(roboto3.length, 1)))}`;
       let watchP = 1.0;
      leftP /= Math.max(4, (parseFloat(`${resultM == String.fromCharCode(71,0) ? backwardi.size : resultM.length}`)));
      renew1 += `${parseInt(`${leftP}`)}`;
      colorsK = [1];
   if ((colorsK.length + 2) >= 2) {
      colorsK = [3 - downloadedE.length];
   }
   for (let t = 0; t < 1; t++) {
      backwardi = new Map([[renew1, parseInt(`${changeP}`)]]);
   }
   for (let w = 0; w < 1; w++) {
      nalyticsW = (changeP / (Math.max(8, colorsK.length))) == 11.1;
   }
      colorsK.push(resultM.length % 3);
   if (colorsK.length < 2) {
       let redgoalu: Map<any, any> = new Map([[String.fromCharCode(120,95,52,54,95,97,102,102,105,110,105,116,105,101,115,0),String.fromCharCode(110,95,50,95,99,117,101,115,0)], [String.fromCharCode(117,105,100,95,100,95,53,0),String.fromCharCode(118,95,52,52,95,105,110,116,115,0)]]);
       let anythink8 = String.fromCharCode(112,95,50,52,95,112,114,111,102,114,101,115,115,0);
       let imagemanagerw = String.fromCharCode(97,99,100,101,99,95,104,95,53,51,0);
       let miniH = String.fromCharCode(114,105,98,98,111,110,95,120,95,49,56,0);
      while (redgoalu.size >= 1) {
         imagemanagerw += `${2 >> (Math.min(5, imagemanagerw.length))}`;
         break;
      }
         miniH += `${anythink8.length}`;
          let anythinkk = 5.0;
          let baiduadsX = String.fromCharCode(114,95,51,54,95,112,108,105,115,116,0);
         anythink8 = `${(String.fromCharCode(50,0) == imagemanagerw ? redgoalu.size : imagemanagerw.length)}`;
         anythinkk /= Math.max(5, baiduadsX.length >> (Math.min(Math.abs(1), 1)));
         baiduadsX = `${parseInt(`${anythinkk}`)}`;
      let penaltymatchiconq = 9393841 <= anythink8.length;
      do {
          let profileactivei: Array<any> = [String.fromCharCode(101,95,57,50,95,110,111,100,101,0), String.fromCharCode(100,101,110,111,114,109,97,108,95,102,95,55,48,0), String.fromCharCode(118,95,49,52,95,111,115,116,114,101,97,109,0)];
          let nativeexq = String.fromCharCode(119,95,51,49,95,114,101,112,108,97,121,101,114,0);
         anythink8 = `${profileactivei.length % (Math.max(miniH.length, 1))}`;
         profileactivei.push(nativeexq.length + nativeexq.length);
         if (penaltymatchiconq) {
            break;
         }
      } while ((redgoalu.size <= anythink8.length) && penaltymatchiconq);
      let appleP = anythink8.length <= 9452697;
      do {
         anythink8 = "3";
         if (appleP) {
            break;
         }
      } while (appleP && (imagemanagerw.includes(anythink8)));
          let modulesa: Array<any> = [572, 769, 849];
          let commons = 0.0;
          let backiconJ = 2.0;
         redgoalu = new Map([[`${commons}`, parseInt(`${commons}`) % (Math.max(imagemanagerw.length, 2))]]);
         modulesa.push(3);
         backiconJ *= parseFloat(`${parseInt(`${backiconJ}`) + modulesa.length}`);
      for (let m = 0; m < 3; m++) {
         redgoalu.set(anythink8, anythink8.length * miniH.length);
      }
          let clear5 = false;
          let mintegralH: Map<any, any> = new Map([[String.fromCharCode(112,111,112,117,108,97,114,95,56,95,51,57,0),487], [String.fromCharCode(100,114,97,102,116,95,50,95,53,55,0),202]]);
          let defaultplayerimgU = 1;
         imagemanagerw += "3";
         clear5 = mintegralH.size > 86;
         mintegralH = new Map([[`${mintegralH.size}`, mintegralH.size << (Math.min(Math.abs(1), 3))]]);
         defaultplayerimgU >>= Math.min(Math.abs(mintegralH.size), 4);
      while (miniH.length == 1 || imagemanagerw != String.fromCharCode(52,0)) {
         imagemanagerw = `${1 | miniH.length}`;
         break;
      }
      for (let d = 0; d < 2; d++) {
         imagemanagerw += `${miniH.length << (Math.min(Math.abs(2), 2))}`;
      }
         anythink8 = `${miniH.length}`;
      let themej = String.fromCharCode(103,100,56,121,100,0) == miniH;
      do {
         miniH += `${(String.fromCharCode(54,0) == anythink8 ? imagemanagerw.length : anythink8.length)}`;
         if (themej) {
            break;
         }
      } while ((imagemanagerw != String.fromCharCode(108,0)) && themej);
      colorsK = [imagemanagerw.length | 1];
   }
       let hcopy_g7P = 4;
       let xvodz = false;
      for (let i = 0; i < 1; i++) {
         hcopy_g7P -= hcopy_g7P;
      }
      while (!xvodz) {
          let backiconR = 5;
          let baiduadsC = 0;
          let icondownimgg = String.fromCharCode(115,116,101,112,112,101,100,95,118,95,53,52,0);
         xvodz = icondownimgg.length <= 75;
         backiconR -= 2 << (Math.min(5, Math.abs(baiduadsC)));
         baiduadsC -= backiconR;
         icondownimgg += `${baiduadsC & backiconR}`;
         break;
      }
      while (xvodz) {
         hcopy_g7P += hcopy_g7P;
         break;
      }
      while (3 >= (5 * hcopy_g7P)) {
         xvodz = xvodz && hcopy_g7P <= 64;
         break;
      }
         hcopy_g7P /= Math.max(5, 2);
         xvodz = 21 > hcopy_g7P;
      vignetter += untickZ.size % (Math.max(1, 6));
       let screenl = String.fromCharCode(102,95,56,48,0);
       let navigation0 = String.fromCharCode(112,97,103,95,121,95,51,51,0);
       let iconnewsshareF = String.fromCharCode(110,111,110,110,117,108,108,105,110,99,111,109,105,110,103,95,49,95,50,54,0);
      if (3 <= navigation0.length) {
         screenl += `${screenl.length + navigation0.length}`;
      }
         screenl += `${screenl.length << (Math.min(Math.abs(3), 5))}`;
          let nativeexb = 3.0;
          let react9 = String.fromCharCode(111,95,53,56,95,112,114,105,111,114,105,116,105,101,115,0);
         screenl = `${1 + react9.length}`;
         nativeexb *= parseFloat(`${2 - parseInt(`${nativeexb}`)}`);
         react9 += `${parseInt(`${nativeexb}`)}`;
         iconnewsshareF += `${iconnewsshareF.length}`;
          let matchdetailbgG = true;
          let predictionbannersharedF = String.fromCharCode(108,97,98,101,108,101,100,95,107,95,50,55,0);
          let hongkong9 = 4.0;
         iconnewsshareF += `${predictionbannersharedF.length}`;
         matchdetailbgG = 83.88 <= hongkong9 && hongkong9 <= 83.88;
         predictionbannersharedF = `${1 + parseInt(`${hongkong9}`)}`;
      if (1 == navigation0.length) {
         navigation0 = `${navigation0.length}`;
      }
      let playlistn = 6589234 >= navigation0.length;
      do {
         navigation0 += "2";
         if (playlistn) {
            break;
         }
      } while ((screenl == String.fromCharCode(72,0)) && playlistn);
         screenl += `${2 >> (Math.min(4, iconnewsshareF.length))}`;
      while (3 <= navigation0.length) {
         navigation0 += "3";
         break;
      }
      resultM = `${backwardi.size / (Math.max(5, colorsK.length))}`;
   for (let z = 0; z < 1; z++) {
      volumev /= Math.max(parseFloat(`${logouserE.length / (Math.max(6, backwardi.size))}`), 4);
   }
   while ((1.38 - changeP) == 5.40 || !nalyticsW) {
      changeP += backwardi.size;
      break;
   }
   for (let l = 0; l < 2; l++) {
      renew1 = `${parseInt(`${changeP}`)}`;
   }
      username0 = `${backwardi.size}`;
   for (let q = 0; q < 2; q++) {
      downloadedE += `${downloadedE.length & 2}`;
   }
   if (3 <= (logouserE.length >> (Math.min(Math.abs(5), 1)))) {
      logouserE = `${username0.length}`;
   }
   for (let s = 0; s < 1; s++) {
      changeP *= backwardi.size;
   }
   let eyeclosee = 5017912 <= resultM.length;
   do {
       let selectR = true;
       let showlessF = 4;
       let privatechatbge = String.fromCharCode(99,111,109,112,97,114,101,114,115,95,57,95,54,51,0);
       let pathS: Map<any, any> = new Map([[String.fromCharCode(101,95,51,57,95,97,118,101,114,0),294], [String.fromCharCode(118,95,51,53,95,116,105,116,108,101,0),340]]);
         pathS.set(privatechatbge, (privatechatbge == String.fromCharCode(120,0) ? showlessF : privatechatbge.length));
      let iconrefreshm = privatechatbge.length >= 9558283;
      do {
         privatechatbge = "2";
         if (iconrefreshm) {
            break;
         }
      } while ((privatechatbge.length >= pathS.size) && iconrefreshm);
      let fastforward5 = privatechatbge == String.fromCharCode(109,103,53,115,57,104,0);
      do {
         privatechatbge = `${pathS.size}`;
         if (fastforward5) {
            break;
         }
      } while (fastforward5 && (5 >= (pathS.size % 3)));
      while (selectR) {
          let favoritee: Array<any> = [711, 515, 577];
         selectR = pathS.size < 80 && 80 < favoritee.length;
         break;
      }
      while (showlessF > pathS.size) {
         showlessF *= (String.fromCharCode(112,0) == privatechatbge ? privatechatbge.length : pathS.size);
         break;
      }
       let settingsy: Array<any> = [String.fromCharCode(105,116,101,114,97,116,101,95,56,95,52,49,0), String.fromCharCode(99,97,112,116,117,114,101,116,101,115,116,118,105,100,101,111,95,102,95,55,49,0)];
       let filla: Array<any> = [188, 615, 955];
         filla.push(showlessF);
      while (4 < pathS.size) {
         pathS = new Map([[`${pathS.size}`, pathS.size]]);
         break;
      }
      let baiduI = privatechatbge.length <= 7625411;
      do {
          let homeinactiveN = 2.0;
          let indexM = String.fromCharCode(103,95,49,54,95,110,101,97,114,0);
          let scorej = String.fromCharCode(102,95,57,56,95,105,109,112,114,105,110,116,0);
         privatechatbge = `${((selectR ? 5 : 4))}`;
         homeinactiveN -= parseFloat(`${parseInt(`${homeinactiveN}`)}`);
         indexM = `${3 << (Math.min(3, Math.abs(parseInt(`${homeinactiveN}`))))}`;
         scorej += "2";
         if (baiduI) {
            break;
         }
      } while (((privatechatbge.length / 1) >= 4 && (1 / (Math.max(3, settingsy.length))) >= 4) && baiduI);
      let skips = selectR ? !selectR : selectR;
      do {
         selectR = 93 <= filla.length && pathS.size <= 93;
         if (skips) {
            break;
         }
      } while (skips && ((filla.length + 2) > 5));
      if (5 > (showlessF & privatechatbge.length) || 3 > (privatechatbge.length & 5)) {
         privatechatbge += `${2 | privatechatbge.length}`;
      }
       let single9 = 2.0;
      resultM += `${(resultM == String.fromCharCode(80,0) ? downloadedE.length : resultM.length)}`;
      if (eyeclosee) {
         break;
      }
   } while (((1 - resultM.length) == 4 && 4 == (resultM.length - 1)) && eyeclosee);
   let megaphone3 = String.fromCharCode(101,56,106,116,0) == resultM;
   do {
      resultM += `${renew1.length / (Math.max(10, backwardi.size))}`;
      if (megaphone3) {
         break;
      }
   } while (megaphone3 && (!resultM.endsWith(`${leftP}`)));
 },
}: wawaAwayShow) {
  const { colors, textVariants, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const navigation = useNavigation();
  const [showLoading, setShowLoading] = useState(false);
  const userState = useSelector<wawaPhoneControls>('userReducer');
  const isVip = wawaLibglog.fakeIsVip(userState.user);
  const [totalPage, setTotalPage] = useState(1);
  const [results, setResults] = useState<Array<wawaClearHooks>>([]);

  
  
  //     if ((item?.vod_list?.length ?? 0) === 0) return <></>;

  
  
  
  
  
  
  
  
  
  
  
  
  
  //       >
  
  
  
  
  
  
  
  //         />
  
  
  
  
  
  
  
  
  
  //             />
  
  
  //       </View>
  
  
  
  

  
  function listItem({ item, index }) {
    if ((item?.vod_list?.length ?? 0) === 0) return <></>;

    return (
      <View
        key={`${item.type_name}-${index}`}
        style={{
          gap: spacing.m,
          paddingLeft: spacing.sideOffset,
          paddingRight: spacing.sideOffset,
          backgroundColor: "#0c0c0c",
          borderRadius: 15,
          marginBottom: 15,
          paddingTop: 4,
          paddingBottom: 4,
        }}
      >
        <ShowMoreVodButton
          text={item.type_name.trim()}
          onPress={() => {
            navigation.navigate("午夜场剧情", {
              class: item.type_name,
              vod_source_name: item.vod_source_name
            });
          }}
        />
        {
          item?.vod_list && (
            <VodListVerticalVip
              numOfRows={2}
              vods={item?.vod_list}
              minNumPerRow={2}
              heightToWidthRatio={1 / 1.814}
              playerMode="adult"
            />
          )
        }
      </View>
    );
  }

  const fetchXCategories = (page: number) =>
    wawaHiadCanvas.getXVodList(page).then((results: Array<wawaClearHooks>) => {
      setTotalPage(9);
      console.log("HEH");
      return Object.values(results);
    });

  const {
    data: xData,
    isSuccess,
    hasNextPage,
    fetchNextPage,
    isFetchingNextPage,
    isFetching,
    refetch,
  } = useInfiniteQuery(
    ['xvodPlayList'],
    ({ pageParam = 1 }) => fetchXCategories(pageParam),
    {
      getNextPageParam: (lastPage, allPages) => {
        if (lastPage === null) {
          return undefined;
        }
        const nextPage: any = allPages.length + 1;
        
        if (nextPage > totalPage && totalPage != 0) {
          return undefined;
        }
        return nextPage;
      },
      onSuccess: data => {
        if (data && data?.pages) {
          setResults([...results, ...data.pages[data.pages.length - 1].flat()]);
        }
      },
    },
  );

  useEffect(() => {
    dispatch(setManualShowPopAds(true))
    if (results == undefined) {
      setShowLoading(true);
    }
  }, []);

  useEffect(() => {
    if (results.length == 0) {
      refetch();
    }
  }, [results])

  return (
    <SafeAreaView style={{ flex: 1 }}>
      {showLoading && (
        <View
          style={{
            position: "absolute",
            
            backgroundColor: "#0c0c0c",
            zIndex: 1,
            width: "100%",
            height: "100%",
            justifyContent: "center",
            alignItems: "center",
            borderRadius: 15,

          }}
        >
          <FastImage
            source={require("@static/images/iconnewsshareDist.gif")}
            style={{ width: 100, height: 100 }}
            resizeMode="contain"
          />
        </View>
      )}

      <View style={{ paddingTop: 10 }}>
        <FlatList
          data={results}
          keyExtractor={(item: wawaRedirectNative, index: number) => index.toString() + item.type_name}
          initialNumToRender={0}
          windowSize={3}
          maxToRenderPerBatch={3}
          renderItem={listItem}
          contentContainerStyle={{ paddingBottom: 60 }}
          removeClippedSubviews={true}
          onEndReachedThreshold={0.5}
          scrollEnabled={isVip}
          onEndReached={() => {
            if (hasNextPage && !isFetchingNextPage && !isFetching) {
              fetchNextPage();
            }
          }}
          ListFooterComponent={
            <View style={{ ...styles.loading }}>
              {hasNextPage && (
                <FastImage
                  style={{
                    height: 80,
                    width: 80,

                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                  }}
                  source={require('@static/images/iconnewsshareDist.gif')}
                  resizeMode={'contain'}
                />
              )}
              {!(isFetchingNextPage || isFetching) && !hasNextPage && (
                <Text
                  style={{
                    ...textVariants.subBody,
                    color: colors.muted,
                    paddingTop: 12,
                  }}>
                  已经到底了
                </Text>
              )}
            </View>
          }
        />
      </View>
      { }
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  loading: {
    flexDirection: "row",
    justifyContent: "center",
    flex: 1,
  },
});
