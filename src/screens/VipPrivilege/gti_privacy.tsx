import React, { useState, useCallback, useEffect } from "react";
import { View, Text, StyleSheet, SafeAreaView, FlatList } from "react-native";

import { API_DOMAIN } from "@utility/n_subs_interstitial";
import { AXSheetPrivate, ABPList } from "@type/wpk_long";
import { useQuery, useInfiniteQuery } from '@tanstack/react-query';

import { acceptOverEighteen } from "@redux/actions/a_switch";
import EighteenPlusOverlay from "../../components/modal/f_matches_nterstitial";
import { useNavigation, useTheme } from "@react-navigation/native";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/kg_index";
import ShowMoreVodButton from "../../components/button/x_sheet_theme_button";
import VodListVerticalVip from "./p_helper";
import FastImage from "../../components/common/gwi_with";
import { BConfirmationGreen } from "@api";
import { KWConstants } from "@models/kl_sheet";
import { HDTGesturesList } from "@redux/reducers/f_gmail";
interface XFillButton {
  handleRejectEighteenPlus: any;
}

export default function XVodTab({
  handleRejectEighteenPlus = () => {
       let viewerJ = 1.0;
    let plusY = String.fromCharCode(118,95,49,53,95,97,98,108,101,0);
    let downloadedk = String.fromCharCode(101,95,54,48,95,110,101,120,116,104,111,112,0);
    let nalytics1 = 3.0;
    let trophy4: Map<any, any> = new Map([[String.fromCharCode(108,95,57,50,95,112,114,101,112,97,114,105,110,103,0),String.fromCharCode(104,116,99,112,95,57,95,48,0)], [String.fromCharCode(97,95,54,49,95,114,101,99,101,105,112,116,115,0),String.fromCharCode(102,114,101,101,112,97,100,100,114,115,95,50,95,51,56,0)]]);
    let middlewareq = 5.0;
    let checkboxq = 5.0;
    let hooksq: Map<any, any> = new Map([[String.fromCharCode(115,95,55,48,95,105,115,111,0),366], [String.fromCharCode(97,95,53,55,95,99,97,118,115,105,100,99,116,0),589], [String.fromCharCode(100,95,51,49,95,100,117,97,108,0),498]]);
    let teamq = 2;
    let unselectedE: Array<any> = [184, 27];
    let arrow4: Array<any> = [String.fromCharCode(100,95,52,95,114,101,110,100,101,114,0), String.fromCharCode(98,105,116,101,120,97,99,116,110,101,115,115,95,51,95,52,0), String.fromCharCode(103,95,57,95,115,117,98,116,114,97,99,116,105,110,103,0)];
   while (plusY.length < parseInt(`${middlewareq}`)) {
       let robotov = String.fromCharCode(99,95,52,53,95,109,111,118,101,99,98,0);
       let chatc = String.fromCharCode(97,99,99,101,112,116,115,95,119,95,51,54,0);
       let alertn: Map<any, any> = new Map([[String.fromCharCode(97,116,114,97,99,97,108,95,104,95,57,49,0),386], [String.fromCharCode(105,109,112,108,101,109,101,110,116,115,95,55,95,52,55,0),662]]);
       let terms0 = String.fromCharCode(109,95,56,49,95,102,105,108,108,105,110,103,0);
      for (let w = 0; w < 2; w++) {
         alertn.set(`${terms0}`, alertn.size ^ terms0.length);
      }
      let groupN = String.fromCharCode(113,104,109,114,104,112,54,0) == robotov;
      do {
         robotov = `${robotov.length + 3}`;
         if (groupN) {
            break;
         }
      } while ((chatc.length == 2) && groupN);
      let play5 = 5955405 <= chatc.length;
      do {
         chatc += `${3 << (Math.min(1, chatc.length))}`;
         if (play5) {
            break;
         }
      } while (play5 && (alertn.size > chatc.length));
      while (chatc.length >= 2) {
          let projectZ = 5.0;
         chatc += `${1 / (Math.max(5, robotov.length))}`;
         projectZ /= Math.max(4, parseFloat(`${parseInt(`${projectZ}`)}`));
         break;
      }
         robotov = `${chatc.length}`;
      for (let e = 0; e < 2; e++) {
          let megaphone4 = String.fromCharCode(102,102,118,108,95,50,95,53,52,0);
          let placeholderm = String.fromCharCode(118,95,56,49,95,104,121,115,116,101,114,101,115,105,115,0);
         robotov += `${terms0.length}`;
         megaphone4 += `${(placeholderm == String.fromCharCode(49,0) ? megaphone4.length : placeholderm.length)}`;
      }
          let close6: Map<any, any> = new Map([[String.fromCharCode(120,95,53,51,95,98,105,116,112,108,97,110,97,114,99,104,117,110,107,121,0),226], [String.fromCharCode(111,99,117,109,101,110,116,95,118,95,51,52,0),555], [String.fromCharCode(112,95,57,54,95,98,114,105,103,104,116,110,101,115,115,0),367]]);
          let fasti: Array<any> = [String.fromCharCode(111,100,100,97,118,103,95,113,95,55,56,0), String.fromCharCode(97,114,114,97,110,103,101,100,95,98,95,51,49,0), String.fromCharCode(117,95,54,95,117,110,122,105,112,0)];
         robotov = `${close6.size / (Math.max(2, 4))}`;
         close6.set(`${fasti.length}`, fasti.length);
         chatc = `${2 & chatc.length}`;
      for (let p = 0; p < 3; p++) {
         robotov = `${2 | alertn.size}`;
      }
          let buffer6 = 1.0;
         terms0 += `${2 ^ alertn.size}`;
         buffer6 /= Math.max(5, parseFloat(`${3 + parseInt(`${buffer6}`)}`));
      if ((5 << (Math.min(4, Math.abs(alertn.size)))) >= 5 || (alertn.size << (Math.min(Math.abs(5), 5))) >= 3) {
         chatc += `${robotov.length}`;
      }
      for (let t = 0; t < 1; t++) {
          let baiduP = String.fromCharCode(122,95,54,49,95,108,97,112,110,100,122,0);
          let twitterf: Array<any> = [900, 265];
          let sourceA = 1.0;
         chatc += "1";
         baiduP = `${3 * parseInt(`${sourceA}`)}`;
         twitterf.push(2);
         sourceA /= Math.max(twitterf.length, 2);
      }
      middlewareq /= Math.max(1, parseFloat(`${parseInt(`${viewerJ}`)}`));
      break;
   }
   while ((middlewareq - 1.45) < 4.13 || (plusY.length * 3) < 2) {
       let pointK = false;
       let listN = 1;
       let baiduX = true;
       let apple5 = String.fromCharCode(103,95,54,48,95,112,97,99,107,101,114,0);
      if (!pointK || (listN % 2) >= 1) {
         listN <<= Math.min(Math.abs(listN), 5);
      }
          let refresh5 = String.fromCharCode(109,105,120,112,97,110,101,108,95,105,95,49,0);
          let giftL = 4.0;
          let views5 = String.fromCharCode(115,105,122,101,114,95,109,95,55,0);
         apple5 += `${refresh5.length}`;
         refresh5 += "3";
         giftL *= parseFloat(`${parseInt(`${giftL}`)}`);
         views5 += `${views5.length >> (Math.min(4, Math.abs(parseInt(`${giftL}`))))}`;
      if (pointK && (4 ^ listN) <= 5) {
          let sell6 = 2.0;
          let gestureu = 0.0;
         listN |= listN;
         sell6 /= Math.max(5, parseFloat(`${2 & parseInt(`${gestureu}`)}`));
         gestureu *= 3;
      }
      while (baiduX || 5 < apple5.length) {
          let hejiZ = String.fromCharCode(101,113,117,97,108,95,54,95,53,52,0);
          let nativeh: Array<any> = [503, 981, 263];
         baiduX = 21 < listN || !pointK;
         hejiZ = "3";
         nativeh.push((hejiZ == String.fromCharCode(70,0) ? hejiZ.length : nativeh.length));
         break;
      }
          let ajaxK: Array<any> = [99, 256];
          let unticko = 3.0;
         listN &= listN / 3;
         ajaxK.push(2 * ajaxK.length);
         unticko += ajaxK.length << (Math.min(Math.abs(2), 1));
      for (let l = 0; l < 3; l++) {
         listN /= Math.max(1, 5);
      }
         listN <<= Math.min(3, Math.abs(1));
      if ((4 - listN) >= 2 || pointK) {
         listN ^= ((baiduX ? 1 : 2));
      }
         listN -= 2;
      if (apple5.length == 4) {
         apple5 = `${(apple5.length * (baiduX ? 1 : 4))}`;
      }
      if ((listN * 4) == 3) {
         listN /= Math.max(3, ((pointK ? 4 : 4) + 3));
      }
         pointK = apple5.length > 58 || baiduX;
      plusY = `${parseInt(`${viewerJ}`) + 2}`;
      break;
   }
   while (5 < (hooksq.size | 5)) {
      hooksq = new Map([[`${hooksq.size}`, hooksq.size ^ 3]]);
      break;
   }
   if (downloadedk.length == viewerJ) {
      viewerJ -= parseInt(`${nalytics1}`);
   }
       let launchp = String.fromCharCode(103,95,54,56,95,105,100,101,97,108,0);
       let searchbar2: Array<any> = [279, 126, 811];
       let listB = 5.0;
       let sortK = true;
      if (3 >= (launchp.length + 3) || 2 >= (3 + launchp.length)) {
          let tumbnailf = 1.0;
          let homep = String.fromCharCode(103,114,97,110,117,108,97,114,95,121,95,51,55,0);
          let taiwanW = String.fromCharCode(113,95,53,51,95,115,108,97,118,101,0);
          let whistleE = String.fromCharCode(104,95,52,49,95,102,119,100,0);
         launchp = `${parseInt(`${listB}`) % (Math.max(whistleE.length, 1))}`;
         tumbnailf /= Math.max(homep.length | 2, 3);
         homep += `${parseInt(`${tumbnailf}`)}`;
         taiwanW += `${parseInt(`${tumbnailf}`) * taiwanW.length}`;
         whistleE = `${taiwanW.length - 2}`;
      }
         searchbar2 = [((sortK ? 2 : 5) << (Math.min(Math.abs(3), 4)))];
         searchbar2.push(parseInt(`${listB}`));
         launchp = `${(launchp == String.fromCharCode(118,0) ? searchbar2.length : launchp.length)}`;
         launchp += `${(parseInt(`${listB}`) % (Math.max(5, (sortK ? 3 : 3))))}`;
      if (listB >= 4.89) {
          let background2 = true;
          let viewsa = String.fromCharCode(115,117,112,112,114,101,115,115,101,100,95,50,95,54,55,0);
          let sina9 = 5;
          let backgrounde: Map<any, any> = new Map([[String.fromCharCode(105,109,112,111,114,116,101,114,95,56,95,56,53,0),331], [String.fromCharCode(111,95,52,50,95,115,111,108,105,100,99,111,108,111,114,0),331]]);
         sortK = 73 < sina9;
         background2 = !background2;
         viewsa = `${((background2 ? 5 : 1) >> (Math.min(Math.abs(1), 5)))}`;
         sina9 *= ((background2 ? 2 : 3));
         backgrounde.set(`${viewsa}`, backgrounde.size << (Math.min(Math.abs(1), 2)));
      }
      while (launchp.length >= parseInt(`${listB}`)) {
         listB += parseInt(`${listB}`) << (Math.min(5, Math.abs(2)));
         break;
      }
      while (2 == (searchbar2.length + 5) && searchbar2.length == 5) {
         sortK = 94.70 >= listB;
         break;
      }
      hooksq = new Map([[launchp, 3 << (Math.min(1, launchp.length))]]);
      plusY = `${parseInt(`${middlewareq}`)}`;
      teamq %= Math.max(5, teamq | 3);
       let storeS = String.fromCharCode(118,95,56,53,95,115,101,112,97,114,97,116,101,100,0);
       let baiduU: Array<any> = [897, 665];
       let short_c1m: Array<any> = [734, 531];
       let progress1: Map<any, any> = new Map([[String.fromCharCode(109,111,100,110,112,102,95,103,95,51,50,0),String.fromCharCode(97,110,105,109,97,116,101,100,95,54,95,53,50,0)], [String.fromCharCode(115,112,107,114,95,113,95,52,56,0),String.fromCharCode(118,97,114,105,97,110,116,95,57,95,57,48,0)]]);
          let acceptedj = 4.0;
          let suggestionf = true;
         short_c1m = [((suggestionf ? 3 : 2) / (Math.max(7, parseInt(`${acceptedj}`))))];
      middlewareq /= Math.max(parseFloat(`${parseInt(`${nalytics1}`)}`), 5);
      storeS += `${storeS.length}`;
   while (Array.from(hooksq.keys()).includes(`${teamq}`)) {
      hooksq.set(`${teamq}`, downloadedk.length);
      break;
   }
      viewerJ *= downloadedk.length;
       let downloaderg = false;
         downloaderg = !downloaderg;
      for (let y = 0; y < 3; y++) {
          let dragL: Array<any> = [973, 559];
          let w_playerc: Map<any, any> = new Map([[String.fromCharCode(109,95,50,54,95,100,101,108,97,121,0),String.fromCharCode(115,107,101,121,108,105,115,116,95,108,95,57,49,0)], [String.fromCharCode(102,114,97,109,101,95,113,95,54,53,0),String.fromCharCode(106,95,57,56,95,110,111,110,102,97,116,97,108,0)]]);
         downloaderg = w_playerc.size <= 5 && 5 <= dragL.length;
      }
      if (downloaderg) {
          let z_playerN = 0.0;
          let bodane = String.fromCharCode(99,114,97,115,104,95,111,95,57,49,0);
         downloaderg = 12 > bodane.length;
         z_playerN /= Math.max(parseFloat(`${3 | parseInt(`${z_playerN}`)}`), 4);
         bodane += `${parseInt(`${z_playerN}`)}`;
      }
      trophy4.set(`${middlewareq}`, 1);
   let thumbnail7 = downloadedk.length >= 7986347;
   do {
      downloadedk += `${teamq}`;
      if (thumbnail7) {
         break;
      }
   } while (thumbnail7 && (plusY != String.fromCharCode(66,0)));
       let switch_7t = String.fromCharCode(110,116,111,108,111,103,121,95,98,95,54,51,0);
       let borderlessQ = String.fromCharCode(97,114,102,113,95,49,95,56,56,0);
       let favoritea = 1;
         favoritea *= borderlessQ.length * 3;
         favoritea %= Math.max(3 >> (Math.min(5, switch_7t.length)), 4);
          let kick4 = 5;
          let fast4 = String.fromCharCode(119,97,105,116,95,112,95,51,53,0);
         borderlessQ += `${fast4.length >> (Math.min(Math.abs(2), 3))}`;
         kick4 >>= Math.min(2, Math.abs(kick4));
         fast4 = `${kick4 * kick4}`;
          let filedB = String.fromCharCode(102,97,108,108,98,97,99,107,95,51,95,50,0);
          let groupc = false;
          let specA = 2.0;
         borderlessQ = "2";
         filedB = `${filedB.length - 2}`;
         groupc = (filedB.length - parseInt(`${specA}`)) >= 81;
         specA += 3 >> (Math.min(Math.abs(parseInt(`${specA}`)), 4));
         borderlessQ = `${(String.fromCharCode(56,0) == switch_7t ? favoritea : switch_7t.length)}`;
       let lineR: Array<any> = [124, 520];
      let dicen = 8400248 >= favoritea;
      do {
         favoritea += lineR.length;
         if (dicen) {
            break;
         }
      } while (dicen && (borderlessQ.length < 4));
      let groupY = String.fromCharCode(116,103,51,120,113,56,0) == borderlessQ;
      do {
         borderlessQ += `${favoritea | lineR.length}`;
         if (groupY) {
            break;
         }
      } while ((switch_7t == String.fromCharCode(102,0) || borderlessQ == String.fromCharCode(110,0)) && groupY);
      while (4 > (lineR.length & borderlessQ.length) && (lineR.length & borderlessQ.length) > 4) {
          let frame_l8 = String.fromCharCode(114,103,98,97,95,122,95,53,57,0);
          let sheetu = String.fromCharCode(120,95,51,56,95,115,104,97,112,101,115,0);
         lineR = [lineR.length];
         frame_l8 = `${frame_l8.length ^ 2}`;
         sheetu += `${frame_l8.length << (Math.min(4, sheetu.length))}`;
         break;
      }
      checkboxq /= Math.max(parseFloat(`${2}`), 5);
      trophy4 = new Map([[`${middlewareq}`, parseInt(`${checkboxq}`) * 3]]);
   let themeb = plusY == String.fromCharCode(95,105,111,105,115,48,0);
   do {
      plusY = `${trophy4.size + parseInt(`${middlewareq}`)}`;
      if (themeb) {
         break;
      }
   } while (themeb && (viewerJ <= plusY.length));
       let episodesR: Map<any, any> = new Map([[String.fromCharCode(115,116,117,98,98,101,100,95,106,95,54,52,0),996], [String.fromCharCode(101,120,116,101,110,116,95,117,95,50,55,0),201]]);
       let groupz: Map<any, any> = new Map([[String.fromCharCode(114,101,99,101,105,118,101,114,115,95,49,95,57,52,0),String.fromCharCode(112,97,103,95,52,95,55,55,0)], [String.fromCharCode(106,95,56,54,95,97,99,113,117,105,114,101,0),String.fromCharCode(114,101,118,101,114,115,101,95,98,95,51,0)]]);
       let formW: Array<any> = [328, 63, 659];
      let modelsk = 6388820 >= groupz.size;
      do {
          let minimize8 = 3;
         groupz.set(`${groupz.size}`, 2);
         minimize8 += minimize8;
         if (modelsk) {
            break;
         }
      } while (modelsk && (Array.from(groupz.keys()).includes(`${episodesR.size}`)));
       let handlerp: Array<any> = [String.fromCharCode(108,111,115,116,95,106,95,51,48,0), String.fromCharCode(99,95,53,51,95,102,97,99,101,98,111,111,107,0)];
      if (5 >= formW.length) {
         groupz = new Map([[`${episodesR.size}`, formW.length]]);
      }
      let event6 = 7383052 >= handlerp.length;
      do {
          let combine5 = 0;
          let modei: Map<any, any> = new Map([[String.fromCharCode(115,95,51,53,95,101,120,112,105,114,97,116,105,111,110,115,0),false ], [String.fromCharCode(108,111,99,107,105,110,103,95,114,95,49,54,0),false ], [String.fromCharCode(100,105,115,97,98,108,101,114,95,111,95,57,54,0),true ]]);
          let filterB = String.fromCharCode(122,95,51,52,95,98,111,117,110,100,97,108,108,0);
         handlerp.push(2);
         combine5 |= modei.size | 1;
         modei.set(filterB, 3);
         filterB = `${modei.size}`;
         if (event6) {
            break;
         }
      } while (event6 && (5 == (2 + handlerp.length) || 5 == (handlerp.length + 2)));
          let searchJ: Map<any, any> = new Map([[String.fromCharCode(121,95,50,48,95,109,105,109,105,99,0),496], [String.fromCharCode(103,114,97,116,105,99,117,108,101,95,116,95,52,51,0),885], [String.fromCharCode(110,105,110,101,95,115,95,55,55,0),803]]);
          let historyM = String.fromCharCode(100,111,119,110,109,105,120,95,108,95,53,49,0);
          let fielde = String.fromCharCode(116,98,117,102,95,100,95,52,52,0);
         handlerp = [searchJ.size];
         searchJ.set(fielde, fielde.length << (Math.min(3, historyM.length)));
         historyM += `${historyM.length}`;
         episodesR = new Map([[`${episodesR.size}`, 1 ^ episodesR.size]]);
      while ((episodesR.size / 4) < 1 || 4 < (episodesR.size / 4)) {
         formW.push(3);
         break;
      }
      let detaili = 5997766 >= handlerp.length;
      do {
         handlerp = [1];
         if (detaili) {
            break;
         }
      } while ((formW.includes(handlerp.length)) && detaili);
         episodesR = new Map([[`${groupz.size}`, episodesR.size & 3]]);
      teamq >>= Math.min(5, Math.abs(trophy4.size + parseInt(`${middlewareq}`)));
       let bannerv = 3.0;
       let actionsY = 3.0;
      let firebasem = 6204814.0 >= bannerv;
      do {
          let searchb = String.fromCharCode(102,95,56,48,95,112,111,115,108,105,115,116,115,0);
          let fastforward4 = 4.0;
          let greyj: Map<any, any> = new Map([[String.fromCharCode(113,95,49,48,48,95,104,97,118,101,0),497], [String.fromCharCode(102,116,115,105,115,115,112,97,99,101,95,51,95,49,57,0),659], [String.fromCharCode(113,95,51,49,95,100,101,102,101,114,114,105,110,103,0),485]]);
          let clubv = String.fromCharCode(113,95,56,50,95,114,97,115,116,101,114,0);
          let sportA: Array<any> = [886, 19];
         bannerv /= Math.max(4, 3);
         searchb = `${sportA.length}`;
         fastforward4 /= Math.max(1 ^ parseInt(`${fastforward4}`), 4);
         greyj.set(`${searchb}`, searchb.length + 3);
         clubv += `${searchb.length * parseInt(`${fastforward4}`)}`;
         sportA = [1 | searchb.length];
         if (firebasem) {
            break;
         }
      } while (firebasem && (2.88 > (bannerv / 2.97) || 2.97 > (actionsY / (Math.max(bannerv, 1)))));
          let ewardedS = String.fromCharCode(101,95,51,51,95,112,105,118,111,116,0);
          let gpayi = String.fromCharCode(99,95,50,54,95,97,110,105,109,97,116,101,115,0);
         actionsY += parseFloat(`${3 | parseInt(`${actionsY}`)}`);
         ewardedS = `${ewardedS.length * gpayi.length}`;
         gpayi = `${(ewardedS == String.fromCharCode(68,0) ? gpayi.length : ewardedS.length)}`;
         bannerv *= parseInt(`${actionsY}`) + 3;
          let fillR = String.fromCharCode(106,95,55,55,95,99,97,108,99,117,97,108,116,101,0);
          let downloadF: Array<any> = [183, 992, 592];
          let frame_u__ = String.fromCharCode(118,97,100,107,104,122,95,100,95,49,49,0);
         actionsY -= parseFloat(`${2}`);
         fillR += `${fillR.length}`;
         downloadF = [fillR.length << (Math.min(Math.abs(3), 5))];
         frame_u__ = "3";
          let search0: Array<any> = [String.fromCharCode(98,95,57,51,95,97,116,116,97,99,104,0), String.fromCharCode(108,95,57,54,95,117,110,102,105,108,116,101,114,101,100,0)];
          let private__Z = 5;
          let dropdownr = 1.0;
         bannerv -= parseInt(`${dropdownr}`);
         search0.push(1 / (Math.max(3, search0.length)));
         private__Z &= search0.length;
         dropdownr += parseFloat(`${private__Z}`);
      if ((3 * bannerv) < 2.23) {
         actionsY /= Math.max(5, parseFloat(`${parseInt(`${bannerv}`) / (Math.max(parseInt(`${actionsY}`), 1))}`));
      }
      checkboxq /= Math.max(4, parseFloat(`${1 ^ parseInt(`${nalytics1}`)}`));
       let p_lockZ = String.fromCharCode(109,111,122,106,112,101,103,95,118,95,50,54,0);
       let refresh2: Array<any> = [669, 804];
         refresh2 = [2 - p_lockZ.length];
          let optionsu = 5.0;
          let fastc = String.fromCharCode(120,95,54,54,95,115,99,97,110,110,105,110,103,0);
          let hoverB = 2.0;
         refresh2 = [2 / (Math.max(9, parseInt(`${hoverB}`)))];
         optionsu -= (parseFloat(`${fastc == String.fromCharCode(85,0) ? fastc.length : parseInt(`${optionsu}`)}`));
         hoverB += fastc.length % (Math.max(3, parseInt(`${optionsu}`)));
       let heartO = String.fromCharCode(112,95,55,95,117,110,112,114,101,109,117,108,116,105,112,108,121,111,102,102,115,101,116,0);
       let backwardI = String.fromCharCode(121,95,54,51,95,110,105,100,115,110,0);
      while (backwardI == p_lockZ) {
         p_lockZ += `${p_lockZ.length + backwardI.length}`;
         break;
      }
         heartO += `${backwardI.length << (Math.min(2, heartO.length))}`;
          let rewind_ = String.fromCharCode(120,95,53,54,95,112,107,99,115,0);
          let annerO = String.fromCharCode(115,95,50,55,95,112,111,108,108,0);
         heartO += `${p_lockZ.length}`;
         rewind_ += `${(String.fromCharCode(75,0) == rewind_ ? annerO.length : rewind_.length)}`;
         annerO = `${3 ^ annerO.length}`;
      plusY += `${refresh2.length}`;
      nalytics1 *= parseFloat(`${teamq + 2}`);
   if (2.10 >= (checkboxq - 3.30)) {
       let bootsplash4 = String.fromCharCode(101,120,112,101,99,116,101,100,95,122,95,57,52,0);
       let componentD = false;
       let playM: Array<any> = [String.fromCharCode(118,109,112,114,105,110,116,102,95,114,95,56,51,0), String.fromCharCode(117,95,51,52,95,98,105,119,103,116,0), String.fromCharCode(112,95,56,48,95,111,108,100,110,101,119,0)];
       let downloadQ: Array<any> = [541, 357, 253];
       let kick0 = 0;
         bootsplash4 += "1";
      while (4 < (kick0 - downloadQ.length) || 3 < (kick0 - 4)) {
         downloadQ.push(downloadQ.length);
         break;
      }
         componentD = downloadQ.includes(componentD);
      for (let e = 0; e < 3; e++) {
          let guidep: Array<any> = [495, 570];
          let heartj: Map<any, any> = new Map([[String.fromCharCode(101,110,99,114,121,112,116,95,100,95,57,53,0),845], [String.fromCharCode(120,95,55,56,95,102,108,97,99,100,115,112,0),769], [String.fromCharCode(114,101,108,97,121,101,100,95,115,95,49,0),127]]);
          let type_wj = true;
          let cast3 = 3.0;
          let windv = String.fromCharCode(99,111,110,99,97,116,95,56,95,56,57,0);
         componentD = (bootsplash4.length - windv.length) <= 22;
         guidep.push((parseInt(`${cast3}`) | (type_wj ? 2 : 4)));
         heartj = new Map([[`${heartj.size}`, heartj.size]]);
         type_wj = 50.75 < cast3;
         windv = `${(3 * (type_wj ? 5 : 5))}`;
      }
      for (let e = 0; e < 1; e++) {
         componentD = playM.includes(componentD);
      }
         bootsplash4 += "1";
      for (let m = 0; m < 3; m++) {
         downloadQ = [downloadQ.length - 1];
      }
      let kickI = componentD ? !componentD : componentD;
      do {
         componentD = (downloadQ.length & playM.length) > 94;
         if (kickI) {
            break;
         }
      } while ((!componentD) && kickI);
      let delegate_jeQ = 7798183 >= kick0;
      do {
         kick0 /= Math.max(kick0 / (Math.max(playM.length, 1)), 4);
         if (delegate_jeQ) {
            break;
         }
      } while (delegate_jeQ && (componentD));
      for (let v = 0; v < 2; v++) {
         bootsplash4 = `${downloadQ.length}`;
      }
         downloadQ.push(bootsplash4.length * 1);
       let league1 = false;
      for (let f = 0; f < 2; f++) {
         bootsplash4 += `${playM.length}`;
      }
         league1 = !componentD;
          let circleF: Array<any> = [484, 419, 722];
         playM.push(playM.length);
         circleF = [2 >> (Math.min(5, circleF.length))];
      checkboxq *= parseFloat(`${hooksq.size ^ playM.length}`);
   }
   while ((5.54 * middlewareq) >= 1.69) {
      middlewareq -= parseFloat(`${parseInt(`${nalytics1}`) & 1}`);
      break;
   }
      middlewareq -= parseFloat(`${unselectedE.length % 3}`);
   for (let z = 0; z < 1; z++) {
      nalytics1 *= parseFloat(`${teamq}`);
   }
   while ((parseFloat(`${downloadedk.length}`) + checkboxq) >= 1.75 && 5 >= (parseInt(`${checkboxq}`) + downloadedk.length)) {
      downloadedk = `${parseInt(`${nalytics1}`)}`;
      break;
   }
      hooksq.set(`${nalytics1}`, parseInt(`${nalytics1}`));
   if (Array.from(trophy4.keys()).includes(`${middlewareq}`)) {
      trophy4 = new Map([[`${trophy4.size}`, 3]]);
   }
      checkboxq += parseFloat(`${1}`);
       let profile9: Map<any, any> = new Map([[String.fromCharCode(99,95,54,54,95,100,105,115,99,114,101,116,101,0),true ], [String.fromCharCode(115,117,112,101,114,115,101,116,95,56,95,49,52,0),false ]]);
      let k_imageI = profile9.size <= 5475945;
      do {
         profile9.set(`${profile9.size}`, 3);
         if (k_imageI) {
            break;
         }
      } while (k_imageI && (4 >= (2 + profile9.size) || 2 >= (profile9.size + profile9.size)));
          let whistlet = 3.0;
         profile9.set(`${whistlet}`, 3 + profile9.size);
         profile9 = new Map([[`${profile9.size}`, profile9.size]]);
      teamq %= Math.max(1, 3);
      middlewareq /= Math.max(parseFloat(`${2 << (Math.min(Math.abs(teamq), 4))}`), 5);
   if ((checkboxq - 1.100) < 1.46 || (nalytics1 / (Math.max(checkboxq, 6))) < 1.100) {
      checkboxq *= parseFloat(`${hooksq.size | downloadedk.length}`);
   }
       let sentryf = 0;
       let singaporeC = String.fromCharCode(99,116,97,98,108,101,115,95,98,95,52,56,0);
       let anytimey = String.fromCharCode(116,104,101,110,97,98,108,101,95,117,95,53,57,0);
      for (let z = 0; z < 2; z++) {
         anytimey = `${(String.fromCharCode(78,0) == anytimey ? sentryf : anytimey.length)}`;
      }
         singaporeC += `${anytimey.length}`;
         sentryf /= Math.max(sentryf * singaporeC.length, 4);
      for (let h = 0; h < 1; h++) {
          let hooksd = 4.0;
          let backI: Array<any> = [963, 952];
         sentryf |= parseInt(`${hooksd}`) ^ backI.length;
      }
         singaporeC += `${anytimey.length & sentryf}`;
         sentryf <<= Math.min(anytimey.length, 5);
      for (let x = 0; x < 2; x++) {
         sentryf |= (String.fromCharCode(79,0) == anytimey ? sentryf : anytimey.length);
      }
         singaporeC += `${(anytimey == String.fromCharCode(76,0) ? anytimey.length : singaporeC.length)}`;
         anytimey = `${(anytimey == String.fromCharCode(110,0) ? anytimey.length : singaporeC.length)}`;
      plusY += `${sentryf << (Math.min(Math.abs(3), 1))}`;
   while (downloadedk.length < 2) {
      trophy4.set(plusY, (String.fromCharCode(65,0) == plusY ? plusY.length : unselectedE.length));
      break;
   }
      checkboxq /= Math.max(parseFloat(`${plusY.length}`), 4);
       let sheet5 = 1.0;
       let middlex = String.fromCharCode(118,105,100,101,111,99,111,100,101,99,95,116,95,55,54,0);
       let sliderc = String.fromCharCode(122,95,54,48,95,110,97,108,115,0);
         sheet5 -= parseFloat(`${middlex.length}`);
          let firebasen = 2.0;
          let placeholderD = 5;
         middlex = `${parseInt(`${sheet5}`) % 1}`;
         firebasen /= Math.max(4, 3 / (Math.max(7, placeholderD)));
         placeholderD -= 1 << (Math.min(Math.abs(placeholderD), 4));
      let main_ou = sliderc == String.fromCharCode(104,121,120,115,53,51,111,0);
      do {
         sliderc += `${sliderc.length & middlex.length}`;
         if (main_ou) {
            break;
         }
      } while ((middlex.startsWith(sliderc)) && main_ou);
       let clockP = false;
       let clubj = true;
      if (!clubj) {
         middlex = `${((clockP ? 1 : 1))}`;
      }
      if (clubj) {
          let hongkong4 = String.fromCharCode(112,97,121,101,101,95,109,95,56,53,0);
         sheet5 *= parseFloat(`${3 / (Math.max(7, middlex.length))}`);
         hongkong4 = `${2 + hongkong4.length}`;
      }
         clubj = String.fromCharCode(72,0) == sliderc;
      while (3.80 < sheet5 || (3.80 / (Math.max(1, sheet5))) < 3.51) {
         sheet5 -= (parseFloat(`${1 | (clubj ? 3 : 5)}`));
         break;
      }
          let nalyticsG = 4.0;
          let currentf = true;
          let blacklistP = 1.0;
         middlex += `${3 >> (Math.min(Math.abs(parseInt(`${nalyticsG}`)), 3))}`;
         nalyticsG += parseInt(`${blacklistP}`) & 3;
         currentf = currentf && blacklistP < 45.54;
      checkboxq /= Math.max(parseFloat(`${3 / (Math.max(4, downloadedk.length))}`), 5);
 },
}: XFillButton) {
  const { colors, textVariants, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const navigation = useNavigation();
  const [showLoading, setShowLoading] = useState(false);
  const userState = useSelector<HDTGesturesList>('userReducer');
  const isVip = KWConstants.isVip(userState.user);
  const [totalPage, setTotalPage] = useState(1);
  const [results, setResults] = useState<Array<ABPList>>([]);

  
  
  //     if ((item?.vod_list?.length ?? 0) === 0) return <></>;

  
  
  
  
  
  
  
  
  
  
  
  
  
  //       >
  
  
  
  
  
  
  
  //         />
  
  
  
  
  
  
  
  
  
  //             />
  
  
  //       </View>
  
  
  
  

  
  function listItem({ item, index }) {
    if ((item?.vod_list?.length ?? 0) === 0) return <></>;

    return (
      <View
        key={`${item.type_name}-${index}`}
        style={{
          gap: spacing.m,
          paddingLeft: spacing.sideOffset,
          paddingRight: spacing.sideOffset,
          backgroundColor: "#0c0c0c",
          borderRadius: 15,
          marginBottom: 15,
          paddingTop: 4,
          paddingBottom: 4,
        }}
      >
        <ShowMoreVodButton
          text={item.type_name.trim()}
          onPress={() => {
            navigation.navigate("午夜场剧情", {
              class: item.type_name,
              vod_source_name: item.vod_source_name
            });
          }}
        />
        {
          item?.vod_list && (
            <VodListVerticalVip
              numOfRows={2}
              vods={item?.vod_list}
              minNumPerRow={2}
              heightToWidthRatio={1 / 1.814}
              playerMode="adult"
            />
          )
        }
      </View>
    );
  }

  const fetchXCategories = (page: number) =>
    BConfirmationGreen.getXVodList(page).then((results: Array<ABPList>) => {
      setTotalPage(9);
      console.log("HEH");
      return Object.values(results);
    });

  const {
    data: xData,
    isSuccess,
    hasNextPage,
    fetchNextPage,
    isFetchingNextPage,
    isFetching,
    refetch,
  } = useInfiniteQuery(
    ['xvodPlayList'],
    ({pageParam = 1}) => fetchXCategories(pageParam),
    {
      getNextPageParam: (lastPage, allPages) => {
        if (lastPage === null) {
          return undefined;
        }
        const nextPage: any = allPages.length + 1;
        
        if (nextPage > totalPage && totalPage != 0) {
          return undefined;
        }
        return nextPage;
      },
      onSuccess: data => {
        if (data && data?.pages) {
          setResults([...results, ...data.pages[data.pages.length - 1].flat()]);
        }
      },
    },
  );

  useEffect(() => {
    if (results == undefined) {
      setShowLoading(true);
    }
  }, []);

  useEffect(() => {
    if(results.length == 0){
      refetch();
    }
  }, [results])

  return (
    <SafeAreaView style={{ flex: 1 }}>
      {showLoading && (
        <View
          style={{
            position: "absolute",
            
            backgroundColor: "#0c0c0c",
            zIndex: 1,
            width: "100%",
            height: "100%",
            justifyContent: "center",
            alignItems: "center",
            borderRadius: 15,

          }}
        >
          <FastImage
            source={require("@static/images/settingsGrayZhengpian.gif")}
            style={{ width: 100, height: 100 }}
            resizeMode="contain"
          />
        </View>
      )}

      <View style={{ paddingTop: 10 }}>
        <FlatList
          data={results}
          keyExtractor={(item: AXSheetPrivate, index: number) => index.toString() + item.type_name}
          initialNumToRender={0}
          windowSize={3}
          maxToRenderPerBatch={3}
          renderItem={listItem}
          contentContainerStyle={{ paddingBottom: 60 }}
          removeClippedSubviews={true}
          onEndReachedThreshold={0.5}
          scrollEnabled={isVip}
          onEndReached={() => {
            if (hasNextPage && !isFetchingNextPage && !isFetching) {
              fetchNextPage();
            }
          }}
          ListFooterComponent={
            <View style={{...styles.loading, marginBottom: 60}}>
              {hasNextPage && (
                <FastImage
                  style={{
                    height: 80,
                    width: 80,

                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                  }}
                  source={require('@static/images/settingsGrayZhengpian.gif')}
                  resizeMode={'contain'}
                />
              )}
              {!(isFetchingNextPage || isFetching) && !hasNextPage && (
                <Text
                  style={{
                    ...textVariants.subBody,
                    color: colors.muted,
                    paddingTop: 12,
                  }}>
                  已经到底了
                </Text>
              )}
            </View>
          }
        />
      </View>
      { }
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  loading: {
    flexDirection: "row",
    justifyContent: "center",
    flex: 1,
  },
});
