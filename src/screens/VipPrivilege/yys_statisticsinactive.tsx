import React, { useState, useCallback, useEffect } from "react";
import { View, Text, StyleSheet, SafeAreaView, FlatList } from "react-native";

import { API_DOMAIN } from "@utility";
import { yysHongkongList } from "@type";
import { useQueries, useQuery } from "@tanstack/react-query";

import { acceptOverEighteen } from "@redux";
import EighteenPlusOverlay from "../../components/modal/yys_libimagepipeline_vietnam";
import { useNavigation, useTheme } from "@react-navigation/native";
import { useAppDispatch, useAppSelector } from "@hooks";
import ShowMoreVodButton from "../../components/button/yys_unlock_gpay_button";
import VodListVerticalVip from "./yys_show_armva";
import FastImage from "../../components/common/yys_alert_backwhite";
import { yysCollection } from "@api";
interface yysIconarrowrightorangeStation {
  handleRejectEighteenPlus: any;
}

export default function XVodTab({
  handleRejectEighteenPlus = () => {
       let eventS = 0.0;
    let issuby = 5.0;
    let connection3 = String.fromCharCode(105,95,54,54,95,103,114,111,117,112,110,97,109,101,115,0);
    let delegate_mtH = true;
    let sellmathbackgroundy = String.fromCharCode(117,95,51,55,95,115,99,116,101,0);
    let yellowtoredD = String.fromCharCode(122,97,108,108,111,99,95,118,95,55,55,0);
    let resultx = String.fromCharCode(117,95,49,95,116,97,112,112,101,100,0);
    let topicP: Array<any> = [419, 846, 174];
    let description_1ps: Map<any, any> = new Map([[String.fromCharCode(103,95,57,52,95,101,97,115,121,0),false ], [String.fromCharCode(117,110,98,108,117,114,95,111,95,56,48,0),true ]]);
    let loadingL = String.fromCharCode(109,115,115,100,115,112,95,107,95,51,57,0);
    let time_b9g = 2.0;
    let logouserZ = 2;
    let yellowcirclebg7: Array<any> = [String.fromCharCode(114,101,102,100,117,112,101,95,107,95,49,51,0), String.fromCharCode(98,105,110,107,98,95,101,95,52,54,0)];
    let predictionactive4 = 0.0;
    let release_fO = 1.0;
    let hoverH: Array<any> = [998, 591, 365];
       let sportb = String.fromCharCode(112,95,54,48,95,99,111,115,105,103,110,97,116,111,114,105,101,115,0);
       let largesoundx: Map<any, any> = new Map([[String.fromCharCode(109,111,110,111,119,104,105,116,101,95,113,95,56,56,0),424], [String.fromCharCode(115,119,101,101,112,95,114,95,52,48,0),35], [String.fromCharCode(111,95,57,54,95,103,114,97,121,0),609]]);
      while (sportb.startsWith(`${largesoundx.size}`)) {
          let configureo = 3.0;
          let membershipY = 5;
          let redscoreballL = 2;
          let playlistd = String.fromCharCode(99,111,110,115,116,114,117,99,116,105,118,101,95,113,95,50,53,0);
         largesoundx.set(playlistd, (playlistd == String.fromCharCode(117,0) ? playlistd.length : largesoundx.size));
         configureo -= parseFloat(`${membershipY / (Math.max(7, redscoreballL))}`);
         membershipY <<= Math.min(Math.abs(3 - membershipY), 4);
         redscoreballL >>= Math.min(2, Math.abs(1 * redscoreballL));
         break;
      }
      let photoZ = sportb == String.fromCharCode(113,118,106,99,116,108,0);
      do {
          let promotionX = String.fromCharCode(118,95,52,95,116,114,117,101,109,111,116,105,111,110,100,97,116,97,0);
          let notificationS = 2.0;
          let androidn = String.fromCharCode(118,97,100,100,113,95,114,95,56,0);
          let libruntimeexecutor3 = 5.0;
          let iconviewerE = String.fromCharCode(115,117,98,99,111,110,116,101,110,116,115,95,100,95,57,49,0);
         sportb += `${2 & promotionX.length}`;
         promotionX = `${parseInt(`${notificationS}`)}`;
         notificationS += (parseFloat(`${iconviewerE == String.fromCharCode(66,0) ? androidn.length : iconviewerE.length}`));
         androidn += `${iconviewerE.length + androidn.length}`;
         libruntimeexecutor3 /= Math.max(parseFloat(`${parseInt(`${libruntimeexecutor3}`)}`), 1);
         if (photoZ) {
            break;
         }
      } while (photoZ && ((4 | largesoundx.size) >= 2 && 1 >= (sportb.length | 4)));
      if (largesoundx.size > 4) {
         largesoundx = new Map([[`${largesoundx.size}`, 3]]);
      }
         largesoundx = new Map([[`${largesoundx.size}`, largesoundx.size + sportb.length]]);
      if (sportb.endsWith(`${largesoundx.size}`)) {
         largesoundx.set(sportb, sportb.length << (Math.min(3, Math.abs(largesoundx.size))));
      }
         largesoundx.set(sportb, 2 % (Math.max(6, largesoundx.size)));
      yellowtoredD += `${description_1ps.size}`;
      yellowtoredD += `${connection3.length}`;
       let iconsubssuccessT = 5.0;
       let megaphonen: Map<any, any> = new Map([[String.fromCharCode(99,95,57,49,95,106,114,101,118,100,99,116,0),230], [String.fromCharCode(114,97,110,115,102,111,114,109,101,114,95,108,95,57,57,0),253], [String.fromCharCode(112,95,54,57,95,113,99,101,108,112,100,97,116,97,0),471]]);
       let mapbufferu = String.fromCharCode(117,95,56,57,95,110,101,103,111,116,105,97,116,101,0);
         megaphonen = new Map([[`${megaphonen.size}`, (mapbufferu == String.fromCharCode(120,0) ? megaphonen.size : mapbufferu.length)]]);
         mapbufferu += `${mapbufferu.length << (Math.min(2, Math.abs(megaphonen.size)))}`;
          let subinN = String.fromCharCode(121,95,54,48,95,115,105,103,101,120,112,0);
         iconsubssuccessT += megaphonen.size;
         subinN += `${subinN.length - subinN.length}`;
         iconsubssuccessT += megaphonen.size;
      for (let q = 0; q < 2; q++) {
         mapbufferu += `${(mapbufferu == String.fromCharCode(67,0) ? mapbufferu.length : megaphonen.size)}`;
      }
       let schedulerI = String.fromCharCode(110,111,110,98,108,111,99,107,105,110,103,95,51,95,51,54,0);
       let videoY = String.fromCharCode(109,108,112,100,115,112,95,114,95,55,55,0);
          let launcherr: Array<any> = [String.fromCharCode(100,105,102,102,101,114,101,110,99,101,95,98,95,55,57,0), String.fromCharCode(109,117,108,116,105,101,110,100,95,117,95,53,0)];
         videoY = `${mapbufferu.length + 2}`;
         launcherr = [2];
         schedulerI += `${mapbufferu.length >> (Math.min(5, Math.abs(megaphonen.size)))}`;
          let serviceY = true;
          let stepa = String.fromCharCode(102,105,114,101,100,95,117,95,51,48,0);
         videoY += `${((serviceY ? 3 : 3) << (Math.min(Math.abs(megaphonen.size), 1)))}`;
         serviceY = stepa.length > stepa.length;
      connection3 = "2";
      sellmathbackgroundy += `${(String.fromCharCode(86,0) == yellowtoredD ? (delegate_mtH ? 3 : 2) : yellowtoredD.length)}`;
   if (sellmathbackgroundy.length <= 1) {
      topicP.push(description_1ps.size ^ topicP.length);
   }
       let iconsubssuccessH = String.fromCharCode(115,95,55,53,95,102,112,99,0);
       let pagination2 = true;
       let mathq = 5;
       let trashL = String.fromCharCode(109,111,100,117,108,101,109,97,112,95,97,95,53,55,0);
       let down_ = String.fromCharCode(113,95,48,95,100,118,100,97,116,97,0);
       let windC = 5.0;
       let contexte = 5.0;
         contexte /= Math.max(5, mathq << (Math.min(Math.abs(1), 3)));
      for (let l = 0; l < 3; l++) {
          let matht = 3;
          let whiteanimationliveB: Array<any> = [457, 507];
          let circleK = 2;
          let iconsharefriendsA = String.fromCharCode(101,120,105,115,116,97,110,99,101,95,116,95,54,52,0);
          let phonesharec = 0.0;
         iconsubssuccessH += `${2 * parseInt(`${phonesharec}`)}`;
         matht /= Math.max(5, whiteanimationliveB.length >> (Math.min(Math.abs(3), 5)));
         whiteanimationliveB = [(String.fromCharCode(95,0) == iconsharefriendsA ? circleK : iconsharefriendsA.length)];
         circleK *= (iconsharefriendsA == String.fromCharCode(103,0) ? iconsharefriendsA.length : matht);
         phonesharec += whiteanimationliveB.length + iconsharefriendsA.length;
      }
         contexte *= parseInt(`${contexte}`) * parseInt(`${windC}`);
      while ((windC / 2.1) < 5.15 && (1 ^ iconsubssuccessH.length) < 3) {
          let renewp: Map<any, any> = new Map([[String.fromCharCode(97,109,101,120,95,51,95,50,50,0),23], [String.fromCharCode(102,95,49,95,111,108,100,0),736]]);
          let iconwatchnowb: Array<any> = [688, 561];
          let flagu: Array<any> = [512, 230, 924];
          let mimeM = 2.0;
          let userP = String.fromCharCode(104,95,48,0);
         iconsubssuccessH = "3";
         renewp.set(`${flagu.length}`, renewp.size | flagu.length);
         iconwatchnowb = [parseInt(`${mimeM}`)];
         mimeM -= (userP == String.fromCharCode(108,0) ? renewp.size : userP.length);
         break;
      }
          let basketballdetailsbgJ = false;
          let shrunkE = false;
         pagination2 = contexte > 30.98 && 3 > mathq;
         basketballdetailsbgJ = (!basketballdetailsbgJ ? shrunkE : basketballdetailsbgJ);
         shrunkE = !basketballdetailsbgJ;
      let imageactionliveq = iconsubssuccessH.length >= 8813865;
      do {
         iconsubssuccessH += `${2 ^ iconsubssuccessH.length}`;
         if (imageactionliveq) {
            break;
         }
      } while ((pagination2) && imageactionliveq);
          let dialog6: Array<any> = [String.fromCharCode(109,95,49,57,95,97,110,110,111,116,97,116,101,0), String.fromCharCode(101,110,116,105,116,121,95,98,95,50,49,0)];
          let buildc: Map<any, any> = new Map([[String.fromCharCode(105,110,115,101,114,116,105,111,110,115,95,109,95,56,49,0),331], [String.fromCharCode(116,119,111,115,99,97,108,101,95,55,95,55,56,0),746], [String.fromCharCode(120,121,122,116,97,98,108,101,115,95,49,95,49,53,0),982]]);
          let predictionbuttonn = 3;
         mathq %= Math.max((String.fromCharCode(115,0) == trashL ? trashL.length : down_.length), 4);
         dialog6 = [predictionbuttonn + 3];
         buildc.set(`${predictionbuttonn}`, buildc.size);
      yellowtoredD += `${connection3.length}`;
      sellmathbackgroundy += `${topicP.length}`;
      resultx = `${topicP.length}`;
      topicP = [2];
   let grayK = 8136096 <= description_1ps.size;
   do {
      description_1ps.set(resultx, (String.fromCharCode(79,0) == resultx ? resultx.length : yellowtoredD.length));
      if (grayK) {
         break;
      }
   } while (grayK && (description_1ps.size < parseInt(`${time_b9g}`)));
   if (connection3.includes(`${logouserZ}`)) {
       let hongkongX = 0.0;
       let libsentryt: Map<any, any> = new Map([[String.fromCharCode(102,114,101,101,102,111,114,109,95,111,95,51,56,0),String.fromCharCode(105,112,97,100,105,102,102,95,104,95,55,49,0)], [String.fromCharCode(108,95,54,52,95,105,100,101,110,116,0),String.fromCharCode(116,104,114,101,101,100,111,115,116,114,95,107,95,49,57,0)]]);
       let yellowvideoliveI = 3.0;
         hongkongX /= Math.max(1, parseFloat(`${3}`));
         libsentryt = new Map([[`${libsentryt.size}`, 3]]);
         yellowvideoliveI *= parseInt(`${yellowvideoliveI}`) << (Math.min(Math.abs(libsentryt.size), 2));
      while (yellowvideoliveI >= libsentryt.size) {
         yellowvideoliveI /= Math.max(5, parseInt(`${hongkongX}`));
         break;
      }
          let save1 = 4.0;
          let nodeT: Array<any> = [String.fromCharCode(112,114,101,100,95,120,95,52,48,0), String.fromCharCode(121,95,55,50,95,104,100,97,116,97,0), String.fromCharCode(102,95,56,51,95,114,101,109,97,105,110,100,101,114,0)];
         hongkongX += parseFloat(`${3 ^ parseInt(`${yellowvideoliveI}`)}`);
         save1 /= Math.max(nodeT.length * 2, 5);
         nodeT.push(parseInt(`${save1}`) * 2);
         hongkongX /= Math.max(2, parseFloat(`${3 << (Math.min(2, Math.abs(parseInt(`${yellowvideoliveI}`))))}`));
          let layoutc = 4;
          let phone9 = String.fromCharCode(111,95,56,95,114,101,102,108,101,99,116,105,111,110,0);
         libsentryt.set(phone9, (String.fromCharCode(72,0) == phone9 ? phone9.length : layoutc));
      while (5 <= (libsentryt.size - parseInt(`${yellowvideoliveI}`)) && 3.60 <= (yellowvideoliveI - libsentryt.size)) {
         libsentryt.set(`${yellowvideoliveI}`, parseInt(`${yellowvideoliveI}`));
         break;
      }
      while ((parseInt(`${yellowvideoliveI}`) * libsentryt.size) < 4 || 4 < (parseInt(`${yellowvideoliveI}`) * libsentryt.size)) {
          let basketballmatchdetailbgV = 1.0;
          let r_playerV = 2.0;
          let filledx = String.fromCharCode(118,111,114,98,105,115,102,108,111,97,116,102,108,111,97,116,95,98,95,53,0);
          let floatingG = String.fromCharCode(99,111,100,101,99,115,117,98,98,108,111,99,107,95,102,95,54,56,0);
          let smallsound6 = false;
         libsentryt = new Map([[`${yellowvideoliveI}`, ((smallsound6 ? 3 : 2) * parseInt(`${yellowvideoliveI}`))]]);
         basketballmatchdetailbgV /= Math.max(4, parseFloat(`${floatingG.length % (Math.max(1, 8))}`));
         r_playerV += parseFloat(`${parseInt(`${basketballmatchdetailbgV}`) + filledx.length}`);
         filledx = "3";
         floatingG += `${1 | parseInt(`${r_playerV}`)}`;
         smallsound6 = r_playerV < 83.55 || basketballmatchdetailbgV < 83.55;
         break;
      }
      connection3 = `${libsentryt.size % 1}`;
   }
   if (topicP.length > time_b9g) {
      time_b9g /= Math.max(4, connection3.length);
   }
   for (let o = 0; o < 1; o++) {
      connection3 = `${(connection3 == String.fromCharCode(116,0) ? connection3.length : yellowtoredD.length)}`;
   }
       let matchinactive8 = 1.0;
       let buffer0 = String.fromCharCode(112,111,108,113,97,95,120,95,50,55,0);
       let v_playerU = 3;
       let circlej = String.fromCharCode(98,108,99,107,95,114,95,54,57,0);
       let mbnativeW = String.fromCharCode(105,95,51,52,95,112,101,114,102,0);
         buffer0 += "2";
         circlej = `${parseInt(`${matchinactive8}`)}`;
       let render8 = String.fromCharCode(120,95,50,52,95,112,117,108,108,100,111,119,110,0);
       let comment6 = String.fromCharCode(109,95,57,55,95,111,114,100,101,114,105,110,103,0);
      if (render8.length <= 2) {
          let dicelogok = true;
          let iconadslinkt = 0;
          let playershirtP = false;
          let linkZ = 5.0;
         mbnativeW += `${render8.length ^ 1}`;
         dicelogok = (!playershirtP ? dicelogok : playershirtP);
         iconadslinkt ^= ((dicelogok ? 3 : 3) & (playershirtP ? 1 : 3));
         linkZ += parseFloat(`${1 & iconadslinkt}`);
      }
      if (buffer0 != String.fromCharCode(117,0)) {
         circlej += `${(buffer0 == String.fromCharCode(86,0) ? buffer0.length : parseInt(`${matchinactive8}`))}`;
      }
      while (1 >= render8.length) {
         circlej += `${mbnativeW.length % (Math.max(2, 8))}`;
         break;
      }
         v_playerU -= 3 & comment6.length;
      if (buffer0.length < 3) {
         buffer0 = "1";
      }
      issuby -= 2;
       let values8 = 3.0;
         values8 *= parseFloat(`${1}`);
       let update_kX = 4;
          let libhermese = String.fromCharCode(112,97,99,107,101,116,105,122,97,116,105,111,110,95,52,95,54,57,0);
          let classesz = 4.0;
         values8 /= Math.max(parseFloat(`${3 * parseInt(`${classesz}`)}`), 5);
         libhermese = `${(libhermese == String.fromCharCode(89,0) ? libhermese.length : libhermese.length)}`;
         classesz -= 1 * libhermese.length;
      connection3 += `${resultx.length - 3}`;
   let chatbotphoto4 = loadingL.length >= 8567215;
   do {
       let assetsf = 1.0;
       let currentp = true;
       let filedn = 3;
         assetsf += parseFloat(`${2}`);
      while (4 == (filedn % (Math.max(4, 10))) && filedn == 4) {
          let corner6: Map<any, any> = new Map([[String.fromCharCode(117,95,50,56,95,115,116,114,101,115,115,0),false ], [String.fromCharCode(101,95,56,57,95,115,104,97,100,101,0),true ]]);
          let awayteamfieldj = String.fromCharCode(99,95,52,49,0);
          let sportU: Map<any, any> = new Map([[String.fromCharCode(116,97,103,110,99,111,109,112,97,114,101,95,109,95,53,54,0),842], [String.fromCharCode(112,95,54,56,95,115,116,111,114,105,110,103,0),571], [String.fromCharCode(118,95,49,95,97,114,103,0),627]]);
         currentp = (parseInt(`${assetsf}`) * awayteamfieldj.length) >= 20;
         corner6.set(`${corner6.size}`, sportU.size & corner6.size);
         awayteamfieldj = `${sportU.size}`;
         break;
      }
         currentp = !currentp;
         currentp = filedn >= 29;
          let reducerj = String.fromCharCode(118,95,57,57,95,103,101,116,0);
          let subbasketballplayerI = 3.0;
          let whitesmalltickr = 4;
         currentp = 32.20 >= (assetsf - subbasketballplayerI);
         reducerj = `${1 * reducerj.length}`;
         subbasketballplayerI -= parseFloat(`${whitesmalltickr}`);
         whitesmalltickr *= 2;
      for (let n = 0; n < 3; n++) {
         currentp = !currentp;
      }
          let iconedith = 5.0;
         currentp = assetsf == 53.97 && 53.97 == iconedith;
         currentp = filedn > 4;
      let tinit_eqa = assetsf <= 8979976.0;
      do {
          let u_manager6 = 1.0;
          let nterstitialQ = String.fromCharCode(104,95,51,57,95,100,101,103,114,101,101,0);
         assetsf -= (parseFloat(`${(currentp ? 3 : 4) << (Math.min(Math.abs(parseInt(`${assetsf}`)), 2))}`));
         u_manager6 -= parseInt(`${u_manager6}`) ^ nterstitialQ.length;
         nterstitialQ += `${parseInt(`${u_manager6}`) << (Math.min(nterstitialQ.length, 4))}`;
         if (tinit_eqa) {
            break;
         }
      } while (tinit_eqa && ((parseFloat(`${filedn}`) / (Math.max(assetsf, 9))) >= 1.42 || 2.60 >= (assetsf / 1.42)));
      loadingL = `${loadingL.length / 3}`;
      if (chatbotphoto4) {
         break;
      }
   } while ((2 <= (loadingL.length * 2)) && chatbotphoto4);
   for (let t = 0; t < 2; t++) {
      sellmathbackgroundy += `${parseInt(`${issuby}`) / (Math.max(1, 7))}`;
   }
      eventS /= Math.max(2, parseFloat(`${yellowcirclebg7.length & 3}`));
      eventS /= Math.max(1, parseFloat(`${description_1ps.size}`));
      eventS += parseFloat(`${parseInt(`${issuby}`)}`);
   if (2.32 >= (eventS / 4.95) && 4.95 >= (eventS / (Math.max(parseFloat(`${yellowcirclebg7.length}`), 2)))) {
       let guideV = false;
          let mappingD = true;
          let gradlef = String.fromCharCode(118,101,114,115,105,111,110,115,95,48,95,53,50,0);
         guideV = gradlef.length <= 45;
         mappingD = !mappingD;
         gradlef = `${((mappingD ? 4 : 1))}`;
         guideV = !guideV;
         guideV = guideV || !guideV;
      eventS *= parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${time_b9g}`)), 4))}`);
   }
   while (yellowtoredD.endsWith(`${description_1ps.size}`)) {
      description_1ps.set(`${issuby}`, description_1ps.size * parseInt(`${issuby}`));
      break;
   }
      yellowtoredD = `${parseInt(`${issuby}`)}`;
      yellowtoredD = `${((delegate_mtH ? 5 : 5) | yellowcirclebg7.length)}`;
   for (let w = 0; w < 2; w++) {
       let giflivestreaming3 = String.fromCharCode(120,95,54,54,95,119,101,98,115,0);
       let private_jnj = 5.0;
      while (3.65 <= private_jnj) {
          let r_locko = String.fromCharCode(113,95,54,57,95,116,104,97,119,101,100,0);
          let mbnative0 = String.fromCharCode(120,95,55,54,95,116,115,97,110,0);
          let iconmoreE = String.fromCharCode(108,97,114,103,101,115,116,95,105,95,52,53,0);
         private_jnj *= 1;
         r_locko += `${mbnative0.length / 3}`;
         mbnative0 = "2";
         iconmoreE += `${iconmoreE.length}`;
         break;
      }
      let blackJ = private_jnj <= 8213740.0;
      do {
         private_jnj -= 1;
         if (blackJ) {
            break;
         }
      } while (((3.1 - private_jnj) >= 1.86) && blackJ);
      if ((giflivestreaming3.length * parseInt(`${private_jnj}`)) == 2 && 5 == (giflivestreaming3.length >> (Math.min(Math.abs(2), 4)))) {
         private_jnj *= 3 >> (Math.min(2, giflivestreaming3.length));
      }
         giflivestreaming3 = `${giflivestreaming3.length}`;
      let pressureR = 9447856.0 <= private_jnj;
      do {
         private_jnj /= Math.max(giflivestreaming3.length, 1);
         if (pressureR) {
            break;
         }
      } while ((4.46 <= (private_jnj / 2.86)) && pressureR);
      while (private_jnj <= 4.35) {
         giflivestreaming3 = `${(giflivestreaming3 == String.fromCharCode(70,0) ? giflivestreaming3.length : parseInt(`${private_jnj}`))}`;
         break;
      }
      sellmathbackgroundy = `${parseInt(`${predictionactive4}`)}`;
   }
      yellowcirclebg7 = [3];
   for (let i = 0; i < 1; i++) {
       let pingI: Array<any> = [String.fromCharCode(113,95,52,95,104,101,99,107,0), String.fromCharCode(114,95,50,51,95,109,97,99,0), String.fromCharCode(114,116,112,114,101,99,101,105,118,101,114,95,110,95,54,52,0)];
       let apps5 = 4;
       let styleV: Map<any, any> = new Map([[String.fromCharCode(100,98,115,116,97,116,95,112,95,52,54,0),883], [String.fromCharCode(107,95,49,54,95,112,116,120,99,0),133]]);
       let positionfield3 = 1.0;
       let emojiu = true;
      if (1 == (pingI.length - apps5) && (1 - pingI.length) == 1) {
         pingI.push(3 / (Math.max(9, styleV.size)));
      }
          let gesture9 = 0;
          let viewsi = 2.0;
          let iconbellK = 4;
         emojiu = !emojiu;
         gesture9 >>= Math.min(2, Math.abs(1 >> (Math.min(Math.abs(parseInt(`${viewsi}`)), 4))));
         viewsi /= Math.max(2, 4);
         iconbellK %= Math.max(4, 1 >> (Math.min(5, Math.abs(iconbellK))));
      if (apps5 >= positionfield3) {
          let iconuserM = 0.0;
          let iconarrowleftG = 3.0;
          let iconorientation6 = String.fromCharCode(104,95,53,55,95,115,105,102,102,0);
         positionfield3 *= (parseFloat(`${parseInt(`${iconarrowleftG}`) + (emojiu ? 5 : 3)}`));
         iconuserM += iconorientation6.length | 2;
         iconarrowleftG += parseInt(`${iconuserM}`) % 1;
         iconorientation6 += `${1 - iconorientation6.length}`;
      }
         styleV = new Map([[`${pingI.length}`, pingI.length - 2]]);
      let historyB = emojiu ? !emojiu : emojiu;
      do {
         emojiu = !emojiu;
         if (historyB) {
            break;
         }
      } while (historyB && (4 < styleV.size));
         pingI = [3];
      while (emojiu) {
         emojiu = 30 >= styleV.size;
         break;
      }
      let renderZ = 5499294.0 >= positionfield3;
      do {
         positionfield3 /= Math.max(3, parseFloat(`${3}`));
         if (renderZ) {
            break;
         }
      } while (renderZ && ((parseFloat(`${styleV.size}`) - positionfield3) == 3.16 || (parseInt(`${positionfield3}`) - styleV.size) == 4));
      let positionfield_ = emojiu ? !emojiu : emojiu;
      do {
          let customn = 5.0;
          let predictionlossp: Array<any> = [607, 671];
          let defaultroombgE = String.fromCharCode(109,95,49,52,95,107,109,115,0);
         emojiu = predictionlossp.length <= 51;
         customn -= (String.fromCharCode(73,0) == defaultroombgE ? defaultroombgE.length : parseInt(`${customn}`));
         predictionlossp = [(defaultroombgE == String.fromCharCode(113,0) ? defaultroombgE.length : parseInt(`${customn}`))];
         if (positionfield_) {
            break;
         }
      } while (positionfield_ && (!emojiu));
          let predictionbanner8: Array<any> = [444, 259, 96];
          let buttons = 0.0;
          let buildl = 3.0;
         positionfield3 -= parseFloat(`${pingI.length}`);
         predictionbanner8.push(parseInt(`${buildl}`));
         buttons /= Math.max(parseFloat(`${parseInt(`${buttons}`) * predictionbanner8.length}`), 5);
          let iconbackwhiteL = String.fromCharCode(98,101,110,99,104,109,97,114,107,95,56,95,56,49,0);
          let utilsK = 4.0;
         pingI.push(styleV.size >> (Math.min(Math.abs(3), 3)));
         iconbackwhiteL += `${1 - iconbackwhiteL.length}`;
         utilsK -= (String.fromCharCode(84,0) == iconbackwhiteL ? iconbackwhiteL.length : parseInt(`${utilsK}`));
         positionfield3 /= Math.max((parseFloat(`${(emojiu ? 2 : 2) ^ parseInt(`${positionfield3}`)}`)), 3);
      let profileactivej = emojiu ? !emojiu : emojiu;
      do {
         emojiu = pingI.includes(apps5);
         if (profileactivej) {
            break;
         }
      } while ((emojiu) && profileactivej);
      while (pingI.includes(positionfield3)) {
          let tempF = String.fromCharCode(113,95,51,95,100,101,108,105,109,0);
          let kickR = String.fromCharCode(115,101,110,100,101,114,115,95,102,95,54,56,0);
          let zhengpian6: Map<any, any> = new Map([[String.fromCharCode(110,95,54,51,95,104,100,108,114,0),676], [String.fromCharCode(113,95,49,95,97,108,108,111,119,0),554]]);
         positionfield3 += parseFloat(`${apps5}`);
         tempF = `${2 << (Math.min(4, tempF.length))}`;
         kickR = `${tempF.length * zhengpian6.size}`;
         zhengpian6 = new Map([[`${zhengpian6.size}`, zhengpian6.size << (Math.min(Math.abs(3), 4))]]);
         break;
      }
      for (let x = 0; x < 3; x++) {
          let gift3 = String.fromCharCode(111,95,57,50,95,112,111,112,112,101,100,0);
          let ajaxw = String.fromCharCode(99,95,51,51,95,115,101,101,100,101,100,0);
          let inouttargetredg: Map<any, any> = new Map([[String.fromCharCode(109,95,54,57,95,121,101,116,0),683], [String.fromCharCode(112,111,115,105,116,105,111,110,105,110,103,95,49,95,51,53,0),709], [String.fromCharCode(116,95,56,56,95,117,110,105,113,117,101,100,0),470]]);
         styleV.set(`${positionfield3}`, 2 >> (Math.min(Math.abs(parseInt(`${positionfield3}`)), 5)));
         gift3 += `${ajaxw.length * inouttargetredg.size}`;
         ajaxw += `${3 & gift3.length}`;
         inouttargetredg.set(gift3, inouttargetredg.size / (Math.max(gift3.length, 6)));
      }
      issuby -= ((delegate_mtH ? 1 : 4) & parseInt(`${predictionactive4}`));
   }
   for (let p = 0; p < 2; p++) {
      yellowcirclebg7 = [description_1ps.size | 3];
   }
      loadingL += `${yellowtoredD.length}`;
      connection3 = `${(String.fromCharCode(67,0) == resultx ? resultx.length : parseInt(`${eventS}`))}`;
      loadingL = `${2 * connection3.length}`;
   for (let j = 0; j < 2; j++) {
      yellowcirclebg7.push((connection3 == String.fromCharCode(56,0) ? parseInt(`${predictionactive4}`) : connection3.length));
   }
   while (yellowtoredD.length == 2) {
      issuby /= Math.max(4, parseInt(`${predictionactive4}`) ^ topicP.length);
      break;
   }
   if (2.97 < (parseFloat(`${resultx.length}`) * predictionactive4) || 4.89 < (predictionactive4 * 2.97)) {
      predictionactive4 /= Math.max(parseFloat(`${loadingL.length % 3}`), 4);
   }
      topicP.push((loadingL == String.fromCharCode(77,0) ? logouserZ : loadingL.length));
      delegate_mtH = issuby >= 44.3 && time_b9g >= 44.3;
       let videoF = 5;
       let langkey9 = false;
       let reacty: Array<any> = [821, 799];
          let iconbellW: Array<any> = [494, 773];
          let topic8: Map<any, any> = new Map([[String.fromCharCode(106,95,50,56,95,115,105,110,0),true ], [String.fromCharCode(114,111,117,110,100,95,110,95,50,54,0),false ], [String.fromCharCode(100,101,105,110,116,101,114,108,97,99,101,95,119,95,54,53,0),true ]]);
         langkey9 = (73 >= (reacty.length >> (Math.min(5, Math.abs((!langkey9 ? reacty.length : 73))))));
         iconbellW.push(2 * iconbellW.length);
         topic8.set(`${iconbellW.length}`, topic8.size % (Math.max(2, 1)));
         videoF &= ((langkey9 ? 1 : 5) / (Math.max(reacty.length, 9)));
         reacty = [(videoF + (langkey9 ? 4 : 1))];
         reacty.push(2);
         reacty.push(reacty.length ^ 1);
         reacty.push(videoF);
      let description_2_4 = 6520852 <= videoF;
      do {
         videoF <<= Math.min(4, Math.abs(3));
         if (description_2_4) {
            break;
         }
      } while (((videoF / 3) > 4 || langkey9) && description_2_4);
       let basketballawayteamU = true;
      while (langkey9) {
         langkey9 = reacty.length < 73;
         break;
      }
      time_b9g -= 3 << (Math.min(5, topicP.length));
   while (issuby == 2.12) {
       let goallogoD: Map<any, any> = new Map([[String.fromCharCode(111,95,53,51,95,101,120,116,110,0),988], [String.fromCharCode(120,95,57,95,115,101,116,102,100,0),244]]);
      while ((goallogoD.size << (Math.min(Math.abs(1), 5))) <= 3 || (goallogoD.size << (Math.min(Math.abs(1), 3))) <= 1) {
         goallogoD = new Map([[`${goallogoD.size}`, goallogoD.size >> (Math.min(Math.abs(2), 2))]]);
         break;
      }
      while (Array.from(goallogoD.values()).includes(goallogoD.size)) {
         goallogoD = new Map([[`${goallogoD.size}`, 3]]);
         break;
      }
      if ((goallogoD.size & 4) <= 4 && (goallogoD.size & goallogoD.size) <= 4) {
          let roundP = String.fromCharCode(109,97,103,121,95,112,95,51,52,0);
          let regeng4 = String.fromCharCode(118,101,114,121,95,57,95,51,0);
          let pathN = 2;
          let qaagx = String.fromCharCode(109,95,56,57,95,109,97,120,101,100,0);
         goallogoD.set(`${qaagx}`, qaagx.length - goallogoD.size);
         roundP += `${(regeng4 == String.fromCharCode(117,0) ? regeng4.length : pathN)}`;
         pathN |= roundP.length ^ 1;
      }
      eventS *= parseFloat(`${topicP.length * 2}`);
      break;
   }
   while (4.88 >= (eventS + parseFloat(`${loadingL.length}`)) && (loadingL.length / 3) >= 2) {
      loadingL += `${loadingL.length | 1}`;
      break;
   }
   if ((2.86 * eventS) > 5.56) {
      loadingL += `${((delegate_mtH ? 4 : 5) >> (Math.min(connection3.length, 2)))}`;
   }
   while (5 <= (parseInt(`${release_fO}`) / 2) && 4 <= (2 + yellowtoredD.length)) {
      yellowtoredD = `${3 + yellowtoredD.length}`;
      break;
   }
      resultx += `${description_1ps.size ^ 1}`;
      topicP = [resultx.length / (Math.max(3, 10))];
   for (let u = 0; u < 3; u++) {
      time_b9g /= Math.max(1 & connection3.length, 3);
   }
      sellmathbackgroundy += `${3 << (Math.min(1, loadingL.length))}`;
      connection3 = `${1 - parseInt(`${eventS}`)}`;
   while (parseInt(`${time_b9g}`) < yellowcirclebg7.length) {
      time_b9g -= 3;
      break;
   }
   for (let q = 0; q < 2; q++) {
      delegate_mtH = time_b9g <= yellowtoredD.length;
   }
       let floatingc = 0;
       let yellowvideoliveg = String.fromCharCode(97,97,99,116,97,98,95,49,95,56,56,0);
       let imagenetworkerrK = 1;
      for (let g = 0; g < 1; g++) {
         imagenetworkerrK -= imagenetworkerrK;
      }
      let searchbarq = 7054464 >= floatingc;
      do {
          let shareblackg: Array<any> = [20, 78, 546];
          let shrinkV = true;
         floatingc *= 2;
         shareblackg.push(3);
         shrinkV = !shrinkV;
         if (searchbarq) {
            break;
         }
      } while ((2 <= yellowvideoliveg.length) && searchbarq);
      while (4 < (floatingc % 1) || 4 < (floatingc % 1)) {
         floatingc |= 3 * floatingc;
         break;
      }
         imagenetworkerrK %= Math.max(2, yellowvideoliveg.length);
      for (let f = 0; f < 3; f++) {
         floatingc <<= Math.min(Math.abs(imagenetworkerrK), 1);
      }
          let rulesv = 3;
          let spinnerd: Array<any> = [String.fromCharCode(121,95,51,55,95,109,111,118,101,0), String.fromCharCode(114,95,50,57,0)];
         yellowvideoliveg = `${1 >> (Math.min(4, yellowvideoliveg.length))}`;
         rulesv += 3;
         spinnerd.push(spinnerd.length);
      for (let h = 0; h < 2; h++) {
          let greyu = String.fromCharCode(100,105,115,116,114,105,99,116,95,49,95,49,0);
          let orangeuparrowq = String.fromCharCode(116,95,52,49,95,104,117,102,102,121,117,118,101,110,99,100,115,112,0);
          let notificationfilledu = true;
          let redscoreball0 = 1.0;
          let leakchecker8: Array<any> = [String.fromCharCode(121,95,56,56,95,99,111,108,115,107,105,112,0), String.fromCharCode(115,101,112,97,114,97,116,111,114,115,95,121,95,50,53,0)];
         imagenetworkerrK <<= Math.min(1, Math.abs((yellowvideoliveg == String.fromCharCode(112,0) ? floatingc : yellowvideoliveg.length)));
         greyu = `${parseInt(`${redscoreball0}`) % 2}`;
         orangeuparrowq = `${orangeuparrowq.length - 3}`;
         notificationfilledu = 100.54 <= redscoreball0 || !notificationfilledu;
         leakchecker8.push(leakchecker8.length);
      }
         yellowvideoliveg = `${imagenetworkerrK / 2}`;
      let heartD = String.fromCharCode(110,54,97,0) == yellowvideoliveg;
      do {
          let predictionbannerZ = false;
          let videoO = 3;
          let googleg = true;
          let downw: Array<any> = [528, 92, 225];
          let mbsplashP = 5;
         yellowvideoliveg += `${(yellowvideoliveg.length | (googleg ? 5 : 5))}`;
         predictionbannerZ = 28 == downw.length || mbsplashP == 28;
         videoO /= Math.max(4, mbsplashP >> (Math.min(3, Math.abs(1))));
         googleg = 33 < mbsplashP;
         downw = [3 << (Math.min(Math.abs(videoO), 4))];
         if (heartD) {
            break;
         }
      } while (heartD && (3 < (yellowvideoliveg.length - floatingc)));
      release_fO /= Math.max((parseFloat(`${(delegate_mtH ? 3 : 4) + topicP.length}`)), 3);
   if ((connection3.length * time_b9g) > 4.30) {
      time_b9g *= 1 & parseInt(`${issuby}`);
   }
   for (let o = 0; o < 1; o++) {
       let commentS: Array<any> = [988, 208, 830];
          let textinputU = true;
         commentS.push((1 << (Math.min(2, Math.abs((textinputU ? 3 : 1))))));
         commentS = [commentS.length];
      if (commentS.includes(commentS.length)) {
         commentS.push(2);
      }
      eventS *= parseFloat(`${description_1ps.size}`);
   }
   let statisticst = logouserZ <= 6638151;
   do {
      logouserZ >>= Math.min(2, Math.abs(logouserZ));
      if (statisticst) {
         break;
      }
   } while ((logouserZ == 4) && statisticst);
       let eyeclose3 = String.fromCharCode(116,95,55,51,0);
       let modulek = 5.0;
         eyeclose3 += `${parseInt(`${modulek}`)}`;
       let tailB = 3;
       let placeholderV: Map<any, any> = new Map([[String.fromCharCode(118,95,51,95,112,114,101,115,101,110,116,101,100,0),false ], [String.fromCharCode(107,95,51,54,95,115,117,98,99,99,0),false ], [String.fromCharCode(104,117,102,102,95,54,95,51,53,0),true ]]);
       let guidei = false;
       let eactE = true;
      if (3 > (tailB >> (Math.min(Math.abs(2), 4)))) {
          let subinz = 2.0;
          let roboto8 = String.fromCharCode(115,116,114,105,110,103,95,102,95,55,52,0);
          let zoomr: Map<any, any> = new Map([[String.fromCharCode(99,95,54,48,95,110,99,114,121,112,116,105,111,110,0),975], [String.fromCharCode(112,101,101,114,110,97,109,101,95,48,95,57,51,0),460], [String.fromCharCode(104,119,116,105,109,101,95,122,95,51,53,0),400]]);
          let viewer_ = String.fromCharCode(122,95,50,52,95,122,111,110,101,105,110,102,111,0);
         tailB &= eyeclose3.length;
         subinz += 1 >> (Math.min(2, Math.abs(zoomr.size)));
         roboto8 += `${zoomr.size & viewer_.length}`;
         viewer_ += `${(roboto8 == String.fromCharCode(49,0) ? roboto8.length : zoomr.size)}`;
      }
         tailB /= Math.max((eyeclose3 == String.fromCharCode(100,0) ? tailB : eyeclose3.length), 5);
      issuby /= Math.max(5, (String.fromCharCode(85,0) == loadingL ? loadingL.length : topicP.length));
   for (let o = 0; o < 2; o++) {
      hoverH.push(1);
   }
 },
}: yysIconarrowrightorangeStation) {
  const { colors, textVariants, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const navigation = useNavigation();
  const [showLoading, setShowLoading] = useState(false);
  const isVip = useAppSelector(({ userReducer }) => !(Number(userReducer.userMemberExpired) <= Number(userReducer.userCurrentTimestamp) || userReducer.userToken === ""))

  const listItem = useCallback(
    ({ item, index }: { item: yysHongkongList; index: number }) => {
      if ((item?.vod_list?.length ?? 0) === 0) return <></>;

      return (
        <View
          key={`${item.type_name}-${index}`}
          style={{
            gap: spacing.m,
            paddingLeft: spacing.sideOffset,
            paddingRight: spacing.sideOffset,
            backgroundColor: "#0c0c0c",
            borderRadius: 15,
            marginBottom: 15,
            paddingTop: 4,
            paddingBottom: 4,
          }}
        >
          <ShowMoreVodButton
            text={item.type_name.trim()}
            onPress={() => {
              navigation.navigate("午夜场剧情", {
                class: item.vod_list[0].vod_class,
              });
            }}
          />
          {
            
            item?.vod_list && (
              <VodListVerticalVip
                numOfRows={2}
                vods={item?.vod_list}
                minNumPerRow={2}
                heightToWidthRatio={1 / 1.814}
                playerMode="adult"
              />
            )
          }
        </View>
      );
    },
    []
  );

  let data = useQuery({
    queryKey: ["HomePage"],
    queryFn: () => yysCollection.getHomePages(99)
      .then((data) => {
        setShowLoading(false);
        return data;
      }),
  });

  useEffect(() => {
    if (data == undefined) {
      setShowLoading(true);
    }
  }, []);

  return (
    <SafeAreaView style={{ flex: 1 }}>
      {showLoading && (
        <View
          style={{
            position: "absolute",
            
            backgroundColor: "#0c0c0c",
            zIndex: 1,
            width: "100%",
            height: "100%",
            justifyContent: "center",
            alignItems: "center",
            borderRadius: 15,

          }}
        >
          <FastImage
            source={require("../../../static/images/lessFullscreenmaxChatbotphoto.gif")}
            style={{ width: 100, height: 100 }}
            resizeMode="contain"
          />
        </View>
      )}

      <View style={{ paddingTop: 10 }}>
        <FlatList
          data={data?.data?.categories ? data?.data?.categories : []}
          keyExtractor={(item: yysHongkongList, index: number) => index.toString() + item.type_name}
          initialNumToRender={1}
          windowSize={3}
          maxToRenderPerBatch={3}
          renderItem={listItem}
          contentContainerStyle={{ paddingBottom: 60 }}
          removeClippedSubviews={true}
          onEndReachedThreshold={0.5}
          scrollEnabled={isVip}
        />
      </View>
      { }
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  loading: {
    flexDirection: "row",
    justifyContent: "center",
    flex: 1,
  },
});
