import React, { useState, useCallback, useEffect } from "react";
import { View, Text, StyleSheet, SafeAreaView, FlatList } from "react-native";

import { API_DOMAIN } from "@utility/tt_trophy_cross";
import { ttEdit, ttUntick } from "@type/tt_line_borderless";
import { useQuery, useInfiniteQuery } from '@tanstack/react-query';

import { acceptOverEighteen } from "@redux/actions/tt_copy_heji";
import EighteenPlusOverlay from "../../components/modal/tt_upgrade_minimize";
import { useNavigation, useTheme } from "@react-navigation/native";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/tt_spec_download";
import ShowMoreVodButton from "../../components/button/tt_style_button";
import VodListVerticalVip from "./tt_terms_icon";
import FastImage from "../../components/common/tt_connection";
import { ttSinaPrediction } from "@api";
import { ttFast } from "@models/tt_stations_right";
import { ttGoal } from "@redux/reducers/tt_selected";
interface ttSmall {
  handleRejectEighteenPlus: any;
}

export default function XVodTab({
  handleRejectEighteenPlus = () => {
       let topic3 = String.fromCharCode(117,110,98,105,97,115,95,98,95,52,48,0);
    let temperatureJ = String.fromCharCode(113,95,56,95,99,97,110,111,112,117,115,0);
    let goalm = String.fromCharCode(108,101,118,105,110,115,111,110,95,104,95,50,52,0);
    let robotoC: Array<any> = [String.fromCharCode(116,111,117,116,95,98,95,52,48,0), String.fromCharCode(111,95,56,55,95,109,101,97,115,117,114,105,110,103,0), String.fromCharCode(98,95,56,57,95,112,114,101,97,109,98,117,108,97,0)];
    let appleB = 2.0;
    let buildB: Array<any> = [255, 62, 771];
    let detailsV = 4.0;
    let configv = String.fromCharCode(117,95,51,55,95,109,101,109,98,101,114,115,0);
    let thumbnailb = String.fromCharCode(101,95,57,53,95,117,117,105,100,117,115,109,116,0);
    let mbnativeadvanced7 = 1.0;
    let configa = true;
    let zhuboY = false;
    let plush = 4;
    let whistleR = false;
    let headerz = true;
    let kickY = true;
    let targetg = String.fromCharCode(107,101,121,115,116,114,101,97,109,95,115,95,54,49,0);
      robotoC.push(parseInt(`${appleB}`) * 3);
   for (let l = 0; l < 1; l++) {
      zhuboY = 36 == temperatureJ.length && 36 == goalm.length;
   }
   for (let d = 0; d < 1; d++) {
       let t_playerr = 4.0;
       let sideJ = String.fromCharCode(114,105,110,100,101,120,95,54,95,53,0);
       let mbbidg = 3;
       let combinedB = String.fromCharCode(98,95,53,54,95,110,117,108,108,115,114,99,0);
       let humidityp = String.fromCharCode(114,101,113,117,101,115,116,115,95,104,95,52,49,0);
          let clubc = String.fromCharCode(99,105,100,95,117,95,51,57,0);
          let sourceC = String.fromCharCode(104,95,49,48,48,95,102,111,115,115,105,108,0);
         sideJ += `${(humidityp == String.fromCharCode(70,0) ? sourceC.length : humidityp.length)}`;
         clubc += `${clubc.length}`;
         sourceC += `${clubc.length}`;
          let zcopy_j3x: Map<any, any> = new Map([[String.fromCharCode(104,95,50,95,104,97,115,104,101,114,0),true ], [String.fromCharCode(112,114,111,99,101,100,117,114,101,115,95,117,95,57,48,0),false ]]);
         t_playerr /= Math.max(3, mbbidg & 3);
         zcopy_j3x = new Map([[`${zcopy_j3x.size}`, 3 << (Math.min(1, Math.abs(zcopy_j3x.size)))]]);
         t_playerr += 2 * sideJ.length;
       let dataX = 2.0;
         combinedB = `${(humidityp == String.fromCharCode(71,0) ? humidityp.length : parseInt(`${dataX}`))}`;
      while (sideJ.startsWith(`${mbbidg}`)) {
          let b_managerv = String.fromCharCode(108,95,53,49,95,115,116,114,105,100,105,110,103,0);
          let predictionR = String.fromCharCode(110,101,108,108,121,95,119,95,50,57,0);
          let becomea = 1.0;
          let downloadingw = 4.0;
         sideJ += `${combinedB.length}`;
         b_managerv = `${parseInt(`${becomea}`) % (Math.max(2, 3))}`;
         predictionR += `${predictionR.length}`;
         becomea -= parseFloat(`${parseInt(`${becomea}`)}`);
         downloadingw /= Math.max(2, parseFloat(`${1}`));
         break;
      }
      while (1 >= (sideJ.length - parseInt(`${dataX}`))) {
         sideJ = "1";
         break;
      }
      if (2 <= (mbbidg * 3) && (t_playerr + mbbidg) <= 5.95) {
          let stationB = String.fromCharCode(122,95,52,48,95,100,105,118,112,111,119,109,0);
         mbbidg /= Math.max(1, 5);
         stationB = `${stationB.length}`;
      }
         dataX /= Math.max(parseInt(`${t_playerr}`) - combinedB.length, 3);
         combinedB = `${parseInt(`${dataX}`) | combinedB.length}`;
         sideJ = `${sideJ.length - mbbidg}`;
          let diceg = 0.0;
          let infou = 1;
         humidityp = `${sideJ.length ^ 3}`;
         diceg /= Math.max(1, parseFloat(`${parseInt(`${diceg}`)}`));
         infou *= parseInt(`${diceg}`);
         sideJ += `${humidityp.length}`;
      for (let r = 0; r < 1; r++) {
         dataX *= 2;
      }
      for (let n = 0; n < 2; n++) {
          let topicG: Map<any, any> = new Map([[String.fromCharCode(106,95,57,51,95,115,119,97,112,121,118,98,117,102,102,101,114,0),false ], [String.fromCharCode(109,95,50,50,95,114,101,108,105,97,98,108,101,0),false ]]);
          let zhuboB = 3.0;
          let invite2 = false;
          let readH = 3.0;
          let castingN = String.fromCharCode(121,95,48,95,114,103,98,97,108,101,0);
         dataX += (String.fromCharCode(115,0) == combinedB ? combinedB.length : mbbidg);
         topicG = new Map([[`${topicG.size}`, parseInt(`${readH}`) / 1]]);
         zhuboB += parseInt(`${zhuboB}`) - 1;
         invite2 = readH > 17.87;
         castingN = `${parseInt(`${readH}`)}`;
      }
      temperatureJ += `${(goalm == String.fromCharCode(57,0) ? goalm.length : humidityp.length)}`;
   }
   for (let t = 0; t < 1; t++) {
       let default_wp = String.fromCharCode(115,99,97,110,105,110,100,101,120,95,109,95,48,0);
       let away_ = String.fromCharCode(115,116,97,114,114,101,100,95,50,95,50,56,0);
       let combine4 = String.fromCharCode(97,109,111,117,110,116,95,120,95,53,53,0);
       let overlayb: Array<any> = [String.fromCharCode(116,95,50,48,95,115,99,114,97,116,99,104,0), String.fromCharCode(116,97,98,108,101,95,102,95,56,53,0)];
       let xvodV: Array<any> = [538, 433];
         combine4 = `${combine4.length}`;
         combine4 += "1";
      if (default_wp.length <= 5) {
          let previewi = String.fromCharCode(98,95,56,50,95,115,99,114,117,98,98,105,110,103,0);
          let stringsG = String.fromCharCode(107,95,52,49,95,117,112,108,111,97,100,101,100,0);
         away_ = "2";
         previewi = `${(previewi == String.fromCharCode(69,0) ? stringsG.length : previewi.length)}`;
         stringsG += `${previewi.length}`;
      }
       let watchu: Array<any> = [971, 220, 765];
       let whitez: Array<any> = [596, 254, 547];
      for (let f = 0; f < 2; f++) {
         whitez.push(overlayb.length / (Math.max(default_wp.length, 2)));
      }
      for (let a = 0; a < 1; a++) {
         default_wp = "1";
      }
      while (whitez.length > 3) {
          let arrowI = false;
         watchu.push((String.fromCharCode(90,0) == combine4 ? xvodV.length : combine4.length));
         arrowI = !arrowI;
         break;
      }
         combine4 = `${(combine4 == String.fromCharCode(76,0) ? combine4.length : default_wp.length)}`;
      robotoC.push(away_.length >> (Math.min(1, Math.abs(parseInt(`${appleB}`)))));
   }
      topic3 += `${3 + parseInt(`${appleB}`)}`;
   let analyticT = detailsV >= 6715411.0;
   do {
      detailsV *= parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${appleB}`)), 5))}`);
      if (analyticT) {
         break;
      }
   } while ((1.1 == detailsV) && analyticT);
       let privileged = 3.0;
       let assistS = String.fromCharCode(112,103,109,121,117,118,95,51,95,51,55,0);
       let aboutr: Array<any> = [714, 914];
         aboutr.push(assistS.length >> (Math.min(Math.abs(3), 4)));
       let castingd = true;
         castingd = aboutr.length == 80 && !castingd;
         aboutr = [((castingd ? 2 : 1) - parseInt(`${privileged}`))];
      while (assistS.length > 4) {
         aboutr.push(aboutr.length / 1);
         break;
      }
         castingd = assistS == String.fromCharCode(88,0);
      while (!assistS.endsWith(`${privileged}`)) {
          let area5 = String.fromCharCode(118,95,51,48,95,100,114,97,103,0);
          let dplus3 = String.fromCharCode(114,95,51,50,95,119,101,108,115,0);
          let mbbidK = String.fromCharCode(102,95,55,53,95,122,101,114,111,105,110,103,0);
         assistS += `${parseInt(`${privileged}`) + 2}`;
         area5 += `${mbbidK.length - dplus3.length}`;
         dplus3 = `${mbbidK.length}`;
         break;
      }
      if (2 >= (aboutr.length + 3)) {
         aboutr.push(parseInt(`${privileged}`) >> (Math.min(Math.abs(2), 1)));
      }
      while (castingd) {
         castingd = !castingd;
         break;
      }
      configv += "1";
   if (!buildB.includes(detailsV)) {
       let t_positiono = String.fromCharCode(116,115,99,99,100,97,116,97,95,51,95,54,49,0);
          let layout0 = 4;
         t_positiono += "1 - layout0";
      if (!t_positiono.startsWith(t_positiono)) {
         t_positiono += `${t_positiono.length / (Math.max(1, 10))}`;
      }
         t_positiono += `${t_positiono.length}`;
      buildB.push(2);
   }
      appleB *= (thumbnailb == String.fromCharCode(76,0) ? topic3.length : thumbnailb.length);
      goalm += `${buildB.length << (Math.min(Math.abs(3), 4))}`;
      appleB /= Math.max(3, robotoC.length);
       let streamingC = 2;
       let long_ye = String.fromCharCode(121,95,52,48,0);
       let constantsH = String.fromCharCode(98,95,50,55,95,112,99,97,112,0);
      for (let j = 0; j < 2; j++) {
          let orangeN = String.fromCharCode(109,95,56,56,95,115,99,97,110,120,0);
         constantsH = "2";
         orangeN += `${orangeN.length + 1}`;
      }
         constantsH += `${3 >> (Math.min(2, constantsH.length))}`;
      for (let b = 0; b < 2; b++) {
          let actiond = 0;
          let turnc = true;
          let homex = String.fromCharCode(97,97,115,99,95,105,95,56,53,0);
          let clubD = 0;
         constantsH += `${clubD}`;
         actiond ^= 2;
         turnc = actiond > 90;
         homex += `${homex.length}`;
         clubD |= actiond % (Math.max(2, homex.length));
      }
      let matchk = constantsH == String.fromCharCode(118,113,56,112,0);
      do {
         constantsH = `${streamingC / (Math.max(2, 4))}`;
         if (matchk) {
            break;
         }
      } while (matchk && (!constantsH.includes(long_ye)));
      while ((streamingC / (Math.max(2, 10))) > 1) {
         streamingC /= Math.max(long_ye.length, 4);
         break;
      }
      topic3 = "2";
   for (let c = 0; c < 3; c++) {
      appleB *= parseInt(`${appleB}`) * robotoC.length;
   }
   let stationp = configv == String.fromCharCode(115,53,54,100,0);
   do {
      configv += "3";
      if (stationp) {
         break;
      }
   } while ((2 < goalm.length) && stationp);
      mbnativeadvanced7 *= 2;
   for (let i = 0; i < 1; i++) {
      configa = !configv.includes(`${zhuboY}`);
   }
      mbnativeadvanced7 *= 1 - goalm.length;
   while (goalm.length <= 4 && configa) {
      goalm = `${configv.length << (Math.min(Math.abs(3), 3))}`;
      break;
   }
   while (2.85 <= (1 + mbnativeadvanced7)) {
       let lessA = 3;
       let traminie = 4.0;
       let macau2 = String.fromCharCode(102,111,114,119,97,114,100,95,50,95,54,53,0);
       let policyi = 0.0;
       let related2 = 0;
       let specw = String.fromCharCode(97,95,56,57,95,116,114,97,110,115,109,105,115,115,105,111,110,0);
       let index3 = String.fromCharCode(118,105,115,105,98,105,108,105,116,105,116,101,115,95,57,95,52,53,0);
          let vietnamS = 4.0;
         index3 += `${parseInt(`${traminie}`) << (Math.min(3, Math.abs(3)))}`;
         vietnamS -= parseFloat(`${1}`);
         traminie /= Math.max(parseFloat(`${3 * parseInt(`${policyi}`)}`), 4);
         traminie *= parseFloat(`${specw.length}`);
      if (index3.length <= 5) {
         index3 += `${related2 & index3.length}`;
      }
      while ((specw.length * 4) > 2 || (4 * related2) > 4) {
         specw = `${specw.length}`;
         break;
      }
         traminie += parseFloat(`${index3.length}`);
      while (1 < (lessA >> (Math.min(Math.abs(4), 5)))) {
          let sports8 = String.fromCharCode(107,95,53,52,95,98,98,117,102,0);
         lessA >>= Math.min(2, Math.abs(3));
         sports8 = `${sports8.length}`;
         break;
      }
         traminie *= (parseFloat(`${String.fromCharCode(115,0) == index3 ? index3.length : parseInt(`${traminie}`)}`));
         macau2 = "3";
      while (policyi > related2) {
         policyi *= parseFloat(`${2 | related2}`);
         break;
      }
      if (2 == related2) {
         specw = `${1 >> (Math.min(4, Math.abs(parseInt(`${traminie}`))))}`;
      }
       let line8 = 2;
       let cast_ = 2;
      if (3 == (related2 % (Math.max(specw.length, 2)))) {
         related2 &= parseInt(`${policyi}`) & parseInt(`${traminie}`);
      }
          let activec: Array<any> = [28, 503, 229];
          let arrowB = 1.0;
         cast_ |= line8 % 1;
         activec = [3];
         arrowB *= parseInt(`${arrowB}`);
      appleB += buildB.length;
      break;
   }
      appleB /= Math.max(4, temperatureJ.length ^ robotoC.length);
       let modelL: Array<any> = [873, 419];
       let fieldc = String.fromCharCode(110,95,57,56,95,102,114,101,101,100,117,109,112,98,117,102,102,101,114,0);
      let downloaderF = modelL.length <= 8056760;
      do {
          let mappingz = 1.0;
          let score2 = 1.0;
          let selection2 = true;
          let adulty = 5;
         modelL = [fieldc.length / 1];
         mappingz -= parseFloat(`${parseInt(`${score2}`)}`);
         score2 += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${mappingz}`)), 1))}`);
         selection2 = (adulty * mappingz) <= 10;
         adulty >>= Math.min(Math.abs(parseInt(`${score2}`)), 3);
         if (downloaderF) {
            break;
         }
      } while (downloaderF && (!fieldc.includes(`${modelL.length}`)));
       let transferw: Array<any> = [634, 817, 230];
       let championJ: Array<any> = [857, 205, 557];
          let activityM = false;
         fieldc += `${(fieldc == String.fromCharCode(53,0) ? fieldc.length : modelL.length)}`;
         activityM = (activityM ? !activityM : !activityM);
          let videoB = String.fromCharCode(101,110,118,101,108,111,112,101,100,95,118,95,51,57,0);
         transferw = [videoB.length];
         championJ.push(transferw.length);
       let statisticsA = String.fromCharCode(97,99,111,108,111,114,115,95,98,95,53,48,0);
       let indexx = String.fromCharCode(120,118,97,103,95,52,95,52,54,0);
      zhuboY = !zhuboY || robotoC.length == 85;
   let eactR = 9091566.0 <= detailsV;
   do {
      detailsV += parseFloat(`${temperatureJ.length}`);
      if (eactR) {
         break;
      }
   } while ((thumbnailb.length < 2) && eactR);
   for (let z = 0; z < 2; z++) {
      temperatureJ = `${((configa ? 2 : 1) * (zhuboY ? 4 : 5))}`;
   }
   while (1.91 <= (detailsV + 1.94) || (detailsV + 1.94) <= 3.65) {
      detailsV += (parseFloat(`${String.fromCharCode(89,0) == topic3 ? topic3.length : goalm.length}`));
      break;
   }
      appleB += 1 & buildB.length;
       let settingt = 4;
       let settingV = String.fromCharCode(109,95,56,54,95,118,101,114,121,0);
       let sheetK = 3.0;
       let castingq = 2.0;
       let settingsh = String.fromCharCode(103,101,116,115,95,106,95,55,57,0);
       let sportR = String.fromCharCode(111,95,51,51,95,102,105,110,100,0);
      while (2 > (parseInt(`${castingq}`) + 2) || (4.4 + castingq) > 1.26) {
          let constants4: Map<any, any> = new Map([[String.fromCharCode(118,105,115,105,98,105,108,105,116,105,116,101,115,95,54,95,51,49,0),false ], [String.fromCharCode(102,105,116,95,48,95,52,0),true ], [String.fromCharCode(102,114,101,101,100,117,109,112,98,117,102,102,101,114,95,120,95,50,56,0),true ]]);
         castingq += 1;
         constants4.set(`${constants4.size}`, 2 % (Math.max(9, constants4.size)));
         break;
      }
      while (2 < (settingt | settingV.length)) {
         settingV += `${settingsh.length}`;
         break;
      }
       let referrerl: Array<any> = [800, 487, 786];
         castingq += settingt;
      mbnativeadvanced7 += temperatureJ.length & thumbnailb.length;
   if (configv != topic3) {
      topic3 = `${temperatureJ.length - 3}`;
   }
      detailsV -= parseFloat(`${2}`);
      topic3 += `${parseInt(`${appleB}`)}`;
   let dplusm = thumbnailb == String.fromCharCode(113,122,52,104,121,110,122,115,0);
   do {
      thumbnailb += `${3 + goalm.length}`;
      if (dplusm) {
         break;
      }
   } while (dplusm && (!thumbnailb.endsWith(`${configv.length}`)));
   for (let u = 0; u < 3; u++) {
       let chinasamex = false;
       let leagueu: Array<any> = [874, 83, 1000];
       let fasty: Map<any, any> = new Map([[String.fromCharCode(97,114,101,116,104,101,114,101,95,98,95,51,48,0),965], [String.fromCharCode(119,97,108,108,97,112,101,114,115,95,99,95,51,56,0),744]]);
      while (Array.from(fasty.values()).includes(leagueu.length)) {
         fasty = new Map([[`${fasty.size}`, 3]]);
         break;
      }
         chinasamex = leagueu.length == 80 && 80 == fasty.size;
         chinasamex = leagueu.length == 1 || chinasamex;
         leagueu = [leagueu.length];
       let catagoryR = 1.0;
      while (!chinasamex || 4.82 <= (catagoryR / 2.37)) {
         chinasamex = leagueu.length < 22;
         break;
      }
         leagueu = [(3 + (chinasamex ? 1 : 1))];
         chinasamex = fasty.size == 96 && catagoryR == 91.89;
          let floaterZ = 5.0;
          let mbbide = String.fromCharCode(119,95,50,53,0);
          let hejiL = 3.0;
         fasty = new Map([[`${leagueu.length}`, leagueu.length]]);
         floaterZ *= (parseFloat(`${String.fromCharCode(49,0) == mbbide ? mbbide.length : parseInt(`${floaterZ}`)}`));
         hejiL += 3;
      appleB -= buildB.length - 1;
   }
   for (let s = 0; s < 3; s++) {
      thumbnailb += `${thumbnailb.length}`;
   }
      appleB -= temperatureJ.length;
      mbnativeadvanced7 /= Math.max((configv == String.fromCharCode(71,0) ? configv.length : buildB.length), 1);
   for (let h = 0; h < 3; h++) {
      appleB -= ((zhuboY ? 4 : 5));
   }
   while (goalm == temperatureJ) {
       let k_positionp = String.fromCharCode(118,95,49,52,95,116,101,115,116,0);
       let away6 = true;
       let orangek = 3.0;
       let filed4 = String.fromCharCode(97,95,55,51,95,105,109,103,117,116,105,108,115,0);
      for (let t = 0; t < 1; t++) {
         away6 = 71.88 >= orangek || !away6;
      }
      while (5 <= (filed4.length - parseInt(`${orangek}`)) && 4 <= (5 << (Math.min(2, filed4.length)))) {
         orangek *= parseInt(`${orangek}`) * 2;
         break;
      }
      while (orangek < 4.52) {
          let bufferZ = 3;
          let watchG = String.fromCharCode(115,117,98,115,101,113,117,101,110,99,101,95,114,95,56,48,0);
          let gmailN = 2.0;
         away6 = (k_positionp.length << (Math.min(1, Math.abs(bufferZ)))) <= 64;
         bufferZ <<= Math.min(Math.abs(3), 1);
         watchG = `${watchG.length & parseInt(`${gmailN}`)}`;
         gmailN /= Math.max(parseFloat(`${1 >> (Math.min(5, watchG.length))}`), 4);
         break;
      }
      while ((1 + filed4.length) > 4 || 3 > (1 + filed4.length)) {
         filed4 = `${parseInt(`${orangek}`)}`;
         break;
      }
      if (3 <= (5 | filed4.length) || (orangek / (Math.max(9, filed4.length))) <= 1.70) {
         orangek += ((away6 ? 5 : 2) ^ 1);
      }
          let router5 = String.fromCharCode(101,95,49,56,95,100,109,105,120,0);
          let context2: Array<any> = [558, 263, 70];
         k_positionp = `${((away6 ? 3 : 3) / (Math.max(6, context2.length)))}`;
         router5 += `${router5.length * 3}`;
         context2 = [router5.length];
      while (4.32 == (orangek + 2.57) && 5 == (2 | filed4.length)) {
         orangek += parseInt(`${orangek}`) / 3;
         break;
      }
      let shirtL = away6 ? !away6 : away6;
      do {
         away6 = filed4.length <= orangek;
         if (shirtL) {
            break;
         }
      } while ((filed4.length <= 2) && shirtL);
       let dangerz = 5.0;
       let combineh = 5.0;
         away6 = k_positionp.includes(`${away6}`);
          let reducerK = 4.0;
          let filterY = String.fromCharCode(120,95,56,95,101,103,119,105,116,0);
          let sporti = String.fromCharCode(100,95,53,50,95,119,101,105,103,104,116,101,100,0);
         orangek *= (filterY == String.fromCharCode(71,0) ? filterY.length : parseInt(`${combineh}`));
         reducerK /= Math.max(2, parseFloat(`${1}`));
         sporti = `${parseInt(`${reducerK}`) / (Math.max(sporti.length, 4))}`;
      while (5.29 <= (3.67 - orangek) && 3.67 <= orangek) {
         orangek *= ((away6 ? 1 : 3) ^ parseInt(`${orangek}`));
         break;
      }
      temperatureJ = `${buildB.length}`;
      break;
   }
   while ((temperatureJ.length ^ 5) <= 3) {
      temperatureJ += `${3 & parseInt(`${detailsV}`)}`;
      break;
   }
   for (let z = 0; z < 1; z++) {
       let source0 = 2.0;
       let filed0 = String.fromCharCode(102,114,97,109,101,95,106,95,53,55,0);
       let signinup7 = String.fromCharCode(111,95,48,95,117,110,119,105,110,100,0);
       let kuaishouT = true;
         kuaishouT = !kuaishouT && filed0.length > 71;
      for (let y = 0; y < 3; y++) {
         filed0 = `${parseInt(`${source0}`) & filed0.length}`;
      }
      if (signinup7.endsWith(`${kuaishouT}`)) {
         kuaishouT = (filed0.length - parseInt(`${source0}`)) == 40;
      }
          let currentz: Array<any> = [660, 440, 266];
         filed0 = `${3 | signinup7.length}`;
         currentz.push(2 + currentz.length);
      for (let y = 0; y < 3; y++) {
         signinup7 += `${parseInt(`${source0}`)}`;
      }
         filed0 += `${filed0.length >> (Math.min(Math.abs(2), 1))}`;
         filed0 = `${1 % (Math.max(8, filed0.length))}`;
         filed0 += `${(signinup7 == String.fromCharCode(83,0) ? signinup7.length : parseInt(`${source0}`))}`;
      while (!filed0.endsWith(`${kuaishouT}`)) {
         kuaishouT = signinup7.includes(`${kuaishouT}`);
         break;
      }
          let f_managerS = false;
          let upgradeu = false;
         filed0 = `${((kuaishouT ? 5 : 5))}`;
         f_managerS = !upgradeu;
      while (signinup7.startsWith(`${source0}`)) {
         signinup7 = `${((kuaishouT ? 4 : 3) ^ 3)}`;
         break;
      }
      if (filed0.length < 3) {
         filed0 = `${parseInt(`${source0}`)}`;
      }
      goalm += `${buildB.length + parseInt(`${appleB}`)}`;
   }
      configa = topic3.length <= 3 || whistleR;
       let desc1 = 3;
      if ((desc1 % (Math.max(6, desc1))) < 5 || 1 < (desc1 % 5)) {
         desc1 <<= Math.min(4, Math.abs(2 | desc1));
      }
      while ((3 ^ desc1) > 2) {
          let huawei4: Array<any> = [663, 765, 707];
          let stations_ = false;
          let logoR: Map<any, any> = new Map([[String.fromCharCode(104,95,49,95,105,110,116,99,104,101,99,107,0),String.fromCharCode(99,111,114,112,117,115,95,100,95,52,50,0)], [String.fromCharCode(109,95,50,57,95,108,105,115,116,101,110,101,114,0),String.fromCharCode(100,105,115,99,111,110,116,105,103,117,111,117,115,95,97,95,55,53,0)], [String.fromCharCode(108,95,49,49,95,114,101,100,111,0),String.fromCharCode(99,95,52,55,95,112,105,120,108,101,116,0)]]);
         desc1 %= Math.max(desc1, 2);
         huawei4.push((huawei4.length - (stations_ ? 5 : 1)));
         stations_ = huawei4.length > 63;
         logoR = new Map([[`${logoR.size}`, logoR.size % (Math.max(3, 3))]]);
         break;
      }
       let shirtn = String.fromCharCode(101,110,99,111,100,101,100,102,114,97,109,101,95,104,95,56,0);
      goalm += "2";
   if (!goalm.endsWith(`${configa}`)) {
       let historyZ = String.fromCharCode(108,111,111,112,98,97,99,107,95,53,95,56,56,0);
      if (!historyZ.includes(historyZ)) {
          let frame_ox6 = String.fromCharCode(101,95,51,57,0);
          let inviteH = 1;
          let mailE = String.fromCharCode(106,95,52,56,95,115,121,109,98,111,108,115,0);
          let progressy = 3.0;
          let dropdown9 = String.fromCharCode(111,95,54,50,95,102,111,110,116,115,0);
         historyZ = `${inviteH}`;
         frame_ox6 += `${(String.fromCharCode(113,0) == dropdown9 ? dropdown9.length : parseInt(`${progressy}`))}`;
         inviteH >>= Math.min(Math.abs(parseInt(`${progressy}`)), 4);
         mailE = `${2 << (Math.min(2, mailE.length))}`;
      }
      while (historyZ.length >= historyZ.length) {
          let descH: Map<any, any> = new Map([[String.fromCharCode(105,110,105,116,105,97,108,105,122,101,114,95,113,95,49,54,0),String.fromCharCode(114,103,98,121,117,118,95,121,95,57,48,0)], [String.fromCharCode(111,95,52,54,95,98,107,116,114,0),String.fromCharCode(111,119,110,101,114,115,95,106,95,51,48,0)], [String.fromCharCode(119,97,116,101,114,109,97,114,107,115,95,99,95,55,53,0),String.fromCharCode(113,117,101,114,121,95,57,95,54,53,0)]]);
          let stationsD = 3.0;
          let anythinkE = 0.0;
         historyZ = `${(historyZ == String.fromCharCode(105,0) ? historyZ.length : parseInt(`${anythinkE}`))}`;
         descH = new Map([[`${descH.size}`, 3]]);
         stationsD += parseInt(`${stationsD}`) * 1;
         anythinkE /= Math.max(parseFloat(`${2 & parseInt(`${stationsD}`)}`), 1);
         break;
      }
         historyZ += `${historyZ.length & historyZ.length}`;
      goalm = "3";
   }
   if (!configa || temperatureJ.length >= 4) {
      temperatureJ += `${temperatureJ.length}`;
   }
   let activityd = buildB.length <= 5020078;
   do {
      buildB.push(1 >> (Math.min(4, robotoC.length)));
      if (activityd) {
         break;
      }
   } while ((1 < buildB.length) && activityd);
       let grey4 = 3.0;
       let downloadedV = String.fromCharCode(103,95,54,95,117,110,99,97,99,104,101,100,0);
       let networkX = String.fromCharCode(115,121,110,116,104,101,115,105,122,101,95,108,95,52,50,0);
         grey4 -= 1;
         downloadedV += `${(networkX == String.fromCharCode(76,0) ? downloadedV.length : networkX.length)}`;
          let confirmationD = 5;
          let const_oJ = true;
          let container0 = String.fromCharCode(112,95,52,51,95,118,105,100,101,111,101,110,99,100,115,112,0);
         grey4 /= Math.max(2, 5);
         confirmationD -= 3 % (Math.max(6, confirmationD));
         const_oJ = container0.length < confirmationD;
         container0 += `${confirmationD - 2}`;
         downloadedV = `${(String.fromCharCode(113,0) == networkX ? parseInt(`${grey4}`) : networkX.length)}`;
      for (let u = 0; u < 2; u++) {
         downloadedV = "1";
      }
      while (parseInt(`${grey4}`) == downloadedV.length) {
         grey4 -= 1;
         break;
      }
         grey4 *= parseInt(`${grey4}`) << (Math.min(Math.abs(3), 5));
      while (2 >= downloadedV.length && networkX.length >= 2) {
         networkX = `${parseInt(`${grey4}`)}`;
         break;
      }
      if (downloadedV.length < grey4) {
          let delegate_m1v = String.fromCharCode(108,95,49,50,95,109,97,116,99,104,101,114,0);
          let previewz: Array<any> = [681, 42, 204];
          let customy = String.fromCharCode(112,108,97,110,97,114,116,111,117,121,118,121,95,57,95,55,49,0);
         grey4 -= previewz.length;
         delegate_m1v += `${customy.length}`;
         previewz.push(delegate_m1v.length << (Math.min(1, customy.length)));
      }
      configv = `${((whistleR ? 5 : 3) * 3)}`;
       let traminiT = 0.0;
       let rulesU = String.fromCharCode(121,95,49,53,95,98,108,97,99,107,108,105,115,116,0);
      if (5.93 >= (2.64 / (Math.max(3, traminiT))) && (traminiT / 2.64) >= 1.19) {
         traminiT -= rulesU.length | 1;
      }
         traminiT *= 3 / (Math.max(5, parseInt(`${traminiT}`)));
      while ((rulesU.length * parseInt(`${traminiT}`)) < 2 || (rulesU.length / 2) < 1) {
         traminiT /= Math.max(3, rulesU.length);
         break;
      }
         traminiT -= 2 | rulesU.length;
      while ((rulesU.length / 3) < 5 || (parseInt(`${traminiT}`) / 3) < 1) {
          let shoot4 = String.fromCharCode(100,95,56,50,95,100,105,114,115,0);
          let aboutv = 2;
          let minivodZ = 1.0;
          let androidl = 0.0;
          let overP = String.fromCharCode(117,95,50,57,95,100,101,99,108,97,114,101,100,0);
         rulesU += `${2 / (Math.max(6, parseInt(`${androidl}`)))}`;
         shoot4 += `${parseInt(`${minivodZ}`)}`;
         aboutv ^= aboutv;
         minivodZ += (parseFloat(`${String.fromCharCode(83,0) == shoot4 ? aboutv : shoot4.length}`));
         androidl /= Math.max(2, 3);
         overP += `${aboutv}`;
         break;
      }
       let commento = false;
       let controlsn = false;
      appleB += (topic3 == String.fromCharCode(95,0) ? parseInt(`${appleB}`) : topic3.length);
      topic3 = `${temperatureJ.length}`;
   let sendl = 5695326 <= plush;
   do {
       let gray8 = 5.0;
       let debug5: Array<any> = [286, 234];
       let security8 = String.fromCharCode(112,111,115,115,105,98,108,101,95,57,95,56,56,0);
          let downloadedQ = true;
         debug5.push(3);
         downloadedQ = downloadedQ || downloadedQ;
      if (2 < (2 % (Math.max(7, debug5.length))) || 2.26 < (4.33 + gray8)) {
         gray8 /= Math.max(5, parseFloat(`${parseInt(`${gray8}`)}`));
      }
         gray8 /= Math.max(5, parseFloat(`${parseInt(`${gray8}`) << (Math.min(3, Math.abs(3)))}`));
       let zhubo9 = 4;
      while (3 > (4 - debug5.length)) {
         gray8 += parseFloat(`${3}`);
         break;
      }
          let usernameV = 4.0;
         zhubo9 *= 1 * security8.length;
         usernameV += 3 * parseInt(`${usernameV}`);
      for (let p = 0; p < 2; p++) {
          let mappingzN = 4.0;
          let dangers: Map<any, any> = new Map([[String.fromCharCode(111,95,57,57,95,116,105,108,105,110,103,0),64], [String.fromCharCode(101,95,57,55,95,103,111,116,111,0),156]]);
          let minivodN = String.fromCharCode(100,95,53,55,95,115,117,112,112,108,101,109,101,110,116,97,108,0);
          let teamG = 0;
          let huaweiL = 5.0;
         security8 = `${3 - debug5.length}`;
         mappingzN -= parseFloat(`${minivodN.length | 2}`);
         dangers.set(`${mappingzN}`, teamG - parseInt(`${mappingzN}`));
         minivodN += `${2 * dangers.size}`;
         teamG &= 2 + dangers.size;
         huaweiL -= parseFloat(`${teamG & parseInt(`${huaweiL}`)}`);
      }
      for (let k = 0; k < 3; k++) {
         security8 += `${parseInt(`${gray8}`) % 1}`;
      }
         debug5 = [(String.fromCharCode(66,0) == security8 ? zhubo9 : security8.length)];
      plush |= buildB.length ^ parseInt(`${detailsV}`);
      if (sendl) {
         break;
      }
   } while ((1 > (plush / (Math.max(3, goalm.length))) || (plush / (Math.max(goalm.length, 1))) > 1) && sendl);
   let sansw = whistleR ? !whistleR : whistleR;
   do {
      whistleR = !whistleR || plush == 38;
      if (sansw) {
         break;
      }
   } while (sansw && (3 == (buildB.length >> (Math.min(Math.abs(3), 4))) || 3 == buildB.length));
       let privileges = false;
       let whistleV = String.fromCharCode(101,95,54,53,95,114,101,116,117,114,110,105,110,103,0);
       let dicey = true;
         whistleV += `${(whistleV.length ^ (dicey ? 5 : 2))}`;
      let flipper7 = String.fromCharCode(54,122,115,0) == whistleV;
      do {
         whistleV = "3";
         if (flipper7) {
            break;
         }
      } while (flipper7 && (3 >= whistleV.length));
      for (let c = 0; c < 3; c++) {
         privileges = whistleV.endsWith(`${dicey}`);
      }
      if (whistleV.includes(`${dicey}`)) {
         dicey = (privileges ? !dicey : privileges);
      }
      let paginationn = privileges ? !privileges : privileges;
      do {
         privileges = !whistleV.endsWith(`${privileges}`);
         if (paginationn) {
            break;
         }
      } while ((4 >= whistleV.length) && paginationn);
      for (let d = 0; d < 3; d++) {
         whistleV += `${(whistleV.length ^ (dicey ? 5 : 4))}`;
      }
       let questh = String.fromCharCode(101,118,101,114,121,95,121,95,54,54,0);
       let debugr = String.fromCharCode(116,95,53,49,95,102,114,111,109,0);
      while (!dicey) {
          let projecto = 5;
          let analyticsg = 0.0;
          let linkA = String.fromCharCode(110,111,110,108,105,110,101,97,114,95,122,95,52,53,0);
         dicey = String.fromCharCode(67,0) == whistleV;
         projecto <<= Math.min(3, Math.abs(parseInt(`${analyticsg}`)));
         analyticsg *= parseFloat(`${projecto ^ 3}`);
         linkA = `${linkA.length << (Math.min(Math.abs(2), 1))}`;
         break;
      }
      while (questh.length > debugr.length) {
          let selectn = 0;
          let commentN = 0;
         debugr += `${debugr.length + 1}`;
         selectn %= Math.max(1, commentN >> (Math.min(3, Math.abs(selectn))));
         commentN %= Math.max(5, selectn + 2);
         break;
      }
      temperatureJ = `${((headerz ? 4 : 4))}`;
      appleB -= parseInt(`${detailsV}`);
      zhuboY = (goalm.length % (Math.max(2, robotoC.length))) > 43;
   while (!whistleR) {
       let sportsK = 2;
      for (let g = 0; g < 2; g++) {
          let agreementQ = String.fromCharCode(115,117,110,112,111,115,95,98,95,51,56,0);
          let mbbido: Array<any> = [687, 624];
          let q_centerJ = 4;
          let connection9 = 4.0;
          let middlewareF = 0.0;
         sportsK /= Math.max(parseInt(`${middlewareF}`) ^ 1, 3);
         agreementQ += `${q_centerJ}`;
         mbbido.push(q_centerJ % (Math.max(1, 10)));
         connection9 -= mbbido.length & q_centerJ;
         middlewareF -= 2;
      }
      let specy = sportsK <= 5371356;
      do {
         sportsK /= Math.max(sportsK, 3);
         if (specy) {
            break;
         }
      } while (((sportsK * 2) >= 5 && 3 >= (sportsK * 2)) && specy);
          let fields: Array<any> = [170, 664, 319];
         sportsK -= sportsK;
         fields = [1];
      mbnativeadvanced7 += ((configa ? 5 : 1) * robotoC.length);
      break;
   }
      mbnativeadvanced7 /= Math.max((2 ^ (headerz ? 2 : 2)), 2);
 },
}: ttSmall) {
  const { colors, textVariants, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const navigation = useNavigation();
  const [showLoading, setShowLoading] = useState(false);
  const userState = useSelector<ttGoal>('userReducer');
  const isVip = ttFast.isVip(userState.user);
  const [totalPage, setTotalPage] = useState(1);
  const [results, setResults] = useState<Array<ttUntick>>([]);

  
  
  //     if ((item?.vod_list?.length ?? 0) === 0) return <></>;

  
  
  
  
  
  
  
  
  
  
  
  
  
  //       >
  
  
  
  
  
  
  
  //         />
  
  
  
  
  
  
  
  
  
  //             />
  
  
  //       </View>
  
  
  
  

  
  function listItem({ item, index }) {
    if ((item?.vod_list?.length ?? 0) === 0) return <></>;

    return (
      <View
        key={`${item.type_name}-${index}`}
        style={{
          gap: spacing.m,
          paddingLeft: spacing.sideOffset,
          paddingRight: spacing.sideOffset,
          backgroundColor: "#0c0c0c",
          borderRadius: 15,
          marginBottom: 15,
          paddingTop: 4,
          paddingBottom: 4,
        }}
      >
        <ShowMoreVodButton
          text={item.type_name.trim()}
          onPress={() => {
            navigation.navigate("午夜场剧情", {
              class: item.type_name,
              vod_source_name: item.vod_source_name
            });
          }}
        />
        {
          item?.vod_list && (
            <VodListVerticalVip
              numOfRows={2}
              vods={item?.vod_list}
              minNumPerRow={2}
              heightToWidthRatio={1 / 1.814}
              playerMode="adult"
            />
          )
        }
      </View>
    );
  }

  const fetchXCategories = (page: number) =>
    ttSinaPrediction.getXVodList(page).then((results: Array<ttUntick>) => {
      setTotalPage(9);
      console.log("HEH");
      return Object.values(results);
    });

  const {
    data: xData,
    isSuccess,
    hasNextPage,
    fetchNextPage,
    isFetchingNextPage,
    isFetching,
    refetch,
  } = useInfiniteQuery(
    ['xvodPlayList'],
    ({pageParam = 1}) => fetchXCategories(pageParam),
    {
      getNextPageParam: (lastPage, allPages) => {
        if (lastPage === null) {
          return undefined;
        }
        const nextPage: any = allPages.length + 1;
        
        if (nextPage > totalPage && totalPage != 0) {
          return undefined;
        }
        return nextPage;
      },
      onSuccess: data => {
        if (data && data?.pages) {
          setResults([...results, ...data.pages[data.pages.length - 1].flat()]);
        }
      },
    },
  );

  useEffect(() => {
    if (results == undefined) {
      setShowLoading(true);
    }
  }, []);

  useEffect(() => {
    if(results.length == 0){
      refetch();
    }
  }, [results])

  return (
    <SafeAreaView style={{ flex: 1 }}>
      {showLoading && (
        <View
          style={{
            position: "absolute",
            
            backgroundColor: "#0c0c0c",
            zIndex: 1,
            width: "100%",
            height: "100%",
            justifyContent: "center",
            alignItems: "center",
            borderRadius: 15,

          }}
        >
          <FastImage
            source={require("@static/images/toponTerms.gif")}
            style={{ width: 100, height: 100 }}
            resizeMode="contain"
          />
        </View>
      )}

      <View style={{ paddingTop: 10 }}>
        <FlatList
          data={results}
          keyExtractor={(item: ttEdit, index: number) => index.toString() + item.type_name}
          initialNumToRender={0}
          windowSize={3}
          maxToRenderPerBatch={3}
          renderItem={listItem}
          contentContainerStyle={{ paddingBottom: 60 }}
          removeClippedSubviews={true}
          onEndReachedThreshold={0.5}
          scrollEnabled={isVip}
          onEndReached={() => {
            if (hasNextPage && !isFetchingNextPage && !isFetching) {
              fetchNextPage();
            }
          }}
          ListFooterComponent={
            <View style={{...styles.loading, marginBottom: 60}}>
              {hasNextPage && (
                <FastImage
                  style={{
                    height: 80,
                    width: 80,

                    flex: 1,
                    justifyContent: 'center',
                    alignItems: 'center',
                  }}
                  source={require('@static/images/toponTerms.gif')}
                  resizeMode={'contain'}
                />
              )}
              {!(isFetchingNextPage || isFetching) && !hasNextPage && (
                <Text
                  style={{
                    ...textVariants.subBody,
                    color: colors.muted,
                    paddingTop: 12,
                  }}>
                  已经到底了
                </Text>
              )}
            </View>
          }
        />
      </View>
      { }
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  loading: {
    flexDirection: "row",
    justifyContent: "center",
    flex: 1,
  },
});
