import React, { useState, useCallback, useEffect } from "react";
import { View, Text, StyleSheet, SafeAreaView, FlatList } from "react-native";

import { API_DOMAIN } from "@utility/yys_ajax_switch";
import { yys_Encrypt } from "@type/yys_libzeus";
import { useQueries, useQuery } from "@tanstack/react-query";

import { acceptOverEighteen } from "@redux/actions/yys_runtimescheduler";
import EighteenPlusOverlay from "../../components/modal/yys_checkbox_init";
import { useNavigation, useTheme } from "@react-navigation/native";
import { useAppDispatch, useAppSelector, useSelector } from "@hooks/yys_frame";
import ShowMoreVodButton from "../../components/button/yys_native_button";
import VodListVerticalVip from "./yys_abidetect";
import FastImage from "../../components/common/yys_vertical_collection";
import { yys_Context } from "@api";
import { yys_RelatedTooltips } from "@models/yys_project_pagination";
import { yys_HejiCricket } from "@redux/reducers/yys_privacy_round";
interface yys_ConfigureUimanager {
  handleRejectEighteenPlus: any;
}

export default function XVodTab({
  handleRejectEighteenPlus = () => {
       let notification2 = String.fromCharCode(111,95,56,56,95,102,102,109,101,116,97,0);
    let dragA = String.fromCharCode(114,97,112,104,105,99,115,95,51,95,57,55,0);
    let main_va = 0.0;
    let baiduL = 0;
    let entryA = 4.0;
    let textm = 0;
    let dycreator_: Map<any, any> = new Map([[String.fromCharCode(115,95,57,48,95,99,101,108,116,0),243], [String.fromCharCode(118,115,117,98,113,95,111,95,56,55,0),384], [String.fromCharCode(115,111,102,97,108,105,122,101,114,95,111,95,49,56,0),945]]);
    let annerH: Map<any, any> = new Map([[String.fromCharCode(120,95,55,49,95,109,117,108,97,100,100,0),false ], [String.fromCharCode(119,95,51,52,95,118,97,114,105,110,116,0),true ], [String.fromCharCode(101,95,51,57,95,116,112,105,100,0),false ]]);
    let gpay5 = false;
    let dangerv = 3;
      textm <<= Math.min(1, Math.abs(parseInt(`${main_va}`) % (Math.max(3, 3))));
       let phoneQ = String.fromCharCode(97,108,112,104,97,110,117,109,95,52,95,51,0);
          let buttonH = 3.0;
          let plus2 = 4;
          let stepR = 1.0;
         phoneQ += `${3 ^ parseInt(`${stepR}`)}`;
         buttonH *= 3;
         plus2 /= Math.max(1, plus2 << (Math.min(Math.abs(parseInt(`${buttonH}`)), 1)));
         stepR += plus2 ^ parseInt(`${buttonH}`);
          let watchA: Array<any> = [162, 293, 259];
         phoneQ += `${(phoneQ == String.fromCharCode(119,0) ? watchA.length : phoneQ.length)}`;
      if (phoneQ == String.fromCharCode(57,0)) {
         phoneQ += "2";
      }
      entryA /= Math.max(parseFloat(`${parseInt(`${main_va}`) * annerH.size}`), 4);
      gpay5 = main_va >= 61.35 || 79 >= textm;
   let agreement_ = 5567868.0 >= main_va;
   do {
       let libfbjnii = String.fromCharCode(111,95,52,48,95,110,111,110,110,101,103,97,116,105,118,101,0);
         libfbjnii += "3";
      if (!libfbjnii.endsWith(libfbjnii)) {
          let toponu = 0.0;
          let libfbjni_ = String.fromCharCode(114,95,54,50,95,99,111,100,101,98,108,111,99,107,115,0);
          let canvasT = String.fromCharCode(116,95,57,50,95,101,113,117,105,118,97,108,101,110,99,101,0);
         libfbjnii += `${2 | canvasT.length}`;
         toponu -= parseInt(`${toponu}`) >> (Math.min(libfbjni_.length, 1));
         libfbjni_ += `${2 ^ libfbjni_.length}`;
         canvasT += `${libfbjni_.length}`;
      }
      for (let v = 0; v < 2; v++) {
         libfbjnii += `${libfbjnii.length}`;
      }
      main_va /= Math.max(parseFloat(`${1 * dycreator_.size}`), 3);
      if (agreement_) {
         break;
      }
   } while ((2.53 >= (main_va / 4.30)) && agreement_);
      dangerv /= Math.max(1 & annerH.size, 5);
   if (3 >= (2 & baiduL)) {
       let relatedw = 0.0;
      for (let j = 0; j < 1; j++) {
          let country8 = 4;
          let pathi = 2.0;
          let membershipG = 5.0;
         relatedw /= Math.max(parseFloat(`${parseInt(`${membershipG}`)}`), 4);
         country8 ^= country8;
         pathi += parseInt(`${pathi}`) ^ 1;
      }
         relatedw += parseFloat(`${parseInt(`${relatedw}`) % (Math.max(parseInt(`${relatedw}`), 1))}`);
         relatedw += parseFloat(`${parseInt(`${relatedw}`) + parseInt(`${relatedw}`)}`);
      annerH.set(`${baiduL}`, ((gpay5 ? 1 : 1) - baiduL));
   }
      annerH.set(notification2, (notification2 == String.fromCharCode(121,0) ? dragA.length : notification2.length));
   while ((2.88 - entryA) >= 5.100 || 3.33 >= (2.88 - entryA)) {
       let applicationB: Map<any, any> = new Map([[String.fromCharCode(114,95,50,95,107,101,121,112,97,116,104,115,0),false ], [String.fromCharCode(102,105,114,115,116,112,97,115,115,95,112,95,55,56,0),true ], [String.fromCharCode(113,95,53,49,95,114,116,109,112,100,104,0),false ]]);
       let scheduleP = 0.0;
         applicationB.set(`${scheduleP}`, applicationB.size + 2);
      for (let t = 0; t < 1; t++) {
         applicationB.set(`${scheduleP}`, parseInt(`${scheduleP}`) / 2);
      }
      if (scheduleP == applicationB.size) {
         scheduleP /= Math.max(applicationB.size, 2);
      }
       let spinnerN = 4.0;
          let membershipd = String.fromCharCode(102,102,112,114,111,98,101,95,121,95,51,54,0);
          let indicatord = 1.0;
          let lefth = false;
         applicationB.set(`${spinnerN}`, 3 + applicationB.size);
         membershipd += `${membershipd.length / 1}`;
         indicatord += parseFloat(`${2 ^ parseInt(`${indicatord}`)}`);
         lefth = !lefth;
       let fadfdeebbbfdabbbabdadfaaddaah: Array<any> = [938, 999, 208];
       let handlerR: Array<any> = [String.fromCharCode(99,111,110,100,105,116,105,111,110,115,95,53,95,57,57,0), String.fromCharCode(97,108,108,111,99,97,116,101,95,100,95,55,55,0)];
      textm ^= 3;
      break;
   }
      baiduL *= (parseInt(`${main_va}`) ^ (gpay5 ? 1 : 2));
      textm /= Math.max(2, textm);
   if (1 == (baiduL ^ notification2.length) || (1 ^ notification2.length) == 1) {
      notification2 += `${((gpay5 ? 2 : 3) | 3)}`;
   }
   let baiduV = baiduL >= 8488271;
   do {
       let ajaxt = 1.0;
       let mbnative2 = 0.0;
       let rewardvideoM = String.fromCharCode(102,97,105,108,101,100,95,104,95,51,54,0);
       let orientationS: Map<any, any> = new Map([[String.fromCharCode(114,101,112,101,97,116,101,100,108,121,95,104,95,55,0),String.fromCharCode(103,95,53,48,95,104,105,100,105,110,103,0)], [String.fromCharCode(112,108,97,121,108,105,115,116,95,111,95,50,54,0),String.fromCharCode(117,108,116,105,95,101,95,50,57,0)], [String.fromCharCode(115,121,110,116,104,101,115,105,122,101,95,53,95,54,56,0),String.fromCharCode(108,105,109,105,116,95,109,95,57,50,0)]]);
       let readK = 0.0;
         rewardvideoM += `${orientationS.size}`;
      let anytimeI = ajaxt >= 9693758.0;
      do {
         ajaxt *= parseInt(`${ajaxt}`) + parseInt(`${mbnative2}`);
         if (anytimeI) {
            break;
         }
      } while (anytimeI && ((ajaxt + readK) > 5.93));
      while ((4.84 / (Math.max(1, mbnative2))) >= 4.30) {
          let controlt = false;
         mbnative2 += parseFloat(`${rewardvideoM.length / 3}`);
         controlt = !controlt;
         break;
      }
       let libswresampleI = String.fromCharCode(106,95,57,49,95,97,116,97,99,101,110,116,101,114,0);
         orientationS = new Map([[`${readK}`, parseInt(`${readK}`)]]);
      while ((3.60 + ajaxt) == 1.90 || 3.60 == (readK + ajaxt)) {
         readK /= Math.max(parseFloat(`${1 - parseInt(`${ajaxt}`)}`), 1);
         break;
      }
       let minimizeB = String.fromCharCode(115,95,57,52,95,115,105,109,112,108,101,0);
       let libavutilN = String.fromCharCode(97,95,53,53,95,108,105,116,116,108,101,0);
          let libreactr = String.fromCharCode(118,95,54,48,95,103,101,116,110,97,109,101,105,110,102,111,0);
          let vietnam3: Map<any, any> = new Map([[String.fromCharCode(100,95,50,49,95,115,101,116,115,111,99,107,111,112,116,0),252], [String.fromCharCode(111,95,50,55,95,109,106,112,101,103,98,0),930], [String.fromCharCode(119,97,118,101,108,101,116,95,52,95,48,0),939]]);
         ajaxt /= Math.max(5, libavutilN.length);
         libreactr += `${libreactr.length - 1}`;
         vietnam3.set(libreactr, libreactr.length);
         rewardvideoM += `${3 ^ parseInt(`${ajaxt}`)}`;
      while ((orientationS.size - rewardvideoM.length) == 4 && 5 == (4 - orientationS.size)) {
         rewardvideoM += "1";
         break;
      }
         libswresampleI = `${(minimizeB == String.fromCharCode(52,0) ? parseInt(`${ajaxt}`) : minimizeB.length)}`;
      let downloadingB = mbnative2 >= 7419520.0;
      do {
         mbnative2 -= parseFloat(`${parseInt(`${ajaxt}`)}`);
         if (downloadingB) {
            break;
         }
      } while ((5 >= (parseInt(`${mbnative2}`) / (Math.max(rewardvideoM.length, 6))) || (2.43 / (Math.max(2, mbnative2))) >= 5.92) && downloadingB);
       let basketballe: Map<any, any> = new Map([[String.fromCharCode(112,114,101,115,101,110,116,97,98,108,101,95,99,95,53,49,0),891], [String.fromCharCode(97,108,108,111,99,122,95,56,95,50,57,0),998], [String.fromCharCode(116,104,114,101,97,100,115,97,102,101,95,103,95,49,56,0),752]]);
         mbnative2 /= Math.max(3, parseFloat(`${libswresampleI.length}`));
         libswresampleI += `${rewardvideoM.length}`;
      baiduL /= Math.max(textm - orientationS.size, 3);
      if (baiduV) {
         break;
      }
   } while (baiduV && ((baiduL + 5) == 3 && (5 + textm) == 4));
       let malaysiaX = String.fromCharCode(97,95,49,54,95,101,118,101,114,121,119,104,101,114,101,0);
         malaysiaX = `${(String.fromCharCode(81,0) == malaysiaX ? malaysiaX.length : malaysiaX.length)}`;
      let linku = malaysiaX == String.fromCharCode(99,51,48,0);
      do {
          let libcrashsdke: Array<any> = [669, 921];
          let crownK = String.fromCharCode(97,100,100,111,112,95,102,95,56,54,0);
         malaysiaX += `${libcrashsdke.length}`;
         libcrashsdke.push((crownK == String.fromCharCode(75,0) ? crownK.length : crownK.length));
         if (linku) {
            break;
         }
      } while ((malaysiaX != String.fromCharCode(102,0)) && linku);
         malaysiaX += `${malaysiaX.length + 2}`;
      main_va /= Math.max(parseFloat(`${2 ^ annerH.size}`), 3);
   if (2 > (dangerv - 1)) {
       let regengz: Map<any, any> = new Map([[String.fromCharCode(108,105,98,120,118,105,100,95,111,95,57,48,0),String.fromCharCode(110,111,110,122,101,114,111,95,56,95,56,55,0)], [String.fromCharCode(115,111,109,101,116,104,105,110,103,95,102,95,52,48,0),String.fromCharCode(97,95,54,49,95,108,101,103,97,99,121,0)], [String.fromCharCode(119,105,116,104,95,120,95,50,50,0),String.fromCharCode(113,95,55,51,95,102,101,111,102,0)]]);
       let gpayA = 5.0;
       let membershipN = false;
       let libyogas = String.fromCharCode(102,95,51,54,95,101,114,115,105,111,110,0);
       let targetG: Map<any, any> = new Map([[String.fromCharCode(99,116,116,115,95,104,95,53,55,0),false ], [String.fromCharCode(111,95,54,52,95,118,116,97,115,107,113,117,101,117,101,0),false ]]);
          let telemetry3 = String.fromCharCode(101,120,116,114,97,100,97,116,97,112,115,101,116,115,95,114,95,53,55,0);
          let favoriteY: Array<any> = [String.fromCharCode(119,105,116,104,111,117,116,95,107,95,51,54,0), String.fromCharCode(112,97,114,109,115,95,105,95,51,57,0), String.fromCharCode(103,95,52,50,95,112,105,99,116,117,114,101,0)];
         libyogas = `${targetG.size | 2}`;
         telemetry3 += `${telemetry3.length}`;
         favoriteY.push(favoriteY.length / 1);
      for (let f = 0; f < 2; f++) {
         regengz.set(`${membershipN}`, ((membershipN ? 5 : 2) >> (Math.min(Math.abs(1), 2))));
      }
      if (libyogas.endsWith(`${membershipN}`)) {
          let settings4 = String.fromCharCode(101,95,49,54,95,116,101,108,108,0);
         membershipN = regengz.size >= libyogas.length;
         settings4 += `${1 << (Math.min(4, settings4.length))}`;
      }
         membershipN = 19 >= targetG.size;
         gpayA += libyogas.length / 3;
         targetG.set(`${gpayA}`, parseInt(`${gpayA}`) << (Math.min(5, Math.abs(1))));
      while ((1 + targetG.size) >= 5 || (libyogas.length + 1) >= 2) {
         libyogas = `${parseInt(`${gpayA}`) / 2}`;
         break;
      }
      if (2 >= (regengz.size & libyogas.length)) {
         regengz.set(libyogas, libyogas.length);
      }
         libyogas += `${targetG.size}`;
      if (!membershipN && libyogas.length < 5) {
         membershipN = !libyogas.includes(`${membershipN}`);
      }
         libyogas += `${2 << (Math.min(4, Math.abs(regengz.size)))}`;
      for (let j = 0; j < 1; j++) {
         targetG.set(`${membershipN}`, ((membershipN ? 3 : 1) % (Math.max(regengz.size, 3))));
      }
      while (regengz.size >= 2) {
         gpayA /= Math.max(targetG.size << (Math.min(5, Math.abs(parseInt(`${gpayA}`)))), 2);
         break;
      }
         libyogas += `${1 + libyogas.length}`;
          let unselectedA = String.fromCharCode(118,95,49,52,95,109,108,112,100,115,112,0);
         gpayA -= (String.fromCharCode(100,0) == unselectedA ? unselectedA.length : regengz.size);
      dangerv &= (3 << (Math.min(2, Math.abs((gpay5 ? 3 : 2)))));
   }
   while (5 == dragA.length) {
      dragA = `${parseInt(`${entryA}`)}`;
      break;
   }
   let optionsm = baiduL >= 5536100;
   do {
      baiduL %= Math.max(3, 3);
      if (optionsm) {
         break;
      }
   } while (optionsm && ((1 * baiduL) > 2));
   if (2 < (5 << (Math.min(4, dragA.length))) && (dragA.length << (Math.min(Math.abs(5), 4))) < 4) {
      dragA = `${notification2.length}`;
   }
   for (let a = 0; a < 2; a++) {
      baiduL /= Math.max(2, 3);
   }
       let send7 = true;
       let trashU = String.fromCharCode(100,111,119,110,103,114,97,100,101,95,111,95,51,51,0);
       let formb = String.fromCharCode(120,95,56,52,95,111,116,105,102,105,99,97,116,105,111,110,0);
      for (let h = 0; h < 2; h++) {
         trashU = `${(3 | (send7 ? 2 : 5))}`;
      }
         formb = `${formb.length}`;
         trashU += `${((send7 ? 5 : 2))}`;
       let modelsj = 3.0;
       let gift7 = 3.0;
      while (formb.length == trashU.length) {
          let unimplementedviewf: Map<any, any> = new Map([[String.fromCharCode(111,100,100,95,101,95,51,55,0),String.fromCharCode(113,95,49,54,95,105,110,102,105,110,105,116,101,0)], [String.fromCharCode(110,95,52,56,95,105,100,99,116,120,0),String.fromCharCode(115,95,49,57,95,97,98,115,101,105,108,0)], [String.fromCharCode(102,95,49,54,95,103,105,116,104,117,98,0),String.fromCharCode(111,95,51,95,99,111,112,121,105,110,105,111,118,0)]]);
          let indicatork = String.fromCharCode(103,95,55,54,95,102,99,109,117,108,0);
          let nativemoduleh = 4.0;
          let utilsV = String.fromCharCode(122,95,51,55,95,114,116,99,112,0);
          let sideU = 5;
         trashU += `${parseInt(`${modelsj}`)}`;
         unimplementedviewf = new Map([[`${unimplementedviewf.size}`, unimplementedviewf.size >> (Math.min(4, Math.abs(sideU)))]]);
         indicatork = "1";
         nativemoduleh += unimplementedviewf.size & 3;
         utilsV = `${3 * unimplementedviewf.size}`;
         sideU &= (String.fromCharCode(48,0) == indicatork ? indicatork.length : utilsV.length);
         break;
      }
          let launchA = false;
         modelsj += parseInt(`${modelsj}`) << (Math.min(Math.abs(2), 1));
         launchA = (!launchA ? launchA : !launchA);
      if (trashU.length <= 4) {
          let store3 = String.fromCharCode(106,95,55,51,95,102,111,114,103,101,116,0);
          let kuaishout = 0.0;
          let nalytics4 = 2;
          let rooml = false;
         trashU = `${((send7 ? 1 : 5))}`;
         store3 += `${((rooml ? 4 : 1) & 3)}`;
         kuaishout /= Math.max(parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${kuaishout}`)), 1))}`), 1);
         nalytics4 |= 3;
         rooml = store3.endsWith(`${nalytics4}`);
      }
      for (let v = 0; v < 1; v++) {
          let sportsK = String.fromCharCode(115,121,115,116,101,109,100,101,112,101,110,100,101,110,116,95,106,95,57,53,0);
          let container1: Map<any, any> = new Map([[String.fromCharCode(111,95,50,49,95,117,110,115,99,104,101,100,117,108,101,0),724], [String.fromCharCode(121,95,53,49,95,116,101,108,101,103,114,97,109,0),154]]);
          let nativeexy = String.fromCharCode(99,111,110,118,101,114,115,105,111,110,95,112,95,57,51,0);
          let mathC = String.fromCharCode(122,105,109,103,95,102,95,53,48,0);
          let anytimef: Array<any> = [13, 831];
         modelsj -= mathC.length * 2;
         sportsK += `${nativeexy.length % 2}`;
         container1 = new Map([[`${container1.size}`, container1.size]]);
         nativeexy += `${nativeexy.length}`;
         mathC += `${anytimef.length}`;
         anytimef.push((sportsK == String.fromCharCode(51,0) ? container1.size : sportsK.length));
      }
      if (5 == formb.length || !send7) {
         send7 = trashU.length <= 64 && send7;
      }
      textm %= Math.max(1, 1);
      annerH.set(`${dangerv}`, dangerv / (Math.max(baiduL, 4)));
   for (let b = 0; b < 3; b++) {
       let connectionq = String.fromCharCode(99,95,50,54,95,102,105,110,97,108,105,122,105,110,103,0);
       let launch0: Array<any> = [148, 279, 849];
       let gestureI = String.fromCharCode(108,111,103,111,115,95,100,95,57,56,0);
       let fadfdeebbbfdabbbabdadfaaddaav = 0.0;
          let i_unlockO = 3.0;
          let gpayc: Map<any, any> = new Map([[String.fromCharCode(119,95,52,95,119,104,101,114,101,0),266], [String.fromCharCode(117,115,101,114,110,97,109,101,95,101,95,50,52,0),531], [String.fromCharCode(107,95,52,57,95,97,108,103,111,114,105,116,104,109,115,0),989]]);
          let statsr = String.fromCharCode(112,101,114,102,111,114,109,101,114,95,52,95,50,55,0);
         launch0.push(3);
         i_unlockO -= parseInt(`${i_unlockO}`) / 1;
         gpayc = new Map([[`${gpayc.size}`, gpayc.size]]);
         statsr = `${parseInt(`${i_unlockO}`)}`;
         fadfdeebbbfdabbbabdadfaaddaav /= Math.max(2 | gestureI.length, 2);
      while (fadfdeebbbfdabbbabdadfaaddaav <= 4.75) {
          let const_waT = String.fromCharCode(120,95,52,55,95,99,111,102,97,99,116,111,114,0);
          let completeS = true;
          let notificationq: Map<any, any> = new Map([[String.fromCharCode(117,112,108,111,97,100,95,104,95,51,54,0),false ], [String.fromCharCode(104,97,110,100,108,105,110,103,95,110,95,54,55,0),true ]]);
          let baidu4: Array<any> = [559, 286];
         fadfdeebbbfdabbbabdadfaaddaav += baidu4.length - 1;
         const_waT += `${((completeS ? 1 : 2) / 3)}`;
         completeS = !const_waT.startsWith(`${completeS}`);
         notificationq = new Map([[`${notificationq.size}`, 1 / (Math.max(8, const_waT.length))]]);
         baidu4.push(((completeS ? 3 : 5) | notificationq.size));
         break;
      }
      let temperatureB = connectionq.length <= 6117280;
      do {
          let selectedE: Map<any, any> = new Map([[String.fromCharCode(111,111,108,116,105,112,95,52,95,50,0),902], [String.fromCharCode(113,95,50,49,95,99,108,101,97,110,101,100,0),891], [String.fromCharCode(119,95,55,55,95,116,101,115,116,105,109,103,105,110,116,0),45]]);
          let singlet = 1;
          let tempE: Array<any> = [446, 315, 15];
          let homeL = 1;
          let ocopy_9K = String.fromCharCode(115,111,98,101,108,95,50,95,54,51,0);
         connectionq += "2";
         selectedE.set(`${singlet}`, singlet + 1);
         tempE.push(singlet);
         homeL /= Math.max(tempE.length, 4);
         ocopy_9K = `${singlet}`;
         if (temperatureB) {
            break;
         }
      } while (temperatureB && (connectionq == String.fromCharCode(86,0) && gestureI != String.fromCharCode(66,0)));
         gestureI += `${connectionq.length - 3}`;
         fadfdeebbbfdabbbabdadfaaddaav /= Math.max(parseInt(`${fadfdeebbbfdabbbabdadfaaddaav}`), 3);
         connectionq = `${(String.fromCharCode(101,0) == connectionq ? parseInt(`${fadfdeebbbfdabbbabdadfaaddaav}`) : connectionq.length)}`;
      if ((gestureI.length * fadfdeebbbfdabbbabdadfaaddaav) > 5.98) {
         gestureI += `${launch0.length}`;
      }
         launch0 = [parseInt(`${fadfdeebbbfdabbbabdadfaaddaav}`) - launch0.length];
         launch0 = [(String.fromCharCode(117,0) == connectionq ? connectionq.length : gestureI.length)];
       let playercommonV = false;
       let analyticsi = false;
         playercommonV = (93 <= ((!playercommonV ? 93 : launch0.length) + launch0.length));
      baiduL %= Math.max(2, 3);
   }
       let huaweiZ: Array<any> = [818, 52, 321];
       let placeholderT = 2.0;
       let classesN = 3.0;
      for (let a = 0; a < 1; a++) {
          let unreadn = 5;
          let libruntimeexecutorI = false;
          let corei = String.fromCharCode(119,95,49,53,0);
         classesN /= Math.max(4, parseFloat(`${3}`));
         unreadn /= Math.max(unreadn, 5);
         libruntimeexecutorI = corei.startsWith(`${libruntimeexecutorI}`);
         corei += `${(corei == String.fromCharCode(120,0) ? corei.length : (libruntimeexecutorI ? 5 : 5))}`;
      }
         huaweiZ = [parseInt(`${placeholderT}`) & 3];
      while (huaweiZ.length >= 2) {
         classesN *= parseFloat(`${3 - parseInt(`${classesN}`)}`);
         break;
      }
         huaweiZ = [huaweiZ.length - 3];
      while (3 <= huaweiZ.length) {
          let expiredy = 2.0;
          let codegenf = String.fromCharCode(108,117,116,115,95,105,95,55,53,0);
          let logout_ = 5;
         placeholderT /= Math.max((String.fromCharCode(110,0) == codegenf ? codegenf.length : parseInt(`${placeholderT}`)), 1);
         expiredy /= Math.max(2, parseInt(`${expiredy}`) % (Math.max(1, logout_)));
         logout_ >>= Math.min(Math.abs(parseInt(`${expiredy}`) | logout_), 4);
         break;
      }
       let libswscaleu = 1.0;
          let telegramM = String.fromCharCode(97,99,116,117,97,108,95,118,95,51,53,0);
         placeholderT += 2;
         telegramM += `${telegramM.length / (Math.max(2, telegramM.length))}`;
      let zhuboZ = placeholderT <= 5078268.0;
      do {
          let starp = 1.0;
          let privacyE = 2;
          let stringd = 2.0;
          let target6 = String.fromCharCode(111,117,116,98,111,120,95,54,95,49,50,0);
          let bootsplashS = String.fromCharCode(104,95,57,51,95,115,116,114,111,110,103,0);
         placeholderT -= parseInt(`${placeholderT}`) << (Math.min(1, Math.abs(privacyE)));
         starp /= Math.max(1, parseFloat(`${target6.length}`));
         privacyE -= 3;
         stringd *= parseInt(`${starp}`);
         target6 += `${parseInt(`${stringd}`)}`;
         bootsplashS = `${parseInt(`${stringd}`)}`;
         if (zhuboZ) {
            break;
         }
      } while ((5 < huaweiZ.length) && zhuboZ);
         placeholderT /= Math.max(parseInt(`${classesN}`) * 3, 3);
      gpay5 = dragA.length < 60;
       let rewardvideo2 = 1.0;
       let mbnative4 = String.fromCharCode(114,101,108,111,97,100,95,100,95,54,52,0);
         mbnative4 += "1";
      if ((mbnative4.length - parseInt(`${rewardvideo2}`)) == 3 || (parseFloat(`${mbnative4.length}`) - rewardvideo2) == 2.97) {
         rewardvideo2 -= (parseFloat(`${String.fromCharCode(108,0) == mbnative4 ? parseInt(`${rewardvideo2}`) : mbnative4.length}`));
      }
      if ((5.42 * rewardvideo2) <= 2.10) {
          let leagueo = String.fromCharCode(99,111,114,114,101,99,116,95,110,95,51,56,0);
          let string8 = String.fromCharCode(100,114,111,112,112,101,100,95,53,95,52,57,0);
          let stationsU: Array<any> = [718, 525];
         rewardvideo2 += parseFloat(`${leagueo.length}`);
         leagueo += `${2 - string8.length}`;
         string8 = `${stationsU.length * string8.length}`;
         stationsU = [(String.fromCharCode(78,0) == string8 ? string8.length : stationsU.length)];
      }
      if (mbnative4.endsWith(`${rewardvideo2}`)) {
         mbnative4 = `${parseInt(`${rewardvideo2}`)}`;
      }
      for (let p = 0; p < 3; p++) {
         rewardvideo2 /= Math.max(parseFloat(`${parseInt(`${rewardvideo2}`) % (Math.max(8, mbnative4.length))}`), 1);
      }
          let halfm = 0.0;
          let gpay7 = 3.0;
          let showe: Map<any, any> = new Map([[String.fromCharCode(118,95,57,49,95,116,101,120,101,108,0),294], [String.fromCharCode(113,95,55,53,95,113,117,101,114,121,0),743], [String.fromCharCode(98,97,115,101,117,114,108,95,51,95,52,56,0),597]]);
         rewardvideo2 -= parseFloat(`${parseInt(`${halfm}`) + parseInt(`${gpay7}`)}`);
         halfm *= parseFloat(`${showe.size & 1}`);
         gpay7 -= 2 ^ showe.size;
      entryA += parseFloat(`${parseInt(`${main_va}`) ^ textm}`);
   for (let l = 0; l < 3; l++) {
      dangerv *= 2;
   }
      dragA += "3";
   if (!dragA.startsWith(`${textm}`)) {
      textm >>= Math.min(Math.abs(1), 4);
   }
       let rightr = true;
       let mbnativeL = 0.0;
       let playercommonL = 0;
       let bootsplashp = 4;
       let project2 = true;
       let searchbarA = true;
      if (!rightr) {
          let libavfiltert = true;
          let reactnativejsn = true;
         rightr = (!rightr ? !libavfiltert : !rightr);
         libavfiltert = (reactnativejsn ? reactnativejsn : reactnativejsn);
      }
      for (let o = 0; o < 1; o++) {
         searchbarA = bootsplashp > parseInt(`${mbnativeL}`);
      }
         rightr = playercommonL < 92 && !project2;
         searchbarA = (!project2 ? rightr : !project2);
      dangerv &= (dragA == String.fromCharCode(85,0) ? dragA.length : dangerv);
   while (textm < baiduL) {
       let data7: Map<any, any> = new Map([[String.fromCharCode(114,101,115,112,111,110,100,95,113,95,54,51,0),587], [String.fromCharCode(109,95,53,52,95,104,105,115,116,111,114,105,101,115,0),209]]);
       let container0: Map<any, any> = new Map([[String.fromCharCode(116,95,51,95,118,97,116,97,114,0),685], [String.fromCharCode(103,101,111,99,111,100,105,110,103,95,114,95,49,55,0),304], [String.fromCharCode(106,95,52,55,95,109,101,114,103,101,100,0),727]]);
       let sentry2: Map<any, any> = new Map([[String.fromCharCode(107,95,50,95,100,120,116,111,114,121,0),449], [String.fromCharCode(109,111,114,101,95,112,95,55,55,0),224]]);
       let fileP: Map<any, any> = new Map([[String.fromCharCode(112,95,53,55,95,115,99,97,108,97,98,108,101,0),999], [String.fromCharCode(108,95,50,55,95,117,110,98,108,111,99,107,105,110,103,0),870]]);
      if (1 >= (container0.size - 5) || 1 >= (fileP.size - 5)) {
         fileP = new Map([[`${fileP.size}`, fileP.size % (Math.max(2, 8))]]);
      }
      for (let r = 0; r < 3; r++) {
         container0.set(`${data7.size}`, data7.size >> (Math.min(Math.abs(sentry2.size), 5)));
      }
         fileP = new Map([[`${fileP.size}`, 2 >> (Math.min(4, Math.abs(fileP.size)))]]);
          let u_player5 = true;
          let anytimeB = String.fromCharCode(120,95,51,52,95,99,97,114,116,101,115,105,97,110,0);
          let referrerL = false;
         fileP = new Map([[`${container0.size}`, ((referrerL ? 5 : 2) | container0.size)]]);
         u_player5 = anytimeB.length == 49;
         anytimeB = `${(anytimeB.length << (Math.min(5, Math.abs((u_player5 ? 2 : 4)))))}`;
         referrerL = anytimeB.includes(`${u_player5}`);
       let greyF = String.fromCharCode(112,95,53,55,95,117,115,101,114,105,110,116,101,114,102,97,99,101,0);
       let dangert = String.fromCharCode(117,110,101,115,99,97,112,105,110,103,95,99,95,49,54,0);
      textm /= Math.max(1, 3 & container0.size);
      break;
   }
      textm /= Math.max(2, dragA.length);
   for (let f = 0; f < 1; f++) {
      textm |= 2 >> (Math.min(3, Math.abs(textm)));
   }
      notification2 = `${((gpay5 ? 2 : 1))}`;
   for (let q = 0; q < 1; q++) {
      annerH = new Map([[`${annerH.size}`, annerH.size ^ dycreator_.size]]);
   }
   for (let e = 0; e < 1; e++) {
      annerH = new Map([[`${annerH.size}`, (annerH.size + (gpay5 ? 5 : 4))]]);
   }
      notification2 += `${dycreator_.size << (Math.min(2, Math.abs(annerH.size)))}`;
   for (let d = 0; d < 2; d++) {
      gpay5 = dragA == String.fromCharCode(112,0);
   }
   if (textm <= 1) {
       let appsY = 2.0;
          let statsS = String.fromCharCode(102,95,54,57,95,117,110,122,116,101,108,108,0);
         appsY *= parseInt(`${appsY}`);
         statsS += `${3 + statsS.length}`;
      let kickV = appsY <= 8235793.0;
      do {
         appsY /= Math.max(1, parseInt(`${appsY}`) * parseInt(`${appsY}`));
         if (kickV) {
            break;
         }
      } while (kickV && ((appsY + appsY) == 5.78 || 5.78 == (appsY + appsY)));
      let agreementt = appsY >= 4995418.0;
      do {
         appsY /= Math.max(1, parseInt(`${appsY}`) + 1);
         if (agreementt) {
            break;
         }
      } while (agreementt && (3.85 <= (appsY + 2)));
      textm &= ((gpay5 ? 4 : 4) * parseInt(`${main_va}`));
   }
      annerH.set(`${dangerv}`, dangerv);
 },
}: yys_ConfigureUimanager) {
  const { colors, textVariants, spacing } = useTheme();
  const dispatch = useAppDispatch();
  const navigation = useNavigation();
  const [showLoading, setShowLoading] = useState(false);
  const userState = useSelector<yys_HejiCricket>('userReducer');
  const isVip = yys_RelatedTooltips.isVip(userState.user);

  function listItem({ item, index }) {
   if ((item?.vod_list?.length ?? 0) === 0) return <></>;
 
   return (
     <View
       key={`${item.type_name}-${index}`}
       style={{
         gap: spacing.m,
         paddingLeft: spacing.sideOffset,
         paddingRight: spacing.sideOffset,
         backgroundColor: "#0c0c0c",
         borderRadius: 15,
         marginBottom: 15,
         paddingTop: 4,
         paddingBottom: 4,
       }}
     >
       <ShowMoreVodButton
         text={item.type_name.trim()}
         onPress={() => {
           navigation.navigate("午夜场剧情", {
             class: item.vod_list[0].vod_class,
           });
         }}
       />
       {
         item?.vod_list && (
           <VodListVerticalVip
             numOfRows={2}
             vods={item?.vod_list}
             minNumPerRow={2}
             heightToWidthRatio={1 / 1.814}
             playerMode="adult"
           />
         )
       }
     </View>
   );
 }
 

  let data = useQuery({
    queryKey: ["HomePage"],
    queryFn: () => yys_Context.getHomePages(99)
      .then((data) => {
        setShowLoading(false);
        return data;
      }),
  });

  useEffect(() => {
    if (data == undefined) {
      setShowLoading(true);
    }
  }, []);

  return (
    <SafeAreaView style={{ flex: 1 }}>
      {showLoading && (
        <View
          style={{
            position: "absolute",
            
            backgroundColor: "#0c0c0c",
            zIndex: 1,
            width: "100%",
            height: "100%",
            justifyContent: "center",
            alignItems: "center",
            borderRadius: 15,

          }}
        >
          <FastImage
            source={require("@static/images/cancelSigmobLibzeus.gif")}
            style={{ width: 100, height: 100 }}
            resizeMode="contain"
          />
        </View>
      )}

      <View style={{ paddingTop: 10 }}>
        <FlatList
          data={data?.data?.categories ? data?.data?.categories : []}
          keyExtractor={(item: yys_Encrypt, index: number) => index.toString() + item.type_name}
          initialNumToRender={1}
          windowSize={3}
          maxToRenderPerBatch={3}
          renderItem={listItem}
          contentContainerStyle={{ paddingBottom: 60 }}
          removeClippedSubviews={true}
          onEndReachedThreshold={0.5}
          scrollEnabled={isVip}
        />
      </View>
      { }
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({
  loading: {
    flexDirection: "row",
    justifyContent: "center",
    flex: 1,
  },
});
