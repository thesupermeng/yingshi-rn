import React, { memo, useCallback, useEffect } from 'react';
import { View, Text, StyleSheet } from 'react-native';
import ScreenContainer from '../../components/container/tt_backward';
import { useTheme } from '@react-navigation/native';
import { useAppSelector } from '@hooks/tt_spec_download';
import { ttOrange } from '@redux/tt_updates_bottom';
import TitleWithBackButtonHeader from '../../components/header/tt_typing_tail_header';
import FavoritePlaylistButton from '../../components/button/tt_quest_active_button';
import { RootStackScreenProps } from '@type/tt_temperature';
import VodWithDescriptionList from '../../components/vod/tt_carousel_circle';
import tt_humidity_guide from '../../../Umeng/tt_humidity_guide';

function PlaylistDetail({ navigation }: RootStackScreenProps<'PlaylistDetail'>) {
  const { textVariants, colors, spacing } = useTheme();
  const playlistReducer = useAppSelector(
    ({ vodPlaylistReducer }: ttOrange) => vodPlaylistReducer,
  );
  const playlist = playlistReducer?.playlistDetails?.playlist;

  
  useEffect(() => {
    if (playlist !== null) {
      tt_humidity_guide.playlistTopicsViewsAnalytics({
        topic_id: playlist.topic_id.toString(),
        topic_name: playlist.topic_name,
      });
    }
  }, []);

  const onClickCatalogVideo = useCallback(() => {
       let chinasameU = 2.0;
    let sell2 = String.fromCharCode(99,95,57,57,95,114,101,110,100,101,114,0);
    let settingh = 3;
    let w_manager_: Array<any> = [562, 158, 260];
    let style3: Map<any, any> = new Map([[String.fromCharCode(122,95,50,56,95,114,108,111,116,116,105,101,99,111,109,109,111,110,0),false ], [String.fromCharCode(101,95,49,53,95,107,108,97,121,116,110,0),false ], [String.fromCharCode(101,105,110,116,114,95,103,95,53,53,0),true ]]);
    let modalG = true;
    let fastR: Map<any, any> = new Map([[String.fromCharCode(100,95,57,56,95,108,105,102,101,116,105,109,101,0),420], [String.fromCharCode(97,95,52,50,95,112,114,111,112,111,115,101,100,0),332], [String.fromCharCode(115,117,112,112,114,101,115,115,105,110,103,95,112,95,49,54,0),2]]);
    let r_managerI = true;
    let country4 = true;
    let movies5 = 5.0;
   let hejin = 5823386 <= sell2.length;
   do {
      sell2 = `${parseInt(`${movies5}`) & 3}`;
      if (hejin) {
         break;
      }
   } while (hejin && (5 <= sell2.length));
   while ((1 << (Math.min(5, sell2.length))) >= 1 || (style3.size << (Math.min(Math.abs(1), 3))) >= 5) {
       let klevinX = String.fromCharCode(111,95,53,51,95,119,105,100,101,102,101,108,101,109,0);
       let tickedH = 1.0;
       let specb = 4.0;
       let common4 = 2;
         specb -= parseInt(`${tickedH}`) & 2;
         tickedH += parseInt(`${tickedH}`) & parseInt(`${specb}`);
      while ((tickedH - 2.97) <= 2.73) {
          let info8 = String.fromCharCode(104,95,57,50,95,118,99,116,101,115,116,0);
          let miniX: Array<any> = [166, 198];
          let minimizel = 3;
          let robotoF = 4.0;
          let promotion7 = String.fromCharCode(122,95,51,50,95,111,108,100,0);
         tickedH *= (String.fromCharCode(71,0) == klevinX ? klevinX.length : parseInt(`${robotoF}`));
         info8 += `${info8.length}`;
         miniX = [promotion7.length / (Math.max(3, 5))];
         minimizel *= minimizel;
         robotoF *= parseFloat(`${1}`);
         promotion7 += "2";
         break;
      }
      for (let d = 0; d < 1; d++) {
          let forward_ = 0;
          let read9: Array<any> = [548, 422];
          let analyticm = String.fromCharCode(114,101,112,101,97,116,95,122,95,57,48,0);
          let downloadedf = String.fromCharCode(120,95,54,55,95,109,109,115,104,0);
          let related9 = true;
         specb *= (String.fromCharCode(108,0) == downloadedf ? downloadedf.length : analyticm.length);
         forward_ *= 1;
         read9 = [forward_];
         analyticm = `${1 << (Math.min(Math.abs(forward_), 3))}`;
         related9 = !related9;
      }
          let evento = true;
          let pagei = 3.0;
         specb -= klevinX.length * 3;
         evento = !evento;
         pagei -= parseFloat(`${1 - parseInt(`${pagei}`)}`);
          let annerY = String.fromCharCode(115,95,49,54,95,114,97,116,101,99,116,114,108,0);
          let usernameO = 2.0;
         tickedH -= parseInt(`${specb}`) >> (Math.min(Math.abs(3), 4));
         annerY = `${(String.fromCharCode(75,0) == annerY ? parseInt(`${usernameO}`) : annerY.length)}`;
         usernameO *= (annerY == String.fromCharCode(115,0) ? annerY.length : parseInt(`${usernameO}`));
      let reminderX = String.fromCharCode(104,102,55,118,101,0) == klevinX;
      do {
         klevinX = `${common4 ^ parseInt(`${specb}`)}`;
         if (reminderX) {
            break;
         }
      } while (((tickedH - klevinX.length) >= 5.90) && reminderX);
      for (let g = 0; g < 2; g++) {
         klevinX += `${parseInt(`${tickedH}`)}`;
      }
      if ((klevinX.length << (Math.min(Math.abs(3), 2))) <= 1 || 1 <= (3 - parseInt(`${tickedH}`))) {
          let y_imageM = 4;
          let round3 = String.fromCharCode(109,111,114,101,95,105,95,56,48,0);
         tickedH /= Math.max(5, 2);
         y_imageM -= round3.length;
         round3 = `${y_imageM * 2}`;
      }
      for (let k = 0; k < 3; k++) {
          let largew = String.fromCharCode(109,100,110,115,95,50,95,50,54,0);
          let selected7 = String.fromCharCode(112,95,57,51,95,115,111,97,99,99,101,112,116,0);
          let resend7 = 2;
          let backgroundE: Map<any, any> = new Map([[String.fromCharCode(105,95,57,95,113,112,105,115,0),973], [String.fromCharCode(107,112,115,95,103,95,49,52,0),796], [String.fromCharCode(115,95,57,56,95,102,97,109,105,108,121,0),856]]);
         specb -= 1 * klevinX.length;
         largew = `${resend7}`;
         selected7 += `${backgroundE.size}`;
         resend7 <<= Math.min(Math.abs(resend7), 3);
         backgroundE.set(largew, 2);
      }
         specb += 3;
          let default_i1: Array<any> = [531, 599, 696];
          let calendarU = String.fromCharCode(99,104,97,114,103,101,95,103,95,56,49,0);
         specb /= Math.max(5, parseInt(`${specb}`) + parseInt(`${tickedH}`));
         default_i1.push(calendarU.length * default_i1.length);
         calendarU = `${calendarU.length | default_i1.length}`;
      sell2 = "2";
      break;
   }
   while (!modalG) {
      country4 = String.fromCharCode(71,0) == sell2;
      break;
   }
       let delegate_vh_ = 0.0;
       let pause8 = String.fromCharCode(102,95,57,56,95,116,104,114,101,97,100,0);
       let storen = 2;
       let gmailx = String.fromCharCode(110,95,51,54,95,115,101,115,115,105,111,110,115,0);
       let anews8 = String.fromCharCode(100,121,110,97,114,114,97,121,95,119,95,56,0);
      let tumbnaila = delegate_vh_ <= 9383583.0;
      do {
         delegate_vh_ /= Math.max(parseFloat(`${storen % (Math.max(2, 5))}`), 1);
         if (tumbnaila) {
            break;
         }
      } while (tumbnaila && ((parseInt(`${delegate_vh_}`) / (Math.max(anews8.length, 9))) == 3 || 1.63 == (1.39 / (Math.max(10, delegate_vh_)))));
         anews8 += "3";
         delegate_vh_ *= parseFloat(`${3 ^ storen}`);
      while (4 == (1 + storen) || 1 == (storen + anews8.length)) {
         storen <<= Math.min(Math.abs((gmailx == String.fromCharCode(49,0) ? parseInt(`${delegate_vh_}`) : gmailx.length)), 5);
         break;
      }
      while (3 < (pause8.length + parseInt(`${delegate_vh_}`)) || 5 < (3 * pause8.length)) {
         pause8 += `${storen % (Math.max(pause8.length, 3))}`;
         break;
      }
      if (anews8.length <= 2) {
         pause8 = `${1 << (Math.min(5, pause8.length))}`;
      }
      while (storen > 3) {
         storen += pause8.length % (Math.max(3, 9));
         break;
      }
         pause8 += "1";
      chinasameU += parseFloat(`${3}`);
   for (let s = 0; s < 3; s++) {
       let acceptedF = 3;
       let renewK: Array<any> = [String.fromCharCode(101,120,116,114,97,100,97,116,97,95,122,95,53,53,0), String.fromCharCode(100,105,102,102,101,114,101,110,116,95,102,95,54,52,0)];
       let skipo = String.fromCharCode(98,95,51,51,95,102,114,111,109,98,121,116,101,97,114,114,97,121,0);
       let orientationZ = String.fromCharCode(119,105,114,101,108,101,115,115,95,116,95,53,55,0);
          let roundA: Map<any, any> = new Map([[String.fromCharCode(113,95,52,50,95,112,105,120,109,97,112,0),434], [String.fromCharCode(119,95,49,49,95,114,101,118,105,111,117,115,0),983]]);
         renewK = [renewK.length | 3];
         roundA = new Map([[`${roundA.size}`, roundA.size | 3]]);
          let bootsplashK = String.fromCharCode(108,95,49,48,95,104,105,115,116,111,114,105,101,115,0);
          let datak = 1.0;
          let context1 = String.fromCharCode(114,95,50,53,95,98,111,114,100,101,114,108,101,115,115,0);
         skipo = `${skipo.length - 2}`;
         bootsplashK += "1";
         datak -= (parseFloat(`${context1 == String.fromCharCode(95,0) ? context1.length : parseInt(`${datak}`)}`));
          let rewardi: Map<any, any> = new Map([[String.fromCharCode(113,95,51,52,95,112,114,101,97,108,108,111,99,97,116,101,0),176], [String.fromCharCode(99,117,114,108,95,105,95,53,55,0),376]]);
          let crossc = false;
          let taiwanB: Map<any, any> = new Map([[String.fromCharCode(114,95,49,51,95,115,117,98,116,114,97,99,116,105,110,103,0),false ], [String.fromCharCode(101,110,99,95,56,95,55,48,0),false ], [String.fromCharCode(106,95,50,51,95,115,104,117,102,102,108,101,112,108,97,110,101,115,0),false ]]);
         renewK.push(rewardi.size);
         rewardi = new Map([[`${taiwanB.size}`, 2 * taiwanB.size]]);
         crossc = 35 <= taiwanB.size;
      while (renewK.includes(acceptedF)) {
         acceptedF *= (String.fromCharCode(48,0) == orientationZ ? orientationZ.length : renewK.length);
         break;
      }
         renewK.push(orientationZ.length % (Math.max(skipo.length, 5)));
      while (4 <= skipo.length) {
         skipo = `${orientationZ.length * 1}`;
         break;
      }
          let shareR = 5.0;
          let sellf = false;
         orientationZ += "2";
         shareR *= parseInt(`${shareR}`) + 3;
         sellf = !sellf;
         renewK.push(1 & acceptedF);
          let bannera = 4.0;
          let analyticsq: Map<any, any> = new Map([[String.fromCharCode(98,110,98,105,110,112,97,100,95,50,95,49,50,0),76], [String.fromCharCode(119,95,49,48,95,102,105,120,115,116,114,105,100,101,0),154], [String.fromCharCode(115,116,114,115,116,97,114,116,95,108,95,57,56,0),661]]);
          let searchj = String.fromCharCode(103,101,116,109,97,120,114,115,115,95,54,95,57,51,0);
         acceptedF *= renewK.length / (Math.max(3, 1));
         bannera -= 3;
         analyticsq = new Map([[`${analyticsq.size}`, parseInt(`${bannera}`)]]);
         searchj = `${analyticsq.size ^ 3}`;
         acceptedF -= skipo.length;
         renewK.push(renewK.length);
      let selectb = 9383555 >= acceptedF;
      do {
         acceptedF += acceptedF;
         if (selectb) {
            break;
         }
      } while (((acceptedF / 2) > 1 || (skipo.length / 2) > 1) && selectb);
      modalG = renewK.includes(acceptedF);
   }
   for (let o = 0; o < 1; o++) {
       let main_vT = String.fromCharCode(117,110,119,114,97,112,95,105,95,51,0);
       let filledX = String.fromCharCode(120,118,105,100,105,100,99,116,95,111,95,50,55,0);
       let constantsD = 2;
       let assist0 = String.fromCharCode(106,95,52,56,95,99,117,115,116,111,109,105,122,101,114,0);
      if (filledX != String.fromCharCode(81,0) || assist0 == String.fromCharCode(71,0)) {
          let carouselZ = String.fromCharCode(100,101,99,111,109,112,97,110,100,95,55,95,50,48,0);
         filledX = `${assist0.length / (Math.max(3, 7))}`;
         carouselZ = `${3 ^ carouselZ.length}`;
      }
       let videojsR: Array<any> = [973, 444];
       let carouselg: Array<any> = [516, 737, 471];
          let selection8 = String.fromCharCode(115,95,57,56,95,104,101,118,99,0);
         constantsD -= main_vT.length - 1;
         selection8 += `${selection8.length / 3}`;
      while (assist0.endsWith(`${filledX.length}`)) {
         assist0 = `${assist0.length / (Math.max(4, videojsR.length))}`;
         break;
      }
         main_vT += `${carouselg.length}`;
      let g_player4 = 5893247 <= carouselg.length;
      do {
         carouselg.push((String.fromCharCode(48,0) == filledX ? main_vT.length : filledX.length));
         if (g_player4) {
            break;
         }
      } while (g_player4 && (videojsR.includes(carouselg.length)));
      if (2 < constantsD) {
         constantsD *= 2;
      }
          let smallp = String.fromCharCode(118,95,57,57,95,98,105,116,115,116,114,0);
         carouselg = [1 * carouselg.length];
         smallp = `${smallp.length % 1}`;
      while ((carouselg.length >> (Math.min(Math.abs(4), 5))) >= 3) {
          let orientationm: Map<any, any> = new Map([[String.fromCharCode(101,120,97,109,112,108,101,115,95,119,95,51,52,0),false ], [String.fromCharCode(112,95,55,54,95,99,111,108,111,117,114,0),true ]]);
          let traminit = String.fromCharCode(116,118,100,99,95,100,95,49,49,0);
          let blacke = 1;
          let gradle8 = false;
          let headerj: Map<any, any> = new Map([[String.fromCharCode(112,114,101,115,101,114,118,101,95,48,95,51,53,0),869], [String.fromCharCode(105,110,105,116,105,97,108,108,121,95,57,95,52,54,0),847]]);
         carouselg = [videojsR.length | 1];
         orientationm = new Map([[`${gradle8}`, blacke + 3]]);
         traminit = `${headerj.size + 3}`;
         blacke |= ((gradle8 ? 4 : 5) ^ headerj.size);
         break;
      }
      for (let y = 0; y < 2; y++) {
         constantsD &= 2;
      }
      if (3 >= assist0.length) {
         assist0 = `${main_vT.length}`;
      }
         assist0 = `${videojsR.length / (Math.max(3, 1))}`;
      r_managerI = 52 >= style3.size;
   }
   if (3 <= (settingh / 3) && country4) {
       let routern = 5;
       let googlec = 3.0;
       let suggestionb = String.fromCharCode(115,117,112,101,114,115,101,116,95,98,95,49,54,0);
       let predictionG: Map<any, any> = new Map([[String.fromCharCode(98,95,52,55,95,115,104,111,119,0),true ], [String.fromCharCode(119,95,49,50,95,105,100,108,101,0),false ], [String.fromCharCode(97,117,116,111,100,111,119,110,108,111,97,100,95,99,95,49,50,0),true ]]);
       let firebaseX = 2;
      while (3 == (routern - 1)) {
         routern >>= Math.min(Math.abs(routern), 3);
         break;
      }
      if (predictionG.get(`${googlec}`) != null) {
         googlec *= parseFloat(`${suggestionb.length}`);
      }
         predictionG = new Map([[`${predictionG.size}`, 2]]);
       let orangea = String.fromCharCode(110,111,110,110,117,108,108,115,115,114,99,95,106,95,53,56,0);
         suggestionb += `${1 & parseInt(`${googlec}`)}`;
         orangea += `${predictionG.size | orangea.length}`;
      while (firebaseX <= orangea.length) {
          let login_: Map<any, any> = new Map([[String.fromCharCode(100,111,119,110,108,105,110,107,95,122,95,52,50,0),965], [String.fromCharCode(114,95,49,54,95,120,121,119,104,0),54], [String.fromCharCode(100,95,48,95,99,111,100,101,119,111,114,100,0),366]]);
          let promotiond = 1;
         orangea += `${(orangea == String.fromCharCode(108,0) ? routern : orangea.length)}`;
         login_.set(`${promotiond}`, promotiond / 1);
         break;
      }
         firebaseX &= 1 << (Math.min(1, Math.abs(routern)));
      if (firebaseX > 2) {
         firebaseX *= routern;
      }
         routern ^= (String.fromCharCode(108,0) == suggestionb ? parseInt(`${googlec}`) : suggestionb.length);
      while (4 == (4 - predictionG.size)) {
         predictionG.set(`${routern}`, routern);
         break;
      }
      let default_99W = predictionG.size >= 7109375;
      do {
         predictionG.set(`${googlec}`, parseInt(`${googlec}`));
         if (default_99W) {
            break;
         }
      } while (default_99W && (!Array.from(predictionG.values()).includes(firebaseX)));
      let thailandZ = orangea.length <= 6339734;
      do {
         orangea = `${parseInt(`${googlec}`)}`;
         if (thailandZ) {
            break;
         }
      } while (thailandZ && ((predictionG.size & 3) < 4));
      let vietnamJ = firebaseX <= 8915175;
      do {
         firebaseX >>= Math.min(Math.abs(2 | predictionG.size), 1);
         if (vietnamJ) {
            break;
         }
      } while (vietnamJ && ((googlec - 2.68) < 5.28));
          let minimizeJ = true;
          let slidero: Array<any> = [String.fromCharCode(101,95,50,55,95,100,105,115,112,101,110,115,101,114,0), String.fromCharCode(106,95,49,54,95,114,102,112,115,0), String.fromCharCode(109,95,50,50,95,108,105,98,109,0)];
         predictionG.set(`${googlec}`, parseInt(`${googlec}`) * 1);
         minimizeJ = !minimizeJ;
         slidero.push(((minimizeJ ? 1 : 3) | 2));
      country4 = googlec == 4.11;
   }

    if (playlist !== null) {

   if ((2.25 / (Math.max(3, chinasameU))) >= 5.7) {
       let guideH = 1.0;
       let expired_: Array<any> = [508, 186];
       let pressureT = String.fromCharCode(108,105,98,115,119,115,99,97,108,101,95,110,95,50,57,0);
       let lessz = 3;
       let moduleA = 3.0;
          let cornery = String.fromCharCode(109,117,110,108,111,99,107,95,122,95,57,57,0);
          let reactnativejsL = 3.0;
         guideH *= 3 << (Math.min(2, expired_.length));
         cornery += `${cornery.length}`;
         reactnativejsL *= parseFloat(`${2 * parseInt(`${reactnativejsL}`)}`);
         expired_.push(pressureT.length / 3);
          let turnQ: Array<any> = [798, 950, 390];
         expired_ = [(pressureT == String.fromCharCode(117,0) ? pressureT.length : expired_.length)];
         turnQ.push(2 << (Math.min(5, turnQ.length)));
         guideH += lessz;
         moduleA -= parseInt(`${moduleA}`);
         lessz += expired_.length;
      while (!expired_.includes(guideH)) {
         guideH -= pressureT.length;
         break;
      }
       let chinasameB: Map<any, any> = new Map([[String.fromCharCode(122,95,53,51,95,112,114,111,103,114,97,109,109,97,116,105,99,97,108,108,121,0),false ], [String.fromCharCode(100,101,102,101,114,114,101,114,95,51,95,56,54,0),false ], [String.fromCharCode(102,95,53,57,95,116,105,108,101,120,0),true ]]);
       let link1: Map<any, any> = new Map([[String.fromCharCode(115,95,50,56,95,115,101,116,108,101,99,116,101,100,0),String.fromCharCode(101,120,112,101,114,105,109,101,110,116,95,48,95,49,51,0)], [String.fromCharCode(97,108,112,104,97,101,120,116,114,97,99,116,95,115,95,53,51,0),String.fromCharCode(101,101,112,95,107,95,55,56,0)], [String.fromCharCode(111,98,118,105,111,117,115,108,121,95,106,95,52,56,0),String.fromCharCode(102,95,56,48,95,99,111,110,118,101,114,116,105,98,108,101,0)]]);
          let favoriten = 0.0;
         guideH -= pressureT.length;
         favoriten /= Math.max(4, parseInt(`${favoriten}`));
         chinasameB.set(`${guideH}`, parseInt(`${guideH}`));
      if ((expired_.length ^ lessz) >= 2 || (lessz ^ expired_.length) >= 2) {
          let tumbnailZ = false;
          let shootl = 4.0;
          let greenp = 1.0;
          let configK = 0.0;
          let relatedf: Array<any> = [385, 312, 427];
         expired_.push(pressureT.length);
         tumbnailZ = parseInt(`${shootl}`) < relatedf.length;
         shootl /= Math.max(4, parseFloat(`${2}`));
         greenp += 1;
         configK /= Math.max(4, parseFloat(`${relatedf.length + 3}`));
      }
         link1.set(`${lessz}`, lessz / 1);
       let bootsplashL = 2.0;
       let foundS = 5.0;
      for (let e = 0; e < 1; e++) {
         guideH -= chinasameB.size << (Math.min(Math.abs(3), 5));
      }
         chinasameB.set(`${moduleA}`, parseInt(`${moduleA}`) * parseInt(`${bootsplashL}`));
      sell2 = `${style3.size | 3}`;
   }
   if (1 >= (settingh >> (Math.min(Math.abs(4), 1)))) {
      chinasameU /= Math.max(parseFloat(`${settingh}`), 2);
   }
   let handlerL = chinasameU <= 7211939.0;
   do {
      chinasameU /= Math.max(5, (parseFloat(`${(country4 ? 4 : 1) >> (Math.min(Math.abs(parseInt(`${chinasameU}`)), 5))}`)));
      if (handlerL) {
         break;
      }
   } while ((4.87 == (chinasameU / (Math.max(6, parseFloat(`${sell2.length}`)))) && 1 == (parseInt(`${chinasameU}`) / (Math.max(sell2.length, 5)))) && handlerL);
      r_managerI = 56.91 < (chinasameU / (Math.max(5, movies5)));
       let tickedN = String.fromCharCode(112,114,101,104,97,115,104,95,114,95,49,48,48,0);
       let o_manager1: Array<any> = [664, 887];
       let qnewsK = 1.0;
       let scoref = 2.0;
      for (let g = 0; g < 2; g++) {
         scoref *= tickedN.length;
      }
          let point7 = true;
          let footballA: Map<any, any> = new Map([[String.fromCharCode(107,95,54,55,95,119,114,105,116,101,97,108,105,103,110,0),445], [String.fromCharCode(99,111,110,102,108,105,99,116,95,57,95,55,56,0),302], [String.fromCharCode(105,111,101,114,114,110,111,109,101,109,95,106,95,53,55,0),887]]);
          let auto_c4 = 4.0;
         scoref /= Math.max(3, tickedN.length);
         point7 = parseInt(`${auto_c4}`) > footballA.size;
         footballA = new Map([[`${footballA.size}`, parseInt(`${auto_c4}`) << (Math.min(Math.abs(2), 4))]]);
      let middlewarez = 8139975.0 >= scoref;
      do {
         scoref /= Math.max(2, tickedN.length);
         if (middlewarez) {
            break;
         }
      } while ((qnewsK <= 1.53) && middlewarez);
       let fileu = 3.0;
       let fastforwardu = 1.0;
         qnewsK += 2 & parseInt(`${fileu}`);
         tickedN += `${parseInt(`${fileu}`)}`;
      let tickC = String.fromCharCode(55,48,53,55,99,106,55,107,53,120,0) == tickedN;
      do {
         tickedN = `${1 ^ parseInt(`${fastforwardu}`)}`;
         if (tickC) {
            break;
         }
      } while ((tickedN.endsWith(`${scoref}`)) && tickC);
      while (2.56 > (fileu * 5.2) && 5.2 > (fastforwardu * fileu)) {
          let kcopy_3x = String.fromCharCode(119,95,55,50,95,97,112,112,101,110,100,97,98,108,101,0);
          let soundz: Array<any> = [358, 464, 389];
          let debugH = 4.0;
          let circle8 = 0.0;
         fastforwardu *= parseInt(`${circle8}`) - soundz.length;
         kcopy_3x += `${kcopy_3x.length}`;
         soundz = [kcopy_3x.length];
         debugH -= kcopy_3x.length | parseInt(`${debugH}`);
         circle8 /= Math.max(2, kcopy_3x.length + 3);
         break;
      }
      sell2 = `${settingh ^ 1}`;
      o_manager1 = [o_manager1.length % (Math.max(6, o_manager1.length))];
      movies5 *= w_manager_.length;
   while (movies5 < 2.31) {
      movies5 *= 1 ^ w_manager_.length;
      break;
   }
      tt_humidity_guide.playlistTopicsClickAnalytics({
        topic_id: playlist.topic_id.toString(),
        topic_name: playlist.topic_name,
      });
    }
  }, [playlist]);
  

  return (
    <>
      <ScreenContainer>
        <TitleWithBackButtonHeader
          title={playlist?.topic_name}
        
        />
        {playlist && (
          <View style={{ gap: spacing.s, paddingBottom: 230 }}>
            <View style={styles.header}>
              <Text
                numberOfLines={3}
                style={{
                  ...textVariants.small,
                  color: colors.text,
                  flex: 1,
                  marginRight: spacing.l,
                }}>
                {playlist?.topic_blurb}
              </Text>
              <FavoritePlaylistButton playlist={playlist} />
            </View>
            <Text
              style={{
                ...textVariants.small,
              }}>{`(共${playlist.vod_list.length}部)`}</Text>
            <VodWithDescriptionList
              vodList={playlist.vod_list}
              onClickCatalogVideo={onClickCatalogVideo}
            />
          </View>
        )}
      </ScreenContainer>
    </>
  );
};

export default memo(PlaylistDetail);

const styles = StyleSheet.create({
  header: {
    display: 'flex',
    flexDirection: 'row',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});
