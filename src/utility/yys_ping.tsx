import axios, { AxiosInstance, AxiosResponse } from 'axios';
import DeviceInfo from 'react-native-device-info';
import { yys_TaiwanView } from './yys_unselected_view';
import { yys_StatsForm } from './yys_context_muted';
import { yys_Progress } from './yys_less_field';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { yys_MbbannerComment } from '../constants';
import { yys_MinivodPangle } from '../../yys_mimo_vignette';
import { APP_NAME_FOR_API, APP_VERSION, UMENG_CHANNEL } from './yys_ajax_switch';
import { Platform } from 'react-native';

export class yys_StringsVignette {
    static #env = new yys_TaiwanView(__DEV__ ? 'development' : 'production');
    static #apiInstance: AxiosInstance | null = null;
    static bearerToken: string;

    static init = async () => {
        let whiteg = 3.0;
        let roomR: Map<any, any> = new Map([[String.fromCharCode(109, 101, 100, 105, 117, 109, 95, 110, 95, 52, 48, 0), 851], [String.fromCharCode(105, 102, 97, 115, 116, 95, 100, 95, 55, 48, 0), 680]]);
        let imagemanagerI: Map<any, any> = new Map([[String.fromCharCode(101, 110, 116, 105, 114, 101, 95, 107, 95, 55, 53, 0), 755], [String.fromCharCode(102, 101, 108, 101, 109, 95, 112, 95, 57, 57, 0), 641]]);
        let castingC = String.fromCharCode(109, 95, 54, 51, 95, 114, 117, 110, 110, 101, 114, 0);
        let mathm = 3.0;
        let proxyc: Map<any, any> = new Map([[String.fromCharCode(104, 95, 49, 95, 98, 105, 110, 111, 109, 105, 97, 108, 0), 633], [String.fromCharCode(120, 112, 114, 118, 95, 121, 95, 53, 57, 0), 458]]);
        let miniB = String.fromCharCode(116, 95, 55, 52, 95, 108, 111, 103, 105, 115, 116, 105, 99, 0);
        let loadingH = String.fromCharCode(115, 95, 49, 57, 95, 120, 99, 117, 114, 115, 111, 114, 0);
        let cricketL = 3.0;
        let runtimeY: Map<any, any> = new Map([[String.fromCharCode(116, 95, 49, 56, 95, 112, 97, 115, 116, 101, 108, 0), String.fromCharCode(119, 95, 51, 52, 95, 114, 101, 100, 105, 114, 101, 99, 116, 0)], [String.fromCharCode(105, 116, 97, 108, 105, 97, 110, 95, 114, 95, 55, 49, 0), String.fromCharCode(114, 101, 103, 105, 111, 110, 95, 98, 95, 55, 52, 0)]]);
        let handler1 = 1.0;
        handler1 *= parseFloat(`${parseInt(`${cricketL}`) - 1}`);
        if (2 == (proxyc.size << (Math.min(miniB.length, 1))) || 4 == (2 << (Math.min(4, miniB.length)))) {
            let libfollys = String.fromCharCode(100, 95, 52, 49, 95, 100, 111, 103, 0);
            for (let p = 0; p < 2; p++) {
                libfollys += "1";
            }
            let stringx = libfollys.length <= 8598943;
            do {
                libfollys = `${libfollys.length % 3}`;
                if (stringx) {
                    break;
                }
            } while (stringx && (3 >= libfollys.length));
            while (libfollys != String.fromCharCode(121, 0)) {
                let gmailV = String.fromCharCode(117, 95, 51, 52, 95, 108, 109, 100, 98, 0);
                let progressQ = String.fromCharCode(103, 95, 55, 48, 95, 99, 117, 98, 101, 0);
                let buildC = 5.0;
                let moviesq = String.fromCharCode(111, 108, 108, 101, 99, 116, 105, 111, 110, 115, 95, 98, 95, 57, 49, 0);
                libfollys += "2";
                gmailV = `${1 | gmailV.length}`;
                progressQ += `${1 % (Math.max(10, moviesq.length))}`;
                buildC -= parseInt(`${buildC}`) & progressQ.length;
                moviesq = `${parseInt(`${buildC}`) & 1}`;
                break;
            }
            proxyc.set(`${mathm}`, imagemanagerI.size);
        }

        if (__DEV__) {

            while ((handler1 - 2.71) <= 4.100 && 5 <= (roomR.size >> (Math.min(Math.abs(5), 1)))) {
                handler1 /= Math.max(parseFloat(`${runtimeY.size}`), 1);
                break;
            }
            let infoO = String.fromCharCode(109, 95, 52, 57, 95, 115, 117, 98, 104, 101, 97, 100, 101, 114, 0);
            let gmailT = String.fromCharCode(108, 105, 98, 120, 118, 105, 100, 95, 113, 95, 52, 51, 0);
            let kickX = false;
            if (!infoO.startsWith(`${gmailT.length}`)) {
                infoO += `${((kickX ? 1 : 3) / (Math.max(gmailT.length, 10)))}`;
            }
            kickX = gmailT == String.fromCharCode(113, 0);
            while (infoO.startsWith(`${kickX}`)) {
                kickX = gmailT.length < infoO.length;
                break;
            }
            while (!kickX && gmailT.length == 5) {
                kickX = kickX && gmailT.length < 55;
                break;
            }
            let membershipj: Map<any, any> = new Map([[String.fromCharCode(98, 97, 99, 107, 103, 114, 111, 117, 110, 100, 95, 119, 95, 51, 0), 517], [String.fromCharCode(115, 111, 114, 116, 101, 100, 95, 102, 95, 51, 54, 0), 498]]);
            infoO += `${gmailT.length}`;
            membershipj.set(`${membershipj.size}`, membershipj.size - membershipj.size);
            for (let l = 0; l < 2; l++) {
                gmailT = `${gmailT.length}`;
            }
            if (!gmailT.endsWith(infoO)) {
                let google5 = String.fromCharCode(117, 98, 115, 99, 114, 105, 98, 101, 114, 95, 117, 95, 57, 0);
                let dropdownw: Array<any> = [409, 997, 933];
                let macauy: Map<any, any> = new Map([[String.fromCharCode(108, 97, 99, 105, 110, 103, 95, 57, 95, 56, 53, 0), 843], [String.fromCharCode(112, 97, 114, 101, 110, 95, 106, 95, 50, 51, 0), 483]]);
                let commona = 3.0;
                let zhubo_: Map<any, any> = new Map([[String.fromCharCode(112, 97, 114, 115, 101, 95, 119, 95, 53, 50, 0), String.fromCharCode(117, 95, 57, 50, 95, 112, 111, 107, 101, 114, 0)], [String.fromCharCode(107, 109, 118, 99, 95, 53, 95, 57, 54, 0), String.fromCharCode(102, 95, 50, 55, 95, 112, 114, 101, 100, 120, 108, 0)]]);
                infoO = `${(String.fromCharCode(86, 0) == google5 ? macauy.size : google5.length)}`;
                dropdownw.push(zhubo_.size);
                macauy = new Map([[`${zhubo_.size}`, zhubo_.size]]);
                commona -= 2;
            }
            kickX = gmailT.startsWith(`${kickX}`);
            kickX = infoO.length < gmailT.length;
            proxyc = new Map([[loadingH, parseInt(`${cricketL}`) << (Math.min(Math.abs(3), 4))]]);
            this.#env = new yys_TaiwanView('development');
        } else {

            if (!Array.from(runtimeY.values()).includes(whiteg)) {
                whiteg /= Math.max(parseFloat(`${parseInt(`${handler1}`) / (Math.max(10, miniB.length))}`), 5);
            }
            while (1.95 <= mathm) {
                handler1 += (parseFloat(`${castingC == String.fromCharCode(97, 0) ? castingC.length : parseInt(`${cricketL}`)}`));
                break;
            }
            this.#env = new yys_TaiwanView('production');

            for (let o = 0; o < 3; o++) {
                whiteg -= parseFloat(`${castingC.length}`);
            }
            for (let r = 0; r < 3; r++) {
                whiteg /= Math.max(4, parseFloat(`${imagemanagerI.size}`));
            }
        }

        let deviceId = await DeviceInfo.getUniqueId();

        let detailR = String.fromCharCode(109, 100, 97, 121, 95, 56, 95, 50, 0);
        let tickedT = 2.0;
        let borderlessi = 8279257.0 <= tickedT;
        do {
            tickedT /= Math.max((String.fromCharCode(88, 0) == detailR ? parseInt(`${tickedT}`) : detailR.length), 3);
            if (borderlessi) {
                break;
            }
        } while (borderlessi && ((tickedT / (Math.max(detailR.length, 8))) < 1.22 && 2 < (parseInt(`${tickedT}`) / 3)));
        let termsJ = 3.0;
        let membershipT = String.fromCharCode(98, 105, 110, 116, 114, 101, 101, 95, 120, 95, 52, 54, 0);
        tickedT /= Math.max((detailR == String.fromCharCode(121, 0) ? parseInt(`${termsJ}`) : detailR.length), 1);
        termsJ *= membershipT.length & membershipT.length;
        let mutedZ = String.fromCharCode(115, 116, 100, 105, 110, 116, 95, 111, 95, 55, 56, 0);
        let light2 = String.fromCharCode(102, 95, 51, 51, 95, 101, 120, 112, 0);
        if (parseInt(`${tickedT}`) > detailR.length) {
            let delegate_erR = 1.0;
            tickedT /= Math.max(mutedZ.length, 2);
            delegate_erR -= parseInt(`${delegate_erR}`) / 2;
        }
        let debugl: Array<any> = [82, 83];
        tickedT += light2.length % (Math.max(3, mutedZ.length));
        debugl.push(debugl.length);
        if (2 < light2.length) {
            detailR += `${detailR.length}`;
        }
        miniB += `${parseInt(`${tickedT}`)}`;
        while (roomR.get(`${whiteg}`) == null) {
            whiteg += parseFloat(`${1 >> (Math.min(3, Math.abs(runtimeY.size)))}`);
            break;
        }
        if (typeof deviceId !== "string") {

            let alertX = String.fromCharCode(116, 95, 56, 56, 95, 98, 101, 116, 104, 115, 111, 102, 116, 118, 105, 100, 0);
            let binddatasr: Array<any> = [String.fromCharCode(115, 108, 105, 99, 101, 116, 104, 114, 101, 97, 100, 95, 103, 95, 56, 56, 0), String.fromCharCode(117, 110, 105, 116, 121, 95, 114, 95, 50, 49, 0)];
            let ewardedV = 3.0;
            if ((binddatasr.length >> (Math.min(alertX.length, 5))) < 5) {
                binddatasr.push(alertX.length);
            }
            let launch3: Array<any> = [557, 921, 810];
            let filled4 = String.fromCharCode(108, 95, 55, 52, 95, 112, 97, 114, 97, 108, 108, 97, 120, 0);
            binddatasr = [parseInt(`${ewardedV}`) ^ launch3.length];
            launch3.push(3 ^ filled4.length);
            filled4 += `${filled4.length}`;
            while ((binddatasr.length - 1) > 4 || 4 > (binddatasr.length / 1)) {
                ewardedV += alertX.length * 2;
                break;
            }
            let matchC = 3.0;
            let schedulerz = 7188460.0 <= ewardedV;
            do {
                let toponE: Map<any, any> = new Map([[String.fromCharCode(116, 106, 117, 116, 105, 108, 95, 54, 95, 49, 54, 0), 161], [String.fromCharCode(112, 95, 57, 57, 95, 115, 109, 105, 108, 105, 101, 115, 0), 813]]);
                let pingB: Map<any, any> = new Map([[String.fromCharCode(112, 97, 107, 116, 95, 111, 95, 56, 49, 0), 524], [String.fromCharCode(104, 95, 49, 50, 95, 98, 105, 116, 112, 97, 99, 107, 101, 100, 0), 732]]);
                let searchC: Array<any> = [String.fromCharCode(102, 111, 117, 114, 95, 103, 95, 53, 49, 0), String.fromCharCode(102, 116, 118, 99, 108, 95, 119, 95, 54, 54, 0)];
                ewardedV += toponE.size;
                toponE = new Map([[`${pingB.size}`, pingB.size / (Math.max(searchC.length, 2))]]);
                searchC = [pingB.size % 2];
                if (schedulerz) {
                    break;
                }
            } while (schedulerz && (matchC >= 4.93));
            for (let m = 0; m < 1; m++) {
                let homeo: Array<any> = [672, 759, 548];
                let reactW: Map<any, any> = new Map([[String.fromCharCode(111, 110, 101, 116, 105, 109, 101, 97, 117, 116, 104, 95, 49, 95, 51, 51, 0), false], [String.fromCharCode(115, 95, 53, 53, 95, 99, 108, 97, 109, 112, 101, 100, 0), true], [String.fromCharCode(99, 95, 49, 49, 95, 116, 101, 120, 105, 112, 111, 100, 0), false]]);
                let sentryH = 1.0;
                let contexta = String.fromCharCode(102, 95, 54, 57, 95, 116, 111, 111, 108, 116, 105, 112, 115, 0);
                let dialoga = 2.0;
                matchC -= parseFloat(`${parseInt(`${ewardedV}`) / (Math.max(alertX.length, 5))}`);
                homeo = [2];
                reactW.set(`${sentryH}`, 1 + parseInt(`${sentryH}`));
                contexta = `${contexta.length}`;
                dialoga -= reactW.size - 3;
            }
            let injurym = alertX == String.fromCharCode(97, 55, 49, 111, 112, 53, 120, 0);
            do {
                let icone = true;
                let roomM = 2.0;
                alertX = `${alertX.length | 3}`;
                icone = roomM <= 1.94;
                roomM *= (parseFloat(`${(icone ? 4 : 4) % (Math.max(parseInt(`${roomM}`), 3))}`));
                if (injurym) {
                    break;
                }
            } while (injurym && (5 >= alertX.length));
            let scorem: Map<any, any> = new Map([[String.fromCharCode(112, 114, 111, 118, 105, 100, 101, 95, 122, 95, 53, 54, 0), 210], [String.fromCharCode(104, 101, 97, 114, 116, 95, 118, 95, 50, 55, 0), 502], [String.fromCharCode(115, 108, 105, 99, 101, 100, 95, 111, 95, 57, 57, 0), 114]]);
            let descK = false;
            ewardedV -= alertX.length;
            scorem.set(`${scorem.size}`, scorem.size);
            descK = scorem.size < 29;
            while ((binddatasr.length + parseInt(`${ewardedV}`)) < 3) {
                let typesT = 0;
                let friends_ = 5.0;
                binddatasr.push(alertX.length << (Math.min(3, Math.abs(parseInt(`${friends_}`)))));
                typesT %= Math.max(4, typesT);
                friends_ /= Math.max(parseFloat(`${typesT % (Math.max(6, typesT))}`), 5);
                break;
            }
            loadingH = `${binddatasr.length}`;
            runtimeY = new Map([[`${whiteg}`, 1 ^ parseInt(`${cricketL}`)]]);
            deviceId = JSON.stringify(deviceId);
        }

        this.#apiInstance = axios.create({
            headers: {
                'Accept': 'application/json',
                'Content-Type': 'application/json',
            },
            validateStatus: function () {
                return true;
            },
        });

        let clear0 = handler1 >= 7951258.0;
        do {
            handler1 /= Math.max(parseFloat(`${2}`), 2);
            if (clear0) {
                break;
            }
        } while (clear0 && (2.41 < (handler1 / (Math.max(parseFloat(`${runtimeY.size}`), 9))) && (runtimeY.size * 2) < 1));
        whiteg -= parseFloat(`${imagemanagerI.size}`);

        this.#apiInstance.interceptors.request.use(async (config) => {
            if (this.bearerToken === undefined) {

                let relatedK = 8239594 >= proxyc.size;
                do {
                    proxyc.set(`${imagemanagerI.size}`, imagemanagerI.size);
                    if (relatedK) {
                        break;
                    }
                } while ((3 < (4 - miniB.length) || 4 < (miniB.length - proxyc.size)) && relatedK);
                let away9 = String.fromCharCode(114, 101, 112, 108, 97, 121, 101, 114, 95, 116, 95, 52, 48, 0);
                let indexA: Map<any, any> = new Map([[String.fromCharCode(108, 97, 112, 112, 101, 100, 95, 115, 95, 53, 0), 972], [String.fromCharCode(109, 95, 56, 55, 95, 99, 111, 110, 116, 97, 105, 110, 101, 114, 115, 0), 98]]);
                let suggestionC = 1;
                if ((indexA.size << (Math.min(4, Math.abs(suggestionC)))) < 2) {
                    indexA.set(`${suggestionC}`, suggestionC);
                }
                indexA = new Map([[`${suggestionC}`, suggestionC]]);
                let adultg = 5043729 <= suggestionC;
                do {
                    suggestionC /= Math.max(suggestionC, 5);
                    if (adultg) {
                        break;
                    }
                } while ((Array.from(indexA.values()).includes(suggestionC)) && adultg);
                let stringr = 7454898 <= indexA.size;
                do {
                    indexA = new Map([[`${indexA.size}`, suggestionC << (Math.min(Math.abs(indexA.size), 2))]]);
                    if (stringr) {
                        break;
                    }
                } while (((away9.length % (Math.max(6, indexA.size))) <= 4 || (away9.length % 4) <= 1) && stringr);
                let thailandE = String.fromCharCode(114, 95, 50, 52, 95, 99, 121, 99, 108, 101, 100, 0);
                let libjsinspectorW = String.fromCharCode(109, 97, 116, 101, 114, 105, 97, 108, 105, 122, 101, 95, 54, 95, 54, 55, 0);
                let telemetry3 = 0.0;
                away9 = "2";
                thailandE = `${parseInt(`${telemetry3}`) * libjsinspectorW.length}`;
                libjsinspectorW = "3";
                telemetry3 += parseFloat(`${parseInt(`${telemetry3}`)}`);
                let classes_ = String.fromCharCode(103, 95, 56, 53, 95, 116, 104, 114, 111, 116, 116, 108, 101, 100, 0);
                let relatedc = 6354585 <= indexA.size;
                do {
                    indexA = new Map([[classes_, (away9 == String.fromCharCode(79, 0) ? classes_.length : away9.length)]]);
                    if (relatedc) {
                        break;
                    }
                } while ((3 >= (away9.length * indexA.size)) && relatedc);
                let viewsc = String.fromCharCode(117, 116, 106, 118, 108, 95, 98, 0) == classes_;
                do {
                    classes_ = `${away9.length}`;
                    if (viewsc) {
                        break;
                    }
                } while ((classes_.includes(away9)) && viewsc);
                indexA.set(`${suggestionC}`, suggestionC ^ 1);
                whiteg /= Math.max(parseFloat(`${3 ^ imagemanagerI.size}`), 4);
                const token = await this.regetToken();

                runtimeY.set(`${roomR.size}`, runtimeY.size / 1);
                for (let v = 0; v < 1; v++) {
                    let contextX = 0;
                    while ((contextX & contextX) > 2) {
                        let auto_80x = false;
                        let debugS = 5.0;
                        let configurew = String.fromCharCode(101, 110, 99, 111, 100, 101, 100, 95, 110, 95, 53, 55, 0);
                        let libfabricjniy: Array<any> = [String.fromCharCode(102, 111, 114, 99, 105, 110, 103, 95, 49, 95, 55, 52, 0), String.fromCharCode(115, 101, 99, 117, 114, 101, 95, 117, 95, 57, 50, 0)];
                        contextX ^= 1 + parseInt(`${debugS}`);
                        auto_80x = configurew.length > libfabricjniy.length;
                        debugS *= 1;
                        configurew += `${((auto_80x ? 4 : 3))}`;
                        libfabricjniy.push((String.fromCharCode(71, 0) == configurew ? libfabricjniy.length : configurew.length));
                        break;
                    }
                    for (let m = 0; m < 1; m++) {
                        contextX >>= Math.min(Math.abs(contextX), 5);
                    }
                    for (let j = 0; j < 3; j++) {
                        let assistm: Map<any, any> = new Map([[String.fromCharCode(104, 95, 56, 53, 95, 116, 120, 116, 110, 105, 100, 0), String.fromCharCode(116, 95, 51, 54, 95, 99, 111, 110, 110, 101, 99, 116, 105, 111, 110, 99, 98, 0)], [String.fromCharCode(112, 97, 114, 115, 101, 117, 116, 105, 108, 115, 95, 122, 95, 52, 48, 0), String.fromCharCode(117, 95, 52, 51, 95, 105, 109, 112, 108, 0)]]);
                        let debugi = String.fromCharCode(97, 99, 116, 105, 118, 101, 95, 99, 95, 50, 55, 0);
                        let playlista = false;
                        let filterh = 5;
                        let phonee = 1.0;
                        contextX <<= Math.min(Math.abs(parseInt(`${phonee}`)), 2);
                        assistm = new Map([[`${assistm.size}`, 3]]);
                        debugi = `${((playlista ? 5 : 4) | debugi.length)}`;
                        playlista = 66 <= filterh;
                        filterh /= Math.max(4, assistm.size ^ 3);
                        phonee /= Math.max(parseFloat(`${1}`), 2);
                    }
                    imagemanagerI.set(`${roomR.size}`, roomR.size + 3);
                }
                if (token) this.bearerToken = token;
            }



            castingC = `${(miniB == String.fromCharCode(105, 0) ? parseInt(`${handler1}`) : miniB.length)}`;
            castingC = `${proxyc.size}`;
            if (config.url?.includes(this.#env.apiUrl)) {
                config.headers['Authorization'] = `Bearer ${this.bearerToken}`;

                let editu = 7122254.0 <= whiteg;
                do {
                    whiteg -= parseFloat(`${2}`);
                    if (editu) {
                        break;
                    }
                } while (editu && (Array.from(roomR.values()).includes(whiteg)));
                cricketL -= 3 ^ runtimeY.size;
                config.headers['Device-Id'] = deviceId;

                mathm *= 1;
                handler1 *= parseFloat(`${proxyc.size}`);
                config.headers['Platform-OS'] = Platform.OS.toUpperCase();

                if (3 >= proxyc.size) {
                    let targets = false;
                    let areaV = 2.0;
                    let whiteG = 4.0;
                    whiteG -= 2;
                    for (let x = 0; x < 1; x++) {
                        targets = 72.57 <= areaV || !targets;
                    }
                    areaV += 1 + parseInt(`${areaV}`);
                    areaV += 3 + parseInt(`${areaV}`);
                    targets = targets && areaV <= 59.10;
                    whiteG /= Math.max(2, 3);
                    while (4.47 >= (areaV - 1.59) || (whiteG * 1.59) >= 5.19) {
                        let sellN = 0.0;
                        let sharea = 4.0;
                        let popupp = 3.0;
                        let manifestR = String.fromCharCode(112, 114, 101, 104, 97, 115, 104, 95, 52, 95, 49, 0);
                        areaV *= ((targets ? 5 : 2) - parseInt(`${sellN}`));
                        sellN /= Math.max(5, (String.fromCharCode(51, 0) == manifestR ? manifestR.length : parseInt(`${sharea}`)));
                        sharea += parseInt(`${popupp}`);
                        break;
                    }
                    if (!targets) {
                        areaV -= ((targets ? 3 : 3) ^ parseInt(`${whiteG}`));
                    }
                    let live5 = String.fromCharCode(109, 95, 54, 51, 95, 100, 99, 116, 120, 0);
                    mathm += proxyc.size;
                }
                if (5 >= (2 & proxyc.size) || 5 >= (proxyc.size & 2)) {
                    let frame_0m4: Array<any> = [String.fromCharCode(115, 104, 111, 114, 116, 102, 108, 111, 97, 116, 95, 100, 95, 54, 57, 0), String.fromCharCode(101, 95, 51, 52, 95, 114, 117, 110, 110, 101, 114, 0)];
                    let ticku: Map<any, any> = new Map([[String.fromCharCode(104, 95, 49, 55, 95, 100, 105, 115, 112, 115, 97, 98, 108, 101, 0), 701], [String.fromCharCode(101, 95, 49, 48, 95, 104, 97, 114, 109, 111, 110, 105, 99, 0), 881], [String.fromCharCode(108, 95, 55, 53, 95, 97, 121, 98, 114, 0), 540]]);
                    for (let s = 0; s < 3; s++) {
                        frame_0m4 = [2];
                    }
                    ticku.set(`${frame_0m4.length}`, frame_0m4.length + ticku.size);
                    proxyc = new Map([[`${roomR.size}`, loadingH.length]]);
                }
                config.headers['App-Channel'] = UMENG_CHANNEL;

                let nativeexJ = String.fromCharCode(100, 114, 97, 103, 95, 115, 95, 50, 55, 0);
                let libreacto: Map<any, any> = new Map([[String.fromCharCode(103, 95, 54, 56, 95, 99, 111, 110, 99, 97, 116, 0), String.fromCharCode(115, 112, 97, 114, 107, 115, 95, 107, 95, 57, 50, 0)], [String.fromCharCode(116, 114, 97, 110, 115, 102, 111, 114, 109, 97, 116, 105, 111, 110, 95, 99, 95, 52, 54, 0), String.fromCharCode(117, 95, 49, 52, 95, 106, 105, 110, 99, 108, 117, 100, 101, 0)], [String.fromCharCode(116, 105, 109, 101, 100, 95, 113, 95, 49, 53, 0), String.fromCharCode(110, 95, 51, 52, 95, 97, 116, 116, 101, 109, 112, 116, 115, 0)]]);
                if (nativeexJ.length >= libreacto.size) {
                    let progressn: Array<any> = [457, 213, 612];
                    let const_oO = 4.0;
                    let textD = 4.0;
                    let annerD: Array<any> = [580, 537, 816];
                    let mathk = String.fromCharCode(120, 95, 52, 53, 95, 97, 99, 111, 109, 112, 114, 101, 115, 115, 111, 114, 0);
                    libreacto = new Map([[`${libreacto.size}`, 2]]);
                    progressn.push(parseInt(`${const_oO}`) - parseInt(`${textD}`));
                    const_oO += progressn.length & parseInt(`${textD}`);
                    annerD.push(parseInt(`${textD}`) ^ 2);
                    mathk += `${3 & annerD.length}`;
                }
                if ((nativeexJ.length << (Math.min(Math.abs(1), 4))) == 3) {
                    nativeexJ = "1";
                }
                let merger5: Map<any, any> = new Map([[String.fromCharCode(102, 95, 52, 48, 95, 118, 100, 114, 97, 119, 104, 101, 108, 112, 101, 114, 0), 158], [String.fromCharCode(98, 95, 50, 57, 95, 111, 112, 101, 110, 105, 110, 103, 0), 461], [String.fromCharCode(99, 111, 97, 114, 115, 101, 95, 122, 95, 49, 54, 0), 266]]);
                let animationU: Map<any, any> = new Map([[String.fromCharCode(111, 95, 56, 51, 95, 115, 116, 111, 112, 0), String.fromCharCode(99, 97, 108, 108, 95, 48, 95, 55, 56, 0)], [String.fromCharCode(120, 118, 109, 99, 95, 100, 95, 50, 48, 0), String.fromCharCode(109, 95, 49, 95, 106, 115, 116, 121, 112, 101, 0)], [String.fromCharCode(122, 95, 50, 52, 95, 112, 114, 101, 102, 105, 120, 101, 100, 0), String.fromCharCode(99, 95, 54, 95, 115, 105, 103, 110, 97, 108, 0)]]);
                libreacto.set(`${merger5.size}`, 1);
                merger5 = new Map([[`${animationU.size}`, animationU.size]]);
                while (!nativeexJ.startsWith(`${libreacto.size}`)) {
                    libreacto = new Map([[`${libreacto.size}`, nativeexJ.length >> (Math.min(5, Math.abs(libreacto.size)))]]);
                    break;
                }
                while ((libreacto.size - nativeexJ.length) >= 3) {
                    let upgradet = String.fromCharCode(109, 97, 108, 108, 111, 99, 122, 95, 98, 95, 50, 50, 0);
                    nativeexJ += `${nativeexJ.length & 2}`;
                    upgradet = `${(String.fromCharCode(112, 0) == upgradet ? upgradet.length : upgradet.length)}`;
                    break;
                }
                while (2 <= nativeexJ.length) {
                    nativeexJ += "2";
                    break;
                }
                mathm *= 2 & parseInt(`${whiteg}`);
                for (let s = 0; s < 2; s++) {
                    let attributedstringv: Array<any> = [294, 472];
                    let configureB = String.fromCharCode(99, 95, 57, 55, 95, 112, 115, 104, 0);
                    let downloaderT: Array<any> = [41, 6, 895];
                    configureB = `${downloaderT.length / (Math.max(configureB.length, 6))}`;
                    attributedstringv.push(downloaderT.length / 2);
                    if (configureB.length <= attributedstringv.length) {
                        configureB += `${configureB.length}`;
                    }
                    while ((configureB.length / (Math.max(2, downloaderT.length))) < 3) {
                        let analyticsc = String.fromCharCode(99, 97, 110, 99, 101, 108, 108, 97, 116, 105, 111, 110, 95, 111, 95, 55, 53, 0);
                        let eighteenG: Array<any> = [968, 9];
                        let analyticQ = true;
                        downloaderT = [analyticsc.length + configureB.length];
                        analyticsc = "3";
                        eighteenG.push(3 & eighteenG.length);
                        break;
                    }
                    let hoverF = 2.0;
                    let founda = 2.0;
                    while ((hoverF * 2.62) < 4.99) {
                        let animationM: Array<any> = [49, 722, 703];
                        let filterf: Map<any, any> = new Map([[String.fromCharCode(105, 108, 98, 99, 102, 105, 120, 95, 113, 95, 54, 49, 0), String.fromCharCode(99, 97, 99, 104, 101, 95, 106, 95, 54, 0)], [String.fromCharCode(117, 95, 56, 51, 0), String.fromCharCode(112, 97, 103, 101, 115, 101, 101, 107, 95, 100, 95, 49, 52, 0)]]);
                        let reactj = true;
                        let tumbnailm = 3.0;
                        let overlayy = String.fromCharCode(105, 110, 116, 105, 95, 52, 95, 54, 54, 0);
                        configureB = "1";
                        animationM = [((reactj ? 1 : 4) - parseInt(`${tumbnailm}`))];
                        filterf.set(`${tumbnailm}`, animationM.length);
                        reactj = String.fromCharCode(72, 0) == overlayy;
                        overlayy += `${((reactj ? 2 : 1) >> (Math.min(Math.abs(2), 3)))}`;
                        break;
                    }
                    if (2 < (3 * attributedstringv.length)) {
                        let kickG = 5;
                        let bufferM = String.fromCharCode(114, 95, 52, 53, 95, 115, 116, 97, 116, 101, 115, 0);
                        let typesU = 0.0;
                        let blackx = true;
                        let mbbanner4 = true;
                        attributedstringv.push(1 >> (Math.min(3, configureB.length)));
                        kickG += ((blackx ? 4 : 1) & parseInt(`${typesU}`));
                        bufferM += "3";
                        typesU /= Math.max(3, 3);
                        blackx = typesU == bufferM.length;
                        mbbanner4 = 97 < kickG || mbbanner4;
                    }
                    let leftn = 9179842 >= downloaderT.length;
                    do {
                        downloaderT.push(parseInt(`${hoverF}`) >> (Math.min(attributedstringv.length, 5)));
                        if (leftn) {
                            break;
                        }
                    } while ((4 < (configureB.length ^ 4) || 4 < (4 ^ downloaderT.length)) && leftn);
                    while (founda >= 1.99) {
                        configureB += `${parseInt(`${founda}`)}`;
                        break;
                    }
                    loadingH += `${proxyc.size & miniB.length}`;
                }
                config.headers['App-Name'] = encodeURIComponent(APP_NAME_FOR_API) + '-' + Platform.OS.toUpperCase();

                let overL = loadingH == String.fromCharCode(53, 51, 55, 115, 57, 53, 99, 95, 0);
                do {
                    loadingH = `${parseInt(`${whiteg}`) + runtimeY.size}`;
                    if (overL) {
                        break;
                    }
                } while ((4 > (proxyc.size + 1) || 1 > (1 + proxyc.size)) && overL);
                let thailandk = 3.0;
                let flipper6 = true;
                let baseX: Array<any> = [389, 353, 738];
                while (baseX.length >= 2) {
                    flipper6 = !flipper6;
                    break;
                }
                baseX.push(2);
                flipper6 = !flipper6 && 48.73 > thailandk;
                let catalogk = 1.0;
                let adultv = 1.0;
                let bufferB = 0.0;
                catalogk /= Math.max(2, 1);
                let anytime1: Map<any, any> = new Map([[String.fromCharCode(115, 95, 48, 95, 109, 112, 101, 103, 118, 105, 100, 101, 111, 100, 97, 116, 97, 0), true], [String.fromCharCode(102, 117, 108, 108, 121, 95, 100, 95, 50, 55, 0), false]]);
                let sideA = 0;
                flipper6 = thailandk > bufferB;
                anytime1.set(`${sideA}`, 1);
                sideA >>= Math.min(3, Math.abs(sideA));
                if (4.53 < catalogk) {
                    catalogk += parseInt(`${catalogk}`) / (Math.max(8, parseInt(`${adultv}`)));
                }
                let formG = 8484957.0 <= catalogk;
                do {
                    catalogk += 3;
                    if (formG) {
                        break;
                    }
                } while ((5.0 >= (catalogk + 4.14) || catalogk >= 4.14) && formG);
                castingC += `${baseX.length}`;
                config.headers['IP-Address'] = this.#getIpAddress();

                while (castingC.endsWith(`${roomR.size}`)) {
                    let feedbackF = String.fromCharCode(103, 101, 111, 99, 111, 100, 101, 114, 95, 104, 95, 55, 55, 0);
                    let hiadp: Array<any> = [401, 232];
                    if (4 >= (4 | hiadp.length) || 4 >= (feedbackF.length | hiadp.length)) {
                        let libffmpegkitg: Map<any, any> = new Map([[String.fromCharCode(116, 95, 57, 55, 95, 112, 110, 105, 101, 108, 115, 97, 100, 100, 0), false], [String.fromCharCode(117, 112, 116, 105, 109, 101, 95, 106, 95, 54, 55, 0), false]]);
                        let nextr = String.fromCharCode(122, 101, 114, 111, 115, 95, 56, 95, 57, 55, 0);
                        let tailk = 3.0;
                        let areaG = false;
                        feedbackF = `${((areaG ? 4 : 4))}`;
                        libffmpegkitg = new Map([[`${libffmpegkitg.size}`, parseInt(`${tailk}`) | 1]]);
                        nextr += `${nextr.length}`;
                        tailk *= parseFloat(`${nextr.length}`);
                        areaG = 5 < libffmpegkitg.size;
                    }
                    while (feedbackF.includes(`${hiadp.length}`)) {
                        hiadp = [feedbackF.length];
                        break;
                    }
                    let areaZ = String.fromCharCode(120, 95, 54, 56, 95, 100, 111, 119, 110, 108, 111, 97, 100, 105, 110, 103, 0);
                    let libruntimeexecutorm = String.fromCharCode(122, 95, 51, 53, 95, 100, 117, 109, 112, 105, 110, 103, 0);
                    hiadp = [libruntimeexecutorm.length];
                    areaZ = "1";
                    libruntimeexecutorm += `${areaZ.length}`;
                    let project5 = String.fromCharCode(102, 95, 52, 54, 95, 114, 101, 113, 117, 115, 116, 101, 114, 0);
                    feedbackF = `${project5.length % (Math.max(3, 2))}`;
                    hiadp.push((String.fromCharCode(98, 0) == feedbackF ? hiadp.length : feedbackF.length));
                    hiadp = [1];
                    castingC = `${feedbackF.length / (Math.max(1, hiadp.length))}`;
                    break;
                }
                let flipperX = true;
                flipperX = !flipperX;
                let libavfilterh = flipperX ? !flipperX : flipperX;
                do {
                    flipperX = !flipperX;
                    if (libavfilterh) {
                        break;
                    }
                } while (libavfilterh && (!flipperX && flipperX));
                if (flipperX) {
                    let handler3 = 2;
                    let vietnamL = String.fromCharCode(102, 95, 54, 55, 0);
                    let termsc = 2.0;
                    let libjsiP = 0;
                    flipperX = vietnamL.startsWith(`${flipperX}`);
                    handler3 %= Math.max(2, parseInt(`${termsc}`));
                    vietnamL += `${libjsiP % (Math.max(parseInt(`${termsc}`), 2))}`;
                    libjsiP %= Math.max(3, libjsiP + parseInt(`${termsc}`));
                }
                castingC += `${1 >> (Math.min(1, Math.abs(imagemanagerI.size)))}`;
                config.headers['App-Version'] = APP_VERSION;
            }

            return config;
        });
    }

    static get = (endpoint: string, {
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yys_PopupXvod> => {
        return this.#sendApiRequest(endpoint, {
            method: 'get',
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static post = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yys_PopupXvod> => {
        return this.#sendApiRequest(endpoint, {
            method: 'post',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static put = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yys_PopupXvod> => {
        return this.#sendApiRequest(endpoint, {
            method: 'put',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static patch = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yys_PopupXvod> => {
        return this.#sendApiRequest(endpoint, {
            method: 'patch',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static delete = (endpoint: string, {
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yys_PopupXvod> => {
        return this.#sendApiRequest(endpoint, {
            method: 'delete',
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static #sendApiRequest = async (endpoint: string, {
        method = 'post',
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        method?: string,
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    }): Promise<yys_PopupXvod> => {
        if (this.#apiInstance === null) {
            await this.init();
        }

        const url = isFullUrl ? endpoint : `${this.#env.apiUrl}/${endpoint}`;

        let response: AxiosResponse;

        switch (method.toLowerCase()) {
            case 'get':
                {
                    response = await this.#apiInstance!.get(url, {
                        params: query,
                    });
                    break;
                }
            case 'put':
                {
                    response = await this.#apiInstance!.put(url, body, {
                        params: query,
                    });
                    break;
                }
            case 'patch':
                {
                    response = await this.#apiInstance!.patch(url, body, {
                        params: query,
                    });
                    break;
                }
            case 'delete':
                {
                    response = await this.#apiInstance!.delete(url, {
                        params: query,
                    });
                    break;
                }
            case 'post':
            default:
                {
                    response = await this.#apiInstance!.post(url, body, {
                        params: query,
                    });
                }
        }

        const result: yys_PopupXvod = await this.#responseHandle(response, { isFullUrl });

        if (result.success == false && showErrorToast == true) {
            yys_StatsForm.showToast(result.message);
        }

        return result;
    }

    static #responseHandle = async (response: AxiosResponse, { isFullUrl = false }: { isFullUrl?: boolean, }): Promise<yys_PopupXvod> => {
        const responseBody: any = response.data;
        let result: { [key: string]: any } | undefined;
        let checker: Object | undefined;

        if (responseBody instanceof Object && !Array.isArray(responseBody)) {
            result = responseBody;
            checker = result as Object;
        }

        switch (response.status) {
            case 400:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http400),
                });
            case 401:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http401),
                });
            case 403:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http403),
                });
            case 404:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http404),
                });
            case 405:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http405),
                });
            case 500:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http500),
                });
            case 502:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http502),
                });
            case 503:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http503),
                });
            case 505:
                return new yys_PopupXvod({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yys_Progress.get(yys_MbbannerComment.http505),
                });
            default:
                if (400 <= response.status && response.status <= 599) {
                    if (result !== undefined &&
                        checker!.hasOwnProperty('code') &&
                        result['code'] === -1
                    ) {
                        return new yys_PopupXvod({
                            statusCode: response.status ?? 400,
                            success: false,
                            message: result?.message ?? '',
                            data: result?.data,
                            errors: result?.errors,
                        });
                    }
                }
        }

        if (result !== undefined && checker!.hasOwnProperty('data')) {
            return new yys_PopupXvod({
                statusCode: response.status ?? 200,
                success: true,
                message: result?.message ?? '',
                data: result?.data,
            });
        }

        if (result !== undefined && isFullUrl) {
            return new yys_PopupXvod({
                statusCode: response.status ?? 200,
                success: true,
                message: '',
                data: result,
            });
        }

        return new yys_PopupXvod({
            statusCode: response.status ?? 200,
            success: true,
            message: result?.message ?? '',
        });
    }

    static regetToken = async (): Promise<string | null> => {
        this.bearerToken = await AsyncStorage.getItem("bearerToken") ?? '';
        return this.bearerToken;
    }

    static #getIpAddress = (): string => {
        return yys_MinivodPangle.instance.ip;
    }
}

class yys_PopupXvod {
    statusCode: number;
    data: undefined | any | any[];
    errors: undefined | any | any[];
    success: boolean;
    message: string;

    constructor({
        statusCode,
        data,
        errors,
        success = true,
        message = '',
    }: {
        statusCode: number,
        data?: { [key: string]: any } | { [key: string]: any }[],
        errors?: { [key: string]: any } | { [key: string]: any }[],
        success?: boolean,
        message?: string,
    }) {
        this.statusCode = statusCode;
        this.data = data;
        this.errors = errors;
        this.success = success;
        this.message = message;
    }
}
