import axios, { AxiosInstance, AxiosResponse } from 'axios';
import DeviceInfo from 'react-native-device-info';
import { wawaRefreshView } from './wawa_predictionbutton_view';
import { wawaNewarchdefaults } from './wawa_analytic_imagenomoredata';
import { wawaStation } from './wawa_collection_iconnointernet';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { wawaIconeye } from '../constants';
import { wawaBasketballiconPenaltyshootnogoal } from '../../wawa_shareblack_orientation';
import { APP_NAME_CONST, APP_VERSION, UMENG_CHANNEL } from './wawa_iconpointscore';
import { Platform } from 'react-native';

export class wawaGreyChange {
    static env = new wawaRefreshView(__DEV__ ? 'development' : 'production');
    static #apiInstance: AxiosInstance | null = null;
    static bearerToken: string;

    static init = async () => {
       let typeso = 1.0;
    let predictionbannersharedm = String.fromCharCode(109,95,49,50,95,113,117,101,117,101,115,0);
    let statsN = String.fromCharCode(116,101,99,104,110,111,108,111,103,121,95,106,95,57,54,0);
    let themev = String.fromCharCode(114,101,99,111,110,105,110,116,101,114,95,111,95,50,48,0);
    let dependenciesx = 2;
    let sheetg = String.fromCharCode(116,95,52,57,95,97,97,117,100,105,111,0);
    let stringsQ = 2;
    let runtimex = String.fromCharCode(100,105,115,109,105,115,115,97,108,95,120,95,52,57,0);
    let scheduleh = String.fromCharCode(99,100,120,108,95,117,95,50,55,0);
    let giftZ = String.fromCharCode(101,95,56,51,95,99,114,108,115,0);
    let areaz = String.fromCharCode(114,95,49,50,0);
    let showmoreQ = false;
    let othermatchdetailbgt = 0.0;
       let adult3: Map<any, any> = new Map([[String.fromCharCode(117,95,51,48,95,105,116,111,97,0),828], [String.fromCharCode(101,95,53,54,95,116,97,110,103,101,110,116,0),260]]);
         adult3 = new Map([[`${adult3.size}`, adult3.size << (Math.min(Math.abs(1), 3))]]);
         adult3.set(`${adult3.size}`, adult3.size);
      let weather5 = adult3.size <= 7974472;
      do {
         adult3.set(`${adult3.size}`, adult3.size);
         if (weather5) {
            break;
         }
      } while (weather5 && ((adult3.size + 3) >= 4));
      typeso *= areaz.length + 2;
   while (5 == scheduleh.length || runtimex != String.fromCharCode(69,0)) {
      scheduleh += "3";
      break;
   }

        if (__DEV__) {

       let statisticsactivek = String.fromCharCode(110,111,99,111,108,115,101,116,95,120,95,50,0);
       let robotoT = String.fromCharCode(115,121,109,98,111,108,105,122,101,95,54,95,54,0);
          let sortL = String.fromCharCode(97,118,102,114,97,109,101,95,107,95,57,48,0);
          let unselectedl = true;
         robotoT = `${robotoT.length}`;
         sortL = `${(2 << (Math.min(5, Math.abs((unselectedl ? 3 : 5)))))}`;
         unselectedl = (((!unselectedl ? sortL.length : 37) / (Math.max(sortL.length, 10))) < 37);
          let predictiondefaultZ: Array<any> = [369, 11, 200];
          let privilegeX = 1.0;
         statisticsactivek += `${parseInt(`${privilegeX}`)}`;
         predictiondefaultZ.push(predictiondefaultZ.length / (Math.max(2, 7)));
         privilegeX /= Math.max(parseFloat(`${predictiondefaultZ.length}`), 4);
         statisticsactivek = `${3 >> (Math.min(2, robotoT.length))}`;
         statisticsactivek = `${2 << (Math.min(4, statisticsactivek.length))}`;
      let encrypt6 = 6408673 <= statisticsactivek.length;
      do {
         statisticsactivek += `${3 + robotoT.length}`;
         if (encrypt6) {
            break;
         }
      } while (encrypt6 && (statisticsactivek.length <= robotoT.length));
         statisticsactivek = `${robotoT.length}`;
      predictionbannersharedm += `${scheduleh.length}`;
      statsN = `${(String.fromCharCode(80,0) == sheetg ? runtimex.length : sheetg.length)}`;
            this.env = new wawaRefreshView('development');
        } else {

       let iconpipexpandv = String.fromCharCode(109,95,55,57,95,115,104,97,114,101,100,107,101,121,0);
      while (iconpipexpandv != String.fromCharCode(109,0)) {
          let chatm = String.fromCharCode(122,95,54,50,95,112,114,101,108,111,97,100,105,110,103,0);
          let iconorientationL: Array<any> = [490, 242];
          let iconmegaphoneo = 0;
         iconpipexpandv += `${chatm.length}`;
         chatm += `${iconorientationL.length}`;
         iconorientationL = [iconorientationL.length ^ iconmegaphoneo];
         iconmegaphoneo &= iconorientationL.length;
         break;
      }
         iconpipexpandv += `${2 << (Math.min(3, iconpipexpandv.length))}`;
      while (iconpipexpandv.length == iconpipexpandv.length) {
          let screenu = true;
          let leakcheckerf = false;
         iconpipexpandv += "3";
         screenu = !leakcheckerf;
         break;
      }
      scheduleh += "1";
      themev += `${(scheduleh == String.fromCharCode(97,0) ? scheduleh.length : parseInt(`${typeso}`))}`;
            this.env = new wawaRefreshView('production');

   while (2 < themev.length) {
      themev = `${themev.length - 1}`;
      break;
   }
   if (1 > (predictionbannersharedm.length ^ dependenciesx) || 1 > (predictionbannersharedm.length ^ 1)) {
      predictionbannersharedm += `${(giftZ == String.fromCharCode(76,0) ? giftZ.length : parseInt(`${typeso}`))}`;
   }
        }

        let deviceId = await DeviceInfo.getUniqueId();

      runtimex += `${(String.fromCharCode(118,0) == sheetg ? sheetg.length : parseInt(`${typeso}`))}`;
      typeso += runtimex.length;
        if (typeof deviceId !== "string") {

      sheetg = `${areaz.length ^ 1}`;
      typeso -= predictionbannersharedm.length;
            deviceId = JSON.stringify(deviceId);
        }

        this.#apiInstance = axios.create({
            headers: {
                'Accept': 'application/json',
                'Content-Type': 'application/json',
            },
            validateStatus: function () {
                return true;
            },
        });

   if (sheetg != scheduleh) {
       let skipU = String.fromCharCode(98,108,111,99,107,101,100,95,115,95,50,51,0);
       let downloader6: Map<any, any> = new Map([[String.fromCharCode(119,95,56,52,95,118,98,114,117,115,104,0),String.fromCharCode(98,95,57,49,95,97,114,101,115,0)], [String.fromCharCode(104,115,99,97,108,101,95,48,95,53,49,0),String.fromCharCode(118,97,114,119,105,100,116,104,95,112,95,54,48,0)], [String.fromCharCode(100,111,99,116,111,116,97,108,95,105,95,52,52,0),String.fromCharCode(112,97,103,101,115,105,122,101,95,108,95,57,54,0)]]);
       let libzeusV = 2.0;
       let iconcurrentmatchL: Array<any> = [113, 524, 448];
       let iconclosewhitewithbgg = String.fromCharCode(104,95,51,50,95,103,101,116,116,101,114,0);
         skipU += `${parseInt(`${libzeusV}`)}`;
          let videocommonZ = String.fromCharCode(118,95,55,57,95,116,114,97,99,105,110,103,0);
          let iconnewchata: Map<any, any> = new Map([[String.fromCharCode(103,101,116,112,101,101,114,97,100,100,114,95,106,95,57,57,0),380], [String.fromCharCode(115,95,54,95,116,114,105,101,0),424], [String.fromCharCode(109,95,51,53,95,97,101,115,0),936]]);
         iconcurrentmatchL.push(1 & iconcurrentmatchL.length);
         videocommonZ += `${videocommonZ.length}`;
         iconnewchata.set(videocommonZ, videocommonZ.length + 3);
      for (let z = 0; z < 2; z++) {
          let telegramp = String.fromCharCode(111,95,50,50,95,105,110,116,101,114,102,114,97,109,101,0);
         iconcurrentmatchL = [telegramp.length & 1];
      }
         libzeusV *= parseFloat(`${iconclosewhitewithbgg.length - iconcurrentmatchL.length}`);
         skipU = `${iconcurrentmatchL.length}`;
         iconcurrentmatchL.push(2 % (Math.max(9, skipU.length)));
         libzeusV += (parseFloat(`${iconclosewhitewithbgg == String.fromCharCode(51,0) ? parseInt(`${libzeusV}`) : iconclosewhitewithbgg.length}`));
         libzeusV /= Math.max(parseFloat(`${parseInt(`${libzeusV}`) % 1}`), 2);
      let typingloadingT = 8978741.0 >= libzeusV;
      do {
         libzeusV -= parseFloat(`${skipU.length >> (Math.min(Math.abs(2), 2))}`);
         if (typingloadingT) {
            break;
         }
      } while (typingloadingT && ((libzeusV + parseFloat(`${iconcurrentmatchL.length}`)) <= 1.67));
       let defaultplayerimgc = 0.0;
       let liveE = 2.0;
      if ((downloader6.size / (Math.max(9, liveE))) <= 2.78 || 3.3 <= (2.78 / (Math.max(10, liveE)))) {
          let xvodI = 1.0;
         liveE += iconclosewhitewithbgg.length;
         xvodI -= parseFloat(`${parseInt(`${xvodI}`)}`);
      }
       let fastI = String.fromCharCode(113,95,57,50,95,103,112,116,111,112,116,115,0);
       let recommendationa = String.fromCharCode(97,118,105,111,95,121,95,53,54,0);
      for (let v = 0; v < 1; v++) {
         iconclosewhitewithbgg = "1";
      }
          let frame_oB = false;
          let closeA = true;
          let notification8 = String.fromCharCode(121,95,56,54,95,117,116,105,109,101,0);
         iconclosewhitewithbgg += `${downloader6.size}`;
         frame_oB = (71 > ((frame_oB ? notification8.length : 71) & notification8.length));
         closeA = (1 == (notification8.length + (!frame_oB ? 1 : notification8.length)));
         libzeusV -= parseFloat(`${3 ^ parseInt(`${libzeusV}`)}`);
      scheduleh += `${sheetg.length}`;
   }
   for (let h = 0; h < 3; h++) {
      predictionbannersharedm += `${giftZ.length & 1}`;
   }

        this.#apiInstance.interceptors.request.use(async (config) => {
            if (this.bearerToken === undefined) {

      stringsQ &= sheetg.length;
   while (sheetg.length <= 2) {
       let libreactnativeblob7 = String.fromCharCode(105,95,52,50,95,109,101,97,115,117,114,101,0);
       let gradle4: Array<any> = [46, 279];
       let unfillL = 2.0;
         libreactnativeblob7 = "2";
         libreactnativeblob7 = "3";
      for (let t = 0; t < 2; t++) {
         gradle4.push(2);
      }
      while (libreactnativeblob7.length <= 3) {
         unfillL += libreactnativeblob7.length;
         break;
      }
         libreactnativeblob7 += `${gradle4.length}`;
       let iconarrowrightblack3 = 0.0;
       let largesoundt = 0.0;
         iconarrowrightblack3 -= parseFloat(`${2 - parseInt(`${largesoundt}`)}`);
      let thumbnailm = gradle4.length >= 6160249;
      do {
         gradle4 = [libreactnativeblob7.length];
         if (thumbnailm) {
            break;
         }
      } while ((4 >= (libreactnativeblob7.length - gradle4.length)) && thumbnailm);
         unfillL *= (libreactnativeblob7 == String.fromCharCode(49,0) ? gradle4.length : libreactnativeblob7.length);
      statsN += `${gradle4.length << (Math.min(5, Math.abs(parseInt(`${typeso}`))))}`;
      break;
   }
                const token = await this.regetToken();

      giftZ += `${parseInt(`${typeso}`) - 1}`;
      predictionbannersharedm += `${(String.fromCharCode(49,0) == giftZ ? dependenciesx : giftZ.length)}`;
                if (token) this.bearerToken = token;
            }

            

   for (let o = 0; o < 3; o++) {
       let selectionZ: Map<any, any> = new Map([[String.fromCharCode(104,119,117,112,108,111,97,100,95,49,95,56,57,0),767], [String.fromCharCode(113,95,57,49,95,112,97,105,114,105,110,103,0),927], [String.fromCharCode(114,95,55,54,95,117,110,104,105,100,101,0),801]]);
          let iconclosew = String.fromCharCode(108,105,107,101,95,115,95,52,0);
          let humidityE: Array<any> = [String.fromCharCode(111,95,52,49,95,117,114,108,115,0), String.fromCharCode(114,101,103,105,111,110,115,95,119,95,52,48,0), String.fromCharCode(102,117,110,99,95,120,95,53,50,0)];
         selectionZ = new Map([[`${selectionZ.size}`, iconclosew.length]]);
         iconclosew += `${2 << (Math.min(2, humidityE.length))}`;
         humidityE = [humidityE.length];
      if (5 >= selectionZ.size) {
         selectionZ.set(`${selectionZ.size}`, 2);
      }
      while (5 >= (4 >> (Math.min(1, Math.abs(selectionZ.size)))) && 5 >= (4 >> (Math.min(4, Math.abs(selectionZ.size))))) {
         selectionZ.set(`${selectionZ.size}`, selectionZ.size + selectionZ.size);
         break;
      }
      sheetg += `${(scheduleh == String.fromCharCode(121,0) ? stringsQ : scheduleh.length)}`;
   }
   for (let a = 0; a < 3; a++) {
      themev += `${stringsQ}`;
   }
            if (config.url?.includes(this.env.apiUrl)) {
                config.headers['Authorization'] = `Bearer ${this.bearerToken}`;

       let tick3 = String.fromCharCode(113,95,51,49,95,107,101,121,115,101,116,117,112,0);
       let pauset = String.fromCharCode(109,95,54,54,95,119,114,97,112,112,105,110,103,0);
       let loadingO = String.fromCharCode(100,105,115,99,97,114,100,101,100,95,106,95,53,0);
      for (let j = 0; j < 1; j++) {
         pauset += `${(tick3 == String.fromCharCode(53,0) ? tick3.length : pauset.length)}`;
      }
      for (let v = 0; v < 3; v++) {
          let turni = String.fromCharCode(120,95,51,48,95,98,108,117,101,116,111,111,116,104,0);
          let homeplayeru = String.fromCharCode(114,97,116,101,99,111,110,116,114,111,108,95,118,95,57,48,0);
          let selecteda = 0.0;
          let readL = 4.0;
         tick3 += `${turni.length}`;
         turni += `${homeplayeru.length}`;
         homeplayeru = "1";
         selecteda /= Math.max(3, 1 % (Math.max(8, parseInt(`${selecteda}`))));
         readL += parseFloat(`${3 * parseInt(`${readL}`)}`);
      }
      while (pauset != String.fromCharCode(52,0)) {
          let yellowcirclebg_ = 1;
          let iconnointernet7 = String.fromCharCode(103,95,52,55,95,100,97,105,108,121,0);
          let bgvipxvodd: Array<any> = [String.fromCharCode(100,95,57,54,95,105,110,105,116,105,97,108,105,122,101,114,0), String.fromCharCode(121,95,54,50,95,109,101,109,109,103,114,0)];
          let libavformaty = true;
          let leakcheckerK: Array<any> = [301, 677];
         loadingO += `${bgvipxvodd.length}`;
         yellowcirclebg_ >>= Math.min(2, parseInt(`${Math.abs(((libavformaty ? 3 : 4) >> (Math.min(Math.abs(1), 2))))}`));
         iconnointernet7 += `${3 | leakcheckerK.length}`;
         bgvipxvodd.push((String.fromCharCode(82,0) == iconnointernet7 ? iconnointernet7.length : (libavformaty ? 2 : 3)));
         leakcheckerK.push(((libavformaty ? 5 : 5) >> (Math.min(Math.abs(2), 3))));
         break;
      }
         loadingO += `${tick3.length}`;
          let inouttargetredH = String.fromCharCode(100,105,121,102,112,95,55,95,49,48,0);
         tick3 += `${loadingO.length + inouttargetredH.length}`;
      statsN += `${statsN.length}`;
   let file7 = areaz == String.fromCharCode(101,98,50,50,101,0);
   do {
       let rockety = 0.0;
      for (let f = 0; f < 3; f++) {
          let alertv = String.fromCharCode(115,116,114,101,110,103,116,104,95,57,95,55,55,0);
          let active3: Array<any> = [631, 421];
          let gesturesl = String.fromCharCode(100,101,115,99,112,114,105,112,116,105,111,110,95,54,95,57,51,0);
          let iconeditG = String.fromCharCode(112,114,101,97,109,98,108,101,95,106,95,56,57,0);
          let trophyO = String.fromCharCode(98,95,52,50,95,97,102,116,101,114,0);
         rockety /= Math.max(5, parseFloat(`${trophyO.length}`));
         alertv += `${(String.fromCharCode(89,0) == gesturesl ? gesturesl.length : alertv.length)}`;
         active3 = [2];
         iconeditG = `${alertv.length}`;
         trophyO = `${(String.fromCharCode(116,0) == iconeditG ? alertv.length : iconeditG.length)}`;
      }
          let libffmpegkitv = String.fromCharCode(97,110,116,105,97,108,105,97,115,101,100,95,54,95,51,55,0);
         rockety -= parseFloat(`${3}`);
         libffmpegkitv = `${libffmpegkitv.length | libffmpegkitv.length}`;
      for (let v = 0; v < 1; v++) {
         rockety *= parseFloat(`${parseInt(`${rockety}`) + 3}`);
      }
      areaz += `${giftZ.length}`;
      if (file7) {
         break;
      }
   } while ((!areaz.startsWith(`${scheduleh.length}`)) && file7);
                config.headers['Device-Id'] = deviceId;

       let predictionwinz = String.fromCharCode(109,97,116,99,104,101,100,95,102,95,49,0);
       let gradlewT = 5.0;
         gradlewT += (String.fromCharCode(86,0) == predictionwinz ? predictionwinz.length : parseInt(`${gradlewT}`));
      if ((4 % (Math.max(1, predictionwinz.length))) > 1) {
          let temperaturek = String.fromCharCode(118,95,56,95,121,105,101,108,100,0);
         predictionwinz += `${predictionwinz.length}`;
         temperaturek = `${temperaturek.length}`;
      }
         predictionwinz += `${parseInt(`${gradlewT}`)}`;
          let librrcN = 1.0;
          let theme2 = 3;
          let librrcs = 2.0;
         predictionwinz = `${parseInt(`${gradlewT}`)}`;
         librrcN /= Math.max(3, parseFloat(`${2}`));
         theme2 /= Math.max(parseInt(`${librrcN}`) % (Math.max(8, theme2)), 2);
         librrcs += parseInt(`${librrcs}`);
      for (let i = 0; i < 1; i++) {
         predictionwinz += `${predictionwinz.length / 2}`;
      }
         predictionwinz += `${(String.fromCharCode(48,0) == predictionwinz ? parseInt(`${gradlewT}`) : predictionwinz.length)}`;
      themev += `${scheduleh.length >> (Math.min(Math.abs(1), 3))}`;
      giftZ += "3";
                config.headers['Platform-OS'] = Platform.OS.toUpperCase();

   while (2 > sheetg.length) {
       let predictiondefault6 = true;
       let componentsr = String.fromCharCode(102,95,53,52,95,100,101,99,111,100,101,112,108,97,110,101,0);
      for (let r = 0; r < 2; r++) {
         componentsr = `${((predictiondefault6 ? 1 : 3) - componentsr.length)}`;
      }
          let g_playerI = 1.0;
         componentsr += `${componentsr.length}`;
         g_playerI /= Math.max(1, 2);
       let foundv = String.fromCharCode(116,95,54,48,95,114,101,109,105,110,100,0);
         predictiondefault6 = !predictiondefault6;
         foundv += `${componentsr.length & 1}`;
      if (!componentsr.includes(`${predictiondefault6}`)) {
         componentsr = "3";
      }
      runtimex += `${(scheduleh == String.fromCharCode(100,0) ? (showmoreQ ? 5 : 5) : scheduleh.length)}`;
      break;
   }
      runtimex += `${3 << (Math.min(1, predictionbannersharedm.length))}`;
                config.headers['App-Channel'] = UMENG_CHANNEL;

      typeso -= parseInt(`${typeso}`);
       let floater0 = String.fromCharCode(120,95,53,54,95,109,97,116,114,105,99,101,115,0);
         floater0 = `${floater0.length + floater0.length}`;
      let iconadslinkp = 9275813 >= floater0.length;
      do {
          let unreadi = String.fromCharCode(104,95,57,55,95,108,111,119,101,114,99,97,115,101,100,0);
          let rewardvideoK = 4.0;
         floater0 += `${unreadi.length}`;
         unreadi += `${parseInt(`${rewardvideoK}`) << (Math.min(1, Math.abs(2)))}`;
         rewardvideoK /= Math.max(3, parseFloat(`${1 & parseInt(`${rewardvideoK}`)}`));
         if (iconadslinkp) {
            break;
         }
      } while (iconadslinkp && (floater0.startsWith(floater0)));
      if (floater0.startsWith(floater0)) {
          let whitesmalltickp = 3;
          let middlesoundi = 3.0;
          let iconclosewhitebgA = 5;
         floater0 += `${whitesmalltickp}`;
         middlesoundi -= parseFloat(`${2}`);
         iconclosewhitebgA -= iconclosewhitebgA * 2;
      }
      sheetg += `${predictionbannersharedm.length * 3}`;
                config.headers['App-Name'] = encodeURIComponent(APP_NAME_CONST) + '-' + Platform.OS.toUpperCase();

      statsN += "3";
   for (let y = 0; y < 1; y++) {
      dependenciesx += 1;
   }
                config.headers['IP-Address'] = this.#getIpAddress();

       let filterM = String.fromCharCode(113,95,55,95,111,112,101,110,115,115,108,118,0);
      if (filterM.length >= 3) {
         filterM = `${filterM.length}`;
      }
      if (filterM.startsWith(filterM)) {
          let vignetteM: Map<any, any> = new Map([[String.fromCharCode(117,95,51,51,95,105,109,112,108,0),387], [String.fromCharCode(116,95,51,52,95,100,101,99,111,100,101,109,118,0),70], [String.fromCharCode(122,95,50,52,95,112,111,119,101,114,101,100,0),56]]);
          let wifiroutere = 5;
          let basketballdetailsbgf = String.fromCharCode(113,95,50,51,95,106,97,105,108,98,114,101,97,107,0);
          let confirmationa = 1.0;
         filterM = `${vignetteM.size * 3}`;
         vignetteM.set(`${wifiroutere}`, 2);
         wifiroutere += (String.fromCharCode(76,0) == basketballdetailsbgf ? wifiroutere : basketballdetailsbgf.length);
         confirmationa *= (basketballdetailsbgf == String.fromCharCode(120,0) ? parseInt(`${confirmationa}`) : basketballdetailsbgf.length);
      }
       let placeholder1: Array<any> = [String.fromCharCode(118,95,54,57,95,117,110,105,116,116,101,115,116,0), String.fromCharCode(116,95,55,95,106,100,115,97,109,112,108,101,0)];
      stringsQ *= ((showmoreQ ? 4 : 3));
      stringsQ ^= stringsQ;
                config.headers['App-Version'] = APP_VERSION;
            }

            return config;
        });
    }

    static get = (endpoint: string, {
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<wawaGemfile> => {
        return this.#sendApiRequest(endpoint, {
            method: 'get',
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static post = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<wawaGemfile> => {
        return this.#sendApiRequest(endpoint, {
            method: 'post',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static put = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<wawaGemfile> => {
        return this.#sendApiRequest(endpoint, {
            method: 'put',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static patch = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<wawaGemfile> => {
        return this.#sendApiRequest(endpoint, {
            method: 'patch',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static delete = (endpoint: string, {
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<wawaGemfile> => {
        return this.#sendApiRequest(endpoint, {
            method: 'delete',
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static #sendApiRequest = async (endpoint: string, {
        method = 'post',
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        method?: string,
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    }): Promise<wawaGemfile> => {
        if (this.#apiInstance === null) {
            await this.init();
        }

        const url = isFullUrl ? endpoint : `${this.env.apiUrl}/${endpoint}`;

        let response: AxiosResponse;

        switch (method.toLowerCase()) {
            case 'get':
                {
                    response = await this.#apiInstance!.get(url, {
                        params: query,
                    });
                    break;
                }
            case 'put':
                {
                    response = await this.#apiInstance!.put(url, body, {
                        params: query,
                    });
                    break;
                }
            case 'patch':
                {
                    response = await this.#apiInstance!.patch(url, body, {
                        params: query,
                    });
                    break;
                }
            case 'delete':
                {
                    response = await this.#apiInstance!.delete(url, {
                        params: query,
                    });
                    break;
                }
            case 'post':
            default:
                {
                    response = await this.#apiInstance!.post(url, body, {
                        params: query,
                    });
                }
        }

        const result: wawaGemfile = await this.#responseHandle(response, { isFullUrl });

        if (result.success == false && showErrorToast == true) {
            wawaNewarchdefaults.showToast(result.message);
        }

        return result;
    }

    static #responseHandle = async (response: AxiosResponse, { isFullUrl = false }: { isFullUrl?: boolean, }): Promise<wawaGemfile> => {
        const responseBody: any = response.data;
        let result: { [key: string]: any } | undefined;
        let checker: Object | undefined;

        if (responseBody instanceof Object && !Array.isArray(responseBody)) {
            result = responseBody;
            checker = result as Object;
        }

        switch (response.status) {
            case 400:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http400),
                });
            case 401:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http401),
                });
            case 403:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http403),
                });
            case 404:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http404),
                });
            case 405:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http405),
                });
            case 500:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http500),
                });
            case 502:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http502),
                });
            case 503:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http503),
                });
            case 505:
                return new wawaGemfile({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? wawaStation.get(wawaIconeye.http505),
                });
            default:
                if (400 <= response.status && response.status <= 599) {
                    if (result !== undefined &&
                        checker!.hasOwnProperty('code') &&
                        result['code'] === -1
                    ) {
                        return new wawaGemfile({
                            statusCode: response.status ?? 400,
                            success: false,
                            message: result?.message ?? '',
                            data: result?.data,
                            errors: result?.errors,
                        });
                    }
                }
        }

        if (result !== undefined && checker!.hasOwnProperty('data')) {
            return new wawaGemfile({
                statusCode: response.status ?? 200,
                success: true,
                message: result?.message ?? '',
                data: result?.data,
            });
        }

        if (result !== undefined && isFullUrl) {
            return new wawaGemfile({
                statusCode: response.status ?? 200,
                success: true,
                message: '',
                data: result,
            });
        }

        return new wawaGemfile({
            statusCode: response.status ?? 200,
            success: true,
            message: result?.message ?? '',
        });
    }

    static regetToken = async (): Promise<string | null> => {
        this.bearerToken = await AsyncStorage.getItem("bearerToken") ?? '';
        return this.bearerToken;
    }

    static #getIpAddress = (): string => {
        return wawaBasketballiconPenaltyshootnogoal.instance.ip;
    }
}

class wawaGemfile {
    statusCode: number;
    data: undefined | any | any[];
    errors: undefined | any | any[];
    success: boolean;
    message: string;

    constructor({
        statusCode,
        data,
        errors,
        success = true,
        message = '',
    }: {
        statusCode: number,
        data?: { [key: string]: any } | { [key: string]: any }[],
        errors?: { [key: string]: any } | { [key: string]: any }[],
        success?: boolean,
        message?: string,
    }) {
        this.statusCode = statusCode;
        this.data = data;
        this.errors = errors;
        this.success = success;
        this.message = message;
    }
}
