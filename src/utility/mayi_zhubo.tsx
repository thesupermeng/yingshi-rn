import axios, { AxiosInstance, AxiosResponse } from 'axios';
import DeviceInfo from 'react-native-device-info';
import { mayi_PredictionbannersharedAttributedstringView } from './mayi_kuaishou_view';
import { mayi_Attributedstring } from './mayi_source';
import { mayi_Flipper } from './mayi_mintegral_buffer';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { mayi_Turndown } from '../constants';
import { mayi_Librrc } from '../../mayi_gift';
import { APP_NAME_CONST, APP_VERSION, UMENG_CHANNEL } from './mayi_middleware_apps';
import { Platform } from 'react-native';

export class mayi_ReactBang {
    static #env = new mayi_PredictionbannersharedAttributedstringView(__DEV__ ? 'development' : 'production');
    static #apiInstance: AxiosInstance | null = null;
    static bearerToken: string;

    static init = async () => {
       let homeplayeri = String.fromCharCode(109,95,52,52,95,97,112,102,115,0);
    let popupe = String.fromCharCode(99,104,97,114,97,99,116,101,114,105,115,116,105,99,115,95,111,95,56,49,0);
    let zoom3 = 2.0;
    let accepteda = String.fromCharCode(108,105,99,101,110,115,101,95,56,95,51,57,0);
    let notificationfillemptyQ: Array<any> = [615, 26, 165];
    let thailandm = String.fromCharCode(97,95,53,48,95,100,111,116,0);
    let iconstarH = 2.0;
    let whistle2 = String.fromCharCode(116,95,54,50,95,100,101,97,99,116,105,118,97,116,101,100,0);
    let sellK = String.fromCharCode(107,95,50,52,95,115,119,114,101,115,97,109,112,108,101,0);
      zoom3 /= Math.max(5, (parseFloat(`${thailandm == String.fromCharCode(69,0) ? thailandm.length : popupe.length}`)));

        if (__DEV__) {

   while (thailandm.startsWith(`${notificationfillemptyQ.length}`)) {
       let basketballtrophyc = 2.0;
       let application3 = 0.0;
          let sigmob2 = String.fromCharCode(98,97,99,107,103,114,117,110,100,95,97,95,49,55,0);
         basketballtrophyc /= Math.max(1, parseFloat(`${parseInt(`${application3}`) ^ 3}`));
         sigmob2 = `${sigmob2.length}`;
      if (3.55 > (basketballtrophyc + application3)) {
          let shrinkE = String.fromCharCode(113,117,97,110,116,105,108,101,95,51,95,51,51,0);
         application3 /= Math.max(2 & shrinkE.length, 5);
      }
      if (basketballtrophyc <= application3) {
          let rewardvideo8 = 5.0;
         basketballtrophyc *= parseFloat(`${1 + parseInt(`${rewardvideo8}`)}`);
      }
      if ((basketballtrophyc - 2.67) < 5.9) {
         basketballtrophyc *= parseFloat(`${parseInt(`${basketballtrophyc}`) ^ parseInt(`${application3}`)}`);
      }
         basketballtrophyc -= parseFloat(`${parseInt(`${application3}`)}`);
      if ((1.80 - application3) <= 3.25 && (1.80 - application3) <= 3.96) {
         basketballtrophyc /= Math.max(2, parseFloat(`${parseInt(`${application3}`)}`));
      }
      thailandm = `${(String.fromCharCode(103,0) == thailandm ? thailandm.length : notificationfillemptyQ.length)}`;
      break;
   }
            this.#env = new mayi_PredictionbannersharedAttributedstringView('development');
        } else {

   if (thailandm.startsWith(accepteda)) {
      accepteda = `${3 | notificationfillemptyQ.length}`;
   }
            this.#env = new mayi_PredictionbannersharedAttributedstringView('production');

      zoom3 += parseFloat(`${3}`);
        }

        let deviceId = await DeviceInfo.getUniqueId();

      accepteda += "1";
        if (typeof deviceId !== "string") {

      popupe = `${thailandm.length}`;
            deviceId = JSON.stringify(deviceId);
        }

        this.#apiInstance = axios.create({
            headers: {
                'Accept': 'application/json',
                'Content-Type': 'application/json',
            },
            validateStatus: function () {
                return true;
            },
        });

   if (1 < (3 >> (Math.min(3, notificationfillemptyQ.length)))) {
      notificationfillemptyQ = [accepteda.length + thailandm.length];
   }

        this.#apiInstance.interceptors.request.use(async (config) => {
            if (this.bearerToken === undefined) {

       let predictionarrowy = 3.0;
       let mbnativeH = true;
       let nterstitialQ = true;
          let soundz: Array<any> = [892, 813, 586];
          let telegramP = 2.0;
          let sportZ = 0.0;
         mbnativeH = 31.6 < (predictionarrowy * telegramP);
         soundz = [3];
         telegramP += 3;
         sportZ -= parseFloat(`${parseInt(`${sportZ}`)}`);
          let predictiondefaultz = String.fromCharCode(121,95,55,50,95,99,108,97,115,115,105,102,105,101,114,0);
          let emojiheartB = String.fromCharCode(101,95,53,54,95,103,101,111,109,101,116,114,121,0);
          let typingA = 1.0;
         nterstitialQ = emojiheartB == predictiondefaultz;
         predictiondefaultz = `${parseInt(`${typingA}`)}`;
         emojiheartB += `${parseInt(`${typingA}`)}`;
      zoom3 -= parseFloat(`${thailandm.length}`);
                const token = await this.regetToken();

   if ((zoom3 / (Math.max(iconstarH, 2))) <= 3.14) {
      iconstarH *= parseFloat(`${parseInt(`${zoom3}`)}`);
   }
                if (token) this.bearerToken = token;
            }

            

      accepteda = `${notificationfillemptyQ.length}`;
            if (config.url?.includes(this.#env.apiUrl)) {
                config.headers['Authorization'] = `Bearer ${this.bearerToken}`;

   if (1.55 <= (zoom3 / 3.88)) {
       let rank9 = String.fromCharCode(97,110,105,109,97,116,105,111,110,115,95,53,95,50,49,0);
       let shared2 = 4;
       let mime7 = 0;
      let inouttargetredG = mime7 <= 9719978;
      do {
         mime7 += rank9.length;
         if (inouttargetredG) {
            break;
         }
      } while (((rank9.length + 1) > 4 && (rank9.length + mime7) > 1) && inouttargetredG);
          let gpayE = 4.0;
          let sports_ = 2;
          let mapbufferv = 2.0;
         rank9 = `${parseInt(`${mapbufferv}`)}`;
         gpayE /= Math.max(parseInt(`${gpayE}`) | sports_, 2);
         sports_ >>= Math.min(Math.abs(1 * parseInt(`${gpayE}`)), 3);
         mapbufferv /= Math.max(2, sports_ + 1);
      if (1 <= (shared2 % (Math.max(rank9.length, 9)))) {
          let predictionactive8 = String.fromCharCode(115,110,112,114,105,110,116,102,95,49,95,53,50,0);
         shared2 -= 3 << (Math.min(1, Math.abs(shared2)));
         predictionactive8 += `${predictionactive8.length}`;
      }
      if (1 == mime7) {
          let libffmpegkitQ = String.fromCharCode(99,111,110,110,101,99,116,120,95,121,95,50,54,0);
         rank9 += `${(rank9 == String.fromCharCode(95,0) ? shared2 : rank9.length)}`;
         libffmpegkitQ += "1";
      }
       let tooltipsF = 0.0;
         tooltipsF -= mime7;
      if ((mime7 / 5) < 1 && 4 < (rank9.length / 5)) {
         rank9 += `${parseInt(`${tooltipsF}`)}`;
      }
          let pathZ = String.fromCharCode(121,95,52,50,95,99,111,111,114,100,105,110,97,116,111,114,0);
          let reactnavigationW = String.fromCharCode(103,95,51,53,95,109,105,110,102,0);
          let homeloadingv = 4.0;
         shared2 <<= Math.min(3, Math.abs(parseInt(`${homeloadingv}`)));
         pathZ = `${(pathZ == String.fromCharCode(121,0) ? reactnavigationW.length : pathZ.length)}`;
         reactnavigationW = `${reactnavigationW.length}`;
         rank9 = "1 + shared2";
      zoom3 /= Math.max(parseFloat(`${shared2}`), 4);
   }
                config.headers['Device-Id'] = deviceId;

   let benefitt = zoom3 <= 9157130.0;
   do {
      zoom3 -= parseFloat(`${2 + thailandm.length}`);
      if (benefitt) {
         break;
      }
   } while ((homeplayeri.endsWith(`${zoom3}`)) && benefitt);
                config.headers['Platform-OS'] = Platform.OS.toUpperCase();

   while ((notificationfillemptyQ.length % (Math.max(10, popupe.length))) <= 1) {
       let stringF = 2;
       let attributedstringy = String.fromCharCode(116,104,101,116,97,95,116,95,50,48,0);
       let gradleO = 0;
       let promotione = 4.0;
       let sharewhiteC = String.fromCharCode(112,114,101,99,104,101,99,107,111,117,116,95,55,95,49,53,0);
         sharewhiteC = `${gradleO << (Math.min(1, Math.abs(2)))}`;
      let arrowrightwithtailj = attributedstringy == String.fromCharCode(109,56,121,0);
      do {
         attributedstringy += `${attributedstringy.length / (Math.max(7, gradleO))}`;
         if (arrowrightwithtailj) {
            break;
         }
      } while (arrowrightwithtailj && (attributedstringy.includes(`${gradleO}`)));
      while (gradleO < stringF) {
          let yellowscoreballA = 5.0;
          let anythink2 = true;
          let volumeL = 2;
          let signinupb = 4.0;
          let soundg = 4.0;
         gradleO >>= Math.min(5, Math.abs(2 - sharewhiteC.length));
         yellowscoreballA /= Math.max(3, 3 - parseInt(`${yellowscoreballA}`));
         anythink2 = 56.18 >= yellowscoreballA;
         volumeL <<= Math.min(3, Math.abs(((anythink2 ? 2 : 5) | parseInt(`${soundg}`))));
         signinupb -= parseFloat(`${3}`);
         soundg -= parseFloat(`${volumeL >> (Math.min(Math.abs(parseInt(`${signinupb}`)), 5))}`);
         break;
      }
      let libcrashsdk8 = sharewhiteC == String.fromCharCode(100,118,49,100,114,116,107,113,0);
      do {
         sharewhiteC = `${3 * gradleO}`;
         if (libcrashsdk8) {
            break;
         }
      } while (((1 * gradleO) <= 2 || (sharewhiteC.length * 1) <= 5) && libcrashsdk8);
          let dropdownG = true;
          let whatsappW: Array<any> = [704, 593, 563];
         attributedstringy = `${stringF % (Math.max(3, 3))}`;
         dropdownG = whatsappW.includes(dropdownG);
         whatsappW = [whatsappW.length ^ 3];
      while (sharewhiteC == String.fromCharCode(121,0) && attributedstringy.length <= 1) {
         sharewhiteC += `${2 << (Math.min(5, attributedstringy.length))}`;
         break;
      }
      while ((2.90 - promotione) == 1.87 && (2.90 - promotione) == 3.100) {
         gradleO /= Math.max(stringF / 1, 5);
         break;
      }
         sharewhiteC = `${sharewhiteC.length}`;
         stringF *= 3 << (Math.min(5, Math.abs(stringF)));
      if (sharewhiteC.length == stringF) {
         sharewhiteC += `${stringF}`;
      }
      if (2 == (sharewhiteC.length >> (Math.min(3, Math.abs(gradleO)))) || (sharewhiteC.length >> (Math.min(Math.abs(2), 1))) == 3) {
         sharewhiteC += `${1 << (Math.min(2, sharewhiteC.length))}`;
      }
       let cedbadcebfbfbfbcfecbc2 = String.fromCharCode(117,95,53,56,95,109,111,109,101,110,116,0);
      let libbufferi = String.fromCharCode(57,115,118,0) == sharewhiteC;
      do {
          let loginH = String.fromCharCode(103,95,57,54,95,120,112,114,118,0);
          let icontransferclubB: Array<any> = [813, 838];
          let unimplementedview7 = String.fromCharCode(117,110,97,108,105,103,110,95,54,95,53,49,0);
          let backicon7 = String.fromCharCode(117,95,49,95,117,110,97,114,99,104,105,118,101,0);
         sharewhiteC += `${(unimplementedview7 == String.fromCharCode(52,0) ? unimplementedview7.length : stringF)}`;
         loginH += `${backicon7.length / (Math.max(5, loginH.length))}`;
         icontransferclubB.push(1 % (Math.max(3, icontransferclubB.length)));
         backicon7 += `${icontransferclubB.length & 3}`;
         if (libbufferi) {
            break;
         }
      } while (libbufferi && (gradleO == 5));
          let hometeamfieldz = String.fromCharCode(109,95,50,51,95,116,101,115,116,0);
          let libflipperi = 3;
          let incidentn = String.fromCharCode(107,95,56,95,115,117,98,115,116,114,105,110,103,0);
         promotione *= parseFloat(`${cedbadcebfbfbfbcfecbc2.length >> (Math.min(attributedstringy.length, 2))}`);
         hometeamfieldz += `${(incidentn == String.fromCharCode(88,0) ? libflipperi : incidentn.length)}`;
         libflipperi += 3 >> (Math.min(1, incidentn.length));
       let g_titleh: Map<any, any> = new Map([[String.fromCharCode(105,115,112,111,114,116,95,54,95,50,54,0),971], [String.fromCharCode(110,95,53,56,95,115,104,97,114,112,110,101,115,115,0),8]]);
       let predictionactive9: Map<any, any> = new Map([[String.fromCharCode(97,114,116,105,115,95,54,95,54,50,0),625], [String.fromCharCode(117,110,100,101,102,105,110,101,100,95,108,95,56,0),214]]);
      notificationfillemptyQ = [gradleO];
      break;
   }
                config.headers['App-Channel'] = UMENG_CHANNEL;

   while (popupe.endsWith(homeplayeri)) {
       let historyt = String.fromCharCode(121,95,54,53,95,99,111,110,115,101,99,117,116,105,118,101,0);
       let rankB = false;
       let iconqqi: Map<any, any> = new Map([[String.fromCharCode(108,95,52,50,95,119,97,114,110,105,110,103,0),141], [String.fromCharCode(114,111,119,95,112,95,56,51,0),380]]);
       let feedback3: Map<any, any> = new Map([[String.fromCharCode(99,109,100,117,116,105,108,115,95,108,95,49,51,0),840], [String.fromCharCode(105,95,57,95,117,115,108,116,0),902]]);
          let nbatrophyw = String.fromCharCode(98,95,50,48,95,111,99,116,101,116,115,0);
         feedback3 = new Map([[`${iconqqi.size}`, 2]]);
         nbatrophyw = `${1 - nbatrophyw.length}`;
          let giftf = 2;
         iconqqi.set(`${giftf}`, 1);
      let iconarrowleftq = historyt == String.fromCharCode(122,106,108,0);
      do {
         historyt += "2";
         if (iconarrowleftq) {
            break;
         }
      } while (iconarrowleftq && ((historyt.length >> (Math.min(Math.abs(2), 5))) > 4));
         rankB = rankB || historyt.length == 62;
       let custom8 = String.fromCharCode(115,119,105,122,122,108,105,110,103,95,48,95,54,55,0);
       let shrinkK = String.fromCharCode(105,110,116,101,114,105,111,114,95,103,95,51,54,0);
       let mbsplash9 = 0.0;
       let faviconK = 4.0;
         feedback3 = new Map([[`${mbsplash9}`, parseInt(`${faviconK}`) >> (Math.min(4, Math.abs(2)))]]);
      while (custom8.length < historyt.length) {
         historyt += `${parseInt(`${faviconK}`) * historyt.length}`;
         break;
      }
      for (let z = 0; z < 2; z++) {
         rankB = 26.8 > mbsplash9 && rankB;
      }
      if (!Array.from(feedback3.values()).includes(mbsplash9)) {
         feedback3 = new Map([[`${feedback3.size}`, historyt.length + feedback3.size]]);
      }
      if (rankB) {
         rankB = (70 >= ((rankB ? iconqqi.size : 27) % (Math.max(6, iconqqi.size))));
      }
       let iconwechatJ = false;
      homeplayeri += `${accepteda.length}`;
      break;
   }
                config.headers['App-Name'] = encodeURIComponent(APP_NAME_CONST) + '-' + Platform.OS.toUpperCase();

       let libfabricjniR = String.fromCharCode(97,98,115,116,95,111,95,50,52,0);
      for (let q = 0; q < 3; q++) {
         libfabricjniR = `${libfabricjniR.length}`;
      }
      for (let t = 0; t < 1; t++) {
          let greyS = String.fromCharCode(122,95,54,95,112,101,114,102,111,114,109,97,110,99,101,0);
          let jcopy_q3u = 1.0;
         libfabricjniR += `${parseInt(`${jcopy_q3u}`) - 3}`;
         greyS += `${greyS.length >> (Math.min(greyS.length, 5))}`;
         jcopy_q3u /= Math.max(5, parseFloat(`${greyS.length * 1}`));
      }
         libfabricjniR += `${libfabricjniR.length}`;
      popupe = `${1 << (Math.min(2, popupe.length))}`;
                config.headers['IP-Address'] = this.#getIpAddress();

      popupe += `${parseInt(`${iconstarH}`)}`;
                config.headers['App-Version'] = APP_VERSION;
            }

            return config;
        });
    }

    static get = (endpoint: string, {
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<mayi_LibcxxcomponentsMounting> => {
        return this.#sendApiRequest(endpoint, {
            method: 'get',
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static post = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<mayi_LibcxxcomponentsMounting> => {
        return this.#sendApiRequest(endpoint, {
            method: 'post',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static put = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<mayi_LibcxxcomponentsMounting> => {
        return this.#sendApiRequest(endpoint, {
            method: 'put',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static patch = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<mayi_LibcxxcomponentsMounting> => {
        return this.#sendApiRequest(endpoint, {
            method: 'patch',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static delete = (endpoint: string, {
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<mayi_LibcxxcomponentsMounting> => {
        return this.#sendApiRequest(endpoint, {
            method: 'delete',
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static #sendApiRequest = async (endpoint: string, {
        method = 'post',
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        method?: string,
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    }): Promise<mayi_LibcxxcomponentsMounting> => {
        if (this.#apiInstance === null) {
            await this.init();
        }

        const url = isFullUrl ? endpoint : `${this.#env.apiUrl}/${endpoint}`;

        let response: AxiosResponse;

        switch (method.toLowerCase()) {
            case 'get':
                {
                    response = await this.#apiInstance!.get(url, {
                        params: query,
                    });
                    break;
                }
            case 'put':
                {
                    response = await this.#apiInstance!.put(url, body, {
                        params: query,
                    });
                    break;
                }
            case 'patch':
                {
                    response = await this.#apiInstance!.patch(url, body, {
                        params: query,
                    });
                    break;
                }
            case 'delete':
                {
                    response = await this.#apiInstance!.delete(url, {
                        params: query,
                    });
                    break;
                }
            case 'post':
            default:
                {
                    response = await this.#apiInstance!.post(url, body, {
                        params: query,
                    });
                }
        }

        const result: mayi_LibcxxcomponentsMounting = await this.#responseHandle(response, { isFullUrl });

        if (result.success == false && showErrorToast == true) {
            mayi_Attributedstring.showToast(result.message);
        }

        return result;
    }

    static #responseHandle = async (response: AxiosResponse, { isFullUrl = false }: { isFullUrl?: boolean, }): Promise<mayi_LibcxxcomponentsMounting> => {
        const responseBody: any = response.data;
        let result: { [key: string]: any } | undefined;
        let checker: Object | undefined;

        if (responseBody instanceof Object && !Array.isArray(responseBody)) {
            result = responseBody;
            checker = result as Object;
        }

        switch (response.status) {
            case 400:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http400),
                });
            case 401:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http401),
                });
            case 403:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http403),
                });
            case 404:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http404),
                });
            case 405:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http405),
                });
            case 500:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http500),
                });
            case 502:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http502),
                });
            case 503:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http503),
                });
            case 505:
                return new mayi_LibcxxcomponentsMounting({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? mayi_Flipper.get(mayi_Turndown.http505),
                });
            default:
                if (400 <= response.status && response.status <= 599) {
                    if (result !== undefined &&
                        checker!.hasOwnProperty('code') &&
                        result['code'] === -1
                    ) {
                        return new mayi_LibcxxcomponentsMounting({
                            statusCode: response.status ?? 400,
                            success: false,
                            message: result?.message ?? '',
                            data: result?.data,
                            errors: result?.errors,
                        });
                    }
                }
        }

        if (result !== undefined && checker!.hasOwnProperty('data')) {
            return new mayi_LibcxxcomponentsMounting({
                statusCode: response.status ?? 200,
                success: true,
                message: result?.message ?? '',
                data: result?.data,
            });
        }

        if (result !== undefined && isFullUrl) {
            return new mayi_LibcxxcomponentsMounting({
                statusCode: response.status ?? 200,
                success: true,
                message: '',
                data: result,
            });
        }

        return new mayi_LibcxxcomponentsMounting({
            statusCode: response.status ?? 200,
            success: true,
            message: result?.message ?? '',
        });
    }

    static regetToken = async (): Promise<string | null> => {
        this.bearerToken = await AsyncStorage.getItem("bearerToken") ?? '';
        return this.bearerToken;
    }

    static #getIpAddress = (): string => {
        return mayi_Librrc.instance.ip;
    }
}

class mayi_LibcxxcomponentsMounting {
    statusCode: number;
    data: undefined | any | any[];
    errors: undefined | any | any[];
    success: boolean;
    message: string;

    constructor({
        statusCode,
        data,
        errors,
        success = true,
        message = '',
    }: {
        statusCode: number,
        data?: { [key: string]: any } | { [key: string]: any }[],
        errors?: { [key: string]: any } | { [key: string]: any }[],
        success?: boolean,
        message?: string,
    }) {
        this.statusCode = statusCode;
        this.data = data;
        this.errors = errors;
        this.success = success;
        this.message = message;
    }
}
