import axios, { AxiosInstance, AxiosResponse } from 'axios';
import DeviceInfo from 'react-native-device-info';
import { yysScheduleChartView } from './yys_baiduads_eighteen_view';
import { yysHomeinactiveUntick } from './yys_vietnam';
import { yysAnimationsMalaysia } from './yys_iconorientation';
import AsyncStorage from '@react-native-async-storage/async-storage';
import { yysResultSport } from '../constants';
import { yysFloaterIconsubssuccess } from '../../yys_yellowscoreball_topon';
import { APP_NAME_CONST, APP_VERSION, UMENG_CHANNEL } from './yys_libffmpegkit';
import { Platform } from 'react-native';

export class yysConfig {
    static #env = new yysScheduleChartView(__DEV__ ? 'development' : 'production');
    static #apiInstance: AxiosInstance | null = null;
    static bearerToken: string;

    static init = async () => {
       let volumex = false;
    let casting8: Array<any> = [172, 749];
    let backwhite1 = 2.0;
    let backiconmaskR = String.fromCharCode(102,95,50,48,95,101,115,116,105,109,97,116,105,110,103,0);
    let assist2 = 0.0;
    let giflivestreamingC = 4.0;
    let readO = 0.0;
    let iconstarE = 3.0;
      assist2 *= parseInt(`${backwhite1}`) << (Math.min(casting8.length, 1));

        if (__DEV__) {

      casting8.push(2 % (Math.max(9, backiconmaskR.length)));
            this.#env = new yysScheduleChartView('development');
        } else {

       let profileframez = String.fromCharCode(99,95,52,49,95,97,117,100,105,111,100,115,112,0);
       let edit8 = String.fromCharCode(97,95,52,56,95,122,101,114,111,99,111,100,101,99,0);
      for (let v = 0; v < 3; v++) {
         edit8 = `${edit8.length}`;
      }
          let libavcodec3 = false;
         edit8 = "2";
         libavcodec3 = !libavcodec3;
      let privatechatbgI = profileframez.length >= 6456982;
      do {
          let chinaQ = false;
          let emojid = String.fromCharCode(122,95,53,51,95,100,101,116,101,99,116,105,111,110,0);
          let modulesk = 0.0;
          let modules6 = String.fromCharCode(99,111,110,115,101,99,117,116,105,118,101,95,104,95,54,52,0);
          let abidetectY: Map<any, any> = new Map([[String.fromCharCode(119,114,105,116,101,95,110,95,49,48,0),148], [String.fromCharCode(112,114,105,109,95,54,95,51,0),325]]);
         profileframez += `${2 - parseInt(`${modulesk}`)}`;
         chinaQ = String.fromCharCode(51,0) == modules6;
         emojid += `${modules6.length | abidetectY.size}`;
         modulesk += (parseFloat(`${String.fromCharCode(69,0) == modules6 ? modules6.length : (chinaQ ? 5 : 2)}`));
         abidetectY = new Map([[`${abidetectY.size}`, emojid.length + abidetectY.size]]);
         if (privatechatbgI) {
            break;
         }
      } while (privatechatbgI && (profileframez.length <= edit8.length));
      while (edit8.length > 4 && profileframez.length > 4) {
         profileframez += `${2 ^ edit8.length}`;
         break;
      }
          let subbasketballplayerU = String.fromCharCode(110,95,51,53,95,97,117,116,104,101,110,116,105,99,97,116,101,0);
          let promotionS = 5.0;
          let giftbuttonH = String.fromCharCode(114,101,99,111,110,110,101,99,116,95,57,95,57,49,0);
         edit8 += `${(profileframez == String.fromCharCode(56,0) ? profileframez.length : edit8.length)}`;
         subbasketballplayerU += `${giftbuttonH.length}`;
         promotionS += parseFloat(`${giftbuttonH.length | parseInt(`${promotionS}`)}`);
         profileframez = `${profileframez.length >> (Math.min(5, edit8.length))}`;
      assist2 *= (String.fromCharCode(51,0) == backiconmaskR ? backiconmaskR.length : (volumex ? 3 : 3));
            this.#env = new yysScheduleChartView('production');

   for (let p = 0; p < 3; p++) {
      readO += casting8.length;
   }
        }

        let deviceId = await DeviceInfo.getUniqueId();

   for (let d = 0; d < 3; d++) {
      volumex = volumex || giflivestreamingC == 59.26;
   }
        if (typeof deviceId !== "string") {

       let libfabricjniW = true;
       let targetk = 3.0;
       let owngoal1: Array<any> = [383, 188];
      if (5 <= (owngoal1.length & 1)) {
          let bellE = 2.0;
          let expand7 = 1.0;
          let mintegralf = 3;
         libfabricjniW = owngoal1.length == 68;
         bellE += parseFloat(`${3}`);
         expand7 += parseFloat(`${mintegralf << (Math.min(Math.abs(parseInt(`${expand7}`)), 1))}`);
         mintegralf %= Math.max(4, 3 / (Math.max(8, parseInt(`${expand7}`))));
      }
      let membershipk = libfabricjniW ? !libfabricjniW : libfabricjniW;
      do {
         libfabricjniW = !libfabricjniW;
         if (membershipk) {
            break;
         }
      } while (membershipk && (!libfabricjniW));
      if ((3 / (Math.max(10, owngoal1.length))) > 1) {
         owngoal1 = [3];
      }
          let form6 = 5;
          let libapminsightaH: Array<any> = [699, 512, 110];
         libfabricjniW = targetk < 99.67;
         form6 -= form6;
         libapminsightaH.push(libapminsightaH.length & form6);
      let libfollyg = 6519017.0 >= targetk;
      do {
         targetk -= parseInt(`${targetk}`) & 3;
         if (libfollyg) {
            break;
         }
      } while (libfollyg && ((2 ^ owngoal1.length) < 2));
      if (libfabricjniW) {
          let mode3 = 3.0;
          let attributedstringv = 2.0;
          let libtob1: Array<any> = [159, 18, 617];
          let other7: Map<any, any> = new Map([[String.fromCharCode(99,95,53,49,95,114,102,99,116,0),167], [String.fromCharCode(112,95,49,53,95,115,105,114,105,0),870]]);
         libfabricjniW = attributedstringv > libtob1.length;
         mode3 += parseFloat(`${1}`);
         attributedstringv *= other7.size;
         libtob1.push(other7.size << (Math.min(Math.abs(1), 1)));
      }
         libfabricjniW = parseInt(`${targetk}`) < owngoal1.length;
      for (let k = 0; k < 3; k++) {
         targetk -= owngoal1.length << (Math.min(Math.abs(2), 3));
      }
          let custom7 = 3.0;
          let fullJ = 3.0;
          let feedbackO = 3;
         targetk /= Math.max(5, parseInt(`${custom7}`));
         fullJ += parseFloat(`${feedbackO >> (Math.min(Math.abs(parseInt(`${fullJ}`)), 1))}`);
         feedbackO %= Math.max(1, feedbackO);
      volumex = 40.20 < backwhite1 && 40.20 < assist2;
            deviceId = JSON.stringify(deviceId);
        }

        this.#apiInstance = axios.create({
            headers: {
                'Accept': 'application/json',
                'Content-Type': 'application/json',
            },
            validateStatus: function () {
                return true;
            },
        });

   for (let n = 0; n < 3; n++) {
      casting8 = [backiconmaskR.length ^ 1];
   }

        this.#apiInstance.interceptors.request.use(async (config) => {
            if (this.bearerToken === undefined) {

   for (let b = 0; b < 1; b++) {
       let dist3: Map<any, any> = new Map([[String.fromCharCode(105,95,55,51,95,97,99,116,105,118,101,0),132], [String.fromCharCode(105,110,116,114,101,97,100,119,114,105,116,101,95,120,95,57,55,0),873]]);
       let plusT = String.fromCharCode(108,95,53,51,95,97,110,116,105,97,108,105,97,115,101,100,0);
       let predictionarrowT: Array<any> = [String.fromCharCode(98,95,55,52,95,105,110,102,0), String.fromCharCode(113,95,53,95,98,115,116,114,97,99,116,0)];
       let searchbark = String.fromCharCode(99,111,102,102,105,110,95,52,95,51,52,0);
         searchbark = `${searchbark.length << (Math.min(3, plusT.length))}`;
          let subtextb = 5;
         searchbark += `${searchbark.length * plusT.length}`;
         subtextb %= Math.max(subtextb, 3);
      let dangerP = predictionarrowT.length >= 7297406;
      do {
         predictionarrowT = [3 | searchbark.length];
         if (dangerP) {
            break;
         }
      } while (dangerP && ((predictionarrowT.length ^ dist3.size) <= 2 || 2 <= (predictionarrowT.length ^ dist3.size)));
      if (2 <= (dist3.size * plusT.length)) {
          let audiences = true;
          let signinupJ = true;
          let grey5: Array<any> = [561, 616, 297];
          let arrowdownQ = 0.0;
         plusT = `${parseInt(`${arrowdownQ}`) >> (Math.min(plusT.length, 1))}`;
         audiences = (signinupJ ? audiences : signinupJ);
         grey5.push((grey5.length << (Math.min(1, Math.abs((signinupJ ? 3 : 2))))));
         arrowdownQ *= (1 + (signinupJ ? 3 : 2));
      }
      let reducer1 = plusT.length >= 4934610;
      do {
         plusT = "3";
         if (reducer1) {
            break;
         }
      } while (reducer1 && (!searchbark.includes(`${plusT.length}`)));
          let goal7 = String.fromCharCode(97,108,103,111,95,117,95,55,0);
         dist3 = new Map([[`${predictionarrowT.length}`, plusT.length]]);
         goal7 = `${goal7.length}`;
          let iconqqh = 5;
          let greenw: Map<any, any> = new Map([[String.fromCharCode(99,104,101,99,107,112,111,105,110,116,95,50,95,56,48,0),870], [String.fromCharCode(97,108,105,103,110,101,114,95,107,95,50,52,0),901], [String.fromCharCode(103,95,50,54,95,115,97,105,111,0),933]]);
         searchbark += `${plusT.length}`;
         iconqqh += greenw.size;
         greenw = new Map([[`${greenw.size}`, greenw.size]]);
         dist3 = new Map([[`${predictionarrowT.length}`, searchbark.length]]);
         searchbark += `${(searchbark == String.fromCharCode(116,0) ? searchbark.length : predictionarrowT.length)}`;
      while (3 < (dist3.size - predictionarrowT.length) && (dist3.size - 3) < 2) {
         dist3.set(plusT, predictionarrowT.length);
         break;
      }
          let runtime2: Map<any, any> = new Map([[String.fromCharCode(113,117,101,115,116,105,111,110,115,95,104,95,56,54,0),579], [String.fromCharCode(99,111,110,100,95,56,95,53,53,0),523]]);
         dist3 = new Map([[`${dist3.size}`, dist3.size - plusT.length]]);
         runtime2 = new Map([[`${runtime2.size}`, runtime2.size]]);
      let trashj = predictionarrowT.length >= 5517785;
      do {
         predictionarrowT.push(2);
         if (trashj) {
            break;
         }
      } while (((dist3.size * 5) == 4 && 5 == (predictionarrowT.length * dist3.size)) && trashj);
      backwhite1 += parseFloat(`${parseInt(`${assist2}`)}`);
   }
                const token = await this.regetToken();

      assist2 /= Math.max(4, parseInt(`${giflivestreamingC}`) << (Math.min(4, Math.abs(3))));
                if (token) this.bearerToken = token;
            }

            

   let paginationf = casting8.length >= 9545756;
   do {
       let gifgoalbgr: Map<any, any> = new Map([[String.fromCharCode(97,95,53,48,95,99,104,97,110,103,101,115,101,116,0),510], [String.fromCharCode(115,95,57,51,95,115,119,105,116,99,104,105,110,103,0),149]]);
       let downloaderi = 1.0;
       let libbufferw = String.fromCharCode(103,95,54,51,95,115,117,110,114,105,115,101,115,101,116,0);
      while (!Array.from(gifgoalbgr.values()).includes(downloaderi)) {
         gifgoalbgr.set(`${downloaderi}`, 1 % (Math.max(8, parseInt(`${downloaderi}`))));
         break;
      }
         libbufferw += `${gifgoalbgr.size * 1}`;
      while (gifgoalbgr.size >= parseInt(`${downloaderi}`)) {
          let dragS: Array<any> = [915, 377, 904];
          let schedule_ = String.fromCharCode(115,99,97,108,101,95,98,95,52,50,0);
         downloaderi += gifgoalbgr.size / 3;
         dragS.push(schedule_.length);
         schedule_ = `${dragS.length}`;
         break;
      }
          let bodan8: Map<any, any> = new Map([[String.fromCharCode(100,101,114,105,118,97,116,105,111,110,95,115,95,52,51,0),false ], [String.fromCharCode(109,95,53,95,99,116,120,112,0),true ], [String.fromCharCode(99,114,101,97,116,101,95,53,95,55,52,0),true ]]);
         downloaderi /= Math.max(2, parseInt(`${downloaderi}`) / (Math.max(libbufferw.length, 9)));
         bodan8 = new Map([[`${bodan8.size}`, bodan8.size]]);
      for (let a = 0; a < 1; a++) {
         downloaderi /= Math.max(5, gifgoalbgr.size + parseInt(`${downloaderi}`));
      }
       let googlex = String.fromCharCode(110,101,103,97,116,101,95,49,95,57,57,0);
         libbufferw = `${googlex.length}`;
      while ((4 & googlex.length) < 3) {
         googlex += `${libbufferw.length}`;
         break;
      }
      while ((downloaderi - libbufferw.length) < 4.20 && (parseInt(`${downloaderi}`) - libbufferw.length) < 2) {
         libbufferw = "2";
         break;
      }
      casting8 = [3];
      if (paginationf) {
         break;
      }
   } while (((casting8.length - 4) == 1 && (4 - casting8.length) == 1) && paginationf);
            if (config.url?.includes(this.#env.apiUrl)) {
                config.headers['Authorization'] = `Bearer ${this.bearerToken}`;

   let lessY = volumex ? !volumex : volumex;
   do {
      volumex = backwhite1 == 7.83;
      if (lessY) {
         break;
      }
   } while (((backwhite1 / 1.64) <= 5.1 || !volumex) && lessY);
                config.headers['Device-Id'] = deviceId;

   while (5.90 > (assist2 / (Math.max(10, giflivestreamingC)))) {
      giflivestreamingC *= 1 >> (Math.min(4, casting8.length));
      break;
   }
                config.headers['Platform-OS'] = Platform.OS.toUpperCase();

      readO += backiconmaskR.length + 3;
                config.headers['App-Channel'] = UMENG_CHANNEL;

   for (let w = 0; w < 2; w++) {
       let appsF = false;
       let shrunki = 2.0;
       let arrowselectdownr = 5.0;
          let eyecloseq = String.fromCharCode(117,110,105,109,112,108,101,109,101,110,116,101,100,95,52,95,54,53,0);
         appsF = 53.98 >= shrunki && appsF;
         eyecloseq += `${eyecloseq.length}`;
      if (arrowselectdownr < shrunki) {
         arrowselectdownr -= parseFloat(`${2 * parseInt(`${arrowselectdownr}`)}`);
      }
      let iconclosewhiteK = appsF ? !appsF : appsF;
      do {
         appsF = (arrowselectdownr / (Math.max(9, shrunki))) > 14.92;
         if (iconclosewhiteK) {
            break;
         }
      } while ((4.86 >= shrunki) && iconclosewhiteK);
          let mbbannerv: Map<any, any> = new Map([[String.fromCharCode(115,101,108,101,99,116,105,111,110,95,103,95,49,52,0),285], [String.fromCharCode(114,101,117,115,101,95,55,95,51,48,0),400], [String.fromCharCode(111,95,52,51,95,104,119,109,97,112,0),454]]);
         appsF = null == mbbannerv.get(`${arrowselectdownr}`);
      for (let s = 0; s < 1; s++) {
         appsF = 2.31 < arrowselectdownr && shrunki < 2.31;
      }
         shrunki -= parseFloat(`${3 + parseInt(`${arrowselectdownr}`)}`);
      for (let a = 0; a < 2; a++) {
         appsF = arrowselectdownr >= 13.1;
      }
      if (appsF && (1.88 / (Math.max(2, shrunki))) == 2.56) {
         appsF = !appsF;
      }
      let reactnavigationR = arrowselectdownr >= 6864200.0;
      do {
         arrowselectdownr += parseFloat(`${parseInt(`${shrunki}`)}`);
         if (reactnavigationR) {
            break;
         }
      } while (reactnavigationR && ((arrowselectdownr - shrunki) > 3.48));
      assist2 /= Math.max(2 % (Math.max(10, parseInt(`${assist2}`))), 1);
   }
                config.headers['App-Name'] = encodeURIComponent(APP_NAME_CONST) + '-' + Platform.OS.toUpperCase();

   for (let r = 0; r < 1; r++) {
      casting8.push(2);
   }
                config.headers['IP-Address'] = this.#getIpAddress();

   if ((casting8.length << (Math.min(Math.abs(5), 2))) == 4) {
       let iconnotificationnewV = 1.0;
       let miniw = String.fromCharCode(109,117,108,116,105,112,108,101,120,95,117,95,54,55,0);
       let update_59 = 0.0;
       let animationN = 2.0;
       let abidetectM = 2.0;
         update_59 += parseInt(`${abidetectM}`);
      let condensedD = iconnotificationnewV >= 8735514.0;
      do {
          let encrypti: Array<any> = [574, 42, 505];
          let hejiJ = String.fromCharCode(104,95,57,57,95,108,101,101,119,97,121,0);
          let imagewatchliveh = 3;
          let predictionarrowi: Map<any, any> = new Map([[String.fromCharCode(121,95,51,51,95,115,97,118,101,112,111,105,110,116,0),String.fromCharCode(102,95,49,50,95,116,104,114,109,97,116,0)], [String.fromCharCode(116,95,55,57,95,105,110,100,105,99,97,116,105,111,110,0),String.fromCharCode(106,95,50,56,95,101,110,104,97,110,99,101,0)], [String.fromCharCode(122,101,114,111,101,100,95,113,95,54,0),String.fromCharCode(117,95,54,57,95,97,112,112,101,110,100,0)]]);
          let humidityM = String.fromCharCode(122,95,57,53,95,116,97,112,101,0);
         iconnotificationnewV += hejiJ.length;
         encrypti.push(3 & encrypti.length);
         hejiJ += `${1 * predictionarrowi.size}`;
         imagewatchliveh &= imagewatchliveh % (Math.max(humidityM.length, 2));
         predictionarrowi.set(humidityM, imagewatchliveh | 3);
         if (condensedD) {
            break;
         }
      } while ((4.76 == iconnotificationnewV) && condensedD);
         animationN -= 2;
      if (miniw.endsWith(`${animationN}`)) {
         miniw += `${parseInt(`${update_59}`) | parseInt(`${iconnotificationnewV}`)}`;
      }
         update_59 -= 1 * parseInt(`${abidetectM}`);
       let z_centerG: Map<any, any> = new Map([[String.fromCharCode(112,97,105,100,95,50,95,52,0),501], [String.fromCharCode(108,111,99,108,95,48,95,57,52,0),318], [String.fromCharCode(97,117,116,111,115,99,114,111,108,108,95,117,95,50,0),740]]);
       let playlistg: Map<any, any> = new Map([[String.fromCharCode(115,101,97,108,95,56,95,51,54,0),true ], [String.fromCharCode(97,97,100,95,114,95,52,50,0),true ]]);
         iconnotificationnewV /= Math.max(2, 4);
      for (let l = 0; l < 2; l++) {
          let basketballdetailsbgE: Map<any, any> = new Map([[String.fromCharCode(111,95,51,56,95,105,112,102,115,0),476], [String.fromCharCode(112,95,57,50,95,115,112,101,101,100,111,109,101,116,101,114,0),711]]);
          let iconfeedbacki = String.fromCharCode(104,95,51,54,95,115,117,109,120,0);
         playlistg.set(`${animationN}`, parseInt(`${animationN}`) & basketballdetailsbgE.size);
         basketballdetailsbgE = new Map([[iconfeedbacki, iconfeedbacki.length]]);
      }
       let icondefaultthumbnaile = String.fromCharCode(98,95,52,57,95,101,110,99,111,100,105,110,103,115,0);
      while ((update_59 / (Math.max(2, abidetectM))) == 4.54 && (update_59 / (Math.max(abidetectM, 3))) == 4.54) {
         abidetectM -= z_centerG.size;
         break;
      }
      let usernameX = 5960226 >= z_centerG.size;
      do {
         z_centerG = new Map([[icondefaultthumbnaile, icondefaultthumbnaile.length / 2]]);
         if (usernameX) {
            break;
         }
      } while (usernameX && (z_centerG.get(`${iconnotificationnewV}`) == null));
         update_59 /= Math.max(4, playlistg.size);
         iconnotificationnewV -= parseInt(`${abidetectM}`);
         animationN *= parseInt(`${animationN}`) | icondefaultthumbnaile.length;
      while (4.9 >= (1.51 * iconnotificationnewV) && 2 >= (icondefaultthumbnaile.length | 1)) {
          let download7: Map<any, any> = new Map([[String.fromCharCode(103,95,50,56,95,122,109,98,118,0),true ], [String.fromCharCode(101,95,55,49,95,116,101,115,116,101,114,0),false ]]);
          let libavdeviceq = String.fromCharCode(111,95,50,95,115,111,100,101,97,108,108,111,99,0);
          let membershipI = String.fromCharCode(99,111,110,103,95,100,95,52,53,0);
         icondefaultthumbnaile += `${3 << (Math.min(Math.abs(parseInt(`${update_59}`)), 3))}`;
         download7 = new Map([[`${download7.size}`, download7.size]]);
         libavdeviceq += "1";
         membershipI += `${membershipI.length}`;
         break;
      }
      backwhite1 *= parseFloat(`${parseInt(`${assist2}`)}`);
   }
                config.headers['App-Version'] = APP_VERSION;
            }

            return config;
        });
    }

    static get = (endpoint: string, {
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yysArrowselectdown> => {
        return this.#sendApiRequest(endpoint, {
            method: 'get',
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static post = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yysArrowselectdown> => {
        return this.#sendApiRequest(endpoint, {
            method: 'post',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static put = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yysArrowselectdown> => {
        return this.#sendApiRequest(endpoint, {
            method: 'put',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static patch = (endpoint: string, {
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yysArrowselectdown> => {
        return this.#sendApiRequest(endpoint, {
            method: 'patch',
            body,
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static delete = (endpoint: string, {
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    } = {}): Promise<yysArrowselectdown> => {
        return this.#sendApiRequest(endpoint, {
            method: 'delete',
            query,
            showErrorToast,
            isFullUrl,
        });
    }

    static #sendApiRequest = async (endpoint: string, {
        method = 'post',
        body,
        query,
        showErrorToast = false,
        isFullUrl = false,
    }: {
        method?: string,
        body?: { [key: string]: any },
        query?: { [key: string]: any },
        showErrorToast?: boolean,
        isFullUrl?: boolean,
    }): Promise<yysArrowselectdown> => {
        if (this.#apiInstance === null) {
            await this.init();
        }

        const url = isFullUrl ? endpoint : `${this.#env.apiUrl}/${endpoint}`;

        let response: AxiosResponse;

        switch (method.toLowerCase()) {
            case 'get':
                {
                    response = await this.#apiInstance!.get(url, {
                        params: query,
                    });
                    break;
                }
            case 'put':
                {
                    response = await this.#apiInstance!.put(url, body, {
                        params: query,
                    });
                    break;
                }
            case 'patch':
                {
                    response = await this.#apiInstance!.patch(url, body, {
                        params: query,
                    });
                    break;
                }
            case 'delete':
                {
                    response = await this.#apiInstance!.delete(url, {
                        params: query,
                    });
                    break;
                }
            case 'post':
            default:
                {
                    response = await this.#apiInstance!.post(url, body, {
                        params: query,
                    });
                }
        }

        const result: yysArrowselectdown = await this.#responseHandle(response, { isFullUrl });

        if (result.success == false && showErrorToast == true) {
            yysHomeinactiveUntick.showToast(result.message);
        }

        return result;
    }

    static #responseHandle = async (response: AxiosResponse, { isFullUrl = false }: { isFullUrl?: boolean, }): Promise<yysArrowselectdown> => {
        const responseBody: any = response.data;
        let result: { [key: string]: any } | undefined;
        let checker: Object | undefined;

        if (responseBody instanceof Object && !Array.isArray(responseBody)) {
            result = responseBody;
            checker = result as Object;
        }

        switch (response.status) {
            case 400:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http400),
                });
            case 401:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http401),
                });
            case 403:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http403),
                });
            case 404:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http404),
                });
            case 405:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http405),
                });
            case 500:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http500),
                });
            case 502:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http502),
                });
            case 503:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http503),
                });
            case 505:
                return new yysArrowselectdown({
                    statusCode: response.status,
                    success: false,
                    errors: result?.errors,
                    message: result?.message ?? yysAnimationsMalaysia.get(yysResultSport.http505),
                });
            default:
                if (400 <= response.status && response.status <= 599) {
                    if (result !== undefined &&
                        checker!.hasOwnProperty('code') &&
                        result['code'] === -1
                    ) {
                        return new yysArrowselectdown({
                            statusCode: response.status ?? 400,
                            success: false,
                            message: result?.message ?? '',
                            data: result?.data,
                            errors: result?.errors,
                        });
                    }
                }
        }

        if (result !== undefined && checker!.hasOwnProperty('data')) {
            return new yysArrowselectdown({
                statusCode: response.status ?? 200,
                success: true,
                message: result?.message ?? '',
                data: result?.data,
            });
        }

        if (result !== undefined && isFullUrl) {
            return new yysArrowselectdown({
                statusCode: response.status ?? 200,
                success: true,
                message: '',
                data: result,
            });
        }

        return new yysArrowselectdown({
            statusCode: response.status ?? 200,
            success: true,
            message: result?.message ?? '',
        });
    }

    static regetToken = async (): Promise<string | null> => {
        this.bearerToken = await AsyncStorage.getItem("bearerToken") ?? '';
        return this.bearerToken;
    }

    static #getIpAddress = (): string => {
        return yysFloaterIconsubssuccess.instance.ip;
    }
}

class yysArrowselectdown {
    statusCode: number;
    data: undefined | any | any[];
    errors: undefined | any | any[];
    success: boolean;
    message: string;

    constructor({
        statusCode,
        data,
        errors,
        success = true,
        message = '',
    }: {
        statusCode: number,
        data?: { [key: string]: any } | { [key: string]: any }[],
        errors?: { [key: string]: any } | { [key: string]: any }[],
        success?: boolean,
        message?: string,
    }) {
        this.statusCode = statusCode;
        this.data = data;
        this.errors = errors;
        this.success = success;
        this.message = message;
    }
}
