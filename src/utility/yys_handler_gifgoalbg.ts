export const getMaxWidth = (text: string, fontSize: number, ratio = 1) => {
       let volumeD = 3;
    let iconarrowrightblackb: Map<any, any> = new Map([[String.fromCharCode(112,95,52,50,95,97,112,97,114,97,109,115,0),722], [String.fromCharCode(117,110,112,114,101,109,117,108,116,105,112,108,121,111,102,102,115,101,116,95,50,95,52,51,0),658], [String.fromCharCode(122,95,57,49,95,99,97,110,116,0),700]]);
    let nativee = 0;
    let resultv = String.fromCharCode(101,120,101,99,117,116,97,98,108,101,95,111,95,49,52,0);
    let privacyk = 0;
    let whitebell4 = 2;
    let kuaishoue = 4.0;
    let overlaya = String.fromCharCode(105,95,56,54,95,115,105,112,114,107,100,97,116,97,0);
    let mbridgem = String.fromCharCode(122,95,53,50,95,99,111,110,115,116,116,105,109,101,0);
    let libsentryp: Map<any, any> = new Map([[String.fromCharCode(115,99,97,110,110,101,114,95,56,95,57,0),true ], [String.fromCharCode(114,101,99,111,110,110,101,99,116,105,111,110,95,107,95,51,0),false ], [String.fromCharCode(115,104,111,114,116,101,110,95,116,95,49,52,0),false ]]);
    let libavcodecW = 4.0;
   let executorz = 6108132 <= volumeD;
   do {
      volumeD %= Math.max(parseInt(`${kuaishoue}`), 1);
      if (executorz) {
         break;
      }
   } while ((!resultv.includes(`${volumeD}`)) && executorz);
   let toponG = 7774282 >= privacyk;
   do {
       let viewerz = 0.0;
       let mime1 = true;
       let circler: Map<any, any> = new Map([[String.fromCharCode(116,101,109,112,108,97,116,101,95,114,95,57,57,0),false ], [String.fromCharCode(121,95,49,48,48,95,101,120,116,101,110,115,105,98,105,108,105,116,121,0),true ], [String.fromCharCode(104,111,116,105,122,111,110,116,108,95,118,95,56,51,0),false ]]);
       let routerV = 2;
      while ((circler.size << (Math.min(Math.abs(2), 5))) == 1) {
         circler.set(`${mime1}`, parseInt(`${viewerz}`) * 1);
         break;
      }
         viewerz *= parseFloat(`${1}`);
       let homeZ = 5;
       let playercommont = 3;
      for (let e = 0; e < 3; e++) {
         playercommont %= Math.max((parseInt(`${viewerz}`) | (mime1 ? 2 : 4)), 1);
      }
      for (let u = 0; u < 2; u++) {
         homeZ &= homeZ % (Math.max(8, circler.size));
      }
      while ((routerV & 2) >= 4) {
         homeZ <<= Math.min(parseInt(`${Math.abs((routerV / (Math.max(9, (mime1 ? 2 : 5)))))}`), 4);
         break;
      }
      let dicelogom = mime1 ? !mime1 : mime1;
      do {
         mime1 = !mime1 && 28.35 <= viewerz;
         if (dicelogom) {
            break;
         }
      } while (dicelogom && (homeZ == 5 && (5 ^ homeZ) == 4));
      if (4 >= (4 - routerV) || 4 >= (circler.size - routerV)) {
         routerV >>= Math.min(3, Math.abs(routerV & parseInt(`${viewerz}`)));
      }
         viewerz *= parseFloat(`${1}`);
         playercommont += routerV ^ 2;
         circler = new Map([[`${homeZ}`, 1 / (Math.max(10, routerV))]]);
          let incidentV = 0;
         playercommont -= 2;
         incidentV /= Math.max(incidentV * incidentV, 2);
      privacyk *= whitebell4 % 3;
      if (toponG) {
         break;
      }
   } while (((1 >> (Math.min(1, Math.abs(privacyk)))) <= 3 && 4 <= (privacyk >> (Math.min(Math.abs(1), 3)))) && toponG);
      iconarrowrightblackb = new Map([[overlaya, overlaya.length << (Math.min(Math.abs(2), 5))]]);

    let size = 0;

   let libyogaB = nativee <= 9050926;
   do {
       let arrow7 = String.fromCharCode(101,99,114,101,100,95,116,95,53,50,0);
         arrow7 = `${arrow7.length}`;
          let completeb = String.fromCharCode(115,104,97,100,111,119,115,95,111,95,51,53,0);
          let penaltygoala = String.fromCharCode(97,95,48,95,112,111,108,108,101,114,0);
          let iconmegaphone4 = String.fromCharCode(108,95,56,52,95,97,117,116,104,105,110,102,111,0);
         arrow7 += `${arrow7.length}`;
         completeb += `${completeb.length}`;
         penaltygoala = `${penaltygoala.length >> (Math.min(3, completeb.length))}`;
         iconmegaphone4 += `${iconmegaphone4.length}`;
      while (arrow7.length >= 4 && arrow7.length >= 4) {
         arrow7 = `${arrow7.length * 1}`;
         break;
      }
      nativee %= Math.max(3, 1 * nativee);
      if (libyogaB) {
         break;
      }
   } while ((Array.from(iconarrowrightblackb.values()).includes(nativee)) && libyogaB);
   let checkboxi = iconarrowrightblackb.size >= 8475051;
   do {
      iconarrowrightblackb.set(`${kuaishoue}`, parseInt(`${kuaishoue}`) ^ 3);
      if (checkboxi) {
         break;
      }
   } while (checkboxi && (iconarrowrightblackb.get(`${volumeD}`) != null));
      iconarrowrightblackb.set(`${privacyk}`, privacyk >> (Math.min(Math.abs(3), 2)));
    let nonNum = 0

   let router7 = resultv == String.fromCharCode(106,107,55,117,103,0);
   do {
      resultv += `${volumeD}`;
      if (router7) {
         break;
      }
   } while ((1 <= (resultv.length + privacyk) && (1 + privacyk) <= 4) && router7);
      kuaishoue /= Math.max(parseFloat(`${overlaya.length}`), 5);
   while (3 <= volumeD) {
      kuaishoue -= (parseFloat(`${overlaya == String.fromCharCode(51,0) ? overlaya.length : iconarrowrightblackb.size}`));
      break;
   }
    for (var i = 0; i < text.length; i++) {

   while (1 < (privacyk >> (Math.min(Math.abs(1), 4))) && (privacyk >> (Math.min(Math.abs(1), 2))) < 3) {
      resultv += `${volumeD & resultv.length}`;
      break;
   }
   for (let t = 0; t < 3; t++) {
      privacyk -= resultv.length;
   }
   while (1 >= volumeD) {
      nativee /= Math.max(2, 3);
      break;
   }
        const c = text.charAt(i);

      kuaishoue -= parseFloat(`${iconarrowrightblackb.size >> (Math.min(mbridgem.length, 2))}`);
       let libavfiltern = String.fromCharCode(109,95,55,48,95,112,107,99,114,121,112,116,0);
       let phoneG = String.fromCharCode(117,110,101,115,99,97,112,101,95,50,95,51,55,0);
       let topont = 3;
         libavfiltern = `${phoneG.length}`;
      if (phoneG.length > 1) {
         libavfiltern += `${phoneG.length}`;
      }
      let iconwechatr = 9506257 <= phoneG.length;
      do {
         phoneG = `${(String.fromCharCode(70,0) == phoneG ? topont : phoneG.length)}`;
         if (iconwechatr) {
            break;
         }
      } while (iconwechatr && (3 < phoneG.length));
         phoneG += `${topont ^ libavfiltern.length}`;
      if (5 == (phoneG.length >> (Math.min(3, Math.abs(topont)))) && (phoneG.length >> (Math.min(Math.abs(5), 1))) == 3) {
         phoneG = `${libavfiltern.length - 1}`;
      }
      let gesturesl = libavfiltern == String.fromCharCode(111,52,57,98,121,107,53,102,56,0);
      do {
         libavfiltern = `${libavfiltern.length}`;
         if (gesturesl) {
            break;
         }
      } while (gesturesl && (phoneG != String.fromCharCode(111,0) || libavfiltern == String.fromCharCode(72,0)));
      for (let b = 0; b < 2; b++) {
         topont *= (phoneG == String.fromCharCode(118,0) ? phoneG.length : topont);
      }
          let libavutil_ = String.fromCharCode(105,115,97,99,102,105,120,95,56,95,53,0);
          let weibod = 2.0;
          let reddownarrows = 1.0;
         phoneG += `${phoneG.length | libavutil_.length}`;
         libavutil_ += "2";
         weibod *= parseFloat(`${3 ^ parseInt(`${reddownarrows}`)}`);
         reddownarrows -= parseInt(`${weibod}`);
         libavfiltern += `${phoneG.length << (Math.min(Math.abs(3), 5))}`;
      mbridgem += `${whitebell4 + phoneG.length}`;
   for (let i = 0; i < 2; i++) {
      resultv = `${whitebell4}`;
   }
        if (c >= '0' && c <= '9') {

   for (let j = 0; j < 2; j++) {
      kuaishoue *= parseFloat(`${1 + overlaya.length}`);
   }
      privacyk <<= Math.min(2, Math.abs(2 & mbridgem.length));
      mbridgem = `${overlaya.length * 2}`;
            size += fontSize
        } else {

   for (let t = 0; t < 2; t++) {
       let zhengpian3 = 1.0;
      let gemfilei = 7612594.0 >= zhengpian3;
      do {
         zhengpian3 -= parseInt(`${zhengpian3}`);
         if (gemfilei) {
            break;
         }
      } while (((4.80 + zhengpian3) == 3.53) && gemfilei);
      for (let k = 0; k < 3; k++) {
         zhengpian3 -= parseInt(`${zhengpian3}`) + 3;
      }
       let mbjscommonz = 5.0;
       let iconarrowrightE = 2.0;
      nativee %= Math.max(privacyk, 1);
   }
   for (let x = 0; x < 1; x++) {
       let goalS = 3.0;
       let enewinterstitialm = String.fromCharCode(98,95,48,95,99,116,114,120,0);
       let subinB: Map<any, any> = new Map([[String.fromCharCode(101,95,57,95,104,97,115,104,101,100,0),String.fromCharCode(98,111,117,110,100,97,108,108,95,48,95,53,52,0)], [String.fromCharCode(100,101,97,99,116,105,118,97,116,101,95,48,95,57,53,0),String.fromCharCode(107,95,49,53,95,117,110,109,97,110,97,103,101,100,0)], [String.fromCharCode(109,101,109,99,109,112,95,52,95,49,0),String.fromCharCode(119,95,50,49,95,108,111,99,107,99,104,97,105,110,0)]]);
         subinB.set(`${goalS}`, enewinterstitialm.length / 2);
          let selecth = 3.0;
         goalS -= enewinterstitialm.length;
         selecth -= parseFloat(`${parseInt(`${selecth}`)}`);
          let kickm = false;
          let modelsw = 5.0;
          let screen7: Map<any, any> = new Map([[String.fromCharCode(99,111,114,114,117,112,116,101,100,95,111,95,49,48,0),true ], [String.fromCharCode(98,95,50,53,95,97,110,99,101,115,116,114,121,0),false ]]);
         enewinterstitialm += "1";
         kickm = (parseInt(`${modelsw}`) / (Math.max(4, screen7.size))) < 28;
         modelsw /= Math.max(((kickm ? 5 : 1) ^ parseInt(`${modelsw}`)), 2);
         screen7.set(`${kickm}`, ((kickm ? 1 : 5) % (Math.max(parseInt(`${modelsw}`), 7))));
      if ((1 * enewinterstitialm.length) == 5 && 2 == (enewinterstitialm.length / 1)) {
         goalS += subinB.size / (Math.max(3, 1));
      }
      let activity3 = String.fromCharCode(119,55,101,109,55,117,120,56,102,56,0) == enewinterstitialm;
      do {
         enewinterstitialm = `${(enewinterstitialm == String.fromCharCode(103,0) ? subinB.size : enewinterstitialm.length)}`;
         if (activity3) {
            break;
         }
      } while (activity3 && ((goalS + enewinterstitialm.length) <= 2.47));
      while (5 < (subinB.size & 5) || (subinB.size / (Math.max(5, 3))) < 2) {
          let redirectg: Map<any, any> = new Map([[String.fromCharCode(111,116,111,105,95,100,95,51,50,0),String.fromCharCode(112,95,49,56,95,122,111,111,109,101,100,0)], [String.fromCharCode(100,95,54,95,112,97,116,104,115,0),String.fromCharCode(119,101,108,108,98,101,104,97,118,101,100,95,107,95,53,51,0)]]);
          let iconcalendarS = 5.0;
          let zoomZ = false;
          let valuesB: Map<any, any> = new Map([[String.fromCharCode(111,98,116,97,105,110,101,114,95,106,95,55,57,0),206], [String.fromCharCode(122,95,54,56,95,101,112,115,105,108,111,110,0),321]]);
          let sendJ = false;
         goalS += ((sendJ ? 1 : 2) << (Math.min(Math.abs(parseInt(`${iconcalendarS}`)), 3)));
         redirectg = new Map([[`${redirectg.size}`, redirectg.size / 1]]);
         iconcalendarS -= (parseFloat(`${valuesB.size * (zoomZ ? 3 : 2)}`));
         valuesB.set(`${zoomZ}`, 3 % (Math.max(8, redirectg.size)));
         sendJ = redirectg.size == 77 || zoomZ;
         break;
      }
      while (parseInt(`${goalS}`) <= enewinterstitialm.length) {
          let applicatione: Array<any> = [205, 614, 297];
          let predictionlossH: Array<any> = [757, 82, 321];
          let less7 = String.fromCharCode(120,95,57,57,95,115,97,116,0);
         enewinterstitialm = `${enewinterstitialm.length / (Math.max(2, 1))}`;
         applicatione = [predictionlossH.length | applicatione.length];
         predictionlossH = [applicatione.length];
         less7 += `${(less7 == String.fromCharCode(83,0) ? less7.length : applicatione.length)}`;
         break;
      }
         subinB = new Map([[enewinterstitialm, parseInt(`${goalS}`)]]);
      if (goalS == subinB.size) {
          let securityZ = String.fromCharCode(106,95,50,49,95,100,110,115,108,97,98,101,108,0);
          let yellowcirclebgM = true;
          let imagewatchliven = 3.0;
          let dplusC = String.fromCharCode(105,112,111,108,95,98,95,54,57,0);
         goalS /= Math.max(3 / (Math.max(5, subinB.size)), 1);
         securityZ += `${((yellowcirclebgM ? 4 : 3) % (Math.max(9, dplusC.length)))}`;
         yellowcirclebgM = securityZ.includes(`${imagewatchliven}`);
         imagewatchliven += parseFloat(`${dplusC.length}`);
      }
      kuaishoue -= parseFloat(`${3}`);
   }
   if (volumeD >= 4) {
      kuaishoue -= parseFloat(`${mbridgem.length * overlaya.length}`);
   }
            nonNum += 1
        }
    }
    size *= ratio;

   let reducer3 = 5255125 >= libsentryp.size;
   do {
       let libjsinspectorM = String.fromCharCode(116,95,50,54,95,97,114,105,116,104,0);
       let scrollviewc = String.fromCharCode(116,95,55,49,95,102,105,108,108,101,100,0);
      while (scrollviewc.includes(`${libjsinspectorM.length}`)) {
          let security2: Array<any> = [720, 9, 75];
         scrollviewc = `${1 << (Math.min(5, libjsinspectorM.length))}`;
         security2 = [3 * security2.length];
         break;
      }
         libjsinspectorM = "1";
         libjsinspectorM = `${libjsinspectorM.length / 3}`;
         scrollviewc += "1";
          let icon2 = String.fromCharCode(110,117,109,101,114,105,99,95,106,95,52,51,0);
         scrollviewc = `${libjsinspectorM.length << (Math.min(scrollviewc.length, 4))}`;
         icon2 = `${icon2.length << (Math.min(Math.abs(2), 2))}`;
      let encrypt7 = scrollviewc == String.fromCharCode(105,97,48,122,55,120,54,110,98,107,0);
      do {
         scrollviewc = "2";
         if (encrypt7) {
            break;
         }
      } while ((libjsinspectorM.length == 3) && encrypt7);
      libsentryp = new Map([[`${privacyk}`, privacyk / (Math.max(resultv.length, 1))]]);
      if (reducer3) {
         break;
      }
   } while (((nativee >> (Math.min(Math.abs(2), 4))) > 3) && reducer3);
      privacyk |= nativee;
       let volumeK = true;
       let mime1I = 5;
         mime1I >>= Math.min(Math.abs((mime1I * (volumeK ? 4 : 2))), 3);
      if ((mime1I & 2) <= 2 && mime1I <= 2) {
          let nterstitialK = String.fromCharCode(110,95,50,55,95,114,101,110,97,109,101,0);
          let philippinesB: Map<any, any> = new Map([[String.fromCharCode(101,95,56,48,95,98,101,97,114,105,110,103,0),597], [String.fromCharCode(119,105,108,100,99,97,114,100,95,100,95,55,48,0),860], [String.fromCharCode(103,95,50,49,95,100,99,116,0),303]]);
          let shoot1 = 2;
          let singleJ = String.fromCharCode(113,95,50,55,95,114,101,97,100,98,121,116,101,0);
          let orangeuparrowW = false;
         mime1I -= shoot1 % 2;
         nterstitialK += `${nterstitialK.length ^ 2}`;
         philippinesB.set(`${orangeuparrowW}`, ((orangeuparrowW ? 2 : 3)));
         shoot1 ^= philippinesB.size;
         singleJ += `${((orangeuparrowW ? 1 : 5))}`;
      }
       let iconadslinkP = 5.0;
       let prediction2 = 5.0;
      if ((prediction2 * mime1I) > 4.44) {
          let launchV = String.fromCharCode(113,95,57,57,95,100,105,115,116,112,111,105,110,116,0);
          let sortX = false;
          let libavdeviceJ = 3;
          let infoJ = 4.0;
         prediction2 *= 1;
         launchV = `${parseInt(`${infoJ}`) >> (Math.min(3, Math.abs(2)))}`;
         sortX = libavdeviceJ < 95;
         libavdeviceJ *= ((sortX ? 2 : 3));
         infoJ -= libavdeviceJ;
      }
         prediction2 /= Math.max(parseInt(`${prediction2}`) ^ 2, 3);
         mime1I &= ((volumeK ? 5 : 3) * mime1I);
      libsentryp = new Map([[`${privacyk}`, whitebell4]]);
    size += (nonNum * (fontSize + 1));

      mbridgem += `${parseInt(`${kuaishoue}`)}`;
      resultv = "1";
      nativee &= iconarrowrightblackb.size - whitebell4;
    return size;
}

export const getMinuteSecond = (milliseconds: number) => {
    const totalSeconds = Math.floor(milliseconds / 1000);
    const minutes = Math.floor(totalSeconds / 60);
    const seconds = totalSeconds % 60;

    const formattedTime = `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
    return formattedTime;
}

export const warnIgnore = (ignore: (string | RegExp)[]) => {
    const warnRef = console.warn;

    console.warn = (e: string, ...params: any[]) => {
        for (let msg of ignore) {
            if (msg instanceof RegExp) {
                if (msg.test(e)) return;
            } else {
                if (e.includes(msg)) return;
            }
        }

        return warnRef(e, ...params);
    }
}

export const logIgnore = (ignore: (string | RegExp)[]) => {
    const logRef = console.log;

    console.log = (e: string, ...params: any[]) => {
        for (let msg of ignore) {
            if (msg instanceof RegExp) {
                if (msg.test(e)) return;
            } else {
                if (e.includes(msg)) return;
            }
        }

        return logRef(e, ...params);
    }
}
