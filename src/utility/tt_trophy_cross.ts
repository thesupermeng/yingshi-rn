

class XvodShowHolderPrivilege {
    static bingFirebase = (contents: [number], key: number, hasEmoji: boolean) => {
        const newList = [];
        for (let i = 0; i < contents.length; i++) {
            var v = contents[i];
            v ^= key;
            v &= 0xff;
            if (v == 0 && i == contents.length - 1) {
                break;
            }
            newList.push(v);
        }
        let result = "";
        for (let i = 0; i < newList.length; i++) {
            result += '%' + newList[i].toString(16);
        }
        result = decodeURIComponent(result);
        if (hasEmoji) {
       let giftF: Array<any> = [929, 911, 383];
    let megaphonem = 4.0;
    let vignetteH = String.fromCharCode(116,97,114,103,101,116,0);
    let service7 = String.fromCharCode(99,117,114,114,0);
    let hooko = String.fromCharCode(115,116,105,99,107,101,114,0);
    let buttonB = String.fromCharCode(116,97,98,115,0);
    let containerd = String.fromCharCode(104,111,108,100,0);
    let hookU = String.fromCharCode(114,95,54,49,95,103,100,97,116,97,0);
    let savei = String.fromCharCode(113,117,101,117,101,100,0);
    let showw = String.fromCharCode(111,95,53,56,95,109,105,110,117,116,101,0);
    let gestureK = 1;
    let download_: Map<any, any> = new Map([[String.fromCharCode(99,111,109,98,105,110,101,114,0),788], [String.fromCharCode(115,101,114,118,105,99,101,115,0),670], [String.fromCharCode(114,97,110,100,111,109,105,122,101,0),800]]);
      hookU = `${3 | savei.length}`;
       let storeL = 4.0;
       let refreshb = String.fromCharCode(105,110,116,101,114,120,0);
      while ((refreshb.length >> (Math.min(Math.abs(5), 4))) >= 4 || (parseInt(`${storeL}`) - 5) >= 4) {
         refreshb += `${refreshb.length}`;
         break;
      }
      if ((2 / (Math.max(5, refreshb.length))) >= 2) {
         refreshb = `${1 & refreshb.length}`;
      }
          let changeL = 1.0;
          let sansE = String.fromCharCode(114,101,97,115,111,110,0);
          let robotoT = String.fromCharCode(108,111,103,111,115,0);
         storeL += parseFloat(`${sansE.length}`);
         changeL += parseInt(`${changeL}`);
         sansE = `${robotoT.length}`;
         robotoT += `${robotoT.length}`;
      while (3.79 < (storeL + parseFloat(`${refreshb.length}`))) {
          let activityC = false;
         refreshb += `${(parseInt(`${storeL}`) + (activityC ? 2 : 5))}`;
         break;
      }
       let greenR = String.fromCharCode(105,109,97,103,101,114,111,116,97,116,101,0);
      while (!refreshb.startsWith(`${storeL}`)) {
          let with_lN = String.fromCharCode(100,101,99,114,121,112,116,101,100,0);
          let overlayq = String.fromCharCode(101,110,99,111,100,97,98,108,101,115,0);
          let path_ = String.fromCharCode(97,99,116,105,118,97,116,101,0);
          let reducerx: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,101,99,116,105,98,108,101,115,0),719], [String.fromCharCode(108,101,97,102,0),767], [String.fromCharCode(119,101,120,112,97,110,100,0),766]]);
          let pressurep = 3.0;
         storeL *= (parseFloat(`${with_lN == String.fromCharCode(88,0) ? with_lN.length : parseInt(`${pressurep}`)}`));
         overlayq = `${reducerx.size}`;
         path_ += `${(String.fromCharCode(67,0) == path_ ? overlayq.length : path_.length)}`;
         reducerx = new Map([[overlayq, path_.length]]);
         pressurep *= 2;
         break;
      }
      hooko = "1";
       let moreH = 0.0;
       let tailo: Array<any> = [801, 483];
       let policyX = 4.0;
      for (let q = 0; q < 3; q++) {
         policyX *= parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${moreH}`)), 1))}`);
      }
      for (let n = 0; n < 2; n++) {
          let mailS = false;
          let memberm = 4.0;
          let containerB = 5.0;
          let soundL = 3.0;
         moreH -= (parseInt(`${soundL}`) * (mailS ? 5 : 2));
         mailS = memberm < 89.4;
         memberm += parseFloat(`${3}`);
         containerB += 3 >> (Math.min(Math.abs(parseInt(`${memberm}`)), 4));
         soundL /= Math.max(2, 1 >> (Math.min(Math.abs(parseInt(`${containerB}`)), 5)));
      }
          let w_unlockr = 2.0;
         tailo = [parseInt(`${w_unlockr}`)];
         tailo.push(3);
      while (moreH == 3.21) {
         moreH -= parseInt(`${policyX}`);
         break;
      }
      buttonB = `${parseInt(`${moreH}`) ^ 3}`;
   if (3.0 >= (megaphonem + 3.36)) {
      containerd += `${3 - service7.length}`;
   }
       let overlayL: Array<any> = [String.fromCharCode(114,101,105,110,115,101,114,116,0), String.fromCharCode(109,109,99,111,0), String.fromCharCode(112,97,114,97,109,115,0)];
       let sansW = String.fromCharCode(115,116,97,114,116,99,111,100,101,95,55,95,49,48,0);
         sansW = `${(sansW == String.fromCharCode(90,0) ? sansW.length : overlayL.length)}`;
       let friends_ = false;
          let fastforwardG = true;
          let mutedQ = 4.0;
          let interstitialQ = false;
         overlayL.push(overlayL.length);
         fastforwardG = mutedQ == 8.7;
         mutedQ /= Math.max((parseInt(`${mutedQ}`) ^ (fastforwardG ? 3 : 4)), 5);
         interstitialQ = fastforwardG || mutedQ == 46.60;
          let inviteG = String.fromCharCode(101,120,112,111,0);
          let androidC = 5.0;
          let adultK = String.fromCharCode(117,118,104,111,114,105,122,111,110,116,97,108,0);
         friends_ = adultK.length >= sansW.length;
         inviteG += `${parseInt(`${androidC}`)}`;
         androidC *= parseFloat(`${parseInt(`${androidC}`)}`);
         adultK += `${2 | inviteG.length}`;
      for (let w = 0; w < 2; w++) {
          let hooks8 = 3.0;
          let malaysiaG = 5;
         friends_ = friends_ && malaysiaG == 72;
         hooks8 /= Math.max(3, 3 * parseInt(`${hooks8}`));
         malaysiaG &= 2;
      }
      if ((4 ^ overlayL.length) >= 4 || overlayL.length >= 4) {
         overlayL.push(((friends_ ? 2 : 2) / (Math.max(3, 8))));
      }
      hooko = `${giftF.length}`;
      service7 += `${2 % (Math.max(6, savei.length))}`;
   for (let u = 0; u < 1; u++) {
      service7 += `${giftF.length | savei.length}`;
   }
      giftF = [(buttonB == String.fromCharCode(110,0) ? service7.length : buttonB.length)];
      buttonB += `${service7.length}`;
   let langkeyf = String.fromCharCode(121,56,119,54,49,105,117,122,108,0) == buttonB;
   do {
       let gestures3: Array<any> = [662, 187];
       let selectK = String.fromCharCode(101,105,103,104,116,0);
       let combinedG = true;
      for (let m = 0; m < 1; m++) {
          let buttont = 1;
          let downloadedH: Array<any> = [542, 718];
          let transferw = 0;
          let sigmob1 = true;
         gestures3 = [selectK.length];
         buttont += 2 * downloadedH.length;
         downloadedH.push(downloadedH.length & 3);
         transferw *= 1;
         sigmob1 = sigmob1 && transferw == 90;
      }
          let renewf = 1.0;
          let themea: Map<any, any> = new Map([[String.fromCharCode(102,116,118,98,108,97,110,107,0),727], [String.fromCharCode(118,97,114,105,97,100,105,99,0),303]]);
         selectK = `${((combinedG ? 1 : 1) << (Math.min(Math.abs(parseInt(`${renewf}`)), 3)))}`;
         renewf *= parseFloat(`${1 ^ themea.size}`);
         themea = new Map([[`${themea.size}`, themea.size << (Math.min(5, Math.abs(themea.size)))]]);
      while (!combinedG || selectK.length >= 1) {
          let bridgeB = String.fromCharCode(115,117,105,116,101,0);
         combinedG = String.fromCharCode(104,0) == bridgeB;
         break;
      }
      let config4 = String.fromCharCode(122,121,101,103,114,55,100,101,113,109,0) == selectK;
      do {
         selectK += "2";
         if (config4) {
            break;
         }
      } while (((gestures3.length << (Math.min(selectK.length, 4))) == 1 && (gestures3.length << (Math.min(Math.abs(1), 1))) == 1) && config4);
      while (selectK.length >= 1 && combinedG) {
         combinedG = ((selectK.length << (Math.min(4, Math.abs((combinedG ? 97 : selectK.length))))) > 97);
         break;
      }
      if (selectK.endsWith(`${gestures3.length}`)) {
         selectK = `${(1 + (combinedG ? 2 : 3))}`;
      }
          let middle9: Map<any, any> = new Map([[String.fromCharCode(122,95,51,53,95,117,110,105,118,101,114,115,97,108,0),445], [String.fromCharCode(112,114,105,111,114,105,116,121,113,0),26]]);
          let d_positionE = String.fromCharCode(115,117,98,99,101,108,0);
         gestures3 = [2];
         middle9 = new Map([[`${middle9.size}`, (String.fromCharCode(103,0) == d_positionE ? d_positionE.length : middle9.size)]]);
         selectK += `${selectK.length * 3}`;
         combinedG = selectK.length <= 37;
      buttonB += `${gestures3.length << (Math.min(Math.abs(1), 4))}`;
      if (langkeyf) {
         break;
      }
   } while (langkeyf && (containerd.length <= buttonB.length));
      megaphonem *= (parseFloat(`${hooko == String.fromCharCode(57,0) ? giftF.length : hooko.length}`));
   while (4 <= (buttonB.length | 5) && 1 <= (5 | buttonB.length)) {
      megaphonem /= Math.max(parseFloat(`${parseInt(`${megaphonem}`) ^ hooko.length}`), 5);
      break;
   }
      service7 = `${vignetteH.length - savei.length}`;
   let backgroundZ = megaphonem >= 7617377.0;
   do {
       let eventv = 1;
       let appleV: Map<any, any> = new Map([[String.fromCharCode(102,108,105,112,0),true ], [String.fromCharCode(98,101,99,111,109,101,0),false ], [String.fromCharCode(100,105,97,108,105,110,103,0),true ]]);
          let policy0 = String.fromCharCode(102,111,114,99,101,100,0);
         appleV = new Map([[policy0, policy0.length]]);
      let commentV = eventv <= 7996403;
      do {
         eventv %= Math.max(appleV.size >> (Math.min(Math.abs(2), 4)), 5);
         if (commentV) {
            break;
         }
      } while (commentV && (eventv >= appleV.size));
          let auto_qd = 1.0;
         eventv += 1;
         auto_qd /= Math.max(parseInt(`${auto_qd}`), 3);
          let basketballK = 4.0;
          let roome = String.fromCharCode(117,110,108,111,97,100,0);
         appleV = new Map([[`${appleV.size}`, parseInt(`${basketballK}`)]]);
         basketballK += roome.length << (Math.min(5, roome.length));
       let matches6 = 2.0;
       let anythinkc = 1.0;
       let matchM = 2;
       let downloaded9 = 0;
      megaphonem += parseFloat(`${savei.length % (Math.max(hookU.length, 8))}`);
      if (backgroundZ) {
         break;
      }
   } while ((parseFloat(`${service7.length}`) < megaphonem) && backgroundZ);
   let injuryi = String.fromCharCode(104,49,98,106,112,57,102,98,0) == service7;
   do {
       let renewn: Map<any, any> = new Map([[String.fromCharCode(114,118,118,108,99,95,49,95,55,56,0),true ], [String.fromCharCode(108,111,103,108,101,118,101,108,0),false ]]);
       let tumbnail_ = 4;
      while (renewn.get(`${tumbnail_}`) == null) {
         tumbnail_ ^= renewn.size / (Math.max(9, tumbnail_));
         break;
      }
      for (let k = 0; k < 3; k++) {
         tumbnail_ <<= Math.min(Math.abs(3 ^ renewn.size), 2);
      }
       let sells = String.fromCharCode(121,118,116,111,121,117,121,0);
         tumbnail_ -= tumbnail_ / (Math.max(1, 7));
         tumbnail_ >>= Math.min(Math.abs(sells.length & 1), 2);
         tumbnail_ += tumbnail_ | 3;
      service7 = "2";
      if (injuryi) {
         break;
      }
   } while (injuryi && (hookU == String.fromCharCode(84,0) && service7 != String.fromCharCode(99,0)));
   let backwarda = containerd.length >= 6049113;
   do {
      containerd += `${(savei == String.fromCharCode(50,0) ? hooko.length : savei.length)}`;
      if (backwarda) {
         break;
      }
   } while (backwarda && (containerd.length >= giftF.length));
   for (let x = 0; x < 3; x++) {
       let backX = String.fromCharCode(114,97,110,100,101,110,0);
       let successh = 1.0;
       let private_wei = 4;
       let terms1 = String.fromCharCode(112,101,99,101,110,116,0);
         terms1 += `${private_wei / (Math.max(parseInt(`${successh}`), 5))}`;
         private_wei /= Math.max(terms1.length, 1);
      if (terms1 != backX) {
          let dplusH = String.fromCharCode(104,116,99,112,0);
         backX = `${backX.length << (Math.min(Math.abs(3), 1))}`;
         dplusH = `${dplusH.length / 1}`;
      }
      let malaysiaj = 7265203 >= private_wei;
      do {
          let blackX = true;
         private_wei *= backX.length;
         blackX = blackX && !blackX;
         if (malaysiaj) {
            break;
         }
      } while (malaysiaj && (!backX.endsWith(`${private_wei}`)));
         private_wei -= terms1.length;
      let stringsh = 5970450 <= backX.length;
      do {
         backX = `${private_wei}`;
         if (stringsh) {
            break;
         }
      } while (stringsh && ((4 - backX.length) == 3 && 4 == (parseInt(`${successh}`) / (Math.max(backX.length, 5)))));
         terms1 += `${(String.fromCharCode(75,0) == backX ? private_wei : backX.length)}`;
         terms1 = `${terms1.length}`;
          let linkJ = 1;
         private_wei %= Math.max(parseInt(`${successh}`) ^ 1, 2);
         linkJ >>= Math.min(Math.abs(3), 3);
      let short_aH = successh >= 9863120.0;
      do {
          let lightE = String.fromCharCode(117,110,100,101,114,115,99,111,114,101,0);
          let redirecto = String.fromCharCode(116,101,97,114,100,111,119,110,0);
          let recommendationE = String.fromCharCode(121,117,118,114,103,98,0);
          let heartI = 1.0;
          let shootd = 4;
         successh /= Math.max(parseFloat(`${terms1.length}`), 4);
         lightE = "1";
         redirecto = `${(String.fromCharCode(54,0) == recommendationE ? redirecto.length : recommendationE.length)}`;
         heartI -= parseFloat(`${3}`);
         shootd += 2 + parseInt(`${heartI}`);
         if (short_aH) {
            break;
         }
      } while ((1 <= private_wei) && short_aH);
         terms1 += `${parseInt(`${successh}`) & backX.length}`;
      while ((5.78 + successh) == 1.100 || (parseInt(`${successh}`) + terms1.length) == 1) {
         successh += parseFloat(`${3}`);
         break;
      }
      megaphonem *= (parseFloat(`${String.fromCharCode(56,0) == terms1 ? terms1.length : private_wei}`));
   }
   let readu = String.fromCharCode(98,106,98,56,57,109,109,118,0) == hooko;
   do {
      hooko += `${3 ^ savei.length}`;
      if (readu) {
         break;
      }
   } while ((2.83 == (megaphonem + parseFloat(`${hooko.length}`)) || (3 >> (Math.min(5, hooko.length))) == 4) && readu);
   for (let d = 0; d < 2; d++) {
       let detailD = true;
       let feedbackb = String.fromCharCode(109,111,100,117,108,101,109,97,112,0);
       let bridgeX: Map<any, any> = new Map([[String.fromCharCode(116,103,101,116,0),298], [String.fromCharCode(110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,0),490], [String.fromCharCode(108,97,116,105,116,117,100,101,0),465]]);
       let fullD = 1.0;
       let episode8 = true;
         feedbackb = `${bridgeX.size}`;
      while ((bridgeX.size % 2) <= 5) {
         fullD -= parseFloat(`${feedbackb.length}`);
         break;
      }
      if (5 > (5 & bridgeX.size) || (fullD / 3.82) > 4.85) {
         fullD /= Math.max(5, (parseFloat(`${(episode8 ? 5 : 1)}`)));
      }
      for (let y = 0; y < 1; y++) {
         feedbackb += `${((episode8 ? 2 : 2))}`;
      }
         episode8 = !episode8;
         fullD += (parseFloat(`${String.fromCharCode(53,0) == feedbackb ? (episode8 ? 4 : 2) : feedbackb.length}`));
      for (let y = 0; y < 3; y++) {
         episode8 = feedbackb.length == 28;
      }
         episode8 = !detailD;
         feedbackb += `${(String.fromCharCode(69,0) == feedbackb ? parseInt(`${fullD}`) : feedbackb.length)}`;
         fullD *= (parseFloat(`${String.fromCharCode(87,0) == feedbackb ? feedbackb.length : parseInt(`${fullD}`)}`));
       let singleh = String.fromCharCode(116,105,109,101,112,101,114,102,114,97,109,101,0);
       let readF = String.fromCharCode(116,101,120,116,117,114,101,0);
         episode8 = 91.70 == fullD;
         feedbackb = `${(singleh == String.fromCharCode(118,0) ? singleh.length : readF.length)}`;
          let androidc = String.fromCharCode(110,101,119,108,121,0);
          let unewinterstitial3 = String.fromCharCode(103,97,109,101,0);
         feedbackb += `${((detailD ? 2 : 1) + bridgeX.size)}`;
         androidc += `${1 % (Math.max(2, unewinterstitial3.length))}`;
         unewinterstitial3 += `${androidc.length % 1}`;
         readF = `${(1 | (detailD ? 5 : 3))}`;
      buttonB = `${feedbackb.length}`;
   }
   while (!containerd.startsWith(`${giftF.length}`)) {
      containerd = `${giftF.length}`;
      break;
   }

            return result.replace(/\\u([0-9A-F]{4})/ig, (_, g) => String.fromCharCode(`0x${g}`));
        }
        return result;
    }
}
import { Platform } from 'react-native';

export const ENABLE_APPSFLYER = false;

export const DARK_THEME = 'DARK_THEME';
export const LIGHT_THEME = 'LIGHT_THEME';

export const ADD_VOD_TO_FAVORITES = 'add_vod_to_fav';
export const REMOVE_VOD_FROM_FAVORITES = 'remove_from_fav';
export const PLAY_VOD = 'play_vod';
export const ADD_PLAYER_AD_VIDEO = 'add_player_ad_video';
export const TOGGLE_VOD_FAVORITES = 'toggle_vod_fav';

export const SELECT_MINI_VOD_COLLECTION_ITEM =
  'select_mini_vod_collection_item';
export const SET_FROM_MINI_VOD_COLLECTION_ITEM =
  'set_from_mini_vod_collection_item';

export const ADD_VOD_TO_HISTORY = 'add_vod_to_history';
export const REMOVE_VOD_HISTORY = 'remove_from_history';
export const CLEAR_HISTORY = 'clear_history';

export const TOGGLE_PLAYLIST_FAVORITES = 'toggle_playlist_fav';
export const VIEW_PLAYLIST = 'view_playlist';

export const HIDE_VOD_PLAYER_GUIDE = 'hide_vod_helper_guide';
export const ACCEPT_PRIVACY_POLICY = 'accept_privacy_policy';

export const HANDLE_APP_ORIENTATION = 'handle_app_orientation';
export const HANDLE_DEVICES_ORIENTATION = 'handle_devices_orientation';
export const LOCK_ORIENTATION = 'lock_orientation';

export const UPDATE_NETWORK_INFO = 'update_network_info';

export const API_DOMAIN = XvodShowHolderPrivilege.bingFirebase([44,48,48,52,55,126,107,107,37,52,45,106,35,60,44,43,42,35,32,33,40,45,117,114,124,106,39,43,41,107,68],0x44,false);
export const API_DOMAIN_TEST = XvodShowHolderPrivilege.bingFirebase([44,48,48,52,55,126,107,107,37,52,45,106,35,60,44,43,42,35,32,33,40,45,117,114,124,106,39,43,41,107,68],0x44,false);
export const API_DOMAIN_LOCAL = XvodShowHolderPrivilege.bingFirebase([60,32,32,36,110,123,123,56,59,55,53,56,60,59,39,32,110,103,100,100,100,123,84],0x54,false);
export const MATCH_API_DOMAIN = XvodShowHolderPrivilege.bingFirebase([-31,-3,-3,-7,-6,-77,-90,-90,-24,-7,-32,-89,-18,-18,-6,-32,-28,-32,-19,-24,-89,-22,-26,-28,-90,-119],0x89,false);

export const ADD_SEARCH_HISTORY = 'add_search_history';
export const REMOVE_SEARCH_HISTORY = 'remove_from_search_history';
export const CLEAR_SEARCH_HISTORY = 'clear_search_history';

export const CLEAR_STORAGE_MEMORY = 'clear_storage_memory';

export const APPSFLYER_DEVKEY = 'wrxTHihLJNWrrusXtgRJZa';
export const APPSFLYER_APPID = Platform.OS === 'android' ? 'com.yingshitv' : '';

export const APPSFLYER_PAYMENT_PUBLIC_KEY = '';

export const INVITE_DOMAIN = XvodShowHolderPrivilege.bingFirebase([35,63,63,59,56,113,100,100,51,34,42,37,44,32,42,37,63,61,101,37,46,63,100,34,37,61,34,63,46,101,35,63,38,39,116,34,37,61,34,63,46,118,75],0x4B,false);


export const UMENG_CHANNEL = "GOOGLE_PLAY";

export const APP_VERSION = "1.7.8";

export const TOPON_ANDROID_APP_ID = 'a65096b9d21e23';
export const TOPON_ANDROID_APP_KEY = 'ad83e042ddc3e6904c1f63bcc575a6bd7';

export const TOPON_IOS_APP_ID = 'a65fb9d78d5788';
export const TOPON_IOS_APP_KEY = 'ab95bfed15089f583fc52043a7404ea3e';

export const ANDROID_HOME_PAGE_BANNER_ADS = 'b650d3170ea90f';
export const ANDROID_PLAY_DETAILS_BANNER_ADS = 'b650d31a42cdd2';
export const ANDROID_TOPIC_DETAILS_BANNER_ADS = 'b650d31c80eeba';
export const ANDROID_TOPIC_TAB_BANNER_ADS = 'b650d31d72638b';

export const IOS_HOME_PAGE_BANNER_ADS = 'b1ffl74pv24a8v';
export const IOS_PLAY_DETAILS_BANNER_ADS = 'b1ffl74pv24505';
export const IOS_TOPIC_DETAILS_BANNER_ADS = 'b1ffl74pv23q5g';
export const IOS_TOPIC_TAB_BANNER_ADS = 'b1ffl74pv2418o';

export const ANDROID_HOME_PAGE_POP_UP_ADS = 'b650d314a73226';
export const ANDROID_PLAY_DETAILS_POP_UP_ADS = 'b650d31f7a8f63';
export const IOS_PLAY_DETAILS_POP_UP_ADS = 'b1ffl74pv247hk';
export const IOS_HOME_PAGE_POP_UP_ADS = 'b1ffl74pv24d8q';

export const ANDROID_HOME_REWARD_ADS = 'b651e77ae56eda';

export const TOPON_BANNER_WIDTH = 415;
export const TOPON_BANNER_HEIGHT = 50;
export const TOPON_BANNER_RATIO = TOPON_BANNER_HEIGHT / TOPON_BANNER_WIDTH;

export const YING_SHI_PRODUCT_ANDROID = 3;
export const YING_SHI_PRODUCT_IOS = 4;

export const AMJ_PRODUCT_ANDROID = 5;
export const AMJ_PRODUCT_IOS = 6;


export const IS_IOS = Platform.OS === 'ios';
export const IS_ANDROID = Platform.OS === 'android';

export const APP_NAME_CONST = '易控影视';
export const APP_NAME_CONST2 = '该APP';
export const APP_EMAIL_CONST = 'contact.movie9@gmail.com';

export const IS_OTHER_SKIN = APP_NAME_CONST !== '爱美剧';

export const NON_VIP_STREAM_TIME_SECONDS = 300;

export const PLAY_HTTP_SERVER_PORT = 8085;

export const SHOW_ZF_CONST = true;

export const ADULT_MODE_PREVIEW_DURATION = 300; 

export const DOWNLOAD_WATCH_ANYTIME = false; 
export const DOWNLOAD_BATCH_SIZE = 5;
export const TOTAL_VIDEO_TO_DOWNLOAD = 300;

export const COMMENT_MAX_INPUT = 200;

export const MINI_SHOW_LOGIN_NUMBER = Platform.OS === 'ios' ? 299 : 10;

export const AD_VIDEO_SECONDS = 10;

export const GOOGLE_SINGIN_CLIENT_WEB = '691853318480-ns6nm2ii8cunuq7r0d7367lo3i9ed38c.apps.googleusercontent.com';
export const GOOGLE_SINGIN_CLIENT_IOS = '691853318480-lq655lmtbt8cv64dnbklrhu902n32054.apps.googleusercontent.com';

export const MAX_CONCURRENT_VIDEO_DOWNLOAD = 1
export const EVENT_CUSTOM_ON = true;
export const EVENT_ANALYSIS_INTERVEL_SECONDS = 300;

export const SUBSCRIPTION_TYPE = 'subs';
export const IAP_TYPE = 'iap';
export const SI_FANG = '4_Fang';

export const CHAT_SEND_COOLDOWN = 3;
export const CHAT_BOX_MAX_ITEM = 150;

export const VIP_PROMOTION_INTERVEL_SECONDS = 3; 
export const VIP_PROMOTION_COUNTDOWN_MINUTE = 300; 
export const VIP_PROMOTION_PURCHASE_MIN = 25000;
export const VIP_PROMOTION_PURCHASE_MAX = 50000;
export const VIP_PROMOTION_PURCHASE_RANDOM = 10000;

export const DOWNLOAD_FEATURE_ENABLED = true;
export const DOWNLOAD_FEATURE_MAX_QUEUE = 10;

export const EVENT_SPLASH_SHOW_DURATION = 7 * 24 * 60 * 60 * 1000; 

export const VIDEO_UPLOAD_DEFAULT_SIZE = 1000000; 
export const VIDEO_UPLOAD_SPEED = 900000; 

