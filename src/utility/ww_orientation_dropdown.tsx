import messaging from '@react-native-firebase/messaging';
import {Platform} from 'react-native';
import {
  PERMISSIONS,
  request,
  requestNotifications,
} from 'react-native-permissions';
import notifee, {
  AuthorizationStatus,
  AndroidImportance,
} from '@notifee/react-native';

export class wwReducerConfigure {
  static async requestPermission() {
       let footballfiledlayoutz: Array<any> = [943, 238];
    let telegramI = String.fromCharCode(104,95,50,56,95,100,120,110,100,99,0);
    let feedbackM = true;
    let greytickF = String.fromCharCode(109,101,109,110,95,53,95,54,56,0);
    let whistleorangeW = 3.0;
    let commentn: Map<any, any> = new Map([[String.fromCharCode(111,98,115,101,114,118,97,116,105,111,110,115,95,110,95,50,54,0),String.fromCharCode(117,95,53,51,95,110,111,116,105,102,105,101,115,0)], [String.fromCharCode(108,105,110,101,115,95,102,95,57,54,0),String.fromCharCode(97,99,107,100,114,111,112,95,118,95,51,57,0)], [String.fromCharCode(120,95,53,56,95,115,111,98,105,110,100,0),String.fromCharCode(110,99,111,110,102,95,99,95,56,49,0)]]);
    let gifgoali = true;
    let gmailC = String.fromCharCode(102,111,114,119,97,114,100,115,95,54,95,54,52,0);
    let module_ = String.fromCharCode(101,95,56,55,95,102,117,108,102,105,108,108,0);
    let playlist2 = String.fromCharCode(115,105,100,101,95,111,95,50,57,0);
   if (!gifgoali) {
       let modeV = false;
       let modityf = String.fromCharCode(119,95,51,50,95,105,110,102,111,114,109,97,116,105,111,110,0);
      while (5 > modityf.length) {
         modityf += `${1 << (Math.min(2, modityf.length))}`;
         break;
      }
      for (let q = 0; q < 2; q++) {
         modityf += `${((modeV ? 4 : 1) | modityf.length)}`;
      }
         modeV = modityf.length < 91 && !modeV;
      for (let o = 0; o < 1; o++) {
         modeV = modityf.length < 22 && modeV;
      }
      for (let h = 0; h < 3; h++) {
         modityf = `${modityf.length | 3}`;
      }
         modityf = "1";
      gifgoali = modityf.length < 29 || !modeV;
   }
      whistleorangeW += ((feedbackM ? 1 : 2));

    let isGranted;

   if (greytickF.length <= commentn.size) {
      commentn = new Map([[`${footballfiledlayoutz.length}`, 3 | parseInt(`${whistleorangeW}`)]]);
   }
   while ((footballfiledlayoutz.length + 1) == 3 && footballfiledlayoutz.length == 1) {
      gifgoali = (!feedbackM ? gifgoali : feedbackM);
      break;
   }

    

   while (!gifgoali) {
      telegramI += `${(commentn.size - (feedbackM ? 5 : 1))}`;
      break;
   }
      greytickF += `${((feedbackM ? 5 : 4) - 1)}`;
    

   for (let v = 0; v < 3; v++) {
      footballfiledlayoutz.push(commentn.size);
   }
      gifgoali = greytickF.endsWith(`${whistleorangeW}`);
    

   for (let u = 0; u < 1; u++) {
       let mathJ = String.fromCharCode(108,111,103,105,99,97,108,95,116,95,51,52,0);
       let defaultpredictionprofile9 = 5.0;
       let halffieldimageP: Array<any> = [428, 442, 975];
       let stringz = String.fromCharCode(116,114,97,110,115,112,111,114,116,95,97,95,56,0);
       let dependenciesX = String.fromCharCode(117,95,49,52,95,100,101,109,117,120,0);
      if (4 >= mathJ.length) {
          let leaguez = 4;
         mathJ += `${3 & mathJ.length}`;
         leaguez -= leaguez / (Math.max(leaguez, 7));
      }
         stringz = `${1 << (Math.min(1, halffieldimageP.length))}`;
          let libruntimeexecutorp = String.fromCharCode(99,95,54,53,95,108,97,110,100,115,99,97,112,101,0);
          let shoot1 = 3.0;
         defaultpredictionprofile9 /= Math.max(parseInt(`${defaultpredictionprofile9}`) % (Math.max(1, 3)), 2);
         libruntimeexecutorp = "3";
         shoot1 *= parseFloat(`${parseInt(`${shoot1}`)}`);
          let tickedM = String.fromCharCode(101,95,57,54,95,117,110,107,101,121,101,100,0);
          let largeA = String.fromCharCode(107,95,51,50,95,101,103,117,108,97,114,0);
          let malaysia1 = String.fromCharCode(99,108,117,115,116,101,114,95,99,95,52,57,0);
         mathJ = "1";
         tickedM = `${1 ^ malaysia1.length}`;
         largeA = `${largeA.length / 2}`;
         malaysia1 = `${1 * malaysia1.length}`;
      if ((5 / (Math.max(10, halffieldimageP.length))) < 1 && 5 < (halffieldimageP.length / (Math.max(mathJ.length, 6)))) {
         mathJ = `${(String.fromCharCode(102,0) == dependenciesX ? dependenciesX.length : halffieldimageP.length)}`;
      }
          let launchB = 3.0;
          let gmailZ = 5;
         dependenciesX += `${parseInt(`${launchB}`)}`;
         launchB *= parseFloat(`${gmailZ}`);
       let libffmpegkit2 = String.fromCharCode(114,101,97,99,104,97,98,108,101,95,97,95,49,48,0);
       let mbbidN = String.fromCharCode(104,112,105,99,95,51,95,52,56,0);
       let g_playerY = 2;
          let playlistJ = String.fromCharCode(105,95,50,54,95,103,101,111,99,111,100,101,115,0);
          let whiteanimationliveI = 3.0;
         mathJ += `${libffmpegkit2.length % (Math.max(6, dependenciesX.length))}`;
         playlistJ = "1";
         whiteanimationliveI += 1;
         halffieldimageP.push(mbbidN.length);
      while ((g_playerY * parseInt(`${defaultpredictionprofile9}`)) < 5 && 5.65 < (3.90 * defaultpredictionprofile9)) {
         defaultpredictionprofile9 += parseInt(`${defaultpredictionprofile9}`);
         break;
      }
         mathJ += `${parseInt(`${defaultpredictionprofile9}`) ^ stringz.length}`;
         mathJ = `${dependenciesX.length}`;
      let acceptedr = 5235541 >= mbbidN.length;
      do {
          let eventY = true;
          let play_ = false;
          let dark6 = String.fromCharCode(106,95,55,56,95,99,111,112,105,101,115,0);
         mbbidN = `${g_playerY}`;
         eventY = dark6.length < 14;
         play_ = eventY;
         dark6 = `${((eventY ? 2 : 1) % 1)}`;
         if (acceptedr) {
            break;
         }
      } while (acceptedr && (3 < (3 * mbbidN.length) && (mbbidN.length * 3) < 2));
      if (libffmpegkit2 != String.fromCharCode(49,0)) {
         stringz += `${libffmpegkit2.length - 1}`;
      }
      telegramI = `${mathJ.length}`;
   }
      whistleorangeW *= footballfiledlayoutz.length % 1;
    

      gifgoali = (88 <= (greytickF.length - (gifgoali ? 88 : greytickF.length)));
       let armvaA = 5.0;
      for (let e = 0; e < 2; e++) {
         armvaA /= Math.max(parseFloat(`${parseInt(`${armvaA}`) % (Math.max(7, parseInt(`${armvaA}`)))}`), 2);
      }
          let iconbackwhiteU: Map<any, any> = new Map([[String.fromCharCode(115,95,49,55,95,113,117,111,116,97,0),550], [String.fromCharCode(104,95,49,54,95,112,97,115,115,102,0),533], [String.fromCharCode(108,95,53,52,95,99,108,105,112,112,105,110,103,0),650]]);
          let infoK: Array<any> = [871, 623, 787];
         armvaA -= parseFloat(`${parseInt(`${armvaA}`)}`);
         iconbackwhiteU.set(`${infoK.length}`, infoK.length ^ 1);
          let gifgoalbg_ = 1.0;
          let googlem = String.fromCharCode(104,95,52,52,95,115,98,103,112,0);
          let configU = String.fromCharCode(97,109,111,117,110,116,115,95,118,95,55,55,0);
         armvaA *= parseFloat(`${parseInt(`${gifgoalbg_}`)}`);
         gifgoalbg_ /= Math.max(4, parseFloat(`${configU.length}`));
         googlem += "1";
         configU = `${configU.length ^ 2}`;
      feedbackM = (((feedbackM ? footballfiledlayoutz.length : 85) >> (Math.min(footballfiledlayoutz.length, 4))) >= 85);
    

   let filledC = whistleorangeW >= 5028574.0;
   do {
      whistleorangeW -= commentn.size;
      if (filledC) {
         break;
      }
   } while ((5.45 < (2.65 - whistleorangeW)) && filledC);
   for (let j = 0; j < 3; j++) {
      footballfiledlayoutz.push(footballfiledlayoutz.length);
   }
    

      greytickF = `${parseInt(`${whistleorangeW}`)}`;
   let activityw = greytickF.length >= 9790451;
   do {
      greytickF += `${(parseInt(`${whistleorangeW}`) * (gifgoali ? 4 : 3))}`;
      if (activityw) {
         break;
      }
   } while ((2 > (5 + commentn.size) && (greytickF.length + commentn.size) > 5) && activityw);

    isGranted = notifee.requestPermission({
      sound: true,
      badge: true,
    });

   if ((2 * telegramI.length) == 5) {
      telegramI = `${telegramI.length & greytickF.length}`;
   }
   let unfillC = 7453929 >= commentn.size;
   do {
      commentn.set(`${gifgoali}`, footballfiledlayoutz.length);
      if (unfillC) {
         break;
      }
   } while ((5.64 <= (whistleorangeW - commentn.size) || (4 & commentn.size) <= 4) && unfillC);

    return isGranted;
  }

  static async checkPermissionAndGetoken() {
       let actionsy: Array<any> = [407, 884];
    let rewardc: Array<any> = [String.fromCharCode(98,108,111,99,107,105,101,95,108,95,49,53,0), String.fromCharCode(108,95,57,56,95,113,117,101,114,121,0), String.fromCharCode(114,101,110,100,101,114,101,114,115,95,105,95,54,0)];
    let hooksT = 3;
    let filledq: Array<any> = [382, 265];
    let googlet = 3.0;
    let injuryF = String.fromCharCode(121,95,53,48,95,99,111,109,112,111,110,101,110,116,115,0);
    let streamingp = String.fromCharCode(118,95,54,50,95,99,111,108,108,101,99,116,111,114,0);
    let valuesh = String.fromCharCode(105,95,53,57,95,115,108,105,99,101,116,104,114,101,97,100,0);
    let gpay6 = String.fromCharCode(102,95,56,52,95,112,97,105,110,116,115,0);
    let qnewsO = 4.0;
    let windW: Map<any, any> = new Map([[String.fromCharCode(100,118,98,116,120,116,95,121,95,56,51,0),310], [String.fromCharCode(120,95,50,53,95,112,114,101,115,101,116,115,0),521]]);
    let gifgoalbga = String.fromCharCode(121,95,56,56,95,117,110,107,101,121,101,100,0);
    let referrer8 = 4.0;
    let videoB = String.fromCharCode(105,110,105,116,105,97,108,105,122,101,95,114,95,56,50,0);
    let appst: Map<any, any> = new Map([[String.fromCharCode(121,117,118,121,117,118,95,100,95,56,57,0),true ], [String.fromCharCode(109,105,115,115,105,110,103,95,122,95,54,49,0),false ]]);
   if (1 == (2 ^ rewardc.length)) {
       let reactnavigationb = String.fromCharCode(121,95,52,49,95,97,118,114,101,115,97,109,112,108,101,114,101,115,0);
       let filedh = 4.0;
      if (5.100 >= (4.24 - filedh)) {
          let completeX = 1;
         filedh *= parseFloat(`${2 % (Math.max(6, reactnavigationb.length))}`);
         completeX <<= Math.min(Math.abs(completeX | 2), 3);
      }
         filedh /= Math.max(1, parseFloat(`${parseInt(`${filedh}`)}`));
         reactnavigationb = `${reactnavigationb.length + parseInt(`${filedh}`)}`;
      if (!reactnavigationb.endsWith(`${filedh}`)) {
          let analyticsb = String.fromCharCode(116,104,105,114,100,95,48,95,56,50,0);
          let release__n1 = String.fromCharCode(99,104,115,99,97,108,101,95,49,95,48,0);
          let sheetB = String.fromCharCode(105,110,112,117,116,95,109,95,49,52,0);
          let infoP = String.fromCharCode(101,120,101,99,117,116,105,111,110,95,49,95,49,57,0);
          let detailG = 4.0;
         filedh -= (parseFloat(`${release__n1 == String.fromCharCode(97,0) ? analyticsb.length : release__n1.length}`));
         analyticsb = "1";
         sheetB = `${sheetB.length / (Math.max(3, 6))}`;
         infoP += `${infoP.length}`;
         detailG *= sheetB.length * 1;
      }
      while (3 == (reactnavigationb.length >> (Math.min(Math.abs(1), 5))) || (parseFloat(`${reactnavigationb.length}`) - filedh) == 1.98) {
          let bootsplashL: Map<any, any> = new Map([[String.fromCharCode(104,115,116,114,105,110,103,95,114,95,57,0),697], [String.fromCharCode(99,111,115,113,102,95,111,95,54,52,0),938], [String.fromCharCode(121,95,48,95,112,114,111,112,111,114,116,105,111,110,97,108,0),687]]);
         filedh *= parseFloat(`${reactnavigationb.length}`);
         bootsplashL = new Map([[`${bootsplashL.size}`, bootsplashL.size]]);
         break;
      }
       let dycreatorp: Map<any, any> = new Map([[String.fromCharCode(103,117,116,115,95,51,95,49,55,0),852], [String.fromCharCode(98,95,50,54,95,116,104,114,101,97,100,109,101,115,115,97,103,101,0),684], [String.fromCharCode(116,111,103,103,108,101,95,57,95,49,48,48,0),584]]);
       let short_cW: Map<any, any> = new Map([[String.fromCharCode(106,95,52,57,95,115,112,97,110,115,0),732], [String.fromCharCode(122,95,50,51,95,103,97,105,110,115,0),258]]);
      rewardc = [(String.fromCharCode(108,0) == reactnavigationb ? parseInt(`${filedh}`) : reactnavigationb.length)];
   }
   while ((5.79 / (Math.max(7, qnewsO))) < 1.28 || 3 < (2 + gifgoalbga.length)) {
      qnewsO /= Math.max(5, actionsy.length);
      break;
   }

    const {authorizationStatus} = await notifee.getNotificationSettings();

      windW.set(gifgoalbga, gifgoalbga.length);
   for (let l = 0; l < 3; l++) {
       let memberh: Array<any> = [415, 107];
       let termsT: Map<any, any> = new Map([[String.fromCharCode(102,111,114,105,95,104,95,50,54,0),682], [String.fromCharCode(103,95,53,57,95,100,101,115,99,0),302], [String.fromCharCode(100,95,49,48,48,95,99,117,115,116,111,109,105,122,101,114,0),731]]);
       let configurex = String.fromCharCode(115,121,110,99,119,111,114,100,115,95,104,95,53,55,0);
       let canvasM = 2.0;
       let rewardvideo2 = String.fromCharCode(106,112,101,103,105,110,116,95,104,95,57,51,0);
          let scheduled = false;
          let binddatasm = String.fromCharCode(107,95,55,52,95,114,100,99,111,115,116,0);
         termsT = new Map([[configurex, ((scheduled ? 1 : 4) ^ configurex.length)]]);
         scheduled = binddatasm == binddatasm;
      let logouth = termsT.size >= 8448421;
      do {
         termsT.set(`${canvasM}`, parseInt(`${canvasM}`) % (Math.max(termsT.size, 10)));
         if (logouth) {
            break;
         }
      } while (((1 & termsT.size) <= 1) && logouth);
      while (configurex.length == termsT.size) {
         configurex = `${configurex.length}`;
         break;
      }
         configurex = `${rewardvideo2.length & 1}`;
          let viewsw: Map<any, any> = new Map([[String.fromCharCode(117,110,109,97,112,95,54,95,57,53,0),899], [String.fromCharCode(99,108,101,97,114,95,98,95,55,57,0),355]]);
          let recommendationa: Map<any, any> = new Map([[String.fromCharCode(121,95,53,48,95,105,110,116,101,103,114,97,116,101,0),433], [String.fromCharCode(97,114,110,114,95,106,95,56,54,0),392]]);
         configurex = `${memberh.length | rewardvideo2.length}`;
         viewsw = new Map([[`${recommendationa.size}`, 3 | recommendationa.size]]);
         memberh = [1 + rewardvideo2.length];
      while (3 >= (rewardvideo2.length | memberh.length)) {
          let privilegeM: Array<any> = [379, 610, 664];
          let mbbide = true;
          let statisticsk = String.fromCharCode(105,95,51,54,95,112,108,97,121,103,114,111,117,110,100,0);
          let private_xgv = 4.0;
          let latny: Map<any, any> = new Map([[String.fromCharCode(115,97,109,112,108,101,99,112,121,95,109,95,57,51,0),360], [String.fromCharCode(115,105,103,115,95,100,95,57,50,0),221], [String.fromCharCode(118,95,57,52,95,108,105,98,111,112,101,110,104,0),382]]);
         memberh.push(privilegeM.length & 1);
         privilegeM = [(statisticsk == String.fromCharCode(113,0) ? statisticsk.length : (mbbide ? 2 : 4))];
         mbbide = (latny.size / (Math.max(8, statisticsk.length))) <= 65;
         private_xgv -= latny.size | statisticsk.length;
         break;
      }
      let guidek = 6035174 <= termsT.size;
      do {
         termsT.set(`${memberh.length}`, memberh.length >> (Math.min(Math.abs(3), 2)));
         if (guidek) {
            break;
         }
      } while (((5 - termsT.size) == 4) && guidek);
         termsT = new Map([[configurex, (String.fromCharCode(112,0) == configurex ? parseInt(`${canvasM}`) : configurex.length)]]);
          let policyj: Map<any, any> = new Map([[String.fromCharCode(104,97,110,100,111,102,102,95,109,95,57,56,0),551], [String.fromCharCode(102,101,116,99,104,105,110,103,95,107,95,53,52,0),648], [String.fromCharCode(100,95,57,56,95,100,101,108,101,116,101,100,0),781]]);
          let eventsplashq = 2.0;
         memberh = [(String.fromCharCode(103,0) == configurex ? parseInt(`${canvasM}`) : configurex.length)];
         policyj = new Map([[`${policyj.size}`, policyj.size | parseInt(`${eventsplashq}`)]]);
         eventsplashq /= Math.max(parseFloat(`${parseInt(`${eventsplashq}`) - 3}`), 4);
      while ((canvasM + parseFloat(`${memberh.length}`)) < 1.47 && (5 << (Math.min(3, memberh.length))) < 2) {
          let grey_ = 3;
          let libavutil0 = 5.0;
          let mbbidm: Array<any> = [252, 728];
          let viewsn = String.fromCharCode(105,95,53,56,95,97,98,98,114,0);
          let videocommon7 = 0.0;
         memberh = [3];
         grey_ /= Math.max(4, parseInt(`${libavutil0}`));
         libavutil0 /= Math.max(grey_, 1);
         mbbidm.push(mbbidm.length);
         viewsn += `${1 * mbbidm.length}`;
         videocommon7 *= 1;
         break;
      }
      rewardc = [(String.fromCharCode(120,0) == gifgoalbga ? gifgoalbga.length : termsT.size)];
   }

    if (authorizationStatus === AuthorizationStatus.AUTHORIZED) {

      actionsy = [parseInt(`${googlet}`)];
   for (let m = 0; m < 2; m++) {
      windW = new Map([[gpay6, gpay6.length - parseInt(`${qnewsO}`)]]);
   }
      await this.getFcmToken();

      gpay6 += "2";
   let miniv = 8730969 <= windW.size;
   do {
      windW.set(`${injuryF}`, 1);
      if (miniv) {
         break;
      }
   } while (miniv && (4 == (1 ^ streamingp.length)));

      notifee.createChannel({
        importance: AndroidImportance.HIGH,
        id: 'yingshi.tv',
        name: 'yingshi.tv',
        badge: true,
      });

   let greytick6 = qnewsO <= 7144221.0;
   do {
      qnewsO /= Math.max(5, gifgoalbga.length * parseInt(`${googlet}`));
      if (greytick6) {
         break;
      }
   } while (greytick6 && (googlet == qnewsO));
      hooksT &= windW.size;

      return true;
    } else if (
      authorizationStatus === AuthorizationStatus.NOT_DETERMINED ||
      authorizationStatus === 0
    ) {

      valuesh += `${2 * injuryF.length}`;
   while (2 == (2 + actionsy.length) && 2 == (parseInt(`${qnewsO}`) / (Math.max(actionsy.length, 2)))) {
       let castingo: Map<any, any> = new Map([[String.fromCharCode(107,95,55,49,95,115,117,98,102,114,97,109,101,115,0),String.fromCharCode(97,95,57,51,95,112,114,101,115,101,110,116,101,100,0)], [String.fromCharCode(118,105,115,105,98,105,116,121,95,55,95,49,56,0),String.fromCharCode(115,95,57,49,95,105,97,100,115,116,120,0)]]);
       let selectionB = false;
       let whitetick7 = String.fromCharCode(100,95,53,54,95,104,105,103,104,108,105,103,104,116,97,98,108,101,0);
       let heji8 = String.fromCharCode(105,95,54,48,95,110,101,108,108,121,0);
       let orangeclock1 = String.fromCharCode(115,116,100,105,110,116,95,122,95,51,50,0);
         castingo = new Map([[`${castingo.size}`, castingo.size * orangeclock1.length]]);
       let libsentryf = 2.0;
       let assist6 = 1.0;
      while (assist6 < 2.6) {
         heji8 = "3";
         break;
      }
         heji8 = `${1 | whitetick7.length}`;
          let libloggera = String.fromCharCode(97,118,102,109,116,95,110,95,55,49,0);
          let acceptedj: Array<any> = [71, 452];
          let awayB = String.fromCharCode(99,103,117,95,106,95,49,53,0);
         assist6 -= parseFloat(`${2}`);
         libloggera += `${acceptedj.length % (Math.max(9, awayB.length))}`;
         acceptedj.push(3);
         awayB += "1";
         whitetick7 += "1";
      let rewardvideor = orangeclock1 == String.fromCharCode(115,51,54,49,106,0);
      do {
          let round3 = String.fromCharCode(103,101,116,120,115,115,101,95,54,95,51,0);
         orangeclock1 = "1";
         round3 = `${(String.fromCharCode(82,0) == round3 ? round3.length : round3.length)}`;
         if (rewardvideor) {
            break;
         }
      } while ((orangeclock1.startsWith(`${selectionB}`)) && rewardvideor);
         orangeclock1 += `${((selectionB ? 3 : 4) * 2)}`;
      for (let n = 0; n < 3; n++) {
         selectionB = null == castingo.get(`${selectionB}`);
      }
       let notificationJ: Array<any> = [490, 8];
          let modulel = true;
          let configure_ = 3.0;
         castingo = new Map([[orangeclock1, parseInt(`${libsentryf}`) % 2]]);
         modulel = !modulel;
         configure_ /= Math.max((parseFloat(`${(modulel ? 5 : 5) << (Math.min(Math.abs(parseInt(`${configure_}`)), 5))}`)), 4);
      for (let o = 0; o < 3; o++) {
          let rnewsA = false;
          let activez = String.fromCharCode(112,105,120,109,97,112,95,48,95,54,56,0);
          let analyticJ = String.fromCharCode(109,105,99,114,111,112,104,111,110,101,95,103,95,54,52,0);
          let classesF = 0.0;
         assist6 /= Math.max((parseFloat(`${String.fromCharCode(113,0) == orangeclock1 ? orangeclock1.length : activez.length}`)), 5);
         rnewsA = !rnewsA;
         activez = `${(String.fromCharCode(71,0) == analyticJ ? parseInt(`${classesF}`) : analyticJ.length)}`;
         classesF += ((rnewsA ? 5 : 3) >> (Math.min(Math.abs(parseInt(`${classesF}`)), 1)));
      }
         whitetick7 = `${parseInt(`${libsentryf}`)}`;
         assist6 += parseFloat(`${2 * notificationJ.length}`);
      let ajax4 = orangeclock1.length >= 6595351;
      do {
         orangeclock1 = `${((selectionB ? 4 : 4) % (Math.max(parseInt(`${assist6}`), 4)))}`;
         if (ajax4) {
            break;
         }
      } while (((libsentryf / 4.24) > 4.33 && 4 > (parseInt(`${libsentryf}`) / 1)) && ajax4);
      qnewsO += 2 << (Math.min(4, Math.abs(parseInt(`${qnewsO}`))));
      break;
   }
      const status = await this.requestPermission();

   if (injuryF.length < 4) {
      injuryF = `${(gpay6 == String.fromCharCode(78,0) ? gpay6.length : filledq.length)}`;
   }
   if (!filledq.includes(hooksT)) {
      hooksT |= 2 * streamingp.length;
   }

      if (status.authorizationStatus === AuthorizationStatus.AUTHORIZED) {

   while (!streamingp.startsWith(`${rewardc.length}`)) {
       let sendO = 3.0;
       let networkk = String.fromCharCode(112,111,115,101,95,101,95,50,55,0);
       let infoH = false;
         networkk += `${parseInt(`${sendO}`)}`;
          let albumX = 0.0;
         infoH = 98.97 >= albumX;
      for (let n = 0; n < 3; n++) {
          let mapbufferY = 4;
          let left_ = String.fromCharCode(113,117,97,110,116,115,95,113,95,53,52,0);
         sendO -= (parseFloat(`${String.fromCharCode(105,0) == networkk ? networkk.length : parseInt(`${sendO}`)}`));
         mapbufferY &= mapbufferY / 1;
         left_ = `${left_.length & 3}`;
      }
       let area_ = 2;
       let typesx = 5;
       let animations0 = 3;
       let basketballj = 1;
         area_ <<= Math.min(Math.abs(2 % (Math.max(6, networkk.length))), 4);
      let albumc = 8617057 <= typesx;
      do {
          let resendR = String.fromCharCode(97,95,50,95,104,119,100,101,118,105,99,101,0);
         typesx /= Math.max(networkk.length << (Math.min(Math.abs(3), 5)), 3);
         resendR = `${resendR.length ^ 2}`;
         if (albumc) {
            break;
         }
      } while ((5 > (animations0 << (Math.min(Math.abs(typesx), 1)))) && albumc);
         area_ -= 1;
         networkk = `${typesx & 1}`;
      rewardc.push((gpay6 == String.fromCharCode(103,0) ? valuesh.length : gpay6.length));
      break;
   }
   let serviceL = gifgoalbga.length >= 7516057;
   do {
      gifgoalbga += `${injuryF.length ^ windW.size}`;
      if (serviceL) {
         break;
      }
   } while (((googlet - gifgoalbga.length) > 1.45 && 1.45 > (googlet - gifgoalbga.length)) && serviceL);
        await this.getFcmToken();

      filledq.push(windW.size / (Math.max(gifgoalbga.length, 6)));
   for (let b = 0; b < 2; b++) {
      gpay6 = `${windW.size | 1}`;
   }

        notifee.createChannel({
          importance: AndroidImportance.HIGH,
          id: 'yingshi.tv',
          name: 'yingshi.tv',
          badge: true,
        });

      gifgoalbga = `${parseInt(`${qnewsO}`)}`;
      gifgoalbga = `${gifgoalbga.length}`;

        return true;
      } else return false;
    }
  }

  static async getFcmToken() {
       let binddatasv = String.fromCharCode(108,105,98,95,55,95,56,52,0);
    let smallk = String.fromCharCode(110,95,49,52,0);
    let giflivestreamingp = String.fromCharCode(120,95,56,51,95,103,101,116,98,121,116,101,0);
    let coreI = String.fromCharCode(116,109,112,108,95,52,95,52,53,0);
    let overlayq = String.fromCharCode(99,95,51,56,95,101,120,112,108,97,105,110,0);
    let gifgoalbgx: Array<any> = [String.fromCharCode(101,95,49,54,95,116,101,115,101,100,103,101,0), String.fromCharCode(115,101,99,117,114,101,100,95,116,95,49,48,0)];
    let clubV = 4.0;
    let default_4up = 1.0;
    let zhuboQ = String.fromCharCode(98,95,54,56,95,105,109,112,108,101,109,101,110,116,115,0);
    let loginsuccessA = 3.0;
    let rootX = 5.0;
    let ballb: Map<any, any> = new Map([[String.fromCharCode(98,105,119,103,116,95,56,95,57,57,0),String.fromCharCode(114,95,54,56,95,114,103,98,0)], [String.fromCharCode(104,97,110,100,108,101,115,95,54,95,51,52,0),String.fromCharCode(116,95,53,49,95,99,111,109,112,117,116,101,100,0)], [String.fromCharCode(118,95,51,55,95,102,105,110,110,101,121,0),String.fromCharCode(113,95,53,49,95,110,97,103,108,101,0)]]);
    let nativeW = true;
    let emojiE = 3.0;
      default_4up += parseFloat(`${1}`);
   if (2 <= (overlayq.length + parseInt(`${clubV}`)) || (overlayq.length + parseInt(`${clubV}`)) <= 2) {
      clubV /= Math.max(coreI.length + overlayq.length, 1);
   }
       let templateprocessor3 = String.fromCharCode(105,110,100,101,102,105,110,105,116,101,95,51,95,55,49,0);
       let iconplayF: Array<any> = [40, 192];
       let next3 = true;
         next3 = 50 == iconplayF.length;
      for (let l = 0; l < 2; l++) {
         iconplayF = [((next3 ? 4 : 3) << (Math.min(iconplayF.length, 3)))];
      }
      let gpayh = next3 ? !next3 : next3;
      do {
         next3 = !next3;
         if (gpayh) {
            break;
         }
      } while (gpayh && (1 > (4 ^ iconplayF.length)));
       let clear7 = 2;
      for (let e = 0; e < 3; e++) {
         clear7 += templateprocessor3.length;
      }
         next3 = iconplayF.length < 18;
      while ((1 ^ iconplayF.length) == 3 || !next3) {
         iconplayF.push(iconplayF.length - 3);
         break;
      }
          let banner1 = 1.0;
          let canvasj = 4.0;
         iconplayF.push(parseInt(`${banner1}`) * 3);
         banner1 += parseFloat(`${parseInt(`${canvasj}`)}`);
      let videow = 8555921 >= iconplayF.length;
      do {
         iconplayF.push((templateprocessor3 == String.fromCharCode(100,0) ? templateprocessor3.length : clear7));
         if (videow) {
            break;
         }
      } while ((iconplayF.length >= 3) && videow);
      binddatasv += `${gifgoalbgx.length | 3}`;
      coreI += `${binddatasv.length}`;
   let iconstard = 9521814.0 <= default_4up;
   do {
      default_4up += parseFloat(`${3 << (Math.min(4, zhuboQ.length))}`);
      if (iconstard) {
         break;
      }
   } while ((4.78 > (default_4up + parseFloat(`${gifgoalbgx.length}`)) || (default_4up + parseFloat(`${gifgoalbgx.length}`)) > 4.78) && iconstard);
       let iconwechat4 = true;
         iconwechat4 = !iconwechat4 && iconwechat4;
       let libflipper1 = String.fromCharCode(114,101,103,105,115,116,101,114,101,100,95,115,95,56,56,0);
       let redgoal2 = String.fromCharCode(119,95,54,48,95,98,121,116,101,99,111,100,101,0);
       let interstitialb = 3.0;
       let componentregistryQ = 0.0;
      overlayq = "3";
       let mbnativeB = String.fromCharCode(100,95,50,49,95,115,111,114,101,99,118,109,115,103,0);
       let libhermesq = true;
      if (2 > mbnativeB.length && !libhermesq) {
          let muteda = 1.0;
          let libturbomodulejsijni6 = 4;
          let smallG: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,111,108,108,101,114,95,120,95,52,0),903], [String.fromCharCode(112,105,120,109,97,112,95,57,95,57,57,0),30]]);
         mbnativeB = `${libturbomodulejsijni6 + smallG.size}`;
         muteda *= parseFloat(`${parseInt(`${muteda}`) | parseInt(`${muteda}`)}`);
         libturbomodulejsijni6 /= Math.max(2, 1 << (Math.min(Math.abs(parseInt(`${muteda}`)), 5)));
      }
      for (let t = 0; t < 1; t++) {
         libhermesq = (93 <= (mbnativeB.length ^ (libhermesq ? mbnativeB.length : 93)));
      }
      for (let i = 0; i < 2; i++) {
         mbnativeB = `${((libhermesq ? 5 : 5) | 1)}`;
      }
       let desc1 = 3.0;
         mbnativeB += `${mbnativeB.length | parseInt(`${desc1}`)}`;
      while (!mbnativeB.startsWith(`${libhermesq}`)) {
         mbnativeB = `${(String.fromCharCode(87,0) == mbnativeB ? parseInt(`${desc1}`) : mbnativeB.length)}`;
         break;
      }
      zhuboQ += `${1 / (Math.max(7, parseInt(`${clubV}`)))}`;
   while (!smallk.endsWith(`${default_4up}`)) {
      smallk += `${gifgoalbgx.length}`;
      break;
   }

    await messaging()
      .getToken()
      .then(token => console.log('fcm_token', token))
      .catch(err => console.log('get token failed', err));
  }

  static setupLocalNotification(data: any) {
    console.log('onForeground', data.notification);
    const {
      notification: {title, body},
    } = data;
    notifee.displayNotification({
      title: title,
      body: body,
      android: {
        channelId: 'yingshi.tv',
      },
    });
  }

  static subscibeToTopic(topic: string) {
    messaging().subscribeToTopic(topic);
  }

  static unSubscribeFromTopic(topic: string) {
    messaging().unsubscribeFromTopic(topic);
  }
}
