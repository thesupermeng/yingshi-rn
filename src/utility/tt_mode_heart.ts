export const getMaxWidth = (text: string, fontSize: number, ratio = 1) => {
       let reminderK: Map<any, any> = new Map([[String.fromCharCode(114,95,50,50,95,115,116,114,99,115,112,110,0),440], [String.fromCharCode(108,95,50,49,95,102,111,108,100,0),585]]);
    let whatsappo = String.fromCharCode(105,115,115,117,101,114,95,109,95,54,57,0);
    let plashg = 1.0;
    let mbsplashO = 2.0;
    let vietnamk = true;
    let foundw = String.fromCharCode(119,95,53,56,95,99,104,97,114,99,111,110,118,0);
    let activeT = String.fromCharCode(111,95,51,55,95,104,97,118,101,101,118,101,110,116,115,0);
   while (1 == (parseInt(`${mbsplashO}`) + foundw.length)) {
       let privilegeC = String.fromCharCode(109,95,53,54,95,100,105,102,0);
       let product9 = false;
       let chinaQ = String.fromCharCode(113,95,49,56,95,111,116,104,101,114,119,105,115,101,0);
       let connectionu = String.fromCharCode(97,99,113,117,105,114,101,100,95,114,95,52,48,0);
      let index7 = connectionu == String.fromCharCode(53,121,120,103,54,100,104,56,0);
      do {
         connectionu = "2";
         if (index7) {
            break;
         }
      } while ((4 < chinaQ.length) && index7);
      while (!product9) {
          let combineK = String.fromCharCode(121,95,51,56,95,114,111,103,114,101,115,115,0);
         product9 = 48 == privilegeC.length && 48 == connectionu.length;
         combineK += "2";
         break;
      }
       let videocommoni = String.fromCharCode(108,105,98,99,95,121,95,51,48,0);
         connectionu = `${videocommoni.length}`;
          let dialog2 = String.fromCharCode(112,114,105,111,114,105,116,121,95,120,95,52,52,0);
          let incidentn = 4.0;
         videocommoni += `${(String.fromCharCode(109,0) == connectionu ? connectionu.length : videocommoni.length)}`;
         dialog2 += `${dialog2.length}`;
         incidentn /= Math.max(1, (String.fromCharCode(69,0) == dialog2 ? parseInt(`${incidentn}`) : dialog2.length));
      foundw = `${whatsappo.length}`;
      break;
   }
      whatsappo += "3";
       let sportB = String.fromCharCode(105,95,54,57,95,119,101,98,118,105,101,119,0);
       let g_titleJ = String.fromCharCode(112,95,50,48,95,108,101,102,116,109,111,115,116,0);
       let serviceQ = String.fromCharCode(104,95,56,56,95,99,97,108,105,110,103,0);
      while (3 >= g_titleJ.length && sportB != String.fromCharCode(86,0)) {
          let searchbarn = String.fromCharCode(117,110,99,111,109,112,114,101,115,115,101,100,95,57,95,50,54,0);
          let flyere = 5.0;
          let placeholderF = String.fromCharCode(113,95,50,57,95,116,111,111,108,0);
          let teami = 5.0;
          let hooks7 = String.fromCharCode(108,95,52,57,95,108,111,116,116,105,101,112,114,111,120,121,109,111,100,101,108,0);
         sportB += `${g_titleJ.length ^ 2}`;
         searchbarn = `${parseInt(`${flyere}`) | placeholderF.length}`;
         flyere /= Math.max(4, (hooks7 == String.fromCharCode(102,0) ? hooks7.length : searchbarn.length));
         placeholderF += `${parseInt(`${teami}`)}`;
         break;
      }
          let expiredo = 1.0;
         sportB = `${3 + sportB.length}`;
         expiredo *= parseFloat(`${1}`);
         g_titleJ = `${g_titleJ.length ^ sportB.length}`;
       let carouselG: Map<any, any> = new Map([[String.fromCharCode(101,95,56,95,111,117,116,112,117,116,0),false ], [String.fromCharCode(104,119,97,101,115,95,122,95,53,53,0),true ], [String.fromCharCode(109,95,56,55,95,114,101,99,97,108,99,0),true ]]);
       let update_2q: Map<any, any> = new Map([[String.fromCharCode(113,95,52,55,95,118,101,110,117,101,115,0),String.fromCharCode(99,111,110,115,116,97,110,116,95,101,95,55,56,0)], [String.fromCharCode(113,95,53,50,95,100,101,108,101,103,97,116,111,114,0),String.fromCharCode(99,95,54,49,95,116,111,111,108,98,97,114,0)], [String.fromCharCode(108,95,52,56,95,109,97,114,107,105,110,103,0),String.fromCharCode(116,105,109,101,111,117,116,95,51,95,51,55,0)]]);
      while ((carouselG.size << (Math.min(Math.abs(1), 5))) <= 4 && 1 <= (1 << (Math.min(2, Math.abs(carouselG.size))))) {
          let manifestU = String.fromCharCode(116,95,56,50,95,105,110,100,101,102,105,110,105,116,101,0);
          let anner2 = 2;
          let stationI = false;
          let submit8 = String.fromCharCode(105,95,56,55,95,105,110,118,105,116,97,116,105,111,110,115,0);
         carouselG.set(`${stationI}`, carouselG.size / (Math.max(1, 10)));
         manifestU += `${(manifestU == String.fromCharCode(77,0) ? anner2 : manifestU.length)}`;
         anner2 &= submit8.length;
         submit8 += `${submit8.length}`;
         break;
      }
         g_titleJ += `${2 << (Math.min(2, sportB.length))}`;
         update_2q.set(g_titleJ, g_titleJ.length);
      let feedbackN = 5409339 >= update_2q.size;
      do {
         update_2q = new Map([[`${carouselG.size}`, carouselG.size - serviceQ.length]]);
         if (feedbackN) {
            break;
         }
      } while (feedbackN && ((4 - serviceQ.length) < 3));
      while (1 == (serviceQ.length / (Math.max(4, 8))) || (update_2q.size / (Math.max(serviceQ.length, 7))) == 4) {
          let shoot1 = false;
          let nativeex_: Array<any> = [104, 428];
          let refreshn = String.fromCharCode(116,95,52,52,95,100,101,99,111,100,101,0);
          let tumbnail1 = String.fromCharCode(114,97,99,105,110,103,95,112,95,54,48,0);
         update_2q.set(g_titleJ, (String.fromCharCode(74,0) == g_titleJ ? refreshn.length : g_titleJ.length));
         shoot1 = nativeex_.length < 23 && String.fromCharCode(101,0) == tumbnail1;
         nativeex_ = [1];
         refreshn += `${nativeex_.length}`;
         tumbnail1 = `${nativeex_.length}`;
         break;
      }
      whatsappo = `${serviceQ.length | activeT.length}`;

    let size = 0;

       let carouselT = 5;
       let debug8 = String.fromCharCode(110,95,54,50,95,116,105,109,105,110,103,0);
       let spinnerc = String.fromCharCode(122,95,55,56,95,112,97,117,115,101,0);
         carouselT >>= Math.min(2, Math.abs(debug8.length << (Math.min(Math.abs(2), 2))));
      while (1 == debug8.length || spinnerc.length == 1) {
         spinnerc = `${debug8.length}`;
         break;
      }
          let banners = String.fromCharCode(108,97,115,114,95,56,95,56,55,0);
          let internetC: Array<any> = [String.fromCharCode(99,109,111,118,95,48,95,49,51,0), String.fromCharCode(115,116,97,107,101,95,103,95,56,55,0)];
         spinnerc += `${(spinnerc == String.fromCharCode(51,0) ? banners.length : spinnerc.length)}`;
         banners = `${internetC.length}`;
         internetC.push(1);
      while (!spinnerc.includes(debug8)) {
         spinnerc = `${carouselT >> (Math.min(debug8.length, 4))}`;
         break;
      }
      if (4 == (debug8.length ^ 5) && (carouselT ^ 5) == 5) {
          let darkY = String.fromCharCode(97,95,56,95,97,117,116,111,100,111,119,110,108,111,97,100,0);
          let pingq = 4.0;
          let description_6oQ = String.fromCharCode(107,95,55,57,95,109,101,101,116,117,112,0);
          let telegramQ: Map<any, any> = new Map([[String.fromCharCode(110,95,53,48,95,109,97,112,0),465], [String.fromCharCode(99,111,101,102,102,95,48,95,54,50,0),487]]);
         carouselT &= description_6oQ.length << (Math.min(1, Math.abs(telegramQ.size)));
         darkY += `${parseInt(`${pingq}`)}`;
         pingq += (String.fromCharCode(100,0) == darkY ? parseInt(`${pingq}`) : darkY.length);
         description_6oQ = `${(String.fromCharCode(66,0) == darkY ? darkY.length : parseInt(`${pingq}`))}`;
         telegramQ = new Map([[darkY, parseInt(`${pingq}`) - 1]]);
      }
         debug8 = `${3 * spinnerc.length}`;
          let themeQ: Map<any, any> = new Map([[String.fromCharCode(99,95,52,57,95,111,118,97,108,0),196], [String.fromCharCode(98,95,50,56,95,103,105,102,115,0),443], [String.fromCharCode(117,95,50,48,95,109,101,109,115,121,115,0),951]]);
          let roundi: Array<any> = [953, 99];
         spinnerc += `${2 << (Math.min(4, Math.abs(carouselT)))}`;
         themeQ = new Map([[`${themeQ.size}`, 2 & themeQ.size]]);
         roundi.push(themeQ.size / (Math.max(roundi.length, 7)));
       let robotop = String.fromCharCode(98,95,57,49,95,117,110,109,105,120,0);
       let edity = String.fromCharCode(98,95,56,48,95,109,117,116,97,116,105,110,103,0);
      for (let v = 0; v < 3; v++) {
          let hongkongF = false;
          let themec: Map<any, any> = new Map([[String.fromCharCode(116,105,110,116,95,108,95,53,48,0),139], [String.fromCharCode(102,114,97,109,101,110,117,109,95,122,95,55,49,0),351], [String.fromCharCode(114,95,49,52,95,100,111,119,110,108,111,97,100,97,98,108,101,0),502]]);
          let specz = String.fromCharCode(105,95,55,95,105,110,116,101,114,115,101,99,116,0);
          let disconnectedR = 2.0;
          let hejiC = 5;
         spinnerc += `${parseInt(`${disconnectedR}`) >> (Math.min(5, Math.abs(2)))}`;
         hongkongF = hongkongF && specz.length >= 86;
         themec = new Map([[`${themec.size}`, (themec.size << (Math.min(4, Math.abs((hongkongF ? 5 : 5)))))]]);
         specz += `${specz.length | 2}`;
         disconnectedR -= themec.size - 3;
         hejiC &= specz.length;
      }
      whatsappo = `${reminderK.size}`;
       let dataF = false;
       let volumeW = 4.0;
         dataF = volumeW < 44.6;
      if (!dataF) {
         volumeW -= parseInt(`${volumeW}`) ^ 2;
      }
      if (!dataF) {
          let emptym = String.fromCharCode(103,95,57,95,122,111,110,101,0);
          let readw = 5;
          let promotionq = 5;
         dataF = (readw % (Math.max(6, emptym.length))) >= 82;
         emptym = `${promotionq ^ promotionq}`;
         readw -= promotionq;
      }
      if (dataF) {
         dataF = !dataF;
      }
      while (4.68 >= (2.29 - volumeW)) {
         volumeW /= Math.max(1, 2 << (Math.min(Math.abs(parseInt(`${volumeW}`)), 2)));
         break;
      }
          let danger5 = String.fromCharCode(117,95,52,50,95,115,117,101,108,111,0);
         dataF = danger5.length < volumeW;
      activeT = `${parseInt(`${plashg}`) / (Math.max(whatsappo.length, 6))}`;
   if (activeT.length >= 4) {
      activeT = `${activeT.length}`;
   }
    let nonNum = 0

      plashg -= (String.fromCharCode(97,0) == foundw ? parseInt(`${mbsplashO}`) : foundw.length);
       let searchbarV = String.fromCharCode(109,110,99,95,49,95,50,49,0);
          let rewardvideo0 = 2.0;
         searchbarV += `${searchbarV.length}`;
         rewardvideo0 += parseInt(`${rewardvideo0}`);
      for (let m = 0; m < 1; m++) {
          let androidl = false;
          let volume6 = String.fromCharCode(101,95,51,56,95,112,114,105,110,116,102,0);
          let securityi = String.fromCharCode(107,95,56,57,95,115,97,103,97,0);
          let championJ = String.fromCharCode(98,105,116,118,101,99,116,111,114,95,55,95,52,53,0);
          let filledG = String.fromCharCode(115,95,49,53,95,108,105,98,97,118,102,111,114,109,97,116,0);
         searchbarV = `${(3 - (androidl ? 1 : 4))}`;
         androidl = String.fromCharCode(115,0) == filledG;
         volume6 += `${(String.fromCharCode(55,0) == championJ ? championJ.length : securityi.length)}`;
         securityi = `${securityi.length}`;
         filledG = "3";
      }
      while (!searchbarV.startsWith(`${searchbarV.length}`)) {
          let thumbnailA = 0;
          let largeQ = 2.0;
          let containeri = String.fromCharCode(120,95,55,49,95,114,101,115,105,103,110,115,0);
          let headerS = String.fromCharCode(107,95,54,49,95,107,97,116,0);
          let weibov = 1;
         searchbarV = `${1 / (Math.max(5, searchbarV.length))}`;
         thumbnailA <<= Math.min(headerS.length, 4);
         largeQ *= parseFloat(`${1}`);
         containeri += `${parseInt(`${largeQ}`) / (Math.max(headerS.length, 6))}`;
         weibov += headerS.length;
         break;
      }
      reminderK = new Map([[`${plashg}`, ((vietnamk ? 1 : 4) * parseInt(`${plashg}`))]]);
   while (parseInt(`${mbsplashO}`) >= activeT.length) {
      mbsplashO -= parseFloat(`${reminderK.size}`);
      break;
   }
    for (var i = 0; i < text.length; i++) {

   let forwardV = 9505773.0 >= mbsplashO;
   do {
      mbsplashO += parseFloat(`${activeT.length >> (Math.min(1, foundw.length))}`);
      if (forwardV) {
         break;
      }
   } while ((foundw.endsWith(`${mbsplashO}`)) && forwardV);
   let overu = plashg <= 7560009.0;
   do {
       let grayY: Array<any> = [492, 531, 969];
       let backgroundO: Map<any, any> = new Map([[String.fromCharCode(106,95,50,55,95,115,113,108,105,116,101,114,101,98,97,115,101,114,0),String.fromCharCode(111,95,52,52,95,98,105,110,0)], [String.fromCharCode(97,100,100,107,101,121,95,99,95,53,54,0),String.fromCharCode(112,95,55,48,95,116,111,112,109,111,115,116,0)]]);
       let rewardvideoY = false;
       let nalyticsN = false;
       let dycreator3 = String.fromCharCode(99,111,109,112,111,115,105,116,101,95,110,95,57,52,0);
          let fillG = 4.0;
         grayY.push(dycreator3.length % (Math.max(1, 5)));
         fillG += parseFloat(`${parseInt(`${fillG}`) - 1}`);
         dycreator3 = "2";
         nalyticsN = rewardvideoY || backgroundO.size == 75;
      let viewer3 = rewardvideoY ? !rewardvideoY : rewardvideoY;
      do {
         rewardvideoY = (75 == ((nalyticsN ? dycreator3.length : 75) & dycreator3.length));
         if (viewer3) {
            break;
         }
      } while (viewer3 && (dycreator3.length >= 3));
      let helpert = rewardvideoY ? !rewardvideoY : rewardvideoY;
      do {
          let launcherT = String.fromCharCode(101,110,100,101,100,95,57,95,52,55,0);
          let formV = 3.0;
          let constantsr: Map<any, any> = new Map([[String.fromCharCode(108,95,56,95,99,111,110,116,97,105,110,97,98,108,101,0),808], [String.fromCharCode(115,101,110,100,95,54,95,53,56,0),334]]);
          let currentQ: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,114,117,112,116,105,111,110,95,121,95,53,56,0),243], [String.fromCharCode(108,95,56,95,115,101,101,107,105,110,103,0),997], [String.fromCharCode(100,95,57,55,95,115,105,110,101,119,105,110,0),792]]);
          let configA = String.fromCharCode(120,95,49,55,95,101,110,99,108,97,118,101,0);
         rewardvideoY = 40 > grayY.length;
         launcherT = `${constantsr.size ^ 2}`;
         formV += parseFloat(`${currentQ.size}`);
         constantsr.set(`${formV}`, constantsr.size);
         currentQ.set(`${constantsr.size}`, constantsr.size);
         configA += `${(configA == String.fromCharCode(119,0) ? currentQ.size : configA.length)}`;
         if (helpert) {
            break;
         }
      } while ((rewardvideoY) && helpert);
       let sellG: Array<any> = [171, 852, 9];
       let footballV: Array<any> = [String.fromCharCode(116,95,54,53,95,118,112,111,105,110,116,0), String.fromCharCode(117,116,104,111,114,95,118,95,54,0)];
      let typingr = 9896104 <= backgroundO.size;
      do {
         backgroundO.set(`${footballV.length}`, backgroundO.size);
         if (typingr) {
            break;
         }
      } while ((2 <= (backgroundO.size * 4) || 4 <= (4 * backgroundO.size)) && typingr);
      if (1 > (backgroundO.size | 1)) {
         grayY.push(dycreator3.length);
      }
      for (let h = 0; h < 1; h++) {
          let yellow_: Array<any> = [159, 785];
          let commony = 0.0;
          let baiduG = String.fromCharCode(117,95,49,57,95,99,111,112,121,102,100,0);
          let favoriteY = 3;
         sellG.push(2);
         yellow_ = [baiduG.length];
         commony += (baiduG == String.fromCharCode(74,0) ? yellow_.length : baiduG.length);
         favoriteY |= 3;
      }
      if (rewardvideoY) {
         rewardvideoY = (backgroundO.size - footballV.length) > 32;
      }
      while (5 >= (1 & grayY.length)) {
         grayY.push(sellG.length ^ footballV.length);
         break;
      }
          let g_countB = 3.0;
          let foundT = String.fromCharCode(103,95,56,49,95,101,114,114,111,114,118,0);
         backgroundO.set(dycreator3, 3 | foundT.length);
         g_countB -= parseInt(`${g_countB}`) * parseInt(`${g_countB}`);
         foundT += `${parseInt(`${g_countB}`)}`;
         rewardvideoY = footballV.length < 80;
         sellG.push(dycreator3.length);
      let commentn = backgroundO.size <= 6478183;
      do {
         backgroundO = new Map([[`${sellG.length}`, ((nalyticsN ? 4 : 4) >> (Math.min(Math.abs(3), 5)))]]);
         if (commentn) {
            break;
         }
      } while ((5 < (footballV.length + backgroundO.size) && 3 < (5 + backgroundO.size)) && commentn);
      plashg *= grayY.length ^ backgroundO.size;
      if (overu) {
         break;
      }
   } while ((mbsplashO <= 1.41) && overu);
   let sentryO = mbsplashO <= 9290424.0;
   do {
       let short_hu = 1.0;
       let downloading1 = 4;
       let descf = String.fromCharCode(101,115,115,101,110,116,105,97,108,95,122,95,50,49,0);
       let colorsH = 0.0;
       let sharedU = 3.0;
         short_hu *= parseFloat(`${downloading1 | 2}`);
          let expands = String.fromCharCode(97,95,51,50,95,112,114,111,103,114,101,115,105,118,101,0);
         descf += `${1 << (Math.min(Math.abs(parseInt(`${short_hu}`)), 3))}`;
         expands += `${expands.length}`;
      for (let j = 0; j < 3; j++) {
         colorsH *= parseInt(`${sharedU}`);
      }
          let telegramT = 3;
         colorsH += telegramT;
         descf = `${parseInt(`${short_hu}`)}`;
      while (2.1 < (sharedU / (Math.max(4, colorsH)))) {
         colorsH -= parseInt(`${short_hu}`);
         break;
      }
      let routerH = sharedU >= 7565251.0;
      do {
         sharedU *= descf.length | parseInt(`${colorsH}`);
         if (routerH) {
            break;
         }
      } while (routerH && (1 >= (1 / (Math.max(2, downloading1)))));
          let refreshx: Array<any> = [94, 641, 104];
          let toponx = String.fromCharCode(122,95,50,57,95,105,113,109,102,0);
         downloading1 |= toponx.length / 1;
         refreshx = [refreshx.length * 2];
         toponx += `${2 + refreshx.length}`;
      while (downloading1 > colorsH) {
         colorsH += downloading1 * 1;
         break;
      }
      for (let x = 0; x < 2; x++) {
         short_hu /= Math.max(parseFloat(`${parseInt(`${sharedU}`) | 1}`), 5);
      }
         downloading1 /= Math.max(4, 2);
      while (downloading1 <= 3) {
         short_hu /= Math.max(5, parseFloat(`${downloading1}`));
         break;
      }
      for (let b = 0; b < 1; b++) {
         downloading1 |= descf.length;
      }
      while (2.23 > (sharedU / 5)) {
         colorsH += 3;
         break;
      }
          let anythink9: Map<any, any> = new Map([[String.fromCharCode(108,95,50,49,95,118,111,105,100,0),String.fromCharCode(112,101,114,115,105,115,116,101,100,95,101,95,54,49,0)], [String.fromCharCode(98,95,52,49,95,110,111,110,100,99,0),String.fromCharCode(108,117,114,97,108,105,122,97,116,105,111,110,95,104,95,51,56,0)]]);
          let emptyA = String.fromCharCode(98,95,50,56,95,104,118,99,99,0);
         sharedU += anythink9.size - 3;
         anythink9 = new Map([[emptyA, 3 & emptyA.length]]);
      mbsplashO += parseFloat(`${parseInt(`${colorsH}`)}`);
      if (sentryO) {
         break;
      }
   } while (sentryO && (vietnamk));
        const c = text.charAt(i);

   if (vietnamk || (reminderK.size % 1) == 4) {
      vietnamk = foundw.length == 57;
   }
      foundw += `${3 * parseInt(`${mbsplashO}`)}`;
       let cornerj = 5.0;
         cornerj /= Math.max(3, parseFloat(`${parseInt(`${cornerj}`)}`));
      let fillh = cornerj <= 8752929.0;
      do {
         cornerj += parseFloat(`${3 | parseInt(`${cornerj}`)}`);
         if (fillh) {
            break;
         }
      } while ((2.45 == cornerj) && fillh);
      if (3.43 > cornerj) {
         cornerj -= parseFloat(`${parseInt(`${cornerj}`)}`);
      }
      vietnamk = plashg < 25.50;
        if (c >= '0' && c <= '9') {

   if ((whatsappo.length - parseInt(`${mbsplashO}`)) > 3 && 4.47 > (mbsplashO - 2.44)) {
      mbsplashO /= Math.max(4, parseFloat(`${reminderK.size}`));
   }
      reminderK = new Map([[`${reminderK.size}`, 1 - reminderK.size]]);
      mbsplashO -= parseFloat(`${1 & whatsappo.length}`);
            size += fontSize
        } else {

      activeT += `${parseInt(`${plashg}`) - 3}`;
      mbsplashO += parseFloat(`${reminderK.size % (Math.max(3, 9))}`);
   for (let x = 0; x < 3; x++) {
      mbsplashO *= parseFloat(`${2 / (Math.max(3, activeT.length))}`);
   }
            nonNum += 1
        }
    }
    size *= ratio;

      reminderK.set(whatsappo, whatsappo.length);
   while (3 < (5 / (Math.max(8, whatsappo.length))) && (5 | whatsappo.length) < 3) {
      plashg += 1;
      break;
   }
   let selectedG = 8986876.0 >= plashg;
   do {
       let questo = 0.0;
       let catagoryH = 4.0;
       let klevin2 = String.fromCharCode(115,101,114,105,97,108,110,111,95,113,95,55,50,0);
      if ((1 + questo) > 5.21) {
         catagoryH -= (parseFloat(`${String.fromCharCode(49,0) == klevin2 ? parseInt(`${catagoryH}`) : klevin2.length}`));
      }
      let flyerk = catagoryH <= 9529920.0;
      do {
         catagoryH /= Math.max(parseFloat(`${2 - parseInt(`${questo}`)}`), 2);
         if (flyerk) {
            break;
         }
      } while ((5.50 == (questo - 2.99) && 4.41 == (2.99 + catagoryH)) && flyerk);
      while (questo < catagoryH) {
         questo -= 2 >> (Math.min(1, klevin2.length));
         break;
      }
         catagoryH *= parseFloat(`${parseInt(`${catagoryH}`)}`);
      if (4.64 < catagoryH) {
         catagoryH /= Math.max(2, parseFloat(`${parseInt(`${questo}`)}`));
      }
         klevin2 = `${(String.fromCharCode(71,0) == klevin2 ? klevin2.length : parseInt(`${catagoryH}`))}`;
         questo *= 3 << (Math.min(Math.abs(parseInt(`${questo}`)), 2));
      while (4.88 > (catagoryH * 1.65) && (parseInt(`${catagoryH}`) * 4) > 3) {
         klevin2 += `${klevin2.length}`;
         break;
      }
      while ((klevin2.length + 1) < 1) {
         catagoryH *= (parseFloat(`${String.fromCharCode(71,0) == klevin2 ? parseInt(`${catagoryH}`) : klevin2.length}`));
         break;
      }
      plashg *= 3;
      if (selectedG) {
         break;
      }
   } while (selectedG && ((plashg - 3.78) <= 1.24));
    size += (nonNum * (fontSize + 1));

       let floaterp = String.fromCharCode(116,95,55,50,95,114,98,115,112,0);
       let score5 = String.fromCharCode(120,95,50,57,95,98,108,111,99,107,104,97,115,104,0);
       let singapore6 = String.fromCharCode(116,97,99,107,95,119,95,49,55,0);
      let pointt = 6604995 >= singapore6.length;
      do {
          let uploadp = String.fromCharCode(119,95,51,51,95,115,110,110,105,100,0);
         singapore6 += `${singapore6.length + uploadp.length}`;
         if (pointt) {
            break;
         }
      } while (pointt && (singapore6.length < 5 && floaterp.length < 5));
      let viewerq = floaterp.length >= 7544353;
      do {
         floaterp = `${1 * floaterp.length}`;
         if (viewerq) {
            break;
         }
      } while (viewerq && (floaterp.endsWith(singapore6)));
      while (floaterp != String.fromCharCode(98,0) || 4 >= score5.length) {
         score5 = `${score5.length / (Math.max(1, 1))}`;
         break;
      }
      if (floaterp.endsWith(singapore6)) {
          let saves = 1.0;
         singapore6 = `${singapore6.length}`;
         saves += parseFloat(`${3 & parseInt(`${saves}`)}`);
      }
         floaterp += "1";
      let statsY = 9517786 <= singapore6.length;
      do {
          let screenl: Map<any, any> = new Map([[String.fromCharCode(105,110,118,101,114,116,95,104,95,53,55,0),301], [String.fromCharCode(117,95,55,51,95,99,111,108,111,114,102,117,108,0),308], [String.fromCharCode(118,95,55,49,95,116,101,115,115,101,108,108,97,116,101,0),232]]);
          let annerf = 4.0;
         singapore6 = `${floaterp.length}`;
         screenl.set(`${annerf}`, 2 ^ parseInt(`${annerf}`));
         if (statsY) {
            break;
         }
      } while (statsY && (score5 == String.fromCharCode(111,0)));
      while (5 > floaterp.length) {
         score5 = "3";
         break;
      }
         score5 = `${singapore6.length >> (Math.min(score5.length, 1))}`;
      while (score5 != String.fromCharCode(76,0)) {
         floaterp += `${singapore6.length}`;
         break;
      }
      foundw = "2";
      foundw += `${parseInt(`${mbsplashO}`) << (Math.min(1, Math.abs(2)))}`;
   for (let b = 0; b < 1; b++) {
       let miniu: Array<any> = [321, 736, 344];
       let change3 = false;
      let benefit4 = miniu.length <= 6095041;
      do {
         miniu.push(((change3 ? 1 : 5) >> (Math.min(Math.abs(1), 2))));
         if (benefit4) {
            break;
         }
      } while (benefit4 && (miniu.length >= 5 || 2 >= (5 + miniu.length)));
      for (let k = 0; k < 3; k++) {
          let loading5 = String.fromCharCode(106,117,115,116,105,102,105,99,97,116,105,111,110,95,57,95,57,51,0);
          let analytics = String.fromCharCode(98,95,49,56,95,100,97,116,97,98,97,115,101,0);
         change3 = analytics.startsWith(`${change3}`);
         loading5 += `${loading5.length}`;
         analytics = `${loading5.length / (Math.max(loading5.length, 1))}`;
      }
      if ((miniu.length % (Math.max(5, 5))) >= 4 && miniu.length >= 5) {
         miniu = [(miniu.length % (Math.max(4, (change3 ? 2 : 1))))];
      }
         change3 = (99 == ((!change3 ? miniu.length : 99) % (Math.max(miniu.length, 4))));
      for (let y = 0; y < 2; y++) {
          let star0 = 5;
          let founda = String.fromCharCode(103,95,49,55,95,100,117,109,112,101,114,0);
          let detail_ = 5;
         miniu = [2];
         star0 |= 1 - founda.length;
         founda = `${detail_ % (Math.max(7, founda.length))}`;
         detail_ >>= Math.min(Math.abs(star0), 5);
      }
          let guide3 = String.fromCharCode(100,105,115,97,112,112,101,97,114,101,100,95,106,95,50,56,0);
          let bridgeg = String.fromCharCode(117,100,116,97,95,55,95,53,55,0);
          let dialogg = String.fromCharCode(103,95,51,51,95,111,102,102,101,116,0);
         change3 = 29 <= guide3.length;
         guide3 = `${(dialogg == String.fromCharCode(83,0) ? bridgeg.length : dialogg.length)}`;
         bridgeg += `${dialogg.length ^ 3}`;
      vietnamk = !vietnamk;
   }
    return size;
}

export const getMinuteSecond = (milliseconds: number) => {
    const totalSeconds = Math.floor(milliseconds / 1000);
    const minutes = Math.floor(totalSeconds / 60);
    const seconds = totalSeconds % 60;

    const formattedTime = `${minutes}:${seconds < 10 ? '0' : ''}${seconds}`;
    return formattedTime;
}

export const warnIgnore = (ignore: (string | RegExp)[]) => {
    const warnRef = console.warn;

    console.warn = (e: string, ...params: any[]) => {
        for (let msg of ignore) {
            if (msg instanceof RegExp) {
                if (msg.test(e)) return;
            } else {
                if (e.includes(msg)) return;
            }
        }

        return warnRef(e, ...params);
    }
}

export const logIgnore = (ignore: (string | RegExp)[]) => {
    const logRef = console.log;

    console.log = (e: string, ...params: any[]) => {
        for (let msg of ignore) {
            if (msg instanceof RegExp) {
                if (msg.test(e)) return;
            } else if (typeof e === 'string') {
                if (e.includes(msg)) return;
            }
        }

        return logRef(e, ...params);
    }
}
