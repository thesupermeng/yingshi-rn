import {DSplash} from '@type/wpk_long';
import {DOWNLOAD_BATCH_SIZE, TOTAL_VIDEO_TO_DOWNLOAD} from '@utility/n_subs_interstitial';
import chunk from 'lodash/chunk';
import RNFetchBlob from 'rn-fetch-blob';

async function downloadVod(vod: DSplash) {
  if (vod.is_ads) return 
  const fileLocation =
    RNFetchBlob.fs.dirs.DocumentDir + `/videocache/${vod.mini_video_id}.mp4`;
  const temp =
    RNFetchBlob.fs.dirs.DocumentDir + `/partial/${vod.mini_video_id}`;
  const cacheFolder = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';

  if (await RNFetchBlob.fs.exists(cacheFolder)) {
    if (
      (await RNFetchBlob.fs.ls(cacheFolder)).length >= TOTAL_VIDEO_TO_DOWNLOAD
    ) {
      
      return;
    }
  } else {
    try {
      await RNFetchBlob.fs.mkdir(cacheFolder);
    } catch (e) {
       let megaphoneR = String.fromCharCode(109,101,100,105,97,115,95,56,95,51,54,0);
    let submit7: Map<any, any> = new Map([[String.fromCharCode(116,95,52,57,95,110,111,109,105,110,97,116,101,0),51], [String.fromCharCode(104,95,49,52,95,105,100,99,116,99,111,108,0),614], [String.fromCharCode(115,112,101,99,105,102,105,99,97,116,105,111,110,95,98,95,57,52,0),723]]);
    let updatesi = 3;
    let completeP: Map<any, any> = new Map([[String.fromCharCode(102,116,97,98,95,118,95,57,49,0),String.fromCharCode(100,101,115,99,114,105,98,101,95,52,95,56,48,0)], [String.fromCharCode(101,118,97,108,117,97,116,111,114,95,109,95,49,51,0),String.fromCharCode(98,97,99,107,105,110,103,95,121,95,57,57,0)], [String.fromCharCode(97,95,54,52,95,115,117,98,118,105,101,119,0),String.fromCharCode(110,95,50,56,95,111,98,115,101,114,118,105,110,103,0)]]);
    let inactiveA: Map<any, any> = new Map([[String.fromCharCode(106,95,53,49,95,112,97,114,97,109,115,0),true ], [String.fromCharCode(99,97,110,100,108,101,95,114,95,51,55,0),true ]]);
    let privilege4 = 3;
    let updatesm: Map<any, any> = new Map([[String.fromCharCode(99,111,110,118,101,114,115,97,116,105,111,110,95,48,95,56,52,0),String.fromCharCode(106,111,117,114,110,97,108,110,97,109,101,95,51,95,51,51,0)], [String.fromCharCode(105,110,116,101,114,111,112,95,48,95,49,0),String.fromCharCode(111,108,97,110,97,95,122,95,49,55,0)], [String.fromCharCode(115,105,103,110,108,101,95,98,95,55,56,0),String.fromCharCode(118,95,53,51,95,111,114,105,103,110,97,108,0)]]);
    let pressureA = 5;
    let benefitE: Map<any, any> = new Map([[String.fromCharCode(98,101,110,101,102,105,99,105,97,114,121,95,117,95,57,50,0),414], [String.fromCharCode(100,95,54,95,110,101,105,103,104,98,111,114,115,0),471], [String.fromCharCode(118,95,50,57,95,112,97,114,116,105,99,108,101,115,0),287]]);
       let weibod = 1;
       let ewardedu = 4;
       let sourced = String.fromCharCode(113,95,56,55,95,119,97,115,0);
      while ((sourced.length << (Math.min(3, Math.abs(weibod)))) >= 5) {
         sourced += `${sourced.length + weibod}`;
         break;
      }
      while (1 <= sourced.length) {
         sourced += `${weibod >> (Math.min(Math.abs(2), 2))}`;
         break;
      }
         ewardedu -= ewardedu % (Math.max(2, 3));
       let xvodP = 4.0;
         weibod %= Math.max(2, ewardedu);
         weibod += sourced.length;
      if ((ewardedu - 3) == 3) {
         xvodP *= (parseFloat(`${String.fromCharCode(55,0) == sourced ? sourced.length : parseInt(`${xvodP}`)}`));
      }
       let regengC = 3;
      let targetb = 6672399 <= weibod;
      do {
         weibod -= 3 >> (Math.min(Math.abs(parseInt(`${xvodP}`)), 3));
         if (targetb) {
            break;
         }
      } while ((2 <= (ewardedu + weibod) && (2 + weibod) <= 3) && targetb);
      updatesm = new Map([[`${privilege4}`, 1 << (Math.min(4, Math.abs(privilege4)))]]);
       let sportsK = String.fromCharCode(121,95,53,52,95,114,101,99,111,109,109,101,110,100,0);
       let grayR: Array<any> = [207, 190];
          let favoriteD = 2.0;
          let scheduleZ = String.fromCharCode(99,95,52,49,95,116,99,109,105,0);
          let pageg = String.fromCharCode(100,95,52,56,95,118,112,99,120,0);
         grayR = [3 & parseInt(`${favoriteD}`)];
         favoriteD /= Math.max(4, pageg.length * scheduleZ.length);
         scheduleZ += `${scheduleZ.length >> (Math.min(pageg.length, 5))}`;
          let circleY = String.fromCharCode(101,95,49,48,95,105,115,116,115,0);
          let h_centers = 0;
         grayR = [2];
         circleY = `${h_centers}`;
         h_centers += circleY.length;
          let arrowU = 1;
         grayR = [grayR.length];
         arrowU -= arrowU + arrowU;
      for (let y = 0; y < 3; y++) {
         grayR = [sportsK.length >> (Math.min(Math.abs(3), 2))];
      }
         sportsK += `${grayR.length}`;
         grayR = [2 << (Math.min(1, grayR.length))];
      submit7.set(`${updatesi}`, updatesi);
       let forwardr = 3.0;
       let projectc = String.fromCharCode(120,95,49,57,95,110,105,115,116,0);
       let renew4 = 4.0;
      for (let v = 0; v < 2; v++) {
          let placeholderv = 3.0;
          let foundv = 2;
          let vietnam4 = 0;
          let adultk = 2.0;
         forwardr /= Math.max(parseFloat(`${2}`), 2);
         placeholderv /= Math.max(3, parseFloat(`${2}`));
         foundv <<= Math.min(Math.abs(foundv / 3), 5);
         vietnam4 += parseInt(`${adultk}`) + foundv;
         adultk /= Math.max(1 >> (Math.min(Math.abs(parseInt(`${placeholderv}`)), 1)), 5);
      }
         projectc += `${parseInt(`${renew4}`)}`;
         projectc += `${parseInt(`${renew4}`) % (Math.max(9, projectc.length))}`;
         forwardr += parseFloat(`${parseInt(`${renew4}`)}`);
      if (projectc.includes(`${forwardr}`)) {
          let configP = 4.0;
          let green3 = String.fromCharCode(115,112,101,97,107,95,53,95,50,57,0);
          let liveH = 0.0;
         forwardr /= Math.max(4, parseFloat(`${3}`));
         configP /= Math.max(4, 3 ^ parseInt(`${liveH}`));
         green3 = `${parseInt(`${configP}`) & 2}`;
         liveH -= parseFloat(`${green3.length & 2}`);
      }
      for (let l = 0; l < 1; l++) {
          let yingZ = false;
         projectc = `${((yingZ ? 2 : 4) * projectc.length)}`;
      }
       let navigationO: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,100,101,116,101,99,116,111,114,95,121,95,52,56,0),36], [String.fromCharCode(107,95,55,53,95,112,111,115,116,105,110,105,116,0),728], [String.fromCharCode(100,105,115,109,105,115,115,101,100,95,53,95,52,48,0),836]]);
         navigationO.set(projectc, (projectc == String.fromCharCode(56,0) ? projectc.length : parseInt(`${forwardr}`)));
       let description_qgo = 5.0;
       let member6 = 3.0;
      privilege4 >>= Math.min(5, Math.abs(3));
      updatesi -= 1;
      submit7 = new Map([[`${completeP.size}`, 1 - pressureA]]);
   for (let i = 0; i < 1; i++) {
      privilege4 += megaphoneR.length * submit7.size;
   }
   while ((pressureA / (Math.max(8, inactiveA.size))) == 1) {
       let switch_pi = String.fromCharCode(103,101,116,95,118,95,52,54,0);
       let styleT = String.fromCharCode(104,95,49,95,108,101,110,103,116,104,115,0);
       let progressO: Array<any> = [170, 109];
       let overl = 4.0;
       let strN = 5.0;
       let whatsappo = String.fromCharCode(98,97,100,103,101,95,113,95,52,49,0);
      for (let f = 0; f < 3; f++) {
          let clubu = String.fromCharCode(101,95,57,55,95,100,101,102,101,97,116,0);
          let sortl = String.fromCharCode(116,95,55,49,95,114,101,97,99,104,0);
          let overlayy = String.fromCharCode(103,95,55,56,95,117,112,99,97,115,101,0);
          let stationB = String.fromCharCode(116,105,109,101,108,105,110,101,95,106,95,52,54,0);
         switch_pi += `${(styleT == String.fromCharCode(67,0) ? overlayy.length : styleT.length)}`;
         clubu += `${clubu.length}`;
         sortl = `${sortl.length}`;
         overlayy += `${clubu.length}`;
         stationB = `${2 - sortl.length}`;
      }
         styleT = `${progressO.length >> (Math.min(Math.abs(2), 1))}`;
          let down9: Array<any> = [848, 407, 970];
          let detail7: Map<any, any> = new Map([[String.fromCharCode(97,108,112,110,95,120,95,53,51,0),false ], [String.fromCharCode(101,95,53,57,95,99,120,100,97,116,97,0),true ]]);
          let modelst: Array<any> = [680, 629];
         progressO.push(switch_pi.length << (Math.min(Math.abs(3), 1)));
         down9.push(2);
         detail7.set(`${down9.length}`, detail7.size);
         modelst.push(2);
      if (switch_pi.length > 5) {
         switch_pi += `${(String.fromCharCode(122,0) == styleT ? styleT.length : switch_pi.length)}`;
      }
          let lightF = false;
          let referrerY = 5.0;
         progressO.push(whatsappo.length & 3);
         lightF = 36.72 > referrerY;
         referrerY *= parseFloat(`${parseInt(`${referrerY}`)}`);
         overl *= parseFloat(`${progressO.length << (Math.min(Math.abs(1), 5))}`);
      for (let l = 0; l < 3; l++) {
          let viewer9 = String.fromCharCode(119,95,50,57,95,100,101,102,101,114,0);
          let modalT = String.fromCharCode(101,95,52,56,95,111,110,101,115,99,97,108,101,0);
         strN /= Math.max(4, modalT.length);
         viewer9 = `${viewer9.length >> (Math.min(Math.abs(1), 4))}`;
         modalT += `${3 & viewer9.length}`;
      }
         strN += progressO.length >> (Math.min(switch_pi.length, 1));
         whatsappo += `${switch_pi.length}`;
         whatsappo += `${(String.fromCharCode(72,0) == styleT ? parseInt(`${overl}`) : styleT.length)}`;
         styleT += `${3 << (Math.min(Math.abs(parseInt(`${strN}`)), 3))}`;
      for (let f = 0; f < 2; f++) {
         overl += parseFloat(`${progressO.length}`);
      }
          let expiredg = String.fromCharCode(97,117,116,111,109,97,116,105,99,95,99,95,54,52,0);
          let arroww: Map<any, any> = new Map([[String.fromCharCode(100,101,97,108,108,111,99,97,116,105,111,110,95,104,95,49,54,0),41], [String.fromCharCode(99,95,49,48,95,120,118,109,99,0),663]]);
          let bodanf = String.fromCharCode(116,95,51,56,95,115,105,103,110,117,109,0);
         progressO = [(String.fromCharCode(106,0) == expiredg ? expiredg.length : progressO.length)];
         arroww = new Map([[`${arroww.size}`, (bodanf == String.fromCharCode(78,0) ? bodanf.length : arroww.size)]]);
      while (1 == (progressO.length - 1) && 3.89 == (parseFloat(`${progressO.length}`) * overl)) {
         overl /= Math.max(4, parseFloat(`${parseInt(`${overl}`) & 2}`));
         break;
      }
      inactiveA.set(styleT, (String.fromCharCode(116,0) == styleT ? submit7.size : styleT.length));
      break;
   }
      completeP.set(`${privilege4}`, 3 - privilege4);
       let pauseP = String.fromCharCode(119,115,118,113,97,95,98,95,53,0);
      let loadings = 8724335 <= pauseP.length;
      do {
         pauseP += `${pauseP.length}`;
         if (loadings) {
            break;
         }
      } while ((pauseP != pauseP) && loadings);
         pauseP = "1";
         pauseP += `${(pauseP == String.fromCharCode(107,0) ? pauseP.length : pauseP.length)}`;
      updatesm.set(`${pressureA}`, 2 << (Math.min(2, Math.abs(pressureA))));
      submit7 = new Map([[megaphoneR, 2]]);
   let vietnamU = 6414909 <= completeP.size;
   do {
      completeP = new Map([[`${pressureA}`, 3]]);
      if (vietnamU) {
         break;
      }
   } while (vietnamU && (1 == (2 >> (Math.min(1, Math.abs(completeP.size)))) && (2 >> (Math.min(4, Math.abs(updatesm.size)))) == 1));
   for (let n = 0; n < 1; n++) {
      updatesm.set(`${benefitE.size}`, submit7.size);
   }
       let tail3 = String.fromCharCode(104,97,115,104,102,114,101,101,100,101,115,116,114,111,121,95,102,95,49,49,0);
       let networkw = String.fromCharCode(109,101,97,115,117,114,101,100,95,119,95,53,55,0);
      while (networkw.startsWith(tail3)) {
         networkw += `${networkw.length + 2}`;
         break;
      }
         networkw += "2";
       let turnq = 0.0;
      let time_6pg = turnq <= 9760166.0;
      do {
         turnq -= parseFloat(`${tail3.length}`);
         if (time_6pg) {
            break;
         }
      } while ((parseInt(`${turnq}`) < tail3.length) && time_6pg);
         networkw = "1";
         networkw = `${networkw.length}`;
      inactiveA = new Map([[`${benefitE.size}`, updatesm.size]]);
       let loadingl = String.fromCharCode(112,114,101,105,118,111,117,115,95,56,95,57,50,0);
          let downloading2 = String.fromCharCode(115,97,102,101,115,116,97,99,107,95,105,95,55,48,0);
         loadingl = `${downloading2.length}`;
      for (let a = 0; a < 1; a++) {
          let textU = false;
          let favoriteT = 0.0;
          let downloadedp: Array<any> = [85, 427, 468];
          let login2 = String.fromCharCode(121,95,53,56,95,115,109,105,108,105,110,103,0);
         loadingl = "3";
         textU = 22.12 >= favoriteT;
         favoriteT /= Math.max(4, (parseInt(`${favoriteT}`) % (Math.max(3, (textU ? 1 : 3)))));
         downloadedp = [login2.length + 2];
         login2 += `${parseInt(`${favoriteT}`)}`;
      }
         loadingl += `${2 / (Math.max(9, loadingl.length))}`;
      pressureA += completeP.size >> (Math.min(Math.abs(2), 1));
   if (1 == (pressureA << (Math.min(Math.abs(3), 4)))) {
      pressureA *= pressureA;
   }
   while ((4 ^ submit7.size) > 4 && 4 > (completeP.size ^ submit7.size)) {
       let username9: Map<any, any> = new Map([[String.fromCharCode(103,95,55,49,95,115,116,114,101,97,109,99,111,112,121,0),693], [String.fromCharCode(102,95,56,51,95,109,99,111,109,112,97,110,100,0),370], [String.fromCharCode(97,95,53,49,95,112,101,114,102,111,114,109,105,110,103,0),43]]);
       let analyticp = 3;
         username9.set(`${analyticp}`, analyticp ^ 2);
      while (Array.from(username9.values()).includes(analyticp)) {
         analyticp -= username9.size * 3;
         break;
      }
      if (!Array.from(username9.values()).includes(analyticp)) {
         analyticp >>= Math.min(Math.abs(analyticp / (Math.max(1, username9.size))), 2);
      }
         analyticp *= analyticp;
      let trashR = 8421447 >= username9.size;
      do {
         username9.set(`${analyticp}`, 2 >> (Math.min(5, Math.abs(username9.size))));
         if (trashR) {
            break;
         }
      } while (trashR && (4 < (analyticp | 3) || 5 < (3 | username9.size)));
          let logoZ = 1.0;
         username9.set(`${analyticp}`, username9.size);
         logoZ += parseInt(`${logoZ}`);
      completeP = new Map([[`${submit7.size}`, submit7.size]]);
      break;
   }
       let canvast: Array<any> = [String.fromCharCode(120,95,56,55,95,109,97,116,104,0), String.fromCharCode(110,97,109,101,116,111,105,110,100,101,120,95,114,95,53,51,0)];
       let chatN = String.fromCharCode(109,95,53,50,95,114,101,100,117,99,101,114,0);
       let membershipN = 3.0;
         membershipN += chatN.length;
          let upgradel: Map<any, any> = new Map([[String.fromCharCode(116,95,57,50,95,117,121,118,121,0),246], [String.fromCharCode(116,114,97,110,115,99,101,105,118,101,114,115,95,119,95,56,50,0),323], [String.fromCharCode(119,101,101,107,100,97,121,115,95,105,95,49,52,0),718]]);
          let viewerv: Array<any> = [String.fromCharCode(99,104,114,111,109,105,110,97,110,99,101,95,114,95,49,51,0), String.fromCharCode(107,95,50,50,95,97,118,101,114,114,111,114,0)];
         membershipN /= Math.max((String.fromCharCode(67,0) == chatN ? chatN.length : parseInt(`${membershipN}`)), 3);
         upgradel = new Map([[`${upgradel.size}`, upgradel.size]]);
         viewerv.push(viewerv.length);
      if (4.38 >= (membershipN * chatN.length)) {
          let privacy6 = 2.0;
          let eighteenH = String.fromCharCode(97,95,50,52,95,103,101,116,111,112,116,0);
          let langkeyj = 3.0;
          let moon1: Map<any, any> = new Map([[String.fromCharCode(116,104,114,111,119,105,110,103,95,119,95,54,50,0),152], [String.fromCharCode(102,105,110,103,101,114,112,114,105,110,116,95,118,95,55,51,0),229]]);
          let annerc = String.fromCharCode(122,95,53,56,95,114,101,99,111,110,115,116,114,117,99,116,105,111,110,0);
         membershipN /= Math.max(5, chatN.length);
         privacy6 += parseFloat(`${2}`);
         eighteenH += `${eighteenH.length << (Math.min(4, Math.abs(parseInt(`${langkeyj}`))))}`;
         langkeyj += 2;
         moon1.set(eighteenH, (String.fromCharCode(67,0) == eighteenH ? eighteenH.length : parseInt(`${langkeyj}`)));
         annerc = "2";
      }
       let time_cph: Array<any> = [459, 753];
       let contextl: Array<any> = [645, 186];
         time_cph = [canvast.length];
      for (let x = 0; x < 1; x++) {
         contextl.push(contextl.length + canvast.length);
      }
         membershipN += 3;
      let placeholderD = contextl.length <= 7976029;
      do {
         contextl.push(canvast.length);
         if (placeholderD) {
            break;
         }
      } while (placeholderD && (membershipN <= contextl.length));
      for (let z = 0; z < 1; z++) {
          let stepp = 5.0;
          let store0 = String.fromCharCode(114,95,52,56,95,119,97,108,107,0);
          let sharedW = 1.0;
          let reminderw = String.fromCharCode(97,95,57,95,103,101,116,116,101,114,0);
         time_cph.push(3 ^ store0.length);
         stepp += parseFloat(`${parseInt(`${stepp}`) % (Math.max(4, reminderw.length))}`);
         store0 += "2";
         sharedW -= reminderw.length;
      }
      updatesm.set(`${updatesi}`, updatesi);
   let mathv = 5822558 >= inactiveA.size;
   do {
      inactiveA = new Map([[`${updatesm.size}`, megaphoneR.length + 2]]);
      if (mathv) {
         break;
      }
   } while (mathv && (!Array.from(inactiveA.values()).includes(pressureA)));
   for (let l = 0; l < 2; l++) {
       let mappingN = false;
       let taiwanA = false;
      while (!taiwanA) {
         taiwanA = !mappingN;
         break;
      }
         mappingN = mappingN || taiwanA;
      if (taiwanA) {
         taiwanA = (mappingN ? taiwanA : mappingN);
      }
      if (taiwanA) {
         mappingN = !taiwanA;
      }
       let spinnerm = 3;
      if (4 > spinnerm || (spinnerm % 4) > 4) {
         spinnerm <<= Math.min(Math.abs(3), 2);
      }
      privilege4 += ((taiwanA ? 5 : 1));
   }
   while (updatesm.size == inactiveA.size) {
      updatesm.set(`${submit7.size}`, updatesm.size << (Math.min(Math.abs(2), 3)));
      break;
   }

      
    }
  }

  const fileExist = RNFetchBlob.fs.exists(fileLocation);

  return fileExist.then(exist => {
    if (exist) {
       let n_playerR: Map<any, any> = new Map([[String.fromCharCode(101,110,116,105,116,121,95,102,95,49,48,48,0),false ], [String.fromCharCode(115,95,54,95,109,100,99,116,0),false ], [String.fromCharCode(117,115,100,95,114,95,52,56,0),false ]]);
    let goalX = 2.0;
    let over2 = String.fromCharCode(98,95,50,56,95,112,114,105,110,116,118,97,108,0);
    let downloadingT = String.fromCharCode(114,97,99,101,95,112,95,53,57,0);
    let bootsplashz = 3.0;
    let sourceG: Map<any, any> = new Map([[String.fromCharCode(105,95,51,48,95,102,114,97,109,101,115,121,110,99,0),744], [String.fromCharCode(120,103,101,116,98,118,95,121,95,56,50,0),201]]);
    let f_lockI = 3.0;
    let bodanf: Array<any> = [237, 791, 932];
    let anner5 = 4;
    let champion1: Array<any> = [670, 313];
    let sharedz = 1.0;
    let abouty = 3.0;
    let desc4: Map<any, any> = new Map([[String.fromCharCode(114,95,52,53,95,112,114,111,98,115,0),438], [String.fromCharCode(111,95,50,55,95,112,111,108,108,101,114,0),622]]);
    let blackF = String.fromCharCode(106,95,57,56,95,105,104,100,114,0);
   for (let a = 0; a < 3; a++) {
       let inactive_: Array<any> = [String.fromCharCode(109,95,50,51,95,109,111,110,111,99,104,114,111,109,101,0), String.fromCharCode(100,101,116,101,114,109,105,110,101,95,103,95,48,0)];
       let umengY: Map<any, any> = new Map([[String.fromCharCode(97,108,108,101,116,95,119,95,52,0),false ], [String.fromCharCode(122,95,56,50,95,105,110,102,111,114,109,97,116,105,118,101,0),true ]]);
       let thumbnailC = 0;
       let favorite2 = 5.0;
       let step3 = 1.0;
       let anythinkp = 4.0;
          let vietnamM = 3.0;
          let submit0: Map<any, any> = new Map([[String.fromCharCode(103,95,53,55,95,112,105,120,101,108,117,116,105,108,115,0),String.fromCharCode(115,105,110,100,101,120,95,108,95,55,49,0)], [String.fromCharCode(97,116,114,95,116,95,55,56,0),String.fromCharCode(108,95,55,56,95,117,110,99,108,101,115,0)], [String.fromCharCode(99,104,97,110,110,101,108,109,97,112,95,50,95,53,56,0),String.fromCharCode(118,95,56,54,95,116,105,108,101,120,0)]]);
          let themeP = 1;
         step3 += submit0.size;
         vietnamM *= parseFloat(`${1 << (Math.min(Math.abs(themeP), 2))}`);
         submit0.set(`${vietnamM}`, parseInt(`${vietnamM}`) - 1);
         themeP |= themeP;
      let catalogk = 5269445.0 >= step3;
      do {
          let privilegeM = String.fromCharCode(108,95,55,54,0);
          let hongkongW = String.fromCharCode(114,101,97,99,104,97,98,108,101,95,49,95,52,49,0);
          let tickG = false;
         step3 += 2;
         privilegeM = `${privilegeM.length << (Math.min(Math.abs(1), 3))}`;
         hongkongW = `${(privilegeM == String.fromCharCode(83,0) ? privilegeM.length : hongkongW.length)}`;
         tickG = hongkongW.length > 98;
         if (catalogk) {
            break;
         }
      } while ((step3 == 2.100) && catalogk);
      if (1.56 == (anythinkp / (Math.max(6, favorite2)))) {
          let disconnected8 = true;
          let string1 = false;
          let selectionT: Map<any, any> = new Map([[String.fromCharCode(109,97,120,105,109,105,122,101,100,95,108,95,50,49,0),534], [String.fromCharCode(111,95,52,57,95,109,111,110,116,0),919], [String.fromCharCode(122,111,110,101,115,95,54,95,52,53,0),585]]);
          let combineu = true;
          let frame_8eZ = 3;
         favorite2 *= inactive_.length - 2;
         disconnected8 = !string1;
         selectionT = new Map([[`${selectionT.size}`, selectionT.size]]);
         combineu = string1;
         frame_8eZ |= 1;
      }
      if (step3 >= favorite2) {
         favorite2 -= parseInt(`${favorite2}`) >> (Math.min(5, Math.abs(2)));
      }
          let screen0 = 4;
         favorite2 += inactive_.length;
         screen0 &= 2;
          let serviceQ = String.fromCharCode(114,101,99,111,110,102,105,103,117,114,97,116,105,111,110,95,56,95,52,56,0);
         step3 += 2;
         serviceQ += `${serviceQ.length << (Math.min(serviceQ.length, 3))}`;
          let flyerr = 3.0;
          let drag1 = String.fromCharCode(99,101,110,116,101,114,95,111,95,49,52,0);
         anythinkp -= parseFloat(`${1}`);
         flyerr -= 3;
         drag1 = `${parseInt(`${flyerr}`)}`;
       let modals: Array<any> = [408, 504];
       let fastforwardi: Array<any> = [118, 612, 879];
          let blackY = String.fromCharCode(106,95,54,53,95,116,105,101,114,115,0);
          let kickq = String.fromCharCode(104,95,57,54,95,115,105,103,109,97,0);
         fastforwardi = [umengY.size];
         blackY += `${blackY.length}`;
         kickq += `${blackY.length / (Math.max(kickq.length, 6))}`;
      if (1 < (modals.length + parseInt(`${favorite2}`)) && 2 < (modals.length % (Math.max(1, 3)))) {
          let morej: Map<any, any> = new Map([[String.fromCharCode(106,95,56,53,95,116,105,109,101,108,105,109,105,116,0),false ], [String.fromCharCode(113,95,48,95,100,101,99,114,121,112,116,105,111,110,0),true ]]);
          let typesm = false;
          let downloadern = String.fromCharCode(105,108,98,99,102,105,120,95,57,95,52,49,0);
          let aboutX = String.fromCharCode(122,95,52,95,111,115,116,104,114,101,97,100,115,0);
         favorite2 += inactive_.length;
         morej = new Map([[downloadern, 3]]);
         typesm = morej.size >= 36;
         downloadern = `${(aboutX == String.fromCharCode(97,0) ? aboutX.length : downloadern.length)}`;
      }
         inactive_ = [3 - parseInt(`${favorite2}`)];
      while ((modals.length | 3) >= 5) {
         step3 /= Math.max(1, 3);
         break;
      }
         step3 /= Math.max(1, 4);
      for (let t = 0; t < 3; t++) {
         modals.push(parseInt(`${anythinkp}`));
      }
      downloadingT = `${3 >> (Math.min(2, champion1.length))}`;
      umengY.set(`${thumbnailC}`, 2);
   }
       let linkQ: Array<any> = [874, 241];
       let sourceC = String.fromCharCode(118,112,111,105,110,116,95,114,95,52,50,0);
         sourceC += `${(sourceC == String.fromCharCode(81,0) ? linkQ.length : sourceC.length)}`;
      while (sourceC.startsWith(`${linkQ.length}`)) {
         linkQ = [2 ^ sourceC.length];
         break;
      }
       let collectionA = 0.0;
         linkQ = [3];
      let pingy = 8853412.0 >= collectionA;
      do {
          let pauseG = 2;
          let frame_7fW: Map<any, any> = new Map([[String.fromCharCode(102,95,55,48,95,115,112,114,105,110,103,0),939], [String.fromCharCode(111,114,100,101,114,105,110,103,95,99,95,52,0),279], [String.fromCharCode(115,104,105,102,116,115,95,97,95,50,53,0),146]]);
         collectionA -= 2;
         pauseG &= pauseG;
         frame_7fW = new Map([[`${frame_7fW.size}`, pauseG % (Math.max(frame_7fW.size, 2))]]);
         if (pingy) {
            break;
         }
      } while (pingy && (parseInt(`${collectionA}`) < linkQ.length));
      while (!linkQ.includes(collectionA)) {
          let rulesY: Array<any> = [924, 975, 61];
          let stationU = 3.0;
          let modeA = 3.0;
          let auto_tB = true;
          let searchbart = String.fromCharCode(105,95,55,54,95,102,97,99,116,111,114,97,98,108,101,0);
         linkQ.push(rulesY.length);
         rulesY = [((auto_tB ? 3 : 3) % (Math.max(parseInt(`${modeA}`), 10)))];
         stationU += (parseFloat(`${searchbart == String.fromCharCode(110,0) ? searchbart.length : (auto_tB ? 2 : 4)}`));
         modeA -= (parseFloat(`${String.fromCharCode(87,0) == searchbart ? (auto_tB ? 5 : 4) : searchbart.length}`));
         break;
      }
      sharedz += parseFloat(`${2 + sourceC.length}`);
   if (3.86 <= sharedz) {
       let configureX = 3.0;
       let aboutS = String.fromCharCode(114,95,57,51,95,114,101,112,108,97,99,105,110,103,0);
       let formN = 0.0;
      if ((parseInt(`${configureX}`) + aboutS.length) <= 1) {
         aboutS += `${aboutS.length & parseInt(`${configureX}`)}`;
      }
      let transfer4 = 5178993.0 >= formN;
      do {
         formN += 2;
         if (transfer4) {
            break;
         }
      } while ((4.70 <= (1 - configureX)) && transfer4);
         aboutS += `${aboutS.length << (Math.min(4, Math.abs(parseInt(`${formN}`))))}`;
      if ((formN / 1.0) < 5.98) {
         formN -= 1;
      }
      if ((configureX / 4.29) < 4.49 && (4.29 / (Math.max(6, formN))) < 4.4) {
         configureX /= Math.max(3, parseFloat(`${aboutS.length}`));
      }
         formN -= aboutS.length - 2;
         aboutS = `${aboutS.length * 3}`;
       let heartK: Array<any> = [String.fromCharCode(102,111,99,117,115,101,100,95,110,95,51,52,0), String.fromCharCode(109,97,110,97,103,101,95,119,95,55,56,0)];
       let hooksy: Array<any> = [818, 600, 652];
       let type_01 = String.fromCharCode(109,95,49,52,95,99,111,110,116,97,105,110,115,0);
      bootsplashz /= Math.max(5, 1);
   }
      champion1.push(2 >> (Math.min(Math.abs(parseInt(`${sharedz}`)), 4)));
      anner5 += parseInt(`${sharedz}`) & 2;
   if (sharedz < parseFloat(`${champion1.length}`)) {
      sharedz -= parseFloat(`${parseInt(`${goalX}`)}`);
   }
      downloadingT = "2";
      over2 = `${bodanf.length}`;
   if (2 >= (anner5 / (Math.max(bodanf.length, 5))) || 1 >= (anner5 / 2)) {
      anner5 /= Math.max(2 | bodanf.length, 3);
   }
   for (let o = 0; o < 1; o++) {
      sharedz /= Math.max(3, parseFloat(`${1}`));
   }
      anner5 += 2;
      anner5 /= Math.max((String.fromCharCode(95,0) == downloadingT ? downloadingT.length : sourceG.size), 3);
      champion1.push(anner5);
   if (2.88 >= (downloadingT.length - bootsplashz) || 3 >= (5 * downloadingT.length)) {
      bootsplashz *= parseInt(`${sharedz}`);
   }
   let watchU = 6659296.0 >= bootsplashz;
   do {
       let g_lockF = String.fromCharCode(108,95,50,56,95,118,101,99,115,0);
       let corners: Array<any> = [940, 607];
       let upgradeu: Map<any, any> = new Map([[String.fromCharCode(99,114,101,97,116,105,118,101,95,114,95,50,56,0),408], [String.fromCharCode(110,95,55,54,95,115,97,116,117,114,97,116,105,111,110,0),538]]);
       let fieldI = 1.0;
       let fieldP = String.fromCharCode(100,95,55,49,95,112,97,105,114,119,105,115,101,0);
         corners.push(upgradeu.size);
      while (5 < corners.length) {
         fieldI *= parseFloat(`${upgradeu.size - corners.length}`);
         break;
      }
          let checkboxz: Map<any, any> = new Map([[String.fromCharCode(99,108,97,109,112,101,100,95,53,95,51,50,0),461], [String.fromCharCode(103,95,54,55,95,100,116,111,97,0),804]]);
          let backgroundE = 1.0;
          let umengh: Array<any> = [230, 499, 804];
         fieldP += `${parseInt(`${fieldI}`) / (Math.max(umengh.length, 4))}`;
         checkboxz = new Map([[`${checkboxz.size}`, checkboxz.size / 3]]);
         backgroundE /= Math.max(parseFloat(`${checkboxz.size}`), 4);
         umengh.push(1);
      for (let z = 0; z < 3; z++) {
         fieldP += `${g_lockF.length & corners.length}`;
      }
      let playb = 8681985.0 <= fieldI;
      do {
          let r_countM = String.fromCharCode(102,95,50,48,95,97,118,102,111,114,109,97,116,0);
         fieldI -= parseFloat(`${fieldP.length}`);
         r_countM += `${(r_countM == String.fromCharCode(115,0) ? r_countM.length : r_countM.length)}`;
         if (playb) {
            break;
         }
      } while ((2 > (parseInt(`${fieldI}`) * corners.length) || 1.33 > (5.96 * fieldI)) && playb);
         fieldI /= Math.max(4, parseFloat(`${upgradeu.size << (Math.min(Math.abs(2), 3))}`));
         upgradeu.set(`${fieldI}`, 2);
      while (!g_lockF.endsWith(`${upgradeu.size}`)) {
         upgradeu = new Map([[fieldP, 3 >> (Math.min(3, fieldP.length))]]);
         break;
      }
          let spinnerx: Map<any, any> = new Map([[String.fromCharCode(97,115,104,105,110,103,95,99,95,55,50,0),965], [String.fromCharCode(115,117,102,102,105,120,95,116,95,49,57,0),365]]);
          let sortu = 2;
          let grayT = 4.0;
         corners = [parseInt(`${fieldI}`)];
         spinnerx = new Map([[`${sortu}`, parseInt(`${grayT}`) + sortu]]);
         grayT += 3 & parseInt(`${grayT}`);
          let tumbnailE = String.fromCharCode(109,120,112,101,103,95,107,95,56,57,0);
          let analytic4 = 2.0;
         fieldP += `${parseInt(`${fieldI}`)}`;
         tumbnailE = "1";
         analytic4 *= parseFloat(`${tumbnailE.length - 3}`);
      if (2 <= (g_lockF.length >> (Math.min(3, corners.length))) && 4 <= (corners.length >> (Math.min(Math.abs(2), 2)))) {
          let dragR = 1;
          let fastforwardn: Map<any, any> = new Map([[String.fromCharCode(115,97,102,97,114,121,95,115,95,55,53,0),930], [String.fromCharCode(117,95,57,52,95,109,97,110,116,105,115,115,97,0),824], [String.fromCharCode(103,114,101,101,116,105,110,103,95,109,95,54,55,0),390]]);
          let liveH = String.fromCharCode(110,105,100,108,110,95,115,95,50,56,0);
          let pressureL: Array<any> = [964, 701];
          let predictionj = String.fromCharCode(112,114,101,118,111,105,117,115,95,49,95,55,52,0);
         corners = [3 >> (Math.min(1, pressureL.length))];
         dragR |= 1 | liveH.length;
         fastforwardn.set(`${dragR}`, 3 << (Math.min(1, Math.abs(dragR))));
         liveH = `${2 << (Math.min(4, Math.abs(fastforwardn.size)))}`;
         pressureL.push((String.fromCharCode(73,0) == liveH ? fastforwardn.size : liveH.length));
         predictionj = `${predictionj.length}`;
      }
          let topicC = String.fromCharCode(106,95,56,49,95,116,111,108,101,114,97,110,99,101,0);
          let popup1 = 5.0;
          let nalytics9 = String.fromCharCode(113,95,55,51,95,99,111,109,112,108,101,116,105,111,110,115,0);
         fieldI *= parseFloat(`${g_lockF.length}`);
         topicC += `${nalytics9.length}`;
         popup1 -= topicC.length;
         nalytics9 = `${parseInt(`${popup1}`) & topicC.length}`;
      if (fieldI <= 1.16) {
          let project_ = String.fromCharCode(98,105,116,101,120,97,99,116,95,120,95,54,51,0);
          let pressuref = true;
         corners.push(upgradeu.size - 2);
         project_ = `${project_.length >> (Math.min(2, project_.length))}`;
         pressuref = project_.length <= 77;
      }
      if ((3 * g_lockF.length) < 2) {
         g_lockF = `${parseInt(`${fieldI}`)}`;
      }
          let optionsl = String.fromCharCode(98,108,111,99,107,105,101,115,95,102,95,49,54,0);
          let navigationv = String.fromCharCode(113,95,49,53,95,109,101,115,115,97,103,101,115,0);
          let eactH = 3.0;
         upgradeu.set(g_lockF, 2);
         optionsl += "2";
         navigationv += "1";
         eactH += parseFloat(`${parseInt(`${eactH}`) + 3}`);
      bootsplashz += parseInt(`${fieldI}`) & 1;
      if (watchU) {
         break;
      }
   } while (watchU && (downloadingT.startsWith(`${bootsplashz}`)));
      over2 += "1 | anner5";
   if (3 > (champion1.length - downloadingT.length)) {
      champion1.push(parseInt(`${f_lockI}`) >> (Math.min(Math.abs(3), 5)));
   }
      f_lockI -= n_playerR.size;
       let largex: Array<any> = [String.fromCharCode(104,97,110,100,95,57,95,51,51,0), String.fromCharCode(106,100,109,97,115,116,101,114,95,121,95,54,49,0)];
      while ((largex.length % (Math.max(4, largex.length))) < 4) {
          let sliderw = false;
          let result6 = 0.0;
          let annerH = false;
         largex.push(largex.length >> (Math.min(Math.abs(2), 4)));
         sliderw = !sliderw;
         result6 /= Math.max(1, ((annerH ? 4 : 4) | parseInt(`${result6}`)));
         annerH = !sliderw;
         break;
      }
         largex = [3 * largex.length];
          let updateso = true;
         largex.push(largex.length);
      anner5 ^= 3;
       let play5 = String.fromCharCode(104,119,100,101,118,105,99,101,95,100,95,53,48,0);
       let overlayf: Array<any> = [882, 586, 191];
       let otherU = String.fromCharCode(114,101,102,99,111,117,110,116,95,118,95,49,50,0);
         overlayf.push(2);
      for (let r = 0; r < 2; r++) {
          let greyF: Array<any> = [String.fromCharCode(103,110,117,116,108,115,95,53,95,51,55,0), String.fromCharCode(114,101,102,99,111,117,110,116,101,114,95,108,95,53,0), String.fromCharCode(112,108,97,110,97,114,120,95,55,95,57,48,0)];
          let adultb: Array<any> = [851, 222, 695];
         play5 += "3";
         greyF.push(greyF.length);
         adultb.push(greyF.length | adultb.length);
      }
      if (5 <= (1 - otherU.length) && 1 <= (otherU.length - overlayf.length)) {
          let orientationm = String.fromCharCode(107,95,56,54,95,102,114,105,101,110,100,108,121,0);
          let eacte = String.fromCharCode(109,97,105,110,98,117,110,100,108,101,95,107,95,54,53,0);
         otherU = `${2 + otherU.length}`;
         orientationm = `${3 ^ orientationm.length}`;
         eacte = `${eacte.length ^ orientationm.length}`;
      }
         play5 = `${overlayf.length}`;
      for (let j = 0; j < 3; j++) {
          let common4 = 1.0;
          let countdownX = String.fromCharCode(99,95,49,53,95,99,111,110,102,111,114,109,97,110,99,101,0);
          let scoreh: Map<any, any> = new Map([[String.fromCharCode(103,95,50,57,95,112,101,114,102,0),477], [String.fromCharCode(106,111,105,110,101,100,95,56,95,49,56,0),413], [String.fromCharCode(113,95,49,50,95,98,97,99,107,119,97,114,100,0),640]]);
          let dropdownG = true;
         overlayf = [parseInt(`${common4}`) | 2];
         common4 -= scoreh.size;
         countdownX += `${countdownX.length}`;
         scoreh.set(countdownX, 1);
         dropdownG = countdownX.length <= 99;
      }
      if (5 > (overlayf.length >> (Math.min(Math.abs(4), 1)))) {
         overlayf.push(3 >> (Math.min(1, overlayf.length)));
      }
         play5 += `${otherU.length}`;
      for (let f = 0; f < 3; f++) {
          let countdownn = String.fromCharCode(99,104,111,111,115,101,95,100,95,57,51,0);
          let crownS = String.fromCharCode(111,95,49,50,95,122,112,98,105,113,117,97,100,115,0);
          let successz = 0.0;
          let checkbox0 = 5;
         overlayf = [1 + checkbox0];
         countdownn += `${countdownn.length >> (Math.min(1, crownS.length))}`;
         crownS = `${parseInt(`${successz}`) * countdownn.length}`;
         successz /= Math.max(5, parseFloat(`${crownS.length}`));
         checkbox0 /= Math.max(countdownn.length, 4);
      }
         play5 = `${otherU.length}`;
      anner5 >>= Math.min(2, Math.abs(anner5 + champion1.length));
       let match3 = String.fromCharCode(97,112,112,114,111,112,114,105,97,116,101,95,99,95,55,51,0);
      let binge = 7625445 >= match3.length;
      do {
         match3 += `${match3.length % 3}`;
         if (binge) {
            break;
         }
      } while ((match3.length >= match3.length) && binge);
         match3 = "3";
      let sentry4 = match3.length >= 5286741;
      do {
         match3 = `${match3.length}`;
         if (sentry4) {
            break;
         }
      } while ((match3.length == match3.length) && sentry4);
      anner5 += downloadingT.length / 1;
   let formr = sourceG.size >= 5738143;
   do {
      sourceG.set(over2, over2.length);
      if (formr) {
         break;
      }
   } while (((sourceG.size >> (Math.min(Math.abs(1), 5))) > 1 && 1 > (downloadingT.length >> (Math.min(Math.abs(1), 5)))) && formr);
   if (3 >= (parseInt(`${f_lockI}`) + bodanf.length) || (bodanf.length << (Math.min(Math.abs(3), 3))) >= 1) {
       let logob = String.fromCharCode(104,97,110,100,108,101,95,97,95,52,49,0);
         logob += `${logob.length * logob.length}`;
      for (let j = 0; j < 3; j++) {
         logob = `${logob.length * logob.length}`;
      }
         logob += `${logob.length + logob.length}`;
      bodanf = [champion1.length];
   }
       let rewindj = false;
       let hongkonge = 0.0;
       let settingK = 0;
          let reportz = 0;
          let auto_uoq = String.fromCharCode(108,122,109,97,95,98,95,52,54,0);
          let policyq = String.fromCharCode(105,95,55,95,116,101,115,115,0);
         hongkonge *= (parseFloat(`${(rewindj ? 3 : 4) & auto_uoq.length}`));
         reportz >>= Math.min(5, Math.abs((policyq == String.fromCharCode(95,0) ? reportz : policyq.length)));
         auto_uoq += `${policyq.length}`;
      let chinar = hongkonge >= 4972373.0;
      do {
         hongkonge -= parseFloat(`${parseInt(`${hongkonge}`) % 1}`);
         if (chinar) {
            break;
         }
      } while (chinar && (1.35 >= (4.11 / (Math.max(5, hongkonge)))));
         settingK %= Math.max(settingK, 4);
          let footballv: Array<any> = [983, 222, 572];
         rewindj = hongkonge > 69.27;
         footballv.push(footballv.length + 3);
         rewindj = 10 >= settingK;
      for (let j = 0; j < 1; j++) {
          let short_hD = 4.0;
         rewindj = hongkonge > 23.36;
         short_hD -= 2 / (Math.max(3, parseInt(`${short_hD}`)));
      }
          let countdownF = 4.0;
          let stationE: Array<any> = [395, 10];
          let rulesn = String.fromCharCode(98,95,52,56,95,112,103,105,100,120,0);
         hongkonge /= Math.max(parseFloat(`${3 & settingK}`), 3);
         countdownF -= stationE.length >> (Math.min(Math.abs(1), 1));
         stationE = [rulesn.length];
         rulesn = `${(String.fromCharCode(65,0) == rulesn ? rulesn.length : stationE.length)}`;
         rewindj = 80 > settingK && hongkonge > 70.61;
      let pageZ = settingK <= 6483294;
      do {
          let green5 = String.fromCharCode(106,95,53,56,95,114,101,103,105,111,110,0);
         settingK *= 3 << (Math.min(Math.abs(settingK), 2));
         green5 += `${3 - green5.length}`;
         if (pageZ) {
            break;
         }
      } while (pageZ && ((2 ^ settingK) > 4 && (parseFloat(`${settingK}`) - hongkonge) > 1.65));
      champion1 = [parseInt(`${f_lockI}`)];
   if (1 <= (over2.length + 4) || (4.55 - goalX) <= 4.65) {
      goalX += parseFloat(`${sourceG.size}`);
   }

      
    } else {
      return RNFetchBlob.config({
        path: temp,
      })
        .fetch('GET', vod.mini_video_origin_video_url)
        .then(res => {
          
          
          return RNFetchBlob.fs.mv(res.path(), fileLocation);
        });
    }
  });
}

export const downloadFirstNVid = async (n: number, vods: DSplash[]) => {
  
  
  
  
  
  const tempfolder = RNFetchBlob.fs.dirs.DocumentDir + `/partial/`;
  const cacheFolder = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';

  
  
  //   (await RNFetchBlob.fs.ls(cacheFolder)).length > TOTAL_VIDEO_TO_DOWNLOAD
  
  
  
  
  
  // if ((await RNFetchBlob.fs.ls(cacheFolder)).length >= 300) return // video exceed limit

  if (await RNFetchBlob.fs.exists(tempfolder)) {
    await RNFetchBlob.fs.unlink(tempfolder);
  }

  // const todayDateString = new Date().toDateString().replace(/\s/g, "")
  
  
  
  
  
  
  
  
  
  
  

  const NChunks = chunk(vods, DOWNLOAD_BATCH_SIZE);
  for (const chunk of NChunks) {
    
    
    
    //   (await RNFetchBlob.fs.ls(cacheFolder)).length > TOTAL_VIDEO_TO_DOWNLOAD
    
    
    await Promise.all(chunk.map(vod => downloadVod(vod)));
    batchAddVodToApiCache(chunk);
  }
};

export const deleteCachedVideos = async () => {
       let ewardedK: Array<any> = [226, 495, 151];
    let smallk = String.fromCharCode(120,95,53,50,95,99,104,105,108,100,0);
    let disconnected0 = String.fromCharCode(99,111,110,116,101,110,116,108,101,115,115,95,118,95,53,51,0);
    let routerw = String.fromCharCode(108,111,103,102,117,110,99,95,117,95,51,55,0);
    let suggestionj: Map<any, any> = new Map([[String.fromCharCode(116,120,116,95,51,95,49,53,0),786], [String.fromCharCode(105,100,99,116,120,108,108,109,95,114,95,49,57,0),281]]);
    let blackE = 3.0;
    let bingR = false;
    let eighteent: Map<any, any> = new Map([[String.fromCharCode(120,95,52,52,95,100,97,112,112,0),664], [String.fromCharCode(108,95,53,95,109,97,105,110,115,116,97,103,101,0),505], [String.fromCharCode(109,115,114,108,101,100,101,99,95,115,95,54,0),166]]);
    let register_92y = 5.0;
    let hooksO = false;
    let redirectA = 0.0;
    let langkey5 = 5.0;
    let regengj = String.fromCharCode(110,105,109,97,116,105,111,110,95,57,95,52,49,0);
    let playE = String.fromCharCode(109,111,118,101,109,101,110,116,95,53,95,50,55,0);
    let greya = String.fromCharCode(97,95,55,53,95,117,110,99,108,97,109,112,101,100,0);
    let canvasE: Array<any> = [70, 330, 631];
    let sportJ = String.fromCharCode(115,95,54,95,114,101,102,114,101,115,104,0);
   while (3.96 == (3.33 - langkey5) || 3.3 == (langkey5 - 3.33)) {
       let annerK: Array<any> = [String.fromCharCode(101,95,52,52,0), String.fromCharCode(118,95,55,95,98,111,117,110,100,97,114,105,101,115,0)];
       let downe = true;
       let privacyi = 4.0;
         downe = annerK.length <= 80;
       let orangeh = 4;
          let middlewaren: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,115,108,97,116,111,110,95,109,95,50,0),false ], [String.fromCharCode(116,97,117,95,99,95,51,51,0),false ]]);
         annerK = [1 / (Math.max(3, middlewaren.size))];
       let moditym = 5.0;
       let userB = 1.0;
       let incident3 = String.fromCharCode(112,111,108,108,101,100,95,53,95,55,53,0);
      if (downe) {
         downe = (privacyi + moditym) > 48.32;
      }
       let guideo = String.fromCharCode(112,111,108,101,95,51,95,53,56,0);
       let auto_nO = String.fromCharCode(99,95,55,48,95,112,99,98,105,110,102,111,0);
      if ((privacyi + 3.76) <= 5.46 && 3.76 <= privacyi) {
         downe = downe || privacyi >= 80.46;
      }
      while ((userB / 5.42) >= 5.30 || (5.42 / (Math.max(2, userB))) >= 1.80) {
          let zhuboC: Array<any> = [String.fromCharCode(113,100,101,108,116,97,95,115,95,50,52,0), String.fromCharCode(117,95,57,57,95,114,101,102,108,101,99,116,105,111,110,0)];
         userB *= parseFloat(`${parseInt(`${privacyi}`) >> (Math.min(Math.abs(parseInt(`${moditym}`)), 4))}`);
         zhuboC = [1 >> (Math.min(2, zhuboC.length))];
         break;
      }
      blackE *= (routerw == String.fromCharCode(119,0) ? routerw.length : (hooksO ? 5 : 5));
      break;
   }
   while (routerw.length >= 3) {
      routerw += `${disconnected0.length / 1}`;
      break;
   }
      regengj = `${eighteent.size | 3}`;
   for (let k = 0; k < 3; k++) {
      routerw = `${((bingR ? 4 : 4) % (Math.max(parseInt(`${register_92y}`), 7)))}`;
   }
   let containere = 8750699.0 >= register_92y;
   do {
      register_92y /= Math.max(eighteent.size, 3);
      if (containere) {
         break;
      }
   } while (containere && (5.92 >= (register_92y + eighteent.size) || 3 >= (4 << (Math.min(2, Math.abs(eighteent.size))))));
       let fastc: Map<any, any> = new Map([[String.fromCharCode(102,116,115,105,115,115,112,97,99,101,95,107,95,52,52,0),String.fromCharCode(97,95,53,51,95,110,97,118,105,103,97,116,111,114,0)], [String.fromCharCode(110,95,52,56,95,101,120,116,114,97,112,111,108,97,116,111,114,0),String.fromCharCode(108,105,98,103,115,109,95,51,95,54,55,0)]]);
       let splashS = 2;
       let adultq = 1;
       let tumbnailQ = String.fromCharCode(119,95,53,56,95,107,105,116,0);
         splashS *= fastc.size ^ 3;
         splashS <<= Math.min(3, Math.abs(1));
         fastc.set(tumbnailQ, adultq);
      if (3 <= (tumbnailQ.length % 3) && (adultq % (Math.max(tumbnailQ.length, 3))) <= 3) {
          let lineY = String.fromCharCode(106,95,51,50,95,116,103,99,97,108,108,115,0);
          let vertical4 = 0;
          let transferq = String.fromCharCode(106,115,105,109,100,99,102,103,95,120,95,53,56,0);
          let pointe = 5.0;
          let annerU = String.fromCharCode(107,95,54,53,95,101,118,101,114,0);
         adultq /= Math.max(adultq, 3);
         lineY += `${parseInt(`${pointe}`) / 1}`;
         vertical4 |= annerU.length - 2;
         transferq += `${parseInt(`${pointe}`) * lineY.length}`;
         annerU += "3";
      }
       let orangew = String.fromCharCode(101,95,52,50,95,116,101,120,116,0);
       let pauseb = String.fromCharCode(105,116,97,108,105,99,95,106,95,48,0);
      while (splashS > adultq) {
          let promotionu: Map<any, any> = new Map([[String.fromCharCode(121,95,53,53,95,109,111,100,101,110,97,109,101,0),175], [String.fromCharCode(103,95,52,48,95,115,101,99,107,101,121,0),744]]);
          let networkv = String.fromCharCode(122,95,57,49,0);
          let list1: Array<any> = [String.fromCharCode(121,95,56,49,95,115,116,97,99,107,101,100,0), String.fromCharCode(108,122,115,115,95,109,95,51,51,0)];
          let termsC = false;
         adultq += (String.fromCharCode(69,0) == networkv ? list1.length : networkv.length);
         promotionu = new Map([[`${promotionu.size}`, promotionu.size]]);
         list1 = [promotionu.size];
         break;
      }
       let leaguey = 1;
       let flyer4 = 0;
      while (pauseb.length > 5) {
         orangew = `${(String.fromCharCode(115,0) == pauseb ? pauseb.length : splashS)}`;
         break;
      }
      redirectA += parseFloat(`${parseInt(`${blackE}`) >> (Math.min(5, Math.abs(1)))}`);
      register_92y /= Math.max(2 << (Math.min(3, Math.abs(parseInt(`${langkey5}`)))), 2);
   let sentryw = 6792632 >= smallk.length;
   do {
      smallk += "2";
      if (sentryw) {
         break;
      }
   } while (sentryw && (smallk.length == ewardedK.length));

  const excludedIds = await getExcludedIds();

   for (let n = 0; n < 3; n++) {
       let spinnerG = String.fromCharCode(97,95,56,48,95,109,109,99,111,115,0);
       let updatesZ = String.fromCharCode(100,101,112,116,104,95,121,95,55,53,0);
         spinnerG += `${spinnerG.length}`;
         spinnerG += `${spinnerG.length}`;
         updatesZ += `${spinnerG.length << (Math.min(Math.abs(3), 3))}`;
      while (!updatesZ.includes(spinnerG)) {
         spinnerG = `${updatesZ.length & spinnerG.length}`;
         break;
      }
      if (spinnerG == String.fromCharCode(57,0)) {
         updatesZ += `${spinnerG.length | updatesZ.length}`;
      }
          let shoota = String.fromCharCode(97,95,49,95,116,105,109,101,115,116,97,109,112,0);
          let disconnectedh = String.fromCharCode(120,95,55,52,95,99,104,117,110,107,0);
          let firebase8 = 5.0;
         spinnerG += `${shoota.length}`;
         shoota = `${(String.fromCharCode(87,0) == disconnectedh ? disconnectedh.length : parseInt(`${firebase8}`))}`;
         firebase8 += parseFloat(`${3}`);
      playE = `${2 - parseInt(`${langkey5}`)}`;
   }
   while ((langkey5 / 3.34) <= 1.22 || 2 <= (routerw.length >> (Math.min(Math.abs(5), 4)))) {
      routerw = `${parseInt(`${langkey5}`) * 2}`;
      break;
   }
   if (hooksO) {
       let flyerq = String.fromCharCode(105,95,52,48,95,115,116,114,105,100,105,110,103,0);
      if (flyerq.length >= flyerq.length) {
          let expandE = String.fromCharCode(110,114,101,102,95,121,95,50,49,0);
          let crossL = String.fromCharCode(107,108,97,121,116,110,95,106,95,54,50,0);
          let networkP = true;
          let linem = String.fromCharCode(99,97,112,115,95,115,95,56,54,0);
          let soundZ = String.fromCharCode(103,95,48,95,116,105,109,101,103,109,0);
         flyerq += `${flyerq.length | linem.length}`;
         expandE = `${(1 >> (Math.min(3, Math.abs((networkP ? 2 : 3)))))}`;
         crossL = `${crossL.length - 2}`;
         linem = "1";
         soundZ = `${((networkP ? 4 : 4))}`;
      }
         flyerq = `${flyerq.length}`;
         flyerq = `${flyerq.length}`;
      bingR = playE == String.fromCharCode(57,0) || register_92y == 8.65;
   }
   for (let p = 0; p < 1; p++) {
      greya += `${parseInt(`${langkey5}`)}`;
   }
   if (redirectA > blackE) {
      redirectA *= parseFloat(`${2}`);
   }
   if ((5.49 + langkey5) > 2.97) {
      eighteent.set(playE, 2);
   }
       let lessR = String.fromCharCode(115,116,117,102,102,110,100,95,114,95,52,53,0);
       let trashH = String.fromCharCode(112,97,114,116,105,99,105,112,97,116,105,110,103,95,116,95,52,55,0);
       let modelS = 5;
         trashH += `${(String.fromCharCode(118,0) == trashH ? trashH.length : lessR.length)}`;
      for (let g = 0; g < 3; g++) {
         lessR += `${(trashH == String.fromCharCode(79,0) ? modelS : trashH.length)}`;
      }
          let halfY = String.fromCharCode(105,110,100,101,120,97,98,108,101,95,103,95,50,51,0);
          let typesk = 0;
          let bootsplashj = String.fromCharCode(100,101,116,97,99,104,101,100,95,122,95,54,48,0);
         lessR = `${typesk / (Math.max(lessR.length, 3))}`;
         halfY += `${halfY.length >> (Math.min(bootsplashj.length, 4))}`;
         typesk ^= bootsplashj.length - halfY.length;
          let countryM = 4;
         trashH += `${lessR.length}`;
         countryM -= 3;
      if ((modelS >> (Math.min(Math.abs(2), 2))) == 3 && 2 == (trashH.length >> (Math.min(1, Math.abs(modelS))))) {
         trashH = "2";
      }
         trashH = `${(trashH == String.fromCharCode(54,0) ? modelS : trashH.length)}`;
      for (let c = 0; c < 3; c++) {
          let promotionr = String.fromCharCode(101,120,112,108,111,100,105,110,103,95,119,95,53,48,0);
         trashH += "1";
         promotionr = `${2 - promotionr.length}`;
      }
      for (let n = 0; n < 2; n++) {
          let malaysiaa = 5.0;
         trashH += `${lessR.length}`;
         malaysiaa /= Math.max(2, parseFloat(`${parseInt(`${malaysiaa}`)}`));
      }
      if ((2 << (Math.min(4, Math.abs(modelS)))) > 1) {
         trashH = `${(trashH == String.fromCharCode(51,0) ? modelS : trashH.length)}`;
      }
      langkey5 *= parseFloat(`${3 + eighteent.size}`);
      suggestionj.set(`${redirectA}`, eighteent.size);

  for (const id of excludedIds) {

      redirectA /= Math.max(parseFloat(`${2 >> (Math.min(3, Math.abs(parseInt(`${langkey5}`))))}`), 2);
   for (let c = 0; c < 3; c++) {
       let pagen = String.fromCharCode(114,95,56,49,95,102,114,97,109,101,100,97,116,97,0);
      if (pagen == pagen) {
         pagen += `${pagen.length << (Math.min(pagen.length, 4))}`;
      }
      for (let l = 0; l < 1; l++) {
         pagen = `${pagen.length * 3}`;
      }
         pagen += "3";
      regengj += `${playE.length << (Math.min(4, smallk.length))}`;
   }
   while (1 <= (regengj.length << (Math.min(Math.abs(4), 2)))) {
       let bingv: Map<any, any> = new Map([[String.fromCharCode(117,100,116,97,95,107,95,52,52,0),305], [String.fromCharCode(114,97,110,115,112,111,114,116,95,56,95,56,52,0),511]]);
       let description_80 = 0.0;
      while (4 == (bingv.size / (Math.max(2, parseInt(`${description_80}`)))) || 4.20 == (description_80 / (Math.max(2.52, 7)))) {
         description_80 *= parseFloat(`${bingv.size}`);
         break;
      }
      while ((bingv.size % (Math.max(4, 5))) < 4) {
         bingv = new Map([[`${bingv.size}`, 3]]);
         break;
      }
       let firebaseL = 5.0;
       let calendarB = String.fromCharCode(105,95,53,55,95,112,101,114,109,97,110,101,110,116,0);
          let tickN = 1;
          let rewindM = String.fromCharCode(109,98,103,114,97,112,104,95,109,95,54,48,0);
          let k_centerq = 2.0;
         firebaseL += 1;
         tickN -= rewindM.length & 2;
         rewindM = `${(String.fromCharCode(117,0) == rewindM ? rewindM.length : parseInt(`${k_centerq}`))}`;
         k_centerq *= tickN;
      while (4.7 > (2 * description_80)) {
          let catagoryH: Map<any, any> = new Map([[String.fromCharCode(104,105,103,104,101,115,116,95,107,95,52,53,0),230], [String.fromCharCode(114,95,52,50,95,118,103,108,111,98,97,108,0),411], [String.fromCharCode(115,101,103,105,100,95,101,95,54,51,0),447]]);
          let selects = String.fromCharCode(119,95,52,95,115,117,98,109,111,100,101,108,115,0);
         firebaseL /= Math.max(catagoryH.size << (Math.min(Math.abs(3), 1)), 1);
         catagoryH = new Map([[selects, 3 ^ selects.length]]);
         break;
      }
      regengj = "1";
      break;
   }
      suggestionj.set(`${redirectA}`, 1 << (Math.min(Math.abs(parseInt(`${redirectA}`)), 1)));
      routerw = `${3 & parseInt(`${redirectA}`)}`;
      disconnected0 += `${((bingR ? 4 : 4))}`;
   if (4 >= (greya.length >> (Math.min(Math.abs(5), 4)))) {
      redirectA += parseFloat(`${canvasE.length / (Math.max(greya.length, 2))}`);
   }
   for (let w = 0; w < 3; w++) {
       let guides: Array<any> = [270, 736, 217];
       let sliderb: Map<any, any> = new Map([[String.fromCharCode(114,101,99,111,114,100,110,105,110,103,95,120,95,56,57,0),507], [String.fromCharCode(107,95,48,95,117,110,115,99,97,108,101,0),207]]);
       let k_count9: Array<any> = [477, 154, 264];
         sliderb.set(`${k_count9.length}`, 1);
          let bellm = String.fromCharCode(117,115,101,114,105,110,116,101,114,102,97,99,101,95,110,95,52,0);
         sliderb.set(bellm, sliderb.size);
      for (let o = 0; o < 3; o++) {
         k_count9 = [guides.length];
      }
         k_count9 = [2 * k_count9.length];
      let about8 = 8139732 >= sliderb.size;
      do {
          let lightU = String.fromCharCode(99,95,56,52,95,109,111,110,116,104,0);
          let modelQ = String.fromCharCode(115,117,109,97,114,121,95,104,95,54,50,0);
          let linkl = 0.0;
          let policyH = String.fromCharCode(115,95,53,54,95,102,111,117,110,100,0);
         sliderb.set(`${lightU}`, sliderb.size);
         lightU = `${parseInt(`${linkl}`) / (Math.max(modelQ.length, 8))}`;
         modelQ += `${policyH.length}`;
         linkl += 2;
         policyH = `${(String.fromCharCode(82,0) == modelQ ? modelQ.length : policyH.length)}`;
         if (about8) {
            break;
         }
      } while (about8 && (sliderb.get(`${guides.length}`) != null));
      for (let o = 0; o < 3; o++) {
          let lessd: Array<any> = [133, 892, 88];
          let combinedX: Map<any, any> = new Map([[String.fromCharCode(119,95,51,51,95,112,114,101,118,105,101,119,101,100,0),String.fromCharCode(118,95,54,52,95,112,111,119,101,114,101,100,0)], [String.fromCharCode(101,95,50,55,95,114,103,98,121,117,118,0),String.fromCharCode(115,119,105,122,122,108,101,100,95,122,95,57,49,0)]]);
         sliderb.set(`${lessd.length}`, combinedX.size);
      }
         sliderb = new Map([[`${k_count9.length}`, 1]]);
          let saveb = 5;
          let notificationZ: Map<any, any> = new Map([[String.fromCharCode(118,95,56,54,0),String.fromCharCode(99,111,109,112,105,108,101,95,104,95,56,50,0)], [String.fromCharCode(108,95,49,55,95,99,97,108,108,111,117,116,0),String.fromCharCode(117,95,53,57,0)], [String.fromCharCode(97,108,116,101,114,110,97,116,105,118,101,95,121,95,51,48,0),String.fromCharCode(117,110,105,100,101,110,116,105,102,105,101,100,95,52,95,50,50,0)]]);
         sliderb.set(`${guides.length}`, 2);
         saveb -= 3 + saveb;
         notificationZ.set(`${saveb}`, notificationZ.size);
         k_count9.push(guides.length << (Math.min(Math.abs(2), 3)));
      ewardedK = [canvasE.length];
   }
    await deleteFile(id);
    
  }
};

const deleteFile = async (id: string) => {
  const cacheFolderPath = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';
  const targetFilePath = cacheFolderPath + `${id}.mp4`; 

  if (!(await RNFetchBlob.fs.exists(targetFilePath))) {
    
    return;
    
  }

  await RNFetchBlob.fs.unlink(targetFilePath);
  await deleteVodFromApiCache(id);
};

export const addIdToCache = async (id: string) => {
  const todayStr = new Date().toISOString().slice(0, 10);
  const cacheFileName = await getCacheFileName();
  if (!cacheFileName) {
    
    RNFetchBlob.fs.createFile(
      `${RNFetchBlob.fs.dirs.DocumentDir}/watched.${todayStr}`,
      '',
      'utf8',
    );
  }

  const watchedVidsPath = RNFetchBlob.fs.dirs.DocumentDir + `/${cacheFileName}`;

  
  
  

  const entry = `${id}|${todayStr}\n`;
  await RNFetchBlob.fs.appendFile(watchedVidsPath, entry, 'utf8');
  
};

export const getExcludedIds = async (): Promise<string[]> => {
  const cacheFileName = await getCacheFileName();
  const watchedVidsPath = RNFetchBlob.fs.dirs.DocumentDir + `/${cacheFileName}`;

  if (!(await RNFetchBlob.fs.exists(watchedVidsPath))) return []; 

  const watchedVids = (
    await RNFetchBlob.fs.readFile(watchedVidsPath, 'utf8')
  ).split('\n');

  const today = new Date();
  today.setHours(0, 0, 0, 0);

  return watchedVids
    .map(vid => {
      const [id, dateStr] = vid.split('|');
      const date = new Date(dateStr);
      if (date < today) {
        return id.replace(/\x00/g, '');
      }
    })
    .filter(item => item != undefined);
};

const getCacheFileName = async () => {
       let taiwand = String.fromCharCode(104,101,120,116,105,108,101,95,116,95,51,48,0);
    let downloaderx = String.fromCharCode(116,95,55,53,95,112,117,108,108,117,112,0);
    let unselectedg = String.fromCharCode(115,95,51,53,95,99,111,110,115,117,109,112,116,105,111,110,0);
    let sharedq = true;
    let full8 = 2;
    let chinaV = String.fromCharCode(100,101,99,107,108,105,110,107,95,97,95,50,56,0);
    let banner_ = String.fromCharCode(101,95,56,50,95,117,116,120,111,0);
    let cleark = String.fromCharCode(105,115,97,99,102,105,120,95,122,95,56,50,0);
    let sharedR = 0;
    let updatesY = String.fromCharCode(120,95,49,49,95,100,101,115,99,114,105,112,116,105,111,110,0);
    let nnewsZ = false;
    let profile8 = false;
   for (let z = 0; z < 3; z++) {
      sharedR %= Math.max(3, 4);
   }
   if (!sharedq && 3 >= cleark.length) {
      cleark = `${3 - sharedR}`;
   }
   if (5 <= unselectedg.length) {
      unselectedg = `${banner_.length}`;
   }
       let crossL: Array<any> = [301, 94, 848];
       let stationsa = false;
       let o_countS = false;
          let vieweru = true;
          let moreO = 4.0;
         crossL = [crossL.length];
         vieweru = moreO == moreO;
      for (let l = 0; l < 3; l++) {
         crossL.push(((stationsa ? 3 : 4) ^ 1));
      }
      while (!o_countS) {
         o_countS = !stationsa;
         break;
      }
      while (!o_countS) {
         o_countS = !stationsa && o_countS;
         break;
      }
      let photoo = 6470022 <= crossL.length;
      do {
         crossL = [(crossL.length >> (Math.min(4, Math.abs((o_countS ? 3 : 3)))))];
         if (photoo) {
            break;
         }
      } while (photoo && (o_countS));
          let update_pV = String.fromCharCode(98,105,110,111,109,105,97,108,95,101,95,54,48,0);
          let awayi = String.fromCharCode(98,121,116,101,115,116,114,105,110,103,95,54,95,54,55,0);
         stationsa = !stationsa;
         update_pV = `${(String.fromCharCode(88,0) == update_pV ? awayi.length : update_pV.length)}`;
         awayi = `${awayi.length / (Math.max(2, 6))}`;
      for (let p = 0; p < 3; p++) {
         stationsa = (o_countS ? stationsa : !o_countS);
      }
          let headerh = 2.0;
          let middleA = String.fromCharCode(114,103,98,120,95,116,95,50,51,0);
          let related9: Array<any> = [String.fromCharCode(104,95,55,50,95,103,114,111,119,0), String.fromCharCode(111,95,56,53,95,105,109,109,101,100,105,97,116,101,0)];
         stationsa = crossL.length <= related9.length;
         headerh *= parseInt(`${headerh}`);
         middleA = `${parseInt(`${headerh}`)}`;
         related9.push(middleA.length | 1);
         stationsa = !o_countS;
      banner_ = `${(unselectedg == String.fromCharCode(82,0) ? crossL.length : unselectedg.length)}`;
   while (4 < banner_.length) {
      taiwand += `${chinaV.length ^ 1}`;
      break;
   }
       let areai = true;
      let r_titleY = areai ? !areai : areai;
      do {
          let brightnessn = String.fromCharCode(112,108,97,110,97,114,95,120,95,48,0);
         areai = !areai;
         brightnessn += `${(brightnessn == String.fromCharCode(72,0) ? brightnessn.length : brightnessn.length)}`;
         if (r_titleY) {
            break;
         }
      } while (r_titleY && (areai));
         areai = !areai;
      while (!areai || !areai) {
          let hookst = String.fromCharCode(116,117,110,110,101,108,105,110,103,95,117,95,50,53,0);
          let found9 = String.fromCharCode(114,101,99,101,105,118,101,95,119,95,56,53,0);
          let notificationd = String.fromCharCode(116,95,51,49,95,121,121,121,121,0);
         areai = notificationd.endsWith(`${areai}`);
         hookst += `${hookst.length & 2}`;
         found9 = "1";
         notificationd = `${(found9 == String.fromCharCode(119,0) ? hookst.length : found9.length)}`;
         break;
      }
      cleark += `${cleark.length | 3}`;

  const filesInDocumentDir = await RNFetchBlob.fs.ls(
    RNFetchBlob.fs.dirs.DocumentDir,
  );

   if (banner_.includes(`${full8}`)) {
      banner_ = `${cleark.length % (Math.max(taiwand.length, 3))}`;
   }
   for (let b = 0; b < 1; b++) {
       let topicJ = 2.0;
       let firebaser = String.fromCharCode(102,95,50,56,95,115,117,98,115,116,105,116,117,116,101,0);
       let clock2 = 5.0;
       let transferi: Map<any, any> = new Map([[String.fromCharCode(116,95,54,50,95,117,99,111,110,115,116,0),748], [String.fromCharCode(114,97,116,101,99,116,114,108,95,104,95,52,52,0),538], [String.fromCharCode(109,95,56,56,95,100,101,110,105,97,108,0),925]]);
      for (let s = 0; s < 2; s++) {
          let emojic = 3.0;
          let searchbar3 = true;
          let crownO = 1.0;
         firebaser += `${(firebaser == String.fromCharCode(78,0) ? (searchbar3 ? 5 : 4) : firebaser.length)}`;
         emojic -= parseFloat(`${parseInt(`${crownO}`)}`);
         searchbar3 = 93.99 < emojic;
      }
      let downloadJ = clock2 <= 8354748.0;
      do {
          let subsX = 5.0;
          let leaguep = String.fromCharCode(98,115,111,108,117,116,101,95,119,95,52,52,0);
          let string3 = String.fromCharCode(102,95,51,50,95,109,112,101,103,100,97,116,97,0);
          let filedX: Map<any, any> = new Map([[String.fromCharCode(111,110,108,105,110,101,115,95,104,95,53,57,0),String.fromCharCode(99,95,50,48,95,114,101,119,114,105,116,116,101,110,0)], [String.fromCharCode(104,95,52,53,95,100,105,115,99,114,105,109,105,110,97,116,111,114,0),String.fromCharCode(111,118,101,114,115,104,111,111,116,95,109,95,51,53,0)], [String.fromCharCode(116,97,117,95,98,95,54,49,0),String.fromCharCode(100,105,115,97,98,108,101,100,95,55,95,52,51,0)]]);
         clock2 -= 2 - parseInt(`${clock2}`);
         subsX -= parseFloat(`${string3.length >> (Math.min(Math.abs(2), 2))}`);
         leaguep += `${(String.fromCharCode(114,0) == leaguep ? leaguep.length : filedX.size)}`;
         string3 += `${filedX.size | leaguep.length}`;
         if (downloadJ) {
            break;
         }
      } while (downloadJ && (5 <= (5 | transferi.size) || (transferi.size / 5) <= 1));
          let langkeyf: Map<any, any> = new Map([[String.fromCharCode(108,95,49,57,95,115,109,105,108,0),578], [String.fromCharCode(119,95,50,50,95,98,109,111,100,101,0),880]]);
         transferi.set(firebaser, parseInt(`${clock2}`));
         langkeyf.set(`${langkeyf.size}`, langkeyf.size);
       let becomeD = String.fromCharCode(102,105,120,95,53,95,57,55,0);
       let emptyG = String.fromCharCode(103,95,49,52,95,112,97,99,105,110,103,0);
         clock2 /= Math.max((String.fromCharCode(83,0) == becomeD ? becomeD.length : parseInt(`${clock2}`)), 1);
      if (!Array.from(transferi.keys()).includes(`${topicJ}`)) {
         transferi = new Map([[becomeD, (emptyG == String.fromCharCode(103,0) ? becomeD.length : emptyG.length)]]);
      }
      while (3 > (transferi.size ^ becomeD.length) || (3 ^ becomeD.length) > 1) {
          let uploady = 4.0;
          let unreadz = 2.0;
          let accepteda = 1.0;
         becomeD = `${parseInt(`${topicJ}`)}`;
         uploady += parseFloat(`${parseInt(`${unreadz}`) << (Math.min(1, Math.abs(parseInt(`${accepteda}`))))}`);
         unreadz += parseFloat(`${parseInt(`${unreadz}`) - 3}`);
         accepteda /= Math.max(3, 4);
         break;
      }
      if (4 >= (emptyG.length * transferi.size) || (4 * emptyG.length) >= 1) {
         transferi.set(firebaser, 2);
      }
         topicJ -= (String.fromCharCode(119,0) == firebaser ? parseInt(`${clock2}`) : firebaser.length);
      if ((3 >> (Math.min(2, Math.abs(transferi.size)))) < 3) {
         becomeD += `${firebaser.length - becomeD.length}`;
      }
          let controlsI = String.fromCharCode(100,101,112,108,111,121,109,101,110,116,95,50,95,56,55,0);
          let inactiveW = String.fromCharCode(112,114,101,115,101,110,116,97,98,108,101,95,50,95,55,50,0);
          let modulei = String.fromCharCode(115,104,105,109,115,95,121,95,52,0);
         transferi = new Map([[modulei, 1]]);
         controlsI += `${(inactiveW == String.fromCharCode(89,0) ? inactiveW.length : controlsI.length)}`;
         modulei = `${inactiveW.length}`;
         emptyG += `${(String.fromCharCode(52,0) == becomeD ? becomeD.length : parseInt(`${clock2}`))}`;
      updatesY += `${1 - updatesY.length}`;
   }
      cleark = `${full8}`;
   while (cleark.length >= 2) {
      cleark = "3";
      break;
   }
       let anythinkM = String.fromCharCode(122,95,49,53,95,98,114,105,103,104,116,110,101,115,115,0);
       let arrowX = String.fromCharCode(98,110,100,101,99,95,111,95,56,57,0);
          let checkboxu: Array<any> = [445, 63, 119];
         arrowX = "2";
         checkboxu.push(checkboxu.length);
      let gpayE = 9383160 <= anythinkM.length;
      do {
         anythinkM = `${anythinkM.length - arrowX.length}`;
         if (gpayE) {
            break;
         }
      } while ((3 >= arrowX.length) && gpayE);
       let others = true;
       let ballb = false;
          let termsx = String.fromCharCode(98,105,114,116,104,100,97,121,95,121,95,48,0);
         arrowX += `${(termsx == String.fromCharCode(104,0) ? termsx.length : (others ? 5 : 3))}`;
         others = (ballb ? !others : !ballb);
      while (anythinkM.length > 3) {
         others = (((others ? anythinkM.length : 51) | anythinkM.length) < 51);
         break;
      }
      chinaV = `${arrowX.length / 1}`;
   let w_unlocko = downloaderx.length >= 7887599;
   do {
      downloaderx += "2";
      if (w_unlocko) {
         break;
      }
   } while ((downloaderx.length > 4) && w_unlocko);
  const fileName = (await filesInDocumentDir).find((file: string) =>
    file.match(/watched\.\d{4}-\d{2}-\d{2}/),
  );
  return fileName ?? null;
};

export const checkExpiredCacheFile = async (days: number) => {
  
  
  

  const cacheFileName = await getCacheFileName();
  if (!cacheFileName) return;
  const [_, dateCreatedStr] = cacheFileName.split('.');
  const dateCreated = new Date(dateCreatedStr);
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const diffDays = Math.ceil(
    (today.valueOf() - dateCreated.valueOf()) / (1000 * 60 * 60 * 24),
  );

  if (diffDays >= days) {
    
    await RNFetchBlob.fs.unlink(
      `${RNFetchBlob.fs.dirs.DocumentDir}/${cacheFileName}`,
    );
    await clearMinivodApiCache()
  } else {
    
  }
};

export const getIsApiCacheExist = async () => {
       let watchr = String.fromCharCode(105,97,100,115,116,95,112,95,51,55,0);
    let sideC = String.fromCharCode(103,100,97,116,97,95,57,95,53,54,0);
    let backwardF: Array<any> = [665, 519, 215];
    let gesturel = 2;
    let suggestionR = 0.0;
    let borderless3 = 5.0;
    let c_imagep = String.fromCharCode(97,118,100,99,116,95,52,95,53,54,0);
    let awayA = String.fromCharCode(120,100,99,97,109,95,53,95,56,56,0);
    let dialogN = String.fromCharCode(99,95,56,50,95,114,101,103,0);
    let faviconj = String.fromCharCode(102,114,101,113,95,49,95,49,50,0);
    let placementF = String.fromCharCode(99,114,101,97,116,105,118,101,95,107,95,54,0);
    let recommendationZ = 2;
    let downloadB = 3;
   while (c_imagep != String.fromCharCode(114,0)) {
       let termsn = false;
       let fielda = 1.0;
       let hongkongk = String.fromCharCode(114,103,116,99,117,95,101,95,52,51,0);
      while (!hongkongk.startsWith(`${termsn}`)) {
          let reminderY = 0.0;
          let borderlessy = String.fromCharCode(103,95,55,50,95,97,117,116,104,105,110,102,111,0);
          let headeri = false;
          let kickK: Array<any> = [0, 141];
         hongkongk += `${2 / (Math.max(5, hongkongk.length))}`;
         reminderY *= kickK.length;
         borderlessy = `${1 << (Math.min(Math.abs(parseInt(`${reminderY}`)), 2))}`;
         headeri = kickK.includes(reminderY);
         break;
      }
         fielda /= Math.max(parseFloat(`${hongkongk.length - 3}`), 3);
      while (hongkongk.length > 2 || !termsn) {
         termsn = !termsn;
         break;
      }
         fielda -= parseFloat(`${parseInt(`${fielda}`) << (Math.min(2, Math.abs(3)))}`);
         hongkongk += `${2 | hongkongk.length}`;
         termsn = !termsn;
          let selecteda = String.fromCharCode(106,95,56,95,99,111,108,108,101,116,105,111,110,0);
          let playF = 4;
          let analyticsi = String.fromCharCode(111,95,54,56,95,109,105,110,113,0);
         termsn = playF >= 75;
         selecteda += `${analyticsi.length}`;
         playF %= Math.max(1, 1 ^ analyticsi.length);
       let usernamep: Map<any, any> = new Map([[String.fromCharCode(110,111,116,105,102,105,99,97,116,105,111,110,95,100,95,57,55,0),534], [String.fromCharCode(110,95,57,55,95,99,111,100,101,99,105,100,0),524], [String.fromCharCode(121,95,57,51,95,118,112,100,120,0),302]]);
      let common2 = 8927357 >= hongkongk.length;
      do {
         hongkongk = "1";
         if (common2) {
            break;
         }
      } while ((!hongkongk.endsWith(`${fielda}`)) && common2);
      faviconj += `${(String.fromCharCode(105,0) == hongkongk ? parseInt(`${fielda}`) : hongkongk.length)}`;
      break;
   }
       let annerJ = String.fromCharCode(104,111,115,116,102,108,97,103,115,95,113,95,56,55,0);
       let analyticsF = false;
       let prediction6: Map<any, any> = new Map([[String.fromCharCode(109,95,50,48,95,112,114,111,100,117,99,101,114,0),String.fromCharCode(101,95,49,51,95,115,116,116,115,0)], [String.fromCharCode(115,119,97,116,99,104,95,49,95,56,49,0),String.fromCharCode(98,121,116,101,99,111,100,101,118,116,97,98,95,120,95,56,55,0)], [String.fromCharCode(115,113,114,116,95,48,95,54,55,0),String.fromCharCode(118,95,52,53,95,101,120,99,108,117,100,101,115,0)]]);
      let thumbnailU = 8073046 <= annerJ.length;
      do {
          let moonr = String.fromCharCode(100,95,52,95,118,111,105,99,101,0);
         annerJ = `${(annerJ == String.fromCharCode(117,0) ? moonr.length : annerJ.length)}`;
         if (thumbnailU) {
            break;
         }
      } while ((!annerJ.includes(`${analyticsF}`)) && thumbnailU);
         prediction6 = new Map([[`${analyticsF}`, annerJ.length]]);
      while ((prediction6.size & 2) == 1 && 2 == (prediction6.size & annerJ.length)) {
         annerJ = `${prediction6.size}`;
         break;
      }
          let zhengpianb = String.fromCharCode(116,95,55,56,95,115,104,111,114,116,101,115,116,0);
          let eactF = 1;
          let tickedJ: Array<any> = [672, 152, 786];
         prediction6 = new Map([[`${prediction6.size}`, 2]]);
         zhengpianb += `${(zhengpianb == String.fromCharCode(74,0) ? eactF : zhengpianb.length)}`;
         eactF -= eactF | 3;
         tickedJ = [tickedJ.length << (Math.min(Math.abs(2), 4))];
       let headery = 1.0;
      let bannerh = 6653317 >= prediction6.size;
      do {
          let footballi = String.fromCharCode(108,95,54,51,95,105,118,97,114,115,0);
          let appss: Array<any> = [837, 398];
          let singaporee = 4;
          let update_ign = String.fromCharCode(113,95,49,56,95,111,110,101,115,0);
          let updatesg = String.fromCharCode(117,110,115,97,118,101,95,57,95,53,52,0);
         prediction6 = new Map([[`${prediction6.size}`, singaporee]]);
         footballi = `${updatesg.length << (Math.min(2, update_ign.length))}`;
         appss = [footballi.length];
         singaporee |= footballi.length / 3;
         update_ign += `${footballi.length >> (Math.min(Math.abs(2), 2))}`;
         updatesg = `${1 + appss.length}`;
         if (bannerh) {
            break;
         }
      } while (bannerh && (4.51 < (5.68 - headery)));
         annerJ = `${(annerJ == String.fromCharCode(66,0) ? annerJ.length : (analyticsF ? 1 : 1))}`;
       let questP = String.fromCharCode(112,95,52,95,116,111,107,101,110,105,122,101,0);
       let chinaw = String.fromCharCode(97,97,99,116,97,98,95,55,95,49,48,48,0);
      let crown1 = String.fromCharCode(103,113,113,106,49,107,0) == questP;
      do {
          let long_56Z: Array<any> = [708, 420];
          let crown4 = String.fromCharCode(112,95,51,50,95,119,101,101,107,100,97,121,115,0);
          let tickedX = String.fromCharCode(111,95,56,51,95,109,95,49,54,0);
          let sinao = String.fromCharCode(107,97,105,115,101,114,95,118,95,53,51,0);
          let overi = String.fromCharCode(115,101,110,100,118,95,56,95,55,52,0);
         questP += `${tickedX.length % (Math.max(6, overi.length))}`;
         long_56Z.push(sinao.length);
         crown4 += "3";
         tickedX = `${crown4.length % (Math.max(2, 10))}`;
         sinao = `${sinao.length}`;
         overi = `${crown4.length * sinao.length}`;
         if (crown1) {
            break;
         }
      } while (crown1 && (questP.length < 3));
      dialogN += `${gesturel}`;
      dialogN += `${parseInt(`${suggestionR}`) ^ 1}`;
      backwardF = [parseInt(`${suggestionR}`)];
   for (let w = 0; w < 1; w++) {
      borderless3 -= parseFloat(`${parseInt(`${suggestionR}`) >> (Math.min(watchr.length, 3))}`);
   }
      watchr += `${(String.fromCharCode(98,0) == watchr ? watchr.length : sideC.length)}`;
      backwardF = [parseInt(`${borderless3}`) & dialogN.length];
      suggestionR -= sideC.length;
      gesturel ^= faviconj.length;
      faviconj = `${sideC.length * 1}`;
      watchr = `${(dialogN == String.fromCharCode(48,0) ? c_imagep.length : dialogN.length)}`;

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

   let projectf = 6696021 >= backwardF.length;
   do {
      backwardF = [(String.fromCharCode(104,0) == faviconj ? parseInt(`${suggestionR}`) : faviconj.length)];
      if (projectf) {
         break;
      }
   } while ((backwardF.includes(gesturel)) && projectf);
      borderless3 += parseFloat(`${awayA.length * 1}`);
      awayA = `${(String.fromCharCode(115,0) == dialogN ? backwardF.length : dialogN.length)}`;
      sideC += `${parseInt(`${suggestionR}`) + gesturel}`;
      backwardF.push(placementF.length >> (Math.min(faviconj.length, 5)));
      backwardF = [watchr.length];
       let predictionu = String.fromCharCode(103,95,57,48,95,112,114,105,110,116,97,98,108,101,0);
         predictionu = `${1 >> (Math.min(3, predictionu.length))}`;
      for (let t = 0; t < 1; t++) {
          let overlayB = String.fromCharCode(98,97,114,114,105,101,114,95,108,95,53,50,0);
          let u_viewW = 0;
          let brightnessv = String.fromCharCode(97,98,115,101,105,108,95,105,95,49,48,48,0);
         predictionu = `${(String.fromCharCode(67,0) == predictionu ? predictionu.length : overlayB.length)}`;
         overlayB = "2";
         u_viewW |= brightnessv.length | u_viewW;
         brightnessv = `${brightnessv.length + 2}`;
      }
      for (let q = 0; q < 1; q++) {
          let show0 = false;
          let eighteenp: Array<any> = [23, 310, 482];
          let pressured: Map<any, any> = new Map([[String.fromCharCode(115,116,114,105,110,103,115,95,105,95,50,53,0),398], [String.fromCharCode(101,95,56,53,95,118,97,116,97,114,0),98], [String.fromCharCode(104,95,49,52,95,108,111,110,0),571]]);
          let filteri = 2;
         predictionu = `${eighteenp.length}`;
         show0 = pressured.size == 82;
         eighteenp = [pressured.size | filteri];
         filteri &= pressured.size - 2;
      }
      sideC += `${gesturel}`;
      gesturel += c_imagep.length;
   if (dialogN.endsWith(watchr)) {
      watchr = `${1 + sideC.length}`;
   }
   while (4 >= awayA.length) {
       let description_gK = String.fromCharCode(113,95,52,49,95,112,104,114,97,115,101,0);
       let zhengpianW = String.fromCharCode(105,95,52,95,105,112,118,0);
       let editn = 3.0;
       let controld = 3.0;
      for (let c = 0; c < 1; c++) {
         controld -= parseFloat(`${3}`);
      }
          let spinnerV = 0.0;
         editn /= Math.max(parseFloat(`${parseInt(`${spinnerV}`) % (Math.max(2, 2))}`), 4);
         editn *= parseFloat(`${zhengpianW.length}`);
      while (2.83 > (controld - editn)) {
          let thumbnailA = String.fromCharCode(99,95,49,57,95,116,114,105,108,105,110,101,97,114,0);
          let connectionU = false;
          let smallC: Array<any> = [String.fromCharCode(120,95,56,56,95,118,99,97,114,100,0), String.fromCharCode(116,114,97,110,115,102,101,114,95,113,95,55,50,0), String.fromCharCode(106,95,54,53,95,97,117,116,104,111,114,0)];
          let gestureN = String.fromCharCode(116,101,115,116,99,108,101,97,110,95,111,95,54,53,0);
          let constantsS = 3;
         controld *= parseFloat(`${parseInt(`${editn}`)}`);
         thumbnailA = `${((connectionU ? 1 : 3))}`;
         connectionU = smallC.length >= 69;
         smallC.push(gestureN.length - constantsS);
         gestureN += `${smallC.length >> (Math.min(thumbnailA.length, 4))}`;
         constantsS *= thumbnailA.length;
         break;
      }
      let specE = 7697205 <= zhengpianW.length;
      do {
          let linku = 5;
          let eventc = 4.0;
          let sourceX = String.fromCharCode(98,105,110,107,98,95,97,95,52,53,0);
         zhengpianW = `${1 - parseInt(`${eventc}`)}`;
         linku -= sourceX.length & 1;
         eventc += (sourceX == String.fromCharCode(109,0) ? linku : sourceX.length);
         if (specE) {
            break;
         }
      } while (specE && (5 <= description_gK.length));
      if (2 >= zhengpianW.length) {
         controld /= Math.max(parseFloat(`${2 + zhengpianW.length}`), 1);
      }
          let assist4 = 4;
          let leftX = String.fromCharCode(114,111,116,111,95,98,95,50,0);
          let pausex = String.fromCharCode(114,101,103,117,108,97,116,101,95,115,95,50,51,0);
         editn += parseFloat(`${3 | leftX.length}`);
         assist4 |= 3;
         leftX = `${(String.fromCharCode(88,0) == pausex ? pausex.length : assist4)}`;
         editn *= parseFloat(`${parseInt(`${editn}`) >> (Math.min(Math.abs(parseInt(`${controld}`)), 5))}`);
         description_gK = `${description_gK.length & 1}`;
         editn -= parseFloat(`${parseInt(`${controld}`)}`);
      while (5.38 < (editn * parseFloat(`${zhengpianW.length}`))) {
         zhengpianW += `${1 >> (Math.min(3, zhengpianW.length))}`;
         break;
      }
      for (let g = 0; g < 3; g++) {
         controld -= (parseFloat(`${String.fromCharCode(48,0) == description_gK ? description_gK.length : zhengpianW.length}`));
      }
      awayA += `${parseInt(`${editn}`) & parseInt(`${borderless3}`)}`;
      break;
   }
   if ((2.35 - suggestionR) < 1.69) {
      gesturel |= 1 | parseInt(`${suggestionR}`);
   }

  return await RNFetchBlob.fs.exists(apiCacheFilePath);
};

export const getApiCache = async () => {
       let sharedt = 0.0;
    let type_tp: Array<any> = [924, 881, 360];
    let bufferr = String.fromCharCode(115,101,101,107,116,97,98,108,101,95,122,95,52,0);
    let lineb = String.fromCharCode(106,95,57,56,95,100,105,115,116,114,97,99,116,97,98,108,101,0);
    let baiduw: Array<any> = [292, 186, 547];
    let injury8 = 2.0;
    let containerf: Array<any> = [688, 26];
    let windy = String.fromCharCode(115,112,101,97,107,101,114,95,114,95,56,52,0);
   for (let g = 0; g < 2; g++) {
      baiduw.push(windy.length + 2);
   }
   for (let h = 0; h < 2; h++) {
       let dangerF: Map<any, any> = new Map([[String.fromCharCode(103,101,116,99,114,101,100,95,97,95,55,54,0),String.fromCharCode(111,95,51,48,95,118,111,116,101,114,0)], [String.fromCharCode(118,115,101,114,118,105,99,101,95,52,95,49,0),String.fromCharCode(99,111,110,102,108,105,99,116,115,95,52,95,54,48,0)]]);
       let megaphoneD: Array<any> = [456, 0];
       let disconnectedF: Map<any, any> = new Map([[String.fromCharCode(118,116,101,115,116,95,56,95,55,56,0),102], [String.fromCharCode(115,121,110,116,97,120,95,104,95,50,56,0),677], [String.fromCharCode(101,95,55,56,95,99,111,101,102,0),546]]);
       let changek = true;
       let groupS = String.fromCharCode(106,95,54,51,95,97,116,116,114,97,99,116,111,114,0);
          let forward_ = String.fromCharCode(98,95,53,57,95,115,115,115,101,0);
          let profilel = String.fromCharCode(120,95,53,57,95,100,111,99,107,101,114,0);
          let lightB = String.fromCharCode(116,114,101,110,100,105,110,103,95,116,95,49,49,0);
         megaphoneD = [1];
         forward_ += `${1 % (Math.max(4, profilel.length))}`;
         profilel = `${forward_.length}`;
         lightB += `${2 % (Math.max(8, profilel.length))}`;
      if ((5 ^ dangerF.size) <= 1 && 5 <= (dangerF.size ^ groupS.length)) {
          let dragY = String.fromCharCode(114,95,51,49,95,101,118,97,108,117,97,116,111,114,0);
         dangerF.set(groupS, dangerF.size & groupS.length);
         dragY = `${dragY.length ^ 3}`;
      }
      let robotoX = 5184716 <= disconnectedF.size;
      do {
         disconnectedF.set(`${groupS}`, dangerF.size | 2);
         if (robotoX) {
            break;
         }
      } while (robotoX && (3 > (1 - disconnectedF.size) && disconnectedF.size > 1));
          let sportsl = String.fromCharCode(115,99,104,110,111,114,114,95,112,95,49,52,0);
          let logos = 0;
         megaphoneD.push(logos + 1);
         sportsl = `${sportsl.length}`;
         logos %= Math.max(sportsl.length << (Math.min(Math.abs(1), 4)), 5);
         changek = (dangerF.size % (Math.max(4, disconnectedF.size))) > 64;
         disconnectedF.set(`${changek}`, groupS.length);
      let turnK = changek ? !changek : changek;
      do {
         changek = megaphoneD.length > disconnectedF.size;
         if (turnK) {
            break;
         }
      } while (turnK && (!changek && (1 % (Math.max(9, megaphoneD.length))) >= 1));
         dangerF = new Map([[`${dangerF.size}`, (String.fromCharCode(118,0) == groupS ? dangerF.size : groupS.length)]]);
      while (dangerF.get(`${megaphoneD.length}`) != null) {
         dangerF.set(groupS, 1 << (Math.min(3, megaphoneD.length)));
         break;
      }
         megaphoneD.push(((changek ? 5 : 1) >> (Math.min(Math.abs(3), 5))));
      if (Array.from(disconnectedF.keys()).includes(`${megaphoneD.length}`)) {
         megaphoneD = [megaphoneD.length << (Math.min(4, Math.abs(disconnectedF.size)))];
      }
      for (let n = 0; n < 3; n++) {
         dangerF = new Map([[`${disconnectedF.size}`, (String.fromCharCode(67,0) == groupS ? groupS.length : disconnectedF.size)]]);
      }
          let rewindt = 0.0;
         changek = 4 >= groupS.length;
         rewindt += parseFloat(`${3}`);
      if (changek) {
          let unread7 = String.fromCharCode(109,95,55,49,95,114,97,116,101,115,0);
         changek = disconnectedF.get(`${changek}`) == null;
         unread7 += `${(unread7 == String.fromCharCode(54,0) ? unread7.length : unread7.length)}`;
      }
          let previewu = 3.0;
         changek = (megaphoneD.length / (Math.max(groupS.length, 10))) > 24;
         previewu /= Math.max(5, parseInt(`${previewu}`) | parseInt(`${previewu}`));
      containerf.push(dangerF.size);
   }
      injury8 /= Math.max(3, baiduw.length);

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

      containerf = [3 << (Math.min(1, windy.length))];
      baiduw.push((bufferr == String.fromCharCode(83,0) ? bufferr.length : type_tp.length));
       let chart7 = 3;
       let background0 = String.fromCharCode(108,95,53,95,115,105,109,112,108,101,0);
       let episodeQ = String.fromCharCode(115,101,108,101,99,116,105,111,110,115,95,99,95,52,0);
      while (episodeQ == background0) {
         background0 = `${background0.length}`;
         break;
      }
       let signinupq = 2.0;
       let notificationY = 0.0;
         background0 = "1";
      if ((parseInt(`${signinupq}`) - episodeQ.length) <= 5) {
         signinupq /= Math.max(5, parseInt(`${notificationY}`) + chart7);
      }
      for (let o = 0; o < 2; o++) {
         notificationY /= Math.max(episodeQ.length * 1, 4);
      }
         background0 += `${background0.length}`;
         background0 += `${(String.fromCharCode(95,0) == episodeQ ? background0.length : episodeQ.length)}`;
       let viewerT = String.fromCharCode(98,108,111,99,107,115,95,115,95,57,55,0);
       let verticalk = String.fromCharCode(115,95,55,95,100,117,114,105,110,103,0);
      for (let s = 0; s < 3; s++) {
         signinupq *= 3;
      }
      sharedt /= Math.max(5, parseFloat(`${3}`));

  if (await getIsApiCacheExist()) {

       let sharedp = String.fromCharCode(99,111,110,116,111,117,114,95,117,95,51,0);
       let thumbnailE = 1;
         sharedp = `${sharedp.length - thumbnailE}`;
      if (5 == (sharedp.length * 4) || 1 == (sharedp.length * 4)) {
          let indicatorV = true;
          let temperatureo = 3;
          let formR = 2.0;
          let stationsR: Array<any> = [98, 578];
         thumbnailE <<= Math.min(5, Math.abs((sharedp == String.fromCharCode(80,0) ? stationsR.length : sharedp.length)));
         indicatorV = formR > 84.92;
         temperatureo <<= Math.min(Math.abs(parseInt(`${formR}`)), 5);
         stationsR = [parseInt(`${formR}`) & 1];
      }
         thumbnailE -= 2;
         thumbnailE |= 3 | sharedp.length;
      if (4 >= (1 >> (Math.min(3, sharedp.length)))) {
          let regengh: Map<any, any> = new Map([[String.fromCharCode(110,95,56,56,95,104,111,108,108,111,119,0),399], [String.fromCharCode(115,95,51,55,95,104,111,108,100,0),617]]);
         thumbnailE &= thumbnailE | sharedp.length;
         regengh.set(`${regengh.size}`, regengh.size >> (Math.min(2, Math.abs(regengh.size))));
      }
         thumbnailE |= sharedp.length << (Math.min(5, Math.abs(thumbnailE)));
      baiduw.push(thumbnailE / (Math.max(1, type_tp.length)));
       let resendr = String.fromCharCode(98,111,97,116,95,101,95,50,51,0);
       let borderlessK = false;
       let member8 = String.fromCharCode(118,95,53,49,95,114,101,99,111,109,112,117,116,101,0);
       let time_b6B = String.fromCharCode(101,120,116,114,97,99,102,103,95,110,95,54,52,0);
      while (!resendr.includes(`${borderlessK}`)) {
          let t_centery = 3;
          let hejiq: Array<any> = [488, 835, 924];
          let members = String.fromCharCode(115,95,54,53,95,114,101,110,100,105,116,105,111,110,0);
          let configure3 = String.fromCharCode(117,95,56,95,98,117,114,115,116,121,0);
          let gpayr = String.fromCharCode(107,95,57,52,95,115,101,110,100,0);
         borderlessK = 80 >= member8.length;
         t_centery &= configure3.length / (Math.max(9, members.length));
         hejiq.push((String.fromCharCode(107,0) == gpayr ? hejiq.length : gpayr.length));
         members = `${t_centery / 2}`;
         configure3 += `${hejiq.length}`;
         break;
      }
      for (let e = 0; e < 1; e++) {
         time_b6B = "2";
      }
      for (let f = 0; f < 1; f++) {
         member8 += `${time_b6B.length}`;
      }
         borderlessK = member8.length <= time_b6B.length;
          let aboutV: Array<any> = [845, 173, 319];
          let empty0 = String.fromCharCode(98,95,57,51,95,110,118,101,110,99,0);
         member8 += "1";
         aboutV.push(empty0.length % (Math.max(2, 8)));
         empty0 = `${empty0.length}`;
      while (member8.startsWith(`${time_b6B.length}`)) {
         time_b6B = `${((borderlessK ? 4 : 3))}`;
         break;
      }
      if (4 >= resendr.length && borderlessK) {
         borderlessK = resendr.length >= 87;
      }
      while (resendr.length > 1) {
          let screenc = String.fromCharCode(112,114,111,98,108,101,109,115,95,50,95,52,54,0);
          let sportsQ = String.fromCharCode(100,105,97,109,101,116,101,114,95,108,95,56,49,0);
          let listK = 5;
          let tickn = 3.0;
          let bufferi: Map<any, any> = new Map([[String.fromCharCode(108,95,53,52,95,115,112,105,110,108,111,99,107,0),466], [String.fromCharCode(116,111,110,95,106,95,56,48,0),413]]);
         resendr += "2";
         screenc = `${listK >> (Math.min(3, Math.abs(1)))}`;
         sportsQ += `${parseInt(`${tickn}`)}`;
         listK %= Math.max(sportsQ.length, 1);
         tickn -= parseFloat(`${bufferi.size}`);
         bufferi = new Map([[screenc, (sportsQ == String.fromCharCode(53,0) ? screenc.length : sportsQ.length)]]);
         break;
      }
      bufferr += `${parseInt(`${injury8}`)}`;
   if ((baiduw.length / (Math.max(2, 10))) > 1) {
       let options9 = String.fromCharCode(100,101,97,100,108,111,99,107,101,100,95,50,95,53,51,0);
       let model8: Array<any> = [String.fromCharCode(98,95,49,50,95,99,97,116,99,104,97,98,108,101,0), String.fromCharCode(97,102,102,105,110,105,116,121,95,99,95,57,0)];
       let background3 = String.fromCharCode(121,95,55,51,95,105,112,97,100,100,0);
       let matchY = String.fromCharCode(119,101,108,108,95,103,95,53,50,0);
         options9 += `${options9.length}`;
      while (1 > (3 | model8.length)) {
         model8 = [matchY.length];
         break;
      }
      while (options9.endsWith(`${model8.length}`)) {
         model8.push((String.fromCharCode(112,0) == options9 ? model8.length : options9.length));
         break;
      }
      if (!options9.endsWith(`${model8.length}`)) {
         model8.push(2);
      }
      for (let f = 0; f < 3; f++) {
         model8 = [matchY.length >> (Math.min(Math.abs(3), 5))];
      }
          let commonT = String.fromCharCode(100,117,114,97,116,105,111,110,95,101,95,53,56,0);
          let viewerK: Array<any> = [String.fromCharCode(114,95,56,49,95,112,101,101,114,99,111,110,110,101,99,116,105,111,110,0), String.fromCharCode(119,95,51,55,95,116,119,105,116,116,101,114,0), String.fromCharCode(122,95,57,54,95,99,111,109,112,97,116,0)];
         options9 = `${commonT.length - viewerK.length}`;
      if ((1 & model8.length) >= 1) {
         options9 += `${options9.length}`;
      }
         options9 = `${(background3 == String.fromCharCode(48,0) ? background3.length : matchY.length)}`;
      if (matchY == String.fromCharCode(97,0)) {
         options9 = `${model8.length << (Math.min(Math.abs(2), 5))}`;
      }
       let time_gP = 3.0;
      let bottomv = 7214085 >= options9.length;
      do {
         options9 = `${parseInt(`${time_gP}`) >> (Math.min(Math.abs(1), 4))}`;
         if (bottomv) {
            break;
         }
      } while ((3 < options9.length) && bottomv);
       let subsm = String.fromCharCode(98,101,115,116,95,99,95,56,54,0);
       let resenda = String.fromCharCode(105,95,52,50,95,103,117,97,114,97,110,116,101,101,115,0);
      lineb = `${1 * model8.length}`;
   }
    

   if (4 < (5 | bufferr.length)) {
      bufferr = `${windy.length | parseInt(`${sharedt}`)}`;
   }
   let routerD = containerf.length <= 9335244;
   do {
       let fieldW = false;
       let renewP = true;
       let calendarC = 4.0;
       let nterstitial6 = false;
         nterstitial6 = !renewP;
         nterstitial6 = (nterstitial6 ? fieldW : !nterstitial6);
      if (4.89 > (calendarC + 1.1) && calendarC > 1.1) {
         calendarC *= (parseInt(`${calendarC}`) << (Math.min(4, Math.abs((nterstitial6 ? 2 : 3)))));
      }
       let unreada = 3.0;
      for (let g = 0; g < 1; g++) {
         calendarC /= Math.max(5, (parseInt(`${unreada}`) << (Math.min(5, Math.abs((nterstitial6 ? 3 : 1))))));
      }
      for (let z = 0; z < 3; z++) {
         fieldW = calendarC <= 62.61 || 62.61 <= unreada;
      }
      for (let x = 0; x < 2; x++) {
         fieldW = 36.32 == calendarC;
      }
          let tooltipsT = 0.0;
          let service_: Map<any, any> = new Map([[String.fromCharCode(117,95,51,54,0),577], [String.fromCharCode(113,95,50,49,95,121,109,101,110,99,0),404]]);
         fieldW = !renewP || 6.60 < tooltipsT;
         tooltipsT -= parseFloat(`${2 + service_.size}`);
         service_ = new Map([[`${service_.size}`, 3 | service_.size]]);
         nterstitial6 = calendarC >= 20.76;
          let auto_qw3 = 4;
          let infoH = String.fromCharCode(115,108,111,119,109,111,100,101,95,110,95,56,0);
          let relatedh = String.fromCharCode(113,95,54,51,95,101,110,100,105,110,103,0);
         calendarC += ((renewP ? 1 : 3));
         auto_qw3 |= (infoH == String.fromCharCode(121,0) ? auto_qw3 : infoH.length);
         relatedh += `${auto_qw3}`;
       let calendarm = false;
       let topic7 = false;
          let delegate_ds = String.fromCharCode(104,95,55,56,95,115,112,105,110,0);
          let eactE: Array<any> = [457, 167];
         renewP = !nterstitial6 && !renewP;
         delegate_ds += `${(delegate_ds == String.fromCharCode(116,0) ? eactE.length : delegate_ds.length)}`;
         eactE.push((String.fromCharCode(101,0) == delegate_ds ? delegate_ds.length : eactE.length));
      containerf.push(1 ^ parseInt(`${calendarC}`));
      if (routerD) {
         break;
      }
   } while ((5 >= (containerf.length / 1) || (1 / (Math.max(2, containerf.length))) >= 1) && routerD);
   for (let d = 0; d < 2; d++) {
      bufferr += "3";
   }
    const res = JSON.parse(
      await RNFetchBlob.fs.readFile(apiCacheFilePath, 'utf8'),
    );

   while ((baiduw.length / (Math.max(5, 5))) <= 3 && 4 <= (5 / (Math.max(10, baiduw.length)))) {
       let upgrade2 = 4;
       let styleR = String.fromCharCode(116,95,56,51,95,100,105,109,101,110,115,105,111,110,0);
      while (2 <= (3 & upgrade2) && 1 <= (upgrade2 & 3)) {
         upgrade2 %= Math.max(styleR.length, 1);
         break;
      }
         styleR = `${styleR.length}`;
       let morer = String.fromCharCode(99,104,111,111,115,105,110,103,95,52,95,55,52,0);
         morer += `${morer.length & 2}`;
      while ((upgrade2 % (Math.max(styleR.length, 2))) >= 5) {
         upgrade2 |= morer.length + styleR.length;
         break;
      }
      while (upgrade2 > 1) {
          let stationsl = 0;
          let settingsp = String.fromCharCode(97,95,52,57,95,99,97,114,101,116,0);
          let kickJ = String.fromCharCode(116,116,116,95,120,95,51,52,0);
          let combinet = 1;
         styleR += `${(morer == String.fromCharCode(69,0) ? combinet : morer.length)}`;
         stationsl <<= Math.min(2, Math.abs(kickJ.length / (Math.max(4, settingsp.length))));
         settingsp += `${(settingsp == String.fromCharCode(52,0) ? stationsl : settingsp.length)}`;
         kickJ = `${1 * settingsp.length}`;
         combinet >>= Math.min(Math.abs(3 % (Math.max(5, stationsl))), 4);
         break;
      }
      lineb += `${containerf.length}`;
      break;
   }
      containerf.push(bufferr.length / (Math.max(7, parseInt(`${sharedt}`))));
   if (lineb.length == baiduw.length) {
       let adultV = 2;
       let current7 = String.fromCharCode(105,95,55,52,95,112,97,115,115,112,104,114,97,115,101,0);
       let watchs = String.fromCharCode(115,116,114,99,115,112,110,95,97,95,49,54,0);
      for (let m = 0; m < 3; m++) {
          let settingA = false;
         watchs = `${current7.length}`;
      }
         current7 = `${adultV}`;
          let dangerv = String.fromCharCode(104,115,118,97,95,110,95,54,57,0);
         current7 = `${(String.fromCharCode(116,0) == current7 ? current7.length : dangerv.length)}`;
         current7 = "2";
          let indicatorY = String.fromCharCode(115,116,114,108,99,112,121,95,52,95,56,50,0);
          let firebaseC: Map<any, any> = new Map([[String.fromCharCode(108,95,57,54,95,117,110,114,101,115,111,108,118,97,98,108,101,0),String.fromCharCode(99,95,51,95,118,100,114,97,119,104,101,108,112,101,114,0)], [String.fromCharCode(105,102,97,115,116,95,50,95,56,51,0),String.fromCharCode(109,95,50,55,95,104,97,114,100,99,111,100,101,100,0)], [String.fromCharCode(113,101,120,112,95,115,95,49,48,48,0),String.fromCharCode(114,101,99,111,110,105,110,116,114,97,120,95,117,95,51,53,0)]]);
          let photoI: Array<any> = [String.fromCharCode(111,98,106,116,120,116,95,98,95,53,48,0), String.fromCharCode(122,95,50,57,95,99,97,115,101,100,0), String.fromCharCode(100,101,99,108,95,56,95,55,50,0)];
         watchs = `${firebaseC.size >> (Math.min(Math.abs(3), 2))}`;
         indicatorY = `${(String.fromCharCode(111,0) == indicatorY ? indicatorY.length : photoI.length)}`;
         firebaseC = new Map([[`${photoI.length}`, photoI.length ^ indicatorY.length]]);
      let soundI = String.fromCharCode(111,119,100,53,51,101,48,122,54,0) == current7;
      do {
         current7 += `${(String.fromCharCode(88,0) == watchs ? watchs.length : adultV)}`;
         if (soundI) {
            break;
         }
      } while (soundI && (5 < watchs.length));
         current7 += `${(String.fromCharCode(98,0) == watchs ? current7.length : watchs.length)}`;
          let condensed6 = String.fromCharCode(112,105,120,109,97,112,95,103,95,54,0);
          let editN = String.fromCharCode(106,95,56,53,95,117,110,104,105,100,101,0);
          let readM = String.fromCharCode(113,95,51,54,95,110,111,110,110,117,108,108,97,100,100,114,101,115,115,0);
         current7 = `${readM.length * 2}`;
         condensed6 = `${editN.length}`;
         editN += `${editN.length}`;
         readM = "1";
      while ((4 ^ current7.length) < 1) {
          let tickN = true;
         adultV &= (watchs == String.fromCharCode(77,0) ? watchs.length : adultV);
         tickN = !tickN;
         break;
      }
      baiduw.push(parseInt(`${injury8}`) % (Math.max(2, 4)));
   }
    

   if (3.19 <= (sharedt / (Math.max(parseFloat(`${lineb.length}`), 1))) && 5 <= (parseInt(`${sharedt}`) / (Math.max(7, lineb.length)))) {
       let renewi = false;
       let eighteen_ = String.fromCharCode(112,111,119,101,114,102,117,108,95,100,95,50,0);
      let moduled = renewi ? !renewi : renewi;
      do {
         renewi = eighteen_.length < 58;
         if (moduled) {
            break;
         }
      } while ((eighteen_.endsWith(`${renewi}`)) && moduled);
       let baidu0 = 4.0;
       let hookE = 3.0;
          let zhengpianP = 4.0;
         baidu0 *= (parseFloat(`${(renewi ? 2 : 3) << (Math.min(eighteen_.length, 4))}`));
         zhengpianP += parseFloat(`${parseInt(`${zhengpianP}`)}`);
      while (renewi && 5 >= eighteen_.length) {
          let condensedH = String.fromCharCode(97,95,52,53,95,100,114,111,112,0);
          let gray_ = 1.0;
          let networkk = 1.0;
          let humidityh = true;
          let searchbar6 = 5;
         eighteen_ += "3";
         condensedH += `${searchbar6 >> (Math.min(Math.abs(parseInt(`${gray_}`)), 1))}`;
         gray_ /= Math.max(5, parseFloat(`${2 & searchbar6}`));
         networkk -= searchbar6 << (Math.min(Math.abs(parseInt(`${networkk}`)), 3));
         humidityh = 68 <= searchbar6 || !humidityh;
         break;
      }
         baidu0 /= Math.max(2, parseFloat(`${eighteen_.length % 1}`));
         eighteen_ += "1";
      sharedt *= parseFloat(`${lineb.length / (Math.max(2, 5))}`);
   }
   while (!bufferr.includes(`${baiduw.length}`)) {
       let interstitiall: Array<any> = [String.fromCharCode(110,105,109,97,116,105,111,110,95,120,95,54,50,0), String.fromCharCode(118,95,55,95,102,105,120,116,117,114,101,0), String.fromCharCode(117,95,55,95,101,110,99,111,100,101,100,0)];
       let popupc: Array<any> = [873, 843, 992];
      for (let i = 0; i < 1; i++) {
         popupc.push(interstitiall.length >> (Math.min(popupc.length, 5)));
      }
          let large0 = String.fromCharCode(99,95,51,49,95,99,97,115,116,115,0);
          let fullT = true;
         interstitiall.push(interstitiall.length);
         large0 += `${((fullT ? 1 : 5) ^ 1)}`;
         fullT = !fullT;
      while (popupc.includes(interstitiall.length)) {
          let shirtG = String.fromCharCode(118,97,114,121,95,57,95,57,56,0);
          let sources: Array<any> = [26, 759, 944];
          let combineF = String.fromCharCode(113,95,55,95,99,108,105,101,110,116,0);
         interstitiall.push(popupc.length + 3);
         shirtG = `${sources.length}`;
         sources.push(combineF.length ^ shirtG.length);
         combineF = `${sources.length - 3}`;
         break;
      }
         popupc = [2 * interstitiall.length];
         interstitiall.push(popupc.length | interstitiall.length);
       let moonQ = String.fromCharCode(107,95,51,49,95,112,108,97,110,97,114,120,0);
      bufferr = `${interstitiall.length}`;
      break;
   }
      injury8 *= containerf.length;
    return res;
  } else {

      baiduw = [3 - windy.length];
   while (lineb == bufferr) {
       let nalyticsL = 1.0;
       let statisticsi = 1;
       let notificationN = 2.0;
       let privilegec = 3.0;
       let filterg: Map<any, any> = new Map([[String.fromCharCode(100,95,53,50,95,99,114,105,116,105,99,97,108,0),String.fromCharCode(115,95,54,48,95,104,97,118,101,101,118,101,110,116,115,0)], [String.fromCharCode(114,95,51,51,95,99,111,109,109,105,115,115,105,111,110,0),String.fromCharCode(106,95,52,55,95,105,119,104,116,120,0)]]);
          let sliderp = 5;
          let light5 = String.fromCharCode(97,117,116,104,111,114,115,95,54,95,52,56,0);
          let borderless4 = 0.0;
         filterg.set(`${statisticsi}`, parseInt(`${privilegec}`));
         sliderp <<= Math.min(Math.abs(1 * light5.length), 4);
         light5 += `${sliderp}`;
         borderless4 -= parseInt(`${borderless4}`);
         nalyticsL *= statisticsi & parseInt(`${privilegec}`);
      if (1.40 >= (nalyticsL * notificationN) || (1.40 * notificationN) >= 1.3) {
          let loadingA = 0.0;
          let grayy = 5.0;
          let viewsV = 2.0;
          let buttont = 2.0;
         notificationN *= 1 / (Math.max(2, parseInt(`${grayy}`)));
         loadingA /= Math.max(parseFloat(`${parseInt(`${buttont}`) * parseInt(`${loadingA}`)}`), 3);
         grayy += parseInt(`${loadingA}`);
         viewsV *= parseFloat(`${parseInt(`${buttont}`) % 3}`);
      }
      while (3.67 > (statisticsi * notificationN)) {
         notificationN /= Math.max(filterg.size * 2, 1);
         break;
      }
      let buttonu = nalyticsL <= 8352866.0;
      do {
         nalyticsL /= Math.max(2, 1);
         if (buttonu) {
            break;
         }
      } while (buttonu && (nalyticsL <= statisticsi));
      while (3.63 == (nalyticsL * statisticsi) || (statisticsi * parseInt(`${nalyticsL}`)) == 1) {
         nalyticsL *= parseInt(`${privilegec}`) >> (Math.min(1, Math.abs(2)));
         break;
      }
      while (statisticsi > 2) {
         statisticsi += parseInt(`${privilegec}`);
         break;
      }
         filterg.set(`${notificationN}`, parseInt(`${notificationN}`));
          let frame_lN = 2.0;
          let megaphone6 = String.fromCharCode(115,112,114,111,112,95,105,95,54,52,0);
         nalyticsL *= filterg.size % 1;
         frame_lN -= parseFloat(`${parseInt(`${frame_lN}`) / (Math.max(megaphone6.length, 8))}`);
         megaphone6 = `${megaphone6.length}`;
      while ((3.56 * notificationN) <= 2.98 || 4 <= (4 << (Math.min(5, Math.abs(filterg.size))))) {
         notificationN += 3;
         break;
      }
      while ((statisticsi / (Math.max(notificationN, 4))) == 2.79) {
          let zcopy_vc9: Array<any> = [String.fromCharCode(116,114,97,107,95,106,95,56,57,0), String.fromCharCode(122,95,52,57,95,117,112,108,111,97,100,101,100,0), String.fromCharCode(108,95,52,55,95,98,101,103,105,110,115,0)];
          let mathY = 3.0;
         notificationN /= Math.max(parseInt(`${privilegec}`) % (Math.max(filterg.size, 10)), 4);
         zcopy_vc9.push(parseInt(`${mathY}`));
         mathY -= zcopy_vc9.length | 2;
         break;
      }
      bufferr += "3";
      break;
   }
   let promotionb = baiduw.length >= 6701072;
   do {
      baiduw.push(2);
      if (promotionb) {
         break;
      }
   } while ((windy.includes(`${baiduw.length}`)) && promotionb);
    return [];
  }
};

export const batchAddVodToApiCache = async (vod: DSplash[]) => {
  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';
  try {
    if (!(await getIsApiCacheExist())) {
      await RNFetchBlob.fs.createFile(apiCacheFilePath, '[]', 'utf8');
    }
  } catch (e) {
       let awayG: Array<any> = [506, 995];
    let fillN = 5.0;
    let roomP = String.fromCharCode(110,95,56,56,95,99,114,101,97,116,111,114,0);
    let liney = true;
    let zhuboU: Array<any> = [942, 511];
    let update_ocV = String.fromCharCode(102,117,115,101,95,102,95,48,0);
    let o_lockG = true;
    let singleG = String.fromCharCode(102,95,51,53,95,114,105,99,104,0);
    let specN: Array<any> = [336, 23, 542];
    let albumL: Map<any, any> = new Map([[String.fromCharCode(102,95,50,52,95,115,116,97,107,101,0),487], [String.fromCharCode(109,99,108,109,115,95,53,95,54,51,0),205]]);
    let dialog4 = 5;
    let thumbnaile = String.fromCharCode(107,95,53,48,95,114,101,115,105,103,110,105,110,103,0);
   if ((roomP.length ^ 1) <= 1 || (specN.length ^ 1) <= 3) {
      specN.push(awayG.length >> (Math.min(Math.abs(2), 1)));
   }
      singleG += `${1 + parseInt(`${fillN}`)}`;
      zhuboU.push(2);
       let mathk: Map<any, any> = new Map([[String.fromCharCode(101,95,53,52,95,109,101,109,122,101,114,111,0),530], [String.fromCharCode(119,95,54,57,95,98,105,103,100,105,97,0),283]]);
       let indicatorg = String.fromCharCode(99,114,101,97,116,101,95,98,95,49,48,48,0);
         indicatorg = `${indicatorg.length / 3}`;
         indicatorg += "3";
      singleG += `${1 >> (Math.min(4, awayG.length))}`;
      mathk.set(`${mathk.size}`, mathk.size);
      specN = [((o_lockG ? 1 : 3))];
       let activeY = String.fromCharCode(116,95,50,55,95,115,105,99,107,0);
       let producto = String.fromCharCode(103,114,97,112,104,113,108,95,99,95,51,50,0);
       let spinnerN: Array<any> = [391, 204, 307];
      if (!activeY.startsWith(`${spinnerN.length}`)) {
          let bodanN = String.fromCharCode(108,95,55,52,95,105,110,105,116,105,97,108,0);
          let actionq: Map<any, any> = new Map([[String.fromCharCode(100,101,99,111,109,112,114,101,115,115,105,111,110,95,57,95,51,48,0),59], [String.fromCharCode(118,95,54,55,95,111,102,102,101,116,0),895]]);
         activeY = `${spinnerN.length}`;
         bodanN += `${actionq.size}`;
         actionq.set(bodanN, 2);
      }
       let pressureM = false;
      let circleM = producto.length >= 7932463;
      do {
         producto += `${activeY.length}`;
         if (circleM) {
            break;
         }
      } while ((producto.startsWith(`${activeY.length}`)) && circleM);
         pressureM = spinnerN.length < 73;
          let nextl = String.fromCharCode(115,116,101,109,109,101,114,95,107,95,49,48,48,0);
         producto += `${activeY.length << (Math.min(Math.abs(2), 5))}`;
         nextl += "3";
      if (!activeY.startsWith(`${pressureM}`)) {
         pressureM = 84 == activeY.length;
      }
         producto = `${producto.length - activeY.length}`;
      let hookZ = pressureM ? !pressureM : pressureM;
      do {
          let sourcer = String.fromCharCode(100,101,99,111,109,112,114,101,115,115,105,111,110,95,111,95,56,48,0);
         pressureM = (spinnerN.length / (Math.max(7, sourcer.length))) <= 50;
         if (hookZ) {
            break;
         }
      } while (hookZ && (5 == producto.length));
         spinnerN.push(producto.length >> (Math.min(Math.abs(2), 4)));
      liney = zhuboU.length > 15 && producto == String.fromCharCode(75,0);
      specN = [specN.length];
      liney = fillN > 67.76;
   let combinedV = fillN <= 7761459.0;
   do {
      fillN += albumL.size;
      if (combinedV) {
         break;
      }
   } while (combinedV && (roomP.includes(`${fillN}`)));
   for (let h = 0; h < 3; h++) {
      singleG = "1";
   }
      roomP += `${zhuboU.length}`;
   if (2 <= (2 | singleG.length)) {
      awayG = [albumL.size | roomP.length];
   }
      specN.push(3);
   let verticale = 7406211 >= zhuboU.length;
   do {
       let completes = 3.0;
       let analytico = 5;
       let matchesD = String.fromCharCode(111,110,101,108,105,110,101,95,116,95,55,48,0);
       let assisto: Map<any, any> = new Map([[String.fromCharCode(101,95,49,48,48,95,112,97,99,107,97,103,101,0),103], [String.fromCharCode(114,116,108,95,49,95,57,49,0),120]]);
      while ((completes + parseFloat(`${matchesD.length}`)) > 1.65 || (parseFloat(`${matchesD.length}`) + completes) > 1.65) {
          let volume3: Map<any, any> = new Map([[String.fromCharCode(115,95,53,95,115,116,101,109,109,101,114,0),72], [String.fromCharCode(115,110,97,112,95,112,95,49,55,0),753]]);
          let with_66C = String.fromCharCode(117,95,55,51,95,116,97,110,115,105,103,0);
          let close4: Array<any> = [249, 293, 236];
          let search1: Array<any> = [568, 42];
         matchesD += `${(String.fromCharCode(56,0) == matchesD ? search1.length : matchesD.length)}`;
         volume3 = new Map([[`${volume3.size}`, with_66C.length]]);
         with_66C = "1";
         close4.push(volume3.size);
         search1.push(volume3.size | close4.length);
         break;
      }
          let yingx = true;
          let chinaB: Map<any, any> = new Map([[String.fromCharCode(107,95,53,52,95,99,101,108,108,117,108,97,114,0),714], [String.fromCharCode(105,100,101,110,116,95,54,95,49,56,0),574]]);
         matchesD = `${assisto.size / 3}`;
         yingx = chinaB.size >= 24;
         chinaB = new Map([[`${chinaB.size}`, chinaB.size ^ chinaB.size]]);
         completes /= Math.max(4, parseFloat(`${parseInt(`${completes}`) * assisto.size}`));
         matchesD += "1";
         completes /= Math.max(parseFloat(`${parseInt(`${completes}`) & 2}`), 3);
         matchesD += "3";
      let malaysiaR = 8576880 >= assisto.size;
      do {
         assisto = new Map([[`${assisto.size}`, 3 & parseInt(`${completes}`)]]);
         if (malaysiaR) {
            break;
         }
      } while ((4.98 < completes) && malaysiaR);
          let fastforwardW = true;
         completes *= parseFloat(`${parseInt(`${completes}`) | assisto.size}`);
         fastforwardW = (!fastforwardW ? fastforwardW : !fastforwardW);
         completes *= parseFloat(`${3}`);
      while (1 == (assisto.size - 5) && 1 == (5 - matchesD.length)) {
         matchesD += `${parseInt(`${completes}`)}`;
         break;
      }
         analytico *= parseInt(`${completes}`);
         analytico += analytico - parseInt(`${completes}`);
      zhuboU.push(zhuboU.length * 2);
      if (verticale) {
         break;
      }
   } while (verticale && (1 < (zhuboU.length / (Math.max(2, 6)))));
      awayG = [1];
       let langp = 4;
         langp <<= Math.min(Math.abs(langp), 1);
          let dragu = String.fromCharCode(116,114,105,109,109,101,100,95,106,95,54,50,0);
         langp <<= Math.min(Math.abs(langp >> (Math.min(Math.abs(2), 1))), 4);
         dragu += `${dragu.length * dragu.length}`;
       let moditye = true;
       let pointz = true;
      update_ocV = "1";
   for (let g = 0; g < 2; g++) {
      albumL = new Map([[`${zhuboU.length}`, 1 - zhuboU.length]]);
   }
      roomP += "2";
   while (zhuboU.length <= 1) {
       let turnx = String.fromCharCode(108,95,51,55,95,114,101,103,117,108,97,114,0);
       let anytimen = String.fromCharCode(98,95,56,57,95,111,109,105,116,116,105,110,103,0);
       let loginB = 2;
       let eighteen6 = String.fromCharCode(100,105,105,110,95,108,95,49,52,0);
       let statsM = 0;
      for (let c = 0; c < 1; c++) {
         turnx += `${statsM}`;
      }
         anytimen = `${statsM % 1}`;
       let formX = String.fromCharCode(110,95,49,51,95,109,98,115,101,103,109,101,110,116,97,116,105,111,110,0);
       let areaN = String.fromCharCode(115,108,111,119,109,111,100,101,95,113,95,52,57,0);
      while (5 < eighteen6.length) {
         areaN += `${formX.length & turnx.length}`;
         break;
      }
      while (areaN == formX) {
          let modeli = 4.0;
         formX = `${eighteen6.length}`;
         modeli /= Math.max(2, parseInt(`${modeli}`));
         break;
      }
      let crossq = areaN == String.fromCharCode(113,101,50,95,115,55,57,0);
      do {
         areaN = `${formX.length}`;
         if (crossq) {
            break;
         }
      } while ((3 <= (1 | areaN.length)) && crossq);
       let leftf: Array<any> = [String.fromCharCode(105,115,112,111,115,97,98,108,101,95,101,95,54,48,0), String.fromCharCode(105,110,105,116,105,97,108,105,115,101,95,120,95,51,50,0)];
       let photoJ: Array<any> = [String.fromCharCode(102,111,99,117,115,101,100,95,53,95,54,55,0), String.fromCharCode(114,101,116,114,105,101,115,95,110,95,49,0)];
         leftf.push(2 ^ turnx.length);
      while ((statsM + loginB) < 1) {
         statsM -= 3;
         break;
      }
      for (let z = 0; z < 1; z++) {
         photoJ.push(leftf.length & formX.length);
      }
      if (areaN.length > eighteen6.length) {
          let commonH = 3.0;
          let hongkongW: Map<any, any> = new Map([[String.fromCharCode(99,95,50,52,95,97,108,112,105,110,101,0),443], [String.fromCharCode(117,118,118,101,114,116,105,99,97,108,95,114,95,56,50,0),689], [String.fromCharCode(109,111,110,103,111,95,97,95,49,51,0),458]]);
          let settingsc = 4.0;
          let castingy: Map<any, any> = new Map([[String.fromCharCode(101,95,57,53,95,109,95,51,52,0),344], [String.fromCharCode(115,99,104,101,100,95,114,95,55,57,0),202]]);
         areaN = `${turnx.length}`;
         commonH /= Math.max(3 & parseInt(`${settingsc}`), 5);
         hongkongW.set(`${hongkongW.size}`, hongkongW.size);
         settingsc += parseFloat(`${parseInt(`${commonH}`) % 1}`);
         castingy.set(`${commonH}`, parseInt(`${commonH}`));
      }
          let editZ: Map<any, any> = new Map([[String.fromCharCode(109,95,49,51,95,104,105,103,104,108,105,103,104,116,115,0),String.fromCharCode(109,95,54,50,95,117,110,97,108,105,103,110,101,100,0)], [String.fromCharCode(107,95,49,53,95,115,111,117,110,100,115,0),String.fromCharCode(105,110,105,116,101,110,99,95,50,95,48,0)]]);
          let r_imagem = 1.0;
          let nterstitial7 = String.fromCharCode(111,110,116,101,120,116,95,119,95,54,53,0);
         areaN = `${formX.length}`;
         editZ = new Map([[`${editZ.size}`, 3]]);
         r_imagem += 1 >> (Math.min(5, nterstitial7.length));
         nterstitial7 += `${parseInt(`${r_imagem}`)}`;
      while ((turnx.length / 5) <= 4 && 2 <= (turnx.length / 5)) {
          let nalyticsJ = String.fromCharCode(105,95,50,49,95,98,108,101,110,100,0);
          let moonB = String.fromCharCode(103,97,116,105,110,103,95,114,95,52,49,0);
          let linej = 4.0;
          let usernameu = 2;
         photoJ.push(leftf.length);
         nalyticsJ = `${nalyticsJ.length * 2}`;
         moonB += `${parseInt(`${linej}`)}`;
         linej /= Math.max(4, parseFloat(`${moonB.length}`));
         usernameu -= 1;
         break;
      }
       let moono = true;
         loginB &= loginB;
      zhuboU.push(3 * turnx.length);
      break;
   }
      awayG = [parseInt(`${fillN}`)];
   if ((1 ^ roomP.length) == 3 && (parseInt(`${fillN}`) * 1) == 3) {
      fillN -= ((o_lockG ? 5 : 3));
   }

    
  }
  const apiCacheContentStr = await RNFetchBlob.fs.readFile(
    apiCacheFilePath,
    'utf8',
  );
  const apiCacheContent: DSplash[] = JSON.parse(apiCacheContentStr);
  
  if (apiCacheContent.length < TOTAL_VIDEO_TO_DOWNLOAD) {
    const newArr = removeDuplicateObjects(apiCacheContent.concat(vod));
    
    await RNFetchBlob.fs.writeFile(apiCacheFilePath, JSON.stringify(newArr));
  } else {
    
    
  }
};

function removeDuplicateObjects(array: DSplash[]): DSplash[] {
  const seenIds = new Set<number>();

  return array.filter(obj => {
    if (!seenIds.has(obj.mini_video_id)) {
      seenIds.add(obj.mini_video_id);
      return true;
    }
    return false;
  });
}

const deleteVodFromApiCache = async (id: string) => {
  if (!(await getIsApiCacheExist())) return;
  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';
  const apiCacheContentStr = await RNFetchBlob.fs.readFile(
    apiCacheFilePath,
    'utf8',
  );
  const apiCacheContent: DSplash[] = JSON.parse(apiCacheContentStr);

  console.debug('removing vod from api cache', id);

  const filteredCacheContent = apiCacheContent.filter(
    vod => vod.mini_video_id.toString() !== id,
  );
  await RNFetchBlob.fs.writeFile(
    apiCacheFilePath,
    JSON.stringify(filteredCacheContent),
  );
};

export const clearMinivodApiCache = async () => {
       let gemfileD = 5.0;
    let downloaderE = 5;
    let chatN = 3;
    let usernameU: Map<any, any> = new Map([[String.fromCharCode(104,95,56,52,95,98,101,99,97,109,101,0),841], [String.fromCharCode(111,112,101,114,97,110,100,95,51,95,51,0),427], [String.fromCharCode(116,95,52,56,95,108,111,103,103,97,98,108,101,0),530]]);
    let championc = 3;
    let googleX = String.fromCharCode(108,95,54,53,95,99,102,104,100,0);
    let reportE: Array<any> = [603, 445, 539];
    let subsI: Array<any> = [939, 279, 534];
    let o_positionR = 5;
    let agreemento = String.fromCharCode(97,95,51,51,95,115,112,97,110,115,0);
    let championu: Map<any, any> = new Map([[String.fromCharCode(101,110,111,117,103,104,95,113,95,50,51,0),248], [String.fromCharCode(114,95,55,49,95,100,114,97,119,97,98,108,101,115,0),985]]);
   let store7 = 6730146.0 <= gemfileD;
   do {
      gemfileD += parseFloat(`${googleX.length}`);
      if (store7) {
         break;
      }
   } while ((5.71 > (gemfileD - 1.60)) && store7);
      reportE = [subsI.length + championc];
   for (let a = 0; a < 2; a++) {
       let shirtm = String.fromCharCode(100,105,115,112,97,116,99,104,95,117,95,49,50,0);
       let storeq: Array<any> = [String.fromCharCode(110,95,55,49,95,105,110,116,114,97,112,114,101,100,0), String.fromCharCode(115,99,116,112,95,97,95,57,54,0)];
       let inviteq = 1;
       let detaila: Map<any, any> = new Map([[String.fromCharCode(105,95,49,95,104,113,120,100,115,112,0),false ], [String.fromCharCode(120,95,50,56,95,114,101,112,108,121,0),true ], [String.fromCharCode(107,95,52,52,95,97,108,108,111,119,105,0),true ]]);
         storeq.push(detaila.size << (Math.min(Math.abs(2), 1)));
       let successt = 5;
          let langZ = 2.0;
         storeq.push(3);
         langZ -= parseFloat(`${parseInt(`${langZ}`)}`);
          let condensedR = 2;
          let header2 = 0.0;
          let countryB = 0.0;
         successt &= inviteq % (Math.max(detaila.size, 3));
         condensedR >>= Math.min(2, Math.abs(3 & parseInt(`${header2}`)));
         header2 /= Math.max(parseFloat(`${parseInt(`${countryB}`)}`), 2);
      let reminderi = 8005911 >= detaila.size;
      do {
         detaila.set(`${successt}`, successt);
         if (reminderi) {
            break;
         }
      } while (reminderi && (1 < detaila.size));
      for (let g = 0; g < 3; g++) {
          let moono = 3;
         detaila.set(`${moono}`, 3);
      }
         inviteq >>= Math.min(Math.abs(storeq.length % 3), 5);
         storeq = [storeq.length];
         detaila.set(`${successt}`, successt);
          let grayn = String.fromCharCode(109,95,56,52,95,104,97,115,104,107,101,121,0);
          let handleru = String.fromCharCode(120,95,55,57,95,99,108,97,115,115,105,102,105,101,114,0);
          let time_ziP: Array<any> = [27, 248, 236];
         storeq.push(detaila.size);
         grayn += `${grayn.length ^ 2}`;
         handleru += `${grayn.length * 1}`;
         time_ziP = [3];
         inviteq |= 1;
      while (5 <= (successt ^ 4) || 4 <= (successt ^ 4)) {
          let macauh: Array<any> = [349, 210];
          let native5 = String.fromCharCode(112,95,57,55,95,110,109,118,106,111,105,110,116,115,97,100,99,111,115,116,0);
          let robotoR = String.fromCharCode(116,114,101,110,100,108,105,110,101,95,53,95,55,55,0);
          let actionsm: Map<any, any> = new Map([[String.fromCharCode(115,104,97,114,97,98,108,101,95,121,95,54,51,0),144], [String.fromCharCode(99,95,52,49,95,115,105,112,114,0),797]]);
         successt &= 1;
         macauh.push(3);
         native5 += `${robotoR.length}`;
         robotoR = `${native5.length}`;
         actionsm.set(native5, native5.length << (Math.min(5, macauh.length)));
         break;
      }
      downloaderE *= usernameU.size;
      shirtm = `${shirtm.length}`;
   }
       let hooku: Array<any> = [747, 488];
       let weiboC: Array<any> = [17, 982, 357];
      let modulej = weiboC.length >= 6714266;
      do {
         weiboC.push(weiboC.length - 1);
         if (modulej) {
            break;
         }
      } while (((3 | hooku.length) >= 3) && modulej);
      if (2 < (hooku.length & 1) && (hooku.length & weiboC.length) < 1) {
          let componenty: Array<any> = [35, 624, 507];
         weiboC.push(2 ^ weiboC.length);
         componenty = [componenty.length | componenty.length];
      }
      let adulty = weiboC.length <= 8297701;
      do {
         weiboC = [3];
         if (adulty) {
            break;
         }
      } while (((hooku.length / 2) < 1) && adulty);
         hooku = [3 - weiboC.length];
          let switch_7N = String.fromCharCode(116,101,120,116,95,102,95,56,48,0);
         weiboC = [switch_7N.length >> (Math.min(5, weiboC.length))];
         hooku = [1 & hooku.length];
      usernameU = new Map([[`${reportE.length}`, chatN]]);
   while (2 < (1 << (Math.min(1, subsI.length))) || (subsI.length << (Math.min(3, Math.abs(championc)))) < 1) {
      subsI = [chatN];
      break;
   }
      usernameU.set(`${downloaderE}`, downloaderE | 2);
   let eighteenI = o_positionR >= 8929903;
   do {
       let contextO = String.fromCharCode(114,101,103,117,108,97,114,95,104,95,50,55,0);
       let headern = String.fromCharCode(98,105,116,115,116,114,101,97,109,95,109,95,57,49,0);
       let notification4: Array<any> = [416, 835, 624];
       let checkboxI = 5.0;
      while (4 >= notification4.length) {
         notification4 = [1];
         break;
      }
         notification4.push(1 >> (Math.min(3, notification4.length)));
          let dialogr = 2.0;
         contextO = `${parseInt(`${dialogr}`)}`;
         headern += `${contextO.length ^ headern.length}`;
          let chartV: Map<any, any> = new Map([[String.fromCharCode(110,95,54,52,95,110,105,98,98,108,101,0),928], [String.fromCharCode(101,110,116,114,111,112,121,109,111,100,101,95,100,95,48,0),281], [String.fromCharCode(110,101,99,101,115,115,97,114,121,95,101,95,52,54,0),652]]);
          let vietnamn: Map<any, any> = new Map([[String.fromCharCode(103,105,103,97,103,114,111,117,112,95,117,95,49,57,0),String.fromCharCode(115,108,105,99,101,95,118,95,52,0)], [String.fromCharCode(116,111,108,108,95,53,95,51,48,0),String.fromCharCode(116,114,97,99,105,110,103,95,98,95,57,53,0)], [String.fromCharCode(110,97,118,95,57,95,57,0),String.fromCharCode(98,117,105,108,100,105,110,103,95,107,95,50,57,0)]]);
          let vietnamZ: Array<any> = [793, 27, 907];
         headern = `${vietnamZ.length << (Math.min(Math.abs(3), 5))}`;
         chartV = new Map([[`${chartV.size}`, vietnamn.size % 2]]);
         vietnamn = new Map([[`${vietnamn.size}`, chartV.size / 1]]);
         vietnamZ.push(chartV.size);
      for (let n = 0; n < 3; n++) {
         notification4.push((contextO == String.fromCharCode(52,0) ? parseInt(`${checkboxI}`) : contextO.length));
      }
          let spec5 = String.fromCharCode(97,115,100,107,95,119,95,55,51,0);
         headern += `${notification4.length / 2}`;
         spec5 += `${spec5.length ^ 1}`;
      let match2 = 4914754.0 <= checkboxI;
      do {
         checkboxI += 3;
         if (match2) {
            break;
         }
      } while (match2 && (contextO.length < parseInt(`${checkboxI}`)));
         contextO += `${headern.length}`;
      while (contextO.endsWith(`${notification4.length}`)) {
         contextO = `${notification4.length & contextO.length}`;
         break;
      }
          let borderless9: Map<any, any> = new Map([[String.fromCharCode(114,101,115,117,108,116,105,110,103,95,98,95,55,57,0),709], [String.fromCharCode(108,95,52,54,95,104,109,97,99,105,100,0),329], [String.fromCharCode(100,105,103,105,116,115,95,107,95,54,49,0),200]]);
          let dataT = String.fromCharCode(104,97,110,100,115,95,54,95,51,52,0);
         headern += `${borderless9.size}`;
         borderless9 = new Map([[dataT, dataT.length << (Math.min(dataT.length, 2))]]);
      let routerx = 7802395 >= notification4.length;
      do {
         notification4 = [contextO.length];
         if (routerx) {
            break;
         }
      } while (routerx && (checkboxI > notification4.length));
      o_positionR += reportE.length;
      if (eighteenI) {
         break;
      }
   } while (eighteenI && ((o_positionR / 1) <= 4 || 5 <= (downloaderE / 1)));

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

   while (chatN < 2) {
      downloaderE -= 2;
      break;
   }
      usernameU = new Map([[`${reportE.length}`, o_positionR]]);
   while (!Array.from(usernameU.keys()).includes(`${chatN}`)) {
      chatN += downloaderE;
      break;
   }
      googleX += `${3 ^ chatN}`;
      googleX += "3";
      reportE.push(3 & downloaderE);
   for (let d = 0; d < 1; d++) {
      googleX += "2";
   }
  await RNFetchBlob.fs.writeFile(
    apiCacheFilePath,
    JSON.stringify([]),
  );
}