import {ttAnimationSource} from '@type/tt_line_borderless';
import {DOWNLOAD_BATCH_SIZE, TOTAL_VIDEO_TO_DOWNLOAD} from '@utility/tt_trophy_cross';
import chunk from 'lodash/chunk';
import RNFetchBlob from 'rn-fetch-blob';

async function downloadVod(vod: ttAnimationSource) {
  if (vod.is_ads) return 
  const fileLocation =
    RNFetchBlob.fs.dirs.DocumentDir + `/videocache/${vod.mini_video_id}.mp4`;
  const temp =
    RNFetchBlob.fs.dirs.DocumentDir + `/partial/${vod.mini_video_id}`;
  const cacheFolder = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';

  if (await RNFetchBlob.fs.exists(cacheFolder)) {
    if (
      (await RNFetchBlob.fs.ls(cacheFolder)).length >= TOTAL_VIDEO_TO_DOWNLOAD
    ) {
      
      return;
    }
  } else {
    try {
      await RNFetchBlob.fs.mkdir(cacheFolder);
    } catch (e) {
       let thumbnail_: Array<any> = [473, 338, 41];
    let mappingF = String.fromCharCode(109,117,116,97,98,105,108,105,116,121,0);
    let full2: Array<any> = [String.fromCharCode(116,114,117,110,107,0), String.fromCharCode(102,105,108,108,0), String.fromCharCode(112,114,101,99,111,109,112,0)];
    let stepS: Array<any> = [575, 774, 713];
    let androidM = String.fromCharCode(104,119,97,99,99,101,108,115,0);
    let tail_ = String.fromCharCode(112,114,101,118,105,111,117,115,108,121,0);
    let searchbarV = String.fromCharCode(115,116,111,114,121,98,111,97,114,100,0);
    let pressure6 = 0.0;
    let spinnerF: Map<any, any> = new Map([[String.fromCharCode(97,110,110,101,120,0),410], [String.fromCharCode(97,116,111,105,95,117,95,57,49,0),436], [String.fromCharCode(109,115,109,112,101,103,100,97,116,97,0),564]]);
    let nativeexu = 2;
      androidM = `${thumbnail_.length}`;
   for (let t = 0; t < 1; t++) {
       let championp: Array<any> = [741, 242, 985];
      let bottomx = championp.length <= 9258166;
      do {
         championp.push(championp.length / 1);
         if (bottomx) {
            break;
         }
      } while ((3 <= (4 % (Math.max(5, championp.length))) || (championp.length % (Math.max(championp.length, 6))) <= 4) && bottomx);
      while (4 >= (championp.length >> (Math.min(5, championp.length)))) {
          let default_w7T: Map<any, any> = new Map([[String.fromCharCode(112,117,98,108,105,115,104,101,100,0),843], [String.fromCharCode(116,114,97,110,115,117,112,112,0),9]]);
          let emptyS = String.fromCharCode(104,105,115,116,111,103,114,97,109,0);
         championp.push(2);
         default_w7T = new Map([[`${default_w7T.size}`, default_w7T.size * emptyS.length]]);
         emptyS = `${default_w7T.size}`;
         break;
      }
          let userU = String.fromCharCode(115,101,112,97,114,97,116,111,114,0);
         championp.push(3);
         userU += `${userU.length}`;
      stepS.push(championp.length);
   }
       let thumbnail8 = 5;
      let overlayQ = thumbnail8 <= 6116469;
      do {
         thumbnail8 %= Math.max(2, 2 << (Math.min(4, Math.abs(thumbnail8))));
         if (overlayQ) {
            break;
         }
      } while ((2 < (thumbnail8 >> (Math.min(3, Math.abs(thumbnail8))))) && overlayQ);
         thumbnail8 &= thumbnail8 | thumbnail8;
       let sportN = false;
       let successn = true;
      searchbarV = `${tail_.length * 1}`;
   for (let h = 0; h < 1; h++) {
      thumbnail_ = [1];
   }
   if (2 >= androidM.length) {
       let guideg = String.fromCharCode(115,105,103,110,0);
       let giftE = String.fromCharCode(109,112,101,103,118,105,100,101,111,100,115,112,0);
       let playV = 4.0;
       let sigmobJ = String.fromCharCode(117,115,101,97,103,101,0);
      while (guideg.length >= giftE.length) {
         giftE += `${1 | sigmobJ.length}`;
         break;
      }
         giftE = `${(String.fromCharCode(97,0) == sigmobJ ? sigmobJ.length : guideg.length)}`;
      if (!guideg.startsWith(`${playV}`)) {
          let refreshB = true;
          let reducer5 = String.fromCharCode(111,112,101,114,97,116,111,114,0);
         guideg = `${((refreshB ? 3 : 5) << (Math.min(Math.abs(2), 4)))}`;
         refreshB = reducer5.length == reducer5.length;
      }
      if (sigmobJ.endsWith(`${playV}`)) {
          let catalogf = String.fromCharCode(115,108,111,119,0);
          let userg: Map<any, any> = new Map([[String.fromCharCode(108,95,49,54,95,99,111,110,115,116,97,110,116,0),true ], [String.fromCharCode(118,95,54,95,112,114,101,118,105,101,119,105,110,103,0),false ], [String.fromCharCode(115,112,97,99,105,110,103,0),false ]]);
         sigmobJ = `${(catalogf == String.fromCharCode(88,0) ? userg.size : catalogf.length)}`;
      }
      let orientationv = 6229530.0 <= playV;
      do {
         playV /= Math.max(2, (String.fromCharCode(115,0) == guideg ? sigmobJ.length : guideg.length));
         if (orientationv) {
            break;
         }
      } while (((5 ^ giftE.length) <= 4) && orientationv);
      while (3 > guideg.length) {
         giftE = `${(String.fromCharCode(49,0) == giftE ? parseInt(`${playV}`) : giftE.length)}`;
         break;
      }
      if (!giftE.endsWith(`${playV}`)) {
          let circleR = false;
          let stationn = String.fromCharCode(98,102,108,121,0);
          let readM = 2;
          let aboutq = String.fromCharCode(116,105,108,101,115,0);
          let selectW = 0.0;
         playV += (aboutq == String.fromCharCode(100,0) ? readM : aboutq.length);
         circleR = !circleR;
         stationn += `${parseInt(`${selectW}`) * 3}`;
         readM <<= Math.min(5, Math.abs(3));
         selectW += (parseInt(`${selectW}`) << (Math.min(4, Math.abs((circleR ? 1 : 2)))));
      }
      while (3 > sigmobJ.length) {
         sigmobJ += `${parseInt(`${playV}`)}`;
         break;
      }
      if (1.64 == (playV / 4.19)) {
         playV /= Math.max(5, guideg.length ^ 3);
      }
      while (playV == giftE.length) {
         playV /= Math.max(guideg.length - parseInt(`${playV}`), 1);
         break;
      }
      while (5 > (3 * guideg.length)) {
         guideg += `${giftE.length - guideg.length}`;
         break;
      }
      while ((5.59 + playV) <= 5.2 || 2.92 <= (5.59 + playV)) {
         playV += sigmobJ.length & guideg.length;
         break;
      }
      androidM = `${3 | sigmobJ.length}`;
   }
   for (let n = 0; n < 2; n++) {
      stepS.push(stepS.length / 3);
   }
   if (mappingF.includes(`${full2.length}`)) {
      mappingF += "1";
   }
       let minimizex = 3.0;
      let eactd = 4995563.0 <= minimizex;
      do {
         minimizex -= parseInt(`${minimizex}`) << (Math.min(2, Math.abs(2)));
         if (eactd) {
            break;
         }
      } while (((minimizex / (Math.max(1, minimizex))) == 5.19) && eactd);
      let mbridgeX = minimizex >= 5903149.0;
      do {
         minimizex -= 2;
         if (mbridgeX) {
            break;
         }
      } while (mbridgeX && ((minimizex + minimizex) < 5.88 && (minimizex + 5.88) < 4.100));
      while ((1.3 - minimizex) < 3.100 && (1.3 - minimizex) < 3.28) {
         minimizex *= parseInt(`${minimizex}`) & 2;
         break;
      }
      androidM = `${full2.length}`;
   while (1 < tail_.length) {
      stepS.push(mappingF.length);
      break;
   }
      full2 = [(tail_ == String.fromCharCode(75,0) ? tail_.length : searchbarV.length)];
      androidM += "3";
      full2.push(mappingF.length / (Math.max(2, 5)));
   for (let v = 0; v < 1; v++) {
      androidM += `${full2.length / (Math.max(9, parseInt(`${pressure6}`)))}`;
   }
      thumbnail_ = [2 * searchbarV.length];

      
    }
  }

  const fileExist = RNFetchBlob.fs.exists(fileLocation);

  return fileExist.then(exist => {
    if (exist) {
       let dataJ: Map<any, any> = new Map([[String.fromCharCode(97,114,114,97,110,103,101,109,101,110,116,0),false ], [String.fromCharCode(112,97,115,112,0),false ], [String.fromCharCode(115,101,110,115,105,116,105,118,105,116,121,0),false ]]);
    let heartV = true;
    let otherF = true;
    let adultB = String.fromCharCode(100,111,103,0);
    let private_bp = 2.0;
    let placeholderS: Map<any, any> = new Map([[String.fromCharCode(101,100,105,116,105,110,103,0),686], [String.fromCharCode(97,117,100,105,111,100,115,112,0),621], [String.fromCharCode(99,104,105,108,100,0),222]]);
    let gemfile5 = 0.0;
    let matchS = String.fromCharCode(116,105,116,110,116,0);
    let signinupO: Array<any> = [76, 400];
    let unselected9 = String.fromCharCode(111,112,117,115,116,97,98,95,116,95,50,57,0);
    let previewi = String.fromCharCode(99,103,117,0);
    let statisticsu: Map<any, any> = new Map([[String.fromCharCode(116,105,116,108,116,101,0),815], [String.fromCharCode(97,100,112,99,109,0),444], [String.fromCharCode(98,111,111,107,109,97,114,107,0),215]]);
    let modity5 = 2;
       let matchT: Array<any> = [65, 215, 173];
       let downloadG = String.fromCharCode(115,112,97,110,0);
       let mapping_ = String.fromCharCode(114,111,103,114,97,109,0);
          let aboutD = String.fromCharCode(112,97,115,116,101,98,111,97,114,100,0);
          let appsx = 1;
          let photoN: Map<any, any> = new Map([[String.fromCharCode(108,111,115,115,121,0),348], [String.fromCharCode(111,112,116,105,111,110,0),705], [String.fromCharCode(107,95,54,56,95,112,97,105,114,105,110,103,0),220]]);
         downloadG = `${appsx - photoN.size}`;
         aboutD = `${aboutD.length}`;
         appsx <<= Math.min(1, aboutD.length);
      if ((matchT.length & mapping_.length) < 1 || (1 & mapping_.length) < 3) {
         mapping_ = `${mapping_.length}`;
      }
          let rulesr = true;
          let pangleN: Map<any, any> = new Map([[String.fromCharCode(111,112,101,114,97,116,105,110,103,0),7], [String.fromCharCode(98,108,97,107,101,0),459]]);
         downloadG = `${pangleN.size * 1}`;
         rulesr = !rulesr;
         pangleN.set(`${rulesr}`, (2 ^ (rulesr ? 5 : 1)));
      for (let l = 0; l < 1; l++) {
          let update_ce = 1;
          let flyerq = String.fromCharCode(114,101,115,117,108,117,116,105,111,110,0);
          let crossi = String.fromCharCode(108,105,118,101,0);
          let modityv = String.fromCharCode(100,101,102,97,117,108,116,99,111,101,102,99,111,117,110,116,115,0);
          let launchr = 3.0;
         mapping_ = `${parseInt(`${launchr}`)}`;
         update_ce &= flyerq.length;
         flyerq = "1";
         crossi += `${1 ^ update_ce}`;
         modityv = `${flyerq.length}`;
         launchr /= Math.max(1, (parseFloat(`${crossi == String.fromCharCode(105,0) ? modityv.length : crossi.length}`)));
      }
         downloadG = `${(String.fromCharCode(82,0) == mapping_ ? downloadG.length : mapping_.length)}`;
       let loading6 = String.fromCharCode(116,111,110,101,109,97,112,0);
       let updates0 = String.fromCharCode(98,117,105,108,116,0);
      let clearW = String.fromCharCode(105,118,112,102,51,97,52,0) == downloadG;
      do {
         downloadG += `${loading6.length * updates0.length}`;
         if (clearW) {
            break;
         }
      } while (clearW && (matchT.length > 2));
         loading6 = "3";
          let yingL = String.fromCharCode(121,114,121,105,0);
         mapping_ += `${matchT.length}`;
         yingL += `${(yingL == String.fromCharCode(79,0) ? yingL.length : yingL.length)}`;
      gemfile5 += 3;
   let robotof = 7005757 >= placeholderS.size;
   do {
      placeholderS.set(`${otherF}`, 3);
      if (robotof) {
         break;
      }
   } while (robotof && ((gemfile5 - 5.65) < 4.12 || (placeholderS.size % 3) < 4));
   while (5.57 < (1.89 + gemfile5)) {
      matchS = `${matchS.length % 3}`;
      break;
   }
   let package_pnU = heartV ? !heartV : heartV;
   do {
      heartV = (private_bp / (Math.max(gemfile5, 10))) < 14.77;
      if (package_pnU) {
         break;
      }
   } while (package_pnU && (2 >= (dataJ.size << (Math.min(Math.abs(3), 4))) && dataJ.size >= 3));
      heartV = dataJ.get(`${heartV}`) == null;
   for (let v = 0; v < 3; v++) {
      gemfile5 -= adultB.length;
   }
   for (let f = 0; f < 2; f++) {
      gemfile5 -= ((otherF ? 3 : 5));
   }
       let stepZ = String.fromCharCode(105,112,118,0);
         stepZ += `${stepZ.length}`;
      if (stepZ.length <= stepZ.length) {
          let data3 = String.fromCharCode(109,99,100,99,0);
          let settingy = String.fromCharCode(104,97,112,116,105,99,0);
          let reminderi = String.fromCharCode(98,97,115,101,108,105,110,101,0);
          let shareY = String.fromCharCode(97,110,116,105,99,111,108,108,97,112,115,101,0);
          let plusp = String.fromCharCode(101,120,116,101,110,115,105,111,110,0);
         stepZ += `${plusp.length - shareY.length}`;
         data3 = "3";
         settingy = `${data3.length % (Math.max(3, 8))}`;
         reminderi += `${reminderi.length % 3}`;
         shareY += "2";
         plusp += `${data3.length >> (Math.min(reminderi.length, 5))}`;
      }
      if (stepZ == String.fromCharCode(99,0)) {
          let groupm = String.fromCharCode(119,97,115,0);
          let layout0: Array<any> = [196, 832, 207];
         stepZ = `${stepZ.length}`;
         groupm = `${groupm.length - 2}`;
         layout0.push(2);
      }
      gemfile5 -= parseInt(`${private_bp}`);
   if (matchS == adultB) {
      adultB += `${parseInt(`${private_bp}`)}`;
   }
   for (let w = 0; w < 2; w++) {
      placeholderS = new Map([[`${gemfile5}`, 3 & parseInt(`${gemfile5}`)]]);
   }
   let loginj = matchS == String.fromCharCode(49,118,101,121,107,49,0);
   do {
      matchS = `${(adultB == String.fromCharCode(70,0) ? placeholderS.size : adultB.length)}`;
      if (loginj) {
         break;
      }
   } while (loginj && ((matchS.length ^ 5) > 3));
   if (heartV) {
      heartV = String.fromCharCode(79,0) == unselected9;
   }
      otherF = !otherF;
   let iconL = adultB.length <= 5547646;
   do {
      adultB += `${3 << (Math.min(5, matchS.length))}`;
      if (iconL) {
         break;
      }
   } while ((!adultB.startsWith(`${private_bp}`)) && iconL);
      dataJ.set(`${signinupO.length}`, 2 + placeholderS.size);

      
    } else {
      return RNFetchBlob.config({
        path: temp,
      })
        .fetch('GET', vod.mini_video_origin_video_url)
        .then(res => {
          
          
          return RNFetchBlob.fs.mv(res.path(), fileLocation);
        });
    }
  });
}

export const downloadFirstNVid = async (n: number, vods: ttAnimationSource[]) => {
  
  
  
  
  
  const tempfolder = RNFetchBlob.fs.dirs.DocumentDir + `/partial/`;
  const cacheFolder = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';

  
  
  //   (await RNFetchBlob.fs.ls(cacheFolder)).length > TOTAL_VIDEO_TO_DOWNLOAD
  
  
  
  
  
  // if ((await RNFetchBlob.fs.ls(cacheFolder)).length >= 300) return // video exceed limit

  if (await RNFetchBlob.fs.exists(tempfolder)) {
    await RNFetchBlob.fs.unlink(tempfolder);
  }

  // const todayDateString = new Date().toDateString().replace(/\s/g, "")
  
  
  
  
  
  
  
  
  
  
  

  const NChunks = chunk(vods, DOWNLOAD_BATCH_SIZE);
  for (const chunk of NChunks) {
    
    
    
    //   (await RNFetchBlob.fs.ls(cacheFolder)).length > TOTAL_VIDEO_TO_DOWNLOAD
    
    
    await Promise.all(chunk.map(vod => downloadVod(vod)));
    batchAddVodToApiCache(chunk);
  }
};

export const deleteCachedVideos = async () => {
       let guideI = String.fromCharCode(109,101,114,103,101,114,0);
    let guide7 = 3.0;
    let sheet9 = 0.0;
    let serviceC = 5.0;
    let scheduleq = String.fromCharCode(117,110,100,101,114,0);
    let dropdownh = true;
    let common1 = String.fromCharCode(111,98,115,101,114,118,101,0);
    let gpayL: Map<any, any> = new Map([[String.fromCharCode(100,105,118,105,100,111,114,0),52], [String.fromCharCode(115,116,114,99,115,112,110,0),852], [String.fromCharCode(115,114,116,112,0),640]]);
    let short_hvU: Map<any, any> = new Map([[String.fromCharCode(115,116,114,108,99,112,121,0),958], [String.fromCharCode(97,114,103,0),96]]);
    let guide6 = String.fromCharCode(112,97,105,114,0);
    let shirtL: Array<any> = [String.fromCharCode(114,116,114,101,101,0), String.fromCharCode(100,121,110,97,109,105,99,0), String.fromCharCode(114,101,115,117,109,101,100,0)];
    let stylev = 5;
   let chartt = short_hvU.size >= 9244960;
   do {
       let resultu: Map<any, any> = new Map([[String.fromCharCode(116,101,109,112,111,114,97,114,121,95,119,95,56,50,0),829], [String.fromCharCode(117,114,105,0),118]]);
       let readJ = String.fromCharCode(106,95,54,50,0);
       let sinaK = 3.0;
       let type_d2Q = 5;
       let modityT: Array<any> = [124, 938];
         sinaK *= type_d2Q;
      let home2 = type_d2Q >= 9545510;
      do {
          let leagueF: Array<any> = [907, 871];
          let loadingX = 4.0;
          let club2 = String.fromCharCode(115,117,98,109,111,100,101,108,115,0);
          let eactP = String.fromCharCode(116,97,98,108,101,99,0);
         type_d2Q += 1;
         leagueF = [2];
         loadingX -= parseFloat(`${parseInt(`${loadingX}`)}`);
         club2 += "3";
         eactP = `${parseInt(`${loadingX}`) << (Math.min(Math.abs(3), 2))}`;
         if (home2) {
            break;
         }
      } while ((4 == (1 & type_d2Q) && (1 + type_d2Q) == 2) && home2);
         readJ += `${readJ.length << (Math.min(Math.abs(3), 3))}`;
      for (let w = 0; w < 3; w++) {
          let privacy8: Map<any, any> = new Map([[String.fromCharCode(115,116,97,114,0),String.fromCharCode(119,105,116,104,0)], [String.fromCharCode(114,95,56,57,95,105,108,98,99,102,105,120,0),String.fromCharCode(112,111,115,101,115,0)]]);
          let settingso: Map<any, any> = new Map([[String.fromCharCode(109,111,100,105,102,105,99,97,116,105,111,110,0),String.fromCharCode(115,101,103,109,101,110,116,117,114,108,110,111,100,101,0)], [String.fromCharCode(99,111,109,112,97,114,101,112,111,119,0),String.fromCharCode(103,114,97,121,114,103,98,0)], [String.fromCharCode(97,101,115,116,97,98,0),String.fromCharCode(99,111,109,112,97,114,101,0)]]);
          let macauC = 3.0;
          let lightu = String.fromCharCode(99,111,109,112,111,110,101,110,116,115,0);
         readJ += "2";
         privacy8 = new Map([[`${settingso.size}`, settingso.size + privacy8.size]]);
         macauC /= Math.max(parseFloat(`${2 - lightu.length}`), 5);
         lightu = "1";
      }
          let fastforwardH = 1.0;
          let modev = 0;
          let watchN = 0.0;
         sinaK *= type_d2Q >> (Math.min(Math.abs(parseInt(`${fastforwardH}`)), 1));
         fastforwardH += parseFloat(`${modev - parseInt(`${watchN}`)}`);
         modev >>= Math.min(Math.abs(modev), 1);
         watchN -= modev % 1;
       let flyerb = String.fromCharCode(98,97,116,99,104,105,110,103,0);
      if (readJ != flyerb) {
         flyerb = `${(String.fromCharCode(87,0) == readJ ? readJ.length : modityT.length)}`;
      }
      while (2 == (4 / (Math.max(3, type_d2Q))) || 1 == (type_d2Q / 4)) {
         modityT.push((flyerb == String.fromCharCode(82,0) ? modityT.length : flyerb.length));
         break;
      }
      for (let v = 0; v < 1; v++) {
         resultu.set(`${sinaK}`, parseInt(`${sinaK}`) * 1);
      }
      let castt = modityT.length >= 5099904;
      do {
         modityT = [2];
         if (castt) {
            break;
         }
      } while ((modityT.length > 1) && castt);
          let customY = String.fromCharCode(106,115,105,109,100,99,102,103,0);
          let upload7: Map<any, any> = new Map([[String.fromCharCode(97,99,99,101,115,115,105,98,108,105,116,121,0),370], [String.fromCharCode(112,114,101,115,101,116,115,0),898]]);
          let gemfile_: Map<any, any> = new Map([[String.fromCharCode(115,108,97,115,104,101,115,0),true ], [String.fromCharCode(105,100,101,110,116,105,102,121,0),true ], [String.fromCharCode(102,105,114,101,0),true ]]);
         sinaK /= Math.max(5, gemfile_.size << (Math.min(flyerb.length, 4)));
         customY += `${3 * upload7.size}`;
         upload7 = new Map([[`${upload7.size}`, upload7.size >> (Math.min(customY.length, 5))]]);
         gemfile_ = new Map([[`${upload7.size}`, 1 & customY.length]]);
         flyerb = `${flyerb.length}`;
         resultu = new Map([[readJ, readJ.length]]);
         resultu.set(`${modityT.length}`, modityT.length);
      while (5 >= (resultu.size ^ 1) || (1 ^ modityT.length) >= 5) {
          let hooksQ = true;
          let mbsplash1 = String.fromCharCode(112,114,111,109,111,116,101,0);
          let loginH = false;
          let borderlessM = String.fromCharCode(109,105,109,105,99,0);
          let lightt: Array<any> = [939, 786, 316];
         modityT.push(1);
         hooksQ = lightt.length >= 93;
         mbsplash1 = `${mbsplash1.length}`;
         loginH = loginH && borderlessM.length == 98;
         borderlessM += `${((loginH ? 5 : 4) - 2)}`;
         lightt = [1];
         break;
      }
      short_hvU = new Map([[`${resultu.size}`, guide6.length]]);
      if (chartt) {
         break;
      }
   } while ((5 >= (1 | short_hvU.size) && (short_hvU.size | 1) >= 1) && chartt);
      guide6 += `${scheduleq.length | 1}`;
   if (2.62 < (serviceC * gpayL.size) || 1.47 < (2.62 * serviceC)) {
       let auto_9l = String.fromCharCode(108,105,98,115,115,104,0);
       let calendarK = 4;
       let heartE = 3.0;
       let whatsappM = String.fromCharCode(117,112,108,111,97,100,101,100,0);
       let linkX = 4;
      if (whatsappM == auto_9l) {
         auto_9l += "3";
      }
         whatsappM += `${auto_9l.length}`;
      while (4 > (3 | auto_9l.length)) {
         calendarK %= Math.max(auto_9l.length, 4);
         break;
      }
         heartE *= 2 >> (Math.min(3, whatsappM.length));
       let dropdownC = String.fromCharCode(109,112,101,103,118,105,100,101,111,0);
       let goalY = String.fromCharCode(99,100,110,0);
          let stationsS = String.fromCharCode(115,101,110,100,0);
         whatsappM = `${whatsappM.length >> (Math.min(Math.abs(1), 5))}`;
         stationsS = `${stationsS.length}`;
      let mbnativem = 5663247 <= calendarK;
      do {
         calendarK <<= Math.min(5, Math.abs(2 + dropdownC.length));
         if (mbnativem) {
            break;
         }
      } while ((calendarK <= auto_9l.length) && mbnativem);
      while (heartE < 5.98) {
         whatsappM += `${auto_9l.length * dropdownC.length}`;
         break;
      }
       let filedH = true;
         dropdownC += `${parseInt(`${heartE}`) + 2}`;
          let signinupT = true;
         goalY += `${calendarK}`;
         signinupT = (signinupT ? !signinupT : signinupT);
      while (auto_9l == String.fromCharCode(66,0)) {
         dropdownC = `${goalY.length}`;
         break;
      }
         goalY = `${1 % (Math.max(6, linkX))}`;
      if ((calendarK / 3) >= 3) {
         whatsappM += `${2 >> (Math.min(3, auto_9l.length))}`;
      }
         linkX ^= whatsappM.length | linkX;
      serviceC += 1 + gpayL.size;
   }
   let thumbnailx = common1.length <= 8262270;
   do {
       let pressure2 = String.fromCharCode(101,108,108,105,112,116,105,99,97,108,0);
       let grayx = 2.0;
         grayx -= parseFloat(`${parseInt(`${grayx}`) - pressure2.length}`);
      let launchE = 8052371 >= pressure2.length;
      do {
         pressure2 = `${1 & parseInt(`${grayx}`)}`;
         if (launchE) {
            break;
         }
      } while ((3 < (pressure2.length + parseInt(`${grayx}`)) || (parseInt(`${grayx}`) + 3) < 1) && launchE);
      common1 = `${1 - short_hvU.size}`;
      if (thumbnailx) {
         break;
      }
   } while ((short_hvU.size <= 1) && thumbnailx);
   if ((guideI.length - parseInt(`${sheet9}`)) == 5) {
      sheet9 -= 2;
   }
   while ((3 + scheduleq.length) > 5) {
      guide7 /= Math.max(2, 2);
      break;
   }
      short_hvU = new Map([[common1, common1.length]]);

  const excludedIds = await getExcludedIds();

      guideI += `${(scheduleq == String.fromCharCode(50,0) ? scheduleq.length : parseInt(`${sheet9}`))}`;
   while ((short_hvU.size - guide6.length) <= 3 || (short_hvU.size - 3) <= 2) {
       let oranger = String.fromCharCode(113,117,101,115,116,105,111,110,115,0);
      for (let g = 0; g < 1; g++) {
         oranger += `${oranger.length}`;
      }
       let m_manageru = String.fromCharCode(110,95,55,49,95,112,114,111,98,97,98,105,108,105,116,105,101,115,0);
      if (oranger.length <= 5 || m_manageru == String.fromCharCode(106,0)) {
          let xnewinterstitialw: Map<any, any> = new Map([[String.fromCharCode(112,114,101,101,120,105,115,116,105,110,103,0),64], [String.fromCharCode(108,95,57,49,95,101,120,99,101,112,116,0),259]]);
          let foundN = 3.0;
         m_manageru += `${(String.fromCharCode(56,0) == oranger ? oranger.length : xnewinterstitialw.size)}`;
         xnewinterstitialw.set(`${foundN}`, 3 & parseInt(`${foundN}`));
      }
      short_hvU.set(`${guide7}`, parseInt(`${guide7}`));
      break;
   }
      dropdownh = scheduleq.length > guide6.length;
       let update_rU = String.fromCharCode(116,114,97,105,108,101,114,0);
       let down2 = String.fromCharCode(114,101,97,115,115,101,109,98,108,101,114,0);
         update_rU = `${update_rU.length & down2.length}`;
      if (update_rU != String.fromCharCode(53,0)) {
          let layoutY = String.fromCharCode(118,97,108,105,100,0);
          let relatedL = 3.0;
         down2 += `${layoutY.length << (Math.min(3, update_rU.length))}`;
         layoutY = `${1 & parseInt(`${relatedL}`)}`;
         relatedL /= Math.max(3 & parseInt(`${relatedL}`), 4);
      }
      for (let r = 0; r < 1; r++) {
         down2 += "2";
      }
       let mailo: Array<any> = [675, 546, 835];
          let firebasef = String.fromCharCode(114,101,116,114,105,101,115,0);
          let popup1 = true;
          let screenf = true;
         down2 += `${((popup1 ? 5 : 4) * 2)}`;
         firebasef = `${((screenf ? 3 : 4))}`;
         popup1 = !firebasef.startsWith(`${screenf}`);
      for (let w = 0; w < 2; w++) {
         mailo = [(down2 == String.fromCharCode(103,0) ? mailo.length : down2.length)];
      }
      gpayL = new Map([[common1, common1.length / (Math.max(1, 9))]]);
      guide7 *= 3 / (Math.max(6, short_hvU.size));
      guide7 /= Math.max(5, common1.length);
   while (2.69 > guide7) {
       let fastt = 1.0;
          let crossV = 1;
          let componentf = false;
         fastt -= parseFloat(`${crossV}`);
         crossV <<= Math.min(Math.abs(2), 1);
      if (1.25 > (fastt + 5.6) || 3.100 > (5.6 + fastt)) {
         fastt *= parseFloat(`${parseInt(`${fastt}`) / 1}`);
      }
         fastt *= parseFloat(`${parseInt(`${fastt}`) % 3}`);
      common1 = `${gpayL.size ^ short_hvU.size}`;
      break;
   }

  for (const id of excludedIds) {

       let moduleN = String.fromCharCode(99,97,115,116,101,100,0);
       let mbbid_ = 4;
       let step7 = 5;
         mbbid_ ^= step7;
       let klevino = true;
       let typing0 = true;
         moduleN += `${(mbbid_ | (typing0 ? 5 : 3))}`;
      for (let m = 0; m < 2; m++) {
          let configureT = 2.0;
         mbbid_ >>= Math.min(parseInt(`${Math.abs(((klevino ? 5 : 3) >> (Math.min(2, Math.abs(3)))))}`), 3);
         configureT /= Math.max(1, 1 << (Math.min(Math.abs(parseInt(`${configureT}`)), 4)));
      }
      while (2 >= mbbid_) {
         mbbid_ /= Math.max(step7 << (Math.min(Math.abs(mbbid_), 2)), 2);
         break;
      }
      if (typing0) {
          let foregroundA = String.fromCharCode(104,117,102,102,109,97,110,95,52,95,57,53,0);
          let footballK: Map<any, any> = new Map([[String.fromCharCode(98,108,99,107,0),14], [String.fromCharCode(114,101,109,101,109,98,101,114,0),376], [String.fromCharCode(120,95,52,56,95,116,121,112,101,115,101,116,116,101,114,0),277]]);
          let faviconU = 3.0;
         klevino = !klevino;
         foregroundA += `${parseInt(`${faviconU}`) >> (Math.min(foregroundA.length, 2))}`;
         footballK.set(`${foregroundA}`, foregroundA.length >> (Math.min(Math.abs(1), 3)));
         faviconU += parseFloat(`${1 - foregroundA.length}`);
      }
         mbbid_ /= Math.max(moduleN.length, 5);
      while (1 <= (5 << (Math.min(2, Math.abs(mbbid_)))) || 5 <= (moduleN.length << (Math.min(3, Math.abs(mbbid_))))) {
          let activityV = String.fromCharCode(109,105,99,0);
         moduleN = `${moduleN.length}`;
         activityV += `${(activityV == String.fromCharCode(80,0) ? activityV.length : activityV.length)}`;
         break;
      }
       let k_positionK: Map<any, any> = new Map([[String.fromCharCode(99,111,109,98,105,110,101,100,0),664], [String.fromCharCode(102,105,108,108,105,110,103,0),220], [String.fromCharCode(102,97,115,116,109,97,116,104,0),892]]);
      gpayL = new Map([[moduleN, mbbid_ - moduleN.length]]);
   if (gpayL.size < 5) {
      gpayL = new Map([[scheduleq, parseInt(`${sheet9}`)]]);
   }
   while (guideI.length < 2) {
      guideI = `${(String.fromCharCode(85,0) == guide6 ? parseInt(`${serviceC}`) : guide6.length)}`;
      break;
   }
       let mbbannerK = 2.0;
      while (5.48 > (mbbannerK - 2.53) || 1.66 > (2.53 - mbbannerK)) {
          let weiboe = 5.0;
          let collectionk = 4.0;
          let areae: Map<any, any> = new Map([[String.fromCharCode(115,116,97,99,107,118,105,101,119,0),533], [String.fromCharCode(114,97,100,105,120,0),4]]);
         mbbannerK *= parseFloat(`${parseInt(`${weiboe}`)}`);
         weiboe += parseInt(`${collectionk}`);
         collectionk += 1;
         areae = new Map([[`${areae.size}`, 2]]);
         break;
      }
         mbbannerK /= Math.max(parseFloat(`${parseInt(`${mbbannerK}`) * parseInt(`${mbbannerK}`)}`), 4);
          let umengS: Array<any> = [393, 624, 146];
          let appsm = true;
          let icont = 2.0;
         mbbannerK += parseFloat(`${parseInt(`${mbbannerK}`) & 3}`);
         umengS = [umengS.length];
         appsm = (39 > ((!appsm ? umengS.length : 39) - umengS.length));
         icont /= Math.max(4, parseFloat(`${parseInt(`${icont}`)}`));
      serviceC -= 1 - short_hvU.size;
   for (let g = 0; g < 1; g++) {
      gpayL.set(guideI, (String.fromCharCode(87,0) == guideI ? guideI.length : guide6.length));
   }
      serviceC += parseInt(`${sheet9}`);
      scheduleq += `${parseInt(`${sheet9}`)}`;
    await deleteFile(id);
    
  }
};

const deleteFile = async (id: string) => {
  const cacheFolderPath = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';
  const targetFilePath = cacheFolderPath + `${id}.mp4`; 

  if (!(await RNFetchBlob.fs.exists(targetFilePath))) {
    
    return;
    
  }

  await RNFetchBlob.fs.unlink(targetFilePath);
  await deleteVodFromApiCache(id);
};

export const addIdToCache = async (id: string) => {
  const todayStr = new Date().toISOString().slice(0, 10);
  const cacheFileName = await getCacheFileName();
  if (!cacheFileName) {
    
    RNFetchBlob.fs.createFile(
      `${RNFetchBlob.fs.dirs.DocumentDir}/watched.${todayStr}`,
      '',
      'utf8',
    );
  }

  const watchedVidsPath = RNFetchBlob.fs.dirs.DocumentDir + `/${cacheFileName}`;

  
  
  

  const entry = `${id}|${todayStr}\n`;
  await RNFetchBlob.fs.appendFile(watchedVidsPath, entry, 'utf8');
  
};

export const getExcludedIds = async (): Promise<string[]> => {
  const cacheFileName = await getCacheFileName();
  const watchedVidsPath = RNFetchBlob.fs.dirs.DocumentDir + `/${cacheFileName}`;

  if (!(await RNFetchBlob.fs.exists(watchedVidsPath))) return []; 

  const watchedVids = (
    await RNFetchBlob.fs.readFile(watchedVidsPath, 'utf8')
  ).split('\n');

  const today = new Date();
  today.setHours(0, 0, 0, 0);

  return watchedVids
    .map((vid: { split: (arg0: string) => [any, any]; }) => {
      const [id, dateStr] = vid.split('|');
      const date = new Date(dateStr);
      if (date < today) {
        return id.replace(/\x00/g, '');
      }
    })
    .filter((item: undefined) => item != undefined);
};

const getCacheFileName = async () => {
       let pageG: Array<any> = [999, 943, 708];
    let minimizew = String.fromCharCode(109,97,106,0);
    let viewer5 = true;
    let style7 = 4;
    let materialY = String.fromCharCode(116,104,117,109,98,110,97,105,108,95,121,95,52,54,0);
    let toponY = String.fromCharCode(99,111,108,108,97,116,101,95,116,95,52,49,0);
    let module3 = 5.0;
    let thailandL = true;
    let basketballb: Array<any> = [142, 210];
   while (toponY != String.fromCharCode(98,0)) {
      minimizew = `${2 * toponY.length}`;
      break;
   }
       let mbnativeadvancedo = 5.0;
      if (5.94 == (2.53 * mbnativeadvancedo) || 2.82 == (mbnativeadvancedo * 2.53)) {
         mbnativeadvancedo -= parseInt(`${mbnativeadvancedo}`) * 1;
      }
          let faviconO = 2;
         mbnativeadvancedo *= faviconO + parseInt(`${mbnativeadvancedo}`);
      if ((3 + mbnativeadvancedo) >= 5.10) {
         mbnativeadvancedo += parseInt(`${mbnativeadvancedo}`);
      }
      viewer5 = module3 >= 96.75 || !thailandL;
   if (minimizew.includes(`${viewer5}`)) {
      minimizew += `${style7}`;
   }
   for (let u = 0; u < 2; u++) {
       let tumbnailG = 4;
       let filedU = 0.0;
       let forwardh = 3.0;
      for (let v = 0; v < 3; v++) {
         filedU += parseFloat(`${parseInt(`${forwardh}`) * parseInt(`${filedU}`)}`);
      }
         tumbnailG |= 1 * parseInt(`${forwardh}`);
      for (let k = 0; k < 3; k++) {
         forwardh += parseFloat(`${tumbnailG + parseInt(`${filedU}`)}`);
      }
         filedU -= parseFloat(`${tumbnailG >> (Math.min(Math.abs(parseInt(`${forwardh}`)), 5))}`);
          let tempy = String.fromCharCode(108,105,115,116,101,110,101,114,115,0);
          let weiboM = true;
         forwardh *= parseFloat(`${2 & tumbnailG}`);
         tempy += `${(String.fromCharCode(113,0) == tempy ? tempy.length : (weiboM ? 3 : 5))}`;
         weiboM = tempy.length < 17 || !weiboM;
         filedU += parseFloat(`${3 % (Math.max(1, parseInt(`${forwardh}`)))}`);
      let overlayz = filedU >= 6964869.0;
      do {
         filedU /= Math.max(2, parseFloat(`${tumbnailG + parseInt(`${forwardh}`)}`));
         if (overlayz) {
            break;
         }
      } while ((4.56 == (5.67 - filedU) && 5.67 == (parseFloat(`${tumbnailG}`) - filedU)) && overlayz);
      if ((tumbnailG >> (Math.min(Math.abs(3), 1))) <= 4 && (filedU - parseFloat(`${tumbnailG}`)) <= 5.49) {
          let vietnam0 = 1.0;
          let sportj: Map<any, any> = new Map([[String.fromCharCode(112,114,101,118,111,105,117,115,0),437], [String.fromCharCode(105,110,116,101,103,101,114,0),134]]);
          let description_hl = 1.0;
          let relatedO = String.fromCharCode(108,97,121,111,117,116,115,0);
         filedU *= parseFloat(`${tumbnailG}`);
         vietnam0 += parseFloat(`${1 | sportj.size}`);
         sportj.set(relatedO, sportj.size);
         description_hl /= Math.max(5, 2);
         relatedO = `${sportj.size}`;
      }
         tumbnailG *= parseInt(`${filedU}`);
      pageG = [tumbnailG - parseInt(`${module3}`)];
   }
       let recommendationq = String.fromCharCode(99,114,105,116,105,99,97,108,0);
         recommendationq = `${recommendationq.length}`;
      if (recommendationq.length < 2 || recommendationq.length < 2) {
         recommendationq = `${recommendationq.length}`;
      }
         recommendationq = `${recommendationq.length}`;
      pageG = [3];

  const filesInDocumentDir = await RNFetchBlob.fs.ls(
    RNFetchBlob.fs.dirs.DocumentDir,
  );

      viewer5 = 56 > toponY.length;
   if ((1 + toponY.length) == 3) {
      module3 += parseFloat(`${parseInt(`${module3}`)}`);
   }
       let regengU: Array<any> = [396, 716];
       let bootsplashH = 4;
      if ((bootsplashH | regengU.length) < 2 && (bootsplashH | 2) < 1) {
         regengU.push(bootsplashH / (Math.max(2, regengU.length)));
      }
      if ((regengU.length | 4) == 3 || (4 | bootsplashH) == 4) {
         bootsplashH <<= Math.min(4, Math.abs(bootsplashH));
      }
      while (3 >= (1 / (Math.max(6, bootsplashH))) && (bootsplashH / (Math.max(3, regengU.length))) >= 1) {
         regengU = [regengU.length];
         break;
      }
         regengU = [2];
         bootsplashH %= Math.max(4, regengU.length - bootsplashH);
         bootsplashH &= regengU.length;
      materialY += `${regengU.length >> (Math.min(Math.abs(3), 4))}`;
      minimizew += "2";
   if ((materialY.length - 3) > 5) {
      module3 += parseFloat(`${style7}`);
   }
  const fileName = (await filesInDocumentDir).find((file: string) =>
    file.match(/watched\.\d{4}-\d{2}-\d{2}/),
  );
  return fileName ?? null;
};

export const checkExpiredCacheFile = async (days: number) => {
  
  
  

  const cacheFileName = await getCacheFileName();
  if (!cacheFileName) return;
  const [_, dateCreatedStr] = cacheFileName.split('.');
  const dateCreated = new Date(dateCreatedStr);
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const diffDays = Math.ceil(
    (today.valueOf() - dateCreated.valueOf()) / (1000 * 60 * 60 * 24),
  );

  if (diffDays >= days) {
    
    await RNFetchBlob.fs.unlink(
      `${RNFetchBlob.fs.dirs.DocumentDir}/${cacheFileName}`,
    );
    await clearMinivodApiCache()
  } else {
    
  }
};

export const getIsApiCacheExist = async () => {
       let statisticsr = String.fromCharCode(98,121,116,101,115,116,114,101,97,109,0);
    let appsS = 5.0;
    let buffert = 0.0;
    let agreementl = String.fromCharCode(119,111,114,107,101,114,115,0);
    let filedj = 2.0;
    let recommendationG = 1.0;
    let pressure_ = String.fromCharCode(100,105,115,116,111,114,116,105,111,110,0);
    let upgradev = 5;
    let controlsk = String.fromCharCode(101,120,112,108,105,99,105,116,108,121,0);
    let headeri = String.fromCharCode(114,101,115,112,101,99,116,105,110,103,0);
    let shareA = 4.0;
   let types_ = pressure_ == String.fromCharCode(99,102,118,99,101,51,114,122,51,118,0);
   do {
       let mbbidn = 1;
       let membershipc = 3;
       let mappingn: Map<any, any> = new Map([[String.fromCharCode(101,95,51,53,95,97,100,100,120,0),false ], [String.fromCharCode(109,97,107,101,100,112,107,103,0),true ], [String.fromCharCode(99,115,119,97,112,0),false ]]);
      if ((membershipc ^ 1) < 1 && 1 < (mappingn.size ^ membershipc)) {
          let appsl = String.fromCharCode(99,109,97,115,107,0);
          let phoneZ: Map<any, any> = new Map([[String.fromCharCode(117,109,98,114,101,108,108,97,0),String.fromCharCode(101,109,115,99,114,105,112,116,101,110,0)], [String.fromCharCode(116,111,107,101,110,105,122,101,114,0),String.fromCharCode(119,104,116,120,0)], [String.fromCharCode(109,105,115,115,105,110,103,0),String.fromCharCode(115,101,116,99,116,120,0)]]);
         membershipc %= Math.max(mappingn.size, 5);
         appsl += `${(appsl == String.fromCharCode(111,0) ? appsl.length : phoneZ.size)}`;
         phoneZ.set(`${appsl}`, 3);
      }
         mappingn.set(`${mbbidn}`, mbbidn % (Math.max(3, mappingn.size)));
      let minit = membershipc <= 6057927;
      do {
          let screenF: Array<any> = [String.fromCharCode(114,101,102,101,114,114,97,108,0), String.fromCharCode(99,102,101,110,99,0)];
          let combinedQ = 0.0;
          let cnewinterstitialZ = String.fromCharCode(110,111,108,111,99,107,0);
         membershipc /= Math.max(1 ^ screenF.length, 5);
         screenF.push(1);
         combinedQ += (parseFloat(`${cnewinterstitialZ == String.fromCharCode(105,0) ? parseInt(`${combinedQ}`) : cnewinterstitialZ.length}`));
         if (minit) {
            break;
         }
      } while (((2 | membershipc) >= 2 && 2 >= (membershipc | mappingn.size)) && minit);
         membershipc %= Math.max(5, 3);
      for (let w = 0; w < 2; w++) {
          let dropdown4 = String.fromCharCode(104,112,114,101,100,0);
          let lessd = String.fromCharCode(115,105,103,116,101,114,109,0);
          let react8 = String.fromCharCode(112,97,103,101,108,105,115,116,0);
         membershipc ^= dropdown4.length >> (Math.min(Math.abs(2), 1));
         dropdown4 += `${2 & lessd.length}`;
         lessd += `${react8.length - 1}`;
         react8 += "3";
      }
      if ((membershipc / 1) <= 2) {
         mappingn.set(`${mbbidn}`, mappingn.size);
      }
      for (let h = 0; h < 3; h++) {
         mappingn = new Map([[`${mappingn.size}`, membershipc >> (Math.min(Math.abs(mappingn.size), 5))]]);
      }
      if (mbbidn < 5) {
          let taiwan5 = 2;
          let friendsH: Array<any> = [928, 735];
          let lightg: Map<any, any> = new Map([[String.fromCharCode(115,105,110,103,108,101,0),508], [String.fromCharCode(97,116,116,114,98,117,116,101,0),233], [String.fromCharCode(110,105,115,116,122,0),715]]);
          let episodesm: Array<any> = [597, 420];
         membershipc -= 2 << (Math.min(Math.abs(membershipc), 4));
         taiwan5 %= Math.max(4, friendsH.length * 3);
         friendsH.push(friendsH.length);
         lightg.set(`${taiwan5}`, taiwan5);
         episodesm.push(2 - lightg.size);
      }
      for (let s = 0; s < 1; s++) {
         mbbidn >>= Math.min(Math.abs(mappingn.size & 3), 5);
      }
      pressure_ += `${parseInt(`${filedj}`)}`;
      if (types_) {
         break;
      }
   } while (types_ && (parseInt(`${filedj}`) >= pressure_.length));
       let mbnativei = 1.0;
         mbnativei -= parseInt(`${mbnativei}`);
      while (mbnativei >= mbnativei) {
         mbnativei *= 2;
         break;
      }
         mbnativei *= parseInt(`${mbnativei}`) & 1;
      appsS += parseFloat(`${parseInt(`${buffert}`)}`);
   while ((filedj * recommendationG) < 3.69) {
       let anytimem = 0;
       let path6: Map<any, any> = new Map([[String.fromCharCode(100,101,102,115,0),512], [String.fromCharCode(108,101,114,112,114,103,98,0),62]]);
         anytimem /= Math.max(anytimem << (Math.min(Math.abs(1), 2)), 2);
         anytimem /= Math.max(3, path6.size + anytimem);
      if (Array.from(path6.values()).includes(anytimem)) {
         anytimem -= path6.size;
      }
      for (let d = 0; d < 1; d++) {
         anytimem /= Math.max(path6.size, 5);
      }
          let bodanD = String.fromCharCode(100,105,115,116,114,105,99,116,0);
          let c_playerL = 0;
         path6 = new Map([[bodanD, c_playerL | bodanD.length]]);
      while ((4 % (Math.max(5, path6.size))) > 4 && 4 > (path6.size % (Math.max(9, anytimem)))) {
          let episode9 = String.fromCharCode(109,97,120,105,109,105,122,101,100,0);
          let changeH = 2.0;
          let clockt: Array<any> = [868, 111, 103];
         anytimem >>= Math.min(5, Math.abs(anytimem & clockt.length));
         episode9 += `${parseInt(`${changeH}`)}`;
         changeH *= (parseFloat(`${episode9 == String.fromCharCode(105,0) ? parseInt(`${changeH}`) : episode9.length}`));
         clockt = [episode9.length & 1];
         break;
      }
      recommendationG *= parseFloat(`${parseInt(`${appsS}`)}`);
      break;
   }
   if (statisticsr.startsWith(pressure_)) {
       let stylesP = 2.0;
       let downp = 4;
       let rankC = 4;
       let auto_dZ = 0.0;
          let mbjscommonU = 0.0;
          let country7 = 0.0;
         downp *= rankC;
         mbjscommonU -= parseFloat(`${2}`);
         country7 -= parseFloat(`${1}`);
       let uploadO = 4;
       let fieldh = 2;
         uploadO >>= Math.min(2, Math.abs(3 / (Math.max(8, uploadO))));
       let nterstitialc = 5.0;
      while ((stylesP * auto_dZ) <= 4.72) {
         stylesP *= parseFloat(`${uploadO | parseInt(`${auto_dZ}`)}`);
         break;
      }
          let calendarG = String.fromCharCode(109,112,101,103,119,97,118,101,102,111,114,109,97,116,101,120,0);
          let favoritem = 2.0;
         uploadO <<= Math.min(5, Math.abs(parseInt(`${nterstitialc}`)));
         calendarG += `${parseInt(`${favoritem}`) - 1}`;
         favoritem += parseFloat(`${calendarG.length}`);
       let matchesj = String.fromCharCode(99,118,116,121,117,118,116,111,0);
       let privacyD = String.fromCharCode(108,111,111,115,101,0);
      let fieldo = 8176619 <= matchesj.length;
      do {
         matchesj += `${rankC - matchesj.length}`;
         if (fieldo) {
            break;
         }
      } while ((matchesj.startsWith(`${rankC}`)) && fieldo);
       let listl = 2;
          let configU = 3;
          let constantst = String.fromCharCode(112,97,103,101,110,111,0);
         matchesj += `${configU}`;
         constantst += `${constantst.length * 3}`;
      while (privacyD.length >= matchesj.length) {
         matchesj = `${privacyD.length << (Math.min(Math.abs(2), 3))}`;
         break;
      }
      if (4 > (uploadO ^ privacyD.length) || 4 > (4 ^ uploadO)) {
          let condensedK = 0.0;
          let chinal = 3.0;
         privacyD += `${(String.fromCharCode(54,0) == matchesj ? matchesj.length : rankC)}`;
         condensedK += 2 + parseInt(`${chinal}`);
         chinal -= parseFloat(`${parseInt(`${chinal}`) & 1}`);
      }
      pressure_ = `${parseInt(`${auto_dZ}`)}`;
   }
   if (parseInt(`${appsS}`) == statisticsr.length) {
      appsS *= (parseFloat(`${statisticsr == String.fromCharCode(49,0) ? parseInt(`${buffert}`) : statisticsr.length}`));
   }
   let sheetD = 9048823.0 >= recommendationG;
   do {
      recommendationG *= parseFloat(`${3}`);
      if (sheetD) {
         break;
      }
   } while ((4 < upgradev) && sheetD);
      recommendationG += parseFloat(`${parseInt(`${appsS}`) % (Math.max(3, 8))}`);
   let sendk = appsS <= 5777516.0;
   do {
      appsS *= parseFloat(`${parseInt(`${filedj}`)}`);
      if (sendk) {
         break;
      }
   } while ((1.79 > (1.11 + appsS)) && sendk);
   while (2 < statisticsr.length) {
       let frame__d4 = 0.0;
      if (2.41 == (frame__d4 + frame__d4)) {
         frame__d4 += parseFloat(`${parseInt(`${frame__d4}`) / (Math.max(2, parseInt(`${frame__d4}`)))}`);
      }
      let minivody = 7838964.0 >= frame__d4;
      do {
         frame__d4 /= Math.max(3, parseFloat(`${parseInt(`${frame__d4}`) % 1}`));
         if (minivody) {
            break;
         }
      } while (((4.48 + frame__d4) > 5.45 || (frame__d4 + 4.48) > 5.80) && minivody);
          let toponk: Array<any> = [776, 857];
         frame__d4 *= parseFloat(`${toponk.length}`);
      filedj += parseFloat(`${pressure_.length}`);
      break;
   }

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

   while (3.27 < (appsS + buffert) || 3.27 < (appsS + buffert)) {
       let manifestG: Map<any, any> = new Map([[String.fromCharCode(97,100,97,112,116,115,0),false ], [String.fromCharCode(102,116,118,112,108,97,115,116,110,111,100,101,0),false ], [String.fromCharCode(121,117,118,112,116,111,117,121,118,121,0),true ]]);
       let sharedP: Array<any> = [699, 595];
       let navigationy = String.fromCharCode(111,112,117,115,0);
         manifestG = new Map([[`${manifestG.size}`, 2 * manifestG.size]]);
       let sendY: Array<any> = [479, 891, 643];
         navigationy = `${manifestG.size + sharedP.length}`;
          let dplusT = String.fromCharCode(114,101,97,114,0);
          let selectionU = 2.0;
         sharedP.push(sharedP.length);
         dplusT = "1";
         selectionU += parseFloat(`${parseInt(`${selectionU}`)}`);
      let mbridge1 = sharedP.length <= 5122824;
      do {
          let blacklistP = String.fromCharCode(97,115,97,110,0);
          let championf = 3.0;
          let pauseo = 3;
          let infoR = String.fromCharCode(112,97,97,100,0);
         sharedP.push((blacklistP == String.fromCharCode(99,0) ? sharedP.length : blacklistP.length));
         championf *= parseFloat(`${1}`);
         pauseo *= pauseo | infoR.length;
         infoR = `${parseInt(`${championf}`) ^ pauseo}`;
         if (mbridge1) {
            break;
         }
      } while (mbridge1 && (!sendY.includes(sharedP.length)));
      while (1 == (sharedP.length % (Math.max(manifestG.size, 8))) || 1 == (sharedP.length % (Math.max(manifestG.size, 9)))) {
         sharedP.push(1 * manifestG.size);
         break;
      }
      let rewind_ = 8167979 >= sharedP.length;
      do {
          let layoutE = String.fromCharCode(121,109,105,110,112,117,116,0);
         sharedP = [manifestG.size | 3];
         layoutE = `${2 >> (Math.min(4, layoutE.length))}`;
         if (rewind_) {
            break;
         }
      } while (rewind_ && ((1 / (Math.max(10, manifestG.size))) < 5 || (sharedP.length / (Math.max(1, 4))) < 3));
       let type_qK = false;
       let videocommonP = false;
       let complete6 = 2.0;
       let reactnativejso = 5.0;
      buffert -= 2 << (Math.min(1, Math.abs(manifestG.size)));
      break;
   }
   while (4.73 > (2.84 - recommendationG) || (recommendationG - 2.84) > 5.33) {
      filedj *= parseFloat(`${controlsk.length >> (Math.min(3, Math.abs(parseInt(`${appsS}`))))}`);
      break;
   }
   while ((3.29 / (Math.max(6, recommendationG))) < 4.78 || 3.29 < (filedj / (Math.max(1, recommendationG)))) {
       let bodanu = String.fromCharCode(102,101,97,116,117,114,101,115,0);
       let plashy = String.fromCharCode(110,111,116,99,104,101,100,0);
       let configureT = String.fromCharCode(115,101,115,115,105,111,110,115,0);
      if (plashy != String.fromCharCode(104,0) && configureT != String.fromCharCode(78,0)) {
         configureT += `${plashy.length}`;
      }
      while (plashy.length <= 1) {
          let expands = String.fromCharCode(105,110,100,101,120,105,110,103,0);
          let with_35 = 5.0;
          let predictionz: Map<any, any> = new Map([[String.fromCharCode(117,110,107,105,99,107,0),String.fromCharCode(109,100,101,99,0)], [String.fromCharCode(97,110,105,109,97,116,101,100,0),String.fromCharCode(105,109,112,111,114,116,101,100,0)]]);
          let halfO = 1.0;
          let headery = String.fromCharCode(99,104,97,110,103,101,103,114,111,117,112,0);
         configureT = `${configureT.length % (Math.max(2, expands.length))}`;
         expands = `${predictionz.size}`;
         with_35 += (parseFloat(`${String.fromCharCode(104,0) == headery ? parseInt(`${with_35}`) : headery.length}`));
         predictionz = new Map([[`${predictionz.size}`, headery.length]]);
         halfO /= Math.max(1, parseInt(`${halfO}`) ^ headery.length);
         break;
      }
         plashy = `${1 * plashy.length}`;
         bodanu = `${configureT.length}`;
      if (bodanu.length < 1) {
         bodanu += `${plashy.length}`;
      }
          let unselectedC = false;
          let iconw = 5;
          let service7: Array<any> = [482, 664, 966];
         configureT = "2";
         unselectedC = iconw == 11;
         iconw /= Math.max(iconw << (Math.min(4, Math.abs(3))), 4);
         service7.push(iconw);
          let leagueC = 3.0;
          let m_unlockv = String.fromCharCode(102,105,112,115,0);
         bodanu = `${3 & parseInt(`${leagueC}`)}`;
         leagueC /= Math.max(3, parseFloat(`${m_unlockv.length}`));
         m_unlockv = `${1 ^ m_unlockv.length}`;
       let frame_b16 = String.fromCharCode(100,99,112,114,101,100,0);
          let rewardz: Array<any> = [String.fromCharCode(114,98,115,112,0), String.fromCharCode(115,105,103,109,97,0)];
         plashy += `${(configureT == String.fromCharCode(119,0) ? configureT.length : bodanu.length)}`;
         rewardz.push(rewardz.length);
      recommendationG /= Math.max(3, parseFloat(`${agreementl.length}`));
      break;
   }
       let loadingb = String.fromCharCode(97,100,100,105,116,105,111,110,97,108,108,121,0);
       let video9 = String.fromCharCode(119,101,98,109,0);
       let aboutA = String.fromCharCode(112,99,98,105,110,102,111,0);
          let smallN: Map<any, any> = new Map([[String.fromCharCode(100,111,99,116,111,116,97,108,0),501], [String.fromCharCode(114,101,99,101,110,116,0),222], [String.fromCharCode(103,114,101,103,111,114,105,97,110,0),837]]);
          let malaysiat = 3;
         loadingb += `${aboutA.length % (Math.max(video9.length, 6))}`;
         smallN.set(`${malaysiat}`, smallN.size << (Math.min(2, Math.abs(malaysiat))));
      while (aboutA.length < 2) {
         aboutA += `${1 % (Math.max(4, loadingb.length))}`;
         break;
      }
         loadingb = `${loadingb.length >> (Math.min(Math.abs(1), 4))}`;
          let kicke = String.fromCharCode(100,117,97,108,105,110,112,117,116,0);
         video9 = `${(String.fromCharCode(55,0) == video9 ? video9.length : loadingb.length)}`;
         kicke = `${kicke.length | kicke.length}`;
          let textW = String.fromCharCode(109,101,109,122,101,114,111,0);
         loadingb = `${1 | loadingb.length}`;
         textW += `${textW.length + textW.length}`;
          let dplusW = false;
         video9 = `${loadingb.length}`;
         dplusW = !dplusW && dplusW;
      if (video9.length > 3) {
         video9 += `${aboutA.length + 3}`;
      }
          let b_managerC = 0.0;
         aboutA = `${(aboutA == String.fromCharCode(79,0) ? aboutA.length : parseInt(`${b_managerC}`))}`;
          let unselectedj = String.fromCharCode(115,117,98,100,101,109,117,120,101,114,0);
          let club0 = false;
          let eactJ = String.fromCharCode(102,117,110,99,116,105,111,110,115,0);
         video9 += `${((club0 ? 4 : 4) + 3)}`;
         unselectedj = `${eactJ.length - 3}`;
         club0 = eactJ.length >= unselectedj.length;
      buffert -= 1;
       let full7: Array<any> = [String.fromCharCode(99,111,110,100,105,116,105,111,110,0), String.fromCharCode(100,101,108,105,118,101,114,0)];
       let projectp: Array<any> = [718, 126, 293];
       let settingn: Array<any> = [465, 29, 578];
         settingn.push(settingn.length / (Math.max(1, 7)));
         settingn.push(projectp.length + full7.length);
         full7.push(full7.length ^ projectp.length);
      if (settingn.length == full7.length) {
          let sheetU = String.fromCharCode(104,117,102,102,121,117,118,0);
         settingn.push(3);
         sheetU += `${sheetU.length}`;
      }
          let confirmatione = String.fromCharCode(101,108,101,109,115,0);
         full7.push(full7.length % 1);
         confirmatione += `${1 | confirmatione.length}`;
         projectp = [projectp.length & 2];
      for (let p = 0; p < 3; p++) {
         full7 = [projectp.length];
      }
      let pangleG = 6896259 <= settingn.length;
      do {
         settingn = [2 ^ full7.length];
         if (pangleG) {
            break;
         }
      } while (pangleG && ((projectp.length % (Math.max(3, settingn.length))) < 4 && 3 < (projectp.length % 4)));
      while ((2 << (Math.min(3, settingn.length))) <= 3 && (projectp.length << (Math.min(Math.abs(2), 4))) <= 1) {
         projectp = [projectp.length << (Math.min(settingn.length, 4))];
         break;
      }
      agreementl += `${upgradev}`;
      upgradev <<= Math.min(Math.abs(parseInt(`${appsS}`) / 3), 3);
   if (5 < (3 & agreementl.length)) {
       let streamingk = 0;
       let spinnerx = 5.0;
       let configuref = true;
       let moon6 = String.fromCharCode(119,95,49,51,95,120,111,114,101,100,0);
       let suggestionK: Map<any, any> = new Map([[String.fromCharCode(99,97,108,108,101,100,0),186], [String.fromCharCode(100,114,97,119,101,114,0),199]]);
      while (4 >= (1 * streamingk)) {
         spinnerx /= Math.max((moon6 == String.fromCharCode(75,0) ? suggestionK.size : moon6.length), 1);
         break;
      }
         suggestionK = new Map([[moon6, 2]]);
      if ((spinnerx / 3.91) == 5.2) {
          let activew = String.fromCharCode(108,95,51,56,95,104,105,115,116,111,103,114,97,109,115,0);
          let skipU = 2.0;
          let blacklistK: Array<any> = [480, 241, 739];
         streamingk *= parseInt(`${spinnerx}`);
         activew += `${parseInt(`${skipU}`)}`;
         skipU -= 2 * parseInt(`${skipU}`);
         blacklistK = [parseInt(`${skipU}`)];
      }
      if ((4 - suggestionK.size) == 4) {
         spinnerx *= parseInt(`${spinnerx}`);
      }
      let sound8 = configuref ? !configuref : configuref;
      do {
         configuref = 15 >= (streamingk / (Math.max(5, spinnerx)));
         if (sound8) {
            break;
         }
      } while (sound8 && ((2.15 / (Math.max(7, spinnerx))) == 3.27 || spinnerx == 2.15));
      if (4 < (1 * suggestionK.size)) {
          let storeA = String.fromCharCode(116,104,101,116,97,0);
         moon6 = `${storeA.length}`;
      }
      for (let z = 0; z < 1; z++) {
          let modelse = true;
          let i_centerR: Array<any> = [166, 974];
          let zoom5 = 4.0;
          let faviconh = 4.0;
          let short_w3: Array<any> = [162, 578];
         spinnerx *= 3 * suggestionK.size;
         modelse = (faviconh / (Math.max(short_w3.length, 5))) < 95.35;
         i_centerR = [((modelse ? 1 : 4) * i_centerR.length)];
         zoom5 *= parseFloat(`${parseInt(`${zoom5}`) ^ 2}`);
         faviconh /= Math.max(short_w3.length - 2, 5);
      }
         suggestionK = new Map([[`${suggestionK.size}`, suggestionK.size]]);
         configuref = spinnerx >= 42.85 && configuref;
         configuref = moon6.length < 48;
         suggestionK.set(`${moon6}`, suggestionK.size);
          let adult4 = 1.0;
          let pressureU = 1.0;
          let manifestA: Map<any, any> = new Map([[String.fromCharCode(112,111,115,116,98,111,120,0),String.fromCharCode(111,117,116,102,105,108,101,115,0)], [String.fromCharCode(102,105,108,116,0),String.fromCharCode(100,101,99,108,105,110,101,100,0)]]);
         spinnerx /= Math.max(5, 3);
         adult4 -= manifestA.size - parseInt(`${adult4}`);
         pressureU /= Math.max(2, parseFloat(`${2 - parseInt(`${pressureU}`)}`));
         manifestA = new Map([[`${manifestA.size}`, parseInt(`${pressureU}`) % (Math.max(manifestA.size, 7))]]);
      while (configuref) {
         spinnerx *= ((configuref ? 4 : 3) | moon6.length);
         break;
      }
         moon6 += `${(2 * (configuref ? 4 : 1))}`;
         configuref = 70.22 >= spinnerx || 29 >= moon6.length;
      agreementl = `${suggestionK.size}`;
   }
   while (recommendationG == upgradev) {
      recommendationG += (parseFloat(`${statisticsr == String.fromCharCode(85,0) ? statisticsr.length : agreementl.length}`));
      break;
   }
   if ((statisticsr.length / (Math.max(4, 3))) > 5 && (4.22 / (Math.max(9, appsS))) > 5.99) {
      appsS += parseFloat(`${parseInt(`${filedj}`) ^ controlsk.length}`);
   }

  return await RNFetchBlob.fs.exists(apiCacheFilePath);
};

export const getApiCache = async () => {
       let entry9: Map<any, any> = new Map([[String.fromCharCode(111,112,101,110,115,101,97,0),String.fromCharCode(108,105,107,101,108,121,0)], [String.fromCharCode(97,99,116,105,118,97,116,111,114,0),String.fromCharCode(109,97,116,99,104,101,114,95,106,95,51,56,0)], [String.fromCharCode(98,103,109,99,0),String.fromCharCode(114,101,103,105,115,116,101,114,101,114,0)]]);
    let feedback8 = 5;
    let save4: Array<any> = [651, 229, 124];
    let libcrashsdk4 = String.fromCharCode(103,108,121,112,104,115,0);
    let singapore_ = 2;
    let linku = 4;
    let singaporeQ: Array<any> = [55, 453];
    let gradlewo = true;
    let stationv: Array<any> = [458, 143];
    let footballi = 0.0;
    let mintegralg = 2.0;
       let trashU: Array<any> = [750, 141, 153];
       let layout7 = false;
       let recommendationb = 2;
      let grey9 = recommendationb <= 6410784;
      do {
         recommendationb /= Math.max(2, 2 + trashU.length);
         if (grey9) {
            break;
         }
      } while (grey9 && (3 > (recommendationb * 5) || layout7));
         trashU = [trashU.length >> (Math.min(Math.abs(1), 2))];
      for (let y = 0; y < 2; y++) {
         recommendationb >>= Math.min(2, Math.abs(recommendationb * trashU.length));
      }
         recommendationb <<= Math.min(1, trashU.length);
      while (3 < (4 % (Math.max(8, trashU.length)))) {
          let anythinkk: Map<any, any> = new Map([[String.fromCharCode(115,117,112,112,114,101,115,115,105,110,103,0),true ], [String.fromCharCode(118,101,114,115,105,111,110,101,100,0),true ]]);
         recommendationb ^= trashU.length;
         anythinkk.set(`${anythinkk.size}`, anythinkk.size);
         break;
      }
      for (let y = 0; y < 3; y++) {
         layout7 = recommendationb <= 90;
      }
      if (recommendationb >= 4 && 4 >= (recommendationb * 4)) {
         layout7 = trashU.length >= 14;
      }
       let adult_ = String.fromCharCode(109,106,112,101,103,100,101,99,0);
      let blackl = layout7 ? !layout7 : layout7;
      do {
          let tempX = String.fromCharCode(112,97,114,101,110,116,0);
          let filterv = String.fromCharCode(114,101,97,100,97,98,108,101,0);
         layout7 = filterv.length >= 61;
         tempX += `${(String.fromCharCode(53,0) == tempX ? tempX.length : tempX.length)}`;
         filterv += `${tempX.length - tempX.length}`;
         if (blackl) {
            break;
         }
      } while (blackl && (trashU.length < 2));
      gradlewo = 42 <= linku && 42 <= recommendationb;
   while ((singaporeQ.length & singapore_) > 1 || 3 > (singaporeQ.length & 1)) {
      singapore_ += 2 % (Math.max(9, entry9.size));
      break;
   }
       let b_lockB = true;
       let with_yo: Array<any> = [499, 496, 541];
      if (4 < (with_yo.length >> (Math.min(Math.abs(5), 4))) && !b_lockB) {
          let thumbnailr = String.fromCharCode(100,101,99,114,121,112,116,105,111,110,0);
         b_lockB = (((!b_lockB ? thumbnailr.length : 47) * thumbnailr.length) < 47);
      }
       let bannerf = 3.0;
       let usernameC = 5.0;
      if (2.98 < (bannerf * usernameC)) {
          let shirt0 = String.fromCharCode(102,97,117,108,116,121,0);
          let filee = String.fromCharCode(106,95,52,53,0);
          let actionZ: Array<any> = [143, 859];
          let list2 = 4.0;
          let benefitW = String.fromCharCode(103,101,111,0);
         bannerf -= parseFloat(`${with_yo.length}`);
         shirt0 += `${parseInt(`${list2}`) / 2}`;
         filee += `${benefitW.length}`;
         actionZ.push(benefitW.length);
         list2 *= parseFloat(`${parseInt(`${list2}`)}`);
      }
         bannerf -= parseFloat(`${parseInt(`${bannerf}`)}`);
      if ((2 * with_yo.length) == 1 && 2 == with_yo.length) {
         with_yo.push(1 >> (Math.min(Math.abs(parseInt(`${usernameC}`)), 5)));
      }
          let holderM = 3.0;
          let store_: Map<any, any> = new Map([[String.fromCharCode(106,95,55,49,95,117,110,115,97,116,105,115,102,105,101,100,0),881], [String.fromCharCode(110,115,116,97,110,116,0),825]]);
         b_lockB = 22.47 >= bannerf;
         holderM /= Math.max(parseFloat(`${store_.size}`), 3);
         store_ = new Map([[`${store_.size}`, store_.size & 3]]);
      stationv.push(1 - with_yo.length);
      feedback8 += parseInt(`${footballi}`);

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

   let moreq = 5554088.0 <= footballi;
   do {
      footballi += 2 + singapore_;
      if (moreq) {
         break;
      }
   } while (moreq && (5.81 >= (5.33 * footballi) && gradlewo));
   while (!gradlewo) {
      gradlewo = 91 >= save4.length;
      break;
   }
       let hooksW: Map<any, any> = new Map([[String.fromCharCode(109,101,114,103,101,97,98,108,101,0),727], [String.fromCharCode(98,105,110,107,97,117,100,105,111,0),776], [String.fromCharCode(115,97,109,112,0),248]]);
      while ((hooksW.size - hooksW.size) > 2) {
         hooksW.set(`${hooksW.size}`, hooksW.size);
         break;
      }
      for (let z = 0; z < 1; z++) {
         hooksW.set(`${hooksW.size}`, 2);
      }
          let settingv = 1.0;
          let commentk = 3;
          let securityf = String.fromCharCode(112,115,100,115,112,95,108,95,55,48,0);
         hooksW.set(`${settingv}`, 2 | securityf.length);
         settingv /= Math.max(2, 2 << (Math.min(5, Math.abs(commentk))));
         commentk >>= Math.min(Math.abs(commentk), 1);
      libcrashsdk4 = `${parseInt(`${footballi}`) | 1}`;
       let dangerV = 0;
       let condensedc: Array<any> = [584, 886, 624];
       let changeF = String.fromCharCode(114,101,111,115,0);
      let logink = 4926537 >= dangerV;
      do {
         dangerV *= condensedc.length;
         if (logink) {
            break;
         }
      } while (logink && (!condensedc.includes(dangerV)));
      for (let y = 0; y < 3; y++) {
          let photor = String.fromCharCode(105,110,115,101,114,116,105,110,103,0);
          let nativeE = 0;
          let mimoh = 5;
          let time_07 = String.fromCharCode(99,111,108,111,114,115,0);
          let questG = 5;
         changeF = `${time_07.length - questG}`;
         photor += `${nativeE << (Math.min(Math.abs(1), 4))}`;
         nativeE *= mimoh << (Math.min(Math.abs(2), 3));
         mimoh >>= Math.min(2, Math.abs(mimoh));
         time_07 += `${1 + photor.length}`;
         questG += photor.length / 3;
      }
          let networkE = String.fromCharCode(97,99,99,101,112,116,0);
          let downloading0 = 1;
         condensedc.push(2 ^ changeF.length);
         networkE = "3";
         downloading0 *= downloading0 % 3;
          let friendsZ = String.fromCharCode(99,111,108,105,110,101,97,114,0);
          let stringse = true;
         changeF += `${condensedc.length / (Math.max(changeF.length, 3))}`;
         friendsZ += "2";
         stringse = (((stringse ? friendsZ.length : 59) ^ friendsZ.length) <= 59);
          let showZ: Map<any, any> = new Map([[String.fromCharCode(115,116,117,102,102,105,110,103,0),151], [String.fromCharCode(104,95,54,52,95,115,101,99,111,110,100,97,114,121,0),71], [String.fromCharCode(112,114,111,98,101,114,0),255]]);
         changeF = `${showZ.size % (Math.max(6, changeF.length))}`;
      if ((changeF.length ^ condensedc.length) >= 1 && (changeF.length ^ condensedc.length) >= 1) {
         changeF = `${dangerV | condensedc.length}`;
      }
         condensedc = [2 | dangerV];
         condensedc = [changeF.length];
       let entryi = 1.0;
       let modelm = 3.0;
      footballi *= save4.length / 3;

  if (await getIsApiCacheExist()) {

   let const_bgt = 9260552 >= singapore_;
   do {
       let t_counts: Array<any> = [56, 853];
       let banner3 = 0.0;
       let foundD = true;
       let contextx = 1;
       let hookw = String.fromCharCode(101,120,99,101,112,116,95,100,95,52,53,0);
      if (!foundD) {
         foundD = (hookw.length - t_counts.length) <= 25;
      }
      let anythinkl = foundD ? !foundD : foundD;
      do {
         foundD = hookw.endsWith(`${contextx}`);
         if (anythinkl) {
            break;
         }
      } while (anythinkl && (!foundD));
      while (5 >= contextx) {
         t_counts.push(3);
         break;
      }
         t_counts = [contextx];
       let sportsn = 4.0;
       let plashg = 4.0;
      while ((1.40 * sportsn) < 2.89) {
         hookw += `${hookw.length}`;
         break;
      }
         hookw += `${1 % (Math.max(9, hookw.length))}`;
          let weiboa = String.fromCharCode(119,105,100,101,115,99,114,101,101,110,0);
         hookw += `${contextx << (Math.min(4, Math.abs(3)))}`;
         weiboa += `${weiboa.length}`;
      if (sportsn >= plashg) {
         sportsn -= parseFloat(`${parseInt(`${plashg}`) ^ 2}`);
      }
         banner3 /= Math.max(1, parseFloat(`${3}`));
      while (2 <= contextx) {
         foundD = (2 <= (t_counts.length << (Math.min(1, Math.abs((!foundD ? t_counts.length : 2))))));
         break;
      }
         sportsn /= Math.max(5, parseFloat(`${parseInt(`${sportsn}`) + 1}`));
      if ((4.34 - plashg) <= 1.2 || (2 + t_counts.length) <= 3) {
          let updatest: Array<any> = [825, 907, 929];
         plashg += parseFloat(`${updatest.length}`);
      }
       let fullo: Array<any> = [String.fromCharCode(109,97,116,99,104,101,100,95,97,95,54,55,0), String.fromCharCode(108,105,110,101,0), String.fromCharCode(114,116,114,101,101,99,104,101,99,107,0)];
       let taiwanL: Array<any> = [207, 75];
          let debugF = 4.0;
          let pauseg: Array<any> = [713, 716];
         sportsn /= Math.max(5, parseFloat(`${taiwanL.length}`));
         debugF += parseFloat(`${pauseg.length}`);
         pauseg.push(parseInt(`${debugF}`) - pauseg.length);
      singapore_ |= entry9.size;
      if (const_bgt) {
         break;
      }
   } while (const_bgt && ((linku | singapore_) <= 5));
      linku -= 1 - linku;
   while ((footballi - 5.51) == 4.50) {
       let castinga: Array<any> = [115, 90, 995];
       let huaweiA = String.fromCharCode(118,108,99,115,112,101,99,0);
       let yellowH: Map<any, any> = new Map([[String.fromCharCode(107,101,121,102,114,97,109,101,115,0),646], [String.fromCharCode(97,108,115,97,0),405], [String.fromCharCode(98,95,52,95,97,121,98,114,105,0),339]]);
       let hongkongF = 2.0;
      for (let r = 0; r < 2; r++) {
          let round5 = 1.0;
         huaweiA += `${castinga.length & 3}`;
         round5 *= 1 + parseInt(`${round5}`);
      }
       let crowns = String.fromCharCode(115,117,98,106,101,99,116,115,0);
       let changeD = String.fromCharCode(115,101,116,117,112,0);
         hongkongF /= Math.max(5, yellowH.size);
         yellowH.set(huaweiA, 3);
      if (yellowH.get(`${hongkongF}`) != null) {
         hongkongF /= Math.max(1 * crowns.length, 2);
      }
         castinga.push(parseInt(`${hongkongF}`));
         castinga.push(crowns.length - huaweiA.length);
         crowns = "3";
         hongkongF += (String.fromCharCode(116,0) == changeD ? castinga.length : changeD.length);
      while ((4 << (Math.min(2, castinga.length))) > 5 && 3 > (huaweiA.length << (Math.min(Math.abs(4), 5)))) {
         castinga = [crowns.length >> (Math.min(Math.abs(1), 5))];
         break;
      }
       let pathx = 3.0;
      let rightY = String.fromCharCode(103,117,52,101,52,114,50,105,117,95,0) == huaweiA;
      do {
         huaweiA += `${2 & parseInt(`${pathx}`)}`;
         if (rightY) {
            break;
         }
      } while (rightY && (5 <= (castinga.length / (Math.max(huaweiA.length, 6))) || (castinga.length / 5) <= 3));
      footballi /= Math.max(libcrashsdk4.length & 2, 1);
      break;
   }
   for (let h = 0; h < 1; h++) {
      save4 = [stationv.length & 3];
   }
    

       let singleZ = 4;
       let transferM: Array<any> = [814, 990, 8];
      if ((singleZ % 5) >= 4 || (transferM.length % 5) >= 3) {
         singleZ ^= singleZ;
      }
         singleZ += 3;
      if ((transferM.length - 3) > 5) {
         singleZ /= Math.max(singleZ + transferM.length, 5);
      }
      if (2 < transferM.length) {
          let router3 = String.fromCharCode(112,108,97,99,101,0);
         transferM.push(router3.length >> (Math.min(Math.abs(1), 5)));
      }
      if (4 > (singleZ / (Math.max(transferM.length, 4))) || (transferM.length / 4) > 4) {
          let scheduleK = 5;
          let catagoryp = 4.0;
          let closeh = 4.0;
         transferM.push(singleZ * 3);
         scheduleK %= Math.max(2, 5);
         catagoryp += parseFloat(`${parseInt(`${closeh}`) * 1}`);
         closeh += parseFloat(`${parseInt(`${catagoryp}`) - scheduleK}`);
      }
      while ((singleZ ^ transferM.length) == 5 || (transferM.length ^ 5) == 4) {
         transferM = [2 ^ transferM.length];
         break;
      }
      libcrashsdk4 = `${singleZ}`;
       let calendarz = String.fromCharCode(101,115,116,97,98,108,105,115,104,95,97,95,53,49,0);
       let condensedV = String.fromCharCode(108,95,56,48,95,110,97,110,112,97,0);
       let malaysiav = String.fromCharCode(109,105,103,114,97,116,101,100,0);
      let heart3 = malaysiav.length >= 8553278;
      do {
         malaysiav = `${calendarz.length}`;
         if (heart3) {
            break;
         }
      } while (heart3 && (2 == malaysiav.length));
         calendarz = `${calendarz.length / (Math.max(condensedV.length, 5))}`;
      while (!calendarz.includes(`${condensedV.length}`)) {
         condensedV += `${malaysiav.length | condensedV.length}`;
         break;
      }
       let trophy4 = String.fromCharCode(109,106,112,101,103,101,110,99,0);
       let default_gi = String.fromCharCode(105,95,54,51,95,113,117,97,110,116,120,0);
      for (let x = 0; x < 1; x++) {
         condensedV += `${2 * calendarz.length}`;
      }
          let themed = 2.0;
          let humidityp = 4.0;
          let popup6 = String.fromCharCode(112,111,115,116,112,114,111,99,114,101,115,0);
         condensedV = "1";
         themed *= parseFloat(`${parseInt(`${humidityp}`)}`);
         humidityp -= 3 * parseInt(`${themed}`);
         popup6 = `${parseInt(`${themed}`)}`;
      while (!trophy4.startsWith(`${default_gi.length}`)) {
          let watchm = String.fromCharCode(122,105,109,103,0);
          let line4 = 4;
          let telegramD = 5;
          let changeq = true;
          let activityT = true;
         trophy4 += `${line4 & malaysiav.length}`;
         watchm += `${((changeq ? 5 : 1))}`;
         line4 *= telegramD;
         telegramD <<= Math.min(Math.abs(3), 3);
         changeq = !activityT;
         activityT = watchm.length == 21;
         break;
      }
      let downloadingD = trophy4.length >= 6691090;
      do {
         trophy4 += `${(String.fromCharCode(99,0) == default_gi ? default_gi.length : trophy4.length)}`;
         if (downloadingD) {
            break;
         }
      } while ((!default_gi.endsWith(`${trophy4.length}`)) && downloadingD);
      let cornerp = default_gi.length >= 6418118;
      do {
         default_gi += `${1 & trophy4.length}`;
         if (cornerp) {
            break;
         }
      } while ((malaysiav == String.fromCharCode(87,0)) && cornerp);
      linku |= (condensedV == String.fromCharCode(106,0) ? condensedV.length : parseInt(`${footballi}`));
       let vietnamz: Array<any> = [1000, 874, 857];
       let mintegralJ: Map<any, any> = new Map([[String.fromCharCode(115,101,110,115,111,114,0),371], [String.fromCharCode(116,111,112,0),886]]);
       let becomer = String.fromCharCode(99,108,97,115,115,0);
      if (5 < (3 ^ vietnamz.length)) {
         vietnamz.push(vietnamz.length);
      }
      if ((1 + becomer.length) < 3) {
          let math9 = 5.0;
          let predictionO = 2.0;
          let bridgeO = String.fromCharCode(108,108,118,105,100,101,110,99,100,115,112,0);
          let securityT: Map<any, any> = new Map([[String.fromCharCode(105,100,115,0),664], [String.fromCharCode(114,116,108,0),166]]);
         vietnamz = [3 ^ parseInt(`${math9}`)];
         math9 *= parseFloat(`${securityT.size >> (Math.min(bridgeO.length, 3))}`);
         predictionO *= securityT.size;
         bridgeO = `${securityT.size + parseInt(`${predictionO}`)}`;
      }
      while (!Array.from(mintegralJ.values()).includes(vietnamz.length)) {
         vietnamz = [mintegralJ.size];
         break;
      }
          let greyY = 0.0;
          let ewardedL = 2.0;
         mintegralJ = new Map([[`${vietnamz.length}`, vietnamz.length / 1]]);
         greyY -= parseInt(`${greyY}`) >> (Math.min(2, Math.abs(2)));
         ewardedL -= parseFloat(`${parseInt(`${greyY}`)}`);
      for (let o = 0; o < 2; o++) {
         becomer += `${1 * mintegralJ.size}`;
      }
         mintegralJ.set(`${becomer}`, becomer.length);
      if ((vietnamz.length << (Math.min(Math.abs(1), 3))) > 4 || 4 > (1 << (Math.min(4, vietnamz.length)))) {
         mintegralJ = new Map([[`${mintegralJ.size}`, becomer.length]]);
      }
      for (let g = 0; g < 1; g++) {
         becomer += `${1 | vietnamz.length}`;
      }
         mintegralJ = new Map([[`${mintegralJ.size}`, vietnamz.length]]);
      gradlewo = feedback8 > 45 && 45 > entry9.size;
   while (!gradlewo) {
      gradlewo = stationv.includes(gradlewo);
      break;
   }
    const res = JSON.parse(
      await RNFetchBlob.fs.readFile(apiCacheFilePath, 'utf8'),
    );

   for (let q = 0; q < 1; q++) {
       let otherV = 2.0;
       let mbnativeadvancedz = 2;
       let buildA: Array<any> = [410, 678, 713];
       let securityt = String.fromCharCode(117,110,109,117,116,101,0);
       let adultH = String.fromCharCode(114,101,109,111,116,101,0);
      for (let i = 0; i < 2; i++) {
         buildA.push(3);
      }
          let memberW = 3.0;
          let gradle7: Array<any> = [474, 847];
         securityt += `${parseInt(`${otherV}`) ^ 3}`;
         memberW -= 3 % (Math.max(9, gradle7.length));
         gradle7 = [gradle7.length];
         mbnativeadvancedz /= Math.max(1 | buildA.length, 5);
      if (5 == (4 ^ mbnativeadvancedz)) {
         mbnativeadvancedz -= 3 << (Math.min(3, securityt.length));
      }
      while (1 >= (adultH.length * mbnativeadvancedz) && 1 >= (adultH.length * 1)) {
          let turn6 = 3;
          let flipperb: Map<any, any> = new Map([[String.fromCharCode(99,111,114,114,101,99,116,105,110,103,0),964], [String.fromCharCode(118,101,99,116,111,114,0),138], [String.fromCharCode(109,101,97,115,117,114,105,110,103,0),417]]);
          let b_imageF = 2;
         mbnativeadvancedz |= b_imageF % 2;
         turn6 |= 1 / (Math.max(1, turn6));
         flipperb = new Map([[`${flipperb.size}`, turn6]]);
         b_imageF ^= turn6 >> (Math.min(Math.abs(3), 3));
         break;
      }
         securityt += `${1 + securityt.length}`;
      if (adultH.startsWith(`${securityt.length}`)) {
         adultH += "1";
      }
         buildA = [mbnativeadvancedz];
       let typesL = 5.0;
       let champion7 = 2.0;
      while ((mbnativeadvancedz % (Math.max(buildA.length, 2))) >= 1 && (buildA.length % (Math.max(1, 1))) >= 3) {
         buildA.push(1);
         break;
      }
      while (5 <= (parseInt(`${champion7}`) * adultH.length)) {
         adultH += `${mbnativeadvancedz}`;
         break;
      }
         champion7 *= parseFloat(`${adultH.length}`);
          let fieldr = 0.0;
          let turnQ = String.fromCharCode(111,103,103,0);
          let goalC = true;
         mbnativeadvancedz |= parseInt(`${fieldr}`) << (Math.min(securityt.length, 4));
         fieldr += turnQ.length ^ 2;
         turnQ += "3";
      for (let f = 0; f < 2; f++) {
          let eventj: Map<any, any> = new Map([[String.fromCharCode(115,116,114,116,111,105,110,116,0),3], [String.fromCharCode(97,100,106,117,115,116,101,100,0),483], [String.fromCharCode(103,101,116,112,95,99,95,54,0),655]]);
         mbnativeadvancedz |= securityt.length >> (Math.min(3, buildA.length));
         eventj = new Map([[`${eventj.size}`, eventj.size | eventj.size]]);
      }
      let nalyticsI = mbnativeadvancedz <= 9596660;
      do {
         mbnativeadvancedz &= 2 / (Math.max(8, mbnativeadvancedz));
         if (nalyticsI) {
            break;
         }
      } while (nalyticsI && ((mbnativeadvancedz + 3) > 3 || (otherV - 1.91) > 3.5));
      linku %= Math.max(1, feedback8);
   }
   if ((libcrashsdk4.length + stationv.length) < 4 && (libcrashsdk4.length + 4) < 4) {
      libcrashsdk4 += `${save4.length + singapore_}`;
   }
       let vietnamt = String.fromCharCode(115,117,109,100,95,102,95,54,50,0);
       let greenK = 4;
          let clearn = 2;
          let privacyO: Map<any, any> = new Map([[String.fromCharCode(111,118,101,114,115,99,114,111,108,108,0),785], [String.fromCharCode(114,95,56,56,95,108,105,99,101,110,115,101,0),644], [String.fromCharCode(102,114,97,109,101,0),399]]);
         vietnamt += `${clearn}`;
         clearn ^= privacyO.size & 1;
         privacyO.set(`${privacyO.size}`, 1);
      let mailr = String.fromCharCode(52,56,57,117,97,107,101,122,116,102,0) == vietnamt;
      do {
         vietnamt += "1";
         if (mailr) {
            break;
         }
      } while (mailr && (vietnamt.includes(`${greenK}`)));
      let mimoj = vietnamt.length <= 9015906;
      do {
          let loadingW: Map<any, any> = new Map([[String.fromCharCode(115,101,110,100,101,114,115,0),String.fromCharCode(117,112,103,114,97,100,101,0)], [String.fromCharCode(97,114,114,97,121,115,105,122,101,0),String.fromCharCode(112,97,108,98,97,114,115,0)], [String.fromCharCode(110,105,99,101,0),String.fromCharCode(103,97,109,109,97,118,97,108,0)]]);
          let securityu = String.fromCharCode(99,108,97,115,115,105,99,0);
          let basketballN = true;
          let mutedL: Map<any, any> = new Map([[String.fromCharCode(102,114,97,110,100,111,109,95,100,95,56,56,0),808], [String.fromCharCode(109,109,120,101,120,116,0),263]]);
          let skipv: Map<any, any> = new Map([[String.fromCharCode(112,114,111,112,111,115,101,114,0),279], [String.fromCharCode(99,104,97,110,103,101,114,0),307]]);
         vietnamt = `${loadingW.size << (Math.min(Math.abs(1), 2))}`;
         loadingW = new Map([[`${skipv.size}`, 2 * skipv.size]]);
         securityu = `${skipv.size}`;
         basketballN = skipv.size < mutedL.size;
         mutedL = new Map([[`${mutedL.size}`, ((basketballN ? 1 : 5) << (Math.min(Math.abs(mutedL.size), 3)))]]);
         if (mimoj) {
            break;
         }
      } while (((1 << (Math.min(5, Math.abs(greenK)))) > 2) && mimoj);
      if (5 == (vietnamt.length >> (Math.min(4, Math.abs(greenK))))) {
         greenK -= greenK;
      }
      for (let f = 0; f < 1; f++) {
          let speck = String.fromCharCode(105,110,116,114,105,110,115,105,99,0);
          let more4 = String.fromCharCode(99,108,111,115,105,110,103,0);
         greenK /= Math.max(more4.length >> (Math.min(3, Math.abs(greenK))), 1);
         speck += `${speck.length}`;
         more4 += `${speck.length}`;
      }
          let stringS: Map<any, any> = new Map([[String.fromCharCode(105,102,97,99,101,0),950], [String.fromCharCode(97,99,116,105,118,97,116,105,111,110,115,0),901], [String.fromCharCode(97,112,105,0),954]]);
          let gmailO = String.fromCharCode(112,114,101,112,0);
          let floater4 = String.fromCharCode(118,97,114,108,101,110,103,116,104,0);
         vietnamt += `${2 & stringS.size}`;
         stringS = new Map([[floater4, 1]]);
         gmailO = `${floater4.length}`;
      save4.push(entry9.size << (Math.min(Math.abs(2), 3)));
      feedback8 >>= Math.min(singaporeQ.length, 2);
    

   if (5 > linku) {
      singaporeQ.push(stationv.length - libcrashsdk4.length);
   }
      libcrashsdk4 += `${3 | linku}`;
   if (save4.length >= 4) {
       let anythink1 = false;
       let reactnativejs_ = true;
       let hookse = 1;
      while (reactnativejs_) {
          let rewindO = String.fromCharCode(103,108,98,108,0);
          let vignetteT: Map<any, any> = new Map([[String.fromCharCode(116,95,57,48,95,107,101,121,100,105,114,0),134], [String.fromCharCode(99,97,108,108,105,110,103,0),231], [String.fromCharCode(98,95,51,54,95,117,110,109,97,112,102,105,108,101,0),516]]);
          let searchbarP: Map<any, any> = new Map([[String.fromCharCode(97,108,108,112,97,115,115,0),935], [String.fromCharCode(105,115,110,97,110,0),677]]);
         reactnativejs_ = rewindO.length > 5;
         rewindO = `${searchbarP.size | vignetteT.size}`;
         vignetteT.set(`${searchbarP.size}`, 2);
         break;
      }
         reactnativejs_ = (anythink1 ? reactnativejs_ : !anythink1);
       let module4 = 4.0;
          let long_0a2: Array<any> = [41, 303, 933];
          let borderlessQ: Array<any> = [954, 34, 228];
         hookse %= Math.max(3, hookse << (Math.min(2, Math.abs(3))));
         long_0a2 = [3];
         borderlessQ.push(1);
      while (3 > hookse) {
         module4 /= Math.max((parseFloat(`${(reactnativejs_ ? 2 : 3) >> (Math.min(1, Math.abs(2)))}`)), 2);
         break;
      }
         hookse -= parseInt(`${module4}`) + 2;
      let topon_ = anythink1 ? !anythink1 : anythink1;
      do {
          let pagination7 = 1.0;
          let charta: Map<any, any> = new Map([[String.fromCharCode(115,111,99,107,97,100,100,114,0),true ], [String.fromCharCode(111,112,116,0),true ], [String.fromCharCode(97,110,103,108,101,100,0),true ]]);
         anythink1 = charta.size > 1 && reactnativejs_;
         pagination7 -= parseFloat(`${2}`);
         charta = new Map([[`${pagination7}`, 3 >> (Math.min(Math.abs(parseInt(`${pagination7}`)), 3))]]);
         if (topon_) {
            break;
         }
      } while (topon_ && (anythink1));
          let settingK = String.fromCharCode(112,97,114,116,105,116,105,111,110,105,110,103,0);
          let slider5 = String.fromCharCode(99,111,110,116,105,110,117,111,117,115,0);
          let mbnativeadvancedG = String.fromCharCode(97,114,99,104,105,118,105,110,103,0);
         reactnativejs_ = 31 < hookse || !reactnativejs_;
         settingK = `${settingK.length}`;
         slider5 += `${(slider5 == String.fromCharCode(121,0) ? slider5.length : settingK.length)}`;
         mbnativeadvancedG = `${2 >> (Math.min(1, slider5.length))}`;
         reactnativejs_ = 70 >= hookse;
      save4 = [2 ^ singapore_];
   }
   for (let z = 0; z < 3; z++) {
      entry9 = new Map([[libcrashsdk4, parseInt(`${footballi}`) / 2]]);
   }
    return res;
  } else {

   for (let s = 0; s < 1; s++) {
       let sigmobk = true;
       let fieldv = 5.0;
       let containern = String.fromCharCode(115,112,101,99,105,97,108,0);
       let source1: Map<any, any> = new Map([[String.fromCharCode(101,95,55,95,117,112,108,111,97,100,105,110,103,0),383], [String.fromCharCode(115,117,112,101,114,103,114,111,117,112,0),932], [String.fromCharCode(105,110,116,114,111,100,117,99,116,105,111,110,0),837]]);
       let playH = String.fromCharCode(116,101,115,101,100,103,101,0);
          let orientationb = 1.0;
         source1.set(`${sigmobk}`, ((sigmobk ? 4 : 2) << (Math.min(Math.abs(parseInt(`${orientationb}`)), 5))));
         playH += `${(String.fromCharCode(84,0) == playH ? (sigmobk ? 3 : 1) : playH.length)}`;
      if (4 > (3 << (Math.min(3, Math.abs(source1.size)))) || 1 > (source1.size << (Math.min(Math.abs(3), 2)))) {
         playH = `${3 - source1.size}`;
      }
          let becomeH = false;
          let selectg = 2.0;
          let materiall = false;
         fieldv /= Math.max((parseFloat(`${playH == String.fromCharCode(113,0) ? source1.size : playH.length}`)), 4);
         becomeH = 4.46 == selectg;
         selectg *= (parseFloat(`${parseInt(`${selectg}`) << (Math.min(1, Math.abs((becomeH ? 5 : 5))))}`));
         materiall = becomeH && !materiall;
       let favorite7 = false;
       let lessb = 4.0;
       let episodeo = 4.0;
      if (fieldv >= 4.91) {
          let mbnativeadvanced9: Array<any> = [343, 777, 367];
         fieldv -= parseFloat(`${mbnativeadvanced9.length ^ containern.length}`);
      }
      while ((containern.length / 4) < 2) {
         containern += `${parseInt(`${fieldv}`) >> (Math.min(Math.abs(3), 4))}`;
         break;
      }
         lessb *= parseFloat(`${source1.size}`);
         source1 = new Map([[playH, playH.length]]);
      for (let l = 0; l < 2; l++) {
          let launcherK = String.fromCharCode(111,118,101,114,108,97,121,115,0);
         playH += `${playH.length}`;
         launcherK = `${launcherK.length % (Math.max(launcherK.length, 9))}`;
      }
      while (sigmobk) {
         episodeo -= parseFloat(`${2}`);
         break;
      }
          let stepS: Map<any, any> = new Map([[String.fromCharCode(102,95,52,53,95,97,116,116,114,97,99,116,105,111,110,0),String.fromCharCode(99,104,97,110,0)], [String.fromCharCode(99,111,110,110,101,99,116,120,0),String.fromCharCode(101,118,97,108,117,97,116,101,100,0)], [String.fromCharCode(112,114,101,115,101,110,116,97,116,105,111,110,115,0),String.fromCharCode(118,105,115,105,98,105,108,105,116,105,116,101,115,0)]]);
          let headery: Map<any, any> = new Map([[String.fromCharCode(105,100,101,110,116,105,116,105,121,0),161], [String.fromCharCode(98,117,103,115,0),241]]);
          let anneru: Array<any> = [58, 238];
         playH += `${((sigmobk ? 4 : 5) << (Math.min(4, Math.abs(1))))}`;
         stepS = new Map([[`${stepS.size}`, 3 & stepS.size]]);
         headery = new Map([[`${stepS.size}`, headery.size / 3]]);
         anneru = [2 << (Math.min(1, Math.abs(headery.size)))];
       let weiboO = 5.0;
       let bridgew = 1.0;
      let splashl = bridgew <= 6163614.0;
      do {
         bridgew /= Math.max(4, (playH == String.fromCharCode(50,0) ? parseInt(`${weiboO}`) : playH.length));
         if (splashl) {
            break;
         }
      } while (splashl && (1.6 <= (1.16 + bridgew)));
      footballi *= ((gradlewo ? 5 : 1) | 1);
   }
       let hooksY = 5;
       let orangey: Map<any, any> = new Map([[String.fromCharCode(116,108,117,116,0),769], [String.fromCharCode(117,110,99,108,105,112,112,101,100,0),463]]);
       let tooltips0 = String.fromCharCode(105,110,100,105,99,97,116,111,114,0);
         orangey = new Map([[`${hooksY}`, (String.fromCharCode(74,0) == tooltips0 ? tooltips0.length : hooksY)]]);
      while (tooltips0.endsWith(`${orangey.size}`)) {
         tooltips0 += `${orangey.size / 3}`;
         break;
      }
         hooksY <<= Math.min(5, Math.abs(3));
       let navigationi = 4;
         tooltips0 = "3";
         orangey.set(tooltips0, 1 >> (Math.min(4, tooltips0.length)));
      while (4 < (tooltips0.length & 5) || (tooltips0.length & 5) < 2) {
         navigationi ^= 3;
         break;
      }
         tooltips0 += `${tooltips0.length % 3}`;
          let shootz = 3.0;
         hooksY -= orangey.size % 1;
         shootz -= parseFloat(`${3}`);
      feedback8 /= Math.max(2, singaporeQ.length * save4.length);
      linku |= 3 & save4.length;
       let c_titleK = String.fromCharCode(116,95,49,49,95,115,116,114,101,97,109,105,110,103,0);
         c_titleK += `${c_titleK.length ^ c_titleK.length}`;
          let searchp: Map<any, any> = new Map([[String.fromCharCode(115,99,111,112,101,0),703], [String.fromCharCode(99,97,108,108,111,117,116,0),662]]);
         c_titleK += "2";
         searchp.set(`${searchp.size}`, searchp.size + 3);
         c_titleK += `${3 << (Math.min(1, c_titleK.length))}`;
      save4 = [linku >> (Math.min(4, Math.abs(2)))];
    return [];
  }
};

export const batchAddVodToApiCache = async (vod: ttAnimationSource[]) => {
  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';
  try {
    if (!(await getIsApiCacheExist())) {
      await RNFetchBlob.fs.createFile(apiCacheFilePath, '[]', 'utf8');
    }
  } catch (e) {
       let storee = String.fromCharCode(100,97,114,107,0);
    let telegrams: Array<any> = [407, 65];
    let detail0: Map<any, any> = new Map([[String.fromCharCode(103,117,97,114,97,110,116,101,101,0),String.fromCharCode(118,97,108,115,0)], [String.fromCharCode(122,95,49,55,95,99,104,97,114,115,101,116,0),String.fromCharCode(108,97,100,100,101,114,115,116,101,112,0)]]);
    let catalogs: Map<any, any> = new Map([[String.fromCharCode(108,97,116,116,105,99,101,0),989], [String.fromCharCode(105,110,102,108,97,116,101,0),733]]);
    let link8 = String.fromCharCode(114,101,110,97,109,101,0);
    let filterQ = false;
    let settingsh = String.fromCharCode(97,100,100,0);
    let selectionK = 3.0;
    let phonei = 0.0;
    let readu = 2.0;
   while (3 > (parseInt(`${selectionK}`) + catalogs.size) || 5 > (3 >> (Math.min(4, Math.abs(catalogs.size))))) {
      catalogs.set(`${filterQ}`, ((filterQ ? 3 : 1) | link8.length));
      break;
   }
   if (3 == (detail0.size + storee.length) && 3 == (3 + storee.length)) {
       let foundK = 0.0;
       let changeJ = String.fromCharCode(111,118,101,114,108,97,112,115,0);
       let incident9 = String.fromCharCode(105,110,105,116,97,108,105,122,101,0);
      for (let n = 0; n < 2; n++) {
         changeJ += `${changeJ.length / (Math.max(3, 3))}`;
      }
         foundK += parseFloat(`${changeJ.length << (Math.min(4, Math.abs(parseInt(`${foundK}`))))}`);
      if (incident9.length <= changeJ.length) {
          let single0 = true;
         changeJ += `${incident9.length - 1}`;
      }
         incident9 = "2";
         changeJ += `${changeJ.length}`;
      detail0.set(storee, telegrams.length % 2);
   }
   while (detail0.size <= 2) {
       let buildI = String.fromCharCode(110,112,114,111,98,101,0);
       let overlayt: Array<any> = [939, 193];
       let long_mh = String.fromCharCode(114,101,118,101,114,115,101,0);
       let benefitD = 4.0;
       let specF = String.fromCharCode(112,114,111,102,105,108,105,110,103,95,107,95,53,49,0);
         overlayt.push(parseInt(`${benefitD}`));
         specF += `${parseInt(`${benefitD}`)}`;
      let blackB = String.fromCharCode(52,110,107,51,99,0) == long_mh;
      do {
         long_mh = `${parseInt(`${benefitD}`)}`;
         if (blackB) {
            break;
         }
      } while ((long_mh.length > 1) && blackB);
       let layoutF = String.fromCharCode(116,95,57,0);
          let mailS = String.fromCharCode(99,108,101,97,110,115,101,0);
          let telegramS = 4.0;
         overlayt = [mailS.length / (Math.max(3, 9))];
         mailS += `${3 >> (Math.min(Math.abs(parseInt(`${telegramS}`)), 4))}`;
         telegramS *= parseFloat(`${parseInt(`${telegramS}`)}`);
         specF = `${3 << (Math.min(1, Math.abs(parseInt(`${benefitD}`))))}`;
       let u_lock9 = 5.0;
       let zoom4 = 4.0;
         buildI += "1";
         benefitD += (parseFloat(`${String.fromCharCode(103,0) == buildI ? buildI.length : parseInt(`${u_lock9}`)}`));
         specF += `${(specF == String.fromCharCode(115,0) ? specF.length : parseInt(`${benefitD}`))}`;
       let orientationv = 2.0;
       let buildf = 4;
       let backwardn = 2;
         zoom4 /= Math.max(overlayt.length >> (Math.min(2, Math.abs(parseInt(`${zoom4}`)))), 2);
      if ((4 | backwardn) < 5) {
         backwardn += specF.length | layoutF.length;
      }
         benefitD /= Math.max(parseFloat(`${parseInt(`${benefitD}`)}`), 1);
      settingsh = `${(String.fromCharCode(49,0) == buildI ? buildI.length : overlayt.length)}`;
      break;
   }
   if ((catalogs.size >> (Math.min(Math.abs(1), 4))) > 4 || (catalogs.size >> (Math.min(Math.abs(1), 4))) > 4) {
       let recommendationF = String.fromCharCode(97,100,100,99,111,110,115,116,0);
         recommendationF = `${recommendationF.length}`;
       let gestures_ = false;
       let stringsW = 4.0;
       let inactive6 = 3.0;
      catalogs = new Map([[`${telegrams.length}`, telegrams.length % 2]]);
   }
      filterQ = telegrams.length < 89;
   if (3 >= storee.length) {
       let trophy6 = String.fromCharCode(115,101,101,110,0);
       let privacym = 3.0;
       let gpayw = 3.0;
       let pointJ = 3.0;
          let uploadY = String.fromCharCode(99,111,110,99,114,101,116,101,0);
         privacym += uploadY.length;
       let internet2 = false;
       let gradlewM = true;
          let statsg = 1.0;
          let bufferA = 3.0;
          let main_tP: Map<any, any> = new Map([[String.fromCharCode(111,112,101,110,115,115,108,118,0),931], [String.fromCharCode(114,97,112,112,101,114,0),664], [String.fromCharCode(114,100,106,112,103,99,111,109,0),800]]);
         internet2 = 63.17 > pointJ;
         statsg *= parseFloat(`${parseInt(`${statsg}`) * main_tP.size}`);
         bufferA *= parseInt(`${statsg}`);
         main_tP.set(`${statsg}`, parseInt(`${statsg}`) ^ parseInt(`${bufferA}`));
      while (!internet2) {
         gradlewM = (85 > (trophy6.length & (internet2 ? trophy6.length : 85)));
         break;
      }
      if ((4.6 - gpayw) > 3.63 && gpayw > 4.6) {
          let materiala: Map<any, any> = new Map([[String.fromCharCode(109,95,57,51,95,106,99,115,97,109,112,108,101,0),String.fromCharCode(99,100,97,116,97,0)], [String.fromCharCode(99,111,108,111,114,109,97,112,0),String.fromCharCode(109,105,115,109,97,116,99,104,101,115,0)], [String.fromCharCode(99,117,116,111,102,102,0),String.fromCharCode(99,108,97,105,109,115,0)]]);
          let complete1: Map<any, any> = new Map([[String.fromCharCode(105,110,108,105,110,107,0),782], [String.fromCharCode(118,101,110,117,101,115,0),280]]);
         internet2 = gradlewM;
         materiala = new Map([[`${materiala.size}`, 3]]);
         complete1.set(`${complete1.size}`, materiala.size - complete1.size);
      }
         pointJ -= parseInt(`${gpayw}`) / 2;
      while (3.2 > (pointJ - gpayw) && 2.40 > (3.2 - pointJ)) {
         gpayw /= Math.max(parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${privacym}`)), 5))}`), 1);
         break;
      }
      while (gradlewM) {
         gradlewM = 25 == trophy6.length || pointJ == 14.18;
         break;
      }
      let anytimew = 7440298 >= trophy6.length;
      do {
          let selly = 0.0;
          let storeF = 1.0;
          let dragW = 4.0;
         trophy6 = `${trophy6.length}`;
         selly /= Math.max(3, parseFloat(`${parseInt(`${dragW}`) >> (Math.min(5, Math.abs(parseInt(`${storeF}`))))}`));
         storeF *= parseFloat(`${parseInt(`${selly}`)}`);
         dragW /= Math.max(5, parseFloat(`${parseInt(`${storeF}`) % (Math.max(parseInt(`${dragW}`), 9))}`));
         if (anytimew) {
            break;
         }
      } while ((5.35 < (5.96 + gpayw)) && anytimew);
      if (privacym >= 4.93) {
         gradlewM = 42.0 > privacym && pointJ > 42.0;
      }
      if (1.95 == gpayw && 2.88 == (1.95 - gpayw)) {
         gradlewM = 94.24 >= gpayw && gradlewM;
      }
      if ((privacym / (Math.max(4.50, 5))) >= 1.89) {
         trophy6 += `${((gradlewM ? 4 : 3) - parseInt(`${pointJ}`))}`;
      }
      settingsh = `${parseInt(`${pointJ}`) / (Math.max(9, storee.length))}`;
   }
       let referrer9: Map<any, any> = new Map([[String.fromCharCode(101,120,116,101,114,105,111,114,0),505], [String.fromCharCode(108,97,114,112,111,108,121,0),933], [String.fromCharCode(105,110,115,112,101,99,116,0),621]]);
       let e_positiona: Map<any, any> = new Map([[String.fromCharCode(109,97,110,97,103,101,0),152], [String.fromCharCode(116,100,108,115,0),582]]);
          let hongkongc: Array<any> = [575, 556];
          let shootK = String.fromCharCode(105,110,118,111,99,97,116,105,111,110,115,0);
          let build3 = false;
         referrer9.set(shootK, (shootK == String.fromCharCode(114,0) ? referrer9.size : shootK.length));
         hongkongc.push(2);
         build3 = ((hongkongc.length / (Math.max(6, (!build3 ? 82 : hongkongc.length)))) >= 82);
         referrer9.set(`${e_positiona.size}`, 3);
      while (!Array.from(e_positiona.values()).includes(referrer9.size)) {
          let vignetteN = String.fromCharCode(109,97,107,101,109,97,99,112,107,103,95,48,95,50,55,0);
          let termsa = 5.0;
          let heartW = true;
          let reactK = 2.0;
         e_positiona = new Map([[`${referrer9.size}`, referrer9.size ^ parseInt(`${termsa}`)]]);
         vignetteN = `${((heartW ? 5 : 4) | parseInt(`${reactK}`))}`;
         termsa -= parseFloat(`${parseInt(`${reactK}`)}`);
         heartW = 4.78 < reactK && heartW;
         break;
      }
         e_positiona.set(`${referrer9.size}`, e_positiona.size << (Math.min(1, Math.abs(referrer9.size))));
      while (1 < (4 / (Math.max(10, referrer9.size)))) {
          let overlayp = true;
          let catagoryD = String.fromCharCode(117,108,116,105,0);
          let fieldV = 4.0;
          let vnewinterstitial9 = 3.0;
         e_positiona = new Map([[`${e_positiona.size}`, 2]]);
         overlayp = fieldV > 38.59;
         catagoryD += `${((overlayp ? 4 : 5))}`;
         fieldV += parseFloat(`${3}`);
         vnewinterstitial9 -= parseInt(`${fieldV}`) % 2;
         break;
      }
          let notificationL = true;
          let hooks7 = 1;
          let transferA = 2.0;
         e_positiona.set(`${transferA}`, hooks7 | 3);
         notificationL = notificationL || notificationL;
         hooks7 &= ((notificationL ? 5 : 4) ^ 3);
      settingsh = `${3 * telegrams.length}`;
       let activityX = 1.0;
      while ((activityX + 5.58) <= 3.17) {
          let action7 = String.fromCharCode(100,99,97,100,99,116,0);
          let g_viewh: Map<any, any> = new Map([[String.fromCharCode(99,111,110,99,101,97,108,101,100,0),440], [String.fromCharCode(116,114,117,116,104,95,119,95,53,54,0),972]]);
          let previewe = 5.0;
          let teamC = 1.0;
         activityX += action7.length * g_viewh.size;
         action7 = `${parseInt(`${previewe}`) % 2}`;
         g_viewh.set(`${teamC}`, parseInt(`${teamC}`));
         previewe /= Math.max(3, parseInt(`${teamC}`));
         break;
      }
      for (let a = 0; a < 1; a++) {
         activityX *= 3;
      }
      let ewardedZ = 8125075.0 <= activityX;
      do {
          let auto_fG = 0.0;
         activityX *= 1;
         auto_fG /= Math.max(parseInt(`${auto_fG}`), 5);
         if (ewardedZ) {
            break;
         }
      } while ((5.78 >= (activityX + activityX)) && ewardedZ);
      filterQ = activityX <= 93.90 || !filterQ;
      link8 += `${parseInt(`${phonei}`) & 1}`;
   if (filterQ) {
      catalogs = new Map([[`${detail0.size}`, storee.length]]);
   }
       let statsM = String.fromCharCode(102,97,99,116,111,114,0);
         statsM = `${statsM.length & 2}`;
         statsM = `${statsM.length}`;
      while (statsM.length > 5) {
         statsM = `${statsM.length / (Math.max(1, 5))}`;
         break;
      }
      telegrams = [link8.length];
       let giftS = 2.0;
       let updatesU = 0;
       let inactivej = 4.0;
          let modelsh = 3.0;
          let reducerm = false;
          let baidu7: Array<any> = [164, 487];
         updatesU *= 2;
         modelsh += (parseFloat(`${(reducerm ? 3 : 3) * baidu7.length}`));
         reducerm = baidu7.length <= 66;
      for (let e = 0; e < 2; e++) {
          let change3 = 4.0;
          let volumet = 3.0;
          let trophyQ = String.fromCharCode(112,101,114,0);
          let aboutK = String.fromCharCode(97,112,112,101,110,100,97,98,108,101,0);
          let gestures8: Map<any, any> = new Map([[String.fromCharCode(103,108,97,115,115,0),984], [String.fromCharCode(113,117,97,108,105,102,105,101,114,115,0),305], [String.fromCharCode(112,95,56,50,95,119,105,116,104,0),491]]);
         inactivej /= Math.max(parseFloat(`${parseInt(`${change3}`) << (Math.min(2, Math.abs(2)))}`), 5);
         change3 *= trophyQ.length;
         volumet *= aboutK.length >> (Math.min(Math.abs(2), 1));
         trophyQ = `${aboutK.length}`;
         gestures8.set(aboutK, 2);
      }
       let footballV = 1;
       let bodanK = 0;
         inactivej /= Math.max(2, parseFloat(`${footballV}`));
      for (let k = 0; k < 2; k++) {
         bodanK >>= Math.min(4, Math.abs(parseInt(`${inactivej}`) % 3));
      }
      while ((giftS / (Math.max(inactivej, 4))) <= 5.2) {
          let descQ = 3;
          let with_ts = String.fromCharCode(108,101,116,116,101,114,0);
          let loadingJ = String.fromCharCode(97,98,115,101,105,108,0);
          let sendk = 5.0;
          let routerg = 2.0;
         inactivej += parseFloat(`${bodanK}`);
         descQ ^= 2;
         with_ts += "3";
         loadingJ = `${descQ}`;
         sendk -= parseFloat(`${2 % (Math.max(parseInt(`${sendk}`), 3))}`);
         routerg -= 2 % (Math.max(7, descQ));
         break;
      }
      for (let c = 0; c < 1; c++) {
          let bottomt = String.fromCharCode(110,97,110,111,115,101,99,111,110,100,115,0);
          let dangerU = String.fromCharCode(110,95,56,51,95,97,115,115,101,115,115,109,101,110,116,0);
          let alert1 = String.fromCharCode(115,115,101,100,105,102,102,0);
          let bridgew: Map<any, any> = new Map([[String.fromCharCode(101,102,102,101,99,116,105,118,101,108,121,0),String.fromCharCode(99,95,51,52,95,115,97,116,117,114,97,116,101,0)], [String.fromCharCode(102,105,102,111,115,0),String.fromCharCode(99,95,50,53,95,97,114,116,119,111,114,107,0)]]);
         inactivej -= parseFloat(`${bottomt.length}`);
         bottomt += `${bridgew.size}`;
         dangerU = `${alert1.length | 2}`;
         alert1 = `${dangerU.length % (Math.max(3, 1))}`;
         bridgew = new Map([[`${bridgew.size}`, (String.fromCharCode(89,0) == alert1 ? bridgew.size : alert1.length)]]);
      }
      if ((3 + bodanK) > 2) {
         bodanK -= 1 - parseInt(`${giftS}`);
      }
          let animationY = 1.0;
          let bannerZ = 1;
         giftS -= parseFloat(`${parseInt(`${inactivej}`) + parseInt(`${giftS}`)}`);
         animationY += bannerZ;
         bannerZ -= parseInt(`${animationY}`) % (Math.max(10, bannerZ));
      catalogs.set(`${updatesU}`, 2 + updatesU);
   let resendY = filterQ ? !filterQ : filterQ;
   do {
      filterQ = (link8.length & detail0.size) == 24;
      if (resendY) {
         break;
      }
   } while ((!settingsh.endsWith(`${filterQ}`)) && resendY);
   while (2 == (detail0.size / (Math.max(2, 7))) || (2 / (Math.max(4, catalogs.size))) == 3) {
      catalogs.set(`${phonei}`, 2 * detail0.size);
      break;
   }
   if (3 >= (detail0.size + 5) && detail0.size >= 5) {
       let carouselK = String.fromCharCode(105,110,116,112,0);
       let libcrashsdkD = String.fromCharCode(99,97,116,99,104,0);
       let dplusw = 3;
      while (libcrashsdkD.length == 3) {
         carouselK = `${carouselK.length}`;
         break;
      }
      let analyticT = String.fromCharCode(101,104,97,0) == carouselK;
      do {
         carouselK = "2";
         if (analyticT) {
            break;
         }
      } while (analyticT && (carouselK.length <= 3));
      let orientation0 = 6530949 <= libcrashsdkD.length;
      do {
         libcrashsdkD += `${libcrashsdkD.length - dplusw}`;
         if (orientation0) {
            break;
         }
      } while ((libcrashsdkD.length < dplusw) && orientation0);
         dplusw += 1 ^ libcrashsdkD.length;
         libcrashsdkD = `${libcrashsdkD.length}`;
      while (3 <= (dplusw | libcrashsdkD.length) || 2 <= (libcrashsdkD.length | 3)) {
          let signinupp = 4.0;
          let ballK: Array<any> = [27, 490];
          let sliderh = false;
          let textY = 3.0;
          let zoomY = 0.0;
         dplusw %= Math.max(((sliderh ? 1 : 2) % 2), 2);
         signinupp += 3;
         ballK = [ballK.length];
         sliderh = 49.84 <= signinupp;
         textY -= parseInt(`${zoomY}`);
         zoomY /= Math.max(parseFloat(`${ballK.length}`), 5);
         break;
      }
       let delegate_sI = String.fromCharCode(99,111,109,98,105,110,101,0);
       let moonX = 2.0;
         libcrashsdkD = `${parseInt(`${moonX}`) % (Math.max(3, 10))}`;
      filterQ = 43 >= libcrashsdkD.length;
   }
   for (let r = 0; r < 2; r++) {
      storee = `${telegrams.length % (Math.max(link8.length, 5))}`;
   }
       let clock8 = 0.0;
          let descv = String.fromCharCode(98,105,110,107,95,109,95,53,55,0);
          let backi = 4;
          let default_9n5 = 3.0;
         clock8 *= parseFloat(`${parseInt(`${default_9n5}`)}`);
         descv = `${descv.length | backi}`;
         backi &= backi | descv.length;
         default_9n5 += parseFloat(`${backi + 3}`);
      let mbjscommonQ = 9880761.0 >= clock8;
      do {
          let emojiV = String.fromCharCode(114,101,97,108,0);
          let actionz = String.fromCharCode(118,111,105,100,0);
         clock8 *= (parseFloat(`${emojiV == String.fromCharCode(71,0) ? parseInt(`${clock8}`) : emojiV.length}`));
         actionz = `${actionz.length / 1}`;
         if (mbjscommonQ) {
            break;
         }
      } while (((clock8 / (Math.max(1, clock8))) < 1.96) && mbjscommonQ);
          let minimizem = 3.0;
          let updatesZ = 4.0;
         clock8 += parseFloat(`${parseInt(`${updatesZ}`) - 1}`);
         minimizem -= parseFloat(`${parseInt(`${minimizem}`)}`);
         updatesZ -= parseFloat(`${parseInt(`${minimizem}`)}`);
      telegrams = [storee.length - 2];
   for (let c = 0; c < 3; c++) {
       let tickedH = 1;
       let photoK = false;
       let gpayf = 2;
      while (photoK) {
          let pageq = 1.0;
          let klevinX = 3.0;
          let inviteC = String.fromCharCode(112,95,52,48,95,99,97,114,100,97,110,111,0);
         photoK = gpayf == 38 && pageq == 79.25;
         pageq += parseFloat(`${parseInt(`${klevinX}`)}`);
         klevinX /= Math.max((parseFloat(`${String.fromCharCode(119,0) == inviteC ? parseInt(`${klevinX}`) : inviteC.length}`)), 2);
         break;
      }
      let common9 = 5240323 >= tickedH;
      do {
         tickedH /= Math.max(1, 1);
         if (common9) {
            break;
         }
      } while (common9 && (5 >= tickedH));
          let preview0 = String.fromCharCode(115,105,109,105,108,97,114,105,116,121,0);
          let mbnativec = String.fromCharCode(115,101,116,115,104,97,114,101,0);
          let disconnectedO = false;
         gpayf += gpayf;
         preview0 += `${preview0.length}`;
         mbnativec = `${((disconnectedO ? 5 : 5) / 2)}`;
         disconnectedO = (((!disconnectedO ? 21 : preview0.length) >> (Math.min(preview0.length, 5))) == 21);
      while ((tickedH << (Math.min(Math.abs(4), 4))) == 4 && !photoK) {
          let gemfile0 = true;
         tickedH ^= tickedH;
         gemfile0 = !gemfile0 && !gemfile0;
         break;
      }
         tickedH |= ((photoK ? 2 : 3) + gpayf);
      catalogs.set(link8, settingsh.length / (Math.max(2, 10)));
   }
      selectionK *= parseFloat(`${parseInt(`${selectionK}`) & 1}`);

    
  }
  const apiCacheContentStr = await RNFetchBlob.fs.readFile(
    apiCacheFilePath,
    'utf8',
  );
  const apiCacheContent: ttAnimationSource[] = JSON.parse(apiCacheContentStr);
  
  if (apiCacheContent.length < TOTAL_VIDEO_TO_DOWNLOAD) {
    const newArr = removeDuplicateObjects(apiCacheContent.concat(vod));
    
    await RNFetchBlob.fs.writeFile(apiCacheFilePath, JSON.stringify(newArr));
  } else {
    
    
  }
};

function removeDuplicateObjects(array: ttAnimationSource[]): ttAnimationSource[] {
  const seenIds = new Set<number>();

  return array.filter(obj => {
    if (!seenIds.has(obj.mini_video_id)) {
      seenIds.add(obj.mini_video_id);
      return true;
    }
    return false;
  });
}

const deleteVodFromApiCache = async (id: string) => {
  if (!(await getIsApiCacheExist())) return;
  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';
  const apiCacheContentStr = await RNFetchBlob.fs.readFile(
    apiCacheFilePath,
    'utf8',
  );
  const apiCacheContent: ttAnimationSource[] = JSON.parse(apiCacheContentStr);

  console.debug('removing vod from api cache', id);

  const filteredCacheContent = apiCacheContent.filter(
    vod => vod.mini_video_id.toString() !== id,
  );
  await RNFetchBlob.fs.writeFile(
    apiCacheFilePath,
    JSON.stringify(filteredCacheContent),
  );
};

export const clearMinivodApiCache = async () => {
       let connectionf = String.fromCharCode(104,115,98,0);
    let calendarG = 0.0;
    let detailP = String.fromCharCode(112,114,101,102,101,114,0);
    let sansx = 1.0;
    let ncopy_s6 = String.fromCharCode(105,95,49,50,95,117,110,102,101,116,99,104,0);
    let holderi = 4;
    let malaysiaL = 5.0;
    let overM = 0;
    let serviceV = true;
    let benefitb: Map<any, any> = new Map([[String.fromCharCode(115,119,105,122,122,108,105,110,103,0),524], [String.fromCharCode(110,95,56,54,95,102,111,108,100,0),988], [String.fromCharCode(109,98,109,111,100,101,0),488]]);
   if (1 >= (3 >> (Math.min(3, ncopy_s6.length))) && (holderi >> (Math.min(Math.abs(3), 5))) >= 4) {
      holderi += parseInt(`${calendarG}`);
   }
   if (5.12 <= (calendarG - 2)) {
      malaysiaL -= parseFloat(`${holderi * detailP.length}`);
   }
   while (1.73 > (sansx / 3.85)) {
      sansx /= Math.max(parseFloat(`${detailP.length % 1}`), 1);
      break;
   }
       let gemfileU = true;
       let singlef: Map<any, any> = new Map([[String.fromCharCode(108,111,111,112,101,120,105,116,0),5], [String.fromCharCode(117,112,108,111,97,100,101,114,0),913], [String.fromCharCode(102,99,112,117,98,108,105,115,104,0),892]]);
       let twitterA: Array<any> = [String.fromCharCode(101,108,105,115,116,0), String.fromCharCode(114,97,119,100,101,99,0)];
          let favoriteL = String.fromCharCode(101,100,105,116,0);
         singlef = new Map([[`${twitterA.length}`, twitterA.length * 3]]);
         favoriteL = `${favoriteL.length}`;
       let station8 = false;
       let delegate_reB = true;
         station8 = !gemfileU;
          let coreV = 1.0;
         gemfileU = 39.85 >= coreV || delegate_reB;
      for (let c = 0; c < 3; c++) {
         singlef.set(`${station8}`, 1 + twitterA.length);
      }
         station8 = (((station8 ? 62 : twitterA.length) / (Math.max(twitterA.length, 6))) == 62);
      while (!Array.from(singlef.values()).includes(twitterA.length)) {
         twitterA.push(((gemfileU ? 5 : 3)));
         break;
      }
         twitterA.push((1 | (station8 ? 2 : 2)));
         twitterA.push(2);
      detailP += `${parseInt(`${sansx}`) / 3}`;
       let expiredn = 4;
       let containeri: Array<any> = [849, 917, 249];
       let sideH = String.fromCharCode(99,111,117,110,116,114,121,0);
         expiredn /= Math.max(4, sideH.length);
      let floatere = 9847366 <= expiredn;
      do {
         expiredn &= containeri.length;
         if (floatere) {
            break;
         }
      } while (floatere && (containeri.includes(expiredn)));
      for (let g = 0; g < 1; g++) {
          let profile6 = String.fromCharCode(112,95,50,0);
          let policyA = true;
          let updatesr = String.fromCharCode(109,117,108,120,0);
         containeri.push(3);
         profile6 += `${((policyA ? 1 : 1) - 3)}`;
         policyA = profile6.length > 39;
         updatesr += "1";
      }
       let routere = String.fromCharCode(97,112,112,108,105,99,97,116,105,111,110,0);
         containeri = [expiredn + containeri.length];
       let blackc = false;
       let signinupK = true;
         routere = `${((signinupK ? 2 : 2))}`;
      let sendH = blackc ? !blackc : blackc;
      do {
          let appley: Map<any, any> = new Map([[String.fromCharCode(108,101,114,116,0),String.fromCharCode(112,111,108,121,109,101,115,104,0)], [String.fromCharCode(97,109,101,120,0),String.fromCharCode(103,117,97,114,100,0)]]);
          let trashh: Array<any> = [700, 453, 512];
         blackc = String.fromCharCode(82,0) == routere;
         appley = new Map([[`${appley.size}`, trashh.length ^ appley.size]]);
         trashh = [trashh.length];
         if (sendH) {
            break;
         }
      } while ((5 > (expiredn | 3) && expiredn > 3) && sendH);
      for (let p = 0; p < 3; p++) {
          let window_8t = String.fromCharCode(100,101,115,116,114,117,99,116,0);
          let championv = 4.0;
          let linkj: Array<any> = [String.fromCharCode(114,101,97,99,116,105,111,110,0), String.fromCharCode(114,101,99,111,110,0)];
          let agreementc = true;
         signinupK = routere == String.fromCharCode(103,0);
         window_8t += `${linkj.length}`;
         championv /= Math.max(2 | parseInt(`${championv}`), 5);
         linkj.push((String.fromCharCode(65,0) == window_8t ? linkj.length : window_8t.length));
         agreementc = 8 < linkj.length;
      }
      detailP = `${sideH.length >> (Math.min(3, Math.abs(holderi)))}`;

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

   if ((1 + parseInt(`${sansx}`)) > 1 || 3.13 > (sansx + 1.65)) {
      sansx *= parseFloat(`${3 >> (Math.min(1, Math.abs(holderi)))}`);
   }
   while (ncopy_s6 == detailP) {
       let refreshp = String.fromCharCode(114,97,100,105,111,0);
       let whistlea = 2.0;
          let regengX = String.fromCharCode(110,111,110,98,0);
          let connectionfb = 5.0;
          let videocommonA = String.fromCharCode(98,111,100,101,114,0);
         refreshp = `${parseInt(`${connectionfb}`)}`;
         regengX += `${1 | regengX.length}`;
         connectionfb -= videocommonA.length;
         videocommonA += `${videocommonA.length << (Math.min(2, regengX.length))}`;
      let o_center3 = whistlea <= 5039340.0;
      do {
          let actionsE: Map<any, any> = new Map([[String.fromCharCode(101,120,116,114,97,112,111,108,97,116,101,0),false ], [String.fromCharCode(104,101,97,100,105,110,103,0),false ], [String.fromCharCode(101,110,116,105,116,101,115,0),false ]]);
          let borderless2 = 5.0;
          let launcher5 = 3;
          let close2: Map<any, any> = new Map([[String.fromCharCode(120,95,53,50,95,118,97,108,105,100,97,116,105,110,103,0),147], [String.fromCharCode(116,105,108,105,110,103,0),288], [String.fromCharCode(115,116,117,98,0),428]]);
          let hoverG = String.fromCharCode(109,97,112,108,105,109,105,116,0);
         whistlea *= (String.fromCharCode(75,0) == hoverG ? launcher5 : hoverG.length);
         actionsE.set(`${borderless2}`, parseInt(`${borderless2}`));
         launcher5 |= parseInt(`${borderless2}`);
         close2.set(`${borderless2}`, 3);
         if (o_center3) {
            break;
         }
      } while (o_center3 && ((2.88 - whistlea) <= 1.34));
         refreshp += `${parseInt(`${whistlea}`)}`;
         whistlea *= 3;
      let actions8 = 8321536 <= refreshp.length;
      do {
         refreshp += `${parseInt(`${whistlea}`)}`;
         if (actions8) {
            break;
         }
      } while (((whistlea / (Math.max(5.97, 4))) == 1.10) && actions8);
         refreshp += `${refreshp.length}`;
      detailP = `${ncopy_s6.length & detailP.length}`;
      break;
   }
      sansx += parseFloat(`${detailP.length}`);
      malaysiaL += (parseFloat(`${String.fromCharCode(81,0) == ncopy_s6 ? ncopy_s6.length : parseInt(`${calendarG}`)}`));
   for (let d = 0; d < 2; d++) {
      holderi ^= 3;
   }
  await RNFetchBlob.fs.writeFile(
    apiCacheFilePath,
    JSON.stringify([]),
  );
}