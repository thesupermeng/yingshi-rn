import {mayi_Small} from '@type/mayi_green';
import {DOWNLOAD_BATCH_SIZE, TOTAL_VIDEO_TO_DOWNLOAD} from '@utility/mayi_middleware_apps';
import chunk from 'lodash/chunk';
import RNFetchBlob from 'rn-fetch-blob';

async function downloadVod(vod: mayi_Small) {
  if (vod.is_ads) return 
  const fileLocation =
    RNFetchBlob.fs.dirs.DocumentDir + `/videocache/${vod.mini_video_id}.mp4`;
  const temp =
    RNFetchBlob.fs.dirs.DocumentDir + `/partial/${vod.mini_video_id}`;
  const cacheFolder = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';

  if (await RNFetchBlob.fs.exists(cacheFolder)) {
    if (
      (await RNFetchBlob.fs.ls(cacheFolder)).length >= TOTAL_VIDEO_TO_DOWNLOAD
    ) {
      
      return;
    }
  } else {
    try {
      await RNFetchBlob.fs.mkdir(cacheFolder);
    } catch (e) {
       let splashK = true;
    let entryk: Map<any, any> = new Map([[String.fromCharCode(115,117,112,101,114,95,99,95,49,53,0),976], [String.fromCharCode(122,95,55,53,95,111,98,116,97,105,110,0),616], [String.fromCharCode(101,110,101,114,103,121,95,48,95,55,54,0),387]]);
    let becomeq = String.fromCharCode(109,95,54,55,95,114,101,109,97,116,114,105,120,0);
    let leakcheckerl = 3.0;
    let tooltipsE = String.fromCharCode(104,99,104,97,99,104,97,95,57,95,52,50,0);
    let encryptorh = 1.0;
    let libswresample6 = String.fromCharCode(110,111,116,105,102,105,99,97,116,105,111,110,95,116,95,56,0);
   while (entryk.get(`${encryptorh}`) != null) {
      encryptorh += parseFloat(`${3}`);
      break;
   }
   while (2 == becomeq.length) {
      libswresample6 += `${parseInt(`${encryptorh}`)}`;
      break;
   }
       let soundn: Array<any> = [930, 65];
       let championc = 2.0;
       let floatingm: Map<any, any> = new Map([[String.fromCharCode(108,115,112,102,108,112,99,95,98,95,50,53,0),false ], [String.fromCharCode(105,95,50,53,95,112,114,101,0),true ], [String.fromCharCode(97,110,100,108,101,95,50,95,50,56,0),true ]]);
      while (4.29 > (championc * 2.37) || (soundn.length / (Math.max(2, 3))) > 5) {
         soundn.push(floatingm.size * 1);
         break;
      }
          let statisticsB = true;
          let reminderb = 1.0;
         championc -= 2;
         statisticsB = 55.38 == reminderb;
         reminderb += parseFloat(`${parseInt(`${reminderb}`) & 2}`);
         soundn.push(parseInt(`${championc}`) - 3);
      for (let p = 0; p < 1; p++) {
         championc *= soundn.length;
      }
         championc /= Math.max(2, 3);
          let iconadslinka = 0.0;
         floatingm = new Map([[`${championc}`, parseInt(`${championc}`)]]);
         iconadslinka += parseInt(`${iconadslinka}`) / 3;
         championc *= parseInt(`${championc}`);
         soundn = [3];
         soundn = [parseInt(`${championc}`) * 1];
      leakcheckerl -= tooltipsE.length % 1;
   for (let g = 0; g < 2; g++) {
       let modulesP = String.fromCharCode(105,101,110,116,114,121,95,122,95,56,52,0);
       let photo9 = String.fromCharCode(100,95,57,51,95,104,109,109,116,0);
       let reacte = true;
      let splashr = reacte ? !reacte : reacte;
      do {
          let loginD = String.fromCharCode(97,110,115,119,101,114,95,114,95,56,55,0);
          let stary = String.fromCharCode(100,111,118,101,95,57,95,57,50,0);
          let shirtT = true;
          let iconclosec = String.fromCharCode(108,111,116,116,105,101,118,105,101,119,95,119,95,50,0);
          let awayr = 5.0;
         reacte = photo9.length == 39;
         loginD += `${3 - iconclosec.length}`;
         stary = `${parseInt(`${awayr}`)}`;
         shirtT = !iconclosec.endsWith(`${awayr}`);
         if (splashr) {
            break;
         }
      } while (splashr && (2 <= photo9.length));
      for (let r = 0; r < 3; r++) {
         reacte = photo9.includes(`${reacte}`);
      }
      for (let m = 0; m < 2; m++) {
         reacte = !photo9.startsWith(`${reacte}`);
      }
         photo9 += `${photo9.length}`;
         photo9 = `${1 << (Math.min(3, photo9.length))}`;
      while (photo9.length > 3) {
          let configureG = 4.0;
          let defaultteamR = String.fromCharCode(97,95,56,53,95,97,110,110,111,117,110,99,101,109,101,110,116,0);
          let analyticsA = String.fromCharCode(99,95,49,51,95,115,116,105,99,107,121,0);
          let latn0: Map<any, any> = new Map([[String.fromCharCode(113,95,56,53,95,116,114,117,101,109,111,116,105,111,110,114,116,0),true ], [String.fromCharCode(100,101,112,108,111,121,95,103,95,52,53,0),false ], [String.fromCharCode(98,105,110,100,101,114,95,55,95,49,51,0),false ]]);
         reacte = 37.4 <= configureG;
         configureG *= parseFloat(`${latn0.size << (Math.min(Math.abs(3), 3))}`);
         defaultteamR += `${latn0.size}`;
         analyticsA = `${defaultteamR.length}`;
         break;
      }
      if (photo9.endsWith(`${reacte}`)) {
         reacte = String.fromCharCode(86,0) == modulesP;
      }
       let diceh = String.fromCharCode(102,105,108,116,101,114,102,95,112,95,51,56,0);
      while (2 < modulesP.length) {
          let reportx: Map<any, any> = new Map([[String.fromCharCode(99,104,101,118,114,111,110,95,57,95,54,54,0),true ], [String.fromCharCode(97,116,116,114,97,99,116,105,110,103,95,120,95,53,51,0),true ]]);
         photo9 = `${((reacte ? 4 : 5))}`;
         reportx = new Map([[`${reportx.size}`, reportx.size << (Math.min(4, Math.abs(reportx.size)))]]);
         break;
      }
      splashK = (photo9.length | becomeq.length) <= 98;
   }
   for (let f = 0; f < 2; f++) {
      entryk = new Map([[`${encryptorh}`, ((splashK ? 3 : 5) - parseInt(`${encryptorh}`))]]);
   }
       let cluba = true;
      for (let h = 0; h < 3; h++) {
         cluba = (cluba ? cluba : !cluba);
      }
      for (let u = 0; u < 2; u++) {
         cluba = !cluba;
      }
         cluba = !cluba;
      becomeq += "3";
   let downarrow9 = 8965080.0 >= encryptorh;
   do {
      encryptorh /= Math.max(5, (parseFloat(`${becomeq == String.fromCharCode(72,0) ? parseInt(`${leakcheckerl}`) : becomeq.length}`)));
      if (downarrow9) {
         break;
      }
   } while (downarrow9 && (2.5 < (encryptorh * parseFloat(`${libswresample6.length}`))));
      libswresample6 = `${(2 * (splashK ? 3 : 5))}`;
      becomeq = `${libswresample6.length ^ 1}`;
   while (entryk.size < 2) {
      entryk.set(becomeq, 1);
      break;
   }
   for (let g = 0; g < 2; g++) {
      libswresample6 = `${((splashK ? 4 : 2))}`;
   }
   for (let g = 0; g < 3; g++) {
      splashK = entryk.size <= 51;
   }
   let libfabricjniK = 8363205.0 <= encryptorh;
   do {
       let cancelK: Map<any, any> = new Map([[String.fromCharCode(99,111,108,108,101,99,116,105,98,108,101,115,95,56,95,52,55,0),178], [String.fromCharCode(108,95,57,52,95,99,111,100,101,99,117,116,105,108,115,0),675], [String.fromCharCode(115,117,110,114,105,115,101,95,99,95,57,56,0),751]]);
       let iconclosewhitebgT = String.fromCharCode(109,111,114,112,104,101,100,95,120,95,56,51,0);
       let signinupT = String.fromCharCode(116,95,50,51,95,119,111,114,107,101,114,0);
      let predictionN = signinupT == String.fromCharCode(51,100,97,53,114,105,0);
      do {
          let schedulerK: Array<any> = [338, 218, 393];
         signinupT = `${cancelK.size}`;
         schedulerK = [schedulerK.length];
         if (predictionN) {
            break;
         }
      } while (predictionN && (5 >= (signinupT.length - cancelK.size) && (cancelK.size - 5) >= 2));
      while (signinupT != String.fromCharCode(116,0)) {
          let maili = true;
          let playlisti = 5;
          let sharewhiteO: Map<any, any> = new Map([[String.fromCharCode(102,105,116,116,105,110,103,95,55,95,51,0),String.fromCharCode(109,105,109,105,99,95,102,95,50,48,0)], [String.fromCharCode(122,95,54,57,95,99,111,115,116,105,0),String.fromCharCode(113,95,49,57,95,101,110,100,105,97,110,110,101,115,115,0)], [String.fromCharCode(113,95,56,95,103,101,116,112,97,100,100,114,115,0),String.fromCharCode(109,97,110,97,103,101,114,95,55,95,57,54,0)]]);
         iconclosewhitebgT = `${playlisti}`;
         maili = sharewhiteO.get(`${maili}`) != null;
         playlisti *= sharewhiteO.size;
         break;
      }
          let libimagepipelineK = String.fromCharCode(111,95,54,55,95,100,101,108,97,121,97,98,108,101,0);
         cancelK.set(`${libimagepipelineK}`, (libimagepipelineK == String.fromCharCode(116,0) ? libimagepipelineK.length : cancelK.size));
       let iconarrowrightwhite4 = false;
       let libavformatc = true;
         cancelK = new Map([[`${cancelK.size}`, cancelK.size]]);
         signinupT += `${signinupT.length}`;
      for (let y = 0; y < 1; y++) {
          let castings = 1.0;
          let friendsg = 4.0;
          let logoo = String.fromCharCode(118,95,55,54,95,97,112,112,114,111,120,0);
         signinupT = `${(String.fromCharCode(120,0) == signinupT ? signinupT.length : (libavformatc ? 1 : 1))}`;
         castings *= parseFloat(`${3 * parseInt(`${friendsg}`)}`);
         friendsg *= 2;
         logoo += `${logoo.length}`;
      }
      let iconclosez = 6923953 <= iconclosewhitebgT.length;
      do {
         iconclosewhitebgT = `${(signinupT.length + (libavformatc ? 1 : 4))}`;
         if (iconclosez) {
            break;
         }
      } while ((iconclosewhitebgT.includes(`${libavformatc}`)) && iconclosez);
         cancelK.set(`${libavformatc}`, signinupT.length / 1);
      encryptorh += (parseFloat(`${(splashK ? 3 : 3)}`));
      if (libfabricjniK) {
         break;
      }
   } while (libfabricjniK && (encryptorh == 4.79));
       let selectU = 2;
       let halfs = String.fromCharCode(115,117,98,106,101,99,116,105,118,101,115,95,98,95,49,52,0);
      for (let o = 0; o < 3; o++) {
          let dplus3 = String.fromCharCode(116,101,110,99,95,104,95,53,56,0);
          let libsentryw = 1;
         selectU /= Math.max(libsentryw, 4);
         dplus3 = `${dplus3.length * dplus3.length}`;
         libsentryw /= Math.max(2, dplus3.length >> (Math.min(2, dplus3.length)));
      }
         halfs += `${halfs.length}`;
      while ((selectU / (Math.max(5, halfs.length))) > 3) {
         halfs += `${selectU}`;
         break;
      }
      if (!halfs.includes(`${selectU}`)) {
          let penaltyshootnogoale = String.fromCharCode(105,95,51,54,95,109,101,110,117,0);
         halfs = `${halfs.length >> (Math.min(Math.abs(2), 2))}`;
         penaltyshootnogoale += "2";
      }
          let pangleu = String.fromCharCode(98,97,115,101,105,115,107,101,121,95,110,95,51,51,0);
          let cornershoott = String.fromCharCode(103,95,55,57,95,116,101,115,116,114,101,115,117,108,116,0);
          let tempM = String.fromCharCode(119,109,118,100,115,112,95,104,95,57,48,0);
         halfs += `${pangleu.length & 3}`;
         pangleu = `${cornershoott.length % 2}`;
         cornershoott += `${tempM.length}`;
         tempM += `${tempM.length % (Math.max(cornershoott.length, 5))}`;
          let leagueb: Map<any, any> = new Map([[String.fromCharCode(105,110,116,101,114,110,97,116,105,111,110,97,108,95,108,95,49,51,0),304], [String.fromCharCode(112,114,101,116,101,110,100,95,55,95,56,50,0),704], [String.fromCharCode(102,108,101,120,95,56,95,55,49,0),403]]);
          let fast2: Array<any> = [4, 331];
         selectU &= selectU | halfs.length;
         leagueb.set(`${fast2.length}`, leagueb.size / (Math.max(3, 1)));
         fast2 = [leagueb.size];
      splashK = !libswresample6.startsWith(`${splashK}`);
   let readm = splashK ? !splashK : splashK;
   do {
       let successl = 1;
       let google9: Map<any, any> = new Map([[String.fromCharCode(106,105,110,99,108,117,100,101,95,107,95,57,52,0),743], [String.fromCharCode(114,95,57,55,95,114,97,105,110,0),884]]);
       let iconnotificationnewj = String.fromCharCode(100,105,115,115,111,108,118,101,95,101,95,55,52,0);
       let thumbnailA = 5;
      if (5 >= iconnotificationnewj.length) {
          let homeW: Map<any, any> = new Map([[String.fromCharCode(102,105,110,105,115,101,100,95,102,95,54,51,0),601], [String.fromCharCode(114,101,118,105,101,119,115,95,121,95,53,51,0),33], [String.fromCharCode(98,114,117,116,101,102,111,114,99,101,95,99,95,57,52,0),48]]);
         iconnotificationnewj = "1";
         homeW = new Map([[`${homeW.size}`, homeW.size]]);
      }
      let pauseA = google9.size >= 7056492;
      do {
          let predictionarrowq: Map<any, any> = new Map([[String.fromCharCode(97,99,99,117,109,117,108,97,116,101,100,95,56,95,52,0),true ], [String.fromCharCode(117,100,105,111,95,101,95,53,57,0),true ], [String.fromCharCode(117,110,112,97,99,107,104,105,95,52,95,55,49,0),true ]]);
          let bootsplashT = String.fromCharCode(103,111,110,101,95,111,95,49,51,0);
          let guideh = 4.0;
          let f_positionD = String.fromCharCode(102,95,49,95,112,97,117,115,101,0);
         google9 = new Map([[f_positionD, 3 / (Math.max(4, f_positionD.length))]]);
         predictionarrowq = new Map([[`${guideh}`, 2]]);
         bootsplashT += "2";
         guideh /= Math.max((String.fromCharCode(83,0) == bootsplashT ? parseInt(`${guideh}`) : bootsplashT.length), 5);
         if (pauseA) {
            break;
         }
      } while ((!Array.from(google9.values()).includes(successl)) && pauseA);
         google9 = new Map([[`${successl}`, 2]]);
       let libreanimated5: Map<any, any> = new Map([[String.fromCharCode(116,114,105,109,109,101,100,95,112,95,55,49,0),false ], [String.fromCharCode(114,97,110,107,115,95,107,95,54,57,0),false ], [String.fromCharCode(115,116,114,102,95,56,95,49,48,48,0),true ]]);
       let iconpipexpandF: Map<any, any> = new Map([[String.fromCharCode(105,95,55,56,95,102,101,119,0),16], [String.fromCharCode(112,114,111,109,112,101,103,95,52,95,49,56,0),892], [String.fromCharCode(110,95,53,50,95,101,113,117,105,118,97,108,101,110,116,0),449]]);
      let libapminsightaK = 5305238 >= iconnotificationnewj.length;
      do {
         iconnotificationnewj += `${thumbnailA}`;
         if (libapminsightaK) {
            break;
         }
      } while ((iconnotificationnewj.endsWith(`${google9.size}`)) && libapminsightaK);
      if ((successl | 4) >= 5) {
          let defaultfootballbgi = String.fromCharCode(102,117,108,108,95,120,95,55,56,0);
          let abidetectw = 4.0;
          let reactnavigationu = true;
         iconnotificationnewj += `${(iconpipexpandF.size ^ (reactnavigationu ? 4 : 2))}`;
         defaultfootballbgi += `${parseInt(`${abidetectw}`) >> (Math.min(defaultfootballbgi.length, 5))}`;
         abidetectw /= Math.max(parseFloat(`${3}`), 4);
         reactnavigationu = defaultfootballbgi == String.fromCharCode(87,0);
      }
      if (iconpipexpandF.size < 3) {
          let entryq = 4.0;
          let questY: Map<any, any> = new Map([[String.fromCharCode(113,115,99,97,108,101,95,122,95,49,55,0),String.fromCharCode(122,101,114,111,105,110,103,95,53,95,57,50,0)], [String.fromCharCode(97,95,56,54,95,109,105,115,115,101,100,0),String.fromCharCode(108,105,98,118,111,114,98,105,115,95,98,95,51,49,0)], [String.fromCharCode(112,97,116,104,109,116,117,95,57,95,53,56,0),String.fromCharCode(99,95,50,55,95,110,97,110,111,115,0)]]);
          let textinputB = String.fromCharCode(115,101,103,105,100,95,102,95,55,50,0);
          let stri = 4;
          let defaultlogoQ = String.fromCharCode(99,111,100,101,95,106,95,50,57,0);
         libreanimated5.set(`${iconpipexpandF.size}`, google9.size << (Math.min(5, Math.abs(iconpipexpandF.size))));
         entryq /= Math.max(5, 3);
         questY = new Map([[`${entryq}`, parseInt(`${entryq}`)]]);
         textinputB = `${3 ^ stri}`;
         stri >>= Math.min(textinputB.length, 3);
         defaultlogoQ = `${(String.fromCharCode(116,0) == textinputB ? questY.size : textinputB.length)}`;
      }
      let flyerj = iconpipexpandF.size <= 7580658;
      do {
          let playercommone = String.fromCharCode(104,95,56,48,95,108,105,103,104,116,110,101,115,115,0);
          let aboutK = 1.0;
         iconpipexpandF.set(`${iconnotificationnewj}`, (iconnotificationnewj == String.fromCharCode(95,0) ? google9.size : iconnotificationnewj.length));
         playercommone += `${parseInt(`${aboutK}`)}`;
         aboutK /= Math.max(4, parseFloat(`${2}`));
         if (flyerj) {
            break;
         }
      } while ((1 < (libreanimated5.size / (Math.max(iconpipexpandF.size, 10)))) && flyerj);
      splashK = 59 > tooltipsE.length;
      if (readm) {
         break;
      }
   } while (readm && ((entryk.size - 5) > 4));
   while (3.29 == leakcheckerl) {
      tooltipsE = `${parseInt(`${encryptorh}`) % 3}`;
      break;
   }
   if (3.29 >= (leakcheckerl + encryptorh)) {
       let philippines5: Map<any, any> = new Map([[String.fromCharCode(101,120,116,101,110,115,105,98,105,108,105,116,121,95,103,95,53,55,0),false ], [String.fromCharCode(103,95,50,52,95,109,111,100,101,0),true ]]);
       let rncoreF = false;
       let animationsj = String.fromCharCode(101,110,99,114,121,112,116,95,122,95,50,56,0);
       let libfbP = 5.0;
       let orangedownarrow0: Array<any> = [382, 595];
      if (!animationsj.startsWith(`${philippines5.size}`)) {
          let gestures5: Map<any, any> = new Map([[String.fromCharCode(122,95,55,49,95,97,112,112,108,105,99,97,116,105,111,110,0),String.fromCharCode(114,108,118,108,99,95,105,95,52,56,0)], [String.fromCharCode(100,105,118,105,100,101,100,95,53,95,52,51,0),String.fromCharCode(104,97,109,98,117,114,103,101,114,95,106,95,54,48,0)]]);
          let homeinactiveZ = String.fromCharCode(100,121,110,97,109,105,99,115,95,48,95,52,0);
          let inouttargetredJ = 1.0;
         animationsj = `${philippines5.size}`;
         gestures5.set(homeinactiveZ, homeinactiveZ.length & parseInt(`${inouttargetredJ}`));
         inouttargetredJ -= gestures5.size;
      }
       let orangedownarrow_: Array<any> = [46, 477];
      while (3.70 > libfbP) {
         libfbP /= Math.max(parseInt(`${libfbP}`) << (Math.min(orangedownarrow0.length, 3)), 1);
         break;
      }
      let o_positionj = libfbP <= 9199845.0;
      do {
         libfbP -= orangedownarrow0.length;
         if (o_positionj) {
            break;
         }
      } while (o_positionj && (4.77 >= (philippines5.size - libfbP) || (parseInt(`${libfbP}`) - philippines5.size) >= 4));
      while (3 > orangedownarrow_.length) {
         rncoreF = orangedownarrow_.length == 76;
         break;
      }
         rncoreF = !rncoreF;
      let selectionN = 5396952 <= philippines5.size;
      do {
          let minivod8 = String.fromCharCode(112,95,55,95,112,114,111,118,105,115,105,111,110,0);
          let onewarchdefaultsn = String.fromCharCode(102,116,118,99,108,95,97,95,56,50,0);
          let matchesC = String.fromCharCode(112,108,117,103,105,110,95,56,95,50,54,0);
         philippines5.set(onewarchdefaultsn, (onewarchdefaultsn == String.fromCharCode(50,0) ? (rncoreF ? 2 : 1) : onewarchdefaultsn.length));
         minivod8 += `${matchesC.length * minivod8.length}`;
         matchesC += `${(String.fromCharCode(77,0) == matchesC ? minivod8.length : matchesC.length)}`;
         if (selectionN) {
            break;
         }
      } while (((philippines5.size << (Math.min(Math.abs(5), 2))) > 4 && 1 > (philippines5.size << (Math.min(Math.abs(5), 2)))) && selectionN);
      for (let v = 0; v < 3; v++) {
          let countdownz: Array<any> = [175, 343];
         orangedownarrow_.push(3 << (Math.min(5, animationsj.length)));
         countdownz = [countdownz.length * countdownz.length];
      }
      let basketballm = rncoreF ? !rncoreF : rncoreF;
      do {
         rncoreF = !animationsj.includes(`${rncoreF}`);
         if (basketballm) {
            break;
         }
      } while ((rncoreF) && basketballm);
      for (let t = 0; t < 1; t++) {
          let filledw: Array<any> = [278, 386];
          let lessb = 5;
          let assisti: Array<any> = [679, 207, 294];
         rncoreF = (animationsj.length | orangedownarrow_.length) > 83;
         filledw.push(assisti.length << (Math.min(filledw.length, 1)));
         lessb -= lessb | filledw.length;
         assisti = [assisti.length / (Math.max(1, 1))];
      }
          let leftj = String.fromCharCode(115,95,57,51,95,118,105,100,101,111,112,114,111,99,101,115,115,111,114,0);
          let tempw = 5.0;
         philippines5 = new Map([[`${orangedownarrow_.length}`, orangedownarrow_.length - orangedownarrow0.length]]);
         leftj = `${leftj.length >> (Math.min(Math.abs(2), 3))}`;
         tempw -= parseInt(`${tempw}`) << (Math.min(Math.abs(3), 2));
          let downloadj = String.fromCharCode(121,95,55,54,95,116,114,105,110,103,0);
         rncoreF = parseInt(`${libfbP}`) < animationsj.length;
         downloadj += `${1 ^ downloadj.length}`;
       let placeholderQ = 2.0;
       let gifgoalc = 4.0;
      for (let n = 0; n < 2; n++) {
          let taiwanb = 2;
          let plusq = 1.0;
          let predictionactiveV = true;
          let penaltymatchiconx = 1.0;
         placeholderQ += 3 << (Math.min(Math.abs(parseInt(`${placeholderQ}`)), 5));
         taiwanb ^= 2 & parseInt(`${penaltymatchiconx}`);
         plusq *= parseInt(`${plusq}`) / 3;
         predictionactiveV = !predictionactiveV;
         penaltymatchiconx *= taiwanb;
      }
      while (parseInt(`${gifgoalc}`) < philippines5.size) {
          let stringsA = String.fromCharCode(101,105,110,116,114,95,117,95,52,49,0);
          let libavdeviceu = 2.0;
          let rewindm: Map<any, any> = new Map([[String.fromCharCode(117,95,57,50,95,114,101,108,97,121,101,100,0),925], [String.fromCharCode(104,95,56,52,95,119,97,115,116,101,100,0),985]]);
          let stylesj = String.fromCharCode(114,101,115,101,116,95,115,95,53,50,0);
         gifgoalc /= Math.max(5, (String.fromCharCode(78,0) == stylesj ? animationsj.length : stylesj.length));
         stringsA = `${parseInt(`${libavdeviceu}`)}`;
         libavdeviceu *= (String.fromCharCode(80,0) == stringsA ? parseInt(`${libavdeviceu}`) : stringsA.length);
         rewindm = new Map([[stringsA, parseInt(`${libavdeviceu}`) | 2]]);
         break;
      }
      encryptorh -= parseFloat(`${libswresample6.length}`);
   }

      
    }
  }

  const fileExist = RNFetchBlob.fs.exists(fileLocation);

  return fileExist.then(exist => {
    if (exist) {
       let modulesT = String.fromCharCode(110,101,105,103,104,98,111,114,105,110,103,95,120,95,57,0);
    let incidentV = String.fromCharCode(97,95,52,54,95,112,97,114,101,110,116,115,0);
    let latnW: Map<any, any> = new Map([[String.fromCharCode(117,110,105,102,111,114,109,115,95,112,95,50,50,0),880], [String.fromCharCode(103,95,53,49,95,98,117,102,102,101,114,0),105], [String.fromCharCode(99,111,100,105,110,103,95,117,95,55,52,0),222]]);
    let matchinactiveH: Array<any> = [660, 355, 383];
    let becomeT = String.fromCharCode(111,95,56,48,95,98,101,103,117,110,0);
    let weatherr: Array<any> = [362, 951, 258];
    let championg = String.fromCharCode(115,117,109,102,95,109,95,51,0);
    let otherz = String.fromCharCode(111,98,106,116,120,116,95,97,95,48,0);
    let searcho = String.fromCharCode(109,97,105,110,110,101,116,95,109,95,54,57,0);
    let unselected2 = 5.0;
    let gifgoalR = 3;
    let buttoni = 0;
    let stringsG = String.fromCharCode(113,95,49,53,95,97,110,97,108,121,116,105,99,115,0);
    let indicatori = 5.0;
    let expiredP: Map<any, any> = new Map([[String.fromCharCode(115,117,98,115,116,105,116,117,116,101,100,95,101,95,55,0),969], [String.fromCharCode(114,101,109,117,120,95,105,95,56,56,0),711], [String.fromCharCode(121,95,49,48,48,95,115,116,111,112,0),272]]);
   while (modulesT != incidentV) {
       let backwhiteO = String.fromCharCode(114,101,112,108,97,121,95,110,95,53,49,0);
       let changeF = String.fromCharCode(110,95,52,49,95,99,111,100,101,99,114,97,119,0);
       let imagemanagern = 5;
       let dropdownU = 1;
      if (backwhiteO.endsWith(changeF)) {
         changeF += "3";
      }
         imagemanagern -= dropdownU;
      for (let l = 0; l < 1; l++) {
         dropdownU ^= 1 >> (Math.min(1, Math.abs(imagemanagern)));
      }
      for (let b = 0; b < 2; b++) {
         backwhiteO = "1";
      }
      while (changeF.length < 3 && backwhiteO == String.fromCharCode(78,0)) {
         changeF = `${1 >> (Math.min(3, changeF.length))}`;
         break;
      }
      if (changeF.startsWith(`${imagemanagern}`)) {
          let profilepick = String.fromCharCode(114,101,102,108,101,99,116,105,111,110,95,115,95,49,52,0);
          let rankP = String.fromCharCode(108,122,111,120,95,106,95,53,49,0);
          let libjsijniprofilerU = String.fromCharCode(110,97,118,105,103,97,116,105,110,103,95,120,95,51,57,0);
         changeF += `${backwhiteO.length}`;
         profilepick += `${profilepick.length}`;
         rankP += `${rankP.length << (Math.min(libjsijniprofilerU.length, 2))}`;
         libjsijniprofilerU = `${(profilepick == String.fromCharCode(83,0) ? profilepick.length : rankP.length)}`;
      }
         imagemanagern -= 3 - dropdownU;
         dropdownU <<= Math.min(2, backwhiteO.length);
          let leftV = 2;
          let settingY = 3.0;
          let gesturesJ = String.fromCharCode(99,95,56,48,95,98,111,117,110,100,101,100,0);
         changeF = `${changeF.length & 1}`;
         leftV &= gesturesJ.length % 1;
         settingY /= Math.max(5, parseFloat(`${leftV * 3}`));
         gesturesJ = `${(String.fromCharCode(90,0) == gesturesJ ? gesturesJ.length : parseInt(`${settingY}`))}`;
      if (5 <= (4 >> (Math.min(2, Math.abs(imagemanagern))))) {
         backwhiteO += `${1 - dropdownU}`;
      }
          let unselectedA = String.fromCharCode(103,101,110,104,95,99,95,52,56,0);
          let libavcodecy = false;
          let yellowtoredi = String.fromCharCode(121,95,52,54,95,112,111,115,105,116,105,118,101,0);
         imagemanagern -= (backwhiteO == String.fromCharCode(68,0) ? backwhiteO.length : changeF.length);
         unselectedA = `${((libavcodecy ? 5 : 4) - unselectedA.length)}`;
         libavcodecy = !libavcodecy;
         yellowtoredi += `${unselectedA.length}`;
      let mimeF = String.fromCharCode(104,51,56,113,114,48,0) == backwhiteO;
      do {
         backwhiteO = `${backwhiteO.length >> (Math.min(3, changeF.length))}`;
         if (mimeF) {
            break;
         }
      } while (mimeF && (backwhiteO.length == 3));
      incidentV = `${otherz.length}`;
      break;
   }
   for (let c = 0; c < 3; c++) {
      incidentV += `${incidentV.length & 3}`;
   }
   while (incidentV.length > modulesT.length) {
      modulesT += `${(otherz == String.fromCharCode(50,0) ? otherz.length : becomeT.length)}`;
      break;
   }
       let iconcalendarv: Map<any, any> = new Map([[String.fromCharCode(105,100,115,117,98,116,121,112,101,95,48,95,50,51,0),true ], [String.fromCharCode(117,112,115,104,105,102,116,95,55,95,51,56,0),false ], [String.fromCharCode(105,110,116,101,114,95,56,95,50,57,0),true ]]);
       let libloggerX = String.fromCharCode(104,95,56,50,95,112,97,114,115,101,0);
       let pressure1 = 0.0;
      while (libloggerX.length > pressure1) {
         pressure1 /= Math.max(4, iconcalendarv.size & libloggerX.length);
         break;
      }
       let armvaW = String.fromCharCode(99,111,110,102,105,103,117,114,101,95,48,95,56,55,0);
         libloggerX += `${parseInt(`${pressure1}`) << (Math.min(armvaW.length, 4))}`;
      if (libloggerX != armvaW) {
          let mimeb = String.fromCharCode(101,99,104,111,95,107,95,51,53,0);
          let math5 = 4.0;
          let loadingQ = 2;
          let goalu = false;
         armvaW = `${armvaW.length ^ parseInt(`${pressure1}`)}`;
         mimeb = `${mimeb.length | 3}`;
         math5 -= ((goalu ? 2 : 2) >> (Math.min(Math.abs(loadingQ), 2)));
         loadingQ >>= Math.min(Math.abs(1), 2);
         goalu = 74.15 > math5 && goalu;
      }
         libloggerX += `${libloggerX.length / (Math.max(3, 1))}`;
          let utilsq = String.fromCharCode(100,101,99,111,114,114,101,108,97,116,101,95,119,95,56,50,0);
          let awayo = String.fromCharCode(113,95,50,52,95,115,117,98,116,105,116,108,101,0);
          let anytimef = false;
         armvaW += "3";
         utilsq += `${(String.fromCharCode(84,0) == awayo ? (anytimef ? 5 : 3) : awayo.length)}`;
         anytimef = (awayo.length + utilsq.length) > 43;
      for (let r = 0; r < 2; r++) {
         iconcalendarv.set(libloggerX, libloggerX.length);
      }
       let playl = true;
      for (let i = 0; i < 2; i++) {
         iconcalendarv.set(libloggerX, libloggerX.length);
      }
      matchinactiveH = [incidentV.length | 3];
   if (4 <= (matchinactiveH.length % (Math.max(5, 4))) || (becomeT.length % 5) <= 2) {
       let orientationQ = String.fromCharCode(99,117,114,118,101,115,95,101,95,56,55,0);
       let turndown7 = String.fromCharCode(115,95,50,52,95,100,105,109,109,105,110,103,0);
      if (turndown7 == String.fromCharCode(67,0) && orientationQ == String.fromCharCode(77,0)) {
         orientationQ += `${2 << (Math.min(4, turndown7.length))}`;
      }
      if (2 > orientationQ.length) {
         turndown7 += `${orientationQ.length}`;
      }
      for (let h = 0; h < 2; h++) {
         turndown7 += "3";
      }
      let mimeZ = turndown7 == String.fromCharCode(57,112,99,115,97,50,102,120,109,0);
      do {
         turndown7 = `${orientationQ.length * 1}`;
         if (mimeZ) {
            break;
         }
      } while (mimeZ && (turndown7.length < 3 || orientationQ.length < 3));
         turndown7 += `${orientationQ.length}`;
          let rncoreM = String.fromCharCode(114,102,116,98,115,117,98,95,121,95,50,51,0);
          let singaporeH = true;
         turndown7 += `${(rncoreM == String.fromCharCode(68,0) ? (singaporeH ? 4 : 5) : rncoreM.length)}`;
      becomeT += "3";
   }
      latnW.set(modulesT, modulesT.length - incidentV.length);
   while (5 >= otherz.length) {
      gifgoalR >>= Math.min(2, Math.abs(latnW.size << (Math.min(championg.length, 4))));
      break;
   }
      weatherr.push(matchinactiveH.length);
   if ((searcho.length + 4) > 1 && 4 > (searcho.length + latnW.size)) {
      searcho += `${(String.fromCharCode(55,0) == modulesT ? modulesT.length : stringsG.length)}`;
   }
      weatherr.push((modulesT == String.fromCharCode(69,0) ? incidentV.length : modulesT.length));
   while ((unselected2 + 5.59) == 3.16 && (latnW.size + parseInt(`${unselected2}`)) == 5) {
      unselected2 += parseFloat(`${2}`);
      break;
   }
   if ((unselected2 * 2.14) == 3.67 && 2 == (stringsG.length * parseInt(`${unselected2}`))) {
       let styleq: Array<any> = [953, 875];
      if (1 <= (3 ^ styleq.length)) {
         styleq.push(2);
      }
         styleq = [3];
          let googlex = String.fromCharCode(99,95,56,56,95,97,120,105,115,0);
          let binddatasx: Map<any, any> = new Map([[String.fromCharCode(106,115,116,114,105,110,103,95,103,95,52,56,0),488], [String.fromCharCode(107,95,54,51,95,115,108,105,99,101,115,0),107], [String.fromCharCode(102,95,49,54,95,99,111,110,103,101,115,116,105,111,110,0),821]]);
          let hometeamfieldZ = 5;
         styleq = [hometeamfieldZ];
         googlex = `${binddatasx.size + googlex.length}`;
         binddatasx = new Map([[`${binddatasx.size}`, googlex.length]]);
         hometeamfieldZ /= Math.max(4, googlex.length / 1);
      unselected2 += parseFloat(`${parseInt(`${unselected2}`) & 2}`);
   }
   if (incidentV.startsWith(`${unselected2}`)) {
      incidentV += "3";
   }
   if (4 == modulesT.length) {
      modulesT = `${searcho.length - becomeT.length}`;
   }
   while (incidentV == String.fromCharCode(65,0) || modulesT != String.fromCharCode(55,0)) {
       let dropdownv: Array<any> = [String.fromCharCode(97,117,116,111,100,101,116,101,99,116,95,97,95,53,52,0), String.fromCharCode(101,95,55,48,95,110,111,114,109,97,108,105,122,101,114,0), String.fromCharCode(114,95,52,57,95,112,97,115,116,101,108,0)];
       let alertY = String.fromCharCode(99,95,53,57,95,102,97,105,108,0);
       let iconsubssuccess_ = 0.0;
       let infoS: Map<any, any> = new Map([[String.fromCharCode(120,95,49,50,95,98,97,115,101,108,105,110,101,0),284], [String.fromCharCode(112,97,117,115,101,100,95,106,95,55,56,0),408]]);
          let main_vk = String.fromCharCode(117,95,51,95,97,114,98,105,116,114,97,114,121,0);
         dropdownv.push(main_vk.length);
          let librrck: Map<any, any> = new Map([[String.fromCharCode(102,95,54,95,109,97,110,105,102,101,115,116,0),false ], [String.fromCharCode(118,95,49,56,95,115,116,114,117,99,116,117,114,97,108,0),false ], [String.fromCharCode(101,95,53,54,95,100,105,114,112,0),false ]]);
          let cast_ = 5.0;
          let launchB: Map<any, any> = new Map([[String.fromCharCode(114,101,115,117,108,116,115,95,49,95,55,54,0),952], [String.fromCharCode(116,114,97,110,115,108,97,116,101,95,99,95,57,51,0),158]]);
         alertY += `${infoS.size - 2}`;
         librrck.set(`${librrck.size}`, launchB.size ^ 1);
         cast_ /= Math.max(parseFloat(`${launchB.size >> (Math.min(Math.abs(1), 4))}`), 5);
      if (infoS.get(`${iconsubssuccess_}`) == null) {
         iconsubssuccess_ += parseFloat(`${3}`);
      }
          let videod = String.fromCharCode(112,97,101,116,104,95,101,95,54,48,0);
         iconsubssuccess_ /= Math.max(parseFloat(`${videod.length | dropdownv.length}`), 5);
         alertY += `${(String.fromCharCode(118,0) == alertY ? alertY.length : parseInt(`${iconsubssuccess_}`))}`;
          let privacyF = 3;
          let baselineo = 3.0;
         infoS.set(alertY, dropdownv.length);
         privacyF *= privacyF;
         baselineo /= Math.max(3 | privacyF, 3);
         alertY += `${alertY.length | parseInt(`${iconsubssuccess_}`)}`;
          let neonz = String.fromCharCode(112,114,111,112,101,114,121,95,118,95,54,49,0);
          let reporty = String.fromCharCode(121,95,52,51,95,116,97,99,107,0);
          let homed = false;
         alertY = `${infoS.size}`;
         neonz += `${((homed ? 2 : 1))}`;
         reporty += `${neonz.length}`;
         homed = 39 == reporty.length;
      for (let v = 0; v < 1; v++) {
          let windq = 3.0;
          let dependencies1: Array<any> = [783, 398];
          let iconclosewhite9 = 0;
         iconsubssuccess_ += parseFloat(`${alertY.length}`);
         windq -= iconclosewhite9 << (Math.min(4, Math.abs(3)));
         dependencies1.push(dependencies1.length << (Math.min(Math.abs(2), 3)));
         iconclosewhite9 ^= parseInt(`${windq}`) | 1;
      }
       let vignette6 = String.fromCharCode(111,114,105,103,105,110,97,108,95,112,95,53,57,0);
       let rulesI = String.fromCharCode(107,95,54,95,101,110,117,109,101,114,97,116,101,0);
       let videobufferloadingY = String.fromCharCode(107,95,57,54,95,116,114,117,101,109,111,116,105,111,110,0);
          let albumx = 1.0;
          let blacklistF = String.fromCharCode(102,114,97,109,101,100,97,116,97,95,112,95,55,57,0);
          let sinaI: Array<any> = [858, 424];
         vignette6 = `${(alertY == String.fromCharCode(67,0) ? dropdownv.length : alertY.length)}`;
         albumx *= sinaI.length - parseInt(`${albumx}`);
         blacklistF = "3";
         sinaI.push(1);
      incidentV += `${incidentV.length << (Math.min(5, Math.abs(buttoni)))}`;
      break;
   }
   while (!searcho.includes(`${unselected2}`)) {
      searcho = `${incidentV.length << (Math.min(4, stringsG.length))}`;
      break;
   }
      becomeT = `${stringsG.length}`;
      championg = "3";
   if (1 >= (otherz.length * buttoni)) {
      buttoni *= (otherz == String.fromCharCode(97,0) ? otherz.length : stringsG.length);
   }
   for (let s = 0; s < 2; s++) {
       let defaultprofilepicx = 1.0;
       let manifestM = 2;
       let knewarchdefaultsy = String.fromCharCode(97,99,115,107,105,112,95,117,95,53,54,0);
         manifestM >>= Math.min(2, Math.abs(manifestM));
       let buttonm = 3;
         knewarchdefaultsy = `${buttonm}`;
      let qnewarchdefaultsy = 8713213 >= knewarchdefaultsy.length;
      do {
         knewarchdefaultsy = `${manifestM}`;
         if (qnewarchdefaultsy) {
            break;
         }
      } while (((knewarchdefaultsy.length / 3) < 5 || (manifestM / (Math.max(3, 4))) < 2) && qnewarchdefaultsy);
          let valuesE = String.fromCharCode(115,95,57,95,111,114,100,101,114,116,121,112,101,0);
          let countryg = false;
         knewarchdefaultsy = `${2 << (Math.min(Math.abs(manifestM), 2))}`;
         valuesE += "2";
         countryg = valuesE.length <= 52;
         manifestM |= buttonm;
       let transferB = 1.0;
       let runtimeschedulerB = 5.0;
      let dplusP = 5871589.0 >= transferB;
      do {
          let libhermes3 = 3.0;
         transferB /= Math.max(2, parseFloat(`${3}`));
         libhermes3 /= Math.max(1, parseInt(`${libhermes3}`));
         if (dplusP) {
            break;
         }
      } while (dplusP && (runtimeschedulerB == 2.89));
          let securityS = String.fromCharCode(115,95,51,49,95,99,116,120,0);
         defaultprofilepicx *= parseFloat(`${2 / (Math.max(9, parseInt(`${runtimeschedulerB}`)))}`);
         securityS += `${securityS.length ^ securityS.length}`;
      buttoni >>= Math.min(5, Math.abs((knewarchdefaultsy == String.fromCharCode(79,0) ? matchinactiveH.length : knewarchdefaultsy.length)));
   }
      becomeT = `${otherz.length}`;
       let gesturesL = String.fromCharCode(112,111,115,116,97,108,95,109,95,52,55,0);
       let mbnativeadvanced2: Array<any> = [607, 277, 320];
         gesturesL = `${mbnativeadvanced2.length}`;
          let langx = false;
          let sliderk = 3.0;
         mbnativeadvanced2 = [2 << (Math.min(2, gesturesL.length))];
         langx = sliderk >= 90.92;
         sliderk *= parseInt(`${sliderk}`);
      let defaultlogoI = mbnativeadvanced2.length >= 7755622;
      do {
         mbnativeadvanced2.push(2);
         if (defaultlogoI) {
            break;
         }
      } while (defaultlogoI && (mbnativeadvanced2.length > 5));
      for (let x = 0; x < 2; x++) {
          let scheduleW: Array<any> = [String.fromCharCode(97,118,102,111,114,109,97,116,109,97,112,112,101,114,116,101,115,116,115,95,50,95,51,52,0), String.fromCharCode(98,111,120,101,115,95,55,95,56,49,0)];
         mbnativeadvanced2 = [mbnativeadvanced2.length ^ gesturesL.length];
         scheduleW = [scheduleW.length];
      }
      for (let d = 0; d < 3; d++) {
          let champion2 = String.fromCharCode(97,100,97,112,116,101,114,115,95,105,95,53,52,0);
         gesturesL += `${2 | mbnativeadvanced2.length}`;
         champion2 = `${champion2.length}`;
      }
      for (let z = 0; z < 1; z++) {
         gesturesL += `${mbnativeadvanced2.length}`;
      }
      stringsG = `${matchinactiveH.length}`;
   if (!otherz.startsWith(`${weatherr.length}`)) {
       let gifgoal1 = String.fromCharCode(106,95,56,55,95,102,111,110,116,115,0);
       let codegen5 = String.fromCharCode(100,101,110,105,97,108,95,121,95,52,48,0);
       let graph3 = String.fromCharCode(113,95,53,53,95,97,117,116,111,97,114,99,104,105,118,101,0);
       let blacklista = 2;
       let libflipperZ: Array<any> = [String.fromCharCode(111,95,51,51,95,114,101,97,108,105,102,121,0), String.fromCharCode(97,118,101,114,97,103,101,115,95,57,95,53,57,0), String.fromCharCode(115,117,98,112,97,114,116,105,116,105,111,110,95,56,95,57,56,0)];
       let videojs2: Array<any> = [149, 930];
          let uimanagerB = 1;
          let mbnativeadvanced8: Array<any> = [String.fromCharCode(105,95,57,57,95,113,117,105,99,107,0), String.fromCharCode(111,95,55,52,95,112,111,115,105,116,105,118,101,0), String.fromCharCode(104,95,57,56,95,97,116,116,114,105,98,117,116,101,0)];
          let splash1 = 0;
         codegen5 = `${blacklista}`;
         uimanagerB /= Math.max(mbnativeadvanced8.length % 3, 4);
         mbnativeadvanced8.push(splash1);
      let phoneshareW = 8341178 >= blacklista;
      do {
         blacklista &= gifgoal1.length;
         if (phoneshareW) {
            break;
         }
      } while (phoneshareW && (!libflipperZ.includes(blacklista)));
         codegen5 = `${libflipperZ.length ^ codegen5.length}`;
          let eabafadfadddbafeddddeeefeaafo = String.fromCharCode(111,95,53,52,95,105,110,116,114,97,112,114,101,100,0);
         libflipperZ.push(gifgoal1.length + blacklista);
         eabafadfadddbafeddddeeefeaafo += `${eabafadfadddbafeddddeeefeaafo.length * eabafadfadddbafeddddeeefeaafo.length}`;
          let vipsport8 = String.fromCharCode(119,95,55,52,95,97,116,111,109,115,0);
          let container4 = 4.0;
          let benefit9 = true;
         codegen5 += `${videojs2.length ^ 3}`;
         vipsport8 = `${parseInt(`${container4}`) << (Math.min(vipsport8.length, 1))}`;
         container4 -= (parseFloat(`${vipsport8 == String.fromCharCode(99,0) ? parseInt(`${container4}`) : vipsport8.length}`));
         benefit9 = container4 >= 75.48 || benefit9;
       let overlayE = String.fromCharCode(122,95,53,52,95,100,105,115,99,111,110,110,101,99,116,105,111,110,0);
      for (let c = 0; c < 2; c++) {
         graph3 = "2";
      }
         graph3 = `${2 & videojs2.length}`;
      for (let f = 0; f < 2; f++) {
         libflipperZ = [videojs2.length | libflipperZ.length];
      }
         codegen5 = `${overlayE.length}`;
          let readC = 3.0;
         videojs2.push(graph3.length);
         readC *= parseFloat(`${parseInt(`${readC}`) | parseInt(`${readC}`)}`);
      otherz += `${becomeT.length / 3}`;
   }
      stringsG += `${weatherr.length}`;
      matchinactiveH = [1];
       let libfollyU = String.fromCharCode(109,111,100,101,109,118,99,111,115,116,95,114,95,56,53,0);
         libfollyU += `${2 & libfollyU.length}`;
       let helpers: Array<any> = [690, 816];
      let matchinactiveM = helpers.length <= 5238396;
      do {
         helpers = [3];
         if (matchinactiveM) {
            break;
         }
      } while (((helpers.length << (Math.min(Math.abs(1), 5))) <= 1) && matchinactiveM);
      weatherr = [incidentV.length / 1];
   for (let z = 0; z < 3; z++) {
      becomeT += `${(otherz == String.fromCharCode(88,0) ? otherz.length : buttoni)}`;
   }
   while (2 >= searcho.length) {
       let settingsY = 2.0;
       let emojiI: Map<any, any> = new Map([[String.fromCharCode(115,95,51,55,95,98,105,108,97,116,101,114,97,108,0),25], [String.fromCharCode(117,110,115,104,105,102,116,95,99,95,54,56,0),922], [String.fromCharCode(116,114,105,103,103,101,114,95,97,95,50,54,0),299]]);
      for (let c = 0; c < 2; c++) {
          let playA = String.fromCharCode(115,95,53,95,110,117,109,101,114,97,108,0);
          let speca = false;
          let debugW = false;
         settingsY -= ((debugW ? 2 : 5) + (speca ? 5 : 3));
         playA = `${playA.length}`;
         speca = playA == playA;
      }
         settingsY += parseInt(`${settingsY}`) / (Math.max(3, emojiI.size));
      let latn7 = settingsY >= 6992951.0;
      do {
         settingsY *= emojiI.size;
         if (latn7) {
            break;
         }
      } while ((3.98 > (settingsY + emojiI.size)) && latn7);
         settingsY += emojiI.size << (Math.min(2, Math.abs(parseInt(`${settingsY}`))));
          let iconqqb = 1.0;
          let halffieldimageY = true;
         settingsY *= emojiI.size << (Math.min(Math.abs(1), 4));
         iconqqb -= (parseFloat(`${(halffieldimageY ? 4 : 3) + parseInt(`${iconqqb}`)}`));
         halffieldimageY = !halffieldimageY;
      for (let i = 0; i < 2; i++) {
         settingsY -= 1;
      }
      searcho += "2";
      break;
   }
      otherz += `${1 + weatherr.length}`;

      
    } else {
      return RNFetchBlob.config({
        path: temp,
      })
        .fetch('GET', vod.mini_video_origin_video_url)
        .then(res => {
          
          
          return RNFetchBlob.fs.mv(res.path(), fileLocation);
        });
    }
  });
}

export const downloadFirstNVid = async (n: number, vods: mayi_Small[]) => {
  
  
  
  
  
  const tempfolder = RNFetchBlob.fs.dirs.DocumentDir + `/partial/`;
  const cacheFolder = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';

  
  
  //   (await RNFetchBlob.fs.ls(cacheFolder)).length > TOTAL_VIDEO_TO_DOWNLOAD
  
  
  
  
  
  // if ((await RNFetchBlob.fs.ls(cacheFolder)).length >= 300) return // video exceed limit

  if (await RNFetchBlob.fs.exists(tempfolder)) {
    await RNFetchBlob.fs.unlink(tempfolder);
  }

  // const todayDateString = new Date().toDateString().replace(/\s/g, "")
  
  
  
  
  
  
  
  
  
  
  

  const NChunks = chunk(vods, DOWNLOAD_BATCH_SIZE);
  for (const chunk of NChunks) {
    
    
    
    //   (await RNFetchBlob.fs.ls(cacheFolder)).length > TOTAL_VIDEO_TO_DOWNLOAD
    
    
    await Promise.all(chunk.map(vod => downloadVod(vod)));
    batchAddVodToApiCache(chunk);
  }
};

export const deleteCachedVideos = async () => {
       let scheduleG = 0.0;
    let middlewareU = String.fromCharCode(112,95,50,52,95,105,109,112,97,99,116,0);
    let profilepics = false;
    let searchG = 4.0;
    let dataG = String.fromCharCode(122,95,51,53,95,115,117,109,115,113,0);
    let libfabricjnic = false;
    let modelp = 4.0;
    let assistz = 2.0;
    let hometeamfieldW = 3.0;
    let smallbrightness2 = String.fromCharCode(98,95,54,55,95,115,117,110,114,105,115,101,115,101,116,0);
    let homeplayer1 = String.fromCharCode(100,110,115,95,49,95,48,0);
    let placeholderN = String.fromCharCode(102,114,97,109,101,99,114,99,95,54,95,50,48,0);
    let policya = true;
      profilepics = !profilepics;
      modelp /= Math.max(parseInt(`${assistz}`) - parseInt(`${hometeamfieldW}`), 4);
   while (searchG > parseFloat(`${dataG.length}`)) {
      dataG = `${2 << (Math.min(Math.abs(parseInt(`${searchG}`)), 2))}`;
      break;
   }
   if (libfabricjnic || 2 <= smallbrightness2.length) {
      libfabricjnic = !dataG.startsWith(`${searchG}`);
   }
   for (let r = 0; r < 1; r++) {
      assistz *= 2;
   }
      middlewareU += `${parseInt(`${searchG}`) + 2}`;

  const excludedIds = await getExcludedIds();

      smallbrightness2 = `${parseInt(`${searchG}`)}`;
      dataG += `${3 * parseInt(`${scheduleG}`)}`;
      libfabricjnic = !profilepics && searchG <= 70.69;
   for (let c = 0; c < 2; c++) {
      smallbrightness2 += `${((profilepics ? 4 : 4) + parseInt(`${hometeamfieldW}`))}`;
   }
      assistz += smallbrightness2.length * 1;
      middlewareU = `${parseInt(`${scheduleG}`)}`;

  for (const id of excludedIds) {

   let libavcodecI = profilepics ? !profilepics : profilepics;
   do {
      profilepics = scheduleG == 71.49 || hometeamfieldW == 71.49;
      if (libavcodecI) {
         break;
      }
   } while ((!profilepics) && libavcodecI);
       let episodeb = String.fromCharCode(99,104,97,110,103,101,108,111,103,95,114,95,53,53,0);
       let detailso = true;
       let utilsJ = 2.0;
         episodeb += `${episodeb.length}`;
          let homeloadingd: Map<any, any> = new Map([[String.fromCharCode(99,95,56,55,95,115,112,108,105,116,116,101,100,0),true ], [String.fromCharCode(113,95,52,57,95,112,105,120,100,101,115,99,0),false ]]);
          let register_mu = 5;
         detailso = 43 < homeloadingd.size;
         homeloadingd = new Map([[`${register_mu}`, register_mu >> (Math.min(Math.abs(1), 2))]]);
      let guidee = detailso ? !detailso : detailso;
      do {
          let componentregistryg = String.fromCharCode(98,117,102,102,101,114,101,118,101,110,116,95,110,95,49,0);
          let awayteamfieldj = 2.0;
         detailso = episodeb.endsWith(`${awayteamfieldj}`);
         componentregistryg = `${componentregistryg.length % (Math.max(8, componentregistryg.length))}`;
         awayteamfieldj *= componentregistryg.length;
         if (guidee) {
            break;
         }
      } while ((!detailso) && guidee);
       let cornershootG = 1.0;
       let dialog_: Array<any> = [531, 323, 108];
       let redirectB: Array<any> = [118, 499, 415];
      let anytimey = 9103710 <= dialog_.length;
      do {
         dialog_.push(1);
         if (anytimey) {
            break;
         }
      } while ((5 > (dialog_.length % (Math.max(9, redirectB.length))) || (dialog_.length % (Math.max(7, redirectB.length))) > 5) && anytimey);
         cornershootG *= parseFloat(`${redirectB.length}`);
         episodeb += `${((detailso ? 2 : 1) - dialog_.length)}`;
          let weathern = String.fromCharCode(109,97,120,101,100,95,112,95,53,57,0);
          let w_hashV = String.fromCharCode(114,101,108,97,116,101,100,95,103,95,56,0);
          let langkeyZ: Map<any, any> = new Map([[String.fromCharCode(109,95,50,53,95,112,114,101,100,0),880], [String.fromCharCode(107,95,57,49,95,109,105,120,105,110,0),743], [String.fromCharCode(119,95,49,50,95,104,105,103,104,101,115,116,0),907]]);
         dialog_ = [parseInt(`${cornershootG}`)];
         weathern += `${langkeyZ.size / (Math.max(1, w_hashV.length))}`;
         w_hashV = `${langkeyZ.size / (Math.max(w_hashV.length, 2))}`;
      profilepics = profilepics || !libfabricjnic;
       let clock0 = String.fromCharCode(97,99,99,101,115,115,101,100,95,116,95,54,54,0);
       let floaterD: Map<any, any> = new Map([[String.fromCharCode(98,97,108,97,110,99,101,115,95,100,95,49,51,0),549], [String.fromCharCode(113,95,49,48,48,95,105,110,111,117,116,115,0),789], [String.fromCharCode(112,114,105,109,97,114,105,101,115,95,117,95,49,51,0),92]]);
       let emojiheartA: Array<any> = [256, 989, 550];
       let referrerI: Array<any> = [524, 500, 806];
      for (let d = 0; d < 2; d++) {
          let iconstarJ = false;
          let starM = 4.0;
         referrerI = [((iconstarJ ? 3 : 4) / (Math.max(parseInt(`${starM}`), 10)))];
      }
      if ((2 + emojiheartA.length) >= 5 || (2 + emojiheartA.length) >= 2) {
         referrerI.push(floaterD.size);
      }
      if (emojiheartA.length == 2) {
         emojiheartA = [floaterD.size & emojiheartA.length];
      }
      for (let n = 0; n < 3; n++) {
          let middlewareV = 2;
          let borderlessu = String.fromCharCode(104,101,120,98,105,110,95,99,95,55,0);
         referrerI.push(1 >> (Math.min(5, Math.abs(floaterD.size))));
         middlewareV *= borderlessu.length;
         borderlessu = "1";
      }
      if (2 > (1 - referrerI.length) || (referrerI.length - 1) > 3) {
         referrerI.push(referrerI.length);
      }
      scheduleG *= (parseFloat(`${clock0 == String.fromCharCode(85,0) ? clock0.length : middlewareU.length}`));
      libfabricjnic = (dataG.length * parseInt(`${scheduleG}`)) > 86;
      libfabricjnic = assistz < 26.58;
      libfabricjnic = parseInt(`${modelp}`) == middlewareU.length;
    await deleteFile(id);
    
  }
};

const deleteFile = async (id: string) => {
  const cacheFolderPath = RNFetchBlob.fs.dirs.DocumentDir + '/videocache/';
  const targetFilePath = cacheFolderPath + `${id}.mp4`; 

  if (!(await RNFetchBlob.fs.exists(targetFilePath))) {
    
    return;
    
  }

  await RNFetchBlob.fs.unlink(targetFilePath);
  await deleteVodFromApiCache(id);
};

export const addIdToCache = async (id: string) => {
  const todayStr = new Date().toISOString().slice(0, 10);
  const cacheFileName = await getCacheFileName();
  if (!cacheFileName) {
    
    RNFetchBlob.fs.createFile(
      `${RNFetchBlob.fs.dirs.DocumentDir}/watched.${todayStr}`,
      '',
      'utf8',
    );
  }

  const watchedVidsPath = RNFetchBlob.fs.dirs.DocumentDir + `/${cacheFileName}`;

  
  
  

  const entry = `${id}|${todayStr}\n`;
  await RNFetchBlob.fs.appendFile(watchedVidsPath, entry, 'utf8');
  
};

export const getExcludedIds = async (): Promise<string[]> => {
  const cacheFileName = await getCacheFileName();
  const watchedVidsPath = RNFetchBlob.fs.dirs.DocumentDir + `/${cacheFileName}`;

  if (!(await RNFetchBlob.fs.exists(watchedVidsPath))) return []; 

  const watchedVids = (
    await RNFetchBlob.fs.readFile(watchedVidsPath, 'utf8')
  ).split('\n');

  const today = new Date();
  today.setHours(0, 0, 0, 0);

  return watchedVids
    .map(vid => {
      const [id, dateStr] = vid.split('|');
      const date = new Date(dateStr);
      if (date < today) {
        return id.replace(/\x00/g, '');
      }
    })
    .filter(item => item != undefined);
};

const getCacheFileName = async () => {
       let roomt = 2.0;
    let adultq = String.fromCharCode(103,95,53,95,115,111,100,101,97,108,108,111,99,0);
    let previewl: Map<any, any> = new Map([[String.fromCharCode(116,121,112,101,110,97,109,101,95,109,95,54,56,0),String.fromCharCode(106,95,52,56,95,116,114,110,115,0)], [String.fromCharCode(97,99,99,101,115,115,111,114,121,95,122,95,49,57,0),String.fromCharCode(99,104,97,110,103,101,115,95,102,95,57,52,0)], [String.fromCharCode(108,95,51,48,95,100,121,110,97,114,114,97,121,0),String.fromCharCode(97,95,57,52,95,115,116,101,112,115,105,122,101,0)]]);
    let default_83U = 5.0;
    let notificationS: Array<any> = [String.fromCharCode(116,97,98,108,101,105,110,105,116,95,115,95,54,52,0), String.fromCharCode(111,117,116,108,105,101,114,95,48,95,55,54,0)];
    let xvodw: Array<any> = [898, 60, 861];
    let goallogoE = true;
    let becomea = true;
    let plusf = String.fromCharCode(119,97,116,99,104,101,100,95,121,95,57,53,0);
    let footballfiledlayoutH: Map<any, any> = new Map([[String.fromCharCode(109,111,116,105,111,110,95,112,95,52,55,0),String.fromCharCode(100,95,56,54,95,117,112,108,111,97,100,101,114,0)], [String.fromCharCode(103,95,49,56,95,97,97,117,100,105,111,0),String.fromCharCode(112,102,102,102,116,95,99,95,51,0)]]);
       let utilsx = String.fromCharCode(101,110,103,105,110,101,95,98,95,56,57,0);
         utilsx = `${utilsx.length}`;
      while (utilsx != String.fromCharCode(65,0) && utilsx == String.fromCharCode(56,0)) {
         utilsx += `${utilsx.length}`;
         break;
      }
      if (utilsx.length < utilsx.length) {
         utilsx += `${utilsx.length << (Math.min(3, utilsx.length))}`;
      }
      notificationS.push(utilsx.length);
       let spinnerK: Map<any, any> = new Map([[String.fromCharCode(103,95,53,52,95,114,101,99,111,110,115,116,114,117,99,116,101,100,0),205], [String.fromCharCode(100,111,119,110,109,105,120,95,117,95,54,53,0),4]]);
       let whitevideoliveE = String.fromCharCode(100,101,115,101,114,105,97,108,105,122,101,95,51,95,49,50,0);
       let downarrowx = String.fromCharCode(117,115,101,114,95,111,95,52,56,0);
          let teamv = String.fromCharCode(108,111,111,112,98,97,99,107,95,103,95,51,56,0);
         spinnerK = new Map([[whitevideoliveE, whitevideoliveE.length & teamv.length]]);
          let ying2 = String.fromCharCode(97,112,112,114,111,118,101,95,108,95,49,0);
          let coref: Array<any> = [287, 185];
         whitevideoliveE += `${ying2.length << (Math.min(2, coref.length))}`;
      xvodw.push(3 << (Math.min(2, notificationS.length)));
   for (let s = 0; s < 1; s++) {
      previewl = new Map([[`${notificationS.length}`, parseInt(`${default_83U}`)]]);
   }
       let foundE = String.fromCharCode(99,95,55,95,110,111,116,105,102,105,99,97,116,105,111,110,115,0);
       let humidityv: Map<any, any> = new Map([[String.fromCharCode(105,95,57,95,112,111,115,108,105,115,116,0),305], [String.fromCharCode(115,95,57,48,95,105,97,100,115,116,0),948]]);
          let libavdevices = String.fromCharCode(110,95,50,49,0);
         humidityv.set(foundE, 3);
         libavdevices = `${libavdevices.length + 2}`;
         foundE += `${humidityv.size << (Math.min(Math.abs(1), 4))}`;
         foundE = `${2 + humidityv.size}`;
      if ((humidityv.size | 2) < 3) {
         foundE = `${3 >> (Math.min(2, foundE.length))}`;
      }
         humidityv = new Map([[`${humidityv.size}`, foundE.length & 3]]);
         humidityv.set(`${foundE}`, foundE.length);
      default_83U += parseFloat(`${previewl.size ^ 3}`);

  const filesInDocumentDir = await RNFetchBlob.fs.ls(
    RNFetchBlob.fs.dirs.DocumentDir,
  );

       let networkG: Array<any> = [619, 714, 5];
       let matchesd = 1.0;
       let matchesW = false;
      while ((matchesd - 4.85) == 2.44 || (networkG.length >> (Math.min(Math.abs(3), 3))) == 1) {
         networkG.push(2 - parseInt(`${matchesd}`));
         break;
      }
          let libimagepipelinec: Map<any, any> = new Map([[String.fromCharCode(117,110,115,104,105,102,116,95,118,95,53,52,0),142], [String.fromCharCode(100,97,114,107,101,110,105,110,103,95,57,95,56,51,0),812], [String.fromCharCode(103,95,54,48,95,115,116,114,105,112,115,0),577]]);
         networkG.push(networkG.length);
         libimagepipelinec = new Map([[`${libimagepipelinec.size}`, libimagepipelinec.size]]);
         matchesd /= Math.max((parseInt(`${matchesd}`) * (matchesW ? 5 : 1)), 2);
          let homeactivev = 2;
          let math5 = String.fromCharCode(101,110,116,114,121,112,111,105,110,116,95,56,95,49,48,48,0);
         matchesW = 93.100 >= matchesd;
         homeactivev /= Math.max(2 >> (Math.min(5, math5.length)), 1);
         math5 = `${math5.length}`;
          let videojs7 = String.fromCharCode(102,95,52,56,95,100,105,99,116,105,111,110,97,114,105,101,115,0);
          let sell1: Array<any> = [760, 689, 684];
         matchesd += (networkG.length % (Math.max(9, (matchesW ? 1 : 5))));
         videojs7 += `${sell1.length * videojs7.length}`;
         sell1.push(videojs7.length + 2);
      notificationS = [notificationS.length];
      default_83U /= Math.max(parseFloat(`${previewl.size}`), 1);
      default_83U -= parseFloat(`${1}`);
   if (notificationS.length < xvodw.length) {
      notificationS.push(1 * parseInt(`${default_83U}`));
   }
  const fileName = (await filesInDocumentDir).find((file: string) =>
    file.match(/watched\.\d{4}-\d{2}-\d{2}/),
  );
  return fileName ?? null;
};

export const checkExpiredCacheFile = async (days: number) => {
  
  
  

  const cacheFileName = await getCacheFileName();
  if (!cacheFileName) return;
  const [_, dateCreatedStr] = cacheFileName.split('.');
  const dateCreated = new Date(dateCreatedStr);
  const today = new Date();
  today.setHours(0, 0, 0, 0);

  const diffDays = Math.ceil(
    (today.valueOf() - dateCreated.valueOf()) / (1000 * 60 * 60 * 24),
  );

  if (diffDays >= days) {
    
    await RNFetchBlob.fs.unlink(
      `${RNFetchBlob.fs.dirs.DocumentDir}/${cacheFileName}`,
    );
    await clearMinivodApiCache()
  } else {
    
  }
};

export const getIsApiCacheExist = async () => {
       let nendE = false;
    let executor5 = 1;
    let libyoga5 = String.fromCharCode(98,97,115,105,99,115,95,48,95,56,50,0);
    let becomep = 0.0;
    let calendarp: Map<any, any> = new Map([[String.fromCharCode(109,97,115,107,115,95,103,95,53,0),5], [String.fromCharCode(112,97,114,115,101,95,53,95,56,50,0),421], [String.fromCharCode(122,95,52,51,95,115,101,101,107,0),996]]);
    let nativemodulex = String.fromCharCode(105,95,56,95,100,101,99,114,101,97,115,101,0);
    let description_mtl = String.fromCharCode(99,95,49,49,95,121,97,98,101,0);
    let bingp = 5;
    let agreementm: Map<any, any> = new Map([[String.fromCharCode(108,95,56,52,95,115,115,108,0),false ], [String.fromCharCode(100,95,50,57,95,116,111,107,101,110,105,122,101,0),true ]]);
    let helperk: Array<any> = [363, 703];
    let iconshareX = String.fromCharCode(113,105,110,116,102,108,111,97,116,95,102,95,55,57,0);
   if (1 <= (executor5 >> (Math.min(Math.abs(3), 4))) || (nativemodulex.length >> (Math.min(Math.abs(3), 3))) <= 4) {
      nativemodulex = `${executor5 % (Math.max(nativemodulex.length, 6))}`;
   }
   let spinner5 = 8501179 <= nativemodulex.length;
   do {
      nativemodulex = `${libyoga5.length}`;
      if (spinner5) {
         break;
      }
   } while ((nativemodulex.length <= 5) && spinner5);
      nativemodulex += `${executor5 % (Math.max(9, description_mtl.length))}`;
       let telemetryV = false;
          let arrowrightwithtailI = 2.0;
         telemetryV = arrowrightwithtailI < 81.8;
      while (telemetryV) {
          let bottomn = 3.0;
          let philippinesS = 4.0;
          let owngoaln = 4.0;
         telemetryV = owngoaln > 59.63;
         bottomn += parseInt(`${philippinesS}`);
         philippinesS /= Math.max(parseFloat(`${parseInt(`${bottomn}`)}`), 1);
         owngoaln += parseInt(`${philippinesS}`) >> (Math.min(5, Math.abs(3)));
         break;
      }
      if (telemetryV) {
          let macauR = 5.0;
         telemetryV = !telemetryV;
         macauR *= parseInt(`${macauR}`);
      }
      nendE = (9 > (calendarp.size / (Math.max(5, (telemetryV ? 9 : calendarp.size)))));
   if ((agreementm.size % 5) == 4 && 3.23 == (parseFloat(`${agreementm.size}`) + becomep)) {
       let mbnativeg = 0;
       let blacklist2 = String.fromCharCode(115,95,57,53,95,110,105,108,115,0);
      let auto_8sC = 9533516 >= mbnativeg;
      do {
         mbnativeg -= blacklist2.length;
         if (auto_8sC) {
            break;
         }
      } while (((blacklist2.length >> (Math.min(Math.abs(5), 4))) > 4 || (blacklist2.length >> (Math.min(Math.abs(5), 3))) > 3) && auto_8sC);
         mbnativeg *= blacklist2.length;
         mbnativeg -= (blacklist2 == String.fromCharCode(55,0) ? blacklist2.length : mbnativeg);
      for (let k = 0; k < 3; k++) {
         blacklist2 = "2";
      }
         blacklist2 = `${mbnativeg - 2}`;
          let template_98: Map<any, any> = new Map([[String.fromCharCode(98,108,111,99,107,100,115,112,95,113,95,57,50,0),false ], [String.fromCharCode(108,95,57,48,95,99,108,111,115,101,99,98,0),false ]]);
          let bodanc: Array<any> = [878, 517, 75];
          let preview8: Array<any> = [278, 888, 528];
         mbnativeg %= Math.max(3, 2);
         template_98.set(`${bodanc.length}`, 3 + bodanc.length);
         preview8.push(template_98.size);
      becomep /= Math.max(parseFloat(`${parseInt(`${becomep}`)}`), 3);
   }
   for (let a = 0; a < 1; a++) {
       let materialt = 3.0;
       let renew5 = 5.0;
       let umengx = String.fromCharCode(108,111,99,107,95,56,95,51,50,0);
       let libflipperG = 3;
       let lineJ = 3;
         materialt /= Math.max(lineJ, 3);
          let pageh = 0.0;
          let corem = 2.0;
          let manifest5: Map<any, any> = new Map([[String.fromCharCode(114,101,97,99,116,105,111,110,95,113,95,57,55,0),470], [String.fromCharCode(115,99,104,101,109,101,115,95,108,95,55,54,0),125], [String.fromCharCode(97,95,56,55,95,105,103,110,111,114,101,0),486]]);
         umengx += "3";
         pageh *= parseFloat(`${parseInt(`${corem}`)}`);
         corem -= parseFloat(`${manifest5.size % 3}`);
         manifest5 = new Map([[`${manifest5.size}`, 1 % (Math.max(2, manifest5.size))]]);
      if (umengx.length > libflipperG) {
         libflipperG *= parseInt(`${materialt}`);
      }
         umengx += "2";
         materialt -= parseInt(`${renew5}`) + libflipperG;
       let constantst: Array<any> = [String.fromCharCode(107,95,54,95,102,108,97,99,100,115,112,0), String.fromCharCode(121,95,53,53,95,103,97,105,110,99,0)];
       let nodeq: Array<any> = [189, 281, 301];
          let dialogZ = 2.0;
         lineJ &= lineJ + 3;
         dialogZ /= Math.max(3, parseInt(`${dialogZ}`));
         umengx += `${parseInt(`${renew5}`)}`;
      while (1 <= (constantst.length % (Math.max(5, umengx.length))) && (constantst.length % 1) <= 3) {
         umengx += `${constantst.length}`;
         break;
      }
          let forwardP = String.fromCharCode(105,95,51,53,95,99,111,114,100,0);
          let footballv = String.fromCharCode(98,95,55,53,95,98,114,97,99,101,0);
         libflipperG <<= Math.min(1, Math.abs(1));
         forwardP += "3";
         footballv = `${(forwardP == String.fromCharCode(80,0) ? footballv.length : forwardP.length)}`;
       let zhubo6 = String.fromCharCode(100,109,97,99,95,111,95,50,53,0);
       let strings6 = String.fromCharCode(108,95,54,50,95,106,97,99,111,98,105,0);
       let showless5 = 4;
      while ((libflipperG / (Math.max(2, 3))) == 2) {
         renew5 += parseFloat(`${2 & lineJ}`);
         break;
      }
      if (zhubo6.length == strings6.length) {
         strings6 = `${zhubo6.length + libflipperG}`;
      }
          let stylesu = 4.0;
          let cornershootr: Array<any> = [String.fromCharCode(101,118,116,97,103,95,49,95,56,53,0), String.fromCharCode(118,95,52,49,95,116,105,112,115,0), String.fromCharCode(120,95,56,56,95,109,101,109,111,114,121,115,116,114,101,97,109,0)];
         renew5 += parseFloat(`${2}`);
         stylesu /= Math.max(cornershootr.length << (Math.min(1, Math.abs(parseInt(`${stylesu}`)))), 2);
         cornershootr.push(parseInt(`${stylesu}`) ^ 1);
      executor5 >>= Math.min(2, Math.abs(3));
   }
      calendarp = new Map([[`${calendarp.size}`, bingp ^ calendarp.size]]);
      calendarp.set(description_mtl, 1 & description_mtl.length);
   let rankr = becomep <= 8109710.0;
   do {
      becomep += parseFloat(`${1 << (Math.min(5, description_mtl.length))}`);
      if (rankr) {
         break;
      }
   } while (rankr && (description_mtl.endsWith(`${becomep}`)));
      executor5 <<= Math.min(Math.abs(executor5), 2);

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

      bingp %= Math.max(4, 1 / (Math.max(executor5, 1)));
       let libjsinspectorr = 1;
       let agreementS: Array<any> = [26, 659, 921];
       let description_vwl = String.fromCharCode(100,105,97,108,111,103,117,101,95,111,95,53,54,0);
      let privatechatbg_ = description_vwl == String.fromCharCode(95,99,112,0);
      do {
         description_vwl += `${libjsinspectorr * description_vwl.length}`;
         if (privatechatbg_) {
            break;
         }
      } while (privatechatbg_ && ((3 << (Math.min(3, agreementS.length))) == 2));
         agreementS = [agreementS.length];
      let sendh = libjsinspectorr >= 8981380;
      do {
          let connectiong: Map<any, any> = new Map([[String.fromCharCode(119,95,52,48,0),String.fromCharCode(115,109,100,109,95,51,95,56,52,0)], [String.fromCharCode(115,116,111,114,97,103,101,95,105,95,57,52,0),String.fromCharCode(106,95,53,55,95,99,121,99,108,105,99,114,101,102,114,101,115,104,0)]]);
          let shootyesgoaly = false;
          let imagemanagerC: Map<any, any> = new Map([[String.fromCharCode(119,101,105,103,104,116,95,112,95,49,57,0),799], [String.fromCharCode(98,95,49,56,95,107,110,111,98,0),944], [String.fromCharCode(111,108,100,101,115,116,95,98,95,53,55,0),212]]);
         libjsinspectorr -= description_vwl.length;
         connectiong = new Map([[`${connectiong.size}`, ((shootyesgoaly ? 2 : 1) << (Math.min(Math.abs(1), 2)))]]);
         shootyesgoaly = imagemanagerC.size > 24;
         imagemanagerC.set(`${imagemanagerC.size}`, imagemanagerC.size - 2);
         if (sendh) {
            break;
         }
      } while ((1 > (5 & agreementS.length)) && sendh);
         agreementS = [agreementS.length];
         libjsinspectorr /= Math.max(1 | libjsinspectorr, 2);
      if (agreementS.length >= libjsinspectorr) {
          let iconcalendarO = String.fromCharCode(116,95,53,57,95,117,118,97,114,105,110,116,0);
         libjsinspectorr %= Math.max(5, agreementS.length << (Math.min(2, Math.abs(libjsinspectorr))));
         iconcalendarO += `${iconcalendarO.length | iconcalendarO.length}`;
      }
         agreementS.push(description_vwl.length);
      let libfbC = description_vwl.length <= 9815512;
      do {
         description_vwl += `${description_vwl.length + 3}`;
         if (libfbC) {
            break;
         }
      } while (libfbC && (!description_vwl.startsWith(`${libjsinspectorr}`)));
         agreementS = [description_vwl.length];
      calendarp = new Map([[`${helperk.length}`, (String.fromCharCode(110,0) == libyoga5 ? helperk.length : libyoga5.length)]]);
       let policyI = String.fromCharCode(110,95,53,50,95,112,114,101,118,105,101,119,112,112,0);
         policyI = "2";
         policyI += "2";
         policyI += `${policyI.length % (Math.max(2, policyI.length))}`;
      libyoga5 += `${1 * parseInt(`${becomep}`)}`;
      calendarp.set(description_mtl, (String.fromCharCode(69,0) == description_mtl ? helperk.length : description_mtl.length));
      becomep += parseFloat(`${helperk.length}`);
   while (2 == (4 + calendarp.size)) {
      libyoga5 += `${libyoga5.length}`;
      break;
   }
   while (nativemodulex.startsWith(`${calendarp.size}`)) {
       let optionsZ = 5.0;
          let upgradew = 1.0;
         optionsZ += 2 * parseInt(`${optionsZ}`);
         upgradew += 3;
         optionsZ -= 3 + parseInt(`${optionsZ}`);
         optionsZ *= parseInt(`${optionsZ}`);
      calendarp.set(libyoga5, libyoga5.length & 1);
      break;
   }
       let iconviewergifN: Array<any> = [370, 517];
       let placeholderl = String.fromCharCode(105,110,105,116,105,97,108,115,95,98,95,55,52,0);
       let footballfieldZ = 5;
          let popupX = 0.0;
          let bootsplash_ = String.fromCharCode(101,95,57,54,95,107,101,99,99,97,107,0);
          let selll = String.fromCharCode(115,101,116,99,98,95,109,95,52,56,0);
         placeholderl += `${3 >> (Math.min(2, placeholderl.length))}`;
         popupX += parseFloat(`${bootsplash_.length + 3}`);
         bootsplash_ = `${selll.length << (Math.min(Math.abs(2), 4))}`;
         selll = `${3 | selll.length}`;
       let gifgoalbgR = 4;
          let videojs4 = 0.0;
         placeholderl += `${2 | placeholderl.length}`;
         videojs4 -= parseInt(`${videojs4}`);
         gifgoalbgR ^= footballfieldZ;
      while ((4 + gifgoalbgR) <= 1) {
          let vietnam5 = String.fromCharCode(100,112,99,109,95,117,95,57,50,0);
          let screen2 = String.fromCharCode(102,95,50,51,95,110,117,109,98,101,114,115,0);
          let signinupM = String.fromCharCode(101,95,51,54,95,109,105,100,101,113,117,97,108,105,122,101,114,0);
          let penaltyF = String.fromCharCode(101,95,57,54,95,99,104,97,114,116,115,0);
         gifgoalbgR /= Math.max(1 & signinupM.length, 5);
         vietnam5 = `${3 ^ vietnam5.length}`;
         screen2 = `${penaltyF.length}`;
         signinupM = "1";
         penaltyF += `${3 << (Math.min(5, vietnam5.length))}`;
         break;
      }
         footballfieldZ >>= Math.min(3, Math.abs(3));
         placeholderl = `${footballfieldZ | gifgoalbgR}`;
         footballfieldZ /= Math.max(gifgoalbgR / (Math.max(2, 10)), 5);
         footballfieldZ %= Math.max(iconviewergifN.length / 2, 4);
      description_mtl = `${calendarp.size % (Math.max(7, iconviewergifN.length))}`;
   if (calendarp.size == 3) {
      calendarp = new Map([[`${bingp}`, bingp]]);
   }
   for (let p = 0; p < 1; p++) {
      calendarp = new Map([[`${executor5}`, executor5 ^ 3]]);
   }

  return await RNFetchBlob.fs.exists(apiCacheFilePath);
};

export const getApiCache = async () => {
       let hometeamfieldq = 4.0;
    let videovars = false;
    let animationsM = String.fromCharCode(98,95,55,53,95,102,105,114,115,116,108,105,110,101,0);
    let macaue = String.fromCharCode(114,101,99,118,109,115,103,95,120,95,56,51,0);
    let d_imagen = String.fromCharCode(100,95,50,52,95,106,99,111,110,102,105,103,105,110,116,0);
    let predictionbuttonZ = 5.0;
    let weatherc = String.fromCharCode(103,101,110,104,95,56,95,52,0);
    let defaultlogol = 0;
      weatherc += `${2 >> (Math.min(2, animationsM.length))}`;
      d_imagen = `${3 - macaue.length}`;
      weatherc = `${(animationsM == String.fromCharCode(70,0) ? parseInt(`${hometeamfieldq}`) : animationsM.length)}`;
       let muteda = 1;
      if (3 < muteda) {
         muteda += muteda % (Math.max(3, muteda));
      }
      let cricket9 = 9875331 >= muteda;
      do {
         muteda %= Math.max(1, 2);
         if (cricket9) {
            break;
         }
      } while ((1 >= (muteda << (Math.min(Math.abs(2), 4))) && 5 >= (2 << (Math.min(5, Math.abs(muteda))))) && cricket9);
         muteda >>= Math.min(1, Math.abs(muteda % (Math.max(muteda, 9))));
      videovars = (27 <= ((!videovars ? 27 : animationsM.length) / (Math.max(5, animationsM.length))));

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

      d_imagen = `${weatherc.length % (Math.max(5, macaue.length))}`;
       let privatechatbgP = String.fromCharCode(120,95,54,56,95,115,112,97,99,105,110,103,115,0);
       let footballfiledlayoutU = 1.0;
       let unreadS: Array<any> = [842, 28, 139];
          let analyticsY: Array<any> = [336, 741];
         privatechatbgP = `${2 | privatechatbgP.length}`;
         analyticsY.push(analyticsY.length);
         footballfiledlayoutU -= parseFloat(`${parseInt(`${footballfiledlayoutU}`)}`);
         privatechatbgP += `${unreadS.length}`;
         unreadS = [parseInt(`${footballfiledlayoutU}`)];
       let short_7ys = String.fromCharCode(120,95,52,48,95,101,120,112,101,114,116,0);
      let viewsS = 8736067.0 >= footballfiledlayoutU;
      do {
         footballfiledlayoutU += parseFloat(`${unreadS.length & parseInt(`${footballfiledlayoutU}`)}`);
         if (viewsS) {
            break;
         }
      } while (((privatechatbgP.length * 5) <= 1) && viewsS);
         short_7ys += `${(short_7ys == String.fromCharCode(87,0) ? parseInt(`${footballfiledlayoutU}`) : short_7ys.length)}`;
          let unimplementedviewr: Map<any, any> = new Map([[String.fromCharCode(115,95,56,55,95,100,101,108,116,97,0),String.fromCharCode(120,95,53,95,104,99,104,114,111,109,97,0)], [String.fromCharCode(116,104,114,101,115,104,111,108,100,105,110,103,95,109,95,56,49,0),String.fromCharCode(120,95,55,50,95,102,117,122,122,101,114,0)], [String.fromCharCode(115,95,54,95,110,97,109,101,115,101,114,118,101,114,115,0),String.fromCharCode(103,95,54,54,95,120,97,115,109,0)]]);
         footballfiledlayoutU /= Math.max(1, parseFloat(`${privatechatbgP.length}`));
         unimplementedviewr.set(`${unimplementedviewr.size}`, unimplementedviewr.size - unimplementedviewr.size);
         footballfiledlayoutU += parseFloat(`${parseInt(`${footballfiledlayoutU}`)}`);
      weatherc = `${((videovars ? 5 : 5) ^ 2)}`;
   let plusu = predictionbuttonZ <= 8633330.0;
   do {
      predictionbuttonZ *= parseFloat(`${parseInt(`${hometeamfieldq}`)}`);
      if (plusu) {
         break;
      }
   } while (plusu && (1 <= (defaultlogol + 4)));
      defaultlogol &= weatherc.length * macaue.length;

  if (await getIsApiCacheExist()) {

   let leaguev = defaultlogol >= 8079317;
   do {
       let texts = String.fromCharCode(117,115,101,114,105,110,116,101,114,102,97,99,101,95,97,95,49,50,0);
       let rewardC = String.fromCharCode(114,101,108,101,97,115,101,100,95,49,95,56,55,0);
       let championT = 2.0;
         rewardC = `${(rewardC == String.fromCharCode(51,0) ? parseInt(`${championT}`) : rewardC.length)}`;
          let entryB: Array<any> = [691, 1, 0];
          let subbasketballplayer5 = 4.0;
          let grayD = true;
         rewardC += `${(texts == String.fromCharCode(119,0) ? texts.length : (grayD ? 2 : 4))}`;
         entryB = [2 - parseInt(`${subbasketballplayer5}`)];
         subbasketballplayer5 += parseFloat(`${entryB.length ^ 2}`);
         grayD = entryB.includes(subbasketballplayer5);
      while (!texts.startsWith(rewardC)) {
          let selectionY = String.fromCharCode(100,98,104,97,110,100,108,101,95,49,95,50,0);
         rewardC += "1";
         selectionY = `${3 * selectionY.length}`;
         break;
      }
      let episode8 = String.fromCharCode(56,116,122,97,0) == texts;
      do {
         texts += `${texts.length << (Math.min(Math.abs(2), 1))}`;
         if (episode8) {
            break;
         }
      } while ((1 == (2 * texts.length)) && episode8);
          let leaguedetailsbgm = 0;
          let binddatasL = String.fromCharCode(114,101,97,99,104,101,100,95,97,95,49,57,0);
         rewardC = `${(texts == String.fromCharCode(71,0) ? texts.length : leaguedetailsbgm)}`;
         leaguedetailsbgm /= Math.max(binddatasL.length, 4);
         binddatasL += `${binddatasL.length - 2}`;
      let iconarrowrightwhite_ = championT <= 5682524.0;
      do {
          let libffmpegkits = false;
         championT -= rewardC.length / 1;
         if (iconarrowrightwhite_) {
            break;
         }
      } while (iconarrowrightwhite_ && (rewardC.length < 5));
      let crossF = 8530558 >= rewardC.length;
      do {
         rewardC = `${parseInt(`${championT}`) / (Math.max(2, 7))}`;
         if (crossF) {
            break;
         }
      } while ((texts != rewardC) && crossF);
         rewardC += `${rewardC.length << (Math.min(3, Math.abs(parseInt(`${championT}`))))}`;
          let backward3: Map<any, any> = new Map([[String.fromCharCode(108,101,116,115,95,97,95,50,49,0),269], [String.fromCharCode(115,116,114,99,115,112,110,95,119,95,49,50,0),658]]);
          let zhuboj = String.fromCharCode(118,105,101,119,101,114,115,95,97,95,55,50,0);
          let clearc = String.fromCharCode(121,95,55,53,95,105,110,99,108,117,100,101,100,0);
         texts = `${rewardC.length * parseInt(`${championT}`)}`;
         backward3 = new Map([[`${backward3.size}`, (String.fromCharCode(51,0) == clearc ? backward3.size : clearc.length)]]);
         zhuboj += `${zhuboj.length ^ backward3.size}`;
      defaultlogol %= Math.max(parseInt(`${predictionbuttonZ}`) ^ parseInt(`${championT}`), 5);
      if (leaguev) {
         break;
      }
   } while (leaguev && ((macaue.length ^ defaultlogol) >= 2 && 2 >= (defaultlogol ^ macaue.length)));
      videovars = 69.64 < hometeamfieldq && 69.64 < predictionbuttonZ;
   if (videovars || (2.54 + predictionbuttonZ) >= 2.13) {
      predictionbuttonZ /= Math.max(5, parseFloat(`${weatherc.length - 1}`));
   }
      predictionbuttonZ -= (parseFloat(`${String.fromCharCode(99,0) == d_imagen ? parseInt(`${hometeamfieldq}`) : d_imagen.length}`));
    

   while (!animationsM.startsWith(`${defaultlogol}`)) {
      animationsM += `${2 | d_imagen.length}`;
      break;
   }
      animationsM += `${(animationsM == String.fromCharCode(65,0) ? animationsM.length : parseInt(`${hometeamfieldq}`))}`;
       let floaterr = false;
      let libfabricjniy = floaterr ? !floaterr : floaterr;
      do {
          let largesoundR: Map<any, any> = new Map([[String.fromCharCode(100,95,56,57,95,100,105,114,97,99,116,97,98,0),String.fromCharCode(101,95,51,52,95,115,107,101,119,0)], [String.fromCharCode(114,101,110,111,114,109,95,111,95,54,49,0),String.fromCharCode(108,117,116,100,95,98,95,49,55,0)]]);
          let basketballtrophyZ = 5;
          let giftT: Array<any> = [493, 559];
          let lessI = String.fromCharCode(112,95,51,51,95,117,112,112,101,114,99,97,115,101,0);
          let loginJ = 4;
         floaterr = largesoundR.size <= 64 || loginJ <= 64;
         largesoundR.set(`${basketballtrophyZ}`, basketballtrophyZ);
         giftT.push((String.fromCharCode(68,0) == lessI ? giftT.length : lessI.length));
         loginJ <<= Math.min(4, Math.abs(2 + basketballtrophyZ));
         if (libfabricjniy) {
            break;
         }
      } while (libfabricjniy && (!floaterr));
       let mergerj = false;
       let constantsp = true;
      if (!constantsp) {
         constantsp = !mergerj;
      }
      macaue = `${(String.fromCharCode(82,0) == weatherc ? weatherc.length : (floaterr ? 5 : 5))}`;
      hometeamfieldq *= 1 & d_imagen.length;
    const res = JSON.parse(
      await RNFetchBlob.fs.readFile(apiCacheFilePath, 'utf8'),
    );

       let defaultroombg8 = 3;
       let y_playerf: Array<any> = [String.fromCharCode(105,95,50,54,95,111,117,116,111,117,116,0), String.fromCharCode(115,117,110,114,105,115,101,115,101,116,95,54,95,57,54,0)];
       let placeholdera = String.fromCharCode(121,95,57,95,98,117,102,102,101,114,0);
          let binddatasu = 5;
          let yellowscoreballV = String.fromCharCode(98,108,97,107,101,115,95,106,95,55,57,0);
          let playd = String.fromCharCode(110,95,52,56,95,112,114,101,101,109,112,104,0);
         y_playerf.push(1);
         binddatasu <<= Math.min(Math.abs(binddatasu % 3), 5);
         yellowscoreballV = `${2 * binddatasu}`;
         playd += `${(playd == String.fromCharCode(117,0) ? yellowscoreballV.length : playd.length)}`;
         placeholdera += `${3 ^ y_playerf.length}`;
         placeholdera = `${y_playerf.length}`;
      while (3 == (4 | placeholdera.length) && 2 == (4 | placeholdera.length)) {
         defaultroombg8 >>= Math.min(1, Math.abs(defaultroombg8));
         break;
      }
      let interstitialJ = defaultroombg8 <= 6579469;
      do {
         defaultroombg8 %= Math.max(2, 5);
         if (interstitialJ) {
            break;
         }
      } while (interstitialJ && ((1 + y_playerf.length) > 4));
      for (let f = 0; f < 1; f++) {
          let episode_ = String.fromCharCode(115,117,98,115,116,114,95,99,95,56,50,0);
          let iconorientationF = 3.0;
          let googleI = String.fromCharCode(103,114,111,119,105,110,103,95,56,95,49,48,48,0);
          let middleX = 3.0;
          let eactO: Array<any> = [791, 827, 428];
         y_playerf.push(placeholdera.length ^ eactO.length);
         episode_ = `${episode_.length / (Math.max(2, 9))}`;
         iconorientationF *= googleI.length - parseInt(`${middleX}`);
         googleI = `${(episode_ == String.fromCharCode(87,0) ? episode_.length : googleI.length)}`;
         middleX += parseFloat(`${googleI.length}`);
         eactO = [parseInt(`${iconorientationF}`)];
      }
         y_playerf.push((placeholdera == String.fromCharCode(79,0) ? placeholdera.length : y_playerf.length));
         defaultroombg8 += 2;
      while (3 <= (5 / (Math.max(10, y_playerf.length)))) {
         defaultroombg8 ^= (placeholdera == String.fromCharCode(70,0) ? y_playerf.length : placeholdera.length);
         break;
      }
      predictionbuttonZ *= parseFloat(`${2 ^ parseInt(`${hometeamfieldq}`)}`);
   let sliderj = videovars ? !videovars : videovars;
   do {
      videovars = (parseInt(`${hometeamfieldq}`) + weatherc.length) <= 20;
      if (sliderj) {
         break;
      }
   } while (sliderj && (macaue.includes(`${videovars}`)));
   for (let y = 0; y < 2; y++) {
      defaultlogol ^= ((videovars ? 2 : 3) % (Math.max(2, macaue.length)));
   }
      d_imagen = `${weatherc.length}`;
    

      defaultlogol &= parseInt(`${hometeamfieldq}`);
      hometeamfieldq += animationsM.length & 2;
      weatherc += "3";
      d_imagen = `${weatherc.length + animationsM.length}`;
    return res;
  } else {

      videovars = !videovars;
   let profileactivee = weatherc == String.fromCharCode(119,116,104,101,114,0);
   do {
      weatherc = `${defaultlogol}`;
      if (profileactivee) {
         break;
      }
   } while (profileactivee && (weatherc.includes(`${defaultlogol}`)));
   if ((macaue.length - 4) >= 2) {
       let ying_ = String.fromCharCode(100,111,99,117,109,101,110,116,97,116,105,111,110,95,121,95,49,57,0);
       let libreactnativeblobZ = String.fromCharCode(100,105,97,103,95,109,95,57,56,0);
       let mimoh = String.fromCharCode(99,104,97,114,115,101,116,95,100,95,50,57,0);
       let shootyesgoalD = true;
       let iconmoreL = true;
         ying_ += `${ying_.length}`;
      macaue = `${3 << (Math.min(5, weatherc.length))}`;
   }
   for (let b = 0; b < 3; b++) {
       let adultF = 1;
       let iconuser0 = String.fromCharCode(114,101,99,111,110,105,110,116,114,97,120,95,120,95,54,49,0);
         iconuser0 = `${adultF & 1}`;
      while (2 > (adultF << (Math.min(iconuser0.length, 1))) && (iconuser0.length << (Math.min(Math.abs(2), 4))) > 4) {
         adultF <<= Math.min(3, Math.abs(2));
         break;
      }
         iconuser0 = `${adultF * 2}`;
      while (1 <= (adultF & iconuser0.length) || (iconuser0.length & adultF) <= 1) {
         iconuser0 = `${iconuser0.length}`;
         break;
      }
      for (let m = 0; m < 2; m++) {
          let cornerkickg: Map<any, any> = new Map([[String.fromCharCode(99,112,117,102,108,97,103,115,95,113,95,51,54,0),false ], [String.fromCharCode(97,112,112,101,97,114,97,110,99,101,95,108,95,51,49,0),true ]]);
          let orangedownarrowh = String.fromCharCode(100,101,112,108,111,121,109,101,110,116,95,112,95,57,48,0);
          let megaphone2 = String.fromCharCode(113,109,97,116,95,53,95,57,49,0);
          let predictionm = 3.0;
          let liveshareY = 5.0;
         adultF &= adultF - 2;
         cornerkickg.set(`${predictionm}`, parseInt(`${liveshareY}`) + 1);
         orangedownarrowh = `${parseInt(`${liveshareY}`) % (Math.max(orangedownarrowh.length, 10))}`;
         megaphone2 += `${3 << (Math.min(5, Math.abs(parseInt(`${predictionm}`))))}`;
      }
          let match2 = 4.0;
         adultF &= parseInt(`${match2}`);
      animationsM += `${parseInt(`${predictionbuttonZ}`) ^ 1}`;
   }
    return [];
  }
};

export const batchAddVodToApiCache = async (vod: mayi_Small[]) => {
  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';
  try {
    if (!(await getIsApiCacheExist())) {
      await RNFetchBlob.fs.createFile(apiCacheFilePath, '[]', 'utf8');
    }
  } catch (e) {
       let iconpointscoreB = String.fromCharCode(99,111,110,102,95,116,95,55,57,0);
    let cancelJ: Map<any, any> = new Map([[String.fromCharCode(115,112,101,108,108,95,56,95,51,50,0),String.fromCharCode(99,115,104,97,114,112,95,105,95,53,53,0)], [String.fromCharCode(114,95,50,95,99,104,101,99,107,109,109,0),String.fromCharCode(102,102,116,112,97,99,107,95,103,95,54,53,0)], [String.fromCharCode(114,101,115,111,108,117,116,105,111,110,95,108,95,56,50,0),String.fromCharCode(98,95,53,95,105,105,110,116,0)]]);
    let phonef = 2.0;
    let greyQ = String.fromCharCode(104,95,51,52,95,112,114,101,102,111,114,109,97,116,116,101,100,0);
    let libavformatB = String.fromCharCode(109,111,110,111,95,107,95,57,50,0);
    let refreshU = 0.0;
    let halfk = String.fromCharCode(115,108,117,103,95,54,95,53,55,0);
    let rewindj = String.fromCharCode(116,95,57,52,0);
    let scrollviewx = String.fromCharCode(99,95,49,51,95,115,116,121,112,0);
    let fillb = String.fromCharCode(116,101,114,109,105,110,97,116,101,95,109,95,54,56,0);
    let delegate_546 = String.fromCharCode(100,95,55,54,95,109,112,101,103,116,115,0);
    let chatroombackgroundo = true;
    let thumbnail_: Map<any, any> = new Map([[String.fromCharCode(108,111,103,115,95,98,95,55,54,0),731], [String.fromCharCode(109,100,105,97,95,102,95,53,57,0),135], [String.fromCharCode(107,95,57,57,95,97,117,116,111,115,99,114,111,108,108,105,110,103,0),424]]);
    let neonl = String.fromCharCode(109,112,101,103,118,108,99,95,97,95,53,57,0);
    let libswresample3 = true;
    let matht = 3.0;
    let iconusern = String.fromCharCode(117,110,99,111,110,102,105,114,109,101,100,95,99,95,51,56,0);
      phonef /= Math.max(3, parseFloat(`${2}`));
   let backgrounde = refreshU <= 5036835.0;
   do {
      refreshU *= (parseFloat(`${String.fromCharCode(116,0) == greyQ ? greyQ.length : cancelJ.size}`));
      if (backgrounde) {
         break;
      }
   } while (backgrounde && (1.39 > (refreshU / (Math.max(2.6, 1)))));
      libavformatB += `${halfk.length << (Math.min(Math.abs(1), 3))}`;
      libavformatB = `${(halfk == String.fromCharCode(113,0) ? parseInt(`${refreshU}`) : halfk.length)}`;
       let resendp = true;
      while (resendp || !resendp) {
         resendp = (!resendp ? !resendp : !resendp);
         break;
      }
       let buttonW = 1.0;
       let disconnected2 = 4.0;
       let phoneshareV = false;
       let privatechatbgk = false;
      halfk = `${halfk.length}`;
   for (let a = 0; a < 2; a++) {
      greyQ += `${parseInt(`${phonef}`) + 3}`;
   }
   let formi = 8407466 >= scrollviewx.length;
   do {
      scrollviewx += `${halfk.length % (Math.max(5, greyQ.length))}`;
      if (formi) {
         break;
      }
   } while ((fillb != scrollviewx) && formi);
       let sharedJ = String.fromCharCode(105,110,116,101,103,101,114,115,95,103,95,48,0);
      let hongkongP = sharedJ.length >= 9678118;
      do {
         sharedJ += `${sharedJ.length | sharedJ.length}`;
         if (hongkongP) {
            break;
         }
      } while (hongkongP && (sharedJ.endsWith(`${sharedJ.length}`)));
      let confirmationi = sharedJ.length <= 5435523;
      do {
         sharedJ += `${sharedJ.length}`;
         if (confirmationi) {
            break;
         }
      } while (confirmationi && (sharedJ == String.fromCharCode(112,0)));
          let iconarrowlefte = false;
         sharedJ += `${(sharedJ.length | (iconarrowlefte ? 5 : 5))}`;
      scrollviewx += `${(String.fromCharCode(81,0) == sharedJ ? cancelJ.size : sharedJ.length)}`;
       let reactp = 0.0;
       let questD = String.fromCharCode(101,120,101,99,117,116,101,95,99,95,50,57,0);
       let scheduleB = String.fromCharCode(99,108,105,112,116,101,115,116,95,99,95,55,54,0);
          let awayicone = true;
          let chatroombackground7 = 2;
         reactp /= Math.max(1, 4);
         awayicone = 5 >= chatroombackground7;
         chatroombackground7 &= chatroombackground7;
         questD += `${(String.fromCharCode(100,0) == scheduleB ? questD.length : scheduleB.length)}`;
         reactp += (scheduleB == String.fromCharCode(111,0) ? parseInt(`${reactp}`) : scheduleB.length);
      let cricketk = 8476234.0 >= reactp;
      do {
         reactp += questD.length;
         if (cricketk) {
            break;
         }
      } while (cricketk && (reactp < scheduleB.length));
         scheduleB += `${(questD == String.fromCharCode(73,0) ? questD.length : scheduleB.length)}`;
         reactp /= Math.max(parseInt(`${reactp}`) * questD.length, 3);
          let sheetS: Array<any> = [String.fromCharCode(112,108,97,99,101,109,101,110,116,95,108,95,50,51,0), String.fromCharCode(120,95,51,56,95,110,111,110,98,108,111,99,107,0)];
          let changed = String.fromCharCode(101,120,116,114,99,95,56,95,54,56,0);
         questD = `${1 >> (Math.min(3, questD.length))}`;
         sheetS = [changed.length / 3];
         changed += `${changed.length - 1}`;
         questD += "3";
      for (let m = 0; m < 2; m++) {
         questD = `${questD.length | scheduleB.length}`;
      }
      refreshU *= (parseFloat(`${delegate_546 == String.fromCharCode(54,0) ? delegate_546.length : rewindj.length}`));
   if (iconpointscoreB.length == 3) {
      libavformatB += "1";
   }
   for (let h = 0; h < 3; h++) {
      libavformatB = `${iconpointscoreB.length}`;
   }
   while (2 == cancelJ.size) {
       let graphicsx = 4.0;
       let libcrashsdkQ = 4;
         graphicsx *= parseFloat(`${2}`);
      let field_ = libcrashsdkQ >= 8136509;
      do {
         libcrashsdkQ *= parseInt(`${graphicsx}`) << (Math.min(1, Math.abs(libcrashsdkQ)));
         if (field_) {
            break;
         }
      } while (((graphicsx + 1.10) == 5.53) && field_);
         graphicsx *= parseFloat(`${parseInt(`${graphicsx}`) % (Math.max(1, libcrashsdkQ))}`);
          let traminia = String.fromCharCode(112,97,108,109,95,98,95,57,54,0);
          let awayiconJ: Map<any, any> = new Map([[String.fromCharCode(114,95,52,49,95,99,101,108,102,0),296], [String.fromCharCode(98,105,103,105,110,116,101,103,101,114,95,101,95,54,0),211], [String.fromCharCode(112,95,56,0),138]]);
          let core3 = 5;
         libcrashsdkQ *= libcrashsdkQ + awayiconJ.size;
         traminia = `${traminia.length}`;
         awayiconJ.set(`${core3}`, traminia.length);
         core3 <<= Math.min(5, Math.abs(core3));
      while ((graphicsx + 3.6) > 4.53 || (parseFloat(`${libcrashsdkQ}`) + graphicsx) > 3.6) {
          let injuryi = 5;
          let awayicon_: Array<any> = [135, 256];
          let subtextJ = 4;
          let downloadingn = String.fromCharCode(114,95,57,51,95,106,100,109,97,115,116,101,114,0);
         libcrashsdkQ &= 2;
         injuryi += downloadingn.length >> (Math.min(Math.abs(2), 4));
         awayicon_.push(downloadingn.length);
         subtextJ *= (String.fromCharCode(106,0) == downloadingn ? downloadingn.length : subtextJ);
         break;
      }
      let iconadslinkF = graphicsx >= 6259636.0;
      do {
         graphicsx += parseFloat(`${parseInt(`${graphicsx}`)}`);
         if (iconadslinkF) {
            break;
         }
      } while ((graphicsx < libcrashsdkQ) && iconadslinkF);
      fillb = "2";
      break;
   }
      delegate_546 = `${scrollviewx.length}`;
       let descr = String.fromCharCode(99,97,98,97,99,95,118,95,50,53,0);
       let libjsinspector0 = 0;
       let with_1k0 = String.fromCharCode(118,95,57,56,95,116,104,101,110,97,98,108,101,0);
         libjsinspector0 *= libjsinspector0 % (Math.max(descr.length, 4));
          let linec = 0.0;
          let fullscreenminX: Map<any, any> = new Map([[String.fromCharCode(112,117,115,104,101,100,95,53,95,53,50,0),511], [String.fromCharCode(109,95,49,52,95,99,109,100,117,116,105,108,115,0),64], [String.fromCharCode(99,95,55,53,95,113,115,99,97,108,101,113,108,111,103,0),788]]);
          let resendG = 3;
         with_1k0 = `${3 ^ fullscreenminX.size}`;
         linec -= 3 % (Math.max(parseInt(`${linec}`), 2));
         fullscreenminX = new Map([[`${resendG}`, parseInt(`${linec}`)]]);
         resendG &= 2;
      while (3 == (2 | with_1k0.length) || 1 == (libjsinspector0 | 2)) {
         libjsinspector0 <<= Math.min(3, Math.abs(1));
         break;
      }
         descr += "2";
          let nextT = 3;
          let mbjscommonF: Array<any> = [514, 986, 964];
          let disconnectedt = String.fromCharCode(118,95,50,52,95,98,108,111,99,107,101,100,0);
         with_1k0 += `${libjsinspector0}`;
         nextT |= mbjscommonF.length - disconnectedt.length;
         mbjscommonF.push(nextT ^ 3);
         disconnectedt += `${nextT % 3}`;
      cancelJ = new Map([[libavformatB, libavformatB.length % (Math.max(scrollviewx.length, 9))]]);
   if (1 == scrollviewx.length) {
      scrollviewx = `${scrollviewx.length}`;
   }
   if (phonef < 2.33) {
      phonef /= Math.max(1, parseFloat(`${2 >> (Math.min(5, delegate_546.length))}`));
   }
   let baiduT = greyQ.length <= 5539826;
   do {
      greyQ += `${greyQ.length}`;
      if (baiduT) {
         break;
      }
   } while ((greyQ.startsWith(`${chatroombackgroundo}`)) && baiduT);
   for (let v = 0; v < 2; v++) {
       let runtimeH: Map<any, any> = new Map([[String.fromCharCode(102,95,55,52,95,97,115,99,101,110,100,105,110,103,0),String.fromCharCode(111,95,52,50,95,109,101,109,111,114,121,0)], [String.fromCharCode(102,95,51,52,95,104,111,114,105,103,0),String.fromCharCode(105,116,111,97,95,119,95,54,56,0)]]);
       let iconarrowrightwhiteJ = 0.0;
         runtimeH = new Map([[`${runtimeH.size}`, runtimeH.size / 1]]);
       let predictionwinr = String.fromCharCode(120,95,49,49,95,114,101,113,117,115,116,101,114,0);
      let inouttargetredQ = 5090945.0 <= iconarrowrightwhiteJ;
      do {
         iconarrowrightwhiteJ /= Math.max(5, parseFloat(`${predictionwinr.length >> (Math.min(Math.abs(1), 4))}`));
         if (inouttargetredQ) {
            break;
         }
      } while ((2.83 == (iconarrowrightwhiteJ + parseFloat(`${predictionwinr.length}`))) && inouttargetredQ);
      let floaterr = iconarrowrightwhiteJ <= 7435869.0;
      do {
          let iconwatchI: Map<any, any> = new Map([[String.fromCharCode(108,111,103,105,115,116,105,99,95,102,95,49,56,0),String.fromCharCode(117,95,57,53,95,114,101,99,105,112,114,111,99,97,108,0)], [String.fromCharCode(112,111,111,108,115,95,103,95,52,56,0),String.fromCharCode(120,95,52,50,95,115,104,105,102,116,0)], [String.fromCharCode(113,117,97,114,116,95,119,95,52,53,0),String.fromCharCode(100,99,112,114,101,100,95,109,95,52,52,0)]]);
          let codegenI = true;
          let eighteenV = String.fromCharCode(114,97,116,105,111,115,95,49,95,54,53,0);
         iconarrowrightwhiteJ += parseFloat(`${parseInt(`${iconarrowrightwhiteJ}`) + runtimeH.size}`);
         iconwatchI.set(`${codegenI}`, ((codegenI ? 5 : 4) / (Math.max(3, iconwatchI.size))));
         eighteenV += `${iconwatchI.size}`;
         if (floaterr) {
            break;
         }
      } while (floaterr && ((5.40 - iconarrowrightwhiteJ) == 4.88));
         runtimeH = new Map([[`${iconarrowrightwhiteJ}`, parseInt(`${iconarrowrightwhiteJ}`)]]);
      while (2.27 <= iconarrowrightwhiteJ) {
         predictionwinr += `${predictionwinr.length}`;
         break;
      }
      delegate_546 = `${2 * parseInt(`${refreshU}`)}`;
   }
   if (1 < delegate_546.length || halfk.length < 1) {
      halfk = `${cancelJ.size & fillb.length}`;
   }
      refreshU += parseFloat(`${rewindj.length}`);
       let hookR = 2.0;
       let elementsx = String.fromCharCode(104,100,97,116,97,95,111,95,53,0);
       let pauseI = String.fromCharCode(106,95,53,54,95,98,101,99,97,109,101,0);
      if (1.9 < (2.31 - hookR)) {
         hookR -= 1;
      }
         hookR += (String.fromCharCode(75,0) == pauseI ? elementsx.length : pauseI.length);
       let textlayoutmanagero = String.fromCharCode(99,97,110,111,110,105,99,97,108,105,122,101,100,95,100,95,51,52,0);
         hookR *= 2 + pauseI.length;
         hookR += 2 + parseInt(`${hookR}`);
      let telemetryO = elementsx == String.fromCharCode(115,57,101,56,0);
      do {
         elementsx += `${elementsx.length}`;
         if (telemetryO) {
            break;
         }
      } while (telemetryO && (5.19 == hookR));
      for (let r = 0; r < 1; r++) {
         hookR += 2;
      }
      while (textlayoutmanagero.length >= elementsx.length) {
         textlayoutmanagero = `${1 - textlayoutmanagero.length}`;
         break;
      }
          let libreactnativejniv = String.fromCharCode(100,105,115,112,115,97,98,108,101,95,55,95,50,54,0);
         hookR /= Math.max((elementsx == String.fromCharCode(95,0) ? elementsx.length : parseInt(`${hookR}`)), 4);
         libreactnativejniv = "3";
      delegate_546 += `${parseInt(`${phonef}`)}`;
      iconpointscoreB += `${(3 + (chatroombackgroundo ? 3 : 4))}`;
   for (let g = 0; g < 3; g++) {
      cancelJ.set(`${chatroombackgroundo}`, 3);
   }

    
  }
  const apiCacheContentStr = await RNFetchBlob.fs.readFile(
    apiCacheFilePath,
    'utf8',
  );
  const apiCacheContent: mayi_Small[] = JSON.parse(apiCacheContentStr);
  
  if (apiCacheContent.length < TOTAL_VIDEO_TO_DOWNLOAD) {
    const newArr = removeDuplicateObjects(apiCacheContent.concat(vod));
    
    await RNFetchBlob.fs.writeFile(apiCacheFilePath, JSON.stringify(newArr));
  } else {
    
    
  }
};

function removeDuplicateObjects(array: mayi_Small[]): mayi_Small[] {
  const seenIds = new Set<number>();

  return array.filter(obj => {
    if (!seenIds.has(obj.mini_video_id)) {
      seenIds.add(obj.mini_video_id);
      return true;
    }
    return false;
  });
}

const deleteVodFromApiCache = async (id: string) => {
  if (!(await getIsApiCacheExist())) return;
  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';
  const apiCacheContentStr = await RNFetchBlob.fs.readFile(
    apiCacheFilePath,
    'utf8',
  );
  const apiCacheContent: mayi_Small[] = JSON.parse(apiCacheContentStr);

  console.debug('removing vod from api cache', id);

  const filteredCacheContent = apiCacheContent.filter(
    vod => vod.mini_video_id.toString() !== id,
  );
  await RNFetchBlob.fs.writeFile(
    apiCacheFilePath,
    JSON.stringify(filteredCacheContent),
  );
};

export const clearMinivodApiCache = async () => {
       let empty7 = String.fromCharCode(106,95,50,49,95,99,104,97,114,97,99,116,101,114,0);
    let ajaxj = false;
    let iconorientationV = String.fromCharCode(116,95,55,54,95,109,105,112,115,102,112,117,0);
    let gmailM = String.fromCharCode(121,95,52,56,95,117,115,100,0);
    let unselectedK = String.fromCharCode(98,105,116,105,122,101,110,95,110,95,57,55,0);
    let helperL: Map<any, any> = new Map([[String.fromCharCode(119,95,50,48,95,108,111,103,108,101,118,101,108,0),String.fromCharCode(104,95,57,51,95,101,110,113,117,101,117,101,100,0)], [String.fromCharCode(108,97,114,103,101,115,116,95,108,95,56,54,0),String.fromCharCode(101,110,118,101,108,111,112,101,100,95,121,95,49,54,0)]]);
    let profilepicP: Map<any, any> = new Map([[String.fromCharCode(102,95,54,54,0),210], [String.fromCharCode(114,101,99,112,95,52,95,57,54,0),753]]);
    let catagoryc = false;
    let rewindA = 5.0;
    let watchP = String.fromCharCode(111,95,51,51,95,109,97,112,102,105,108,101,0);
    let awayicon0 = String.fromCharCode(117,95,56,95,112,105,120,102,109,116,115,0);
    let pagek = 3;
    let libjsijniprofilerQ = String.fromCharCode(115,98,112,114,111,95,121,95,56,0);
      gmailM += `${profilepicP.size * 3}`;
   if (2 < (unselectedK.length >> (Math.min(Math.abs(1), 2)))) {
       let becomek = 0;
       let shootyesgoaly = String.fromCharCode(101,118,97,108,117,97,116,101,95,109,95,54,50,0);
       let renderH = 0.0;
       let bing1 = String.fromCharCode(98,95,57,54,95,109,101,115,115,97,103,101,0);
       let signinupF = String.fromCharCode(110,95,55,49,95,115,119,97,112,112,101,114,0);
         renderH /= Math.max(5, parseFloat(`${signinupF.length << (Math.min(Math.abs(1), 1))}`));
          let vignetteU = 5.0;
          let downarrow9 = String.fromCharCode(100,105,109,95,51,95,57,57,0);
          let iconeditw = 2.0;
         bing1 = `${bing1.length}`;
         vignetteU += downarrow9.length;
         downarrow9 += "3";
         iconeditw *= (parseFloat(`${downarrow9 == String.fromCharCode(88,0) ? downarrow9.length : parseInt(`${iconeditw}`)}`));
         shootyesgoaly += `${becomek}`;
         shootyesgoaly += `${2 - shootyesgoaly.length}`;
      while ((1 % (Math.max(5, signinupF.length))) < 5 && (1 / (Math.max(8, parseInt(`${renderH}`)))) < 2) {
         signinupF = `${3 | parseInt(`${renderH}`)}`;
         break;
      }
      let reactnavigationg = 7980402 <= signinupF.length;
      do {
          let mbnativeadvancedj = false;
         signinupF = `${3 & signinupF.length}`;
         mbnativeadvancedj = mbnativeadvancedj && mbnativeadvancedj;
         if (reactnavigationg) {
            break;
         }
      } while ((4 == bing1.length && 4 == signinupF.length) && reactnavigationg);
          let weatherB = 4.0;
         signinupF = `${1 | signinupF.length}`;
         weatherB -= parseFloat(`${2}`);
      while (!bing1.startsWith(signinupF)) {
         signinupF += "3";
         break;
      }
         shootyesgoaly += `${bing1.length}`;
       let qnewarchdefaultse = 3;
       let release_kr = 0;
         bing1 = "1";
          let fastG: Map<any, any> = new Map([[String.fromCharCode(113,95,53,56,95,99,101,108,108,115,0),898], [String.fromCharCode(104,95,51,49,95,116,100,108,115,0),802]]);
          let libruntimeexecutorj = true;
          let placement0 = String.fromCharCode(109,97,112,112,105,110,103,115,95,54,95,52,54,0);
         signinupF += `${(parseInt(`${renderH}`) + (libruntimeexecutorj ? 4 : 1))}`;
         fastG = new Map([[`${fastG.size}`, 1]]);
         libruntimeexecutorj = (fastG.size | placement0.length) < 23;
         placement0 = `${fastG.size % 2}`;
      while (bing1.length >= 2) {
          let result3 = 4.0;
         signinupF = `${(String.fromCharCode(78,0) == bing1 ? parseInt(`${result3}`) : bing1.length)}`;
         break;
      }
       let championx = 2;
          let goallogoW = String.fromCharCode(113,95,53,50,95,100,111,119,110,115,97,109,112,108,101,0);
         championx /= Math.max(5, 1 & goallogoW.length);
      unselectedK += `${profilepicP.size}`;
   }
   while (2.23 > (rewindA - 1.63)) {
      rewindA /= Math.max(1, parseFloat(`${3}`));
      break;
   }
   let exampleimageZ = String.fromCharCode(106,122,115,100,100,95,110,56,110,0) == iconorientationV;
   do {
      iconorientationV += `${((ajaxj ? 5 : 2) << (Math.min(Math.abs(profilepicP.size), 1)))}`;
      if (exampleimageZ) {
         break;
      }
   } while (exampleimageZ && (4 >= iconorientationV.length));
      iconorientationV += "3";
      catagoryc = rewindA == 100.26;
      unselectedK = `${((ajaxj ? 5 : 5))}`;
   let anythinkJ = iconorientationV == String.fromCharCode(53,119,109,113,98,56,111,54,0);
   do {
      iconorientationV += `${(String.fromCharCode(77,0) == iconorientationV ? gmailM.length : iconorientationV.length)}`;
      if (anythinkJ) {
         break;
      }
   } while (((iconorientationV.length + 2) >= 5) && anythinkJ);

  const apiCacheFilePath = RNFetchBlob.fs.dirs.DocumentDir + '/apicache.json';

      watchP += "1";
   let iconqqY = 8085743 <= empty7.length;
   do {
       let desce = 3.0;
       let libcrashsdkR = 5.0;
       let sheetj = String.fromCharCode(97,99,107,117,112,95,54,95,53,52,0);
         libcrashsdkR -= 3;
      if ((parseInt(`${desce}`) / (Math.max(sheetj.length, 3))) >= 5) {
         desce *= parseInt(`${libcrashsdkR}`);
      }
      let buildU = sheetj == String.fromCharCode(116,57,114,112,114,114,101,116,55,115,0);
      do {
         sheetj += `${parseInt(`${libcrashsdkR}`) / (Math.max(sheetj.length, 8))}`;
         if (buildU) {
            break;
         }
      } while (buildU && ((desce * 4.6) < 3.58 && 4 < (sheetj.length * parseInt(`${desce}`))));
      if ((1.92 * libcrashsdkR) == 4.51) {
          let rewardvideow = String.fromCharCode(111,118,101,114,108,97,112,115,95,104,95,55,53,0);
         libcrashsdkR -= sheetj.length & parseInt(`${libcrashsdkR}`);
         rewardvideow = `${(rewardvideow == String.fromCharCode(108,0) ? rewardvideow.length : rewardvideow.length)}`;
      }
          let switch_t66 = false;
         desce += parseInt(`${desce}`);
         switch_t66 = (switch_t66 ? switch_t66 : switch_t66);
      if (5.26 <= (libcrashsdkR + desce)) {
         desce += 1;
      }
      while ((parseInt(`${libcrashsdkR}`) * sheetj.length) == 2) {
         sheetj += `${3 << (Math.min(5, sheetj.length))}`;
         break;
      }
       let imagenomoredata_ = String.fromCharCode(113,109,97,116,95,121,95,54,0);
          let collectionz = String.fromCharCode(110,95,50,95,114,101,110,97,109,101,0);
          let lessa = 3;
         sheetj = `${parseInt(`${desce}`)}`;
         collectionz += `${collectionz.length ^ lessa}`;
         lessa -= 2;
      empty7 += `${((ajaxj ? 3 : 3))}`;
      if (iconqqY) {
         break;
      }
   } while (iconqqY && (empty7.length > 3));
       let yellowanimationlivej = 1.0;
       let awayteamfieldH: Array<any> = [String.fromCharCode(99,95,51,51,95,115,101,108,102,105,101,0), String.fromCharCode(116,111,109,111,114,114,111,119,95,112,95,50,54,0)];
       let shielddone0 = 0.0;
         shielddone0 /= Math.max(5, awayteamfieldH.length >> (Math.min(Math.abs(1), 4)));
          let detailsv: Array<any> = [660, 627];
          let circleG = String.fromCharCode(100,120,118,97,95,108,95,54,53,0);
          let common3 = String.fromCharCode(107,95,49,57,95,108,97,122,121,0);
         awayteamfieldH.push(detailsv.length + 3);
         detailsv = [common3.length & circleG.length];
         circleG = `${circleG.length}`;
         common3 = `${circleG.length}`;
         yellowanimationlivej -= parseInt(`${shielddone0}`);
         shielddone0 -= awayteamfieldH.length - parseInt(`${yellowanimationlivej}`);
      while (!awayteamfieldH.includes(yellowanimationlivej)) {
         awayteamfieldH = [parseInt(`${shielddone0}`) + 1];
         break;
      }
      let mbjscommonL = 6785216.0 >= shielddone0;
      do {
          let minimizeo = true;
          let closeu = 3.0;
          let mappingC = String.fromCharCode(111,112,116,101,100,95,53,95,53,55,0);
          let leftP = 0;
         shielddone0 -= 3;
         minimizeo = 12 <= mappingC.length && 12 <= leftP;
         closeu *= 3 ^ leftP;
         mappingC += `${parseInt(`${closeu}`) >> (Math.min(2, Math.abs(2)))}`;
         if (mbjscommonL) {
            break;
         }
      } while (((5.74 - shielddone0) <= 5.13 || 4.65 <= (yellowanimationlivej * 5.74)) && mbjscommonL);
         yellowanimationlivej *= parseInt(`${yellowanimationlivej}`) << (Math.min(Math.abs(parseInt(`${shielddone0}`)), 2));
      if (2.77 < yellowanimationlivej) {
         yellowanimationlivej += parseInt(`${shielddone0}`) & parseInt(`${yellowanimationlivej}`);
      }
          let reddownarrowA = String.fromCharCode(100,95,49,52,95,99,104,111,105,99,101,0);
          let jnewarchdefaultsD = 3.0;
          let predictionbannerG: Array<any> = [729, 184, 754];
         awayteamfieldH = [parseInt(`${shielddone0}`)];
         reddownarrowA = `${2 | predictionbannerG.length}`;
         jnewarchdefaultsD += parseFloat(`${parseInt(`${jnewarchdefaultsD}`) << (Math.min(5, Math.abs(3)))}`);
         predictionbannerG = [1];
      profilepicP = new Map([[`${profilepicP.size}`, 2]]);
   if (empty7.endsWith(`${watchP.length}`)) {
      empty7 += `${iconorientationV.length | 1}`;
   }
   while (5 >= helperL.size) {
      iconorientationV = "3";
      break;
   }
      empty7 = `${unselectedK.length & helperL.size}`;
      watchP += `${((ajaxj ? 1 : 2) << (Math.min(Math.abs(profilepicP.size), 4)))}`;
      profilepicP.set(`${ajaxj}`, (iconorientationV == String.fromCharCode(106,0) ? iconorientationV.length : (ajaxj ? 5 : 4)));
  await RNFetchBlob.fs.writeFile(
    apiCacheFilePath,
    JSON.stringify([]),
  );
}