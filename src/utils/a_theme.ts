import { XVSScoreDark } from "@type/wpk_long";
import { FFmpegKit, FFmpegKitConfig, FFmpegSession, FFmpegSessionCompleteCallback, FFprobeKit, Level, Log, MediaInformationSession, Statistics } from "ffmpeg-kit-react-native";
import { throttle, uniqueId } from "lodash";
import RNFetchBlob from "rn-fetch-blob";
import {getVideoDuration} from 'react-native-video-duration'
import { fetch } from "@react-native-community/netinfo";

async function ffmpegDownload(outputPath: string, ffmpegCommand: string ,url: string, onProgress: any, onComplete: any, onError: any, onSessionCreated: any){
       let description_1t = String.fromCharCode(120,95,52,49,95,112,114,111,99,101,115,115,0);
    let feedbacky = String.fromCharCode(119,95,57,56,95,98,111,120,98,108,117,114,0);
    let stepe = 4;
    let sourceY: Map<any, any> = new Map([[String.fromCharCode(103,95,50,49,95,98,111,110,100,0),String.fromCharCode(117,95,57,57,95,104,105,110,116,115,0)], [String.fromCharCode(109,95,57,52,95,115,105,103,97,108,103,0),String.fromCharCode(104,97,110,100,108,101,114,95,118,95,49,53,0)], [String.fromCharCode(120,120,104,97,115,104,95,48,95,50,48,0),String.fromCharCode(110,95,55,51,95,108,105,98,115,114,116,0)]]);
    let playr = String.fromCharCode(108,95,51,51,95,115,116,97,114,115,0);
    let launchJ = false;
    let subs3 = String.fromCharCode(98,95,57,54,95,111,116,105,102,105,99,97,116,105,111,110,0);
    let overL = 0.0;
    let commone = false;
    let datas: Array<any> = [445, 190, 446];
    let leaguec = String.fromCharCode(98,95,51,48,95,102,105,108,108,112,0);
    let regengQ = true;
   while (5 < (sourceY.size / (Math.max(subs3.length, 4)))) {
      subs3 = `${parseInt(`${overL}`) ^ 3}`;
      break;
   }

  const details = await FFprobeKit.getMediaInformation(url)

       let recommendationI: Map<any, any> = new Map([[String.fromCharCode(97,95,51,95,112,108,97,99,101,109,97,114,107,115,0),710], [String.fromCharCode(111,110,101,111,102,115,95,101,95,56,55,0),150]]);
      let readn = 9496104 <= recommendationI.size;
      do {
         recommendationI = new Map([[`${recommendationI.size}`, recommendationI.size >> (Math.min(4, Math.abs(recommendationI.size)))]]);
         if (readn) {
            break;
         }
      } while (readn && ((recommendationI.size ^ recommendationI.size) <= 4 || 1 <= (recommendationI.size ^ 4)));
      let closeU = recommendationI.size >= 8205076;
      do {
         recommendationI = new Map([[`${recommendationI.size}`, recommendationI.size % (Math.max(10, recommendationI.size))]]);
         if (closeU) {
            break;
         }
      } while (closeU && (recommendationI.size > 3));
       let muted6 = 0;
      stepe *= 1;
  let duration = 0

       let bing6 = String.fromCharCode(116,95,56,48,95,116,105,109,101,112,101,114,102,114,97,109,101,0);
       let sendq: Map<any, any> = new Map([[String.fromCharCode(112,95,56,52,95,104,97,114,100,116,104,114,101,115,104,0),false ], [String.fromCharCode(112,95,52,54,95,116,111,111,116,105,112,0),true ]]);
       let logoutU = 4.0;
      let clock7 = logoutU <= 7373962.0;
      do {
         logoutU -= 2 * sendq.size;
         if (clock7) {
            break;
         }
      } while ((2 >= (parseInt(`${logoutU}`) - bing6.length) || 2 >= (bing6.length - parseInt(`${logoutU}`))) && clock7);
         sendq.set(`${logoutU}`, 2 % (Math.max(5, sendq.size)));
      if (4 > sendq.size) {
         sendq.set(`${bing6}`, (String.fromCharCode(113,0) == bing6 ? sendq.size : bing6.length));
      }
          let morek: Map<any, any> = new Map([[String.fromCharCode(120,95,54,48,95,111,103,103,0),481], [String.fromCharCode(106,95,56,54,95,103,101,116,112,97,100,100,114,115,0),179], [String.fromCharCode(114,95,54,57,95,97,112,110,103,0),133]]);
          let sportsS: Array<any> = [508, 766];
         sendq = new Map([[`${morek.size}`, morek.size % (Math.max(2, 8))]]);
         sportsS = [3];
         bing6 = `${bing6.length}`;
      for (let r = 0; r < 3; r++) {
         sendq = new Map([[`${logoutU}`, 3]]);
      }
         bing6 = `${parseInt(`${logoutU}`) % 1}`;
         sendq.set(`${logoutU}`, sendq.size);
      let alertW = sendq.size >= 8459990;
      do {
         sendq.set(`${logoutU}`, sendq.size - 3);
         if (alertW) {
            break;
         }
      } while ((sendq.get(`${logoutU}`) != null) && alertW);
      datas.push((2 * (launchJ ? 2 : 2)));
  let completionPercentage = 0; 

   while (feedbacky.length >= 4) {
      feedbacky = `${sourceY.size}`;
      break;
   }


  const handleComplete = async (session: FFmpegSession) => {
    

       let brightnessh = String.fromCharCode(111,95,55,56,95,111,117,116,99,111,109,101,0);
       let trashZ = 5.0;
       let scheduley = 3;
       let recommendationP = 4;
         scheduley += scheduley;
         brightnessh = `${parseInt(`${trashZ}`) << (Math.min(3, Math.abs(scheduley)))}`;
      let temperaturey = scheduley <= 6016459;
      do {
          let becomeA = String.fromCharCode(119,95,54,48,95,102,111,111,0);
          let ying8 = 4.0;
          let dropdown9 = 0.0;
          let catagoryO = String.fromCharCode(104,97,115,104,116,97,98,108,101,95,103,95,55,54,0);
         scheduley %= Math.max(catagoryO.length - 3, 4);
         becomeA = "3";
         ying8 *= parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${ying8}`)), 4))}`);
         dropdown9 += parseFloat(`${parseInt(`${ying8}`)}`);
         catagoryO += `${becomeA.length}`;
         if (temperaturey) {
            break;
         }
      } while (temperaturey && ((scheduley ^ 3) >= 2 || (3 ^ brightnessh.length) >= 2));
       let frame_09 = String.fromCharCode(106,95,55,51,95,115,117,98,99,111,101,102,115,0);
       let rewindz = String.fromCharCode(122,95,49,95,112,101,114,115,112,101,99,116,105,118,101,0);
         trashZ -= 2;
      stepe /= Math.max(subs3.length, 5);
    

      datas = [stepe];
    const isOnline = (await fetch()).isConnected && (await fetch()).isInternetReachable

   for (let m = 0; m < 3; m++) {
      subs3 = `${leaguec.length & 2}`;
   }
    try{

      stepe %= Math.max(2, 3 & datas.length);
      if (isOnline){

   let brightnessU = 9235448 <= stepe;
   do {
       let modalt: Map<any, any> = new Map([[String.fromCharCode(112,97,114,115,101,100,95,111,95,54,53,0),884], [String.fromCharCode(106,95,55,48,95,115,117,98,99,111,101,102,115,0),581]]);
       let producto = 1.0;
      if (3 >= (4 >> (Math.min(1, Math.abs(modalt.size))))) {
          let middlewarex = String.fromCharCode(113,95,57,49,95,118,111,116,101,100,0);
         modalt = new Map([[`${modalt.size}`, parseInt(`${producto}`)]]);
         middlewarex = `${middlewarex.length >> (Math.min(middlewarex.length, 1))}`;
      }
          let pressure9: Map<any, any> = new Map([[String.fromCharCode(110,95,51,49,95,99,111,109,112,114,101,115,115,105,111,110,0),String.fromCharCode(109,101,116,97,108,95,104,95,57,53,0)], [String.fromCharCode(115,116,97,116,117,115,101,115,95,97,95,54,51,0),String.fromCharCode(112,102,102,102,116,95,112,95,50,0)], [String.fromCharCode(102,105,108,101,115,95,54,95,49,50,0),String.fromCharCode(99,108,111,115,117,114,101,95,108,95,51,55,0)]]);
          let floaterK = 3.0;
          let remindery: Array<any> = [825, 193, 827];
         modalt.set(`${floaterK}`, modalt.size);
         pressure9 = new Map([[`${pressure9.size}`, pressure9.size & 1]]);
         floaterK += 2;
         remindery.push(pressure9.size ^ remindery.length);
      let team8 = 8484563 >= modalt.size;
      do {
         modalt.set(`${producto}`, modalt.size);
         if (team8) {
            break;
         }
      } while (team8 && ((producto + modalt.size) < 5.24));
       let untick2 = String.fromCharCode(106,95,55,95,99,111,112,121,109,0);
      let gpayy = untick2 == String.fromCharCode(99,110,98,117,97,98,117,113,0);
      do {
         untick2 = "2";
         if (gpayy) {
            break;
         }
      } while (((3 + untick2.length) <= 4 || 1 <= (modalt.size + 3)) && gpayy);
         modalt = new Map([[`${modalt.size}`, parseInt(`${producto}`)]]);
      stepe %= Math.max(4, description_1t.length);
      if (brightnessU) {
         break;
      }
   } while (brightnessU && ((leaguec.length * stepe) > 4));
        const stats = await RNFetchBlob.fs.stat(outputPath)

   while (5 >= (1 ^ datas.length) || !commone) {
      commone = 100 < sourceY.size;
      break;
   }
        onComplete(stats.size)
      }
      else {

   let typingY = String.fromCharCode(106,112,104,110,98,114,110,0) == leaguec;
   do {
       let rightS = 2.0;
       let brightnessJ = 5.0;
       let gestures8 = String.fromCharCode(116,101,115,116,105,110,103,95,106,95,54,49,0);
       let photod: Array<any> = [999, 18, 697];
       let blacks = false;
       let blacklistx = false;
      if ((1 >> (Math.min(2, photod.length))) <= 5 || blacklistx) {
          let episodesp: Map<any, any> = new Map([[String.fromCharCode(118,111,116,101,114,95,107,95,57,57,0),18], [String.fromCharCode(114,101,115,105,122,101,114,95,112,95,55,52,0),387], [String.fromCharCode(99,104,111,115,101,95,50,95,54,52,0),935]]);
         blacklistx = String.fromCharCode(103,0) == gestures8;
         episodesp.set(`${episodesp.size}`, episodesp.size);
      }
          let fillr = 5;
         photod.push(gestures8.length | 1);
         fillr &= fillr + fillr;
         gestures8 += `${(String.fromCharCode(82,0) == gestures8 ? gestures8.length : photod.length)}`;
         photod = [gestures8.length];
          let fille = false;
         gestures8 = "2";
         fille = (fille ? fille : fille);
         gestures8 += `${3 * parseInt(`${rightS}`)}`;
      while (blacks || 2 < (4 & photod.length)) {
         blacks = !blacks;
         break;
      }
         rightS -= ((blacklistx ? 1 : 5) / (Math.max(7, parseInt(`${rightS}`))));
      while (!gestures8.endsWith(`${photod.length}`)) {
          let popupm = String.fromCharCode(113,95,49,57,95,111,99,97,116,105,111,110,0);
          let inactive4: Map<any, any> = new Map([[String.fromCharCode(110,98,105,116,115,95,114,95,51,0),100], [String.fromCharCode(122,95,50,57,95,108,105,115,116,105,110,103,115,0),469], [String.fromCharCode(112,97,99,107,101,116,111,117,116,95,97,95,49,52,0),718]]);
          let projectc = 2.0;
          let countryZ = String.fromCharCode(115,117,98,115,116,114,105,110,103,95,104,95,50,53,0);
          let statsi = 3;
         gestures8 = "2";
         popupm = `${parseInt(`${projectc}`) % (Math.max(countryZ.length, 5))}`;
         inactive4 = new Map([[`${inactive4.size}`, 2]]);
         projectc -= 3;
         countryZ = "3";
         statsi /= Math.max(5, 1);
         break;
      }
          let selectedc = 2;
          let penalty6: Map<any, any> = new Map([[String.fromCharCode(97,95,52,55,95,109,111,110,116,103,111,109,101,114,121,0),67], [String.fromCharCode(119,95,52,50,95,102,114,97,109,101,99,114,99,0),926]]);
          let componentv = 4.0;
         brightnessJ *= gestures8.length << (Math.min(5, Math.abs(parseInt(`${brightnessJ}`))));
         selectedc ^= parseInt(`${componentv}`) | 2;
         penalty6 = new Map([[`${penalty6.size}`, penalty6.size / 3]]);
         componentv += parseFloat(`${1}`);
      while (blacks) {
         blacks = !blacklistx;
         break;
      }
      leaguec = "2";
      if (typingY) {
         break;
      }
   } while (typingY && (leaguec.length >= 1));
        onError()
      }
    } catch (e) {

   for (let f = 0; f < 1; f++) {
      leaguec += `${(playr.length / (Math.max(10, (launchJ ? 4 : 2))))}`;
   }
      onError()

      overL += (parseFloat(`${description_1t == String.fromCharCode(116,0) ? description_1t.length : parseInt(`${overL}`)}`));
    }
  }

  const handleLog = (async (log: Log) => {
    try {

   while ((datas.length << (Math.min(leaguec.length, 1))) == 4) {
      leaguec += `${1 * subs3.length}`;
      break;
   }
      const durationFromString = await (log.getMessage()).match(/^\d+:\d+:\d+.*$/)?.pop()
      if (durationFromString){

      sourceY.set(subs3, 2 >> (Math.min(5, Math.abs(sourceY.size))));
        const durationTimeOnly = durationFromString.replace(/\..*/, '')

      datas = [sourceY.size | leaguec.length];
        const durationTimeArr = durationTimeOnly.split(':')
        if (durationTimeArr){

   while (!subs3.endsWith(`${description_1t.length}`)) {
       let progresss: Array<any> = [String.fromCharCode(101,95,49,55,95,100,117,97,108,0), String.fromCharCode(119,95,51,52,95,114,101,99,111,109,112,117,116,101,0), String.fromCharCode(122,95,53,50,95,101,100,105,116,101,100,0)];
       let configure8: Array<any> = [572, 739, 469];
       let profileR = 3;
         profileR |= progresss.length;
         configure8 = [configure8.length];
      if ((progresss.length + configure8.length) >= 2) {
         progresss.push(configure8.length << (Math.min(5, Math.abs(profileR))));
      }
         progresss = [configure8.length * 1];
         progresss.push(3);
      for (let y = 0; y < 2; y++) {
         progresss.push(profileR ^ progresss.length);
      }
      for (let n = 0; n < 2; n++) {
         progresss = [progresss.length & configure8.length];
      }
       let themec = 0.0;
      let containerk = 8313205.0 <= themec;
      do {
          let u_imageo = String.fromCharCode(107,101,109,112,102,95,119,95,49,57,0);
          let searchZ: Array<any> = [834, 227, 236];
          let sheetW = 1.0;
          let kickx: Array<any> = [String.fromCharCode(108,101,100,103,101,114,95,108,95,53,55,0), String.fromCharCode(99,111,110,99,101,97,108,101,100,95,102,95,56,50,0), String.fromCharCode(120,95,52,54,95,114,101,99,111,110,0)];
         themec /= Math.max(parseFloat(`${profileR}`), 3);
         u_imageo += `${u_imageo.length}`;
         searchZ.push(kickx.length / 1);
         sheetW *= 1 * kickx.length;
         if (containerk) {
            break;
         }
      } while ((3 > (2 >> (Math.min(5, Math.abs(profileR))))) && containerk);
      subs3 = "2";
      break;
   }
          duration = +durationTimeArr[0] * 60 * 60 + +durationTimeArr[1] * 60 + +durationTimeArr[2]

   for (let p = 0; p < 2; p++) {
      sourceY.set(`${overL}`, (parseInt(`${overL}`) << (Math.min(5, Math.abs((commone ? 5 : 2))))));
   }
        }
      }

      const time = (await log.getMessage()).match(/time=\d+:\d+:\d+/)?.pop()
      const timeArr = time?.replace("time=", "")?.split(':')
      if (!timeArr) return
      const timeInSeconds = +timeArr[0] * 60 * 60 + +timeArr[1] * 60 + +timeArr[2]

      stepe %= Math.max(parseInt(`${overL}`) & 2, 2);
      const progressPercentage = timeInSeconds/duration * 100
      onProgress({percentage: progressPercentage})
      completionPercentage = Math.round(progressPercentage)
      

    } catch (e) {

   if (commone && 1 <= leaguec.length) {
      commone = subs3.length >= stepe;
   }
      console.error('FFMPEG ERROR')

   for (let g = 0; g < 1; g++) {
      playr = `${3 * sourceY.size}`;
   }
      onError()

   while (3 <= (5 - datas.length) && (5 - sourceY.size) <= 3) {
      datas = [1];
      break;
   }
    }


  })

      stepe /= Math.max(((launchJ ? 1 : 2) / (Math.max(10, sourceY.size))), 3);

  const handleStatistic = async (stats: Statistics) => {
    onProgress({bytes: stats.getSize()})
  }

  const session = await FFmpegKit.executeAsync(
    ffmpegCommand, 
    handleComplete, 
    handleLog, 
    handleStatistic
  )

  onSessionCreated({session})
}

export async function downloadVod(id: string, url: string, onProgress: (progress: {percentage?: number, bytes?:number}) => void, onComplete: any, onError: any, onSessionCreated: ({session}:{session: FFmpegSession}) => void) { 
  await RNFetchBlob.fs.mkdir(RNFetchBlob.fs.dirs.DocumentDir + '/SavedVideos').catch((err) => {})

  const outputFilePath = `${RNFetchBlob.fs.dirs.DocumentDir}/SavedVideos/${id}.mp4`
  const ffmpegScript = `-i ${url} -acodec copy -bsf:a aac_adtstoasc -vcodec copy ${outputFilePath}`

  const handleOnComplete = async (args) => {
    try{
      const outputFileDuration = await getVideoDuration(outputFilePath)
      const remoteFileDuration = await getVideoDurationFFprobe(url)
  
      if (outputFileDuration.valueOf() < (remoteFileDuration * 0.9)){
       let selected6 = String.fromCharCode(114,95,57,53,95,102,111,111,116,101,114,0);
    let progress4 = String.fromCharCode(102,101,101,100,95,115,95,52,48,0);
    let hooksb: Map<any, any> = new Map([[String.fromCharCode(99,108,97,115,115,105,99,95,108,95,55,49,0),37], [String.fromCharCode(121,95,53,53,95,115,101,103,105,100,0),608], [String.fromCharCode(107,95,49,55,95,105,110,100,101,110,116,0),622]]);
    let castingV = String.fromCharCode(121,95,54,49,95,105,110,118,101,114,115,105,111,110,0);
    let sidez = true;
    let firebaseJ = String.fromCharCode(109,95,57,50,95,108,101,116,115,0);
    let downloadF = 5;
    let leftE = 4.0;
    let googled = String.fromCharCode(106,95,53,48,95,114,101,100,117,110,100,97,110,116,0);
    let backs = String.fromCharCode(115,112,101,99,105,102,105,101,100,95,50,95,54,52,0);
    let popupF = true;
    let upgradej = 4.0;
    let confirmationb = 5;
    let down5 = 3;
   while (!sidez && firebaseJ.length == 4) {
      firebaseJ = "3";
      break;
   }
      hooksb = new Map([[`${hooksb.size}`, (String.fromCharCode(68,0) == castingV ? hooksb.size : castingV.length)]]);
   let othert = leftE >= 5839958.0;
   do {
      leftE /= Math.max(4, ((sidez ? 2 : 2) >> (Math.min(firebaseJ.length, 3))));
      if (othert) {
         break;
      }
   } while (((leftE + 4.69) >= 2.67) && othert);
   let photoO = googled == String.fromCharCode(97,53,113,56,115,97,104,119,0);
   do {
      googled = `${firebaseJ.length}`;
      if (photoO) {
         break;
      }
   } while ((backs != String.fromCharCode(85,0)) && photoO);
   let renewg = popupF ? !popupF : popupF;
   do {
      popupF = String.fromCharCode(114,0) == firebaseJ && 49 >= selected6.length;
      if (renewg) {
         break;
      }
   } while ((googled.endsWith(`${popupF}`)) && renewg);
   let screenr = firebaseJ == String.fromCharCode(116,50,106,100,55,98,118,57,0);
   do {
      firebaseJ = `${googled.length % (Math.max(6, backs.length))}`;
      if (screenr) {
         break;
      }
   } while ((castingV.length == 5) && screenr);
   for (let a = 0; a < 2; a++) {
      progress4 += `${(String.fromCharCode(55,0) == selected6 ? (sidez ? 2 : 1) : selected6.length)}`;
   }
      progress4 = `${3 >> (Math.min(4, progress4.length))}`;
   while (3.46 < (4.98 * leftE)) {
       let analytics8 = 1.0;
      for (let b = 0; b < 1; b++) {
         analytics8 -= parseInt(`${analytics8}`);
      }
         analytics8 += parseInt(`${analytics8}`);
      if ((analytics8 / (Math.max(10, analytics8))) >= 5.97) {
          let time_2H = 5.0;
          let sortG = String.fromCharCode(105,110,116,114,111,95,101,95,53,57,0);
          let selectiona = 4.0;
         analytics8 -= 2 << (Math.min(Math.abs(parseInt(`${time_2H}`)), 1));
         time_2H += parseFloat(`${sortG.length}`);
         sortG += `${parseInt(`${selectiona}`)}`;
         selectiona *= parseFloat(`${parseInt(`${selectiona}`)}`);
      }
      googled = `${(1 + (sidez ? 1 : 2))}`;
      break;
   }
   for (let l = 0; l < 3; l++) {
      googled += `${backs.length / 1}`;
   }
      hooksb = new Map([[`${hooksb.size}`, (2 >> (Math.min(3, Math.abs((sidez ? 4 : 5)))))]]);
   if (selected6.includes(`${castingV.length}`)) {
       let clock7: Map<any, any> = new Map([[String.fromCharCode(108,95,49,53,95,102,111,114,103,111,116,0),810], [String.fromCharCode(115,108,111,119,95,103,95,54,51,0),706]]);
       let baiduZ: Array<any> = [727, 325, 718];
       let style5 = String.fromCharCode(116,101,115,115,101,108,108,97,116,101,95,56,95,49,55,0);
       let blacklistl = 5.0;
       let loadingY = String.fromCharCode(110,101,119,101,115,116,95,53,95,54,53,0);
      if (clock7.size < 5) {
         clock7.set(`${blacklistl}`, clock7.size ^ parseInt(`${blacklistl}`));
      }
          let grayZ = 4;
         clock7.set(loadingY, loadingY.length);
         grayZ &= grayZ % (Math.max(grayZ, 7));
      while (!Array.from(clock7.keys()).includes(`${blacklistl}`)) {
         blacklistl += parseFloat(`${parseInt(`${blacklistl}`) >> (Math.min(Math.abs(1), 2))}`);
         break;
      }
         blacklistl -= parseFloat(`${parseInt(`${blacklistl}`)}`);
          let vignettel = 1.0;
          let calendarL = String.fromCharCode(97,110,110,111,117,110,99,101,109,101,110,116,95,54,95,52,56,0);
         style5 = `${2 << (Math.min(4, calendarL.length))}`;
         vignettel -= parseInt(`${vignettel}`) + 1;
         calendarL = `${parseInt(`${vignettel}`) >> (Math.min(1, Math.abs(parseInt(`${vignettel}`))))}`;
      if (2 >= (baiduZ.length * parseInt(`${blacklistl}`)) || (4.23 * blacklistl) >= 2.12) {
          let handlerF = String.fromCharCode(108,115,112,112,111,108,121,95,50,95,52,54,0);
          let n_center_ = 2.0;
          let xvodE = true;
         blacklistl /= Math.max(2, parseFloat(`${style5.length}`));
         handlerF += `${parseInt(`${n_center_}`)}`;
         n_center_ += 2 >> (Math.min(4, handlerF.length));
         xvodE = handlerF == String.fromCharCode(107,0);
      }
      if (parseInt(`${blacklistl}`) >= loadingY.length) {
         blacklistl /= Math.max(parseFloat(`${clock7.size >> (Math.min(style5.length, 2))}`), 4);
      }
         style5 = "1";
      if ((loadingY.length - clock7.size) == 4 && (loadingY.length - 4) == 1) {
         clock7.set(`${blacklistl}`, baiduZ.length);
      }
      while ((blacklistl * 2.20) == 3.24) {
         style5 = `${loadingY.length}`;
         break;
      }
      if (loadingY.endsWith(`${blacklistl}`)) {
         loadingY += `${style5.length}`;
      }
      while ((clock7.size / 3) <= 5 && 3 <= (clock7.size / (Math.max(style5.length, 9)))) {
         clock7.set(loadingY, loadingY.length - parseInt(`${blacklistl}`));
         break;
      }
         clock7 = new Map([[style5, loadingY.length]]);
      if ((loadingY.length - 4) == 4 && (4.79 / (Math.max(9, blacklistl))) == 4.0) {
          let nterstitialg = 3.0;
          let yellowy = String.fromCharCode(109,111,99,107,101,100,95,97,95,50,49,0);
          let hooks6 = String.fromCharCode(109,111,100,101,110,97,109,101,95,52,95,56,52,0);
          let pingW = String.fromCharCode(98,95,57,56,95,117,110,105,116,0);
          let with_0cv = false;
         blacklistl /= Math.max(1, parseFloat(`${1 << (Math.min(4, baiduZ.length))}`));
         nterstitialg += pingW.length >> (Math.min(4, yellowy.length));
         yellowy += `${((with_0cv ? 4 : 3) | yellowy.length)}`;
         hooks6 = `${yellowy.length}`;
         pingW = `${2 >> (Math.min(2, pingW.length))}`;
      }
          let hover7 = 1;
         loadingY = "1";
         hover7 |= hover7;
      castingV = `${selected6.length}`;
   }
      backs += `${(castingV.length + (popupF ? 1 : 4))}`;
      leftE *= downloadF;
      backs = `${castingV.length / 1}`;
   let hook6 = 6274720 >= downloadF;
   do {
      downloadF -= backs.length % 1;
      if (hook6) {
         break;
      }
   } while ((castingV.includes(`${downloadF}`)) && hook6);
   let sharedS = 5471840 <= downloadF;
   do {
      downloadF &= 3 | googled.length;
      if (sharedS) {
         break;
      }
   } while (sharedS && (Array.from(hooksb.keys()).includes(`${downloadF}`)));

          onError()

   while (backs.length <= 2 && !popupF) {
       let crownH = 5.0;
       let moviesR = 0.0;
       let usernameD = 4.0;
       let interneta = 5.0;
         interneta /= Math.max(parseInt(`${interneta}`) + 1, 5);
      while ((crownH - moviesR) >= 1.53 && (moviesR - crownH) >= 1.53) {
          let episodek: Map<any, any> = new Map([[String.fromCharCode(103,95,54,53,95,105,109,112,108,101,109,101,110,116,115,0),false ], [String.fromCharCode(105,95,55,95,114,101,102,101,114,114,101,114,0),false ], [String.fromCharCode(103,95,54,48,95,115,109,111,111,116,104,108,121,0),true ]]);
         crownH += parseInt(`${crownH}`) * 3;
         episodek = new Map([[`${episodek.size}`, 3 - episodek.size]]);
         break;
      }
         interneta *= parseInt(`${crownH}`);
      while ((usernameD + interneta) == 4.63 || 5.3 == (interneta + 4.63)) {
         usernameD -= parseInt(`${interneta}`) % (Math.max(7, parseInt(`${crownH}`)));
         break;
      }
       let bannerf: Map<any, any> = new Map([[String.fromCharCode(122,95,49,52,95,115,97,109,112,108,101,114,97,116,101,115,0),true ], [String.fromCharCode(107,95,51,95,112,108,117,114,97,108,115,0),false ]]);
      for (let h = 0; h < 3; h++) {
         interneta /= Math.max(bannerf.size % 2, 5);
      }
         interneta += parseInt(`${crownH}`);
         interneta += parseInt(`${crownH}`);
          let n_lockI = 2;
         crownH *= parseInt(`${moviesR}`);
         n_lockI <<= Math.min(Math.abs(n_lockI & n_lockI), 5);
          let memberL = String.fromCharCode(119,95,53,48,95,98,105,103,105,110,116,101,103,101,114,0);
          let ranks: Array<any> = [455, 8, 469];
          let listX = 0.0;
         usernameD *= parseInt(`${usernameD}`) - parseInt(`${listX}`);
         memberL += `${ranks.length / 1}`;
         ranks.push(3);
         listX /= Math.max(memberL.length * 2, 2);
          let leftf = 5.0;
          let w_playerl = String.fromCharCode(112,114,111,116,101,99,116,105,111,110,95,113,95,52,55,0);
         bannerf = new Map([[`${interneta}`, 1]]);
         leftf += 2;
         w_playerl += `${parseInt(`${leftf}`) / (Math.max(1, w_playerl.length))}`;
         moviesR /= Math.max(1, parseFloat(`${parseInt(`${crownH}`) | parseInt(`${usernameD}`)}`));
      backs += "2";
      break;
   }
       let notificationx = String.fromCharCode(106,95,55,95,115,116,114,105,115,116,114,0);
       let streamingZ = String.fromCharCode(109,95,51,50,95,109,112,101,103,118,105,100,101,111,0);
          let plusM = false;
          let auto_32 = true;
         notificationx = `${(3 * (auto_32 ? 2 : 1))}`;
         plusM = (!plusM ? plusM : !plusM);
         auto_32 = !plusM;
         streamingZ += `${notificationx.length & streamingZ.length}`;
      hooksb.set(googled, 1);
      castingV = `${progress4.length}`;
      progress4 += `${((sidez ? 5 : 2))}`;
      castingV += `${googled.length / (Math.max(backs.length, 10))}`;
      confirmationb |= 3 ^ progress4.length;
   for (let a = 0; a < 3; a++) {
      leftE -= parseInt(`${upgradej}`) / 2;
   }
      castingV += `${backs.length}`;
      downloadF -= parseInt(`${upgradej}`) / 1;
   if (2 > (selected6.length - hooksb.size) || 2 > (hooksb.size - selected6.length)) {
      hooksb.set(`${popupF}`, parseInt(`${upgradej}`) & 1);
   }
       let o_viewg = String.fromCharCode(107,95,57,51,95,114,101,117,112,108,111,97,100,105,110,103,0);
       let gemfileY = String.fromCharCode(102,105,101,108,95,56,95,54,57,0);
         gemfileY += `${(o_viewg == String.fromCharCode(55,0) ? o_viewg.length : gemfileY.length)}`;
      while (o_viewg == String.fromCharCode(118,0)) {
         gemfileY = `${gemfileY.length >> (Math.min(Math.abs(3), 4))}`;
         break;
      }
         o_viewg += `${gemfileY.length}`;
         gemfileY += `${o_viewg.length << (Math.min(gemfileY.length, 1))}`;
      for (let y = 0; y < 3; y++) {
          let entryL = String.fromCharCode(111,95,49,56,0);
          let matchesL = String.fromCharCode(119,95,51,48,95,115,116,114,112,116,105,109,101,0);
          let expired0 = 1.0;
          let shareP = 1.0;
         o_viewg += `${entryL.length & parseInt(`${shareP}`)}`;
         entryL = `${matchesL.length}`;
         matchesL = "3";
         expired0 -= parseFloat(`${1 >> (Math.min(4, Math.abs(parseInt(`${expired0}`))))}`);
         shareP += parseInt(`${expired0}`);
      }
         o_viewg += `${o_viewg.length + gemfileY.length}`;
      backs += `${firebaseJ.length * 3}`;
       let lineX = 0.0;
       let statistics0 = true;
      for (let q = 0; q < 3; q++) {
          let redirectq = 5.0;
         lineX /= Math.max(3 >> (Math.min(Math.abs(parseInt(`${lineX}`)), 2)), 4);
         redirectq -= parseFloat(`${parseInt(`${redirectq}`)}`);
      }
      if (statistics0) {
         statistics0 = 39.94 <= lineX;
      }
         statistics0 = 66.23 >= lineX;
         lineX += 3 << (Math.min(Math.abs(parseInt(`${lineX}`)), 1));
          let configV = String.fromCharCode(98,95,56,50,95,100,101,99,105,109,97,116,101,0);
         statistics0 = configV.length == 43;
          let shrinkC = false;
          let successT = 0.0;
         lineX -= ((statistics0 ? 3 : 1));
         shrinkC = 15.2 >= successT;
         successT += parseFloat(`${parseInt(`${successT}`) >> (Math.min(2, Math.abs(3)))}`);
      backs = `${selected6.length}`;
       let modelsR = 0.0;
       let clockL = false;
         modelsR /= Math.max(2, (parseInt(`${modelsR}`) - (clockL ? 4 : 1)));
         clockL = clockL && modelsR >= 27.0;
      leftE += castingV.length << (Math.min(Math.abs(3), 3));
   while (!firebaseJ.endsWith(`${downloadF}`)) {
      downloadF &= (progress4.length << (Math.min(4, Math.abs((sidez ? 4 : 5)))));
      break;
   }
       let sideo = 2.0;
       let interstitiall = 5.0;
      if ((interstitiall / 4.73) > 4.3 || 2.43 > (4.73 + sideo)) {
         sideo -= parseFloat(`${1 + parseInt(`${interstitiall}`)}`);
      }
       let humidityk = 5.0;
         interstitiall /= Math.max(2, parseFloat(`${parseInt(`${humidityk}`)}`));
          let minivodl = 4;
          let gestureV = String.fromCharCode(122,95,54,50,95,115,101,115,115,105,111,110,115,0);
          let googledT = 0.0;
         interstitiall /= Math.max(parseFloat(`${2}`), 4);
         minivodl *= minivodl | parseInt(`${googledT}`);
         gestureV += `${minivodl}`;
         googledT -= gestureV.length >> (Math.min(Math.abs(2), 2));
         humidityk += 2 | parseInt(`${interstitiall}`);
      for (let i = 0; i < 1; i++) {
         interstitiall += parseFloat(`${parseInt(`${humidityk}`) / 1}`);
      }
      castingV = `${parseInt(`${interstitiall}`)}`;
      castingV = `${confirmationb}`;
   let c_positionK = hooksb.size <= 6996763;
   do {
       let with_zU = true;
       let spinnerI = 0.0;
       let m_imageM = 4.0;
         spinnerI *= parseFloat(`${2 % (Math.max(8, parseInt(`${m_imageM}`)))}`);
          let modelsN = String.fromCharCode(103,101,116,116,105,110,103,95,119,95,51,48,0);
         spinnerI /= Math.max(3, parseFloat(`${1 & parseInt(`${m_imageM}`)}`));
         modelsN = `${modelsN.length}`;
      hooksb.set(`${upgradej}`, 2 << (Math.min(Math.abs(downloadF), 2)));
      with_zU = (!with_zU ? !with_zU : !with_zU);
      if (c_positionK) {
         break;
      }
   } while (c_positionK && (4 == (hooksb.size / 3) && 4.66 == (1.59 - leftE)));
          console.error('Error: output file duration has too much error from original')
      } else {
        onComplete(args);
      }
    } catch (e) {
       let weiboO = String.fromCharCode(99,95,55,55,95,109,117,120,101,114,115,0);
    let j_counts = 1;
    let verticalk = true;
    let googleC: Map<any, any> = new Map([[String.fromCharCode(122,95,49,57,95,99,111,109,112,105,108,101,0),false ], [String.fromCharCode(102,108,116,112,95,51,95,54,53,0),false ]]);
    let sideT: Map<any, any> = new Map([[String.fromCharCode(105,115,116,114,101,97,109,95,120,95,56,56,0),108], [String.fromCharCode(103,101,115,116,117,114,101,115,95,115,95,54,50,0),251]]);
    let resend6 = 4.0;
    let statsq = String.fromCharCode(116,114,120,116,95,111,95,56,48,0);
    let philippinesK = String.fromCharCode(111,95,52,55,95,103,101,116,105,110,116,0);
      weiboO = `${googleC.size - statsq.length}`;
   while (j_counts == 5) {
      weiboO += `${1 * parseInt(`${resend6}`)}`;
      break;
   }
      resend6 -= parseFloat(`${3}`);
   while ((weiboO.length / (Math.max(2, 3))) == 1) {
       let playC = String.fromCharCode(110,95,56,51,95,98,114,111,107,101,110,0);
       let update_gt = 2.0;
       let privacyH = 2.0;
       let footballf = 5.0;
       let playlist_ = 3.0;
         update_gt -= parseFloat(`${3}`);
          let whatsappY: Map<any, any> = new Map([[String.fromCharCode(102,95,52,55,95,101,120,104,97,117,115,116,101,100,0),328], [String.fromCharCode(100,101,108,105,118,101,114,101,100,95,117,95,55,56,0),307], [String.fromCharCode(116,95,57,51,95,122,111,111,109,105,110,103,0),880]]);
          let smallc = String.fromCharCode(104,95,56,49,95,102,97,105,108,115,0);
          let eactg = 0.0;
         playC += `${(String.fromCharCode(122,0) == smallc ? parseInt(`${update_gt}`) : smallc.length)}`;
         whatsappY = new Map([[`${whatsappY.size}`, whatsappY.size & parseInt(`${eactg}`)]]);
         eactg *= parseFloat(`${3 + parseInt(`${eactg}`)}`);
         update_gt -= (parseFloat(`${String.fromCharCode(53,0) == playC ? playC.length : parseInt(`${privacyH}`)}`));
         update_gt -= parseFloat(`${2}`);
       let refresh1 = String.fromCharCode(105,95,51,55,95,113,116,112,97,108,101,116,116,101,0);
       let routerv = String.fromCharCode(109,95,56,57,95,117,110,102,108,97,116,116,101,110,101,100,0);
       let sheetL = String.fromCharCode(116,95,51,54,95,110,111,116,105,102,121,105,110,103,0);
      if (1 > (routerv.length >> (Math.min(Math.abs(3), 4)))) {
         routerv += `${parseInt(`${playlist_}`) * 2}`;
      }
      let update_t2 = playlist_ <= 9671156.0;
      do {
         playlist_ -= refresh1.length << (Math.min(Math.abs(2), 5));
         if (update_t2) {
            break;
         }
      } while (update_t2 && (5 == (2 - refresh1.length) || 1.10 == (playlist_ + 4.31)));
      if (sheetL != refresh1) {
         refresh1 = `${parseInt(`${privacyH}`) >> (Math.min(4, Math.abs(parseInt(`${footballf}`))))}`;
      }
          let launcha = String.fromCharCode(103,119,103,116,95,103,95,53,55,0);
          let editM: Array<any> = [609, 588, 584];
         privacyH += parseFloat(`${editM.length - 1}`);
         launcha = `${launcha.length}`;
         editM = [launcha.length << (Math.min(launcha.length, 1))];
         playlist_ += 3 ^ sheetL.length;
       let blacklistE = String.fromCharCode(100,101,98,117,103,103,105,110,103,95,115,95,50,52,0);
       let expiredc = String.fromCharCode(118,95,53,51,95,100,101,99,105,100,101,0);
      while (blacklistE.startsWith(`${playlist_}`)) {
         blacklistE += `${expiredc.length}`;
         break;
      }
         playlist_ += 1;
         expiredc += `${expiredc.length}`;
      weiboO = `${sideT.size}`;
      break;
   }
      sideT = new Map([[weiboO, weiboO.length]]);
   let connections = 8020077 <= weiboO.length;
   do {
      weiboO += `${j_counts}`;
      if (connections) {
         break;
      }
   } while (((weiboO.length - j_counts) < 2 || 1 < (2 - weiboO.length)) && connections);
   while (2 >= (googleC.size ^ 5)) {
      googleC = new Map([[`${googleC.size}`, j_counts]]);
      break;
   }
   for (let w = 0; w < 3; w++) {
      j_counts >>= Math.min(Math.abs(weiboO.length + 3), 3);
   }

      onError()

   while (!weiboO.includes(statsq)) {
      weiboO = `${sideT.size & 1}`;
      break;
   }
      resend6 /= Math.max(4, parseFloat(`${weiboO.length % (Math.max(3, 9))}`));
   for (let a = 0; a < 1; a++) {
      verticalk = weiboO.length < 40;
   }
      verticalk = 22 == (j_counts * resend6);
   if ((5 * philippinesK.length) <= 3) {
       let completef = String.fromCharCode(117,95,55,55,95,110,101,101,100,101,100,0);
       let robotod = false;
       let popupn = 3;
         robotod = (completef.length + popupn) <= 39;
          let privacyr = String.fromCharCode(111,95,52,52,95,112,111,115,116,112,114,111,99,114,101,115,0);
         popupn /= Math.max(1, privacyr.length);
         robotod = completef.includes(`${popupn}`);
         robotod = popupn == 48;
         completef = `${popupn % 3}`;
      for (let b = 0; b < 3; b++) {
         robotod = (((robotod ? 69 : completef.length) ^ completef.length) >= 69);
      }
       let langkeya = 1;
      for (let z = 0; z < 1; z++) {
         popupn &= ((robotod ? 3 : 2) / (Math.max(2, 9)));
      }
         popupn |= 2;
      philippinesK += `${googleC.size}`;
   }
       let countryp = String.fromCharCode(119,95,49,48,95,115,111,108,105,100,99,111,108,111,114,0);
       let xvodE = 5.0;
       let modityl = String.fromCharCode(122,95,53,55,95,112,114,111,102,105,108,101,115,0);
          let reportH = String.fromCharCode(108,95,56,56,95,111,110,101,99,104,0);
          let empty4 = 2.0;
         modityl += `${reportH.length | parseInt(`${empty4}`)}`;
         xvodE /= Math.max(2, parseFloat(`${modityl.length}`));
          let fillI = false;
          let middlewarex: Map<any, any> = new Map([[String.fromCharCode(113,95,53,54,95,100,101,108,97,121,97,98,108,101,0),629], [String.fromCharCode(108,95,49,95,117,112,115,104,105,102,116,101,100,0),540], [String.fromCharCode(107,101,121,102,114,97,109,101,115,95,122,95,57,54,0),516]]);
         countryp = `${(parseInt(`${xvodE}`) * (fillI ? 3 : 2))}`;
         fillI = middlewarex.size < middlewarex.size;
      if (3.41 == (parseFloat(`${countryp.length}`) + xvodE) || 2 == (countryp.length + parseInt(`${xvodE}`))) {
         xvodE -= parseFloat(`${parseInt(`${xvodE}`) % 1}`);
      }
      if (xvodE == 3.50) {
          let animationZ: Map<any, any> = new Map([[String.fromCharCode(112,97,115,115,101,115,95,102,95,50,52,0),true ], [String.fromCharCode(122,111,109,98,105,101,95,52,95,49,51,0),true ]]);
          let eighteen2 = String.fromCharCode(100,105,115,116,97,110,99,101,115,95,99,95,57,0);
          let liveq = 3;
         modityl = `${animationZ.size}`;
         animationZ.set(`${liveq}`, liveq ^ 2);
         eighteen2 += `${3 - liveq}`;
      }
          let chinaZ: Map<any, any> = new Map([[String.fromCharCode(107,95,55,52,95,98,117,108,107,0),67], [String.fromCharCode(117,116,112,117,116,95,99,95,50,56,0),339]]);
         xvodE /= Math.max(parseFloat(`${countryp.length}`), 5);
         chinaZ.set(`${chinaZ.size}`, 3);
       let sportsz = 3.0;
       let heartx = 0.0;
      if ((countryp.length / (Math.max(7, parseInt(`${xvodE}`)))) >= 1 && (xvodE / (Math.max(5, parseFloat(`${countryp.length}`)))) >= 3.28) {
         countryp += `${2 ^ parseInt(`${heartx}`)}`;
      }
         modityl = `${3 % (Math.max(9, parseInt(`${sportsz}`)))}`;
      sideT = new Map([[`${resend6}`, parseInt(`${resend6}`) >> (Math.min(Math.abs(2), 3))]]);
   for (let c = 0; c < 3; c++) {
       let untickl = String.fromCharCode(117,95,49,50,95,114,102,112,115,0);
       let mathz: Map<any, any> = new Map([[String.fromCharCode(100,95,57,56,95,115,117,99,99,101,115,115,111,114,0),228], [String.fromCharCode(114,101,115,117,108,116,95,120,95,57,50,0),566]]);
          let fillG = 5;
         untickl = `${(String.fromCharCode(111,0) == untickl ? mathz.size : untickl.length)}`;
         fillG <<= Math.min(2, Math.abs(fillG));
          let styleg = String.fromCharCode(121,95,53,56,95,102,114,101,113,98,97,114,107,0);
         mathz.set(untickl, mathz.size | untickl.length);
         styleg += `${(styleg == String.fromCharCode(55,0) ? styleg.length : styleg.length)}`;
       let policyU = 0;
       let macauD = 0;
       let stylegc = String.fromCharCode(99,117,118,105,100,95,52,95,51,49,0);
      let submitq = 8244723 <= macauD;
      do {
         macauD <<= Math.min(Math.abs(macauD), 4);
         if (submitq) {
            break;
         }
      } while (submitq && (5 >= (macauD << (Math.min(Math.abs(2), 5)))));
      if (1 > stylegc.length) {
         stylegc = `${macauD}`;
      }
      resend6 *= parseFloat(`${googleC.size}`);
   }
   for (let d = 0; d < 3; d++) {
      verticalk = philippinesK.length <= 19 || !verticalk;
   }
      console.error(e)

   for (let w = 0; w < 1; w++) {
       let data7: Array<any> = [664, 485, 779];
       let vertical6 = 1;
       let filled0 = String.fromCharCode(112,95,54,55,95,105,110,116,108,101,0);
      for (let u = 0; u < 3; u++) {
         vertical6 >>= Math.min(Math.abs(3), 1);
      }
      while ((1 + vertical6) < 1 || (vertical6 + filled0.length) < 1) {
         vertical6 <<= Math.min(5, Math.abs(vertical6));
         break;
      }
          let eighteeno = true;
          let feedback8 = String.fromCharCode(102,95,55,56,95,106,115,105,109,100,99,112,117,0);
         data7.push(3 << (Math.min(5, data7.length)));
         eighteeno = feedback8 == String.fromCharCode(65,0);
         feedback8 = `${feedback8.length * 2}`;
         data7 = [1];
         data7 = [1 << (Math.min(3, data7.length))];
      let halfi = 8767110 <= vertical6;
      do {
         vertical6 |= vertical6;
         if (halfi) {
            break;
         }
      } while (halfi && ((filled0.length | 3) > 5));
         data7.push(filled0.length ^ 3);
       let friendse = 4.0;
       let e_counto = 1.0;
         vertical6 -= vertical6;
      j_counts -= 3 % (Math.max(parseInt(`${resend6}`), 5));
   }
      statsq = `${weiboO.length}`;
      sideT.set(`${philippinesK}`, sideT.size + philippinesK.length);
   for (let q = 0; q < 2; q++) {
       let current6 = String.fromCharCode(114,97,105,116,95,54,95,54,56,0);
       let action6 = String.fromCharCode(101,95,57,55,95,112,97,114,116,105,116,105,111,110,105,110,103,0);
       let policyL = true;
         policyL = policyL || action6.length == 72;
         current6 = `${current6.length | 2}`;
      let awayu = action6 == String.fromCharCode(118,109,116,113,103,48,98,113,114,105,0);
      do {
          let modityG = 1.0;
          let brightnessQ = String.fromCharCode(102,95,55,54,95,114,101,115,97,109,112,108,101,114,0);
          let bottom0 = 4;
         action6 += `${(String.fromCharCode(80,0) == brightnessQ ? bottom0 : brightnessQ.length)}`;
         modityG += parseInt(`${modityG}`);
         if (awayu) {
            break;
         }
      } while ((policyL) && awayu);
         current6 += `${(action6.length & (policyL ? 3 : 5))}`;
      if (current6 == String.fromCharCode(65,0)) {
         action6 = `${(action6 == String.fromCharCode(102,0) ? (policyL ? 4 : 3) : action6.length)}`;
      }
         policyL = action6 == String.fromCharCode(53,0);
          let lineR = String.fromCharCode(103,95,53,57,95,99,105,112,104,101,114,116,101,120,116,0);
          let resultK = String.fromCharCode(112,104,114,97,115,101,115,95,56,95,49,48,0);
          let temperatures = String.fromCharCode(118,99,97,99,100,97,116,97,95,101,95,57,56,0);
         policyL = temperatures.length <= 48;
         lineR = `${(String.fromCharCode(54,0) == lineR ? lineR.length : resultK.length)}`;
         resultK += `${lineR.length + 2}`;
         temperatures += `${resultK.length}`;
         action6 += `${((policyL ? 5 : 5) / (Math.max(1, 9)))}`;
          let areaj: Array<any> = [980, 307];
          let profileY = String.fromCharCode(97,110,115,105,95,106,95,56,50,0);
         action6 += `${areaj.length}`;
         areaj = [profileY.length % 1];
         profileY += `${profileY.length}`;
      sideT.set(`${verticalk}`, googleC.size);
   }
   for (let v = 0; v < 2; v++) {
       let sendh = 3.0;
       let liveG = 4;
       let stationsp: Map<any, any> = new Map([[String.fromCharCode(105,109,97,103,101,114,111,116,97,116,101,95,51,95,49,50,0),false ], [String.fromCharCode(111,95,57,54,95,100,117,112,101,100,0),false ], [String.fromCharCode(112,95,51,95,116,104,114,101,115,104,111,108,100,115,0),false ]]);
       let modityP = String.fromCharCode(105,114,99,97,109,95,106,95,53,54,0);
       let foundX = String.fromCharCode(100,95,55,49,95,99,105,100,0);
      if (3 < foundX.length) {
          let playlist4 = String.fromCharCode(115,116,105,112,112,101,100,95,53,95,52,50,0);
          let styleW = String.fromCharCode(121,95,56,50,95,102,105,110,116,0);
         foundX += `${playlist4.length % 1}`;
         playlist4 += `${styleW.length / (Math.max(2, styleW.length))}`;
      }
      while (stationsp.get(`${liveG}`) != null) {
         liveG >>= Math.min(5, Math.abs(1));
         break;
      }
      let changeF = String.fromCharCode(54,105,103,101,121,100,117,98,53,50,0) == modityP;
      do {
         modityP = "2";
         if (changeF) {
            break;
         }
      } while ((foundX.startsWith(`${modityP.length}`)) && changeF);
         liveG <<= Math.min(Math.abs(modityP.length - 3), 5);
      let blacklist0 = String.fromCharCode(110,101,122,52,97,106,0) == modityP;
      do {
          let expiredZ = 4.0;
          let infoV = String.fromCharCode(116,111,110,101,109,97,112,95,100,95,54,57,0);
         modityP += `${liveG << (Math.min(modityP.length, 2))}`;
         expiredZ *= parseInt(`${expiredZ}`) % (Math.max(2, 8));
         infoV += `${parseInt(`${expiredZ}`)}`;
         if (blacklist0) {
            break;
         }
      } while (((sendh - 2.76) <= 3.33) && blacklist0);
      if ((liveG ^ 1) >= 2 && 2 >= (1 ^ foundX.length)) {
         foundX = `${stationsp.size ^ 3}`;
      }
      while (stationsp.get(`${sendh}`) == null) {
         sendh += parseFloat(`${parseInt(`${sendh}`)}`);
         break;
      }
         sendh -= parseFloat(`${1}`);
          let dialogL = 2.0;
          let playlistI = String.fromCharCode(103,95,52,50,95,102,105,108,108,115,0);
         modityP += "1";
         dialogL *= parseFloat(`${2 | playlistI.length}`);
         playlistI += `${parseInt(`${dialogL}`)}`;
       let carouselI: Array<any> = [999, 663];
       let const_qG: Array<any> = [102, 205, 255];
          let strs: Map<any, any> = new Map([[String.fromCharCode(122,95,56,55,95,115,101,108,101,99,116,105,111,110,115,0),String.fromCharCode(120,105,112,104,95,112,95,48,0)], [String.fromCharCode(110,95,54,48,95,110,111,104,101,97,100,101,114,0),String.fromCharCode(97,99,99,101,108,101,114,97,116,105,111,110,95,119,95,49,50,0)], [String.fromCharCode(97,95,54,48,95,103,97,116,101,100,0),String.fromCharCode(115,119,114,105,95,49,95,54,48,0)]]);
          let twitterj = 4.0;
         carouselI = [1];
         strs.set(`${twitterj}`, strs.size);
         twitterj *= strs.size / 1;
         foundX += `${carouselI.length}`;
      let relatedW = carouselI.length <= 9473461;
      do {
         carouselI.push(parseInt(`${sendh}`) >> (Math.min(3, Math.abs(2))));
         if (relatedW) {
            break;
         }
      } while (relatedW && (carouselI.includes(liveG)));
      for (let z = 0; z < 2; z++) {
         const_qG = [modityP.length & 1];
      }
      if ((parseInt(`${sendh}`) / (Math.max(foundX.length, 4))) >= 1 || 3.66 >= (parseFloat(`${foundX.length}`) / (Math.max(5, sendh)))) {
          let bufferO = false;
         foundX += `${liveG * 2}`;
         bufferO = (!bufferO ? bufferO : bufferO);
      }
      statsq += `${philippinesK.length}`;
   }
      resend6 *= (parseFloat(`${String.fromCharCode(106,0) == philippinesK ? weiboO.length : philippinesK.length}`));
   while (googleC.size == sideT.size) {
      googleC = new Map([[`${googleC.size}`, (philippinesK == String.fromCharCode(117,0) ? philippinesK.length : googleC.size)]]);
      break;
   }
      weiboO = "1";
      console.error("Error reading video duration.. skipping check for completeness")
    }
    
  }

  ffmpegDownload(outputFilePath, ffmpegScript, url, onProgress, handleOnComplete, onError, onSessionCreated)
  
}

export async function downloadVodImage(vod: XVSScoreDark){
       let leftO: Array<any> = [String.fromCharCode(99,95,56,56,95,111,119,110,101,114,115,0), String.fromCharCode(115,111,105,115,99,111,110,110,101,99,116,105,110,103,95,101,95,53,55,0)];
    let movies9 = 3.0;
    let y_center4 = true;
    let styleY = 0.0;
    let downloadingQ = true;
    let smallk = 2.0;
    let topicM: Map<any, any> = new Map([[String.fromCharCode(104,95,53,49,95,108,111,111,112,105,110,103,0),911], [String.fromCharCode(122,95,54,57,95,112,114,111,112,101,114,0),299], [String.fromCharCode(110,115,116,97,110,116,95,113,95,55,50,0),419]]);
    let leagueS: Map<any, any> = new Map([[String.fromCharCode(109,100,101,99,95,114,95,57,57,0),148], [String.fromCharCode(113,95,51,56,95,120,102,111,114,109,0),804]]);
    let mathC: Map<any, any> = new Map([[String.fromCharCode(106,95,54,49,95,112,121,116,104,111,110,0),649], [String.fromCharCode(99,104,97,112,95,48,95,49,48,0),489], [String.fromCharCode(119,105,110,97,114,109,95,52,95,56,51,0),444]]);
    let livep: Map<any, any> = new Map([[String.fromCharCode(112,95,52,52,95,108,105,110,101,97,114,0),991], [String.fromCharCode(99,95,57,55,95,116,112,105,100,0),856], [String.fromCharCode(99,95,51,57,95,99,104,101,99,107,109,109,0),594]]);
      downloadingQ = y_center4;
   for (let v = 0; v < 1; v++) {
      downloadingQ = leftO.includes(downloadingQ);
   }

  const imagePath = RNFetchBlob.fs.dirs.DocumentDir + '/VodImages' + `/pic${vod.vod_id}.png`

   for (let k = 0; k < 1; k++) {
      topicM = new Map([[`${movies9}`, ((y_center4 ? 3 : 4) - parseInt(`${movies9}`))]]);
   }
       let stationsO = 3.0;
       let readU: Array<any> = [792, 228, 717];
       let greyp = 2.0;
      while (1.95 > (1.46 + greyp)) {
          let sinaM = String.fromCharCode(106,95,51,54,0);
          let ballW = String.fromCharCode(117,112,100,97,116,101,95,117,95,57,52,0);
         readU.push(sinaM.length * 2);
         sinaM += `${ballW.length}`;
         ballW += `${ballW.length}`;
         break;
      }
         readU = [3 / (Math.max(parseInt(`${greyp}`), 7))];
      for (let c = 0; c < 1; c++) {
         stationsO -= readU.length | 1;
      }
          let floatera = 4;
          let appleh = String.fromCharCode(110,95,51,51,95,98,105,110,116,114,101,101,0);
          let modelsq = 5;
         readU.push(3);
         floatera -= (appleh == String.fromCharCode(100,0) ? appleh.length : modelsq);
         modelsq >>= Math.min(Math.abs(1), 1);
      while ((readU.length - parseInt(`${greyp}`)) > 5 && 4 > (readU.length >> (Math.min(Math.abs(5), 4)))) {
          let goalK = String.fromCharCode(105,110,116,114,111,95,106,95,56,0);
          let downloadery: Map<any, any> = new Map([[String.fromCharCode(115,95,51,53,95,104,117,100,0),625], [String.fromCharCode(119,95,50,56,95,112,114,101,102,101,116,99,104,101,114,0),190], [String.fromCharCode(105,116,113,117,101,117,101,95,115,95,53,57,0),551]]);
         readU = [parseInt(`${stationsO}`)];
         goalK = `${downloadery.size}`;
         downloadery = new Map([[`${downloadery.size}`, downloadery.size]]);
         break;
      }
         stationsO -= readU.length;
         stationsO -= readU.length + 1;
         readU = [parseInt(`${greyp}`)];
      while (5 > (2 & readU.length) || (readU.length | 2) > 5) {
         readU.push(parseInt(`${greyp}`));
         break;
      }
      styleY -= parseFloat(`${3 + parseInt(`${movies9}`)}`);
  

   let resultg = 5485810.0 >= movies9;
   do {
      movies9 *= (leftO.length ^ (downloadingQ ? 5 : 5));
      if (resultg) {
         break;
      }
   } while ((movies9 > 1.65) && resultg);
      movies9 /= Math.max(2, 2);

  if (await RNFetchBlob.fs.exists(imagePath)) return

  try {
    await RNFetchBlob.fs.mkdir(RNFetchBlob.fs.dirs.DocumentDir + '/VodImages').catch((err) => {})

   while (topicM.get(`${leftO.length}`) != null) {
       let listb = 4.0;
       let z_image9 = true;
       let humidityE: Map<any, any> = new Map([[String.fromCharCode(116,114,97,110,115,102,111,114,109,101,114,95,101,95,49,0),337], [String.fromCharCode(107,115,101,116,95,97,95,55,50,0),897], [String.fromCharCode(109,98,99,115,95,109,95,56,0),293]]);
         humidityE.set(`${listb}`, parseInt(`${listb}`) ^ humidityE.size);
      for (let i = 0; i < 3; i++) {
          let selectp = String.fromCharCode(121,95,53,55,95,100,105,118,105,100,111,114,115,0);
          let anythinkv = 1.0;
          let x_centerG: Array<any> = [String.fromCharCode(105,95,56,53,95,99,108,108,105,0), String.fromCharCode(119,95,56,56,95,102,114,97,109,101,115,105,122,101,0)];
          let gpayx = String.fromCharCode(117,95,55,48,95,116,111,114,101,100,0);
         z_image9 = gpayx.length < 22 && 22 < selectp.length;
         selectp += `${x_centerG.length}`;
         anythinkv /= Math.max(parseFloat(`${3 * parseInt(`${anythinkv}`)}`), 2);
         x_centerG.push(parseInt(`${anythinkv}`));
         gpayx += `${x_centerG.length & 2}`;
      }
      let ewardedD = 6756432 <= humidityE.size;
      do {
          let malaysiad = false;
         humidityE.set(`${listb}`, ((z_image9 ? 4 : 3) / (Math.max(parseInt(`${listb}`), 3))));
         malaysiad = (!malaysiad ? malaysiad : malaysiad);
         if (ewardedD) {
            break;
         }
      } while (((5 / (Math.max(8, humidityE.size))) <= 5) && ewardedD);
         humidityE.set(`${z_image9}`, ((z_image9 ? 2 : 5) * 2));
      if ((humidityE.size % (Math.max(4, 3))) < 4) {
          let annerN = 1.0;
          let memberg: Array<any> = [753, 838];
         humidityE.set(`${listb}`, 3 * humidityE.size);
         annerN *= parseInt(`${annerN}`) << (Math.min(2, Math.abs(2)));
         memberg = [memberg.length];
      }
      while (2 == humidityE.size) {
          let lnewsc: Array<any> = [21, 934];
         humidityE.set(`${lnewsc.length}`, lnewsc.length);
         break;
      }
      let agreementN = 8590585 >= humidityE.size;
      do {
          let bell4 = String.fromCharCode(117,110,119,114,105,116,97,98,108,101,95,110,95,49,50,0);
          let activeb: Array<any> = [295, 584];
          let policya = 2;
          let grayy: Array<any> = [662, 689];
          let uploads: Map<any, any> = new Map([[String.fromCharCode(121,95,49,57,95,115,116,114,101,116,99,104,0),String.fromCharCode(101,118,101,110,97,118,103,95,105,95,53,57,0)], [String.fromCharCode(112,114,111,100,117,99,116,102,95,52,95,50,48,0),String.fromCharCode(121,95,49,52,95,102,105,108,101,102,117,110,99,0)]]);
         humidityE = new Map([[`${uploads.size}`, grayy.length]]);
         bell4 = `${activeb.length}`;
         activeb = [policya & 3];
         policya -= activeb.length | bell4.length;
         grayy = [activeb.length - 3];
         uploads.set(`${policya}`, (String.fromCharCode(68,0) == bell4 ? bell4.length : policya));
         if (agreementN) {
            break;
         }
      } while (agreementN && ((3 ^ humidityE.size) <= 1 && 3 <= (humidityE.size * parseInt(`${listb}`))));
      while (1.21 < (2.27 / (Math.max(3, listb))) && 2.27 < listb) {
         listb *= parseFloat(`${1}`);
         break;
      }
      while (!Array.from(humidityE.values()).includes(listb)) {
         humidityE = new Map([[`${z_image9}`, ((z_image9 ? 4 : 1) << (Math.min(Math.abs(parseInt(`${listb}`)), 4)))]]);
         break;
      }
      leftO.push((parseInt(`${listb}`) * (downloadingQ ? 3 : 2)));
      break;
   }
      styleY /= Math.max(5, parseFloat(`${topicM.size ^ 1}`));
    const res = await RNFetchBlob
      .config({
        path: imagePath
      })
      .fetch('GET', vod.vod_pic_oss ?? vod.vod_pic) 

    
  } catch {
    
  }

}

export function getUrlOfVod(vod: XVSScoreDark, vodSourceId: number, vodUrlNid: number, vodIsAdult?: boolean) {
  
  if (vodIsAdult){
       let chatu: Map<any, any> = new Map([[String.fromCharCode(97,95,53,50,95,114,101,97,112,101,114,0),164], [String.fromCharCode(105,109,112,97,99,116,95,48,95,54,54,0),740], [String.fromCharCode(104,111,116,95,54,95,50,50,0),885]]);
    let heart7 = String.fromCharCode(98,95,57,53,95,110,115,117,105,0);
    let resendR = String.fromCharCode(100,95,57,55,95,102,112,117,116,115,0);
    let sourceD = false;
    let time_1Q: Map<any, any> = new Map([[String.fromCharCode(109,106,112,101,103,98,95,111,95,52,53,0),594], [String.fromCharCode(121,95,52,48,95,99,104,105,108,100,0),228], [String.fromCharCode(106,105,110,99,108,117,100,101,95,108,95,53,50,0),633]]);
    let actionsw = String.fromCharCode(105,95,50,51,95,113,116,97,98,108,101,115,0);
    let overY = String.fromCharCode(100,117,112,115,111,114,116,95,121,95,52,56,0);
    let notificationd = String.fromCharCode(109,95,56,49,95,114,101,97,99,116,105,111,110,0);
    let ewardedT = 4.0;
      overY = `${(String.fromCharCode(75,0) == heart7 ? heart7.length : overY.length)}`;
       let downloaders = false;
       let acceptedm = String.fromCharCode(116,95,49,52,95,114,101,98,117,105,108,100,0);
       let i_centerl = String.fromCharCode(115,116,111,114,109,98,105,114,100,95,103,95,55,50,0);
          let back2 = String.fromCharCode(98,95,57,50,95,97,99,107,110,111,119,108,101,100,103,101,0);
          let sportsI = 0;
         i_centerl = `${(acceptedm == String.fromCharCode(99,0) ? (downloaders ? 4 : 3) : acceptedm.length)}`;
         back2 = `${sportsI}`;
         sportsI %= Math.max(1, 1);
      if (downloaders || i_centerl.length > 4) {
         downloaders = (((downloaders ? 39 : i_centerl.length) * i_centerl.length) < 39);
      }
          let combinex = String.fromCharCode(112,95,55,54,95,109,109,99,111,115,0);
          let flyer9 = 5.0;
         downloaders = i_centerl == String.fromCharCode(85,0);
         combinex = "1";
         flyer9 -= parseFloat(`${3}`);
      while (acceptedm.length >= 4 || downloaders) {
          let actions8 = String.fromCharCode(118,95,49,56,95,99,111,110,103,0);
          let sportsF: Map<any, any> = new Map([[String.fromCharCode(112,95,51,53,95,119,114,97,112,100,101,116,101,99,116,0),335], [String.fromCharCode(97,95,49,55,95,116,105,109,101,111,117,116,101,100,0),870], [String.fromCharCode(102,114,101,101,100,95,56,95,53,56,0),134]]);
          let room0: Map<any, any> = new Map([[String.fromCharCode(100,101,105,110,105,116,95,116,95,56,48,0),486], [String.fromCharCode(119,95,51,49,95,115,116,105,99,107,121,0),155]]);
          let signinup6 = String.fromCharCode(116,114,105,112,108,101,95,119,95,53,56,0);
         acceptedm += `${actions8.length}`;
         actions8 = `${room0.size}`;
         sportsF = new Map([[`${room0.size}`, sportsF.size - 3]]);
         signinup6 += `${signinup6.length}`;
         break;
      }
      while (i_centerl != String.fromCharCode(111,0)) {
          let shareK = 4;
          let progress_ = String.fromCharCode(105,110,116,101,114,110,97,108,95,51,95,51,52,0);
         acceptedm = `${progress_.length & 1}`;
         shareK -= shareK;
         progress_ = `${shareK}`;
         break;
      }
          let starv = String.fromCharCode(113,95,50,52,95,97,99,99,101,112,116,101,100,0);
          let dataK: Array<any> = [160, 628, 746];
          let sell2 = 5.0;
         downloaders = starv.length <= 71 || acceptedm.length <= 71;
         starv = `${dataK.length >> (Math.min(Math.abs(2), 3))}`;
         dataK.push(parseInt(`${sell2}`) ^ dataK.length);
         sell2 /= Math.max(dataK.length, 2);
          let helperZ = String.fromCharCode(109,117,108,120,95,106,95,57,57,0);
          let stringl = String.fromCharCode(118,95,55,95,101,108,108,105,112,116,105,99,97,108,0);
         acceptedm = `${1 - acceptedm.length}`;
         helperZ = `${stringl.length}`;
         stringl += `${helperZ.length + 2}`;
          let incidente = String.fromCharCode(99,95,54,55,95,116,119,105,99,101,0);
          let weibos = 0;
          let componentx = String.fromCharCode(105,95,55,50,95,119,101,97,107,0);
         acceptedm += `${(2 << (Math.min(5, Math.abs((downloaders ? 4 : 2)))))}`;
         incidente = `${weibos & 1}`;
         weibos *= weibos;
         componentx = `${(String.fromCharCode(107,0) == incidente ? componentx.length : incidente.length)}`;
      while (acceptedm.endsWith(`${downloaders}`)) {
         downloaders = !downloaders && acceptedm.length <= 2;
         break;
      }
      resendR = `${((sourceD ? 4 : 5) % (Math.max(10, overY.length)))}`;
       let overM = String.fromCharCode(103,95,49,57,95,112,116,111,110,0);
       let hejiX: Map<any, any> = new Map([[String.fromCharCode(121,95,55,56,95,116,105,109,101,122,111,110,101,0),334], [String.fromCharCode(109,105,110,117,115,95,104,95,57,0),363]]);
         hejiX = new Map([[`${hejiX.size}`, overM.length + 1]]);
      for (let b = 0; b < 3; b++) {
          let whatsapph = false;
         hejiX.set(`${whatsapph}`, ((whatsapph ? 3 : 3) ^ overM.length));
      }
      let entrys = hejiX.size <= 9876224;
      do {
         hejiX.set(`${overM}`, hejiX.size);
         if (entrys) {
            break;
         }
      } while (entrys && (hejiX.size <= 1));
         overM = `${2 - overM.length}`;
         hejiX.set(`${overM}`, (overM == String.fromCharCode(49,0) ? overM.length : hejiX.size));
      let overi = overM.length >= 8243887;
      do {
         overM += `${overM.length}`;
         if (overi) {
            break;
         }
      } while (overi && ((5 - overM.length) <= 2 || (overM.length - 5) <= 4));
      time_1Q.set(`${actionsw}`, 2 * chatu.size);
      time_1Q = new Map([[`${chatu.size}`, chatu.size | 3]]);
   for (let l = 0; l < 3; l++) {
      heart7 = `${chatu.size % 1}`;
   }
   let sharedW = sourceD ? !sourceD : sourceD;
   do {
      sourceD = heart7 == String.fromCharCode(103,0);
      if (sharedW) {
         break;
      }
   } while (sharedW && (3 <= time_1Q.size));
       let spec2 = String.fromCharCode(101,95,49,50,95,105,112,118,0);
       let disconnectedu = 2;
       let moditys = 4.0;
      let largej = moditys <= 8781124.0;
      do {
          let goalN = true;
          let watchT = true;
          let description_2T: Map<any, any> = new Map([[String.fromCharCode(97,117,100,105,101,110,99,101,95,116,95,51,51,0),289], [String.fromCharCode(100,111,117,103,108,97,115,95,104,95,48,0),389], [String.fromCharCode(114,95,53,57,95,101,110,103,108,105,115,104,0),687]]);
          let hejis = true;
         moditys /= Math.max(parseFloat(`${3}`), 5);
         goalN = null != description_2T.get(`${watchT}`);
         watchT = (91 == (description_2T.size + (goalN ? 91 : description_2T.size)));
         hejis = (68 <= (description_2T.size << (Math.min(4, Math.abs((!goalN ? 68 : description_2T.size))))));
         if (largej) {
            break;
         }
      } while (((disconnectedu + 2) >= 1) && largej);
      for (let y = 0; y < 1; y++) {
          let inactive8 = 5.0;
          let giftm: Array<any> = [536, 459, 344];
          let shoott = String.fromCharCode(97,116,116,97,99,104,95,57,95,50,51,0);
          let resendv = 1;
          let zhengpianY = String.fromCharCode(99,114,101,100,101,110,116,105,97,108,95,107,95,57,48,0);
         moditys -= parseFloat(`${2 & spec2.length}`);
         inactive8 *= giftm.length / 1;
         giftm.push(parseInt(`${inactive8}`));
         shoott += "1";
         resendv %= Math.max(5, 1 >> (Math.min(5, zhengpianY.length)));
         zhengpianY += "2";
      }
       let flyerf: Map<any, any> = new Map([[String.fromCharCode(119,95,56,49,95,97,112,109,116,101,115,116,0),true ], [String.fromCharCode(97,95,57,56,95,116,97,100,100,0),false ], [String.fromCharCode(101,95,53,56,95,105,110,118,111,108,118,101,100,0),true ]]);
         moditys /= Math.max(3, parseFloat(`${spec2.length >> (Math.min(3, Math.abs(flyerf.size)))}`));
         moditys += parseFloat(`${1 | flyerf.size}`);
      overY = `${spec2.length * chatu.size}`;
   for (let z = 0; z < 2; z++) {
       let settingsl = 2.0;
       let baidue = String.fromCharCode(117,95,56,52,95,122,97,108,108,111,99,0);
       let fillY: Array<any> = [String.fromCharCode(116,95,54,48,95,112,97,114,105,116,105,111,110,105,110,103,0), String.fromCharCode(97,95,56,95,101,120,99,108,117,115,105,118,101,0), String.fromCharCode(109,95,55,50,95,114,101,102,99,111,117,110,116,101,100,0)];
       let sound1 = false;
       let matchesT = true;
         sound1 = parseInt(`${settingsl}`) < fillY.length;
      let disconnected0 = matchesT ? !matchesT : matchesT;
      do {
         matchesT = fillY.includes(matchesT);
         if (disconnected0) {
            break;
         }
      } while (disconnected0 && (settingsl <= 5.90));
       let teamv = 5.0;
          let rankI: Map<any, any> = new Map([[String.fromCharCode(109,95,56,56,95,112,99,97,112,0),601], [String.fromCharCode(117,110,112,114,111,116,101,99,116,95,115,95,51,56,0),925], [String.fromCharCode(101,112,101,108,95,48,95,50,50,0),113]]);
          let relatedb = 2;
          let chatZ = true;
         sound1 = settingsl <= parseFloat(`${baidue.length}`);
         rankI = new Map([[`${relatedb}`, relatedb]]);
         chatZ = rankI.size <= 62;
      for (let x = 0; x < 2; x++) {
         teamv -= parseFloat(`${parseInt(`${teamv}`) % 2}`);
      }
      for (let c = 0; c < 2; c++) {
         teamv *= parseFloat(`${2 * baidue.length}`);
      }
          let sharedR = String.fromCharCode(99,95,55,49,95,102,111,117,114,116,104,0);
         baidue = `${sharedR.length >> (Math.min(3, Math.abs(parseInt(`${teamv}`))))}`;
      actionsw = `${fillY.length}`;
   }

    return vod.vod_play_list.urls
      .find(url => url.nid === vodUrlNid)
      ?.url
  }
  return vod.vod_sources
    .find(source => source.source_id === vodSourceId)?.vod_play_list.urls
    .find(url => url.nid === vodUrlNid)
    ?.url

}

export async function pauseDownloadVod(id:string, onPause: any) {
  
  onPause();

  const originalFilePath = `${RNFetchBlob.fs.dirs.DocumentDir}/SavedVideos/${id}.mp4`

  await RNFetchBlob.fs.mkdir(RNFetchBlob.fs.dirs.DocumentDir + '/PartialDownload').catch((err) => {})

  const outputFolder = `${RNFetchBlob.fs.dirs.DocumentDir}/PartialDownload/${id}`

  await RNFetchBlob.fs.mkdir(outputFolder).catch((err) => {})

  const segmentName = `${(await RNFetchBlob.fs.ls(outputFolder)).length}.mp4`

  if (await RNFetchBlob.fs.exists(originalFilePath)){
       let control6 = String.fromCharCode(113,112,113,115,99,97,108,101,95,122,95,54,50,0);
    let memberM = String.fromCharCode(109,95,49,48,95,112,108,97,110,97,114,116,111,117,121,118,121,0);
    let tumbnail5 = true;
    let sportc = String.fromCharCode(105,95,56,56,95,115,105,110,107,115,0);
    let assista = 5.0;
    let nterstitiali = 5.0;
    let topicr = 3;
    let singaporeJ = String.fromCharCode(105,95,49,56,95,109,101,116,97,108,0);
    let home1 = String.fromCharCode(119,97,118,101,108,101,116,95,99,95,51,48,0);
   for (let m = 0; m < 2; m++) {
       let constants0 = String.fromCharCode(109,101,115,115,97,103,101,95,52,95,49,48,0);
       let nalytics_ = false;
       let mappingD = String.fromCharCode(104,95,53,55,95,115,112,114,105,116,101,115,0);
      while (2 == mappingD.length) {
         constants0 += `${((nalytics_ ? 2 : 4))}`;
         break;
      }
         mappingD += `${((nalytics_ ? 1 : 3) % (Math.max(constants0.length, 7)))}`;
      for (let k = 0; k < 3; k++) {
         nalytics_ = !nalytics_;
      }
      for (let u = 0; u < 1; u++) {
          let bannerJ = 5.0;
         nalytics_ = 55.5 <= bannerJ;
      }
         mappingD = `${constants0.length}`;
      let calendarv = nalytics_ ? !nalytics_ : nalytics_;
      do {
          let whistlex = 3.0;
          let dataR: Map<any, any> = new Map([[String.fromCharCode(100,95,55,52,95,110,111,114,109,97,108,0),18], [String.fromCharCode(99,101,105,108,95,109,95,54,52,0),77]]);
          let firebaseR = String.fromCharCode(109,95,57,95,99,111,109,109,105,116,0);
         nalytics_ = (mappingD.length * parseInt(`${whistlex}`)) > 50;
         whistlex /= Math.max(firebaseR.length, 3);
         dataR = new Map([[`${dataR.size}`, dataR.size * 2]]);
         firebaseR = `${firebaseR.length - dataR.size}`;
         if (calendarv) {
            break;
         }
      } while ((constants0.length >= 3) && calendarv);
      if (5 <= mappingD.length || nalytics_) {
          let filedf = 1.0;
          let gemfileu: Array<any> = [String.fromCharCode(103,95,55,55,95,102,114,101,101,112,97,100,100,114,115,0), String.fromCharCode(114,95,49,55,95,105,110,115,116,97,110,99,101,0)];
          let event5 = String.fromCharCode(115,116,101,97,108,95,54,95,52,52,0);
         nalytics_ = constants0.length == 45;
         filedf -= parseFloat(`${event5.length}`);
         gemfileu.push(event5.length);
      }
      for (let o = 0; o < 1; o++) {
         nalytics_ = mappingD.length == 24;
      }
      for (let l = 0; l < 2; l++) {
          let searchbarh = 1;
          let selectO = String.fromCharCode(116,97,98,115,95,48,95,50,51,0);
         mappingD = `${1 + searchbarh}`;
         searchbarh -= 3;
         selectO = `${3 >> (Math.min(5, selectO.length))}`;
      }
      assista *= ((nalytics_ ? 4 : 5) % (Math.max(topicr, 7)));
   }
      control6 += `${(String.fromCharCode(104,0) == control6 ? control6.length : (tumbnail5 ? 3 : 4))}`;
      nterstitiali *= parseFloat(`${topicr & parseInt(`${nterstitiali}`)}`);
       let dangerw = String.fromCharCode(117,118,104,111,114,105,122,111,110,116,97,108,95,109,95,53,52,0);
       let chartQ: Map<any, any> = new Map([[String.fromCharCode(97,95,57,53,95,117,114,118,101,115,0),String.fromCharCode(97,110,111,116,104,101,114,95,112,95,51,56,0)], [String.fromCharCode(118,95,57,52,95,111,112,101,110,109,112,116,0),String.fromCharCode(112,95,51,56,95,105,100,101,110,116,105,99,97,108,0)], [String.fromCharCode(116,111,107,101,110,110,102,116,116,120,95,102,95,50,51,0),String.fromCharCode(108,95,57,49,95,100,101,103,114,97,100,97,116,105,111,110,0)]]);
         chartQ.set(`${dangerw}`, dangerw.length / 3);
         chartQ.set(`${dangerw}`, chartQ.size % (Math.max(7, dangerw.length)));
      memberM = "2";
   if (!memberM.startsWith(control6)) {
      control6 = "1";
   }
   if (memberM.startsWith(`${assista}`)) {
      memberM += `${2 >> (Math.min(3, sportc.length))}`;
   }
      memberM += `${memberM.length - control6.length}`;

    

      topicr *= topicr << (Math.min(2, Math.abs(1)));
       let searchbarg = 2.0;
       let hookse = String.fromCharCode(115,117,98,116,114,101,101,115,95,108,95,52,55,0);
       let greeny = String.fromCharCode(114,95,49,48,95,112,97,99,107,101,116,0);
       let floater8 = String.fromCharCode(116,104,114,101,115,104,111,108,100,95,103,95,51,48,0);
         hookse += `${parseInt(`${searchbarg}`)}`;
      for (let s = 0; s < 2; s++) {
         greeny += `${1 << (Math.min(5, Math.abs(parseInt(`${searchbarg}`))))}`;
      }
      let questF = searchbarg <= 8688061.0;
      do {
          let layoutc = String.fromCharCode(101,109,117,108,97,116,101,95,54,95,52,49,0);
         searchbarg /= Math.max(parseFloat(`${2}`), 5);
         layoutc = `${(layoutc == String.fromCharCode(102,0) ? layoutc.length : layoutc.length)}`;
         if (questF) {
            break;
         }
      } while ((1.51 == (2.89 / (Math.max(1, searchbarg)))) && questF);
      if (floater8.length == greeny.length) {
         greeny = `${hookse.length + 2}`;
      }
          let natives: Map<any, any> = new Map([[String.fromCharCode(105,109,108,116,95,102,95,49,56,0),533], [String.fromCharCode(102,114,97,109,101,115,101,116,116,101,114,95,118,95,55,0),717], [String.fromCharCode(113,95,54,54,95,105,115,100,105,97,99,114,105,116,105,99,0),791]]);
          let analytice: Array<any> = [String.fromCharCode(114,101,108,117,95,49,95,49,48,0), String.fromCharCode(99,95,49,50,95,114,101,118,111,107,97,98,108,101,0)];
         searchbarg -= parseFloat(`${2}`);
         natives.set(`${analytice.length}`, 2);
         analytice = [3 - analytice.length];
      assista += 3 << (Math.min(4, singaporeJ.length));
      sportc += `${topicr | 1}`;
   for (let k = 0; k < 2; k++) {
       let emojiA = 0.0;
       let navigationY = String.fromCharCode(114,97,98,105,110,95,50,95,49,51,0);
       let customa = 3;
         emojiA /= Math.max(1, parseFloat(`${navigationY.length * 3}`));
         emojiA += parseFloat(`${navigationY.length | parseInt(`${emojiA}`)}`);
          let weiboS = String.fromCharCode(118,116,97,103,95,54,95,54,49,0);
         emojiA /= Math.max(parseFloat(`${weiboS.length}`), 2);
          let closeP = String.fromCharCode(103,110,114,101,95,52,95,57,55,0);
          let modelc = String.fromCharCode(112,108,97,99,101,104,111,108,100,101,114,95,120,95,52,55,0);
         emojiA -= (parseFloat(`${String.fromCharCode(67,0) == modelc ? modelc.length : closeP.length}`));
         navigationY += `${parseInt(`${emojiA}`) | 3}`;
      while (!navigationY.startsWith(`${customa}`)) {
          let appsX = String.fromCharCode(114,101,99,114,101,97,116,101,95,55,95,56,54,0);
          let hongkongE = String.fromCharCode(105,115,108,111,119,95,109,95,52,51,0);
          let stringg = String.fromCharCode(118,112,111,105,110,116,95,119,95,56,53,0);
          let long_vA: Map<any, any> = new Map([[String.fromCharCode(99,95,49,57,95,111,112,116,105,109,105,122,101,100,0),450], [String.fromCharCode(111,108,111,114,95,121,95,53,56,0),253]]);
         navigationY = "3";
         appsX += "3";
         hongkongE += `${long_vA.size}`;
         stringg += `${2 + hongkongE.length}`;
         long_vA = new Map([[`${long_vA.size}`, appsX.length ^ long_vA.size]]);
         break;
      }
          let navigationy = 3.0;
         customa %= Math.max(1, 1 | parseInt(`${emojiA}`));
         navigationy *= parseFloat(`${3}`);
         navigationY = `${navigationY.length % 2}`;
      for (let k = 0; k < 3; k++) {
         customa <<= Math.min(navigationY.length, 1);
      }
      topicr += topicr;
   }
   let weibol = 5989130 >= memberM.length;
   do {
      memberM = `${((tumbnail5 ? 4 : 3) | parseInt(`${assista}`))}`;
      if (weibol) {
         break;
      }
   } while ((memberM.includes(`${sportc.length}`)) && weibol);
       let sliderr = false;
       let crossR = 2;
       let vnewsF = 2;
         sliderr = !sliderr;
      let kickx = sliderr ? !sliderr : sliderr;
      do {
          let point7 = String.fromCharCode(100,95,56,0);
          let champion0: Array<any> = [651, 769];
         sliderr = (champion0.length << (Math.min(point7.length, 3))) < 13;
         if (kickx) {
            break;
         }
      } while (kickx && (!sliderr));
      memberM = `${((sliderr ? 5 : 4) % (Math.max(4, parseInt(`${assista}`))))}`;
       let calendarV = 3;
       let cornerr = 5;
          let description_w3o = String.fromCharCode(115,111,108,117,116,105,111,110,95,109,95,52,54,0);
          let infoO = String.fromCharCode(106,112,101,103,95,54,95,52,56,0);
         calendarV -= infoO.length;
         description_w3o = `${2 * description_w3o.length}`;
         infoO += `${description_w3o.length << (Math.min(1, description_w3o.length))}`;
         calendarV >>= Math.min(Math.abs(3 / (Math.max(3, calendarV))), 1);
         cornerr ^= 2 >> (Math.min(2, Math.abs(calendarV)));
         calendarV &= cornerr + calendarV;
      if (calendarV > cornerr) {
         calendarV %= Math.max(5, cornerr * calendarV);
      }
      let configureX = cornerr <= 5172575;
      do {
         cornerr &= 1;
         if (configureX) {
            break;
         }
      } while ((calendarV <= 1) && configureX);
      control6 += `${topicr * 3}`;

    

   while (!tumbnail5 && 1.25 > (5.41 / (Math.max(4, nterstitiali)))) {
      tumbnail5 = 61.16 <= nterstitiali;
      break;
   }
      nterstitiali += parseFloat(`${control6.length}`);
   if (memberM.length > 2 && tumbnail5) {
       let clubO = String.fromCharCode(115,111,108,105,100,99,111,108,111,114,95,51,95,50,51,0);
       let confirmationq = String.fromCharCode(111,95,50,56,95,97,109,102,101,110,99,0);
       let fullY = 2;
       let lefta = String.fromCharCode(114,101,115,117,108,116,105,110,103,95,49,95,56,48,0);
      if (3 > (fullY % (Math.max(5, 3))) || 2 > (clubO.length % 5)) {
         fullY |= lefta.length & 3;
      }
         lefta = `${fullY}`;
          let bellw = 2.0;
          let room2 = String.fromCharCode(109,95,57,50,95,105,115,97,99,0);
         fullY /= Math.max(2, 3);
         bellw += room2.length;
         room2 = "3";
      if (lefta.includes(`${confirmationq.length}`)) {
         confirmationq += "3";
      }
      if (4 > (2 + fullY)) {
         confirmationq = `${confirmationq.length}`;
      }
      while (2 < fullY) {
         fullY ^= clubO.length;
         break;
      }
      if (lefta.length > clubO.length) {
         clubO = "3";
      }
      for (let p = 0; p < 2; p++) {
          let mappingh = String.fromCharCode(107,105,108,108,95,98,95,54,49,0);
          let j_playerj = String.fromCharCode(117,98,108,105,99,95,122,95,49,50,0);
          let firebasek = String.fromCharCode(101,114,108,101,95,110,95,57,48,0);
         fullY |= fullY + j_playerj.length;
         mappingh = `${mappingh.length % 2}`;
         j_playerj += `${firebasek.length}`;
         firebasek += "1";
      }
      memberM = "2";
   }
      nterstitiali += parseFloat(`${3 / (Math.max(6, singaporeJ.length))}`);
      singaporeJ += "2";
      singaporeJ = `${singaporeJ.length}`;
   while (!tumbnail5 || control6.length == 3) {
      control6 = `${memberM.length + 3}`;
      break;
   }
    RNFetchBlob.fs.mv(originalFilePath, `${outputFolder}/${segmentName}`)
  }

}

export async function resumeDownloadVod(id: string, url:string, onProgress: any, onComplete: any, onError: any, onSessionCreated: any) {
  const originalFilePath = `${RNFetchBlob.fs.dirs.DocumentDir}/SavedVideos/${id}.mp4`
  const outputFolder = `${RNFetchBlob.fs.dirs.DocumentDir}/PartialDownload/${id}`

  if (!(await RNFetchBlob.fs.exists(outputFolder))){
       let halfh = 2.0;
    let hejin = String.fromCharCode(111,95,52,49,95,112,114,101,0);
    let dropdownV = String.fromCharCode(111,110,97,118,99,95,50,95,56,48,0);
    let privacyt = 4.0;
    let switch_na3: Array<any> = [936, 615];
    let vignetteI = String.fromCharCode(111,95,56,49,95,103,101,116,119,105,110,116,105,109,101,111,102,100,97,121,0);
    let helperX = 1;
    let albumC = 4.0;
    let bodan2 = String.fromCharCode(115,111,108,97,110,97,95,106,95,55,56,0);
      switch_na3 = [switch_na3.length / (Math.max(8, helperX))];
   for (let f = 0; f < 2; f++) {
      bodan2 += `${hejin.length}`;
   }
   if ((5.67 - albumC) < 5.54 || (albumC + 5.67) < 2.96) {
       let nterstitialf = true;
       let const_lm = 5;
         nterstitialf = !nterstitialf;
         nterstitialf = !nterstitialf && const_lm == 68;
          let weibo_ = String.fromCharCode(112,95,49,50,95,104,105,101,114,0);
          let popupy = 4.0;
         const_lm |= 1;
         weibo_ += `${1 + weibo_.length}`;
         popupy *= 2;
      let smallY = nterstitialf ? !nterstitialf : nterstitialf;
      do {
         nterstitialf = 84 < const_lm || !nterstitialf;
         if (smallY) {
            break;
         }
      } while ((3 >= (5 - const_lm) && nterstitialf) && smallY);
      if ((const_lm & 2) <= 1) {
          let reportR: Array<any> = [String.fromCharCode(97,117,116,111,114,101,115,105,122,101,115,95,57,95,54,55,0), String.fromCharCode(103,95,56,95,97,118,99,111,100,101,99,114,101,115,0), String.fromCharCode(97,95,50,51,95,114,101,100,117,99,101,100,0)];
          let controli: Array<any> = [String.fromCharCode(97,117,116,104,111,114,105,122,97,116,105,111,110,115,95,55,95,55,48,0), String.fromCharCode(97,109,114,119,98,100,97,116,97,95,114,95,49,57,0), String.fromCharCode(120,95,55,52,95,98,105,103,116,114,101,101,0)];
          let photoq = 2;
         const_lm |= ((nterstitialf ? 1 : 5) + photoq);
         reportR.push(1 | controli.length);
         controli = [1];
         photoq %= Math.max(reportR.length, 3);
      }
          let middles = 2;
          let membershiph = String.fromCharCode(121,95,53,57,95,97,98,115,101,105,108,0);
         const_lm -= middles;
         middles -= (String.fromCharCode(88,0) == membershiph ? membershiph.length : membershiph.length);
      albumC += vignetteI.length & helperX;
   }
   for (let p = 0; p < 1; p++) {
      hejin = `${parseInt(`${halfh}`)}`;
   }
   for (let b = 0; b < 1; b++) {
      dropdownV += `${(String.fromCharCode(107,0) == bodan2 ? bodan2.length : dropdownV.length)}`;
   }
   if (2 <= (hejin.length / (Math.max(10, helperX))) && 2 <= (hejin.length / 2)) {
      helperX &= 2 % (Math.max(2, parseInt(`${albumC}`)));
   }
   while (bodan2.length == helperX) {
      helperX %= Math.max(1, 1);
      break;
   }
   for (let s = 0; s < 2; s++) {
       let containerT: Map<any, any> = new Map([[String.fromCharCode(115,116,114,104,97,115,104,95,55,95,57,53,0),String.fromCharCode(100,95,52,53,95,103,97,105,110,0)], [String.fromCharCode(114,103,101,110,95,112,95,52,56,0),String.fromCharCode(114,101,111,114,100,101,114,101,100,95,106,95,53,51,0)]]);
      if (4 >= (containerT.size ^ 4) || (containerT.size ^ containerT.size) >= 4) {
         containerT = new Map([[`${containerT.size}`, containerT.size]]);
      }
      if (!Array.from(containerT.values()).includes(containerT.size)) {
         containerT.set(`${containerT.size}`, containerT.size);
      }
         containerT = new Map([[`${containerT.size}`, 1]]);
      helperX *= 3;
   }
      bodan2 = "1";
      switch_na3.push(parseInt(`${halfh}`) - 3);
   let leaguel = vignetteI.length <= 6646567;
   do {
      vignetteI += `${parseInt(`${halfh}`)}`;
      if (leaguel) {
         break;
      }
   } while (((2 << (Math.min(3, vignetteI.length))) <= 1 && 2 <= (vignetteI.length << (Math.min(2, switch_na3.length)))) && leaguel);

    

   while (4 == (helperX / 4)) {
      hejin += `${(String.fromCharCode(68,0) == vignetteI ? parseInt(`${halfh}`) : vignetteI.length)}`;
      break;
   }
      albumC /= Math.max(4, 2);
   while ((1.4 * albumC) <= 1.41) {
      albumC /= Math.max(2, bodan2.length);
      break;
   }
   if (2.38 >= privacyt) {
       let pressuret = String.fromCharCode(114,95,55,53,95,110,118,111,105,99,101,0);
       let showM: Map<any, any> = new Map([[String.fromCharCode(109,111,117,115,101,95,111,95,49,49,0),658], [String.fromCharCode(120,95,49,49,95,111,98,109,99,0),359]]);
       let blackA = 3;
       let progressb = true;
      for (let j = 0; j < 3; j++) {
          let upgrade5: Array<any> = [992, 613];
          let humidityw = String.fromCharCode(103,95,49,48,95,101,110,102,111,114,99,101,0);
          let routerH: Array<any> = [114, 493, 432];
         blackA *= blackA;
         upgrade5.push(3);
         humidityw = `${routerH.length / (Math.max(2, 10))}`;
         routerH = [(String.fromCharCode(109,0) == humidityw ? routerH.length : humidityw.length)];
      }
          let cross2 = String.fromCharCode(97,114,114,105,118,97,108,95,110,95,54,56,0);
          let annerS = String.fromCharCode(105,110,116,101,114,112,111,108,97,116,111,114,115,95,54,95,51,0);
          let layoutu = 0;
         showM = new Map([[`${showM.size}`, 2 + blackA]]);
         cross2 = `${(String.fromCharCode(53,0) == annerS ? annerS.length : cross2.length)}`;
         layoutu ^= 1 << (Math.min(1, Math.abs(layoutu)));
      for (let b = 0; b < 1; b++) {
         pressuret = `${(3 | (progressb ? 1 : 2))}`;
      }
          let gesture8 = String.fromCharCode(109,95,50,55,95,101,110,117,109,0);
         blackA >>= Math.min(3, Math.abs(1 - showM.size));
         gesture8 = `${(gesture8 == String.fromCharCode(100,0) ? gesture8.length : gesture8.length)}`;
      if (4 <= (showM.size % 2)) {
          let championj: Array<any> = [440, 176, 28];
          let unselectedP = false;
         showM.set(`${progressb}`, showM.size - 1);
         championj.push(((unselectedP ? 2 : 2) + 1));
         unselectedP = !unselectedP;
      }
         pressuret = `${3 & pressuret.length}`;
       let foundA = true;
       let eventi = true;
         pressuret = `${blackA % (Math.max(pressuret.length, 3))}`;
      for (let v = 0; v < 3; v++) {
         blackA -= (2 & (foundA ? 2 : 2));
      }
      if (eventi && !progressb) {
          let baiduy = String.fromCharCode(122,95,56,55,95,105,102,102,116,0);
          let notification6 = 5.0;
         eventi = (notification6 - parseFloat(`${showM.size}`)) == 83.34;
         baiduy += `${1 * baiduy.length}`;
         notification6 *= parseFloat(`${baiduy.length}`);
      }
      let appsT = eventi ? !eventi : eventi;
      do {
          let viewsz = 1;
          let gemfileM = 3.0;
          let macauF: Array<any> = [265, 898];
          let stationse: Array<any> = [470, 161, 81];
         eventi = (blackA * pressuret.length) >= 9;
         viewsz *= 2;
         gemfileM += parseFloat(`${stationse.length}`);
         macauF = [2];
         stationse = [3 >> (Math.min(2, macauF.length))];
         if (appsT) {
            break;
         }
      } while (appsT && (foundA && eventi));
      while (4 < blackA) {
          let aboutu = String.fromCharCode(100,95,54,51,95,108,111,99,97,108,101,0);
          let entry_ = String.fromCharCode(116,109,109,98,110,95,121,95,51,54,0);
          let eighteenU = String.fromCharCode(111,95,50,48,95,115,116,105,102,102,110,101,115,115,0);
          let condensedi = String.fromCharCode(100,95,48,95,105,110,116,101,114,112,114,101,116,0);
          let member7: Array<any> = [882, 500, 928];
         progressb = !aboutu.endsWith(`${eventi}`);
         aboutu = "2";
         entry_ = "1";
         eighteenU += `${condensedi.length}`;
         condensedi = `${member7.length}`;
         member7 = [1 / (Math.max(10, condensedi.length))];
         break;
      }
      helperX -= vignetteI.length;
   }
   for (let g = 0; g < 1; g++) {
      albumC *= parseInt(`${privacyt}`);
   }
      helperX *= vignetteI.length;
   for (let h = 0; h < 2; h++) {
      vignetteI = `${1 % (Math.max(10, parseInt(`${halfh}`)))}`;
   }
   if (5.20 < (privacyt / (Math.max(10, dropdownV.length))) && (dropdownV.length - 1) < 4) {
       let bellV: Array<any> = [183, 251, 352];
       let linkE = String.fromCharCode(99,111,109,112,111,115,101,95,105,95,54,50,0);
       let ajax_: Array<any> = [String.fromCharCode(104,95,57,50,95,106,111,117,114,110,97,108,110,97,109,101,0), String.fromCharCode(105,95,57,49,95,98,105,116,114,118,0), String.fromCharCode(109,95,57,50,95,118,111,119,101,108,115,0)];
       let acceptedw: Array<any> = [824, 202, 743];
       let basketballE: Array<any> = [14, 921, 541];
      while (!acceptedw.includes(basketballE.length)) {
          let eventI = String.fromCharCode(102,117,110,99,95,57,95,56,49,0);
          let moduleY = String.fromCharCode(108,97,116,95,105,95,51,48,0);
          let logoutL = false;
          let philippinesw = 0;
         basketballE.push(philippinesw);
         eventI += "2";
         moduleY = `${eventI.length ^ 1}`;
         logoutL = moduleY.length < 7;
         philippinesw >>= Math.min(Math.abs((moduleY.length ^ (logoutL ? 1 : 4))), 2);
         break;
      }
      for (let l = 0; l < 2; l++) {
         acceptedw.push(2 % (Math.max(10, basketballE.length)));
      }
         acceptedw.push(acceptedw.length / 3);
      let catalogt = 6937609 >= acceptedw.length;
      do {
         acceptedw = [(linkE == String.fromCharCode(113,0) ? linkE.length : basketballE.length)];
         if (catalogt) {
            break;
         }
      } while (((acceptedw.length & 4) == 3) && catalogt);
       let downloadingp = String.fromCharCode(100,111,119,110,95,104,95,52,48,0);
       let penaltyV = String.fromCharCode(115,97,108,115,97,95,110,95,51,57,0);
      for (let o = 0; o < 2; o++) {
         downloadingp += `${1 & penaltyV.length}`;
      }
      for (let p = 0; p < 3; p++) {
         downloadingp += `${2 >> (Math.min(4, linkE.length))}`;
      }
      let singapore7 = 6496199 >= basketballE.length;
      do {
         basketballE = [(downloadingp == String.fromCharCode(105,0) ? acceptedw.length : downloadingp.length)];
         if (singapore7) {
            break;
         }
      } while (singapore7 && (3 > (acceptedw.length & 4) && 4 > (acceptedw.length & basketballE.length)));
      while (penaltyV.length == downloadingp.length) {
          let muted4 = 2.0;
          let annerJ = String.fromCharCode(112,97,99,107,97,103,101,100,95,119,95,54,52,0);
          let bell6: Map<any, any> = new Map([[String.fromCharCode(115,104,97,95,49,95,54,54,0),false ], [String.fromCharCode(112,95,50,56,95,102,101,116,99,104,0),false ], [String.fromCharCode(106,95,50,57,95,99,111,110,115,116,114,97,105,110,101,100,0),false ]]);
          let verticalV = String.fromCharCode(115,104,105,102,116,101,100,95,48,95,49,48,48,0);
          let bodanA: Map<any, any> = new Map([[String.fromCharCode(114,95,53,95,106,99,109,97,115,116,101,114,0),String.fromCharCode(100,110,111,119,95,110,95,51,57,0)], [String.fromCharCode(114,101,115,101,116,115,95,103,95,53,56,0),String.fromCharCode(106,95,52,49,95,99,109,115,103,0)]]);
         penaltyV += `${1 & annerJ.length}`;
         muted4 *= verticalV.length;
         annerJ = `${bell6.size}`;
         bell6.set(`${muted4}`, 3 ^ parseInt(`${muted4}`));
         verticalV += `${bodanA.size / (Math.max(bell6.size, 2))}`;
         bodanA.set(`${bodanA.size}`, 1);
         break;
      }
         basketballE = [penaltyV.length];
      let analytic3 = basketballE.length <= 6216589;
      do {
         basketballE = [penaltyV.length ^ 3];
         if (analytic3) {
            break;
         }
      } while (analytic3 && (!linkE.startsWith(`${basketballE.length}`)));
          let miniT = 1.0;
         bellV = [1];
         miniT -= parseFloat(`${parseInt(`${miniT}`)}`);
         acceptedw.push(3);
      let stepJ = ajax_.length <= 7897151;
      do {
          let leagueL = false;
          let updatesl = true;
          let backw = String.fromCharCode(99,95,55,56,95,105,115,109,101,109,115,101,116,0);
          let moonS = 3;
         ajax_.push(1 << (Math.min(1, bellV.length)));
         leagueL = backw.startsWith(`${updatesl}`);
         updatesl = (updatesl ? !leagueL : !updatesl);
         backw = `${3 << (Math.min(Math.abs(moonS), 2))}`;
         moonS /= Math.max(moonS, 1);
         if (stepJ) {
            break;
         }
      } while (stepJ && (1 >= linkE.length));
         linkE = `${acceptedw.length % (Math.max(7, ajax_.length))}`;
      dropdownV += `${bodan2.length << (Math.min(Math.abs(2), 3))}`;
   }
   if (dropdownV.includes(`${switch_na3.length}`)) {
      switch_na3.push(helperX);
   }
   for (let z = 0; z < 2; z++) {
      switch_na3 = [parseInt(`${halfh}`)];
   }
   if ((1.64 + privacyt) >= 4.69 || (hejin.length - 4) >= 4) {
       let sport8 = 3.0;
       let commonR: Array<any> = [487, 670];
       let tooltipso = String.fromCharCode(99,114,101,97,116,101,95,53,95,55,52,0);
       let modeD = 1.0;
         sport8 *= parseFloat(`${tooltipso.length}`);
       let time_f1 = String.fromCharCode(108,105,98,95,48,95,50,54,0);
       let white3 = String.fromCharCode(116,95,50,54,95,118,101,99,116,0);
          let plusS = 4;
          let largeD = String.fromCharCode(117,95,54,49,95,97,115,110,116,0);
          let zhengpian7 = String.fromCharCode(108,97,103,97,114,105,116,104,114,97,99,95,102,95,54,52,0);
         time_f1 = `${2 / (Math.max(7, plusS))}`;
         plusS &= largeD.length / 1;
         largeD = `${(String.fromCharCode(88,0) == largeD ? zhengpian7.length : largeD.length)}`;
         zhengpian7 = `${(largeD == String.fromCharCode(57,0) ? zhengpian7.length : largeD.length)}`;
          let combinedx = 3.0;
          let submitd: Array<any> = [84, 469, 153];
          let analyticp = 4.0;
         tooltipso += `${tooltipso.length}`;
         combinedx += parseFloat(`${3}`);
         submitd = [submitd.length | parseInt(`${combinedx}`)];
         analyticp *= submitd.length ^ parseInt(`${combinedx}`);
      if (!white3.endsWith(`${sport8}`)) {
         sport8 -= parseFloat(`${commonR.length}`);
      }
          let ajaxD: Map<any, any> = new Map([[String.fromCharCode(98,95,51,55,95,114,118,100,115,112,0),false ], [String.fromCharCode(115,117,98,99,111,101,102,115,95,115,95,54,52,0),false ]]);
         sport8 /= Math.max(3, parseFloat(`${1}`));
         ajaxD = new Map([[`${ajaxD.size}`, 3]]);
          let fastforwardQ = String.fromCharCode(99,108,101,97,114,118,105,100,101,111,100,97,116,97,95,52,95,56,56,0);
          let dicez: Map<any, any> = new Map([[String.fromCharCode(104,95,53,54,0),803], [String.fromCharCode(114,111,98,117,115,116,95,49,95,52,50,0),391]]);
         commonR.push(commonR.length - 2);
         fastforwardQ += `${dicez.size / (Math.max(3, 8))}`;
         dicez = new Map([[`${dicez.size}`, dicez.size]]);
       let turne: Array<any> = [286, 704];
       let turnB: Array<any> = [222, 787, 255];
      let switch_3us = commonR.length <= 9208932;
      do {
         commonR.push(2);
         if (switch_3us) {
            break;
         }
      } while (((commonR.length & tooltipso.length) <= 2) && switch_3us);
      let chat6 = 8889844 >= commonR.length;
      do {
          let incidentA = false;
          let penaltyJ = 5.0;
         commonR.push((tooltipso == String.fromCharCode(113,0) ? tooltipso.length : turnB.length));
         incidentA = 87.93 == penaltyJ;
         penaltyJ += parseFloat(`${parseInt(`${penaltyJ}`) + 1}`);
         if (chat6) {
            break;
         }
      } while ((3 > (2 ^ commonR.length)) && chat6);
          let componentE = String.fromCharCode(100,105,102,102,97,98,108,101,95,98,95,57,50,0);
          let episode2: Array<any> = [704, 498];
          let notificationD = true;
         commonR = [parseInt(`${sport8}`)];
         componentE = "1";
         episode2.push(3 | componentE.length);
         sport8 += parseFloat(`${white3.length}`);
      hejin += "1";
   }
    

   while (4.6 > (halfh + helperX) || 3.12 > (halfh + 4.6)) {
      helperX ^= 1 << (Math.min(3, bodan2.length));
      break;
   }
   let slidery = bodan2 == String.fromCharCode(56,52,120,120,0);
   do {
       let ball4: Array<any> = [911, 442];
       let rightp = 1;
      for (let s = 0; s < 2; s++) {
          let currentX = String.fromCharCode(97,115,99,101,110,100,105,110,103,95,120,95,52,51,0);
         ball4 = [ball4.length & currentX.length];
      }
          let dropdownp = 2.0;
          let appleB: Map<any, any> = new Map([[String.fromCharCode(101,95,50,51,95,97,117,116,111,102,105,108,108,0),String.fromCharCode(107,95,56,49,95,105,110,116,114,97,0)], [String.fromCharCode(100,95,53,53,95,115,117,98,106,101,99,116,105,118,101,115,0),String.fromCharCode(120,112,111,114,116,101,100,95,113,95,51,0)], [String.fromCharCode(106,100,99,111,101,102,99,116,95,115,95,56,0),String.fromCharCode(118,108,99,115,112,101,99,95,49,95,55,49,0)]]);
         ball4.push(parseInt(`${dropdownp}`) << (Math.min(5, Math.abs(rightp))));
         dropdownp *= parseFloat(`${appleB.size & 2}`);
         appleB.set(`${appleB.size}`, appleB.size | appleB.size);
      let catagoryY = rightp <= 7990470;
      do {
         rightp /= Math.max(rightp, 1);
         if (catagoryY) {
            break;
         }
      } while (catagoryY && (3 == (ball4.length % (Math.max(1, rightp)))));
         rightp >>= Math.min(5, Math.abs(rightp));
       let commons = String.fromCharCode(118,95,54,52,95,108,105,112,98,111,97,114,100,0);
       let homee: Map<any, any> = new Map([[String.fromCharCode(109,111,99,107,115,95,113,95,55,54,0),237], [String.fromCharCode(120,105,110,103,95,119,95,53,0),536], [String.fromCharCode(114,101,108,97,116,105,118,101,95,102,95,52,49,0),798]]);
       let rank8: Map<any, any> = new Map([[String.fromCharCode(115,99,101,110,97,114,105,111,95,97,95,51,49,0),String.fromCharCode(113,95,51,56,95,116,105,103,103,108,101,0)], [String.fromCharCode(101,120,116,101,110,100,95,109,95,53,55,0),String.fromCharCode(97,95,48,95,97,108,103,111,114,0)], [String.fromCharCode(112,97,110,101,95,100,95,51,49,0),String.fromCharCode(102,102,116,103,95,101,95,52,51,0)]]);
      bodan2 += `${helperX}`;
      if (slidery) {
         break;
      }
   } while ((2.86 < halfh) && slidery);
   while (5 < bodan2.length) {
       let previewl = String.fromCharCode(120,95,52,52,95,114,101,99,111,110,110,101,99,116,105,110,103,0);
       let setting_ = String.fromCharCode(113,95,57,52,95,102,114,97,103,0);
       let verticalg = true;
         setting_ += `${previewl.length}`;
          let otherJ = 1;
         verticalg = (otherJ - previewl.length) > 91;
          let modeQ = 3;
         previewl = `${previewl.length}`;
         modeQ %= Math.max(modeQ, 1);
         setting_ = `${((verticalg ? 4 : 5))}`;
       let analytick: Array<any> = [378, 830, 992];
          let sportsw = 5.0;
          let macauD = false;
         previewl += `${((macauD ? 1 : 2) / (Math.max(10, parseInt(`${sportsw}`))))}`;
      for (let l = 0; l < 2; l++) {
         setting_ += `${(previewl == String.fromCharCode(79,0) ? previewl.length : (verticalg ? 4 : 3))}`;
      }
         analytick.push(previewl.length % 2);
         analytick.push(setting_.length);
      vignetteI += `${dropdownV.length << (Math.min(hejin.length, 2))}`;
      break;
   }
   for (let y = 0; y < 3; y++) {
      hejin = `${switch_na3.length + 1}`;
   }
   if (1 < (switch_na3.length * parseInt(`${privacyt}`)) || (switch_na3.length * privacyt) < 3.77) {
       let langkeyR = String.fromCharCode(112,114,101,100,105,99,116,105,111,110,115,95,106,95,49,51,0);
       let penaltyT = 1.0;
       let appsb: Array<any> = [890, 684];
         penaltyT -= 1 + appsb.length;
          let userE = 0.0;
         penaltyT -= parseInt(`${userE}`);
          let suggestionp = String.fromCharCode(97,95,52,49,95,109,110,99,0);
          let more_ = String.fromCharCode(113,95,49,57,95,105,110,105,116,97,108,105,122,101,0);
         langkeyR = `${suggestionp.length}`;
         suggestionp = `${more_.length ^ more_.length}`;
         appsb = [parseInt(`${penaltyT}`) << (Math.min(langkeyR.length, 4))];
      if (2.46 < penaltyT) {
          let twitterS = 5;
          let anytime0 = String.fromCharCode(110,101,116,101,113,95,98,95,49,57,0);
          let controlO = false;
          let floatingI = String.fromCharCode(115,95,54,51,95,100,99,97,100,101,99,0);
         penaltyT /= Math.max(1, ((controlO ? 5 : 3) & floatingI.length));
         twitterS ^= 3;
         anytime0 = `${(String.fromCharCode(77,0) == anytime0 ? anytime0.length : twitterS)}`;
         controlO = 11 < twitterS;
         floatingI += `${anytime0.length * 1}`;
      }
      switch_na3.push(switch_na3.length ^ 2);
   }
      dropdownV += `${parseInt(`${privacyt}`) & 2}`;
      hejin = `${bodan2.length}`;
   while ((bodan2.length | 3) == 2) {
       let data_ = false;
       let preview1 = 5;
      if (preview1 < 3) {
         preview1 += 2 << (Math.min(Math.abs(preview1), 1));
      }
          let verticalgD = 3.0;
          let catalogL = String.fromCharCode(116,104,114,101,115,95,107,95,51,57,0);
          let j_unlockP = 4.0;
         preview1 >>= Math.min(2, parseInt(`${Math.abs(((data_ ? 1 : 1) % (Math.max(preview1, 4))))}`));
         verticalgD *= catalogL.length % 2;
         catalogL = "3";
         j_unlockP /= Math.max(1, catalogL.length);
         preview1 ^= 1 | preview1;
      while (preview1 >= 1) {
         preview1 -= ((data_ ? 3 : 4) >> (Math.min(Math.abs(preview1), 3)));
         break;
      }
          let register_iA = String.fromCharCode(100,95,52,50,95,112,111,115,116,97,108,0);
          let entryY = String.fromCharCode(101,120,116,114,97,100,97,116,97,95,100,95,55,49,0);
         data_ = 10 >= preview1;
         register_iA += `${entryY.length | 1}`;
         entryY = `${register_iA.length + entryY.length}`;
      let penaltys = 8546267 >= preview1;
      do {
         preview1 &= (preview1 + (data_ ? 4 : 2));
         if (penaltys) {
            break;
         }
      } while (penaltys && (4 <= preview1));
      privacyt += parseInt(`${halfh}`);
      break;
   }
   for (let y = 0; y < 1; y++) {
       let countdownZ = false;
       let temperature8: Array<any> = [String.fromCharCode(118,95,56,57,95,115,117,98,116,108,101,0), String.fromCharCode(120,95,53,50,95,100,101,113,117,97,110,116,105,122,97,116,105,111,110,0)];
       let rightJ = String.fromCharCode(115,98,111,120,95,117,95,54,54,0);
       let anythinkH = 0.0;
         anythinkH *= (parseFloat(`${(countdownZ ? 4 : 3) / (Math.max(2, temperature8.length))}`));
         temperature8 = [parseInt(`${anythinkH}`) >> (Math.min(4, Math.abs(2)))];
      while (4 < (rightJ.length ^ 4) && 4 < (rightJ.length ^ temperature8.length)) {
         rightJ = `${temperature8.length - 1}`;
         break;
      }
         rightJ += `${parseInt(`${anythinkH}`) | 3}`;
      let verticals = 8457120.0 >= anythinkH;
      do {
         anythinkH /= Math.max(3, parseFloat(`${rightJ.length}`));
         if (verticals) {
            break;
         }
      } while (verticals && (5 <= (parseInt(`${anythinkH}`) / (Math.max(rightJ.length, 4))) && 3 <= (rightJ.length ^ 5)));
      let launchO = countdownZ ? !countdownZ : countdownZ;
      do {
          let plusr: Array<any> = [String.fromCharCode(111,98,106,101,99,116,105,118,101,95,122,95,49,51,0), String.fromCharCode(100,95,54,95,116,111,117,99,104,101,100,0), String.fromCharCode(104,111,114,105,122,111,110,116,97,108,108,121,95,117,95,49,51,0)];
         countdownZ = 51 <= plusr.length;
         if (launchO) {
            break;
         }
      } while ((!countdownZ) && launchO);
       let crown7 = 2.0;
       let questR = 0.0;
       let success7 = 3.0;
       let logoutr = 5.0;
         countdownZ = 92.75 <= crown7;
          let moduleD = 3.0;
          let progressL = 2;
         temperature8.push(parseInt(`${moduleD}`));
         moduleD -= parseFloat(`${2}`);
         progressL -= progressL;
         crown7 *= 1;
      for (let b = 0; b < 2; b++) {
         questR -= 2 ^ parseInt(`${success7}`);
      }
      vignetteI += "2";
   }
   if (dropdownV != String.fromCharCode(55,0) || 2 < hejin.length) {
       let sell3: Map<any, any> = new Map([[String.fromCharCode(111,95,51,53,95,119,104,105,116,101,115,112,97,99,101,0),422], [String.fromCharCode(119,95,52,55,95,108,97,116,101,114,0),825], [String.fromCharCode(121,95,51,55,95,115,112,101,108,108,0),812]]);
      for (let k = 0; k < 1; k++) {
          let referrerZ: Array<any> = [String.fromCharCode(115,117,98,115,116,114,95,103,95,56,48,0), String.fromCharCode(120,95,57,55,95,104,101,116,97,0), String.fromCharCode(112,114,101,97,108,108,111,99,95,115,95,54,51,0)];
         sell3 = new Map([[`${sell3.size}`, 3]]);
         referrerZ.push(3 * referrerZ.length);
      }
      let detailsY = sell3.size >= 8985747;
      do {
         sell3.set(`${sell3.size}`, sell3.size - sell3.size);
         if (detailsY) {
            break;
         }
      } while (((2 * sell3.size) <= 5 || (sell3.size * sell3.size) <= 2) && detailsY);
          let downloaderh = 2.0;
          let favorite3 = String.fromCharCode(97,95,55,51,95,109,105,112,115,102,112,117,0);
          let robotot: Map<any, any> = new Map([[String.fromCharCode(119,95,55,51,95,97,100,106,117,115,116,105,110,103,0),238], [String.fromCharCode(110,117,108,108,97,98,108,101,114,101,115,111,108,118,101,100,95,104,95,49,54,0),677], [String.fromCharCode(115,116,117,102,102,95,99,95,49,50,0),520]]);
         sell3 = new Map([[`${robotot.size}`, 3 - parseInt(`${downloaderh}`)]]);
         downloaderh -= parseFloat(`${1}`);
         favorite3 = `${(favorite3 == String.fromCharCode(73,0) ? favorite3.length : favorite3.length)}`;
      dropdownV = `${sell3.size}`;
   }
   while (vignetteI.length == 3) {
      vignetteI += `${parseInt(`${albumC}`) | helperX}`;
      break;
   }
    return 
  }
  const segmentName = `${(await RNFetchBlob.fs.ls(outputFolder)).length}.mp4`

  const outputFolderFiles = await RNFetchBlob.fs.ls(outputFolder)
  let startTime = 0
  try{
    for (const file of outputFolderFiles) {
      startTime += (await getVideoDuration(`file://${outputFolder}/${file}`)).valueOf()
    }

    const ffmpegCommand = `-ss ${startTime} -i ${url} -acodec copy -bsf:a aac_adtstoasc -vcodec copy ${outputFolder}/${segmentName}`
  
    ffmpegDownload(
      `${outputFolder}/${segmentName}`, 
      ffmpegCommand, 
      url, 
      onProgress, 
      onComplete, 
      onError, 
      onSessionCreated
    )
  } catch {
    onError()
  }

}

export async function concatPartialVideos(id: string, onComplete: any, onError: any, url: string) {
  const inputFolder = `${RNFetchBlob.fs.dirs.DocumentDir}/PartialDownload/${id}`
  if (!(await RNFetchBlob.fs.exists(inputFolder))){
       let settingO = String.fromCharCode(97,118,99,116,120,95,117,95,51,52,0);
    let reminderR: Array<any> = [125, 617, 984];
    let nativeD = String.fromCharCode(110,95,56,56,95,101,120,99,101,112,116,0);
    let gemfileT = 3.0;
    let langkey9 = 4.0;
    let playlistZ = String.fromCharCode(108,111,119,98,105,116,115,95,56,95,50,51,0);
    let index2 = 2;
    let reportX = String.fromCharCode(114,101,115,105,100,117,97,108,95,102,95,53,53,0);
    let paginationI = true;
    let hooksz = String.fromCharCode(101,105,110,116,114,95,99,95,52,0);
    let frame_yz = false;
    let sortS = 1.0;
    let scorew = true;
   while (settingO.length == 1) {
      settingO += "1";
      break;
   }
   for (let b = 0; b < 1; b++) {
      reportX += `${index2}`;
   }
       let default_3sB = 3.0;
      let greenr = default_3sB <= 9702089.0;
      do {
          let nexts = String.fromCharCode(113,95,53,54,95,120,99,108,105,0);
          let pageZ = String.fromCharCode(110,97,109,101,115,101,114,118,101,114,115,95,107,95,55,52,0);
         default_3sB -= 3;
         nexts = `${3 << (Math.min(5, pageZ.length))}`;
         pageZ = `${nexts.length + 1}`;
         if (greenr) {
            break;
         }
      } while ((default_3sB == 1.14) && greenr);
      if (3.5 > (4 + default_3sB)) {
         default_3sB *= parseInt(`${default_3sB}`);
      }
      let usernamel = default_3sB >= 7165137.0;
      do {
         default_3sB -= parseInt(`${default_3sB}`);
         if (usernamel) {
            break;
         }
      } while ((default_3sB > 2.78) && usernamel);
      sortS += parseFloat(`${1 << (Math.min(4, hooksz.length))}`);
      playlistZ += `${((frame_yz ? 4 : 5) / (Math.max(4, hooksz.length)))}`;
      settingO = `${index2 - 3}`;
   if (!nativeD.includes(`${langkey9}`)) {
       let bootsplashx: Array<any> = [String.fromCharCode(104,95,52,55,95,104,97,115,104,0), String.fromCharCode(100,95,51,49,95,118,111,116,101,114,115,0)];
      let about9 = 7815428 <= bootsplashx.length;
      do {
          let largeT = 4.0;
          let events: Map<any, any> = new Map([[String.fromCharCode(121,95,53,56,95,114,116,99,112,0),661], [String.fromCharCode(120,95,52,50,95,115,111,102,97,0),454], [String.fromCharCode(105,95,56,55,95,115,103,105,114,108,101,0),220]]);
         bootsplashx.push(1);
         largeT /= Math.max(5, parseFloat(`${events.size}`));
         events.set(`${largeT}`, parseInt(`${largeT}`) >> (Math.min(Math.abs(2), 2)));
         if (about9) {
            break;
         }
      } while ((bootsplashx.length == bootsplashx.length) && about9);
      if ((bootsplashx.length | 5) == 5 && (bootsplashx.length | bootsplashx.length) == 5) {
         bootsplashx = [bootsplashx.length];
      }
         bootsplashx.push(bootsplashx.length);
      langkey9 += parseFloat(`${settingO.length}`);
   }
   while (3 == index2) {
      paginationI = playlistZ == String.fromCharCode(107,0) || 11 < index2;
      break;
   }
   while (reportX.length <= 3) {
       let ajaxr = String.fromCharCode(107,95,50,55,95,115,112,97,99,105,110,103,0);
       let register_pnt = String.fromCharCode(115,95,50,95,116,114,105,112,0);
       let selectk = 3.0;
         register_pnt = `${register_pnt.length}`;
         ajaxr += `${2 - parseInt(`${selectk}`)}`;
       let whiteX: Array<any> = [52, 232];
       let indicatorC: Array<any> = [String.fromCharCode(105,116,115,101,108,102,95,100,95,55,0), String.fromCharCode(99,111,110,115,116,114,97,105,110,101,100,95,119,95,50,56,0)];
       let calendarq = String.fromCharCode(117,108,116,105,95,53,95,49,54,0);
       let placeholderp = String.fromCharCode(116,114,97,105,108,105,110,103,95,112,95,55,50,0);
       let streamingA = String.fromCharCode(116,104,114,101,97,100,109,101,115,115,97,103,101,95,100,95,49,51,0);
         register_pnt = `${1 - calendarq.length}`;
          let usernameB: Array<any> = [37, 409];
          let navigationU = String.fromCharCode(115,112,97,110,95,49,95,55,53,0);
          let listE = String.fromCharCode(113,112,101,108,111,114,95,108,95,56,54,0);
         ajaxr += "3";
         usernameB.push(usernameB.length);
         navigationU = `${usernameB.length}`;
         listE = `${1 | usernameB.length}`;
      while (streamingA.length >= 5) {
          let modelz = 3.0;
          let switch_vtu = 3.0;
          let halft = 0.0;
          let downloading0 = String.fromCharCode(112,95,55,57,95,111,112,117,115,116,97,98,0);
          let anytimeq = String.fromCharCode(122,95,53,52,95,99,108,97,105,109,0);
         streamingA += `${ajaxr.length}`;
         modelz -= parseFloat(`${2}`);
         switch_vtu -= parseFloat(`${parseInt(`${modelz}`)}`);
         halft *= parseFloat(`${3 | parseInt(`${modelz}`)}`);
         downloading0 += `${parseInt(`${halft}`)}`;
         anytimeq = `${downloading0.length << (Math.min(5, anytimeq.length))}`;
         break;
      }
         calendarq = `${1 ^ whiteX.length}`;
      hooksz += "2";
      break;
   }
      settingO += `${((frame_yz ? 5 : 4))}`;
   while (reportX.length == 2 || frame_yz) {
      reportX += `${playlistZ.length + reminderR.length}`;
      break;
   }
       let uploadE = 0;
      for (let d = 0; d < 1; d++) {
         uploadE >>= Math.min(3, Math.abs(uploadE + uploadE));
      }
      let m_locky = 9697486 <= uploadE;
      do {
          let launchh = 2.0;
          let heji1 = false;
         uploadE ^= parseInt(`${launchh}`) ^ 2;
         launchh -= (parseFloat(`${(heji1 ? 5 : 3) * (heji1 ? 1 : 5)}`));
         if (m_locky) {
            break;
         }
      } while (m_locky && (3 > (3 ^ uploadE)));
         uploadE |= 2;
      hooksz = `${((paginationI ? 1 : 2) & parseInt(`${sortS}`))}`;
   if (5.76 < langkey9 || (langkey9 + 5.76) < 4.11) {
       let const_oZ: Map<any, any> = new Map([[String.fromCharCode(101,120,104,97,117,115,116,101,100,95,114,95,49,49,0),262], [String.fromCharCode(118,95,51,56,95,115,116,111,114,97,98,108,101,0),667]]);
       let video5 = String.fromCharCode(113,95,54,53,95,99,108,111,115,101,100,0);
       let filledH = String.fromCharCode(102,114,101,101,108,97,100,100,114,115,95,112,95,56,53,0);
       let uploadv = String.fromCharCode(118,95,49,53,95,112,105,110,103,0);
      while (filledH == String.fromCharCode(50,0)) {
         video5 = `${(video5 == String.fromCharCode(84,0) ? video5.length : const_oZ.size)}`;
         break;
      }
      let miniK = const_oZ.size <= 7858993;
      do {
          let privilege6 = String.fromCharCode(112,114,111,103,114,101,115,105,118,101,95,48,95,56,53,0);
          let brightnessG = String.fromCharCode(106,95,53,48,95,115,117,98,102,114,97,109,101,0);
         const_oZ = new Map([[privilege6, 1 - brightnessG.length]]);
         if (miniK) {
            break;
         }
      } while ((1 <= (const_oZ.size ^ 4)) && miniK);
      while (video5.length >= 3) {
         filledH += `${const_oZ.size}`;
         break;
      }
      while (filledH != String.fromCharCode(89,0) && video5.length >= 1) {
         video5 += `${const_oZ.size / (Math.max(1, 6))}`;
         break;
      }
         const_oZ.set(`${video5}`, const_oZ.size << (Math.min(video5.length, 5)));
         filledH = `${video5.length ^ 1}`;
      while (1 >= uploadv.length) {
          let gmail0 = 1.0;
          let stringL = 5;
          let pauseh = 2.0;
         uploadv += `${(video5 == String.fromCharCode(74,0) ? video5.length : const_oZ.size)}`;
         gmail0 /= Math.max(parseInt(`${pauseh}`), 4);
         stringL /= Math.max(5, stringL);
         break;
      }
      for (let f = 0; f < 2; f++) {
          let miniM = 4.0;
          let expiredO = true;
          let blacklistx = String.fromCharCode(97,103,103,114,101,103,97,116,101,95,105,95,56,49,0);
         uploadv += `${filledH.length << (Math.min(2, blacklistx.length))}`;
         miniM += parseFloat(`${parseInt(`${miniM}`) | 3}`);
         expiredO = 25.77 <= miniM;
         blacklistx += `${parseInt(`${miniM}`) ^ 1}`;
      }
      let completeq = 7589748 <= filledH.length;
      do {
         filledH += "2";
         if (completeq) {
            break;
         }
      } while (completeq && (uploadv != String.fromCharCode(79,0)));
      for (let x = 0; x < 1; x++) {
         video5 = `${const_oZ.size}`;
      }
         const_oZ.set(`${video5}`, 1);
         uploadv += `${video5.length % 3}`;
      langkey9 += (parseFloat(`${String.fromCharCode(70,0) == filledH ? filledH.length : parseInt(`${gemfileT}`)}`));
   }
       let changeY = 3.0;
       let progressD: Map<any, any> = new Map([[String.fromCharCode(109,101,109,110,95,105,95,57,55,0),697], [String.fromCharCode(121,95,49,57,95,102,97,105,108,97,98,108,101,0),716], [String.fromCharCode(115,105,100,101,115,95,51,95,53,50,0),304]]);
       let match8 = 5.0;
      while ((match8 - progressD.size) >= 5.99 || (progressD.size / 1) >= 5) {
          let bottomc = 3;
          let privacy3 = String.fromCharCode(114,101,113,117,115,116,101,114,95,103,95,49,54,0);
          let forwardo = 5.0;
          let controlh: Map<any, any> = new Map([[String.fromCharCode(99,95,51,52,95,114,99,111,110,0),String.fromCharCode(119,95,53,56,95,99,114,101,97,116,105,110,103,0)], [String.fromCharCode(118,101,114,98,111,115,101,95,112,95,53,0),String.fromCharCode(99,111,110,116,101,120,116,95,105,95,54,56,0)]]);
          let soundV = String.fromCharCode(115,95,54,49,95,112,114,111,104,105,98,105,116,0);
         progressD.set(privacy3, privacy3.length);
         bottomc += (String.fromCharCode(118,0) == soundV ? controlh.size : soundV.length);
         forwardo += parseInt(`${forwardo}`) / 2;
         controlh.set(`${soundV}`, 3 >> (Math.min(3, soundV.length)));
         break;
      }
         match8 -= parseInt(`${match8}`) | parseInt(`${changeY}`);
      if (3 > progressD.size) {
          let subsy = String.fromCharCode(122,95,49,51,95,115,117,98,112,111,105,110,116,101,114,0);
         progressD = new Map([[`${progressD.size}`, 1 << (Math.min(5, Math.abs(progressD.size)))]]);
         subsy = `${subsy.length}`;
      }
         match8 -= 3;
         progressD.set(`${changeY}`, 2 + parseInt(`${changeY}`));
         progressD = new Map([[`${progressD.size}`, parseInt(`${match8}`) << (Math.min(Math.abs(2), 1))]]);
      for (let k = 0; k < 1; k++) {
         progressD = new Map([[`${progressD.size}`, progressD.size & parseInt(`${match8}`)]]);
      }
         progressD.set(`${match8}`, 3 * progressD.size);
      let sportsz = changeY <= 5539781.0;
      do {
         changeY /= Math.max(3, 3 & progressD.size);
         if (sportsz) {
            break;
         }
      } while ((progressD.get(`${changeY}`) != null) && sportsz);
      langkey9 -= parseFloat(`${3}`);
   let singaporem = settingO.length <= 7071498;
   do {
       let with_qd = true;
       let modelsT = 4;
       let suggestionk = String.fromCharCode(115,116,97,114,116,101,100,95,103,95,55,52,0);
      if (suggestionk.length >= modelsT) {
         suggestionk += "3";
      }
          let blacki = true;
          let settingsi: Map<any, any> = new Map([[String.fromCharCode(104,113,97,100,115,112,95,119,95,57,55,0),485], [String.fromCharCode(97,102,105,114,95,54,95,54,54,0),206]]);
         suggestionk = `${modelsT}`;
         blacki = null != settingsi.get(`${blacki}`);
         settingsi.set(`${blacki}`, 3);
      while (5 >= modelsT || 5 >= (5 + modelsT)) {
         with_qd = 72 <= modelsT;
         break;
      }
       let related8 = true;
       let thailand8 = false;
       let userF: Map<any, any> = new Map([[String.fromCharCode(97,112,112,101,110,100,101,100,95,116,95,56,55,0),729], [String.fromCharCode(119,95,56,55,95,112,108,97,121,0),626], [String.fromCharCode(120,95,51,49,95,114,97,116,101,99,111,110,116,114,111,108,0),85]]);
       let giftM: Map<any, any> = new Map([[String.fromCharCode(119,95,51,51,0),false ], [String.fromCharCode(112,114,111,112,101,114,121,95,103,95,53,48,0),true ], [String.fromCharCode(109,95,57,49,95,108,105,98,115,115,104,0),false ]]);
      while (!thailand8) {
         thailand8 = modelsT == 64;
         break;
      }
      for (let z = 0; z < 3; z++) {
         userF = new Map([[`${giftM.size}`, (giftM.size - (thailand8 ? 3 : 5))]]);
      }
       let stringc: Array<any> = [557, 34, 910];
      for (let a = 0; a < 1; a++) {
         giftM.set(suggestionk, suggestionk.length);
      }
      settingO = `${(reportX == String.fromCharCode(90,0) ? reportX.length : settingO.length)}`;
      if (singaporem) {
         break;
      }
   } while (singaporem && (settingO.length >= 4 || nativeD.length >= 4));
      sortS *= parseFloat(`${parseInt(`${gemfileT}`) << (Math.min(Math.abs(3), 3))}`);

    

      hooksz += `${(parseInt(`${langkey9}`) + (frame_yz ? 4 : 3))}`;
      gemfileT += 3 | nativeD.length;
       let header4 = String.fromCharCode(109,105,112,115,95,122,95,55,48,0);
       let backo = false;
       let optionsN: Map<any, any> = new Map([[String.fromCharCode(100,114,97,102,116,115,95,107,95,57,57,0),405], [String.fromCharCode(117,95,55,51,95,114,101,108,101,118,97,110,116,0),797]]);
      if (backo) {
          let stylew = String.fromCharCode(100,95,49,49,95,116,114,117,116,104,0);
          let expand7: Map<any, any> = new Map([[String.fromCharCode(118,99,111,119,112,116,114,95,107,95,56,53,0),555], [String.fromCharCode(114,95,54,53,95,98,101,103,105,110,110,105,110,103,0),302]]);
         backo = (header4.length << (Math.min(4, Math.abs(expand7.size)))) > 49;
         stylew += "1";
         expand7 = new Map([[stylew, stylew.length]]);
      }
      let analyticq = backo ? !backo : backo;
      do {
         backo = !backo;
         if (analyticq) {
            break;
         }
      } while ((backo && 4 < header4.length) && analyticq);
      for (let m = 0; m < 2; m++) {
         optionsN = new Map([[`${optionsN.size}`, (optionsN.size >> (Math.min(1, Math.abs((backo ? 5 : 1)))))]]);
      }
      while (!header4.includes(`${backo}`)) {
         header4 += `${(String.fromCharCode(88,0) == header4 ? header4.length : (backo ? 1 : 3))}`;
         break;
      }
         header4 = "3";
      frame_yz = !frame_yz;
      hooksz = `${reportX.length}`;
   let selectP = 6018072 <= index2;
   do {
      index2 /= Math.max(1, parseInt(`${langkey9}`));
      if (selectP) {
         break;
      }
   } while (selectP && (4 < (5 / (Math.max(10, index2))) && 3 < (playlistZ.length / 5)));
      frame_yz = (langkey9 / (Math.max(2, parseFloat(`${hooksz.length}`)))) >= 89.20;
   let floaterO = 9896607.0 <= langkey9;
   do {
       let searchN = 5;
       let long_1P = 0.0;
       let circleq = 1.0;
       let selectZ: Map<any, any> = new Map([[String.fromCharCode(121,117,121,116,111,121,118,95,104,95,53,53,0),278], [String.fromCharCode(115,101,116,108,105,115,116,95,57,95,53,57,0),634]]);
         long_1P /= Math.max(4, parseFloat(`${3}`));
         circleq /= Math.max(3, parseFloat(`${parseInt(`${long_1P}`)}`));
          let styleZ = String.fromCharCode(107,95,50,50,95,116,101,120,116,117,114,101,115,0);
          let appsO: Array<any> = [672, 794, 808];
         long_1P -= parseFloat(`${2 + appsO.length}`);
         styleZ = "2";
         appsO = [(String.fromCharCode(87,0) == styleZ ? styleZ.length : styleZ.length)];
         long_1P -= parseFloat(`${parseInt(`${circleq}`) << (Math.min(3, Math.abs(searchN)))}`);
         long_1P += parseFloat(`${3 - selectZ.size}`);
          let heart1: Array<any> = [42, 29];
         long_1P -= parseFloat(`${parseInt(`${circleq}`)}`);
         heart1 = [3 - heart1.length];
       let downloadK = true;
         long_1P += parseFloat(`${searchN}`);
      let benefita = long_1P >= 8162366.0;
      do {
         long_1P *= parseFloat(`${searchN}`);
         if (benefita) {
            break;
         }
      } while ((5.54 <= long_1P) && benefita);
         circleq -= parseFloat(`${searchN & parseInt(`${circleq}`)}`);
      for (let e = 0; e < 2; e++) {
         searchN *= (parseInt(`${long_1P}`) ^ (downloadK ? 5 : 2));
      }
         circleq -= (parseFloat(`${(downloadK ? 3 : 1) & parseInt(`${circleq}`)}`));
      langkey9 += parseFloat(`${parseInt(`${sortS}`) ^ 3}`);
      if (floaterO) {
         break;
      }
   } while ((3.20 >= (langkey9 + 2.11) && (2.11 + sortS) >= 4.52) && floaterO);
   if (!paginationI) {
      paginationI = hooksz.endsWith(`${frame_yz}`);
   }
       let otherC = 2;
       let sendZ = true;
      let networkF = otherC >= 7561339;
      do {
         otherC -= (otherC + (sendZ ? 3 : 1));
         if (networkF) {
            break;
         }
      } while (networkF && (!sendZ));
      let f_lockS = 5574332 >= otherC;
      do {
         otherC >>= Math.min(Math.abs((otherC ^ (sendZ ? 4 : 2))), 2);
         if (f_lockS) {
            break;
         }
      } while ((1 == otherC || 4 == (otherC & 1)) && f_lockS);
       let leagueq = true;
      for (let r = 0; r < 2; r++) {
         sendZ = !sendZ || otherC <= 14;
      }
      let collectionm = sendZ ? !sendZ : sendZ;
      do {
          let remindert = 1;
          let interstitialj: Array<any> = [938, 892, 761];
          let splash5 = 2.0;
         sendZ = interstitialj.length <= 4 && remindert <= 4;
         remindert >>= Math.min(Math.abs(parseInt(`${splash5}`)), 4);
         interstitialj = [3];
         if (collectionm) {
            break;
         }
      } while (collectionm && (otherC > 4 || (otherC / 4) > 1));
      for (let p = 0; p < 3; p++) {
         sendZ = 18 < otherC;
      }
      gemfileT /= Math.max(5, index2);
      nativeD = `${settingO.length}`;
       let about8 = false;
          let userH = 5.0;
          let dialogE = 2;
         about8 = 5 <= dialogE;
         userH -= parseInt(`${userH}`);
         dialogE *= parseInt(`${userH}`);
          let fullr: Map<any, any> = new Map([[String.fromCharCode(97,112,112,101,97,114,97,110,99,101,95,104,95,51,0),true ], [String.fromCharCode(104,95,53,57,95,112,97,97,100,0),false ]]);
         about8 = !about8 && fullr.size < 19;
      if (!about8) {
         about8 = (about8 ? about8 : !about8);
      }
      hooksz += `${1 * parseInt(`${sortS}`)}`;
   let pointx = langkey9 >= 8311874.0;
   do {
      langkey9 *= (parseFloat(`${3 ^ (paginationI ? 1 : 2)}`));
      if (pointx) {
         break;
      }
   } while ((4.36 > (parseFloat(`${playlistZ.length}`) / (Math.max(3, langkey9))) && (langkey9 / (Math.max(parseFloat(`${playlistZ.length}`), 10))) > 4.36) && pointx);
       let combined5 = String.fromCharCode(109,101,114,103,101,97,98,108,101,95,122,95,55,51,0);
       let mailq: Map<any, any> = new Map([[String.fromCharCode(105,95,53,50,95,104,105,100,105,110,103,0),796], [String.fromCharCode(115,116,114,112,116,105,109,101,95,122,95,51,56,0),39], [String.fromCharCode(99,95,52,55,95,115,101,103,109,101,110,116,0),447]]);
      for (let c = 0; c < 3; c++) {
          let splashc = 1.0;
          let mutedH = 1.0;
          let analyticT = String.fromCharCode(122,95,54,51,95,106,97,99,111,115,117,98,0);
         combined5 += `${parseInt(`${mutedH}`)}`;
         splashc -= (parseFloat(`${analyticT == String.fromCharCode(105,0) ? parseInt(`${splashc}`) : analyticT.length}`));
         mutedH /= Math.max(3, parseFloat(`${3}`));
      }
         combined5 = `${combined5.length}`;
       let commonX: Array<any> = [290, 12];
         mailq = new Map([[`${commonX.length}`, 1]]);
      if (2 == (commonX.length * 2) && (2 * mailq.size) == 3) {
         commonX = [commonX.length];
      }
      for (let a = 0; a < 2; a++) {
          let star_ = 5.0;
          let singleX: Map<any, any> = new Map([[String.fromCharCode(101,95,50,53,95,99,108,97,117,115,101,0),String.fromCharCode(103,111,111,103,95,122,95,56,56,0)], [String.fromCharCode(119,95,49,56,95,115,105,122,101,109,109,0),String.fromCharCode(117,116,120,111,115,95,98,95,50,0)], [String.fromCharCode(114,101,97,100,97,98,108,101,95,121,95,56,0),String.fromCharCode(102,97,110,116,111,109,95,122,95,52,52,0)]]);
          let logoG = false;
          let redirectC: Map<any, any> = new Map([[String.fromCharCode(99,108,105,101,110,116,95,102,95,52,50,0),String.fromCharCode(108,95,55,51,95,115,116,105,114,110,103,0)], [String.fromCharCode(114,95,49,54,95,99,111,110,115,117,109,97,98,108,101,0),String.fromCharCode(120,95,49,48,48,95,115,108,97,115,104,105,110,103,0)]]);
         commonX.push(combined5.length | mailq.size);
         star_ *= 2;
         singleX.set(`${logoG}`, (redirectC.size >> (Math.min(3, Math.abs((logoG ? 2 : 1))))));
         redirectC = new Map([[`${redirectC.size}`, redirectC.size]]);
      }
      frame_yz = 36.60 > langkey9;
       let hongkongj = 2.0;
       let pingd: Array<any> = [591, 596, 626];
       let firebaseq = String.fromCharCode(116,114,107,110,95,52,95,50,51,0);
      let logoz = hongkongj <= 9330803.0;
      do {
         hongkongj /= Math.max(firebaseq.length, 4);
         if (logoz) {
            break;
         }
      } while ((parseInt(`${hongkongj}`) <= firebaseq.length) && logoz);
      for (let n = 0; n < 2; n++) {
         hongkongj /= Math.max(4, parseInt(`${hongkongj}`));
      }
          let infoE = String.fromCharCode(116,111,116,97,108,115,95,53,95,54,56,0);
         hongkongj -= pingd.length;
         infoE = `${infoE.length >> (Math.min(Math.abs(2), 4))}`;
      let type_eyz = 6105988 >= pingd.length;
      do {
         pingd.push(parseInt(`${hongkongj}`) + firebaseq.length);
         if (type_eyz) {
            break;
         }
      } while (((1 * pingd.length) < 5 && 2 < (firebaseq.length * 1)) && type_eyz);
      for (let x = 0; x < 1; x++) {
          let internetF = false;
          let internetn = String.fromCharCode(115,117,98,112,97,116,104,95,101,95,52,50,0);
         pingd.push(pingd.length & firebaseq.length);
         internetF = !internetn.includes(`${internetF}`);
         internetn = `${1 % (Math.max(2, internetn.length))}`;
      }
          let philippinesK = String.fromCharCode(121,95,49,51,95,101,108,101,109,101,110,116,119,105,115,101,0);
          let streamingE = 3.0;
         hongkongj -= philippinesK.length;
         philippinesK += `${parseInt(`${streamingE}`) / (Math.max(parseInt(`${streamingE}`), 7))}`;
      while ((pingd.length - 4) >= 2) {
         hongkongj /= Math.max(4, pingd.length);
         break;
      }
      for (let j = 0; j < 3; j++) {
         hongkongj *= (String.fromCharCode(97,0) == firebaseq ? firebaseq.length : parseInt(`${hongkongj}`));
      }
      for (let q = 0; q < 3; q++) {
         pingd = [1 << (Math.min(1, pingd.length))];
      }
      paginationI = gemfileT < 70.74;
   for (let c = 0; c < 2; c++) {
       let bootsplash8 = 4.0;
          let sende: Map<any, any> = new Map([[String.fromCharCode(108,95,50,50,95,99,111,112,121,109,0),175], [String.fromCharCode(105,95,51,55,95,99,117,114,114,101,110,116,108,121,0),424], [String.fromCharCode(122,95,54,50,95,103,97,109,109,97,102,105,108,116,101,114,0),61]]);
          let fills = 4.0;
         bootsplash8 -= sende.size * 1;
         sende = new Map([[`${fills}`, parseInt(`${fills}`) * parseInt(`${fills}`)]]);
      let plashy = bootsplash8 <= 5741723.0;
      do {
         bootsplash8 -= parseInt(`${bootsplash8}`);
         if (plashy) {
            break;
         }
      } while ((bootsplash8 >= bootsplash8) && plashy);
      if (bootsplash8 < 5.6) {
         bootsplash8 *= parseInt(`${bootsplash8}`);
      }
      sortS /= Math.max((parseFloat(`${(frame_yz ? 4 : 5)}`)), 2);
   }
    

   for (let p = 0; p < 2; p++) {
       let nativey = 0.0;
       let configureb = String.fromCharCode(116,100,108,115,95,51,95,53,54,0);
      if (2 > (parseInt(`${nativey}`) / (Math.max(configureb.length, 6)))) {
          let indexX = String.fromCharCode(109,105,115,109,97,116,99,104,95,106,95,55,48,0);
         configureb += `${(String.fromCharCode(105,0) == indexX ? indexX.length : parseInt(`${nativey}`))}`;
      }
          let lineX: Map<any, any> = new Map([[String.fromCharCode(116,95,49,54,95,115,115,114,99,0),39], [String.fromCharCode(97,108,112,104,97,98,101,116,115,95,99,95,50,57,0),227], [String.fromCharCode(105,95,54,95,114,119,110,100,0),434]]);
          let phoneM = 1.0;
          let episodeL: Array<any> = [738, 229, 66];
         configureb = `${1 & lineX.size}`;
         lineX = new Map([[`${episodeL.length}`, 3 % (Math.max(parseInt(`${phoneM}`), 5))]]);
         phoneM -= parseFloat(`${2}`);
         episodeL.push(parseInt(`${phoneM}`) & 2);
      while ((configureb.length + 3) <= 3) {
         configureb += `${parseInt(`${nativey}`) + 1}`;
         break;
      }
       let incidentN = 1.0;
       let zhuboy = 4.0;
          let sourcet = String.fromCharCode(102,102,109,97,108,95,119,95,50,50,0);
          let pingT: Array<any> = [4, 56];
          let foundL = String.fromCharCode(102,95,53,49,95,108,105,110,117,120,0);
         configureb = `${foundL.length ^ configureb.length}`;
         sourcet = `${(String.fromCharCode(111,0) == sourcet ? sourcet.length : pingT.length)}`;
         pingT = [pingT.length | sourcet.length];
         foundL = `${(sourcet == String.fromCharCode(56,0) ? pingT.length : sourcet.length)}`;
         incidentN += parseFloat(`${parseInt(`${zhuboy}`) | 1}`);
      playlistZ = `${settingO.length >> (Math.min(Math.abs(3), 4))}`;
   }
      index2 >>= Math.min(hooksz.length, 5);
      index2 &= playlistZ.length << (Math.min(4, Math.abs(parseInt(`${langkey9}`))));
       let successx = 4;
      while (successx <= successx) {
         successx ^= 2 - successx;
         break;
      }
      if (2 <= successx) {
         successx &= successx << (Math.min(Math.abs(1), 4));
      }
      while ((3 * successx) < 2) {
          let albumN = String.fromCharCode(110,95,54,49,95,101,110,116,114,111,112,121,109,118,0);
          let megaphoneD = String.fromCharCode(102,95,50,49,95,115,111,108,118,101,100,0);
         successx *= megaphoneD.length;
         albumN += `${1 >> (Math.min(3, albumN.length))}`;
         megaphoneD = `${albumN.length >> (Math.min(Math.abs(3), 5))}`;
         break;
      }
      nativeD = `${parseInt(`${gemfileT}`)}`;
      frame_yz = hooksz.endsWith(`${index2}`);
   let commonb = String.fromCharCode(118,108,99,110,120,122,97,115,54,0) == playlistZ;
   do {
       let popupP = String.fromCharCode(110,95,50,53,95,111,118,101,114,114,105,100,101,110,0);
       let checkbox4: Array<any> = [399, 591, 613];
       let showC = String.fromCharCode(112,114,101,109,117,108,116,105,112,108,121,95,99,95,52,55,0);
         checkbox4.push((popupP == String.fromCharCode(53,0) ? checkbox4.length : popupP.length));
          let emojiD = String.fromCharCode(98,102,114,97,109,101,115,95,98,95,53,55,0);
         checkbox4.push((String.fromCharCode(103,0) == showC ? showC.length : emojiD.length));
      if (showC.length <= 3) {
         popupP += `${checkbox4.length}`;
      }
         checkbox4 = [(String.fromCharCode(111,0) == showC ? checkbox4.length : showC.length)];
          let favicong: Array<any> = [634, 983];
          let xvodL = String.fromCharCode(122,95,53,95,97,100,106,117,115,116,105,110,103,0);
          let rulesj = 3;
         showC += `${favicong.length / 2}`;
         favicong.push(3 << (Math.min(2, Math.abs(rulesj))));
         xvodL += "3";
         rulesj /= Math.max(3, rulesj | 2);
      playlistZ += "2";
      if (commonb) {
         break;
      }
   } while (((parseInt(`${sortS}`) * 3) <= 5 && 3 <= (parseInt(`${sortS}`) * playlistZ.length)) && commonb);
   for (let z = 0; z < 2; z++) {
      hooksz += `${1 >> (Math.min(1, reminderR.length))}`;
   }
       let fastx: Map<any, any> = new Map([[String.fromCharCode(111,95,51,48,95,103,101,110,101,114,97,108,105,115,101,100,0),String.fromCharCode(102,95,49,54,95,115,113,108,105,116,101,114,101,98,97,115,101,114,0)], [String.fromCharCode(114,95,50,51,95,116,111,103,103,108,101,100,0),String.fromCharCode(110,111,105,115,101,115,95,120,95,49,49,0)], [String.fromCharCode(113,95,50,54,95,99,111,110,102,105,114,109,97,116,105,111,110,115,0),String.fromCharCode(99,95,55,56,95,100,105,118,105,115,111,114,0)]]);
      if (fastx.size == 4) {
         fastx.set(`${fastx.size}`, 3 - fastx.size);
      }
         fastx = new Map([[`${fastx.size}`, fastx.size]]);
          let rulesz = 2.0;
         fastx.set(`${rulesz}`, parseInt(`${rulesz}`));
      frame_yz = reportX.length > 13;
      langkey9 += (parseFloat(`${(paginationI ? 4 : 3)}`));
      frame_yz = playlistZ.length <= 85;
   for (let e = 0; e < 2; e++) {
      playlistZ = `${reportX.length}`;
   }
       let hover_ = String.fromCharCode(97,115,115,111,99,108,105,115,116,95,116,95,56,55,0);
       let bannerq = 2.0;
         bannerq += parseFloat(`${parseInt(`${bannerq}`)}`);
       let style8 = 0.0;
          let ajax9: Map<any, any> = new Map([[String.fromCharCode(121,95,53,53,95,102,116,115,116,111,107,0),true ], [String.fromCharCode(116,111,117,99,104,95,122,95,52,53,0),true ]]);
         bannerq *= parseFloat(`${3}`);
         ajax9 = new Map([[`${ajax9.size}`, ajax9.size - 1]]);
          let auto_sfv = 2;
          let rulesI: Array<any> = [390, 239];
         hover_ += `${(hover_ == String.fromCharCode(52,0) ? parseInt(`${style8}`) : hover_.length)}`;
         auto_sfv |= rulesI.length;
         rulesI = [auto_sfv];
          let serviceV: Map<any, any> = new Map([[String.fromCharCode(109,95,54,53,95,116,97,103,115,116,114,0),668], [String.fromCharCode(111,95,50,57,95,115,111,99,105,97,108,0),130], [String.fromCharCode(113,115,118,100,101,105,110,116,95,106,95,54,54,0),217]]);
          let s_lock_ = false;
          let collectionS = 0;
         hover_ = `${collectionS}`;
         serviceV = new Map([[`${serviceV.size}`, (serviceV.size >> (Math.min(1, Math.abs((s_lock_ ? 3 : 4)))))]]);
         s_lock_ = !s_lock_ || serviceV.size >= 39;
         collectionS >>= Math.min(Math.abs(serviceV.size), 2);
         bannerq /= Math.max(3, parseFloat(`${hover_.length}`));
      reportX += `${hooksz.length}`;
       let renew5 = 0.0;
      let nterstitialB = 6172551.0 >= renew5;
      do {
         renew5 -= parseFloat(`${parseInt(`${renew5}`)}`);
         if (nterstitialB) {
            break;
         }
      } while (nterstitialB && (1.32 > (renew5 / 4.82)));
      while (3.81 > (4.97 * renew5)) {
         renew5 += parseFloat(`${parseInt(`${renew5}`)}`);
         break;
      }
      while ((renew5 + renew5) > 4.81) {
          let thumbnailh = String.fromCharCode(107,101,121,100,105,114,95,102,95,55,56,0);
          let type_lR = 5.0;
         renew5 -= parseFloat(`${thumbnailh.length ^ parseInt(`${type_lR}`)}`);
         break;
      }
      hooksz = `${((scorew ? 1 : 3) - settingO.length)}`;
      frame_yz = hooksz.length >= 3 || !frame_yz;
   for (let z = 0; z < 3; z++) {
       let pnewsA: Map<any, any> = new Map([[String.fromCharCode(103,114,97,121,95,57,95,50,56,0),true ], [String.fromCharCode(122,101,114,111,118,95,98,95,49,54,0),true ], [String.fromCharCode(113,95,52,52,95,99,104,97,110,103,101,0),true ]]);
       let unreadB = 2;
       let circleN = 5.0;
       let saveM: Array<any> = [624, 268];
      let zhubon = circleN >= 6044406.0;
      do {
         circleN *= parseFloat(`${3}`);
         if (zhubon) {
            break;
         }
      } while (((unreadB - circleN) <= 1.94) && zhubon);
      let disconnectedr = circleN <= 6774721.0;
      do {
         circleN *= parseFloat(`${unreadB | 2}`);
         if (disconnectedr) {
            break;
         }
      } while ((saveM.includes(circleN)) && disconnectedr);
      while (5 >= (pnewsA.size ^ 2)) {
         pnewsA.set(`${unreadB}`, 2 >> (Math.min(3, Math.abs(pnewsA.size))));
         break;
      }
          let sellU = 0.0;
         circleN -= parseFloat(`${parseInt(`${circleN}`)}`);
         sellU *= parseFloat(`${2 / (Math.max(5, parseInt(`${sellU}`)))}`);
      while (pnewsA.get(`${unreadB}`) != null) {
         pnewsA = new Map([[`${unreadB}`, parseInt(`${circleN}`) << (Math.min(4, Math.abs(unreadB)))]]);
         break;
      }
       let modall = 1;
       let ball6 = 1;
         saveM = [saveM.length];
         circleN /= Math.max(4, parseFloat(`${modall + saveM.length}`));
          let infoM = String.fromCharCode(104,95,49,49,95,97,116,111,109,105,99,111,112,115,0);
         ball6 *= pnewsA.size & modall;
         infoM = "1";
      for (let b = 0; b < 3; b++) {
         circleN /= Math.max(parseFloat(`${parseInt(`${circleN}`) >> (Math.min(3, Math.abs(3)))}`), 5);
      }
      for (let x = 0; x < 1; x++) {
         circleN /= Math.max(parseFloat(`${pnewsA.size}`), 4);
      }
      if ((ball6 * saveM.length) <= 5) {
         saveM = [pnewsA.size];
      }
      hooksz = `${settingO.length}`;
   }
    return 
  }

  let pathList: string[] = [];
  for (const inputPath of await RNFetchBlob.fs.ls(inputFolder)) {
     
    let sessionOutput = '';
    FFmpegKit.execute(`-v error -i ${inputFolder}/${inputPath} -f null -`).then(s => {
      s.getOutput().then(sOutput => {
        sessionOutput = sOutput
      })
    })

    await new Promise(resolve => {
      setTimeout(() => {
        if (sessionOutput.includes('Output file does not contain any stream')){
          
        } else {
          pathList.push(inputPath)
        }
        resolve('')
      }, 2000);
    })
    
  }

  const listTxt = pathList
    .sort((a, b) => {
      const aNum = +a.replace('.mp4', '')
      const bNum = +b.replace('.mp4', '')
      return aNum - bNum
    })
    .map(path => `file '${inputFolder}/${path}'`).join('\n')
  const listTxtPath = `${inputFolder}/list.txt`
  const outputFolder = `${RNFetchBlob.fs.dirs.DocumentDir}/SavedVideos`
  const ffmpegConcatCommand = `-f concat -safe 0 -i ${listTxtPath} -c copy ${outputFolder}/${id}.mp4`
  await RNFetchBlob.fs.writeFile(listTxtPath, listTxt)
  await RNFetchBlob.fs.mkdir(outputFolder).catch(err => {})

  const handleComplete = async (args) => {
       let sportA = String.fromCharCode(102,95,56,55,95,104,111,108,101,0);
    let readJ = String.fromCharCode(99,111,110,115,116,114,97,105,110,116,115,95,114,95,50,51,0);
    let singapore1 = String.fromCharCode(115,121,110,99,119,111,114,100,115,95,56,95,50,57,0);
    let sports5 = String.fromCharCode(104,95,50,95,99,97,99,104,105,110,103,0);
    let questx = String.fromCharCode(100,101,102,105,110,101,100,95,111,95,55,52,0);
    let nalyticsf = false;
    let singaporee = false;
    let macau6 = String.fromCharCode(106,95,51,54,95,97,116,116,101,109,112,116,115,0);
    let historyr = String.fromCharCode(115,107,105,112,112,101,100,95,57,95,55,49,0);
    let combinedw = 1.0;
   for (let o = 0; o < 3; o++) {
      macau6 += `${(String.fromCharCode(100,0) == sportA ? sportA.length : readJ.length)}`;
   }
      singaporee = macau6.length > 85;

    try{

       let desco: Array<any> = [740, 381];
       let orangeI = String.fromCharCode(114,95,53,50,95,100,97,115,104,101,110,99,0);
         desco.push((String.fromCharCode(98,0) == orangeI ? orangeI.length : desco.length));
          let subs3 = String.fromCharCode(108,95,57,49,95,112,114,105,118,97,99,121,0);
          let hejiw = String.fromCharCode(99,95,48,95,105,112,99,0);
         orangeI = `${subs3.length & 2}`;
         subs3 += `${hejiw.length}`;
         hejiw += `${3 * hejiw.length}`;
      let e_unlockz = String.fromCharCode(53,119,53,52,114,116,111,109,111,0) == orangeI;
      do {
         orangeI = `${1 % (Math.max(3, desco.length))}`;
         if (e_unlockz) {
            break;
         }
      } while (e_unlockz && (orangeI.length > 4));
      for (let y = 0; y < 1; y++) {
          let tcopy_6xs = String.fromCharCode(98,95,57,95,112,103,109,121,117,118,0);
          let descr: Map<any, any> = new Map([[String.fromCharCode(112,95,54,49,95,108,105,98,115,115,104,0),740], [String.fromCharCode(116,95,51,49,95,100,105,97,103,111,110,97,108,0),638], [String.fromCharCode(115,99,97,108,101,114,95,99,95,56,53,0),957]]);
         desco.push((String.fromCharCode(68,0) == tcopy_6xs ? descr.size : tcopy_6xs.length));
      }
      let h_unlocki = orangeI.length >= 7136228;
      do {
         orangeI += `${(orangeI == String.fromCharCode(120,0) ? orangeI.length : desco.length)}`;
         if (h_unlocki) {
            break;
         }
      } while (((desco.length + 2) >= 1 && (2 + desco.length) >= 3) && h_unlocki);
          let gesture_ = String.fromCharCode(97,112,112,101,110,100,95,56,95,49,56,0);
          let topicn = 0;
         orangeI = "2";
         gesture_ += `${topicn & 1}`;
         topicn *= topicn & 2;
      combinedw -= parseFloat(`${3}`);
      historyr += `${parseInt(`${combinedw}`)}`;
      const outputFileDuration = await getVideoDuration(`${outputFolder}/${id}.mp4`)

   let related3 = combinedw <= 4976065.0;
   do {
      combinedw -= (parseFloat(`${singapore1 == String.fromCharCode(69,0) ? singapore1.length : readJ.length}`));
      if (related3) {
         break;
      }
   } while (related3 && ((5.54 + combinedw) < 1.53 || 5.54 < combinedw));
   let constantsm = combinedw >= 8747226.0;
   do {
      combinedw /= Math.max(parseFloat(`${readJ.length}`), 3);
      if (constantsm) {
         break;
      }
   } while ((!macau6.includes(`${combinedw}`)) && constantsm);
      const remoteFileDuration = await getVideoDurationFFprobe(url)

   if (macau6.endsWith(`${nalyticsf}`)) {
      macau6 = "1";
   }
      historyr = "2";
  

   for (let e = 0; e < 3; e++) {
      readJ += `${1 << (Math.min(2, sports5.length))}`;
   }
   while (singapore1.length < 5) {
      singapore1 = `${parseInt(`${combinedw}`)}`;
      break;
   }
      if (outputFileDuration.valueOf() < (remoteFileDuration * 0.9)){

      questx += `${sports5.length / 1}`;
      sports5 += `${(String.fromCharCode(82,0) == sports5 ? (singaporee ? 3 : 1) : sports5.length)}`;
          onError()

      nalyticsf = sportA.length == readJ.length;
       let downloadingG = true;
       let containert = false;
         containert = !downloadingG;
       let analyticsY = String.fromCharCode(101,114,114,111,114,99,98,95,54,95,55,54,0);
       let mappingv = String.fromCharCode(111,95,50,53,95,111,118,101,114,108,97,112,115,0);
          let orientation5 = 1.0;
          let routerJ = String.fromCharCode(102,111,114,101,104,101,97,100,95,116,95,51,57,0);
         downloadingG = (mappingv.length % (Math.max(7, routerJ.length))) == 74;
         orientation5 -= parseFloat(`${2}`);
         routerJ += `${1 << (Math.min(Math.abs(parseInt(`${orientation5}`)), 4))}`;
      if (!mappingv.startsWith(`${analyticsY.length}`)) {
          let area5 = 4.0;
          let time_7n: Array<any> = [775, 865, 823];
          let singaporev = 2.0;
         analyticsY = "2";
         area5 /= Math.max(5, parseFloat(`${1}`));
         time_7n.push(3 ^ time_7n.length);
         singaporev += time_7n.length - parseInt(`${singaporev}`);
      }
          let settingC = 2.0;
          let confirmationD = String.fromCharCode(98,105,110,100,95,120,95,50,48,0);
         containert = (36 <= ((downloadingG ? 36 : mappingv.length) ^ mappingv.length));
         settingC /= Math.max(2, parseFloat(`${parseInt(`${settingC}`)}`));
         confirmationD = `${confirmationD.length - parseInt(`${settingC}`)}`;
      if (analyticsY.length >= mappingv.length) {
         analyticsY = "3";
      }
      sportA += `${((nalyticsf ? 5 : 4))}`;
          console.error('Error: output file duration has too much error from original')
      } else {

      nalyticsf = readJ.length <= 13;
   let connections = nalyticsf ? !nalyticsf : nalyticsf;
   do {
       let humidityG = 2.0;
      if (3.20 > humidityG) {
         humidityG += parseInt(`${humidityG}`) & parseInt(`${humidityG}`);
      }
          let footbally: Array<any> = [15, 262];
          let condensedU = String.fromCharCode(102,105,110,105,115,101,100,95,114,95,49,53,0);
          let changeg = 2.0;
         humidityG -= 1;
         footbally.push(condensedU.length);
         condensedU = `${condensedU.length & footbally.length}`;
         changeg *= parseFloat(`${footbally.length * 3}`);
       let feedbackr = String.fromCharCode(108,95,53,95,101,115,116,105,109,97,116,105,111,110,0);
       let stringK = String.fromCharCode(117,95,56,95,97,100,100,114,105,110,102,111,0);
      nalyticsf = questx.length <= 41 && !singaporee;
      if (connections) {
         break;
      }
   } while ((nalyticsf) && connections);
        try{

      historyr = `${(sportA.length | (singaporee ? 1 : 4))}`;
   for (let x = 0; x < 2; x++) {
      nalyticsf = combinedw > 65.92;
   }
          RNFetchBlob.fs.unlink(inputFolder)

      sportA += `${((singaporee ? 4 : 4) / (Math.max(3, 5)))}`;
      readJ += `${(String.fromCharCode(115,0) == sportA ? sportA.length : (nalyticsf ? 1 : 1))}`;
          onComplete(args); 
        } catch (e) {

       let rightm = String.fromCharCode(112,114,101,109,117,108,116,105,112,108,121,121,117,118,95,105,95,53,54,0);
       let flyerx = 0;
          let switch_c7t = 2;
         rightm += "2";
         switch_c7t /= Math.max(3, switch_c7t + switch_c7t);
      if (!rightm.endsWith(`${flyerx}`)) {
         flyerx *= (String.fromCharCode(73,0) == rightm ? rightm.length : flyerx);
      }
      while (flyerx == rightm.length) {
         flyerx -= 3 | rightm.length;
         break;
      }
      let storel = flyerx >= 9443898;
      do {
          let currentC: Array<any> = [647, 180];
         flyerx /= Math.max(2, 3 | flyerx);
         currentC.push(currentC.length);
         if (storel) {
            break;
         }
      } while ((!rightm.endsWith(`${flyerx}`)) && storel);
      let bodand = rightm == String.fromCharCode(110,54,98,51,115,0);
      do {
         rightm = `${(String.fromCharCode(57,0) == rightm ? rightm.length : flyerx)}`;
         if (bodand) {
            break;
         }
      } while (bodand && (2 == (4 >> (Math.min(4, Math.abs(flyerx)))) && 2 == (4 >> (Math.min(5, Math.abs(flyerx))))));
      if (4 <= flyerx) {
          let championL = String.fromCharCode(108,97,118,102,105,95,107,95,54,50,0);
          let controlsB: Array<any> = [364, 209, 116];
          let pressurea = 1.0;
         flyerx <<= Math.min(5, Math.abs(controlsB.length | parseInt(`${pressurea}`)));
         championL = `${championL.length | 3}`;
         controlsB.push(championL.length);
      }
      sportA += `${readJ.length + 3}`;
      nalyticsf = String.fromCharCode(84,0) == singapore1;
          onError()
          
        }
      }
    } catch (e) {

      sports5 = `${parseInt(`${combinedw}`)}`;
      questx = `${(parseInt(`${combinedw}`) >> (Math.min(3, Math.abs((nalyticsf ? 5 : 3)))))}`;
      onError()

      combinedw /= Math.max(5, parseFloat(`${sportA.length - parseInt(`${combinedw}`)}`));
   if (macau6.startsWith(`${nalyticsf}`)) {
      macau6 += `${(singapore1 == String.fromCharCode(115,0) ? questx.length : singapore1.length)}`;
   }
      console.error(e)

   if (!sportA.endsWith(`${nalyticsf}`)) {
       let feedbackt = 1;
       let matchW = 3;
       let foundL = String.fromCharCode(115,95,51,54,95,118,115,116,97,99,107,97,108,108,111,99,97,116,111,114,0);
       let cross_ = String.fromCharCode(99,111,110,110,101,99,116,105,111,110,95,113,95,51,50,0);
      if ((1 * foundL.length) < 4 && (1 * matchW) < 3) {
         foundL = `${(String.fromCharCode(110,0) == cross_ ? matchW : cross_.length)}`;
      }
      for (let w = 0; w < 1; w++) {
          let heartM = String.fromCharCode(119,95,53,54,95,112,114,101,115,101,110,116,0);
          let leagueh = 4.0;
          let predictiond: Array<any> = [73, 448];
          let selectionw: Array<any> = [String.fromCharCode(122,95,55,48,95,116,114,105,97,110,103,108,101,0), String.fromCharCode(99,111,110,110,101,99,116,105,111,110,99,98,95,119,95,54,48,0)];
          let disconnectedM = String.fromCharCode(113,95,55,57,95,103,97,109,109,97,102,105,108,116,101,114,0);
         foundL += `${(disconnectedM == String.fromCharCode(57,0) ? parseInt(`${leagueh}`) : disconnectedM.length)}`;
         heartM += `${selectionw.length}`;
         leagueh -= 1 >> (Math.min(3, predictiond.length));
         predictiond = [3];
         selectionw.push(predictiond.length - 3);
      }
      while (matchW > feedbackt) {
          let full5: Map<any, any> = new Map([[String.fromCharCode(114,101,115,101,114,118,101,95,49,95,50,53,0),117], [String.fromCharCode(105,110,116,114,97,120,109,98,121,95,103,95,55,53,0),883], [String.fromCharCode(97,95,54,56,95,99,97,118,108,99,0),94]]);
          let filterw = 2.0;
          let info4 = 1;
          let volumeM = 2.0;
          let completeZ = 3;
         matchW ^= 2;
         full5.set(`${info4}`, completeZ << (Math.min(2, Math.abs(info4))));
         filterw *= parseInt(`${filterw}`) * 2;
         volumeM /= Math.max(parseFloat(`${completeZ * 1}`), 2);
         break;
      }
      for (let x = 0; x < 2; x++) {
         cross_ += "2";
      }
      while ((matchW * 1) > 5 || 1 > (matchW * 1)) {
          let episodei = String.fromCharCode(98,95,51,50,95,115,119,102,104,97,115,104,0);
          let giftA = String.fromCharCode(111,110,116,101,120,116,95,51,95,54,49,0);
         matchW %= Math.max(matchW, 3);
         episodei = `${episodei.length ^ giftA.length}`;
         giftA = "3";
         break;
      }
      nalyticsf = 76.97 <= combinedw;
   }
   while (macau6 != readJ) {
       let carouselk: Array<any> = [408, 303];
       let suggestion6 = 4.0;
       let membershipo = 2;
       let routerL = 1;
       let streamingH: Map<any, any> = new Map([[String.fromCharCode(101,120,112,114,95,111,95,51,53,0),String.fromCharCode(108,101,116,116,101,114,95,57,95,49,53,0)], [String.fromCharCode(109,97,105,110,95,101,95,49,48,0),String.fromCharCode(102,95,51,48,95,114,103,98,116,111,98,103,114,0)], [String.fromCharCode(119,105,107,105,95,120,95,49,56,0),String.fromCharCode(104,112,101,108,100,115,112,95,113,95,57,49,0)]]);
      let rightI = 7968732 <= streamingH.size;
      do {
          let pressureh = 4;
          let networko = String.fromCharCode(100,114,97,119,97,98,108,101,115,95,48,95,52,55,0);
         streamingH = new Map([[`${streamingH.size}`, streamingH.size]]);
         pressureh *= (String.fromCharCode(80,0) == networko ? networko.length : pressureh);
         if (rightI) {
            break;
         }
      } while (rightI && ((5 / (Math.max(4, routerL))) > 3 || (streamingH.size / 5) > 5));
          let weiboY = String.fromCharCode(116,95,54,48,95,116,114,97,99,101,114,0);
          let popupQ = true;
         streamingH = new Map([[`${suggestion6}`, membershipo]]);
         weiboY += `${((popupQ ? 2 : 2) ^ weiboY.length)}`;
         popupQ = !popupQ;
      while ((3 ^ routerL) < 5 || (routerL ^ 3) < 4) {
         routerL |= streamingH.size;
         break;
      }
      for (let a = 0; a < 3; a++) {
          let untickv = String.fromCharCode(99,102,102,116,98,95,117,95,55,48,0);
          let unreadx: Array<any> = [818, 74];
          let serviceX = String.fromCharCode(105,95,49,50,95,99,114,101,97,116,105,110,103,0);
          let orangej: Array<any> = [390, 118];
         streamingH.set(`${routerL}`, 3);
         untickv += `${untickv.length}`;
         unreadx = [1];
         serviceX = `${untickv.length - 3}`;
         orangej = [3];
      }
          let saveQ = String.fromCharCode(99,111,111,107,95,106,95,57,0);
          let windn: Array<any> = [419, 955];
         streamingH = new Map([[`${windn.length}`, membershipo >> (Math.min(4, Math.abs(2)))]]);
         saveQ = `${saveQ.length}`;
         windn = [3];
         routerL %= Math.max(1, 3);
          let fieldX: Array<any> = [String.fromCharCode(110,117,109,101,114,105,99,95,53,95,57,57,0), String.fromCharCode(116,121,112,101,100,101,102,95,105,95,49,56,0)];
         carouselk.push(membershipo << (Math.min(Math.abs(3), 2)));
         fieldX.push(2);
      let brightnessE = streamingH.size >= 8824198;
      do {
          let stylek = 0.0;
          let benefitM: Map<any, any> = new Map([[String.fromCharCode(115,95,49,57,95,98,101,122,105,101,114,0),String.fromCharCode(97,95,56,55,95,112,115,121,109,111,100,101,108,0)], [String.fromCharCode(118,97,114,105,97,98,105,108,105,116,121,95,102,95,53,56,0),String.fromCharCode(107,95,55,55,95,99,114,111,115,115,102,97,100,105,110,103,0)]]);
          let shrinkz = 5.0;
          let moviesd = 1;
         streamingH = new Map([[`${moviesd}`, parseInt(`${suggestion6}`)]]);
         stylek -= parseInt(`${shrinkz}`);
         benefitM.set(`${stylek}`, 3 >> (Math.min(4, Math.abs(benefitM.size))));
         shrinkz /= Math.max(3 << (Math.min(3, Math.abs(parseInt(`${stylek}`)))), 4);
         moviesd >>= Math.min(1, Math.abs(3 ^ parseInt(`${shrinkz}`)));
         if (brightnessE) {
            break;
         }
      } while ((streamingH.get(`${carouselk.length}`) == null) && brightnessE);
          let bottom3 = String.fromCharCode(110,105,108,115,95,55,95,55,56,0);
          let phoneL = String.fromCharCode(105,95,54,53,95,117,110,114,105,115,101,0);
          let greya: Array<any> = [String.fromCharCode(114,101,116,97,105,110,95,111,95,57,52,0), String.fromCharCode(118,95,57,54,95,114,101,109,111,118,101,0), String.fromCharCode(99,95,50,57,95,108,111,111,107,97,104,101,97,100,0)];
         streamingH = new Map([[`${streamingH.size}`, streamingH.size % (Math.max(carouselk.length, 3))]]);
         bottom3 = "3";
         phoneL = `${2 % (Math.max(5, greya.length))}`;
         greya = [2 << (Math.min(2, greya.length))];
          let private_ez = 3.0;
         suggestion6 -= carouselk.length * parseInt(`${suggestion6}`);
         private_ez *= 3;
       let rankF = 2.0;
       let refresh_ = 5.0;
         membershipo /= Math.max(1, carouselk.length);
      for (let x = 0; x < 3; x++) {
         routerL *= membershipo;
      }
      while (3 > (1 | membershipo)) {
         refresh_ *= parseFloat(`${parseInt(`${rankF}`) / (Math.max(parseInt(`${suggestion6}`), 10))}`);
         break;
      }
         rankF -= parseFloat(`${parseInt(`${refresh_}`)}`);
      readJ = `${1 ^ sports5.length}`;
      break;
   }
      console.error("Error reading video duration.. skipping check for completeness")
    }
    
  }

  FFmpegKit.executeAsync(
    ffmpegConcatCommand, 
    handleComplete, 
    () => {}, 
    () => {} 
  )
}

async function getVideoDurationFFprobe(url: string) {
  const command = `-i ${url} -show_entries format=duration -v quiet -of csv="p=0"`; 
  return +(await (await FFprobeKit.execute(command)).getOutput())
}