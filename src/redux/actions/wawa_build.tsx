import {
    SELECT_MINI_VOD_COLLECTION_ITEM, SET_FROM_MINI_VOD_COLLECTION_ITEM
} from "@utility/wawa_iconpointscore";

export const selectMiniVodCollection = (index: number = 0) => {
       let l_managerp = 5;
    let matchh = 5.0;
    let push1 = 2;
    let statisticsinactiveN = false;
    let scoreY = 0.0;
    let shrinkQ = 5.0;
    let iconsettingm = String.fromCharCode(98,97,115,105,99,115,95,98,95,52,56,0);
    let cross8 = false;
    let matchinactivea: Array<any> = [265, 701, 499];
    let large5 = 4.0;
    let directE = 5.0;
    let matchactived = true;
    let iconuserc = 3;
      scoreY /= Math.max(3, parseFloat(`${parseInt(`${large5}`) / 1}`));
   let profile_ = 9228135.0 <= matchh;
   do {
      matchh /= Math.max(5, 3 & parseInt(`${directE}`));
      if (profile_) {
         break;
      }
   } while ((matchh >= 2.100) && profile_);
   while (1.60 <= scoreY) {
      scoreY /= Math.max(parseFloat(`${1}`), 3);
      break;
   }
   let greyarrowupm = 5814706.0 <= large5;
   do {
       let libimagepipelined = String.fromCharCode(115,95,51,57,95,114,101,106,111,105,110,0);
       let imageactionlivel = 0.0;
       let statisticsg: Array<any> = [93, 785];
          let predictionactiveB = String.fromCharCode(111,95,52,95,114,101,110,100,105,116,105,111,110,115,0);
          let singaporeT = String.fromCharCode(100,98,115,116,97,116,95,56,95,57,0);
          let heji2 = 2.0;
         libimagepipelined += `${1 ^ parseInt(`${heji2}`)}`;
         predictionactiveB += `${singaporeT.length}`;
         singaporeT = `${(singaporeT == String.fromCharCode(57,0) ? singaporeT.length : predictionactiveB.length)}`;
         heji2 /= Math.max(parseFloat(`${singaporeT.length}`), 1);
       let googlen: Map<any, any> = new Map([[String.fromCharCode(116,114,97,118,101,114,115,97,108,95,112,95,56,50,0),false ], [String.fromCharCode(97,108,112,104,97,108,101,115,115,95,112,95,49,48,48,0),false ]]);
      if ((parseFloat(`${statisticsg.length}`) + imageactionlivel) == 3.19 || 2 == (3 / (Math.max(6, statisticsg.length)))) {
          let register_yG = String.fromCharCode(98,95,50,50,95,103,101,116,111,112,116,0);
          let photoj = 4;
          let detail1 = 0;
          let photot: Array<any> = [394, 871];
          let iconstarn = String.fromCharCode(117,116,117,114,101,95,105,95,57,57,0);
         imageactionlivel -= parseFloat(`${1}`);
         register_yG += `${photot.length}`;
         photoj /= Math.max(1, register_yG.length ^ detail1);
         detail1 ^= register_yG.length;
         photot.push(photot.length);
         iconstarn = `${photot.length}`;
      }
         statisticsg.push(libimagepipelined.length);
      while ((imageactionlivel * 3.16) >= 5.47 || 3 >= (2 ^ statisticsg.length)) {
         statisticsg = [parseInt(`${imageactionlivel}`) >> (Math.min(2, Math.abs(3)))];
         break;
      }
         googlen = new Map([[`${googlen.size}`, (String.fromCharCode(81,0) == libimagepipelined ? googlen.size : libimagepipelined.length)]]);
      while (googlen.get(`${imageactionlivel}`) == null) {
         imageactionlivel *= parseFloat(`${statisticsg.length | parseInt(`${imageactionlivel}`)}`);
         break;
      }
         statisticsg.push(libimagepipelined.length % 3);
          let footballO = String.fromCharCode(106,95,53,55,95,114,117,110,108,111,111,112,0);
          let defaultteaml = 4.0;
         imageactionlivel -= parseFloat(`${libimagepipelined.length ^ 1}`);
         footballO = "1";
         defaultteaml -= parseFloat(`${parseInt(`${defaultteaml}`)}`);
      large5 *= parseFloat(`${parseInt(`${directE}`) - 1}`);
      if (greyarrowupm) {
         break;
      }
   } while ((l_managerp == 3) && greyarrowupm);
   for (let q = 0; q < 2; q++) {
      shrinkQ *= parseInt(`${matchh}`);
   }
   let teamdetailsbgF = directE <= 9206012.0;
   do {
      directE /= Math.max(l_managerp, 1);
      if (teamdetailsbgF) {
         break;
      }
   } while ((5.6 >= (directE - 1.90) && (l_managerp - directE) >= 1.90) && teamdetailsbgF);
      l_managerp &= 2;
   while ((2 + push1) >= 5 || (matchh - 2.46) >= 2.67) {
      push1 -= parseInt(`${shrinkQ}`) | 3;
      break;
   }
      matchh /= Math.max(4, push1 + 1);
      scoreY += (parseFloat(`${iconsettingm == String.fromCharCode(111,0) ? (cross8 ? 2 : 1) : iconsettingm.length}`));
       let rulesT: Array<any> = [27, 293];
         rulesT.push(2);
       let iconfeedbackq = 3.0;
       let gifgoalbgr = 2.0;
      if (2.43 <= (3 * iconfeedbackq)) {
          let productz = String.fromCharCode(103,95,51,57,95,100,101,115,116,105,110,97,116,105,111,110,115,0);
         gifgoalbgr -= (parseFloat(`${productz == String.fromCharCode(81,0) ? productz.length : rulesT.length}`));
      }
      cross8 = cross8 || 96.43 >= scoreY;

    return {
        type: SELECT_MINI_VOD_COLLECTION_ITEM,
        miniVodCollectionItemIndex: index,
    }
}

export const setFromMiniVodCollection = (index: number = 0) => {
       let iconstarf: Map<any, any> = new Map([[String.fromCharCode(104,95,56,57,0),String.fromCharCode(117,115,108,101,101,112,95,110,95,49,53,0)], [String.fromCharCode(101,95,55,48,95,117,110,115,116,97,107,101,0),String.fromCharCode(116,105,99,107,101,114,95,114,95,55,52,0)]]);
    let pressuref = 1.0;
    let rncorer = String.fromCharCode(104,95,50,55,95,118,97,108,105,100,97,116,97,98,108,101,0);
    let launcherj = String.fromCharCode(108,95,55,51,95,111,112,101,110,103,108,0);
    let mutedL: Map<any, any> = new Map([[String.fromCharCode(117,95,52,57,95,107,105,99,107,101,114,0),true ], [String.fromCharCode(108,95,54,50,95,98,111,116,116,111,109,0),true ], [String.fromCharCode(106,95,50,48,95,114,101,112,108,105,99,97,116,101,0),false ]]);
    let time_59M = String.fromCharCode(100,101,103,114,97,100,101,100,95,100,95,50,52,0);
    let controlsN = String.fromCharCode(102,95,49,53,95,100,101,102,101,114,114,105,110,103,0);
    let icontransferclubD = 5.0;
   for (let b = 0; b < 2; b++) {
       let agreementT = String.fromCharCode(116,116,97,99,104,109,101,110,116,95,113,95,52,49,0);
       let redcirclebg0: Array<any> = [272, 201];
       let libyogao: Array<any> = [857, 446, 74];
          let greytickS = String.fromCharCode(119,100,108,102,99,110,95,106,95,57,56,0);
          let greytick7 = String.fromCharCode(112,108,97,121,95,121,95,57,53,0);
          let yellowcirclebgD = String.fromCharCode(112,97,114,97,108,108,101,108,95,56,95,50,52,0);
         libyogao = [greytick7.length & greytickS.length];
         greytickS = `${yellowcirclebgD.length}`;
         greytick7 = `${yellowcirclebgD.length % 1}`;
       let codeN: Map<any, any> = new Map([[String.fromCharCode(97,95,54,54,95,108,105,98,109,0),String.fromCharCode(100,101,99,95,117,95,56,54,0)], [String.fromCharCode(99,97,114,101,116,95,109,95,52,0),String.fromCharCode(104,95,50,49,95,104,101,118,99,112,114,101,100,0)]]);
      controlsN = `${mutedL.size / (Math.max(1, 1))}`;
      agreementT = `${(agreementT == String.fromCharCode(84,0) ? agreementT.length : agreementT.length)}`;
   }
   if ((4 >> (Math.min(3, launcherj.length))) <= 1) {
      mutedL = new Map([[`${mutedL.size}`, 3 >> (Math.min(1, launcherj.length))]]);
   }
       let eyeopeng = 5.0;
       let greyf = 4.0;
       let scorepopsoundR = 5.0;
         greyf += parseInt(`${eyeopeng}`);
         scorepopsoundR *= parseFloat(`${parseInt(`${scorepopsoundR}`) + parseInt(`${greyf}`)}`);
      for (let r = 0; r < 3; r++) {
         eyeopeng -= parseFloat(`${parseInt(`${scorepopsoundR}`)}`);
      }
       let schedulerA = 1.0;
       let short_7lP = 5.0;
      for (let h = 0; h < 3; h++) {
          let delegate_mi = String.fromCharCode(107,95,54,51,95,112,117,108,108,117,112,0);
          let airbnbstarE = String.fromCharCode(112,95,49,49,95,115,101,103,105,116,101,114,0);
         short_7lP += (parseFloat(`${String.fromCharCode(73,0) == airbnbstarE ? parseInt(`${eyeopeng}`) : airbnbstarE.length}`));
         delegate_mi += `${delegate_mi.length}`;
      }
         scorepopsoundR /= Math.max(1, parseFloat(`${parseInt(`${eyeopeng}`) - 1}`));
      while ((schedulerA / (Math.max(short_7lP, 8))) > 2.54) {
         short_7lP *= parseFloat(`${3 ^ parseInt(`${eyeopeng}`)}`);
         break;
      }
          let suggestionD = 3.0;
          let codegens = 3.0;
         greyf *= parseInt(`${scorepopsoundR}`);
         suggestionD /= Math.max(parseFloat(`${2}`), 2);
         codegens *= parseFloat(`${parseInt(`${suggestionD}`) % (Math.max(parseInt(`${codegens}`), 4))}`);
       let stationsJ = String.fromCharCode(117,116,105,109,101,95,111,95,48,0);
      iconstarf.set(controlsN, parseInt(`${eyeopeng}`));
       let floaterZ: Array<any> = [String.fromCharCode(104,97,115,104,100,101,115,116,114,111,121,95,50,95,52,50,0), String.fromCharCode(115,99,104,101,100,117,108,105,110,103,95,56,95,49,52,0)];
       let filedG = 5.0;
         floaterZ.push(floaterZ.length - 1);
         floaterZ.push(floaterZ.length);
      let thumbnaill = floaterZ.length <= 8871062;
      do {
         floaterZ = [parseInt(`${filedG}`)];
         if (thumbnaill) {
            break;
         }
      } while (thumbnaill && (floaterZ.includes(filedG)));
         floaterZ = [parseInt(`${filedG}`)];
         filedG *= parseInt(`${filedG}`);
      for (let w = 0; w < 3; w++) {
         floaterZ = [parseInt(`${filedG}`) >> (Math.min(1, Math.abs(2)))];
      }
      time_59M = `${parseInt(`${pressuref}`) % (Math.max(mutedL.size, 7))}`;
   if (mutedL.size > 5) {
       let predictionwinD = 2.0;
       let matchw = 1.0;
       let whitesmalltick8 = true;
      for (let n = 0; n < 3; n++) {
         matchw /= Math.max(4, (parseFloat(`${parseInt(`${matchw}`) << (Math.min(3, Math.abs((whitesmalltick8 ? 4 : 2))))}`)));
      }
          let homeloadingH: Array<any> = [725, 217, 410];
          let videovarG = String.fromCharCode(117,110,112,114,111,116,101,99,116,95,51,95,49,50,0);
         matchw -= parseFloat(`${videovarG.length}`);
         homeloadingH = [2 | homeloadingH.length];
         videovarG += `${homeloadingH.length}`;
      let colorsR = predictionwinD <= 5871995.0;
      do {
          let imageactionliveR = true;
          let relatedi = 5.0;
          let greenH = 3.0;
          let whistleorange0 = true;
          let privacyi = String.fromCharCode(105,103,110,111,114,101,100,95,107,95,57,49,0);
         predictionwinD /= Math.max(5, parseInt(`${relatedi}`) ^ 2);
         imageactionliveR = 65.32 >= greenH;
         relatedi *= 3 / (Math.max(6, privacyi.length));
         greenH -= parseFloat(`${1 ^ parseInt(`${greenH}`)}`);
         whistleorange0 = parseFloat(`${privacyi.length}`) > greenH;
         if (colorsR) {
            break;
         }
      } while (colorsR && (1.60 <= matchw));
         matchw *= parseFloat(`${parseInt(`${predictionwinD}`) % 2}`);
         matchw /= Math.max(2, (parseFloat(`${(whitesmalltick8 ? 2 : 3) + parseInt(`${matchw}`)}`)));
          let gestures8 = false;
          let megaphoneW = true;
         predictionwinD -= ((megaphoneW ? 5 : 3) & (gestures8 ? 1 : 3));
      let fcdaebecbcbafcdfceaaeccfeacdb6 = 6040895.0 >= matchw;
      do {
         matchw /= Math.max(1, parseFloat(`${parseInt(`${predictionwinD}`)}`));
         if (fcdaebecbcbafcdfceaaeccfeacdb6) {
            break;
         }
      } while (fcdaebecbcbafcdfceaaeccfeacdb6 && (matchw < 2.25 || 1.28 < (2.25 + matchw)));
         whitesmalltick8 = predictionwinD >= 83.87;
         whitesmalltick8 = matchw < predictionwinD;
      mutedL.set(rncorer, rncorer.length);
   }
       let carousel7: Array<any> = [34, 79];
       let yellowcirclebgf = 1.0;
       let alert0 = String.fromCharCode(110,101,108,108,121,109,111,115,101,114,95,57,95,49,55,0);
         carousel7 = [(alert0 == String.fromCharCode(107,0) ? carousel7.length : alert0.length)];
         yellowcirclebgf /= Math.max(parseFloat(`${3}`), 1);
         yellowcirclebgf *= parseFloat(`${3}`);
       let classesx = 3.0;
       let defaultlogoT = 2.0;
      if (!alert0.startsWith(`${carousel7.length}`)) {
         alert0 = `${parseInt(`${defaultlogoT}`) >> (Math.min(4, Math.abs(3)))}`;
      }
         classesx -= parseFloat(`${parseInt(`${yellowcirclebgf}`) ^ 1}`);
         classesx += parseFloat(`${carousel7.length << (Math.min(1, Math.abs(parseInt(`${classesx}`))))}`);
         carousel7 = [1];
      if ((carousel7.length + parseInt(`${classesx}`)) <= 1 && 2.20 <= (classesx + parseFloat(`${carousel7.length}`))) {
         carousel7 = [parseInt(`${defaultlogoT}`)];
      }
      controlsN += `${(String.fromCharCode(56,0) == time_59M ? time_59M.length : parseInt(`${pressuref}`))}`;
       let whitec = String.fromCharCode(100,97,116,97,98,97,115,101,115,95,111,95,49,50,0);
       let libfabricjniz = true;
       let bingB = String.fromCharCode(110,95,52,53,95,97,115,112,101,99,116,115,0);
       let pangleo = true;
      if (!libfabricjniz || !pangleo) {
          let owngoaln = 2;
          let textlayoutmanagera = String.fromCharCode(120,95,51,56,95,98,111,111,115,116,101,100,0);
          let pathP = true;
         pangleo = whitec.length < 22;
         owngoaln >>= Math.min(3, Math.abs(owngoaln % (Math.max(textlayoutmanagera.length, 1))));
         textlayoutmanagera += `${(String.fromCharCode(100,0) == textlayoutmanagera ? owngoaln : textlayoutmanagera.length)}`;
         pathP = !pathP || textlayoutmanagera.length <= 95;
      }
         whitec += `${whitec.length}`;
         bingB += `${whitec.length * 3}`;
      while (1 < whitec.length) {
          let megaphoneu = 3;
         libfabricjniz = !pangleo;
         megaphoneu <<= Math.min(Math.abs(megaphoneu), 1);
         break;
      }
         pangleo = !libfabricjniz || bingB.length > 26;
         pangleo = bingB.length < whitec.length;
         bingB = `${((pangleo ? 2 : 2) << (Math.min(bingB.length, 4)))}`;
         pangleo = whitec.startsWith(`${pangleo}`);
      launcherj = `${2 | bingB.length}`;

    return {
        type: SET_FROM_MINI_VOD_COLLECTION_ITEM,
        fromMiniVodCollectionItemIndex: index,
    }
}