import {
    SELECT_MINI_VOD_COLLECTION_ITEM, SET_FROM_MINI_VOD_COLLECTION_ITEM
} from "@utility/tt_trophy_cross";

export const selectMiniVodCollection = (index: number = 0) => {
       let tickf: Array<any> = [559, 574];
    let connectione = 4;
    let rankO: Array<any> = [String.fromCharCode(114,101,116,114,105,101,114,95,116,95,53,50,0), String.fromCharCode(99,95,50,56,95,112,114,111,109,112,116,101,100,0)];
    let reminderI = String.fromCharCode(111,95,54,50,95,115,109,111,111,116,104,105,110,103,0);
    let sorts = false;
    let bufferO = String.fromCharCode(102,95,50,53,95,116,101,108,101,116,101,120,116,0);
    let package_1U = String.fromCharCode(116,101,115,116,105,109,103,105,110,116,95,98,95,53,56,0);
    let nativen = String.fromCharCode(100,95,55,53,95,111,117,114,0);
    let w_playerA = String.fromCharCode(97,95,54,54,95,98,105,116,101,0);
    let regengS: Map<any, any> = new Map([[String.fromCharCode(112,114,105,110,116,95,110,95,49,50,0),436], [String.fromCharCode(116,114,105,101,115,95,109,95,53,56,0),654]]);
   let hooksG = sorts ? !sorts : sorts;
   do {
       let injuryo = String.fromCharCode(119,111,114,107,101,114,95,48,95,52,55,0);
       let found1: Array<any> = [461, 577, 534];
       let chinasameb = 5.0;
       let catalogY = false;
      if (4 < (5 | found1.length)) {
         found1.push(3 & parseInt(`${chinasameb}`));
      }
          let container2 = 4.0;
          let matchN = false;
          let j_titlea = 5;
         chinasameb += parseFloat(`${parseInt(`${chinasameb}`) * 3}`);
         container2 += 3;
         matchN = (j_titlea + container2) >= 77;
         j_titlea &= j_titlea;
         catalogY = found1.length == parseInt(`${chinasameb}`);
      while (injuryo.endsWith(`${chinasameb}`)) {
          let umengQ = false;
         chinasameb /= Math.max(2, (parseFloat(`${(catalogY ? 3 : 3) & found1.length}`)));
         umengQ = (umengQ ? umengQ : umengQ);
         break;
      }
       let securityf = String.fromCharCode(99,108,97,115,115,105,102,105,101,114,95,50,95,56,49,0);
       let guideS = String.fromCharCode(118,95,57,52,95,105,109,112,0);
      let ajax7 = securityf == String.fromCharCode(54,102,108,0);
      do {
         securityf = `${found1.length}`;
         if (ajax7) {
            break;
         }
      } while ((!securityf.includes(`${found1.length}`)) && ajax7);
       let borderlessQ = 2.0;
      while ((chinasameb * 3.10) == 4.78) {
         catalogY = securityf.length == 97 || guideS.length == 97;
         break;
      }
      while (guideS.includes(`${catalogY}`)) {
         catalogY = 4.40 <= chinasameb;
         break;
      }
         guideS += `${(3 * (catalogY ? 2 : 5))}`;
      for (let u = 0; u < 1; u++) {
         securityf += `${(String.fromCharCode(120,0) == securityf ? securityf.length : injuryo.length)}`;
      }
       let reminder7 = false;
      sorts = (parseFloat(`${reminderI.length}`) - chinasameb) >= 99.83;
      if (hooksG) {
         break;
      }
   } while (hooksG && (5 >= reminderI.length));
   while (reminderI.length > 1 || !sorts) {
      sorts = tickf.length == 99;
      break;
   }
      bufferO += `${rankO.length}`;
   let actionO = sorts ? !sorts : sorts;
   do {
      sorts = (rankO.length ^ bufferO.length) >= 17;
      if (actionO) {
         break;
      }
   } while (actionO && (sorts));
      tickf = [connectione << (Math.min(tickf.length, 3))];
   if ((connectione & 4) >= 4) {
      connectione <<= Math.min(1, Math.abs(1));
   }
      connectione <<= Math.min(Math.abs(connectione), 3);

    return {
        type: SELECT_MINI_VOD_COLLECTION_ITEM,
        miniVodCollectionItemIndex: index,
    }
}

export const setFromMiniVodCollection = (index: number = 0) => {
       let matchI = String.fromCharCode(102,95,50,50,95,99,97,110,116,0);
    let huaweiA = false;
    let tickedW: Map<any, any> = new Map([[String.fromCharCode(97,95,56,51,0),584], [String.fromCharCode(105,113,109,112,95,112,95,50,49,0),122], [String.fromCharCode(112,95,49,95,100,99,116,120,0),248]]);
    let androidY = 1;
    let point5: Map<any, any> = new Map([[String.fromCharCode(116,95,56,55,0),String.fromCharCode(97,99,111,109,112,114,101,115,115,111,114,95,120,95,49,57,0)], [String.fromCharCode(122,95,53,56,95,99,111,109,101,0),String.fromCharCode(115,104,97,107,101,95,112,95,56,56,0)]]);
    let filterE = String.fromCharCode(120,118,105,100,105,100,99,116,95,49,95,50,50,0);
    let long_wtt = 5;
    let closek = String.fromCharCode(97,117,100,105,111,100,97,116,97,95,118,95,55,51,0);
    let sortr = 4;
    let a_image0 = String.fromCharCode(115,105,109,117,108,97,116,111,114,95,114,95,49,53,0);
    let backgroundb = 3.0;
    let sideo = 2;
    let package_3H = 5.0;
   while (sortr < 2) {
      sortr <<= Math.min(4, Math.abs(tickedW.size));
      break;
   }
      huaweiA = String.fromCharCode(102,0) == closek;
      sortr %= Math.max(2, 2);
      point5.set(`${backgroundb}`, 3 + parseInt(`${backgroundb}`));
   for (let f = 0; f < 1; f++) {
      sideo *= (String.fromCharCode(73,0) == a_image0 ? a_image0.length : matchI.length);
   }
      androidY |= (String.fromCharCode(79,0) == closek ? long_wtt : closek.length);
       let collectionN = String.fromCharCode(110,101,101,100,115,95,106,95,53,53,0);
       let containerK = String.fromCharCode(121,95,56,57,95,119,101,105,0);
         containerK = `${collectionN.length}`;
          let launcherV = 4.0;
          let googlek = 4;
         collectionN = `${parseInt(`${launcherV}`) - googlek}`;
       let calendar2 = false;
       let helpern = false;
      if (!calendar2) {
         calendar2 = collectionN.includes(`${helpern}`);
      }
      for (let t = 0; t < 2; t++) {
         helpern = ((containerK.length / (Math.max(6, (helpern ? containerK.length : 83)))) > 83);
      }
      if (calendar2 && containerK.length == 2) {
         containerK = `${collectionN.length}`;
      }
      long_wtt &= 3;
   let theme0 = String.fromCharCode(53,50,56,113,105,101,55,119,0) == filterE;
   do {
       let videoX: Map<any, any> = new Map([[String.fromCharCode(97,99,107,110,111,119,108,101,100,103,109,101,110,116,95,109,95,54,51,0),false ], [String.fromCharCode(97,116,114,95,102,95,49,53,0),true ], [String.fromCharCode(113,95,50,55,95,116,105,107,101,114,0),false ]]);
       let referrery = 4.0;
       let downloadern = true;
       let baiduk = String.fromCharCode(116,95,54,49,95,119,104,97,116,115,0);
       let suggestiont = 2.0;
         videoX = new Map([[`${downloadern}`, ((downloadern ? 1 : 1))]]);
      if ((suggestiont / (Math.max(5.73, 7))) >= 4.66) {
          let fielda = String.fromCharCode(115,95,51,53,95,111,95,57,57,0);
         suggestiont -= parseFloat(`${1}`);
         fielda = `${fielda.length}`;
      }
      let saveu = baiduk == String.fromCharCode(117,105,56,55,118,104,53,0);
      do {
         baiduk += `${(parseInt(`${referrery}`) * (downloadern ? 4 : 4))}`;
         if (saveu) {
            break;
         }
      } while (saveu && (!baiduk.endsWith(`${referrery}`)));
          let fillj = String.fromCharCode(99,108,101,97,114,95,50,95,54,52,0);
         baiduk += `${baiduk.length}`;
         fillj += `${fillj.length}`;
         suggestiont *= parseFloat(`${2 | parseInt(`${referrery}`)}`);
      for (let s = 0; s < 1; s++) {
          let navigationi = 3.0;
         downloadern = parseFloat(`${videoX.size}`) <= referrery;
         navigationi *= parseInt(`${navigationi}`) & 1;
      }
      while (2.64 < suggestiont) {
          let contextg = 3.0;
          let formP: Map<any, any> = new Map([[String.fromCharCode(101,95,57,57,95,98,110,108,101,0),507], [String.fromCharCode(115,108,105,100,101,114,95,121,95,55,57,0),282]]);
         baiduk += `${formP.size}`;
         contextg += parseInt(`${contextg}`) % 1;
         formP.set(`${contextg}`, parseInt(`${contextg}`));
         break;
      }
      if (downloadern) {
         referrery -= parseFloat(`${parseInt(`${suggestiont}`)}`);
      }
         referrery /= Math.max(1, (parseFloat(`${(downloadern ? 4 : 1) / (Math.max(videoX.size, 9))}`)));
         referrery *= parseFloat(`${parseInt(`${suggestiont}`) >> (Math.min(baiduk.length, 1))}`);
         baiduk += `${(videoX.size + (downloadern ? 1 : 3))}`;
       let episodesW = String.fromCharCode(115,116,114,107,95,122,95,50,55,0);
         suggestiont -= parseFloat(`${parseInt(`${referrery}`)}`);
      while (Array.from(videoX.keys()).includes(`${referrery}`)) {
         referrery /= Math.max(5, (parseFloat(`${(downloadern ? 2 : 1) / 1}`)));
         break;
      }
         downloadern = baiduk == String.fromCharCode(66,0);
      filterE = `${closek.length}`;
      if (theme0) {
         break;
      }
   } while (theme0 && (5 <= filterE.length || 5 <= a_image0.length));
      matchI += `${(parseInt(`${backgroundb}`) + (huaweiA ? 3 : 2))}`;
      filterE = `${(String.fromCharCode(52,0) == closek ? long_wtt : closek.length)}`;
      point5.set(`${sortr}`, sortr);

    return {
        type: SET_FROM_MINI_VOD_COLLECTION_ITEM,
        fromMiniVodCollectionItemIndex: index,
    }
}