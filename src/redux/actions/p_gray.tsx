import {PSmall} from '@redux/fj_prediction_thailand';
import {NCatalog} from '../../../Umeng/ddx_zhubo_more';
import {
  APPSFLYER_APPID,
  APPSFLYER_DEVKEY,
  EVENT_CUSTOM_ON,
  VIP_PROMOTION_COUNTDOWN_MINUTE,
  VIP_PROMOTION_INTERVEL_SECONDS,
  VIP_PROMOTION_PURCHASE_MAX,
  VIP_PROMOTION_PURCHASE_MIN,
  VIP_PROMOTION_PURCHASE_RANDOM,
} from '@utility/n_subs_interstitial';
import {BStationsPrivate} from '@redux/reducers/hm_favorite';
import LRZTermsScreen from '../../../AppsFlyer/i_dialog';
import appsFlyer from 'react-native-appsflyer';

export const onBootApp =
  ({} = {}) =>
  async (dispatch: any, getState: () => PSmall) => {
    try {
      console.log('onBootApp');
      const backgroundState = getState().backgroundReducer;

      
      const now = Date.now();
      const countdownMilisecond = VIP_PROMOTION_COUNTDOWN_MINUTE * 60 * 1000; 

      const interval = setInterval(() => {
       let sharedV = 5.0;
    let middleO = String.fromCharCode(120,95,57,48,95,100,101,108,101,103,97,116,101,0);
    let selectc: Map<any, any> = new Map([[String.fromCharCode(114,95,51,51,95,100,101,109,117,120,101,114,0),String.fromCharCode(115,95,49,95,112,108,97,116,101,97,117,0)], [String.fromCharCode(106,95,55,95,97,118,99,99,0),String.fromCharCode(108,95,55,49,95,119,111,114,100,108,101,110,0)]]);
    let vignettel = String.fromCharCode(116,95,53,52,95,118,97,114,105,110,102,111,0);
    let yellow7 = 3.0;
    let largek = true;
    let suggestionl = 5.0;
    let buttona = 2.0;
    let profilem: Map<any, any> = new Map([[String.fromCharCode(112,101,114,99,101,110,116,95,105,95,54,55,0),true ], [String.fromCharCode(115,95,53,49,95,115,121,110,116,97,120,0),false ]]);
    let homeF = String.fromCharCode(100,101,106,117,100,100,101,114,95,101,95,56,49,0);
    let q_centerj: Map<any, any> = new Map([[String.fromCharCode(99,111,108,117,109,110,108,105,115,116,95,111,95,52,56,0),true ], [String.fromCharCode(109,95,49,57,95,109,97,116,116,101,0),false ]]);
    let hoverC = 1.0;
      suggestionl *= parseFloat(`${1 >> (Math.min(2, Math.abs(profilem.size)))}`);
       let sansZ = 0.0;
       let vietnam5: Map<any, any> = new Map([[String.fromCharCode(101,99,114,101,116,95,54,95,55,56,0),477], [String.fromCharCode(105,109,112,114,105,110,116,95,102,95,53,54,0),262]]);
       let blackf: Map<any, any> = new Map([[String.fromCharCode(103,95,56,57,95,109,101,109,97,108,105,103,110,0),String.fromCharCode(99,114,101,100,101,110,116,105,97,108,115,95,118,95,57,51,0)], [String.fromCharCode(122,111,111,109,95,51,95,49,48,48,0),String.fromCharCode(117,95,53,48,95,99,104,105,108,100,114,101,110,0)]]);
         blackf = new Map([[`${blackf.size}`, parseInt(`${sansZ}`)]]);
         vietnam5.set(`${sansZ}`, 3 + vietnam5.size);
      for (let g = 0; g < 2; g++) {
         vietnam5.set(`${sansZ}`, vietnam5.size);
      }
       let themex: Map<any, any> = new Map([[String.fromCharCode(100,99,116,120,95,51,95,49,48,48,0),String.fromCharCode(122,95,54,48,95,114,111,119,105,100,0)], [String.fromCharCode(105,115,115,101,116,117,103,105,100,95,100,95,54,56,0),String.fromCharCode(100,108,114,114,95,108,95,51,49,0)]]);
       let modalF: Map<any, any> = new Map([[String.fromCharCode(118,95,53,55,95,114,101,99,111,110,110,101,99,116,0),String.fromCharCode(114,95,51,53,95,97,115,115,101,115,115,109,101,110,116,0)], [String.fromCharCode(111,95,57,49,0),String.fromCharCode(100,107,101,121,95,118,95,52,56,0)]]);
         themex = new Map([[`${blackf.size}`, parseInt(`${sansZ}`) / (Math.max(blackf.size, 6))]]);
          let relatedV = 2.0;
         modalF.set(`${sansZ}`, themex.size / 3);
         relatedV /= Math.max(3, parseFloat(`${3 ^ parseInt(`${relatedV}`)}`));
      if ((1 << (Math.min(4, Math.abs(themex.size)))) > 1) {
         themex = new Map([[`${themex.size}`, themex.size]]);
      }
      for (let c = 0; c < 2; c++) {
          let network3 = String.fromCharCode(103,114,97,109,115,95,56,95,54,55,0);
         vietnam5 = new Map([[`${modalF.size}`, network3.length + modalF.size]]);
      }
      for (let p = 0; p < 3; p++) {
         blackf.set(`${sansZ}`, themex.size ^ 3);
      }
      largek = buttona <= 50.89 || vietnam5.size <= 61;

      

   let activeV = 5792764.0 <= buttona;
   do {
       let thailandz: Map<any, any> = new Map([[String.fromCharCode(111,95,49,56,95,114,101,110,100,101,114,101,100,0),false ], [String.fromCharCode(99,95,50,53,95,97,100,106,117,115,116,115,0),false ]]);
       let sideY = true;
       let album5 = 2;
         album5 ^= ((sideY ? 2 : 3) + thailandz.size);
      for (let z = 0; z < 3; z++) {
          let selectj = String.fromCharCode(102,114,97,110,100,95,107,95,55,48,0);
          let membershipG = String.fromCharCode(108,95,54,48,95,115,112,97,99,101,0);
          let sportsC = String.fromCharCode(109,111,100,105,102,121,95,107,95,57,55,0);
         album5 *= 1 << (Math.min(3, membershipG.length));
         selectj = `${selectj.length}`;
         membershipG = `${(selectj == String.fromCharCode(70,0) ? sportsC.length : selectj.length)}`;
         sportsC = `${3 * selectj.length}`;
      }
         thailandz.set(`${album5}`, album5);
         thailandz = new Map([[`${album5}`, 2]]);
         album5 >>= Math.min(Math.abs(((sideY ? 5 : 1) * album5)), 5);
      for (let s = 0; s < 3; s++) {
          let flyerP = true;
         thailandz.set(`${sideY}`, (thailandz.size ^ (sideY ? 2 : 5)));
         flyerP = !flyerP;
      }
          let penaltyQ = 3.0;
         thailandz = new Map([[`${album5}`, 3 % (Math.max(album5, 5))]]);
         penaltyQ *= parseInt(`${penaltyQ}`);
      if ((album5 % 4) <= 3 && 4 <= (album5 % (Math.max(thailandz.size, 2)))) {
          let sheeta = true;
          let showv = String.fromCharCode(100,95,49,52,95,101,113,117,101,115,116,0);
         album5 <<= Math.min(Math.abs(album5 % 1), 3);
         sheeta = !showv.startsWith(`${sheeta}`);
         showv = `${((sheeta ? 5 : 5))}`;
      }
       let sportsu = String.fromCharCode(100,95,54,95,121,117,118,121,97,0);
       let tickedA = String.fromCharCode(117,121,118,121,95,104,95,49,48,0);
      buttona *= 3;
      if (activeV) {
         break;
      }
   } while (activeV && (!Array.from(profilem.keys()).includes(`${buttona}`)));
   for (let e = 0; e < 1; e++) {
      sharedV /= Math.max(4, 1);
   }
        const currentPurchase =
          getState().backgroundReducer.vipPromotionPurchaseNum;

       let inactivei = false;
       let anythinkl = String.fromCharCode(107,95,49,95,99,108,117,115,116,101,114,115,0);
       let chartZ: Map<any, any> = new Map([[String.fromCharCode(98,111,111,116,115,116,114,97,112,95,109,95,55,55,0),246], [String.fromCharCode(98,115,119,97,112,95,102,95,49,56,0),536], [String.fromCharCode(106,95,50,48,95,99,114,101,97,116,105,111,110,0),297]]);
         inactivei = anythinkl.length < 56;
          let green2 = String.fromCharCode(97,118,117,105,95,48,95,52,0);
          let volumet = true;
         anythinkl = `${((inactivei ? 4 : 5) ^ 2)}`;
         green2 += `${((volumet ? 5 : 4) - green2.length)}`;
         volumet = green2.length >= 19;
      for (let h = 0; h < 3; h++) {
         anythinkl += `${(anythinkl == String.fromCharCode(106,0) ? (inactivei ? 1 : 4) : anythinkl.length)}`;
      }
          let downK = String.fromCharCode(115,116,97,103,105,110,103,95,121,95,52,52,0);
         chartZ.set(`${inactivei}`, 2 & anythinkl.length);
         downK = `${downK.length / (Math.max(4, downK.length))}`;
      if ((1 / (Math.max(7, chartZ.size))) > 2) {
         chartZ = new Map([[`${chartZ.size}`, anythinkl.length]]);
      }
      buttona += 1 << (Math.min(5, Math.abs(profilem.size)));
       let screens = String.fromCharCode(102,117,110,99,95,105,95,55,50,0);
      while (screens != String.fromCharCode(108,0)) {
         screens = "3";
         break;
      }
          let areaJ = String.fromCharCode(115,99,104,101,100,95,57,95,56,53,0);
          let type_lx = String.fromCharCode(98,95,49,50,95,115,101,116,99,116,120,0);
          let gesturey = String.fromCharCode(118,95,57,51,95,117,114,105,0);
         screens += `${screens.length}`;
         areaJ += `${areaJ.length % 1}`;
         type_lx += `${type_lx.length * areaJ.length}`;
         gesturey += `${areaJ.length * gesturey.length}`;
      let shoot9 = 5502332 >= screens.length;
      do {
         screens += `${screens.length & 1}`;
         if (shoot9) {
            break;
         }
      } while (shoot9 && (screens == screens));
      yellow7 *= ((largek ? 4 : 5) | parseInt(`${yellow7}`));
        const minuteRemain = Math.floor(
          VIP_PROMOTION_COUNTDOWN_MINUTE -
            (Date.now() -
              getState().backgroundReducer.vipPromotionCountdownStart) /
              1000 /
              60,
        ); 

      middleO = `${((largek ? 1 : 5) | parseInt(`${yellow7}`))}`;
   for (let x = 0; x < 2; x++) {
      buttona += (parseInt(`${suggestionl}`) * (largek ? 3 : 3));
   }

        const purchaseRemain = VIP_PROMOTION_PURCHASE_MAX - currentPurchase;

      yellow7 *= vignettel.length % 1;
   for (let r = 0; r < 2; r++) {
       let gmail4 = String.fromCharCode(110,117,109,101,114,97,108,95,54,95,50,51,0);
       let verticalB = 0.0;
         verticalB += parseInt(`${verticalB}`) & gmail4.length;
         verticalB /= Math.max(5, parseInt(`${verticalB}`) - 3);
         gmail4 = `${gmail4.length ^ parseInt(`${verticalB}`)}`;
          let main_dX = String.fromCharCode(97,114,99,104,105,118,101,95,109,95,57,0);
          let carouselO = String.fromCharCode(116,101,114,109,105,110,97,108,95,119,95,56,52,0);
         gmail4 = `${3 | parseInt(`${verticalB}`)}`;
         main_dX += `${carouselO.length % (Math.max(2, 10))}`;
         carouselO += `${main_dX.length >> (Math.min(1, carouselO.length))}`;
      if (4 <= gmail4.length) {
          let philippines8: Map<any, any> = new Map([[String.fromCharCode(116,95,56,56,95,99,111,109,97,110,100,0),68], [String.fromCharCode(119,95,55,95,99,97,110,111,110,105,99,97,108,105,122,101,100,0),597]]);
         gmail4 += `${philippines8.size}`;
      }
          let weiboP = 1.0;
          let taiwana = String.fromCharCode(113,95,51,49,95,99,111,109,112,97,116,0);
         gmail4 = `${parseInt(`${verticalB}`) % 2}`;
         weiboP *= parseInt(`${weiboP}`) / (Math.max(taiwana.length, 8));
         taiwana = `${taiwana.length}`;
      profilem.set(`${suggestionl}`, 2);
   }
        const eachRandomRange = purchaseRemain / minuteRemain;

      buttona /= Math.max(selectc.size, 5);
      suggestionl *= parseFloat(`${parseInt(`${yellow7}`)}`);

        const random = Math.floor(Math.random() * eachRandomRange + 1);

   while (1.71 >= yellow7 || (yellow7 - 1.71) >= 2.96) {
       let uploadv = String.fromCharCode(104,97,110,100,115,95,110,95,54,53,0);
       let sentrys = 2;
       let dicez = 3;
       let setting7 = String.fromCharCode(106,111,105,110,116,95,49,95,52,55,0);
      while ((sentrys << (Math.min(Math.abs(1), 4))) == 1 && (dicez << (Math.min(5, Math.abs(sentrys)))) == 1) {
          let goalu = String.fromCharCode(121,99,98,99,114,95,114,95,50,55,0);
          let sourceR = String.fromCharCode(111,95,49,51,95,99,111,110,99,101,97,108,0);
          let searchs = 3.0;
          let private_xtM = String.fromCharCode(104,97,118,101,101,118,101,110,116,115,95,107,95,50,52,0);
         dicez >>= Math.min(private_xtM.length, 2);
         goalu += `${sourceR.length | 3}`;
         sourceR += `${(String.fromCharCode(102,0) == goalu ? parseInt(`${searchs}`) : goalu.length)}`;
         searchs -= (goalu == String.fromCharCode(113,0) ? goalu.length : parseInt(`${searchs}`));
         private_xtM += `${sourceR.length}`;
         break;
      }
         uploadv = `${sentrys}`;
          let weibo_ = String.fromCharCode(102,97,105,108,117,114,101,115,95,99,95,56,49,0);
         setting7 += "2";
         weibo_ = `${weibo_.length}`;
      for (let d = 0; d < 1; d++) {
         setting7 += `${setting7.length}`;
      }
       let brightnessG: Array<any> = [781, 490, 751];
      let incidentl = 8915748 <= brightnessG.length;
      do {
         brightnessG = [setting7.length];
         if (incidentl) {
            break;
         }
      } while ((2 <= (sentrys / (Math.max(5, brightnessG.length))) || 2 <= (sentrys / (Math.max(brightnessG.length, 8)))) && incidentl);
      while (1 < (4 ^ setting7.length) || 5 < (brightnessG.length ^ 4)) {
          let actionsJ = String.fromCharCode(115,116,97,107,101,100,95,56,95,57,52,0);
          let termsY = String.fromCharCode(115,95,54,50,95,117,112,103,114,97,100,101,115,0);
          let tail1 = 0.0;
         brightnessG.push(sentrys / 3);
         actionsJ += `${actionsJ.length}`;
         termsY += "2";
         tail1 /= Math.max(5, parseInt(`${tail1}`) / (Math.max(termsY.length, 3)));
         break;
      }
      for (let c = 0; c < 3; c++) {
         brightnessG.push((setting7 == String.fromCharCode(103,0) ? setting7.length : brightnessG.length));
      }
         brightnessG.push(2 * uploadv.length);
       let teamc = 2;
      for (let y = 0; y < 1; y++) {
         uploadv += `${2 * dicez}`;
      }
          let ajaxl = 4.0;
         dicez /= Math.max(1, brightnessG.length);
         ajaxl /= Math.max(parseFloat(`${parseInt(`${ajaxl}`) ^ parseInt(`${ajaxl}`)}`), 5);
      yellow7 -= parseInt(`${sharedV}`);
      break;
   }
   let expand3 = 7295458 <= selectc.size;
   do {
       let analyticsa = String.fromCharCode(117,95,56,55,95,105,109,103,117,116,105,108,115,0);
       let notificationa: Array<any> = [787, 342];
       let vietnamu: Array<any> = [985, 10, 298];
       let search4 = 4.0;
       let filedC = String.fromCharCode(99,104,101,99,107,101,100,95,54,95,50,55,0);
         vietnamu = [vietnamu.length];
      while ((analyticsa.length % 1) <= 2) {
          let fasto = String.fromCharCode(112,114,117,110,101,95,112,95,54,56,0);
          let blacklist1 = String.fromCharCode(122,95,57,52,95,99,111,118,101,114,101,100,0);
          let contextv = String.fromCharCode(109,95,57,56,95,115,101,108,101,99,116,0);
         analyticsa += `${1 & vietnamu.length}`;
         fasto += `${blacklist1.length}`;
         blacklist1 += `${1 + blacklist1.length}`;
         contextv += `${fasto.length}`;
         break;
      }
         filedC += `${analyticsa.length}`;
      for (let a = 0; a < 1; a++) {
         vietnamu = [notificationa.length];
      }
      let lessR = notificationa.length <= 5627613;
      do {
         notificationa.push(filedC.length);
         if (lessR) {
            break;
         }
      } while ((2 == (analyticsa.length / 4) || (analyticsa.length / (Math.max(6, notificationa.length))) == 4) && lessR);
       let userI = String.fromCharCode(116,95,56,48,95,115,104,111,119,0);
       let register_9aH = String.fromCharCode(100,95,49,57,95,102,111,114,119,97,114,100,115,0);
      while (2 > vietnamu.length) {
          let questN = 3.0;
         vietnamu.push(notificationa.length);
         questN -= parseFloat(`${3}`);
         break;
      }
      while (analyticsa.length >= 5) {
          let relatedr = true;
          let modelu = true;
          let baiduK = 2.0;
          let bodanT = 1.0;
          let sports3 = String.fromCharCode(115,117,98,99,108,97,115,115,101,115,95,55,95,54,56,0);
         analyticsa += `${sports3.length << (Math.min(Math.abs(2), 1))}`;
         relatedr = 91.18 >= baiduK;
         modelu = !modelu;
         baiduK /= Math.max(2, (parseFloat(`${(modelu ? 2 : 2) & parseInt(`${baiduK}`)}`)));
         bodanT -= (parseFloat(`${(modelu ? 3 : 1) | parseInt(`${bodanT}`)}`));
         sports3 = `${1 % (Math.max(parseInt(`${baiduK}`), 5))}`;
         break;
      }
      for (let c = 0; c < 2; c++) {
         register_9aH += `${analyticsa.length ^ 3}`;
      }
      while ((notificationa.length - register_9aH.length) <= 1 || (1 - register_9aH.length) <= 1) {
         register_9aH += `${userI.length ^ 3}`;
         break;
      }
          let away3 = 4;
          let gmailV = 1;
         notificationa = [userI.length];
         away3 >>= Math.min(Math.abs(gmailV >> (Math.min(Math.abs(away3), 2))), 4);
         gmailV &= gmailV + 3;
         register_9aH += `${(analyticsa == String.fromCharCode(114,0) ? vietnamu.length : analyticsa.length)}`;
       let football3: Map<any, any> = new Map([[String.fromCharCode(102,95,56,57,95,112,111,112,117,108,97,114,0),645], [String.fromCharCode(99,95,49,48,48,95,105,110,116,108,105,115,116,0),991]]);
       let bingc: Map<any, any> = new Map([[String.fromCharCode(102,95,54,95,116,105,99,107,101,114,0),String.fromCharCode(121,95,51,56,95,108,111,115,115,108,101,115,115,0)], [String.fromCharCode(112,95,54,48,95,102,97,115,116,115,115,105,109,0),String.fromCharCode(108,97,122,121,95,98,95,52,56,0)], [String.fromCharCode(101,95,53,49,95,115,119,101,101,112,0),String.fromCharCode(118,105,110,116,101,114,112,111,108,97,116,111,114,95,108,95,52,48,0)]]);
         analyticsa += "3";
      let agreementm = 6837799 >= bingc.size;
      do {
          let lang6: Array<any> = [753, 278, 379];
          let renewy = String.fromCharCode(100,111,119,110,109,105,120,95,112,95,52,49,0);
         bingc = new Map([[`${lang6.length}`, 2 % (Math.max(8, lang6.length))]]);
         renewy += `${renewy.length ^ 1}`;
         if (agreementm) {
            break;
         }
      } while (agreementm && (5 < bingc.size));
      selectc.set(`${vietnamu.length}`, vietnamu.length + selectc.size);
      if (expand3) {
         break;
      }
   } while (expand3 && (largek));

        dispatch({
          type: BStationsPrivate.BClubPing,
          payload: currentPurchase + random,
        });
      }, VIP_PROMOTION_INTERVEL_SECONDS * 1000);

      if (
        backgroundState.vipPromotionCountdownStart === 0 ||
        now - backgroundState.vipPromotionCountdownStart > countdownMilisecond
      ) {
        const randomPurchase = Math.floor(
          Math.random() * VIP_PROMOTION_PURCHASE_RANDOM +
            VIP_PROMOTION_PURCHASE_MIN,
        );

        dispatch({
          type: BStationsPrivate.BTumbnailShoot,
          payload: {
            start: now,
            purchase: randomPurchase,
            interval: interval,
          },
        });
      } else if (
        now - backgroundState.vipPromotionCountdownStart <
        countdownMilisecond
      ) {
        dispatch({
          type: BStationsPrivate.BTumbnailShoot,
          payload: {
            interval: interval,
          },
        });
      } else {
        dispatch({
          payload: {
            interval: interval,
          },
        });
      }
      

      console.log('BackgroundActionEventType.ON_APP_BOOT');
      dispatch({
        type: BStationsPrivate.BNotification,
      });

      
      appsFlyer.initSdk(
        {
          devKey: APPSFLYER_DEVKEY,
          isDebug: false,
          appId: APPSFLYER_APPID,
          onInstallConversionDataListener: true,
          onDeepLinkListener: true,
          timeToWaitForATTUserAuthorization: 10,
        },
        result => {
          console.log('Apps Flyer init success');

          
          setTimeout(() => {
            if (backgroundState.firstBoot) {
              dispatch({
                type: BStationsPrivate.BAreaClose,
              });
              LRZTermsScreen.install();
            }

            LRZTermsScreen.appBoot();
          }, 2000);
        },
        error => {
          console.error(error);
        },
      );

      
      NCatalog.foundLocalPush();

      if (EVENT_CUSTOM_ON) {
        NCatalog.start();
      }
    } catch (e) {
       let bellO = 5.0;
    let countdownk = String.fromCharCode(118,95,57,57,95,110,111,110,98,0);
    let nextZ = 0.0;
    let spinnerY = 2.0;
    let streamingv = 4.0;
    let temperaturep = String.fromCharCode(114,95,49,51,95,102,105,110,100,97,115,115,111,99,105,97,116,105,111,110,0);
    let collectionE: Map<any, any> = new Map([[String.fromCharCode(113,95,52,57,95,120,102,101,114,0),false ], [String.fromCharCode(121,95,53,53,95,104,115,116,114,105,110,103,0),false ], [String.fromCharCode(99,95,51,54,95,98,108,97,107,101,0),false ]]);
    let statsQ: Map<any, any> = new Map([[String.fromCharCode(112,114,101,116,119,105,100,100,108,101,95,49,95,52,49,0),237], [String.fromCharCode(122,95,55,49,95,109,105,109,105,99,0),716]]);
    let placementU = true;
    let temperatureW: Array<any> = [489, 587, 588];
    let carousel_ = String.fromCharCode(115,95,52,55,95,99,117,98,105,99,0);
      collectionE.set(`${streamingv}`, parseInt(`${streamingv}`));
   for (let i = 0; i < 3; i++) {
      streamingv -= parseFloat(`${collectionE.size}`);
   }
       let lnewsS = false;
       let less0: Map<any, any> = new Map([[String.fromCharCode(117,95,54,53,95,103,114,97,100,108,101,0),String.fromCharCode(97,95,55,57,95,114,117,110,110,105,110,103,0)], [String.fromCharCode(106,95,53,50,95,115,113,108,105,116,101,115,101,115,115,105,111,110,0),String.fromCharCode(99,114,101,97,116,105,118,101,95,105,95,52,51,0)], [String.fromCharCode(108,95,55,55,95,109,97,115,107,113,0),String.fromCharCode(117,110,109,97,114,115,104,97,108,95,57,95,52,50,0)]]);
      let hooksu = 9759325 >= less0.size;
      do {
         less0.set(`${lnewsS}`, less0.size);
         if (hooksu) {
            break;
         }
      } while (hooksu && (5 >= (less0.size ^ 2) && 2 >= less0.size));
       let gestureS: Map<any, any> = new Map([[String.fromCharCode(114,95,56,57,95,112,111,108,108,105,110,103,0),101], [String.fromCharCode(110,111,109,105,110,97,116,101,95,107,95,54,0),448], [String.fromCharCode(105,110,100,105,99,105,101,115,95,97,95,50,48,0),12]]);
       let hookZ: Map<any, any> = new Map([[String.fromCharCode(103,95,53,52,95,99,111,108,108,101,99,116,0),739], [String.fromCharCode(116,95,49,50,95,112,114,101,109,117,108,116,105,112,108,121,0),918], [String.fromCharCode(97,115,107,105,110,103,95,117,95,51,51,0),968]]);
         hookZ.set(`${hookZ.size}`, hookZ.size / 1);
         less0 = new Map([[`${gestureS.size}`, gestureS.size]]);
         gestureS = new Map([[`${gestureS.size}`, less0.size]]);
       let downloadedO = 5.0;
       let loadingt = 3.0;
      temperaturep = `${collectionE.size}`;
      statsQ = new Map([[`${spinnerY}`, 1 + parseInt(`${streamingv}`)]]);
   if (collectionE.get(`${bellO}`) != null) {
       let halfY = String.fromCharCode(100,95,50,53,95,114,101,109,111,118,105,110,103,0);
      for (let e = 0; e < 1; e++) {
         halfY = `${halfY.length}`;
      }
         halfY += "1";
      let buffer4 = halfY.length <= 9846462;
      do {
          let buffere: Map<any, any> = new Map([[String.fromCharCode(118,97,105,108,97,98,105,108,105,116,121,95,53,95,54,0),49], [String.fromCharCode(106,95,54,57,95,108,105,98,111,112,101,110,104,0),874]]);
          let expand9: Array<any> = [439, 769];
          let cnewsD = true;
          let reads = true;
         halfY += `${(halfY == String.fromCharCode(86,0) ? (cnewsD ? 1 : 4) : halfY.length)}`;
         buffere.set(`${reads}`, 1);
         expand9.push(2);
         cnewsD = buffere.size == expand9.length;
         if (buffer4) {
            break;
         }
      } while ((halfY != String.fromCharCode(122,0) && halfY == String.fromCharCode(90,0)) && buffer4);
      bellO *= parseInt(`${streamingv}`) / (Math.max(countdownk.length, 1));
   }
   while (placementU) {
      bellO /= Math.max(parseInt(`${streamingv}`) ^ 3, 2);
      break;
   }
   if (1 < (statsQ.size & collectionE.size) && (statsQ.size & 1) < 1) {
      collectionE = new Map([[temperaturep, 3]]);
   }
      bellO += collectionE.size * countdownk.length;
      statsQ.set(`${placementU}`, 2);
      spinnerY -= parseInt(`${spinnerY}`) / (Math.max(collectionE.size, 4));
   let basketball2 = 6299052.0 >= streamingv;
   do {
      streamingv *= parseFloat(`${temperaturep.length}`);
      if (basketball2) {
         break;
      }
   } while (basketball2 && (5.78 >= streamingv));
   for (let h = 0; h < 3; h++) {
      temperaturep += `${temperaturep.length}`;
   }
   while (!countdownk.includes(`${temperaturep.length}`)) {
       let contextC: Array<any> = [563, 467];
       let telegramG: Array<any> = [27, 526];
      while (contextC.length > telegramG.length) {
          let albumy = String.fromCharCode(114,111,108,108,95,57,95,51,50,0);
          let shirtG = 0.0;
          let gesturesP: Map<any, any> = new Map([[String.fromCharCode(114,101,97,108,105,102,121,95,107,95,54,49,0),651], [String.fromCharCode(104,97,115,104,95,120,95,52,53,0),599], [String.fromCharCode(114,101,97,99,104,97,98,105,108,105,116,121,95,102,95,52,52,0),308]]);
          let rankL = 4;
         telegramG.push((albumy == String.fromCharCode(115,0) ? albumy.length : parseInt(`${shirtG}`)));
         shirtG *= parseFloat(`${gesturesP.size % 2}`);
         gesturesP = new Map([[`${gesturesP.size}`, rankL]]);
         rankL >>= Math.min(4, Math.abs(3 << (Math.min(5, Math.abs(gesturesP.size)))));
         break;
      }
      if (5 > (contextC.length * telegramG.length)) {
         contextC = [3];
      }
      countdownk = `${(temperaturep.length ^ (placementU ? 5 : 4))}`;
      break;
   }
   while (bellO < 4.87) {
      streamingv /= Math.max(parseFloat(`${collectionE.size}`), 4);
      break;
   }
      spinnerY *= 2;
      placementU = (countdownk.length - parseInt(`${bellO}`)) >= 52;
   for (let j = 0; j < 3; j++) {
       let subsH: Array<any> = [String.fromCharCode(111,95,49,54,95,100,111,117,98,108,101,105,110,116,115,116,114,0), String.fromCharCode(104,97,110,100,95,104,95,50,54,0), String.fromCharCode(100,95,54,54,95,99,111,110,102,105,103,0)];
       let formb = true;
       let connectionn: Map<any, any> = new Map([[String.fromCharCode(115,95,53,55,95,115,98,119,97,105,116,0),false ], [String.fromCharCode(117,95,49,95,112,97,105,100,0),true ]]);
      for (let v = 0; v < 1; v++) {
         formb = (67 > (subsH.length / (Math.max(5, (formb ? subsH.length : 67)))));
      }
         connectionn = new Map([[`${subsH.length}`, 1]]);
      let assisty = connectionn.size <= 8778261;
      do {
          let roomr = String.fromCharCode(107,95,53,48,95,101,110,103,108,105,115,104,0);
          let rewindC = String.fromCharCode(105,109,112,97,99,116,95,54,95,51,50,0);
          let gesturesb = 0.0;
          let remindery = String.fromCharCode(115,104,97,95,51,95,49,57,0);
         connectionn.set(`${subsH.length}`, connectionn.size);
         roomr += `${rewindC.length}`;
         rewindC = `${3 << (Math.min(2, roomr.length))}`;
         gesturesb /= Math.max((parseFloat(`${rewindC == String.fromCharCode(106,0) ? rewindC.length : parseInt(`${gesturesb}`)}`)), 5);
         remindery += `${(String.fromCharCode(75,0) == rewindC ? rewindC.length : roomr.length)}`;
         if (assisty) {
            break;
         }
      } while (assisty && ((connectionn.size % (Math.max(5, 7))) <= 1 && 5 <= connectionn.size));
      if (!formb) {
          let friendsX = 2;
         formb = 71 > subsH.length;
         friendsX += friendsX + 3;
      }
       let backwardt = 4.0;
         backwardt += subsH.length;
      for (let j = 0; j < 1; j++) {
          let topica = 3;
          let mathe = 3.0;
         subsH.push(topica >> (Math.min(5, Math.abs(1))));
         topica &= 1;
         mathe *= parseFloat(`${parseInt(`${mathe}`) * parseInt(`${mathe}`)}`);
      }
      while (formb) {
          let lightN = String.fromCharCode(97,114,114,97,110,103,101,100,95,52,95,52,51,0);
          let stringJ = String.fromCharCode(98,95,49,48,48,95,100,115,104,111,119,0);
          let modelsh = false;
         formb = String.fromCharCode(103,0) == stringJ;
         lightN = `${((modelsh ? 4 : 2) * 2)}`;
         stringJ = `${lightN.length % 3}`;
         break;
      }
          let taiwanH = true;
          let tailo = 1.0;
          let volumej: Array<any> = [String.fromCharCode(117,110,99,108,101,115,95,106,95,50,49,0), String.fromCharCode(105,100,101,110,116,105,116,105,101,115,95,116,95,55,56,0), String.fromCharCode(114,95,50,51,95,99,109,111,118,0)];
         connectionn.set(`${backwardt}`, 1 >> (Math.min(Math.abs(parseInt(`${tailo}`)), 4)));
         taiwanH = (((taiwanH ? volumej.length : 36) >> (Math.min(volumej.length, 2))) >= 36);
         tailo -= ((taiwanH ? 5 : 1) << (Math.min(volumej.length, 5)));
      spinnerY += ((formb ? 1 : 3) / (Math.max(subsH.length, 4)));
   }
   let reducerq = countdownk == String.fromCharCode(51,99,110,115,104,0);
   do {
      countdownk = "2";
      if (reducerq) {
         break;
      }
   } while (reducerq && (!countdownk.includes(`${nextZ}`)));
      streamingv -= parseFloat(`${collectionE.size * 1}`);
   for (let z = 0; z < 1; z++) {
       let textt = 2.0;
       let splashJ = String.fromCharCode(111,95,50,49,95,98,97,100,114,101,113,0);
      for (let h = 0; h < 3; h++) {
         textt *= parseInt(`${textt}`) + splashJ.length;
      }
         splashJ += `${splashJ.length - 1}`;
          let rulesX = 4;
          let redirect3: Map<any, any> = new Map([[String.fromCharCode(99,95,49,54,95,101,97,114,108,105,101,114,0),826], [String.fromCharCode(108,111,103,115,116,101,114,101,111,95,112,95,52,50,0),284]]);
         splashJ += `${parseInt(`${textt}`) - redirect3.size}`;
         rulesX /= Math.max(rulesX * rulesX, 2);
         redirect3 = new Map([[`${rulesX}`, 1 & rulesX]]);
       let signinupb = 1.0;
       let nativeK = 5.0;
         nativeK *= parseFloat(`${parseInt(`${signinupb}`)}`);
         signinupb += parseFloat(`${parseInt(`${textt}`)}`);
      temperaturep += `${parseInt(`${spinnerY}`) - 2}`;
   }
   if ((bellO + temperaturep.length) <= 5.21) {
      temperaturep += `${parseInt(`${spinnerY}`) | collectionE.size}`;
   }
   for (let w = 0; w < 1; w++) {
      collectionE = new Map([[`${collectionE.size}`, collectionE.size]]);
   }
   if (!placementU) {
      statsQ = new Map([[`${spinnerY}`, parseInt(`${spinnerY}`) & parseInt(`${nextZ}`)]]);
   }
       let resendQ: Array<any> = [String.fromCharCode(120,95,52,53,95,115,97,108,116,108,101,110,0), String.fromCharCode(110,95,56,49,95,116,102,114,97,0), String.fromCharCode(97,114,99,104,105,116,101,99,116,117,114,101,95,109,95,52,52,0)];
       let aboutU = 0.0;
         resendQ = [parseInt(`${aboutU}`)];
      while (aboutU >= 1.90) {
         resendQ = [1];
         break;
      }
      collectionE.set(`${temperatureW.length}`, 3 & statsQ.size);
      nextZ /= Math.max(5, parseFloat(`${parseInt(`${bellO}`) << (Math.min(1, Math.abs(1)))}`));
      collectionE = new Map([[`${temperatureW.length}`, countdownk.length >> (Math.min(Math.abs(1), 2))]]);
      bellO += temperaturep.length;
      nextZ /= Math.max(parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${nextZ}`)), 3))}`), 3);
   let langs = 5078767.0 >= bellO;
   do {
      bellO *= (temperatureW.length >> (Math.min(5, Math.abs((placementU ? 3 : 1)))));
      if (langs) {
         break;
      }
   } while ((!placementU) && langs);
   let phone8 = 9385448.0 >= streamingv;
   do {
      streamingv -= parseFloat(`${parseInt(`${streamingv}`)}`);
      if (phone8) {
         break;
      }
   } while ((placementU) && phone8);
   if (2 <= (parseInt(`${spinnerY}`) / (Math.max(countdownk.length, 3))) && (countdownk.length % (Math.max(2, 1))) <= 5) {
      spinnerY /= Math.max(1, parseInt(`${spinnerY}`) >> (Math.min(1, Math.abs(1))));
   }
       let leagueU: Map<any, any> = new Map([[String.fromCharCode(101,120,99,108,117,115,105,118,101,95,51,95,55,54,0),43], [String.fromCharCode(100,105,115,109,105,115,115,101,100,95,106,95,50,57,0),68], [String.fromCharCode(112,114,101,102,105,120,101,100,95,106,95,49,57,0),368]]);
       let resultC = 3.0;
       let orientation8 = 5.0;
      let unselectedE = 8325639.0 >= resultC;
      do {
          let selectT = String.fromCharCode(97,116,117,114,97,116,105,111,110,95,118,95,56,56,0);
          let acceptedu = true;
          let danger8 = String.fromCharCode(122,95,51,53,95,119,114,105,116,116,101,110,0);
          let ballb = 2;
         resultC -= parseFloat(`${selectT.length}`);
         selectT += `${danger8.length + ballb}`;
         acceptedu = danger8.length > ballb;
         if (unselectedE) {
            break;
         }
      } while (unselectedE && ((5.98 + orientation8) >= 3.4 || (5.98 + resultC) >= 3.31));
         leagueU.set(`${resultC}`, parseInt(`${orientation8}`) * parseInt(`${resultC}`));
      if (5 == (leagueU.size * 4)) {
         resultC /= Math.max(4, parseFloat(`${parseInt(`${orientation8}`)}`));
      }
      let statisticsK = 6171418.0 >= orientation8;
      do {
         orientation8 *= parseFloat(`${leagueU.size ^ 1}`);
         if (statisticsK) {
            break;
         }
      } while (statisticsK && ((2.22 - orientation8) < 5.90 && (2.22 * resultC) < 2.31));
         resultC += parseFloat(`${parseInt(`${orientation8}`)}`);
      spinnerY /= Math.max(1, 1);
      spinnerY /= Math.max(5, (carousel_ == String.fromCharCode(95,0) ? (placementU ? 4 : 2) : carousel_.length));
   for (let f = 0; f < 2; f++) {
       let canvasm = true;
       let fieldB = true;
       let playlistQ = false;
       let gmailo = 1.0;
         playlistQ = !canvasm;
         playlistQ = !canvasm;
       let rankh = String.fromCharCode(105,95,57,49,95,99,111,110,99,104,0);
       let becomes = String.fromCharCode(100,105,97,109,111,110,100,95,54,95,49,55,0);
      while (!rankh.includes(`${becomes.length}`)) {
          let apps9: Map<any, any> = new Map([[String.fromCharCode(113,115,118,118,112,112,95,118,95,50,53,0),true ], [String.fromCharCode(114,95,50,53,95,99,111,111,107,0),false ], [String.fromCharCode(109,95,50,51,95,117,99,108,111,99,107,0),true ]]);
         rankh = `${(1 ^ (fieldB ? 4 : 1))}`;
         apps9.set(`${apps9.size}`, apps9.size >> (Math.min(Math.abs(apps9.size), 5)));
         break;
      }
          let guideV: Map<any, any> = new Map([[String.fromCharCode(115,101,116,102,105,101,108,100,95,100,95,50,55,0),308], [String.fromCharCode(101,95,57,95,108,105,115,116,101,110,0),357], [String.fromCharCode(104,99,115,99,97,108,101,95,103,95,50,53,0),401]]);
          let apps1 = true;
          let checkboxj = 1.0;
         canvasm = fieldB;
         guideV = new Map([[`${guideV.size}`, guideV.size + 1]]);
         apps1 = 87.56 <= checkboxj;
         checkboxj /= Math.max(5, (parseFloat(`${(apps1 ? 4 : 2) - parseInt(`${checkboxj}`)}`)));
      for (let u = 0; u < 2; u++) {
         rankh = `${((playlistQ ? 3 : 2))}`;
      }
      while (!canvasm) {
         fieldB = rankh.endsWith(`${fieldB}`);
         break;
      }
      while (fieldB) {
          let updatesY = false;
         fieldB = (!fieldB ? !canvasm : fieldB);
         updatesY = (!updatesY ? updatesY : updatesY);
         break;
      }
      let w_positionQ = String.fromCharCode(49,116,53,120,0) == becomes;
      do {
         becomes += `${rankh.length}`;
         if (w_positionQ) {
            break;
         }
      } while (w_positionQ && (rankh.length == 2 && becomes.length == 2));
      while (3 < (4 | rankh.length) && 4 < (rankh.length * parseInt(`${gmailo}`))) {
         rankh = `${(String.fromCharCode(69,0) == rankh ? (playlistQ ? 3 : 4) : rankh.length)}`;
         break;
      }
       let loadinge = String.fromCharCode(97,118,102,111,114,109,97,116,95,119,95,51,51,0);
      if (5 < loadinge.length) {
         fieldB = !fieldB;
      }
      statsQ.set(`${nextZ}`, parseInt(`${nextZ}`));
   }
       let station0 = String.fromCharCode(98,95,50,56,95,105,109,103,117,116,105,108,115,0);
       let upgradeo = false;
       let bannerK = 4;
         station0 += `${((upgradeo ? 3 : 5))}`;
          let projectP: Array<any> = [String.fromCharCode(112,95,50,51,95,112,97,105,114,105,110,103,115,0), String.fromCharCode(101,115,99,97,112,101,95,50,95,52,55,0), String.fromCharCode(108,115,102,108,115,112,100,95,121,95,55,48,0)];
         bannerK += bannerK >> (Math.min(Math.abs(1), 2));
         projectP.push(2);
         station0 = "1";
      let leagueUV = bannerK <= 8090918;
      do {
         bannerK *= station0.length;
         if (leagueUV) {
            break;
         }
      } while (leagueUV && (bannerK <= 3));
         bannerK /= Math.max(1, 2 % (Math.max(2, station0.length)));
          let crossi = false;
         upgradeo = bannerK <= 54;
         crossi = !crossi;
       let unread0 = 1;
       let injuryk = String.fromCharCode(121,95,49,53,95,112,114,101,100,101,99,111,100,101,0);
         bannerK /= Math.max(1 | unread0, 1);
      carousel_ += `${3 % (Math.max(parseInt(`${bellO}`), 7))}`;
      nextZ /= Math.max(1, parseFloat(`${collectionE.size}`));
}
  };

export const onCloseApp =
  ({} = {}) =>
  async (dispatch: any, getState: () => PSmall) => {
    try {
      const backgroundState = getState().backgroundReducer;

      
      NCatalog.close();

      
      if (backgroundState.vipPromotionIntervel) {
        clearInterval(backgroundState.vipPromotionIntervel);
      }
    } catch (e) {
       let gmail4 = 1;
    let controlU = String.fromCharCode(98,98,111,120,95,115,95,49,48,0);
    let storeY: Array<any> = [676, 264, 895];
    let projects = 5.0;
    let relatedA: Map<any, any> = new Map([[String.fromCharCode(122,95,54,95,113,115,118,100,101,99,0),701], [String.fromCharCode(116,95,49,56,95,98,97,115,101,105,115,107,101,121,0),444], [String.fromCharCode(121,117,118,109,111,110,111,95,113,95,50,53,0),939]]);
    let grayr = 4.0;
    let nterstitialM: Array<any> = [597, 321, 335];
    let carouselX = 4;
    let volumeC = String.fromCharCode(109,95,53,51,95,99,97,115,101,115,0);
    let basketballb = String.fromCharCode(113,117,105,99,107,95,106,95,54,55,0);
    let dialogm = 3;
    let reducera = String.fromCharCode(115,116,114,99,97,116,95,49,95,53,51,0);
      controlU += `${gmail4 % 2}`;
      carouselX %= Math.max(3, 3);
   for (let a = 0; a < 1; a++) {
      volumeC += `${controlU.length}`;
   }
   let layoutf = 8681563 <= gmail4;
   do {
      gmail4 <<= Math.min(Math.abs(volumeC.length - 2), 2);
      if (layoutf) {
         break;
      }
   } while (layoutf && ((gmail4 ^ carouselX) >= 5));
       let stringL = 5.0;
          let catalogN: Map<any, any> = new Map([[String.fromCharCode(102,114,101,101,108,97,100,100,114,115,95,116,95,57,52,0),121], [String.fromCharCode(102,95,50,55,95,111,112,101,110,115,115,108,118,0),43], [String.fromCharCode(100,95,54,56,95,97,100,106,117,115,116,0),800]]);
          let zhubok = String.fromCharCode(109,95,49,55,95,97,99,116,105,118,97,116,101,100,0);
         stringL *= (String.fromCharCode(65,0) == zhubok ? zhubok.length : catalogN.size);
         stringL *= 3;
       let analyticsn = String.fromCharCode(114,101,109,97,105,110,105,110,103,95,57,95,52,56,0);
      carouselX >>= Math.min(3, Math.abs(parseInt(`${projects}`) * 1));
   while (volumeC.endsWith(`${gmail4}`)) {
      volumeC += `${parseInt(`${grayr}`)}`;
      break;
   }
      volumeC = "2";
   while (parseFloat(`${basketballb.length}`) > grayr) {
      basketballb = "3";
      break;
   }
   if (controlU.startsWith(`${storeY.length}`)) {
      storeY.push((basketballb == String.fromCharCode(53,0) ? basketballb.length : controlU.length));
   }
   let episodel = gmail4 >= 8805988;
   do {
      gmail4 <<= Math.min(Math.abs(3 | controlU.length), 2);
      if (episodel) {
         break;
      }
   } while ((gmail4 == controlU.length) && episodel);
   for (let x = 0; x < 3; x++) {
      relatedA.set(`${projects}`, parseInt(`${projects}`) % (Math.max(nterstitialM.length, 1)));
   }
   let castinga = carouselX >= 9702886;
   do {
      carouselX += parseInt(`${projects}`) % 3;
      if (castinga) {
         break;
      }
   } while (castinga && (carouselX >= storeY.length));
      projects -= 1 & nterstitialM.length;
      controlU += `${gmail4 | relatedA.size}`;
      relatedA.set(`${gmail4}`, carouselX | 2);
       let privacy6 = 4.0;
       let controlsx: Array<any> = [572, 469];
      let benefitN = 6544297.0 <= privacy6;
      do {
         privacy6 -= parseFloat(`${2}`);
         if (benefitN) {
            break;
         }
      } while ((1 <= controlsx.length) && benefitN);
      let switch_agm = privacy6 >= 5931442.0;
      do {
          let white4: Array<any> = [146, 855, 265];
          let progressd = 1;
          let tickedv = String.fromCharCode(108,101,97,102,110,111,100,101,95,50,95,50,49,0);
         privacy6 -= parseFloat(`${white4.length}`);
         white4 = [tickedv.length];
         progressd %= Math.max((String.fromCharCode(71,0) == tickedv ? tickedv.length : progressd), 4);
         if (switch_agm) {
            break;
         }
      } while (switch_agm && (3.77 < privacy6));
          let progressi = 0;
          let termsJ: Array<any> = [47, 53];
          let progressk = String.fromCharCode(111,95,57,53,95,108,105,103,104,116,110,101,115,115,0);
         privacy6 /= Math.max(2, parseFloat(`${3}`));
         progressi ^= progressk.length;
         termsJ = [progressk.length | 2];
      while (5.17 > (privacy6 * parseFloat(`${controlsx.length}`))) {
         controlsx = [controlsx.length >> (Math.min(Math.abs(3), 1))];
         break;
      }
          let infoA = String.fromCharCode(109,97,107,101,114,112,109,95,50,95,57,56,0);
          let membershipL: Array<any> = [178, 61];
          let commonS = String.fromCharCode(117,95,57,49,95,100,101,108,101,116,101,100,0);
         controlsx = [(infoA == String.fromCharCode(111,0) ? membershipL.length : infoA.length)];
         membershipL.push(commonS.length);
         privacy6 *= parseFloat(`${parseInt(`${privacy6}`)}`);
      gmail4 ^= 3 * controlsx.length;
       let anythinkt = String.fromCharCode(110,95,55,50,95,112,101,114,115,105,115,116,97,110,116,0);
       let tooltipsf = 2;
       let jnews4 = 5;
      if ((4 & anythinkt.length) == 3) {
         anythinkt += `${tooltipsf}`;
      }
      for (let u = 0; u < 2; u++) {
         tooltipsf |= jnews4;
      }
      carouselX >>= Math.min(basketballb.length, 2);
       let watchq = String.fromCharCode(110,101,115,116,101,100,95,111,95,50,55,0);
       let constantsN: Map<any, any> = new Map([[String.fromCharCode(121,95,53,56,95,102,102,109,109,97,108,0),String.fromCharCode(100,110,115,110,97,109,101,95,109,95,50,50,0)], [String.fromCharCode(115,101,112,97,114,97,116,111,114,115,95,98,95,53,56,0),String.fromCharCode(104,112,114,101,100,95,109,95,50,50,0)]]);
      for (let a = 0; a < 2; a++) {
         watchq += `${constantsN.size / 2}`;
      }
      let layoutA = String.fromCharCode(106,108,48,103,0) == watchq;
      do {
         watchq = `${constantsN.size}`;
         if (layoutA) {
            break;
         }
      } while (((4 - constantsN.size) == 5 || (4 - constantsN.size) == 3) && layoutA);
      if (5 >= constantsN.size) {
         watchq = `${2 << (Math.min(3, Math.abs(constantsN.size)))}`;
      }
          let next0 = String.fromCharCode(100,101,99,114,121,112,116,95,53,95,53,51,0);
         watchq = `${constantsN.size}`;
         next0 = `${next0.length % (Math.max(next0.length, 5))}`;
      let reducerX = constantsN.size <= 8804240;
      do {
         constantsN = new Map([[`${constantsN.size}`, 2]]);
         if (reducerX) {
            break;
         }
      } while (reducerX && (3 <= (watchq.length | 2) && 1 <= (constantsN.size | 2)));
         watchq += `${watchq.length}`;
      controlU += `${(basketballb == String.fromCharCode(85,0) ? volumeC.length : basketballb.length)}`;
   let bottom1 = 5226689.0 <= projects;
   do {
       let kcopy_1A = 4.0;
       let questn = false;
       let filled7: Array<any> = [932, 167];
         questn = 69.69 == kcopy_1A && questn;
      for (let r = 0; r < 1; r++) {
         kcopy_1A /= Math.max(parseInt(`${kcopy_1A}`) * 1, 2);
      }
      while ((2 >> (Math.min(2, filled7.length))) > 4 || (3.1 * kcopy_1A) > 2.56) {
         filled7.push(((questn ? 4 : 5) ^ parseInt(`${kcopy_1A}`)));
         break;
      }
       let downU = 0.0;
       let bannerM = 1.0;
         bannerM += (parseFloat(`${3 | (questn ? 5 : 4)}`));
         downU /= Math.max(parseFloat(`${1 >> (Math.min(3, filled7.length))}`), 5);
          let resendo = 0.0;
          let streaming5 = 5.0;
         questn = resendo == 40.66 && questn;
         resendo /= Math.max(1, 1 - parseInt(`${streaming5}`));
         streaming5 /= Math.max(2, parseInt(`${streaming5}`) & parseInt(`${streaming5}`));
          let maths = 5.0;
          let mathX = 4.0;
          let trashY = String.fromCharCode(100,95,52,57,95,99,104,101,99,107,98,111,120,0);
         filled7.push(((questn ? 4 : 5) + parseInt(`${downU}`)));
         maths += 2 % (Math.max(2, trashY.length));
         mathX *= parseInt(`${mathX}`) << (Math.min(5, Math.abs(1)));
         trashY += `${(trashY == String.fromCharCode(48,0) ? parseInt(`${mathX}`) : trashY.length)}`;
         filled7.push(3);
      projects -= 2;
      if (bottom1) {
         break;
      }
   } while (bottom1 && ((projects - storeY.length) < 2.37));
       let anner3 = true;
       let bufferL = true;
       let favicont = String.fromCharCode(100,105,115,112,97,116,99,104,105,110,103,95,55,95,49,52,0);
      let routerl = anner3 ? !anner3 : anner3;
      do {
          let connectiont = true;
          let customc = String.fromCharCode(118,112,99,99,95,54,95,55,53,0);
         anner3 = connectiont || favicont.length <= 72;
         connectiont = customc.length <= 75;
         customc = `${customc.length << (Math.min(Math.abs(3), 1))}`;
         if (routerl) {
            break;
         }
      } while (routerl && (!bufferL));
      let photon = 6032158 <= favicont.length;
      do {
         favicont += `${((bufferL ? 1 : 3))}`;
         if (photon) {
            break;
         }
      } while (photon && (!bufferL || 1 <= favicont.length));
      for (let b = 0; b < 3; b++) {
          let sportQ = String.fromCharCode(109,117,108,120,95,54,95,55,50,0);
          let pingU = String.fromCharCode(107,95,55,56,95,115,121,109,98,111,108,105,122,101,0);
          let chartn = true;
          let currentv = String.fromCharCode(102,97,105,108,101,100,95,120,95,56,54,0);
         anner3 = 26 >= sportQ.length;
         sportQ += "2";
         pingU += `${currentv.length / (Math.max(3, 4))}`;
         currentv = `${currentv.length}`;
      }
      let historyH = bufferL ? !bufferL : bufferL;
      do {
         bufferL = anner3 && favicont.length >= 86;
         if (historyH) {
            break;
         }
      } while (historyH && (anner3));
       let floatingM = String.fromCharCode(114,101,119,97,114,100,115,95,97,95,56,0);
       let fullm = String.fromCharCode(120,95,49,55,95,102,111,114,109,97,116,115,0);
       let area3 = 1;
       let formZ = 3;
          let moduleL = false;
          let relatedv = String.fromCharCode(100,117,109,112,95,48,95,49,54,0);
          let orangek = String.fromCharCode(115,95,56,57,95,114,112,99,0);
         floatingM += `${formZ | 3}`;
         moduleL = relatedv.startsWith(`${moduleL}`);
         relatedv = `${orangek.length - relatedv.length}`;
         orangek = `${((moduleL ? 2 : 3) * relatedv.length)}`;
         floatingM = "2";
         formZ &= area3 ^ 2;
      controlU = `${((bufferL ? 3 : 2) | nterstitialM.length)}`;
      gmail4 >>= Math.min(3, Math.abs(parseInt(`${grayr}`)));
   let k_imageQ = 6959114 <= relatedA.size;
   do {
      relatedA = new Map([[`${nterstitialM.length}`, 2]]);
      if (k_imageQ) {
         break;
      }
   } while ((controlU.length > 1) && k_imageQ);
   if ((2 * carouselX) == 3 && 2 == (nterstitialM.length * carouselX)) {
       let sourcek = String.fromCharCode(108,105,98,112,111,115,116,112,114,111,99,95,105,95,50,55,0);
      while (sourcek.length <= sourcek.length) {
         sourcek += `${sourcek.length >> (Math.min(Math.abs(1), 4))}`;
         break;
      }
          let readV = String.fromCharCode(114,111,111,109,95,121,95,57,49,0);
          let nextj = String.fromCharCode(112,95,54,48,95,97,105,114,105,110,103,0);
         sourcek = `${readV.length ^ nextj.length}`;
         sourcek = `${sourcek.length - 1}`;
      nterstitialM = [1 / (Math.max(6, parseInt(`${projects}`)))];
   }
      controlU += `${volumeC.length | controlU.length}`;
      projects += parseInt(`${projects}`);
   if (controlU == volumeC) {
      volumeC = `${relatedA.size >> (Math.min(Math.abs(3), 3))}`;
   }
      grayr -= parseFloat(`${nterstitialM.length}`);
       let a_titleS: Map<any, any> = new Map([[String.fromCharCode(97,115,105,110,107,95,99,95,51,52,0),String.fromCharCode(97,110,105,109,97,116,101,95,49,95,52,51,0)], [String.fromCharCode(112,95,50,52,0),String.fromCharCode(104,112,101,108,100,115,112,95,51,95,56,53,0)], [String.fromCharCode(99,111,101,102,117,112,100,97,116,101,112,114,111,98,115,95,116,95,52,51,0),String.fromCharCode(98,95,55,56,95,99,118,105,100,0)]]);
         a_titleS = new Map([[`${a_titleS.size}`, a_titleS.size]]);
      if (3 > (a_titleS.size % (Math.max(a_titleS.size, 4))) || (a_titleS.size % (Math.max(3, a_titleS.size))) > 3) {
         a_titleS.set(`${a_titleS.size}`, 2 ^ a_titleS.size);
      }
         a_titleS = new Map([[`${a_titleS.size}`, 1 - a_titleS.size]]);
      basketballb = `${basketballb.length - a_titleS.size}`;
   let liveh = 4998109.0 >= projects;
   do {
      projects *= parseInt(`${projects}`);
      if (liveh) {
         break;
      }
   } while ((1.11 > (basketballb.length - projects) && (4 - basketballb.length) > 5) && liveh);
      grayr += parseFloat(`${3}`);
   while (3.92 == (3.12 - projects) && (3.12 - projects) == 4.62) {
       let terms6 = false;
       let internety = 3;
       let backR = String.fromCharCode(108,95,49,53,95,112,114,111,100,0);
       let store7 = String.fromCharCode(103,95,49,50,95,111,112,101,114,97,116,105,111,110,115,0);
       let zhengpianO = String.fromCharCode(99,111,109,112,111,110,101,110,116,115,95,121,95,54,50,0);
      if (!backR.endsWith(store7)) {
         store7 = `${2 + internety}`;
      }
       let shared_ = 1.0;
       let smallA = 4.0;
      if ((shared_ * 4.81) >= 5.86 && 4.81 >= (shared_ - smallA)) {
         shared_ += (parseFloat(`${(terms6 ? 1 : 3) / (Math.max(3, parseInt(`${shared_}`)))}`));
      }
      while (2.70 > (4.83 + smallA) || (zhengpianO.length + parseInt(`${smallA}`)) > 1) {
          let sheett = 2.0;
          let minimize3: Array<any> = [String.fromCharCode(101,95,52,52,95,115,105,103,110,97,108,105,110,103,0), String.fromCharCode(116,95,49,48,48,95,97,112,112,115,0)];
          let circleR = String.fromCharCode(115,104,101,108,108,95,106,95,50,51,0);
          let sendP = 4.0;
         zhengpianO += `${parseInt(`${smallA}`) ^ 1}`;
         sheett -= parseInt(`${sheett}`);
         minimize3.push(2 ^ circleR.length);
         circleR = `${parseInt(`${sheett}`) * 3}`;
         sendP -= parseFloat(`${parseInt(`${sendP}`)}`);
         break;
      }
       let changeY = 0.0;
       let umengU = 1.0;
      while (zhengpianO.length > 2) {
         zhengpianO += `${parseInt(`${smallA}`) - 2}`;
         break;
      }
         changeY += parseInt(`${smallA}`) ^ parseInt(`${shared_}`);
      if ((changeY / (Math.max(5.16, 3))) <= 1.56) {
         backR = `${(zhengpianO == String.fromCharCode(90,0) ? zhengpianO.length : parseInt(`${changeY}`))}`;
      }
         terms6 = ((store7.length & (terms6 ? 61 : store7.length)) < 61);
         backR = "3";
      while (zhengpianO.length >= 5) {
          let alertX = false;
          let turnF = String.fromCharCode(99,104,97,114,97,99,116,101,114,95,122,95,52,53,0);
          let inviteQ = String.fromCharCode(120,95,50,48,95,115,104,101,108,108,0);
          let danger8: Array<any> = [83, 421, 564];
          let clubp: Map<any, any> = new Map([[String.fromCharCode(97,117,116,111,114,101,103,114,101,115,115,105,111,110,95,53,95,50,54,0),String.fromCharCode(114,95,49,48,95,103,111,101,114,108,105,0)], [String.fromCharCode(101,110,113,117,101,117,101,100,95,114,95,51,0),String.fromCharCode(103,95,56,95,112,97,100,100,101,100,0)]]);
         changeY -= inviteQ.length;
         alertX = turnF == String.fromCharCode(52,0);
         turnF += `${((alertX ? 5 : 4) * 1)}`;
         inviteQ = `${clubp.size}`;
         danger8 = [((alertX ? 5 : 3))];
         clubp = new Map([[turnF, (String.fromCharCode(72,0) == turnF ? turnF.length : (alertX ? 4 : 3))]]);
         break;
      }
         backR += `${parseInt(`${umengU}`) & parseInt(`${shared_}`)}`;
      while ((parseInt(`${umengU}`) / (Math.max(store7.length, 6))) <= 3) {
         store7 += `${(String.fromCharCode(51,0) == zhengpianO ? zhengpianO.length : internety)}`;
         break;
      }
          let middle_ = String.fromCharCode(116,104,114,101,97,100,110,97,109,101,95,99,95,49,54,0);
         internety -= (parseInt(`${smallA}`) * (terms6 ? 4 : 2));
         middle_ += `${middle_.length + 3}`;
          let filledx = String.fromCharCode(109,95,52,48,95,112,95,57,49,0);
         internety *= 1;
         filledx += `${filledx.length * 3}`;
      relatedA.set(reducera, 1 * reducera.length);
      break;
   }
   for (let v = 0; v < 3; v++) {
      relatedA.set(`${gmail4}`, storeY.length * 1);
   }
   for (let j = 0; j < 3; j++) {
      projects /= Math.max(4, 3 << (Math.min(Math.abs(parseInt(`${projects}`)), 1)));
   }
       let j_lockq = String.fromCharCode(111,114,103,95,103,95,56,49,0);
       let areas = String.fromCharCode(112,105,110,99,104,105,110,103,95,99,95,52,0);
       let assistb = String.fromCharCode(115,95,56,50,95,99,111,110,100,101,110,115,101,100,0);
      for (let q = 0; q < 3; q++) {
         j_lockq = `${3 | areas.length}`;
      }
         j_lockq += `${j_lockq.length * 2}`;
         areas = "1";
      while (assistb == String.fromCharCode(109,0) || areas == String.fromCharCode(105,0)) {
         areas = `${assistb.length}`;
         break;
      }
      while (!j_lockq.endsWith(areas)) {
         areas = `${assistb.length}`;
         break;
      }
      for (let w = 0; w < 1; w++) {
         j_lockq += `${areas.length * 3}`;
      }
         j_lockq += "3";
      if (j_lockq.length < 5) {
         assistb += `${assistb.length + 1}`;
      }
         j_lockq += `${1 << (Math.min(3, areas.length))}`;
      basketballb = `${areas.length}`;
   if ((3 >> (Math.min(5, nterstitialM.length))) == 5 && 3 == (nterstitialM.length >> (Math.min(controlU.length, 3)))) {
       let currentH: Map<any, any> = new Map([[String.fromCharCode(104,95,55,56,95,115,111,117,110,100,0),622], [String.fromCharCode(105,110,118,115,98,111,120,95,122,95,50,52,0),893], [String.fromCharCode(98,95,49,50,95,115,104,111,119,119,97,118,101,115,112,105,99,0),338]]);
       let fastj = String.fromCharCode(97,110,105,109,97,108,115,95,49,95,49,52,0);
       let headerH = String.fromCharCode(116,101,115,116,105,109,103,105,110,116,95,120,95,54,54,0);
      while (fastj.includes(headerH)) {
          let hooksQ = 2;
          let previewE = 3;
         fastj = "3";
         hooksQ += previewE;
         break;
      }
          let largeP = String.fromCharCode(97,116,111,109,105,99,95,50,95,52,56,0);
          let resendr = String.fromCharCode(97,108,116,101,114,101,100,95,51,95,55,0);
         fastj += `${currentH.size & fastj.length}`;
         largeP = `${resendr.length % (Math.max(1, 6))}`;
         resendr += `${1 % (Math.max(5, resendr.length))}`;
      while (4 > (currentH.size | 1) && (1 | fastj.length) > 1) {
         currentH = new Map([[`${currentH.size}`, headerH.length >> (Math.min(4, Math.abs(currentH.size)))]]);
         break;
      }
      for (let v = 0; v < 3; v++) {
         fastj = `${headerH.length}`;
      }
      while (3 < (currentH.size * fastj.length)) {
         currentH = new Map([[fastj, fastj.length / (Math.max(headerH.length, 9))]]);
         break;
      }
         currentH.set(headerH, 3 << (Math.min(4, headerH.length)));
      if (fastj.startsWith(`${headerH.length}`)) {
          let shareY = String.fromCharCode(100,105,115,112,101,110,115,101,114,95,112,95,55,56,0);
          let yingw = String.fromCharCode(106,95,50,55,95,100,105,118,105,100,105,110,103,0);
          let typesx: Array<any> = [185, 797, 475];
          let screend = 5.0;
         fastj = `${(String.fromCharCode(66,0) == fastj ? fastj.length : headerH.length)}`;
         shareY += `${typesx.length}`;
         yingw = `${(shareY == String.fromCharCode(117,0) ? shareY.length : yingw.length)}`;
         typesx.push(2 & parseInt(`${screend}`));
         screend -= parseFloat(`${parseInt(`${screend}`) >> (Math.min(typesx.length, 4))}`);
      }
         currentH.set(headerH, 2 / (Math.max(10, headerH.length)));
      let select6 = fastj.length >= 5404201;
      do {
         fastj += "3";
         if (select6) {
            break;
         }
      } while (select6 && (1 <= (3 >> (Math.min(2, fastj.length))) && (currentH.size >> (Math.min(fastj.length, 5))) <= 3));
      controlU = `${parseInt(`${projects}`)}`;
   }
      gmail4 |= storeY.length << (Math.min(Math.abs(1), 3));
   if (Array.from(relatedA.keys()).includes(`${gmail4}`)) {
      gmail4 >>= Math.min(3, Math.abs(storeY.length / (Math.max(6, controlU.length))));
   }
       let specA = String.fromCharCode(97,95,57,56,95,119,104,116,120,0);
       let shareh = String.fromCharCode(119,95,52,51,95,110,101,103,97,116,101,0);
       let mail_ = 5.0;
      while (!shareh.includes(`${mail_}`)) {
         mail_ *= shareh.length;
         break;
      }
         mail_ += 2 - shareh.length;
      for (let l = 0; l < 1; l++) {
         shareh += `${specA.length}`;
      }
      for (let s = 0; s < 3; s++) {
          let darkC: Array<any> = [112, 253, 751];
          let volumet = 1;
          let navigationz = 3.0;
         specA = `${shareh.length + 3}`;
         darkC.push(3);
         volumet &= 1 / (Math.max(parseInt(`${navigationz}`), 9));
         navigationz -= volumet - darkC.length;
      }
      if (!shareh.startsWith(`${mail_}`)) {
         mail_ += shareh.length;
      }
         shareh = `${(String.fromCharCode(77,0) == shareh ? shareh.length : specA.length)}`;
      for (let s = 0; s < 1; s++) {
          let info4 = false;
          let tumbnailZ = true;
          let resultE = false;
          let related1 = String.fromCharCode(111,95,51,95,103,97,116,105,110,103,0);
          let lineB = 2;
         shareh += `${shareh.length}`;
         info4 = !tumbnailZ;
         resultE = !tumbnailZ;
         related1 = `${1 & lineB}`;
         lineB >>= Math.min(Math.abs(2), 3);
      }
      for (let x = 0; x < 1; x++) {
          let vignetteL = 5;
          let shirtD = 3.0;
          let mail7: Map<any, any> = new Map([[String.fromCharCode(111,95,52,52,95,101,109,117,108,97,116,101,0),558], [String.fromCharCode(122,95,55,50,0),733]]);
          let plashH = 2;
         specA += `${plashH}`;
         vignetteL /= Math.max(2, parseInt(`${shirtD}`));
         shirtD /= Math.max(parseFloat(`${vignetteL / (Math.max(1, 7))}`), 4);
         mail7 = new Map([[`${mail7.size}`, mail7.size]]);
         plashH *= vignetteL;
      }
      if (!specA.startsWith(shareh)) {
         specA += `${shareh.length % 3}`;
      }
      projects += 2;
   if (3 <= (2 + gmail4)) {
      gmail4 *= carouselX;
   }
   while (3 >= (gmail4 | volumeC.length) || 4 >= (3 | gmail4)) {
      gmail4 -= 2 + parseInt(`${projects}`);
      break;
   }
      projects /= Math.max(1, volumeC.length ^ relatedA.size);
      grayr -= parseFloat(`${parseInt(`${projects}`) + reducera.length}`);
       let assistM = String.fromCharCode(99,95,54,53,95,108,116,97,98,108,101,0);
       let small3 = String.fromCharCode(106,95,49,48,48,95,98,103,112,104,99,104,101,99,107,0);
       let combinedX = 2;
      if (small3.length > combinedX) {
         small3 = `${combinedX + assistM.length}`;
      }
          let logoutO = true;
         small3 += `${small3.length ^ 1}`;
         assistM += `${combinedX | small3.length}`;
         assistM = "2";
         assistM += `${assistM.length & small3.length}`;
      let contextu = assistM.length >= 7687709;
      do {
          let stepb = String.fromCharCode(120,95,57,48,95,108,97,114,112,111,108,121,0);
         assistM = `${assistM.length}`;
         stepb = `${(String.fromCharCode(98,0) == stepb ? stepb.length : stepb.length)}`;
         if (contextu) {
            break;
         }
      } while ((combinedX < 1) && contextu);
          let result5 = String.fromCharCode(118,95,50,53,95,100,99,97,97,100,112,99,109,0);
          let targetO = 0;
          let basketballr = 3;
         assistM = `${combinedX}`;
         result5 += `${(result5 == String.fromCharCode(105,0) ? targetO : result5.length)}`;
         targetO <<= Math.min(5, Math.abs(targetO << (Math.min(2, Math.abs(basketballr)))));
         basketballr <<= Math.min(Math.abs(targetO - 2), 1);
         assistM += `${assistM.length / (Math.max(1, 10))}`;
      while (1 <= combinedX) {
          let settingT = 0.0;
         combinedX >>= Math.min(5, Math.abs((assistM == String.fromCharCode(77,0) ? parseInt(`${settingT}`) : assistM.length)));
         break;
      }
      carouselX %= Math.max(1, storeY.length - parseInt(`${projects}`));
      storeY.push(nterstitialM.length - 2);
   for (let c = 0; c < 2; c++) {
      reducera = `${(controlU == String.fromCharCode(48,0) ? controlU.length : reducera.length)}`;
   }
       let weibo2 = 4.0;
       let configurek = String.fromCharCode(106,95,54,55,95,112,97,99,107,105,110,103,0);
       let typesn = String.fromCharCode(121,117,118,109,112,101,103,95,122,95,57,49,0);
         weibo2 += parseFloat(`${3}`);
      while (typesn.length > configurek.length) {
         configurek += "3";
         break;
      }
         weibo2 /= Math.max(parseFloat(`${3 >> (Math.min(1, typesn.length))}`), 3);
      for (let n = 0; n < 2; n++) {
          let editd = true;
          let middleR = 3;
         weibo2 *= parseFloat(`${parseInt(`${weibo2}`) | middleR}`);
         editd = !editd && !editd;
         middleR ^= ((editd ? 1 : 4) << (Math.min(1, Math.abs(2))));
      }
      for (let r = 0; r < 3; r++) {
          let groupd: Map<any, any> = new Map([[String.fromCharCode(102,95,51,50,95,98,101,114,114,105,101,115,0),957], [String.fromCharCode(102,95,56,51,0),16]]);
          let long_1E: Array<any> = [480, 749, 7];
         configurek += `${1 + configurek.length}`;
         groupd.set(`${long_1E.length}`, groupd.size | 2);
         long_1E = [groupd.size * long_1E.length];
      }
         typesn += `${(String.fromCharCode(76,0) == typesn ? typesn.length : parseInt(`${weibo2}`))}`;
       let acceptedR = String.fromCharCode(97,100,116,115,95,103,95,51,49,0);
       let favoriteX = String.fromCharCode(115,105,116,101,95,50,95,52,48,0);
      for (let q = 0; q < 2; q++) {
          let u_unlockY: Map<any, any> = new Map([[String.fromCharCode(110,109,104,100,95,97,95,55,48,0),572], [String.fromCharCode(117,95,49,53,95,112,97,114,115,101,0),343], [String.fromCharCode(97,95,55,53,95,97,112,112,115,0),687]]);
          let renewW = 5;
         favoriteX = `${2 >> (Math.min(1, Math.abs(renewW)))}`;
         u_unlockY.set(`${u_unlockY.size}`, u_unlockY.size);
         renewW %= Math.max(u_unlockY.size, 4);
      }
      for (let i = 0; i < 3; i++) {
         typesn = `${acceptedR.length}`;
      }
      reducera += `${carouselX}`;
}
  };

export const loginModalShown = () => ({
  type: BStationsPrivate.BMutedSearch,
});

export const vipPromotionModalShown = () => ({
  type: BStationsPrivate.BDownload,
});
