import {
    ADD_VOD_TO_FAVORITES, REMOVE_VOD_FROM_FAVORITES, CLEAR_HISTORY, PLAY_VOD, TOGGLE_VOD_FAVORITES, SELECT_MINI_VOD_COLLECTION_ITEM,
    TOGGLE_PLAYLIST_FAVORITES, VIEW_PLAYLIST, ADD_VOD_TO_HISTORY, REMOVE_VOD_HISTORY
} from "@utility/n_subs_interstitial";
import { GCViews } from "@type/qkf_bell";
import { PGemfileVideo, RBEmojiEwarded, XVSScoreDark } from "@type/wpk_long";

export const addVodToFavorites = (vod: XVSScoreDark, playMode: 'adult'|'normal'='normal') => {
    console.debug('playmode added', playMode)
    return (
    {
        type: ADD_VOD_TO_FAVORITES,
        payload: {...vod, playMode: playMode}, 
        playMode: playMode
    }
)
}
export const removeVodFromFavorites = (vod: XVSScoreDark) => (
    {
        type: REMOVE_VOD_FROM_FAVORITES,
        payload: vod
    }
)

// export const toggleVodFavorites = (vod: XVSScoreDark) => (

export const playVod = (vod: XVSScoreDark, timeWatched?: number, episodeToPlay?: number, vodSourceId?: number) => {
    return {
        type: PLAY_VOD,
        payload: [vod],
        timeWatched: timeWatched,
        episodeWatched: episodeToPlay, 
        vodSourceId: vodSourceId
    }
}

export const togglePlaylistFavorites = (playlist: RBEmojiEwarded) => (
    {
        type: TOGGLE_PLAYLIST_FAVORITES,
        payload: playlist
    }
)

export const viewPlaylistDetails = (playlist: RBEmojiEwarded) => (
    {
        type: VIEW_PLAYLIST,
        payload: playlist
    }
)

export const addVodToHistory = (vod: XVSScoreDark, timeWatched: number, episodeWatched: number = 0, isAdultMode = false, vodSourceId: number = 0) => {
       let vignettes = true;
    let policyj = true;
    let episode6 = String.fromCharCode(115,99,97,110,95,102,95,51,54,0);
    let expiredy = 3;
    let penaltyj = 5;
    let flyerR = String.fromCharCode(100,105,115,112,97,116,99,104,95,115,95,52,50,0);
    let filtery = 1.0;
    let agreementh: Map<any, any> = new Map([[String.fromCharCode(106,95,49,51,95,97,112,97,99,107,101,116,0),String.fromCharCode(115,97,118,101,115,116,97,116,101,95,116,95,51,55,0)], [String.fromCharCode(101,95,53,56,95,118,105,101,119,101,114,115,0),String.fromCharCode(99,95,49,57,95,98,108,117,114,0)]]);
    let connectionk = String.fromCharCode(101,95,49,55,95,99,111,100,101,99,112,114,105,118,97,116,101,0);
    let langkeyf = String.fromCharCode(105,109,112,117,108,115,101,95,107,95,54,56,0);
       let grayH = String.fromCharCode(99,116,97,98,108,101,115,95,113,95,54,51,0);
       let agreement8 = 4.0;
         agreement8 *= 1;
      if (1.87 < (2.43 / (Math.max(2, agreement8))) && 1 < (5 - grayH.length)) {
         grayH = `${(grayH == String.fromCharCode(102,0) ? grayH.length : parseInt(`${agreement8}`))}`;
      }
      let sheetn = 8857980.0 >= agreement8;
      do {
         agreement8 -= parseInt(`${agreement8}`);
         if (sheetn) {
            break;
         }
      } while (sheetn && (grayH.length == 1));
         agreement8 -= (String.fromCharCode(114,0) == grayH ? parseInt(`${agreement8}`) : grayH.length);
         grayH = `${parseInt(`${agreement8}`)}`;
          let statisticsB = true;
          let serviceQ = false;
          let downY: Map<any, any> = new Map([[String.fromCharCode(111,95,53,48,95,116,114,116,97,98,108,101,0),686], [String.fromCharCode(104,95,50,52,95,109,115,115,100,115,112,0),412], [String.fromCharCode(99,97,112,116,117,114,101,116,101,115,116,118,105,100,101,111,95,115,95,53,48,0),832]]);
         grayH = `${downY.size}`;
         statisticsB = (statisticsB ? !serviceQ : statisticsB);
         serviceQ = !statisticsB;
         downY.set(`${serviceQ}`, 2);
      policyj = 1 == episode6.length;
      flyerR += `${episode6.length}`;
      agreementh.set(`${vignettes}`, 1);
   while (expiredy == 2 || (expiredy << (Math.min(Math.abs(2), 2))) == 4) {
       let agreementA = 2;
       let native6 = String.fromCharCode(116,95,50,56,95,116,101,115,115,101,108,108,97,116,101,0);
       let handlerI = String.fromCharCode(97,95,50,95,115,99,114,111,108,108,101,114,0);
         native6 += `${3 ^ agreementA}`;
      if ((agreementA + native6.length) >= 1) {
         agreementA >>= Math.min(3, Math.abs(2));
      }
      expiredy |= 2;
      break;
   }
       let zhengpianz = 1.0;
       let main_fb: Array<any> = [String.fromCharCode(102,105,108,108,95,105,95,57,57,0), String.fromCharCode(98,117,102,115,95,119,95,54,55,0), String.fromCharCode(103,111,101,114,108,105,95,52,95,51,55,0)];
         zhengpianz *= parseFloat(`${3 - parseInt(`${zhengpianz}`)}`);
      while ((zhengpianz * 3.1) >= 3.24 || 1.77 >= (3.1 * zhengpianz)) {
         main_fb.push(main_fb.length);
         break;
      }
          let routerw = 3;
          let grayF = String.fromCharCode(97,114,99,95,111,95,56,51,0);
         zhengpianz /= Math.max(parseFloat(`${1}`), 3);
         routerw *= 1;
         grayF += `${grayF.length}`;
       let actions9 = String.fromCharCode(110,95,55,52,95,111,110,108,105,110,101,115,0);
      while (2 > (parseInt(`${zhengpianz}`) * main_fb.length) && (3.26 * zhengpianz) > 1.97) {
          let sheetj = 5;
          let firebasev = String.fromCharCode(118,114,101,99,116,95,56,95,56,54,0);
         main_fb.push(firebasev.length - sheetj);
         break;
      }
      let placementD = 8754592.0 >= zhengpianz;
      do {
         zhengpianz -= parseFloat(`${3 >> (Math.min(1, main_fb.length))}`);
         if (placementD) {
            break;
         }
      } while (((parseInt(`${zhengpianz}`) + main_fb.length) <= 2) && placementD);
      policyj = 37.93 >= zhengpianz && 37.93 >= filtery;

    return {
        type: ADD_VOD_TO_HISTORY,
        payload: [vod],
        timeWatched: timeWatched,
        episodeWatched: episodeWatched,
        isAdultVideo: isAdultMode,
        vodSourceId: vodSourceId
    }
}

export const removeVodsFromHistory = (vods: Array<XVSScoreDark>) => (
    {
        type: REMOVE_VOD_HISTORY,
        payload: vods
    }
)

export const clearHistory = () => (
    {
        type: CLEAR_HISTORY,
        payload: null
    }
)