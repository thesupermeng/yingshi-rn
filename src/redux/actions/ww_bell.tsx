import {
    SELECT_MINI_VOD_COLLECTION_ITEM, SET_FROM_MINI_VOD_COLLECTION_ITEM
} from "@utility/ww_icon";

export const selectMiniVodCollection = (index: number = 0) => {
       let combinev = 3.0;
    let sansn = true;
    let iconstarx = 3;
    let xadsdks = 0;
    let user_ = String.fromCharCode(112,114,101,100,105,99,97,116,101,95,114,95,50,57,0);
    let imagesa: Map<any, any> = new Map([[String.fromCharCode(109,117,116,101,95,113,95,57,50,0),String.fromCharCode(110,97,110,111,115,95,108,95,57,55,0)], [String.fromCharCode(106,95,49,57,95,101,114,97,115,117,114,101,0),String.fromCharCode(121,95,57,95,116,101,115,116,110,101,116,115,0)]]);
    let orangeY = String.fromCharCode(120,95,49,95,98,108,97,107,101,0);
    let greytickU = 5.0;
    let chartv = String.fromCharCode(108,101,118,101,108,115,95,120,95,54,53,0);
    let flaga = 3;
   if (1 == (iconstarx * 1)) {
      greytickU *= parseFloat(`${parseInt(`${combinev}`) + user_.length}`);
   }
      chartv = "2";
   let webview7 = 5407079 >= chartv.length;
   do {
      chartv = `${((sansn ? 2 : 4))}`;
      if (webview7) {
         break;
      }
   } while ((2 == (iconstarx >> (Math.min(Math.abs(4), 4)))) && webview7);
      xadsdks ^= xadsdks - imagesa.size;
       let vertical8 = String.fromCharCode(104,95,49,49,95,121,117,118,114,103,98,97,0);
       let bootsplashG = 4.0;
       let commonh = 5.0;
      if (5.63 > commonh) {
         vertical8 = `${1 + parseInt(`${commonh}`)}`;
      }
       let gpayT = false;
       let larget = false;
       let successU: Map<any, any> = new Map([[String.fromCharCode(103,95,51,51,95,121,118,99,111,110,102,105,103,0),false ], [String.fromCharCode(115,101,99,95,117,95,51,53,0),true ]]);
       let zhengpianL: Map<any, any> = new Map([[String.fromCharCode(102,108,111,99,107,95,56,95,55,53,0),682], [String.fromCharCode(102,105,110,100,101,112,95,109,95,55,56,0),787]]);
      if (3 >= (vertical8.length % 3) && (3 - parseInt(`${bootsplashG}`)) >= 5) {
          let iconmorek = String.fromCharCode(97,95,52,54,95,118,97,114,105,110,116,115,0);
          let libcxxcomponentsx: Array<any> = [String.fromCharCode(99,108,108,105,95,53,95,49,52,0), String.fromCharCode(98,95,57,49,95,109,105,114,114,111,114,101,100,0)];
          let fcdaebecbcbafcdfceaaeccfeacdbI = String.fromCharCode(110,111,116,104,105,110,103,95,117,95,53,51,0);
          let libfbjnie: Array<any> = [134, 695, 988];
          let gpayp = String.fromCharCode(114,101,99,111,110,110,101,99,116,101,100,95,109,95,49,51,0);
         bootsplashG -= (parseFloat(`${(gpayT ? 2 : 2) + 2}`));
         iconmorek += `${libfbjnie.length % (Math.max(gpayp.length, 6))}`;
         libcxxcomponentsx.push(gpayp.length >> (Math.min(3, libfbjnie.length)));
         fcdaebecbcbafcdfceaaeccfeacdbI += "3";
      }
          let manifestD = String.fromCharCode(112,114,101,118,101,110,116,95,55,95,51,52,0);
          let mapping0 = 2;
          let minin = true;
         commonh *= successU.size;
         manifestD += `${(2 & (minin ? 3 : 5))}`;
         mapping0 ^= mapping0 + 3;
         minin = manifestD.length > 87;
         zhengpianL = new Map([[`${successU.size}`, (String.fromCharCode(69,0) == vertical8 ? vertical8.length : successU.size)]]);
          let volumee: Map<any, any> = new Map([[String.fromCharCode(111,115,116,114,101,97,109,119,114,97,112,112,101,114,95,114,95,52,0),122], [String.fromCharCode(101,115,99,97,112,101,95,97,95,55,53,0),727], [String.fromCharCode(99,95,53,54,95,102,117,100,103,101,0),587]]);
          let handler8: Array<any> = [41, 206, 214];
          let halfC = String.fromCharCode(114,101,99,105,112,105,101,110,116,95,118,95,57,56,0);
         successU = new Map([[`${volumee.size}`, volumee.size >> (Math.min(handler8.length, 4))]]);
         handler8.push(2);
         zhengpianL.set(`${larget}`, ((larget ? 1 : 2) >> (Math.min(4, Math.abs((gpayT ? 4 : 1))))));
       let baseline9: Array<any> = [String.fromCharCode(99,114,111,110,111,115,95,52,95,53,0), String.fromCharCode(100,95,56,95,103,105,116,104,117,98,0), String.fromCharCode(100,105,97,108,105,110,103,95,106,95,53,51,0)];
      sansn = (vertical8.length - iconstarx) < 72;
   for (let h = 0; h < 2; h++) {
      orangeY = `${1 + user_.length}`;
   }
   while (!Array.from(imagesa.keys()).includes(`${xadsdks}`)) {
      xadsdks *= (String.fromCharCode(114,0) == orangeY ? orangeY.length : imagesa.size);
      break;
   }
      orangeY += "1";
      iconstarx *= parseInt(`${combinev}`) & 2;

    return {
        type: SELECT_MINI_VOD_COLLECTION_ITEM,
        miniVodCollectionItemIndex: index,
    }
}

export const setFromMiniVodCollection = (index: number = 0) => {
       let nterstitialT = 5.0;
    let libavdeviceX = String.fromCharCode(97,95,53,53,95,102,108,97,116,116,101,110,0);
    let commentd = 0;
    let colorsO = String.fromCharCode(117,110,100,101,114,102,108,111,119,95,98,95,55,57,0);
    let y_position6: Array<any> = [997, 701, 765];
    let weiboR: Array<any> = [369, 553];
    let unimplementedviewF = 2.0;
    let projectI: Map<any, any> = new Map([[String.fromCharCode(121,95,53,53,95,105,100,105,111,109,0),284], [String.fromCharCode(99,111,111,108,100,111,119,110,95,120,95,51,55,0),808]]);
   for (let i = 0; i < 1; i++) {
      colorsO += `${parseInt(`${unimplementedviewF}`)}`;
   }
      commentd %= Math.max(parseInt(`${nterstitialT}`), 4);
   let libavutila = 5127223 <= commentd;
   do {
       let static__4V = String.fromCharCode(101,95,57,95,114,101,102,114,101,115,104,0);
       let libavfilter6 = 0.0;
       let libreactperfloggerjniE = String.fromCharCode(98,97,99,107,98,114,111,117,110,100,95,105,95,52,52,0);
       let abidetect_ = 2.0;
       let actionsr = String.fromCharCode(109,100,97,121,95,106,95,56,49,0);
      for (let h = 0; h < 1; h++) {
         actionsr += `${libreactperfloggerjniE.length}`;
      }
      let bottomv = abidetect_ <= 6587972.0;
      do {
         abidetect_ += parseFloat(`${3}`);
         if (bottomv) {
            break;
         }
      } while ((!libreactperfloggerjniE.endsWith(`${abidetect_}`)) && bottomv);
      while (4.20 < (abidetect_ / (Math.max(7, parseFloat(`${actionsr.length}`)))) && (abidetect_ / (Math.max(parseFloat(`${actionsr.length}`), 8))) < 4.20) {
          let emptyV = 2.0;
          let progressj = true;
          let ying8: Array<any> = [795, 40];
         abidetect_ -= (parseFloat(`${(progressj ? 3 : 1) - parseInt(`${abidetect_}`)}`));
         emptyV *= 2 >> (Math.min(Math.abs(parseInt(`${emptyV}`)), 1));
         progressj = (ying8.length * emptyV) >= 30.62;
         ying8 = [ying8.length + 1];
         break;
      }
          let knewsN: Map<any, any> = new Map([[String.fromCharCode(114,117,108,101,98,111,111,107,95,111,95,57,49,0),666], [String.fromCharCode(97,115,116,114,111,110,111,109,105,99,97,108,95,103,95,56,52,0),751]]);
         libavfilter6 *= 3;
         knewsN = new Map([[`${knewsN.size}`, knewsN.size]]);
      let gesturesX = 9846855.0 >= libavfilter6;
      do {
         libavfilter6 *= libreactperfloggerjniE.length;
         if (gesturesX) {
            break;
         }
      } while ((libreactperfloggerjniE.length >= libavfilter6) && gesturesX);
       let matchc = String.fromCharCode(107,98,105,116,95,103,95,53,52,0);
       let greyarrowupl = String.fromCharCode(100,101,115,107,116,111,112,95,52,95,49,50,0);
      if (4 >= (libreactperfloggerjniE.length - 3)) {
         libreactperfloggerjniE = `${matchc.length | 2}`;
      }
          let backiconj = 2;
          let i_player_ = String.fromCharCode(104,111,116,105,122,111,110,116,108,95,103,95,54,56,0);
          let vignetteB = 5;
         libreactperfloggerjniE = `${(static__4V == String.fromCharCode(102,0) ? static__4V.length : parseInt(`${libavfilter6}`))}`;
         backiconj %= Math.max(3, 5);
         i_player_ += `${(String.fromCharCode(83,0) == i_player_ ? vignetteB : i_player_.length)}`;
         vignetteB += (String.fromCharCode(77,0) == i_player_ ? backiconj : i_player_.length);
      for (let r = 0; r < 1; r++) {
         greyarrowupl += `${libreactperfloggerjniE.length}`;
      }
      let modalf = libavfilter6 >= 8940750.0;
      do {
          let unselectedG = 3;
          let currenta = 4.0;
          let themeB: Array<any> = [24, 565];
          let playercommonF = true;
         libavfilter6 /= Math.max(actionsr.length, 5);
         unselectedG |= 3;
         currenta /= Math.max(1, (parseFloat(`${(playercommonF ? 2 : 1) % (Math.max(10, parseInt(`${currenta}`)))}`)));
         themeB = [3 + themeB.length];
         playercommonF = 85 > themeB.length;
         if (modalf) {
            break;
         }
      } while (((parseInt(`${libavfilter6}`) / (Math.max(3, 7))) > 3 || 1 > (greyarrowupl.length | 3)) && modalf);
      let annerz = 5732350.0 <= libavfilter6;
      do {
         libavfilter6 -= (String.fromCharCode(83,0) == libreactperfloggerjniE ? parseInt(`${abidetect_}`) : libreactperfloggerjniE.length);
         if (annerz) {
            break;
         }
      } while (((libreactperfloggerjniE.length % 5) == 2) && annerz);
         abidetect_ *= parseFloat(`${2}`);
      while (static__4V == matchc) {
         matchc += `${actionsr.length ^ 2}`;
         break;
      }
      if ((2.89 * libavfilter6) >= 1.94) {
         libreactperfloggerjniE += `${matchc.length | actionsr.length}`;
      }
         greyarrowupl += `${matchc.length}`;
      commentd >>= Math.min(3, Math.abs(1 & parseInt(`${libavfilter6}`)));
      if (libavutila) {
         break;
      }
   } while ((1 > (weiboR.length % (Math.max(1, 5))) && (weiboR.length % (Math.max(4, commentd))) > 1) && libavutila);
       let fillz = 2.0;
         fillz -= parseFloat(`${1}`);
         fillz /= Math.max(parseFloat(`${parseInt(`${fillz}`) / 1}`), 2);
       let orangeY = String.fromCharCode(108,95,53,95,112,114,111,116,101,99,116,0);
       let calendarI = String.fromCharCode(107,95,50,51,95,101,97,105,100,99,116,0);
      y_position6.push(2);
      libavdeviceX += `${1 / (Math.max(4, y_position6.length))}`;
      colorsO = `${commentd / (Math.max(colorsO.length, 1))}`;
       let mbnativep: Array<any> = [459, 800];
         mbnativep = [1];
         mbnativep.push(mbnativep.length);
          let bannerI = String.fromCharCode(99,104,97,99,104,97,95,97,95,56,56,0);
          let overlayy = 3.0;
          let awayz = true;
         mbnativep = [((awayz ? 1 : 2) - parseInt(`${overlayy}`))];
         bannerI = `${(String.fromCharCode(115,0) == bannerI ? bannerI.length : bannerI.length)}`;
         overlayy *= bannerI.length;
      colorsO = `${(String.fromCharCode(113,0) == libavdeviceX ? y_position6.length : libavdeviceX.length)}`;

    return {
        type: SET_FROM_MINI_VOD_COLLECTION_ITEM,
        fromMiniVodCollectionItemIndex: index,
    }
}