import {
    ADD_VOD_TO_FAVORITES, REMOVE_VOD_FROM_FAVORITES, CLEAR_HISTORY, PLAY_VOD, TOGGLE_VOD_FAVORITES, SELECT_MINI_VOD_COLLECTION_ITEM,
    TOGGLE_PLAYLIST_FAVORITES, VIEW_PLAYLIST, ADD_VOD_TO_HISTORY, REMOVE_VOD_HISTORY
} from "@utility/wawa_iconpointscore";
import { wawaDark } from "@type/wawa_reward_logo";
import { wawaOverlayDirect, wawaWithCrown, wawaSellProfileinactive } from "@type/wawa_gradlew";
import { LComment } from "@redux/reducers/wawa_quest_ping";

export const addVodToFavorites = (vod: wawaSellProfileinactive, playMode: 'adult' | 'normal' = 'normal') => {
    console.debug('playmode added', playMode)
    return (
        {
            type: ADD_VOD_TO_FAVORITES,
            payload: { ...vod, playMode: playMode },
            playMode: playMode
        }
    )
}
export const removeVodFromFavorites = (vod: wawaSellProfileinactive) => (
    {
        type: REMOVE_VOD_FROM_FAVORITES,
        payload: vod
    }
)

// export const toggleVodFavorites = (vod: wawaSellProfileinactive) => (

export const playVod = (vod: wawaSellProfileinactive, timeWatched?: number, episodeToPlay?: number, vodSourceId?: number) => {
    return {
        type: PLAY_VOD,
        payload: [vod],
        timeWatched: timeWatched,
        episodeWatched: episodeToPlay,
        vodSourceId: vodSourceId
    }
}

export const togglePlaylistFavorites = (playlist: wawaWithCrown) => (
    {
        type: TOGGLE_PLAYLIST_FAVORITES,
        payload: playlist
    }
)

export const viewPlaylistDetails = (playlist: wawaWithCrown) => (
    {
        type: VIEW_PLAYLIST,
        payload: playlist
    }
)

export const addVodToHistory = (vod: wawaSellProfileinactive, timeWatched: number, episodeWatched: number = 0, isAdultMode = false, vodSourceId: number = 0) => {
       let typingloadingM = 3;
    let templateprocessor0 = true;
    let mbnativeadvanced0: Array<any> = [2, 798, 5];
    let typingr: Array<any> = [879, 636, 362];
    let runtime0: Array<any> = [886, 662];
    let mbbidK = String.fromCharCode(114,95,51,56,95,104,105,110,116,0);
    let photoF: Map<any, any> = new Map([[String.fromCharCode(110,95,53,57,95,102,111,108,100,101,114,0),true ], [String.fromCharCode(116,95,53,52,95,97,112,109,116,101,115,116,0),false ], [String.fromCharCode(109,97,110,121,95,50,95,54,50,0),false ]]);
    let confirmations = String.fromCharCode(103,95,52,49,95,115,119,97,112,112,97,98,108,101,0);
    let modulen = 5.0;
    let controlsl = 0.0;
    let profileactiveh = false;
    let bggradientz: Array<any> = [605, 874];
   let sharewhitey = photoF.size <= 7467867;
   do {
       let iconcurrentmatchJ = String.fromCharCode(100,95,57,52,95,115,101,116,115,111,99,107,111,112,116,0);
       let anytimew = String.fromCharCode(115,117,98,100,105,118,105,115,105,111,110,95,122,95,49,56,0);
       let progressL = String.fromCharCode(112,114,101,115,115,101,100,95,97,95,56,50,0);
         iconcurrentmatchJ += `${iconcurrentmatchJ.length * 1}`;
          let foundx = String.fromCharCode(97,112,102,115,95,105,95,54,56,0);
          let tailU: Array<any> = [426, 128, 805];
          let white_ = 2;
         progressL += "3";
         foundx += `${tailU.length}`;
         tailU = [(String.fromCharCode(97,0) == foundx ? foundx.length : white_)];
         white_ >>= Math.min(4, Math.abs(white_ / (Math.max(9, foundx.length))));
      if (iconcurrentmatchJ.endsWith(`${anytimew.length}`)) {
          let sansF: Array<any> = [318, 86];
          let redirecte = String.fromCharCode(106,95,49,48,48,95,115,116,97,116,101,115,0);
          let minivod4: Map<any, any> = new Map([[String.fromCharCode(119,101,105,103,104,116,101,100,95,117,95,54,51,0),String.fromCharCode(105,115,108,101,97,112,95,51,95,49,51,0)], [String.fromCharCode(114,95,48,95,112,97,115,115,101,115,0),String.fromCharCode(100,105,115,97,108,108,111,119,95,105,95,50,48,0)], [String.fromCharCode(109,111,110,111,115,112,97,99,101,100,95,48,95,57,55,0),String.fromCharCode(120,95,51,54,95,105,109,109,101,100,105,97,116,101,0)]]);
         iconcurrentmatchJ += `${iconcurrentmatchJ.length}`;
         sansF.push(2 << (Math.min(4, redirecte.length)));
         redirecte = `${redirecte.length}`;
         minivod4 = new Map([[`${minivod4.size}`, minivod4.size]]);
      }
       let componentsT = 0;
       let greent = 3;
         componentsT += anytimew.length ^ 2;
      for (let x = 0; x < 1; x++) {
         anytimew += `${anytimew.length * 1}`;
      }
       let inouttargetred4 = String.fromCharCode(110,95,55,55,95,112,97,110,111,114,97,109,97,0);
       let subbasketballplayerA = String.fromCharCode(114,101,112,101,97,116,101,100,108,121,95,57,95,54,57,0);
      if (progressL.includes(`${anytimew.length}`)) {
          let pointS = String.fromCharCode(115,118,97,114,105,110,116,95,119,95,52,57,0);
          let redgoalH = 3.0;
          let iconplayq = true;
         progressL = "1";
         pointS = `${parseInt(`${redgoalH}`) * 3}`;
         redgoalH += parseFloat(`${parseInt(`${redgoalH}`) / 2}`);
         iconplayq = redgoalH > 75.20;
      }
         iconcurrentmatchJ += `${iconcurrentmatchJ.length + greent}`;
      photoF = new Map([[`${templateprocessor0}`, mbbidK.length >> (Math.min(Math.abs(3), 1))]]);
      if (sharewhitey) {
         break;
      }
   } while (sharewhitey && (!Array.from(photoF.keys()).includes(`${typingr.length}`)));
   if (!templateprocessor0) {
      templateprocessor0 = (27 > ((!templateprocessor0 ? 27 : typingr.length) - typingr.length));
   }
      runtime0.push(runtime0.length << (Math.min(mbbidK.length, 5)));
   while (1 > mbbidK.length) {
      mbbidK += `${(String.fromCharCode(49,0) == mbbidK ? mbbidK.length : mbnativeadvanced0.length)}`;
      break;
   }

    return {
        type: ADD_VOD_TO_HISTORY,
        payload: [vod],
        timeWatched: timeWatched,
        episodeWatched: episodeWatched,
        isAdultVideo: isAdultMode,
        vodSourceId: vodSourceId
    }
}

export const removeVodsFromHistory = (vods: Array<wawaSellProfileinactive>) => (
    {
        type: REMOVE_VOD_HISTORY,
        payload: vods
    }
)

export const clearHistory = () => (
    {
        type: CLEAR_HISTORY,
        payload: null
    }
)

export const onViewShortVod = () => (
    {
        type: LComment.LIconsettingDice,
    }
)