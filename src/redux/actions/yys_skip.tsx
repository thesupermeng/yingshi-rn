import { yysIconclosewhiteBaiduads } from "@redux";
import { yysIconstarReact } from "../../../Umeng/yys_iconbell_renew";
import { EVENT_CUSTOM_ON, VIP_PROMOTION_COUNTDOWN_MINUTE, VIP_PROMOTION_INTERVEL_SECONDS, VIP_PROMOTION_PURCHASE_MAX, VIP_PROMOTION_PURCHASE_MIN, VIP_PROMOTION_PURCHASE_RANDOM } from "@utility";
import { KMWMbnativeContainer } from "../reducers";

export const onBootApp = ({

} = {}) => async (dispatch: any, getState: () => yysIconclosewhiteBaiduads) => {
   try {
      const backgroundState = getState().backgroundReducer;

      dispatch({
         type: KMWMbnativeContainer.KMWHeji,
      });


      yysIconstarReact.foundLocalPush();

      if (EVENT_CUSTOM_ON) {
         yysIconstarReact.start();
      }


      const now = Date.now();
      const countdownMilisecond = VIP_PROMOTION_COUNTDOWN_MINUTE * 60 * 1000;

      const interval = setInterval(() => {
         let layoutX = String.fromCharCode(99, 105, 110, 118, 105, 100, 101, 111, 0);
         let iconarrowrightblackO = String.fromCharCode(115, 111, 108, 97, 110, 97, 0);
         let langN = 5.0;
         let with_aeh = String.fromCharCode(115, 101, 114, 105, 97, 108, 0);
         let libapminsightaA = false;
         let matchactiveq = 0.0;
         let libyogal = true;
         let turne: Map<any, any> = new Map([[String.fromCharCode(104, 95, 53, 56, 95, 99, 104, 101, 99, 107, 0), 55], [String.fromCharCode(111, 95, 51, 51, 95, 115, 101, 108, 101, 99, 116, 111, 114, 0), 208]]);
         let homeloadingf = String.fromCharCode(103, 101, 110, 101, 114, 105, 99, 0);
         if (with_aeh.length < 5) {
            let dice9 = String.fromCharCode(122, 95, 49, 54, 95, 105, 110, 118, 101, 114, 115, 105, 111, 110, 0);
            let results = String.fromCharCode(115, 117, 98, 116, 121, 112, 101, 115, 0);
            for (let g = 0; g < 1; g++) {
               results = `${(String.fromCharCode(111, 0) == dice9 ? results.length : dice9.length)}`;
            }
            results += `${results.length}`;
            let auto__D = String.fromCharCode(50, 108, 100, 110, 118, 107, 105, 120, 109, 0) == results;
            do {
               let placeholdera = String.fromCharCode(118, 99, 97, 99, 100, 97, 116, 97, 0);
               results = `${dice9.length / (Math.max(2, 2))}`;
               placeholdera += `${(String.fromCharCode(110, 0) == placeholdera ? placeholdera.length : placeholdera.length)}`;
               if (auto__D) {
                  break;
               }
            } while ((results.startsWith(`${dice9.length}`)) && auto__D);
            results = `${results.length}`;
            if (results.length < dice9.length) {
               dice9 = "2";
            }
            let encryptorM: Map<any, any> = new Map([[String.fromCharCode(113, 117, 97, 114, 116, 122, 0), 752], [String.fromCharCode(99, 100, 97, 116, 101, 0), 997], [String.fromCharCode(118, 105, 100, 101, 111, 105, 110, 102, 111, 104, 101, 97, 100, 101, 114, 0), 28]]);
            let shrunkK = String.fromCharCode(105, 109, 105, 113, 0);
            let predictionbuttonI = String.fromCharCode(114, 101, 102, 105, 108, 108, 0);
            results += `${shrunkK.length}`;
            encryptorM = new Map([[`${encryptorM.size}`, encryptorM.size / (Math.max(predictionbuttonI.length, 2))]]);
            shrunkK = `${encryptorM.size}`;
            predictionbuttonI = `${encryptorM.size}`;
            with_aeh = "3";
         }
         let indexH: Map<any, any> = new Map([[String.fromCharCode(116, 114, 97, 110, 115, 105, 116, 105, 111, 110, 115, 0), String.fromCharCode(99, 111, 115, 105, 103, 110, 97, 116, 111, 114, 121, 0)], [String.fromCharCode(111, 108, 108, 101, 99, 116, 105, 111, 110, 115, 0), String.fromCharCode(97, 105, 109, 100, 0)]]);
         let bridgeL: Map<any, any> = new Map([[String.fromCharCode(122, 112, 98, 105, 113, 117, 97, 100, 115, 0), false], [String.fromCharCode(98, 121, 112, 97, 115, 115, 105, 110, 103, 0), false], [String.fromCharCode(97, 116, 116, 114, 115, 0), false]]);
         let sharewhiteL: Array<any> = [983, 708, 474];
         while (2 >= (indexH.size * 5) && (bridgeL.size * indexH.size) >= 5) {
            bridgeL.set(`${sharewhiteL.length}`, 3 - indexH.size);
            break;
         }
         for (let b = 0; b < 1; b++) {
            sharewhiteL = [bridgeL.size % (Math.max(indexH.size, 2))];
         }
         indexH = new Map([[`${bridgeL.size}`, 2]]);
         let weather2 = 2.0;
         let iconuserK = 4.0;
         let blackF = 2.0;
         bridgeL.set(`${iconuserK}`, bridgeL.size | 1);
         weather2 /= Math.max(5, parseInt(`${blackF}`));
         iconuserK *= parseInt(`${weather2}`);
         blackF *= parseInt(`${weather2}`);
         sharewhiteL.push(1 << (Math.min(1, Math.abs(indexH.size))));
         let toponV = 3;
         let sharewhitei = String.fromCharCode(114, 101, 109, 111, 118, 101, 100, 0);
         bridgeL.set(`${sharewhiteL.length}`, bridgeL.size);
         toponV ^= toponV / (Math.max(8, sharewhitei.length));
         sharewhitei += "1";
         let y_locki = 0;
         indexH.set(`${sharewhiteL.length}`, sharewhiteL.length - indexH.size);
         y_locki *= 3 >> (Math.min(5, Math.abs(y_locki)));
         while (3 <= (5 % (Math.max(9, indexH.size)))) {
            indexH.set(`${bridgeL.size}`, indexH.size / (Math.max(3, 2)));
            break;
         }
         let valuesr = 5;
         let cancelp = String.fromCharCode(99, 97, 110, 99, 101, 108, 108, 0);
         let tempnodatagifu = 0.0;
         bridgeL.set(`${sharewhiteL.length}`, bridgeL.size >> (Math.min(sharewhiteL.length, 4)));
         valuesr *= parseInt(`${tempnodatagifu}`);
         cancelp += `${valuesr}`;
         tempnodatagifu += 2;
         libyogal = 74.21 < matchactiveq;
         let codej = 4;
         let liveendmodallogoz = String.fromCharCode(111, 97, 117, 116, 104, 0);
         if (liveendmodallogoz.length == codej) {
            liveendmodallogoz = `${liveendmodallogoz.length + codej}`;
         }
         codej <<= Math.min(4, Math.abs(1));
         codej /= Math.max(codej, 5);
         if (1 > (5 | codej) && (codej | liveendmodallogoz.length) > 5) {
            liveendmodallogoz = `${codej >> (Math.min(Math.abs(2), 2))}`;
         }
         liveendmodallogoz += `${codej}`;
         while ((2 / (Math.max(6, codej))) > 1 && (2 / (Math.max(3, codej))) > 1) {
            let textlayoutmanagerC = String.fromCharCode(102, 97, 105, 108, 115, 0);
            codej -= textlayoutmanagerC.length / (Math.max(liveendmodallogoz.length, 10));
            break;
         }
         matchactiveq /= Math.max(1, (String.fromCharCode(88, 0) == liveendmodallogoz ? codej : liveendmodallogoz.length));

         const currentPurchase = getState().backgroundReducer.vipPromotionPurchaseNum;

         for (let k = 0; k < 3; k++) {
            let libapminsightaj: Array<any> = [72, 582];
            let malaysiar = true;
            let routerP = String.fromCharCode(114, 95, 54, 49, 95, 114, 101, 100, 117, 99, 101, 100, 0);
            let iconuser3 = 1.0;
            let iconorientationu = malaysiar ? !malaysiar : malaysiar;
            do {
               malaysiar = iconuser3 > parseFloat(`${libapminsightaj.length}`);
               if (iconorientationu) {
                  break;
               }
            } while (iconorientationu && (!malaysiar));
            let appsl = routerP.length <= 7844532;
            do {
               let q_positionP: Map<any, any> = new Map([[String.fromCharCode(99, 104, 101, 98, 121, 115, 104, 101, 118, 0), false], [String.fromCharCode(113, 111, 115, 0), false], [String.fromCharCode(103, 111, 108, 100, 95, 110, 95, 51, 50, 0), true]]);
               routerP = `${(libapminsightaj.length + (malaysiar ? 5 : 4))}`;
               q_positionP.set(`${q_positionP.size}`, 2 % (Math.max(3, q_positionP.size)));
               if (appsl) {
                  break;
               }
            } while (appsl && ((3 % (Math.max(9, libapminsightaj.length))) < 1 || (libapminsightaj.length % (Math.max(routerP.length, 6))) < 3));
            routerP += `${((malaysiar ? 1 : 4) - parseInt(`${iconuser3}`))}`;
            for (let d = 0; d < 2; d++) {
               let iconarrowleftf = 2.0;
               routerP += `${1 * routerP.length}`;
               iconarrowleftf /= Math.max(parseFloat(`${parseInt(`${iconarrowleftf}`)}`), 3);
            }
            routerP += `${1 | routerP.length}`;
            while (malaysiar || 1 >= (libapminsightaj.length & 2)) {
               malaysiar = !malaysiar;
               break;
            }
            while ((iconuser3 / (Math.max(2.11, 2))) <= 5.79) {
               let windB = 1.0;
               let circleT: Map<any, any> = new Map([[String.fromCharCode(121, 101, 115, 116, 101, 114, 100, 97, 121, 0), false], [String.fromCharCode(109, 97, 105, 110, 110, 101, 116, 115, 0), true], [String.fromCharCode(115, 104, 105, 109, 109, 101, 114, 0), true]]);
               let icondownimgW: Array<any> = [786, 815, 743];
               iconuser3 /= Math.max(parseFloat(`${3}`), 5);
               windB -= icondownimgW.length ^ circleT.size;
               circleT = new Map([[`${icondownimgW.length}`, icondownimgW.length & 3]]);
               break;
            }
            routerP += `${parseInt(`${iconuser3}`) + routerP.length}`;
            let left_ = String.fromCharCode(114, 107, 109, 112, 112, 0);
            let stringsb = String.fromCharCode(102, 97, 100, 101, 0);
            iconuser3 += (parseFloat(`${(malaysiar ? 4 : 1) * routerP.length}`));
            left_ = `${stringsb.length % (Math.max(8, left_.length))}`;
            stringsb = `${2 + left_.length}`;
            let fills = iconuser3 <= 5421687.0;
            do {
               iconuser3 += parseFloat(`${3 ^ parseInt(`${iconuser3}`)}`);
               if (fills) {
                  break;
               }
            } while ((!libapminsightaj.includes(iconuser3)) && fills);
            let tempnodatai = 1.0;
            let iconbackwhiteP = 2.0;
            tempnodatai /= Math.max(5, routerP.length);
            libyogal = (matchactiveq / (Math.max(10, layoutX.length))) <= 97.18;
         }
         matchactiveq /= Math.max(layoutX.length | parseInt(`${langN}`), 3);
         if (4.33 > (matchactiveq * 2.61)) {
            let owngoalI = 0.0;
            let updatesa: Map<any, any> = new Map([[String.fromCharCode(110, 111, 110, 113, 117, 111, 116, 101, 100, 0), String.fromCharCode(116, 101, 97, 114, 100, 111, 119, 110, 0)], [String.fromCharCode(109, 115, 109, 112, 101, 103, 0), String.fromCharCode(118, 97, 99, 97, 110, 116, 0)]]);
            let humidityD: Map<any, any> = new Map([[String.fromCharCode(99, 97, 99, 104, 101, 100, 0), true], [String.fromCharCode(99, 111, 108, 99, 111, 108, 0), false], [String.fromCharCode(108, 105, 102, 101, 99, 121, 99, 108, 101, 0), true]]);
            let controls3 = String.fromCharCode(116, 116, 97, 100, 97, 116, 97, 0);
            let anythinkX = true;
            humidityD = new Map([[`${humidityD.size}`, controls3.length / 1]]);
            controls3 += `${((anythinkX ? 2 : 5) ^ 2)}`;
            humidityD.set(`${owngoalI}`, 1);
            matchactiveq -= ((libapminsightaA ? 2 : 5) ^ parseInt(`${matchactiveq}`));
         }
         const minuteRemain = Math.floor(VIP_PROMOTION_COUNTDOWN_MINUTE - ((Date.now() - getState().backgroundReducer.vipPromotionCountdownStart) / 1000 / 60));

         libyogal = with_aeh == String.fromCharCode(109, 0);
         matchactiveq /= Math.max((with_aeh == String.fromCharCode(74, 0) ? parseInt(`${langN}`) : with_aeh.length), 5);
         iconarrowrightblackO += `${1 % (Math.max(8, parseInt(`${matchactiveq}`)))}`;

         const purchaseRemain = VIP_PROMOTION_PURCHASE_MAX - currentPurchase;

         for (let u = 0; u < 2; u++) {
            turne.set(`${langN}`, 3 * turne.size);
         }
         for (let l = 0; l < 1; l++) {
            libyogal = (with_aeh.length + matchactiveq) >= 74.5;
         }
         layoutX = `${layoutX.length}`;
         const eachRandomRange = purchaseRemain / minuteRemain;

         libapminsightaA = !libyogal;
         matchactiveq /= Math.max(((libyogal ? 2 : 3) >> (Math.min(Math.abs(parseInt(`${matchactiveq}`)), 1))), 2);
         iconarrowrightblackO += `${parseInt(`${langN}`)}`;

         const random = Math.floor(Math.random() * eachRandomRange + 1);

         let playlist1 = String.fromCharCode(105, 109, 112, 0);
         let reducer5: Array<any> = [762, 204];
         let tumbnailX = String.fromCharCode(115, 122, 97, 98, 111, 95, 117, 95, 54, 49, 0);
         for (let m = 0; m < 3; m++) {
            let iconarrowleftK = 5.0;
            playlist1 += "3";
            iconarrowleftK /= Math.max(parseFloat(`${3}`), 1);
         }
         let bootsplashB = 1.0;
         bootsplashB += parseFloat(`${2 >> (Math.min(1, tumbnailX.length))}`);
         tumbnailX = `${playlist1.length ^ 2}`;
         if (playlist1 != tumbnailX) {
            let componentregistryz = 2.0;
            let bodanY = 4.0;
            let halffieldimageH = 0;
            tumbnailX += `${parseInt(`${componentregistryz}`)}`;
            componentregistryz += parseInt(`${bodanY}`);
            bodanY *= halffieldimageH % 3;
            halffieldimageH /= Math.max(1, 2);
         }
         let weiboY = tumbnailX.length <= 7083913;
         do {
            let singaporee: Array<any> = [68, 710];
            tumbnailX += `${reducer5.length + 2}`;
            singaporee = [singaporee.length << (Math.min(singaporee.length, 1))];
            if (weiboY) {
               break;
            }
         } while ((playlist1 != tumbnailX) && weiboY);
         let dragclosei = String.fromCharCode(108, 115, 104, 105, 102, 116, 0);
         bootsplashB += parseFloat(`${3 | dragclosei.length}`);
         let paginationS: Map<any, any> = new Map([[String.fromCharCode(97, 115, 115, 101, 114, 116, 0), String.fromCharCode(97, 99, 101, 108, 112, 0)], [String.fromCharCode(115, 119, 115, 99, 97, 108, 101, 0), String.fromCharCode(99, 111, 114, 112, 117, 115, 0)], [String.fromCharCode(114, 101, 115, 105, 103, 110, 101, 100, 95, 121, 95, 51, 50, 0), String.fromCharCode(115, 99, 105, 101, 110, 116, 105, 102, 105, 99, 0)]]);
         let vipsport8 = String.fromCharCode(105, 109, 109, 101, 100, 105, 97, 116, 101, 115, 105, 103, 110, 97, 108, 95, 107, 95, 50, 52, 0);
         bootsplashB *= (parseFloat(`${tumbnailX == String.fromCharCode(109, 0) ? parseInt(`${bootsplashB}`) : tumbnailX.length}`));
         paginationS.set(vipsport8, 1);
         vipsport8 = `${paginationS.size & vipsport8.length}`;
         while (reducer5.length < 2) {
            playlist1 = `${reducer5.length}`;
            break;
         }
         layoutX += `${tumbnailX.length % (Math.max(2, layoutX.length))}`;
         with_aeh += `${((libyogal ? 2 : 1) | 3)}`;
         libapminsightaA = iconarrowrightblackO.length > 26;

         dispatch({
            type: KMWMbnativeContainer.KMWNextScorepopsound,
            payload: currentPurchase + random,
         });

      }, VIP_PROMOTION_INTERVEL_SECONDS * 1000);

      if (backgroundState.vipPromotionCountdownStart === 0 || (now - backgroundState.vipPromotionCountdownStart) > countdownMilisecond) {
         const randomPurchase = Math.floor(Math.random() * VIP_PROMOTION_PURCHASE_RANDOM + VIP_PROMOTION_PURCHASE_MIN);

         dispatch({
            type: KMWMbnativeContainer.KMWMegaphoneRoot,
            payload: {
               start: now,
               purchase: randomPurchase,
               interval: interval,
            }
         })
      } else if (now - backgroundState.vipPromotionCountdownStart < countdownMilisecond) {
         dispatch({
            type: KMWMbnativeContainer.KMWMegaphoneRoot,
            payload: {
               interval: interval,
            }
         })
      }

   } catch (e: any) {

   }
}

export const onCloseApp = ({

} = {}) => async (dispatch: any, getState: () => yysIconclosewhiteBaiduads) => {
   try {
      const backgroundState = getState().backgroundReducer;


      yysIconstarReact.close();


      if (backgroundState.vipPromotionIntervel) {
         clearInterval(backgroundState.vipPromotionIntervel);
      }

   } catch (e: any) {

   }
}

export const loginModalShown = () => ({
   type: KMWMbnativeContainer.KMWDisconnectedGreytick,
});

export const vipPromotionModalShown = () => ({
   type: KMWMbnativeContainer.KMWSchedulerUnselected,
});
