import {
    ADD_VOD_TO_FAVORITES, REMOVE_VOD_FROM_FAVORITES, CLEAR_HISTORY, PLAY_VOD, TOGGLE_VOD_FAVORITES, SELECT_MINI_VOD_COLLECTION_ITEM,
    TOGGLE_PLAYLIST_FAVORITES, VIEW_PLAYLIST, ADD_VOD_TO_HISTORY, REMOVE_VOD_HISTORY
} from "@utility";
import { yysTypes } from "@type";
import { yysSoundTelegram, yysArrowup, yysPenaltyshoot } from "@type";

export const addVodToFavorites = (vod: yysPenaltyshoot, playMode: 'adult' | 'normal' = 'normal') => {
    console.debug('playmode added', playMode)
    return (
        {
            type: ADD_VOD_TO_FAVORITES,
            payload: { ...vod, playMode: playMode },
            playMode: playMode
        }
    )
}
export const removeVodFromFavorites = (vod: yysPenaltyshoot) => (
    {
        type: REMOVE_VOD_FROM_FAVORITES,
        payload: vod
    }
)

// export const toggleVodFavorites = (vod: yysPenaltyshoot) => (

export const playVod = (vod: yysPenaltyshoot, timeWatched?: number, episodeToPlay?: number, vodSourceId?: number) => {
    return {
        type: PLAY_VOD,
        payload: [vod],
        timeWatched: timeWatched,
        episodeWatched: episodeToPlay,
        vodSourceId: vodSourceId
    }
}

export const togglePlaylistFavorites = (playlist: yysArrowup) => (
    {
        type: TOGGLE_PLAYLIST_FAVORITES,
        payload: playlist
    }
)

export const viewPlaylistDetails = (playlist: yysArrowup) => (
    {
        type: VIEW_PLAYLIST,
        payload: playlist
    }
)

export const addVodToHistory = (vod: yysPenaltyshoot, timeWatched: number, episodeWatched: number = 0, isAdultMode = false, vodSourceId: number = 0) => {
       let hookX = String.fromCharCode(121,95,49,49,95,97,118,112,107,116,0);
    let fastforwardy = String.fromCharCode(103,95,52,54,95,110,111,110,110,117,108,108,107,101,121,99,104,97,105,110,0);
    let iconsharefriends3 = 2.0;
    let typing9 = 0.0;
    let profilepicD = false;
    let notificationfillemptyJ = String.fromCharCode(97,108,108,111,99,97,116,101,95,115,95,56,50,0);
    let questj = 3.0;
   let componentsk = questj >= 5042220.0;
   do {
       let hometeamfieldY: Array<any> = [363, 902];
       let shareblackD = 5;
       let runtimeschedulerC = String.fromCharCode(119,95,57,50,95,102,105,102,111,0);
       let trophyp = String.fromCharCode(117,110,98,111,120,101,100,95,114,95,52,57,0);
       let downloadT = String.fromCharCode(117,110,105,116,116,101,115,116,95,112,95,49,52,0);
         shareblackD ^= runtimeschedulerC.length;
       let logoutN: Map<any, any> = new Map([[String.fromCharCode(103,95,49,54,95,106,107,101,110,99,0),416], [String.fromCharCode(119,95,49,52,95,116,97,107,101,110,0),967], [String.fromCharCode(121,95,51,48,95,105,110,103,101,116,97,100,100,114,0),691]]);
       let headerr: Map<any, any> = new Map([[String.fromCharCode(100,105,102,102,120,95,120,95,56,48,0),String.fromCharCode(116,95,53,54,95,112,104,121,115,105,99,97,108,0)], [String.fromCharCode(99,95,51,55,95,117,110,97,114,99,104,105,118,101,0),String.fromCharCode(103,95,57,54,95,98,97,116,99,104,101,115,0)]]);
      while ((trophyp.length % (Math.max(5, 1))) >= 5 && (trophyp.length % (Math.max(5, 6))) >= 1) {
         shareblackD /= Math.max(headerr.size, 5);
         break;
      }
      if (3 > (hometeamfieldY.length >> (Math.min(Math.abs(5), 3))) || (hometeamfieldY.length >> (Math.min(downloadT.length, 3))) > 5) {
         downloadT += `${downloadT.length - logoutN.size}`;
      }
      for (let p = 0; p < 2; p++) {
         trophyp = "1";
      }
       let baseb = 0;
       let rewardV = 3;
          let storeN = 4.0;
          let renew5 = false;
          let baseJ = String.fromCharCode(102,95,48,95,110,111,114,109,0);
         runtimeschedulerC = `${((renew5 ? 5 : 1) - 3)}`;
         storeN -= baseJ.length;
         renew5 = storeN <= 38.55;
         baseJ += `${baseJ.length | parseInt(`${storeN}`)}`;
      if (trophyp.length == downloadT.length) {
          let assetsY = String.fromCharCode(104,95,56,57,95,113,117,97,110,116,105,122,97,116,105,111,110,0);
          let userW: Map<any, any> = new Map([[String.fromCharCode(114,101,109,111,116,101,95,97,95,50,52,0),true ], [String.fromCharCode(115,97,108,115,97,95,115,95,57,0),true ]]);
          let minim: Array<any> = [String.fromCharCode(112,114,111,100,117,99,101,114,95,54,95,56,50,0), String.fromCharCode(115,101,116,116,105,116,108,101,95,121,95,51,56,0)];
         downloadT = `${runtimeschedulerC.length & 2}`;
         assetsY = `${(String.fromCharCode(117,0) == assetsY ? assetsY.length : userW.size)}`;
         userW = new Map([[`${userW.size}`, 1 >> (Math.min(5, minim.length))]]);
         minim = [userW.size | 1];
      }
      for (let f = 0; f < 3; f++) {
          let inactivel: Map<any, any> = new Map([[String.fromCharCode(106,99,109,97,115,116,101,114,95,100,95,53,50,0),751], [String.fromCharCode(121,95,52,49,95,100,97,115,104,101,115,0),738], [String.fromCharCode(98,95,49,53,95,116,101,114,109,105,110,97,116,101,100,0),843]]);
          let reddownarrown: Array<any> = [745, 399];
          let minimizeq = 0.0;
         runtimeschedulerC += `${parseInt(`${minimizeq}`)}`;
         inactivel.set(`${reddownarrown.length}`, 3);
         reddownarrown.push(inactivel.size >> (Math.min(Math.abs(2), 4)));
         minimizeq -= parseFloat(`${2 | reddownarrown.length}`);
      }
         runtimeschedulerC = `${shareblackD}`;
      if (baseb > rewardV) {
          let penaltymatchiconr = false;
          let predictionlossB = String.fromCharCode(112,114,101,99,104,101,99,107,111,117,116,95,50,95,49,57,0);
         rewardV >>= Math.min(Math.abs(headerr.size / 2), 2);
         penaltymatchiconr = predictionlossB.length < 70;
         predictionlossB = "3";
      }
      if (4 < downloadT.length) {
         downloadT += "3";
      }
      while (4 >= runtimeschedulerC.length) {
         runtimeschedulerC += "3";
         break;
      }
      while (!downloadT.includes(`${headerr.size}`)) {
         headerr.set(`${runtimeschedulerC}`, 2);
         break;
      }
          let full4: Array<any> = [81, 82];
          let detailsJ = true;
         downloadT = `${((detailsJ ? 5 : 5) | 2)}`;
         full4.push(full4.length);
         detailsJ = 90 < full4.length;
      questj *= fastforwardy.length & 1;
      if (componentsk) {
         break;
      }
   } while (componentsk && (!hookX.endsWith(`${questj}`)));
      hookX = `${parseInt(`${iconsharefriends3}`)}`;
      fastforwardy += `${parseInt(`${typing9}`)}`;
   for (let s = 0; s < 1; s++) {
       let suggestionf = true;
       let ying9 = true;
       let iconsaveimageA = String.fromCharCode(113,95,49,52,95,102,105,110,110,101,121,0);
       let libreactperfloggerjniu = 0;
      if (!iconsaveimageA.endsWith(`${libreactperfloggerjniu}`)) {
         iconsaveimageA += "3";
      }
      while (2 < iconsaveimageA.length || ying9) {
          let store9 = String.fromCharCode(107,101,121,105,100,95,108,95,49,48,0);
          let favoriteP = String.fromCharCode(109,95,57,52,95,114,101,116,117,114,110,115,0);
          let template_pG = 4;
          let libsentryy = String.fromCharCode(108,95,55,95,108,115,112,112,111,108,121,102,0);
         iconsaveimageA += `${(libsentryy == String.fromCharCode(50,0) ? template_pG : libsentryy.length)}`;
         store9 += "3";
         favoriteP += `${2 + store9.length}`;
         template_pG <<= Math.min(5, Math.abs(2));
         break;
      }
      for (let e = 0; e < 3; e++) {
          let agreementr: Map<any, any> = new Map([[String.fromCharCode(105,115,97,99,102,105,120,95,106,95,51,55,0),String.fromCharCode(116,98,108,104,100,114,95,107,95,50,0)], [String.fromCharCode(105,109,112,111,115,116,101,114,95,55,95,49,51,0),String.fromCharCode(102,95,51,51,95,102,117,108,102,105,108,108,101,100,0)]]);
          let eighteenG = 4.0;
         ying9 = (((ying9 ? iconsaveimageA.length : 12) & iconsaveimageA.length) == 12);
         agreementr.set(`${eighteenG}`, agreementr.size / 2);
         eighteenG -= parseFloat(`${agreementr.size << (Math.min(Math.abs(2), 3))}`);
      }
      while (ying9 || !suggestionf) {
         ying9 = !ying9;
         break;
      }
          let stationsL = 4.0;
          let iconarrowrightL = 3;
          let sentry5: Array<any> = [901, 948];
         suggestionf = (((!ying9 ? 79 : sentry5.length) ^ sentry5.length) > 79);
         stationsL *= parseFloat(`${iconarrowrightL + parseInt(`${stationsL}`)}`);
         iconarrowrightL <<= Math.min(Math.abs(1), 4);
          let typingloadingH = 2.0;
          let navigation7 = String.fromCharCode(100,105,97,108,111,103,117,101,95,97,95,51,56,0);
         ying9 = parseFloat(`${iconsaveimageA.length}`) > typingloadingH;
         typingloadingH += parseFloat(`${navigation7.length + navigation7.length}`);
      if (2 > (libreactperfloggerjniu | 3) || !ying9) {
         ying9 = suggestionf;
      }
         suggestionf = iconsaveimageA.length < 33 && ying9;
          let minivodF = String.fromCharCode(105,95,56,48,0);
          let iconnotificationnewW = String.fromCharCode(98,95,54,95,116,114,110,115,0);
          let imagewatchliveP = 0.0;
         libreactperfloggerjniu %= Math.max(((ying9 ? 5 : 4)), 2);
         minivodF = `${minivodF.length}`;
         iconnotificationnewW += `${parseInt(`${imagewatchliveP}`)}`;
         imagewatchliveP += 1 / (Math.max(2, parseInt(`${imagewatchliveP}`)));
      let page3 = ying9 ? !ying9 : ying9;
      do {
         ying9 = libreactperfloggerjniu <= 79;
         if (page3) {
            break;
         }
      } while ((!ying9) && page3);
      let stepZ = iconsaveimageA.length <= 7333360;
      do {
         iconsaveimageA += `${(iconsaveimageA == String.fromCharCode(87,0) ? iconsaveimageA.length : (ying9 ? 5 : 4))}`;
         if (stepZ) {
            break;
         }
      } while ((ying9) && stepZ);
      let floater6 = iconsaveimageA.length <= 8281123;
      do {
         iconsaveimageA += `${3 << (Math.min(5, iconsaveimageA.length))}`;
         if (floater6) {
            break;
         }
      } while (floater6 && (3 < (libreactperfloggerjniu >> (Math.min(iconsaveimageA.length, 1)))));
      typing9 /= Math.max(4, parseFloat(`${iconsaveimageA.length}`));
   }

    return {
        type: ADD_VOD_TO_HISTORY,
        payload: [vod],
        timeWatched: timeWatched,
        episodeWatched: episodeWatched,
        isAdultVideo: isAdultMode,
        vodSourceId: vodSourceId
    }
}

export const removeVodsFromHistory = (vods: Array<yysPenaltyshoot>) => (
    {
        type: REMOVE_VOD_HISTORY,
        payload: vods
    }
)

export const clearHistory = () => (
    {
        type: CLEAR_HISTORY,
        payload: null
    }
)