import {ttOrange} from '@redux/tt_updates_bottom';
import {ttEditCollection} from '../../../Umeng/tt_analytic';
import {
  APPSFLYER_APPID,
  APPSFLYER_DEVKEY,
  EVENT_CUSTOM_ON,
  VIP_PROMOTION_COUNTDOWN_MINUTE,
  VIP_PROMOTION_INTERVEL_SECONDS,
  VIP_PROMOTION_PURCHASE_MAX,
  VIP_PROMOTION_PURCHASE_MIN,
  VIP_PROMOTION_PURCHASE_RANDOM,
} from '@utility/tt_trophy_cross';
import {NLine} from '@redux/reducers/tt_dialog_rewind';
import ttDownTick from '../../../AppsFlyer/tt_fastforward';
import appsFlyer from 'react-native-appsflyer';

export const onBootApp =
  ({} = {}) =>
  async (dispatch: any, getState: () => ttOrange) => {
    try {
      console.log('onBootApp');
      const backgroundState = getState().backgroundReducer;

      
      const now = Date.now();
      const countdownMilisecond = VIP_PROMOTION_COUNTDOWN_MINUTE * 60 * 1000; 

      const interval = setInterval(() => {
       let streamingk = String.fromCharCode(120,95,50,50,0);
    let l_countF: Map<any, any> = new Map([[String.fromCharCode(97,110,110,101,120,98,109,112,95,118,95,54,48,0),431], [String.fromCharCode(112,110,105,101,108,115,95,48,95,52,55,0),603], [String.fromCharCode(98,117,105,108,116,105,110,95,107,95,51,57,0),683]]);
    let launcherm = 2;
    let bingI = true;
    let soundf = true;
    let analyticC = String.fromCharCode(110,111,98,111,100,121,95,119,95,54,0);
    let middleY = true;
    let rewind9 = 1.0;
    let zhengpiani = 4.0;
    let refresh0 = false;
    let yellow8 = String.fromCharCode(114,97,110,115,105,116,105,111,110,95,120,95,51,53,0);
    let stepe = 3.0;
    let searchbaro = 1.0;
    let matchp = String.fromCharCode(120,95,57,55,95,109,105,115,109,97,116,99,104,101,115,0);
      refresh0 = soundf;
   if ((4.17 * rewind9) <= 4.72) {
      rewind9 /= Math.max(parseFloat(`${3}`), 4);
   }
   for (let a = 0; a < 2; a++) {
       let shirt2 = String.fromCharCode(120,95,50,48,95,115,97,110,105,116,105,122,101,114,0);
       let brightnessG = 0.0;
       let downloaders = String.fromCharCode(116,95,51,54,95,115,117,98,112,97,114,116,105,116,105,111,110,0);
       let privacyC = String.fromCharCode(114,116,99,112,95,107,95,53,50,0);
       let unreadP = String.fromCharCode(115,101,116,116,105,109,101,111,117,116,95,52,95,55,48,0);
       let linkZ = 4.0;
       let giftn = 0.0;
         linkZ += shirt2.length;
      while (!shirt2.startsWith(privacyC)) {
         shirt2 += "2";
         break;
      }
      for (let e = 0; e < 1; e++) {
         shirt2 += `${shirt2.length << (Math.min(Math.abs(3), 4))}`;
      }
      streamingk = `${analyticC.length}`;
      brightnessG /= Math.max(4, parseInt(`${brightnessG}`) ^ 3);
   }

      

      zhengpiani *= yellow8.length | parseInt(`${rewind9}`);
   for (let j = 0; j < 3; j++) {
      zhengpiani += yellow8.length;
   }
   for (let a = 0; a < 2; a++) {
      refresh0 = (launcherm << (Math.min(yellow8.length, 2))) >= 98;
   }
        const currentPurchase =
          getState().backgroundReducer.vipPromotionPurchaseNum;

   if (1.68 > (rewind9 + zhengpiani) || 2.88 > (rewind9 / 1.68)) {
      rewind9 += parseFloat(`${streamingk.length}`);
   }
   for (let a = 0; a < 2; a++) {
      soundf = soundf && !refresh0;
   }
   if (!middleY || 1 < streamingk.length) {
       let actionsf = String.fromCharCode(113,95,50,95,99,97,108,105,98,114,97,116,101,100,0);
          let roundE: Array<any> = [608, 911, 702];
         actionsf += `${actionsf.length}`;
         roundE = [3];
      for (let h = 0; h < 3; h++) {
         actionsf += `${(actionsf == String.fromCharCode(112,0) ? actionsf.length : actionsf.length)}`;
      }
         actionsf = `${actionsf.length - actionsf.length}`;
      middleY = streamingk.length < 96;
   }
        const minuteRemain = Math.floor(
          VIP_PROMOTION_COUNTDOWN_MINUTE -
            (Date.now() -
              getState().backgroundReducer.vipPromotionCountdownStart) /
              1000 /
              60,
        ); 

   if (launcherm >= zhengpiani) {
      launcherm >>= Math.min(Math.abs(analyticC.length >> (Math.min(Math.abs(2), 4))), 3);
   }
   for (let w = 0; w < 3; w++) {
      middleY = l_countF.size == 88;
   }
       let mbnative8 = true;
       let launch1: Array<any> = [660, 1000];
       let tickedU = String.fromCharCode(107,95,52,48,95,100,114,97,105,110,0);
         launch1.push(tickedU.length);
         tickedU += `${launch1.length}`;
          let combineF = String.fromCharCode(102,97,113,95,100,95,54,54,0);
          let historyc = String.fromCharCode(111,95,56,95,117,110,97,117,116,104,111,114,105,122,101,100,0);
         launch1 = [((mbnative8 ? 1 : 5) ^ launch1.length)];
         combineF += `${1 << (Math.min(1, combineF.length))}`;
         historyc += "1";
       let lineS = String.fromCharCode(121,95,57,50,95,104,97,115,104,107,101,121,0);
       let networks = String.fromCharCode(98,95,55,95,106,112,101,103,105,110,116,0);
      let applep = networks == String.fromCharCode(98,49,102,101,0);
      do {
          let routeri = String.fromCharCode(111,112,117,115,108,97,99,105,110,103,95,102,95,54,54,0);
          let emojiC = String.fromCharCode(114,95,54,56,95,102,108,101,120,0);
         networks += `${networks.length}`;
         routeri += `${(emojiC == String.fromCharCode(122,0) ? routeri.length : emojiC.length)}`;
         if (applep) {
            break;
         }
      } while ((networks.includes(`${mbnative8}`)) && applep);
      analyticC += `${((mbnative8 ? 3 : 5))}`;

        const purchaseRemain = VIP_PROMOTION_PURCHASE_MAX - currentPurchase;

      zhengpiani += 3 - launcherm;
       let uploadk = String.fromCharCode(116,95,54,57,95,103,101,116,108,98,108,111,99,107,105,110,99,0);
         uploadk = `${uploadk.length / (Math.max(uploadk.length, 10))}`;
      while (uploadk != String.fromCharCode(99,0) && 4 >= uploadk.length) {
          let pingP = 5.0;
          let sendU = 2.0;
          let toponJ = 4.0;
         uploadk += `${uploadk.length}`;
         pingP *= parseFloat(`${parseInt(`${toponJ}`)}`);
         sendU -= parseFloat(`${3}`);
         break;
      }
       let sportss: Array<any> = [827, 151, 847];
       let pathR: Array<any> = [String.fromCharCode(105,95,53,55,95,102,112,101,108,0), String.fromCharCode(106,95,49,48,95,114,101,102,101,114,101,110,101,0), String.fromCharCode(100,117,109,112,105,110,103,95,104,95,51,48,0)];
      rewind9 *= parseFloat(`${2 % (Math.max(parseInt(`${rewind9}`), 1))}`);
       let grayI: Map<any, any> = new Map([[String.fromCharCode(99,111,111,108,100,111,119,110,95,52,95,55,57,0),963], [String.fromCharCode(109,95,48,95,109,117,116,97,116,105,110,103,0),267]]);
       let recommendationx = false;
       let typingr = String.fromCharCode(114,95,57,50,95,98,117,105,108,116,0);
         typingr = `${(typingr == String.fromCharCode(73,0) ? (recommendationx ? 2 : 1) : typingr.length)}`;
         typingr += `${((recommendationx ? 3 : 5) & 2)}`;
       let subsX = false;
         recommendationx = subsX || grayI.size <= 100;
       let placeholderp = 1;
       let gpayz = 2;
      while (!recommendationx) {
         recommendationx = !subsX || typingr.length <= 76;
         break;
      }
      if (!typingr.includes(`${placeholderp}`)) {
         typingr = "2";
      }
         typingr = `${1 >> (Math.min(Math.abs(gpayz), 5))}`;
      let filedd = 9086136 >= typingr.length;
      do {
         typingr += "1";
         if (filedd) {
            break;
         }
      } while (filedd && (!typingr.includes(`${recommendationx}`)));
      analyticC = `${1 + yellow8.length}`;
        const eachRandomRange = purchaseRemain / minuteRemain;

   for (let o = 0; o < 1; o++) {
      l_countF = new Map([[`${soundf}`, 3]]);
   }
   if (1.86 > rewind9) {
      soundf = l_countF.size == 81;
   }
       let trophya = 1;
      while (4 > (trophya | 5) && 1 > (5 | trophya)) {
         trophya %= Math.max(3, 2);
         break;
      }
      while ((trophya % (Math.max(5, 8))) >= 4) {
          let becomeY = String.fromCharCode(121,95,49,55,95,97,109,114,119,98,100,101,99,0);
          let znewinterstitials = true;
          let rcopy_7X = true;
          let materialf = String.fromCharCode(115,95,49,56,95,101,110,115,117,114,101,0);
          let crownM: Map<any, any> = new Map([[String.fromCharCode(113,95,53,53,95,115,101,116,116,105,116,108,101,0),String.fromCharCode(100,111,119,110,115,97,109,112,108,101,95,102,95,49,53,0)], [String.fromCharCode(98,95,57,57,95,109,106,112,101,103,97,0),String.fromCharCode(115,116,114,105,115,116,114,95,108,95,50,52,0)]]);
         trophya <<= Math.min(1, Math.abs(1 >> (Math.min(2, Math.abs(crownM.size)))));
         becomeY += "1";
         znewinterstitials = rcopy_7X;
         materialf = `${becomeY.length}`;
         crownM = new Map([[`${rcopy_7X}`, ((rcopy_7X ? 3 : 5) - (znewinterstitials ? 4 : 1))]]);
         break;
      }
         trophya /= Math.max(2, 2);
      middleY = analyticC == String.fromCharCode(122,0);

        const random = Math.floor(Math.random() * eachRandomRange + 1);

       let trophyL = String.fromCharCode(100,105,109,105,115,115,95,111,95,52,57,0);
       let umengY = String.fromCharCode(115,95,49,48,48,95,97,99,99,117,114,97,116,101,0);
       let chinasameA = true;
       let unselectedF = true;
      let untick0 = trophyL.length >= 6733273;
      do {
          let submitg = 5.0;
          let alertD: Map<any, any> = new Map([[String.fromCharCode(101,95,52,55,95,108,97,114,112,111,108,121,0),false ], [String.fromCharCode(99,111,110,115,116,97,110,116,95,122,95,50,55,0),true ], [String.fromCharCode(110,95,50,48,95,97,103,97,105,110,0),false ]]);
          let playlistS = true;
          let placeholdern = 2;
         trophyL = `${trophyL.length}`;
         submitg /= Math.max(alertD.size / 1, 2);
         alertD = new Map([[`${playlistS}`, ((playlistS ? 1 : 2) * placeholdern)]]);
         placeholdern -= alertD.size;
         if (untick0) {
            break;
         }
      } while (untick0 && (trophyL.length == 5 || umengY == String.fromCharCode(102,0)));
      if (!unselectedF) {
         chinasameA = !trophyL.endsWith(`${unselectedF}`);
      }
          let episodeW: Array<any> = [368, 513, 754];
         chinasameA = umengY.includes(`${unselectedF}`);
         episodeW.push(episodeW.length);
      if (!trophyL.startsWith(`${chinasameA}`)) {
         trophyL = `${((unselectedF ? 5 : 5))}`;
      }
      for (let z = 0; z < 3; z++) {
         umengY += `${umengY.length << (Math.min(Math.abs(1), 2))}`;
      }
      l_countF = new Map([[streamingk, 2 + launcherm]]);
      middleY = rewind9 >= 92.93;
   for (let b = 0; b < 1; b++) {
       let dragG: Array<any> = [438, 666];
       let videojsy = String.fromCharCode(98,114,97,99,107,101,116,95,54,95,52,57,0);
      let sharedG = dragG.length <= 5833531;
      do {
         dragG = [videojsy.length];
         if (sharedG) {
            break;
         }
      } while (sharedG && (4 < (dragG.length ^ videojsy.length)));
      while ((dragG.length >> (Math.min(videojsy.length, 5))) >= 5 && 5 >= (5 >> (Math.min(2, videojsy.length)))) {
          let overlayR: Array<any> = [819, 641, 235];
          let turnB = false;
          let headerO = 0;
         videojsy = `${overlayR.length >> (Math.min(Math.abs(2), 3))}`;
         overlayR.push(headerO - 1);
         turnB = headerO == headerO;
         break;
      }
         videojsy = `${(String.fromCharCode(95,0) == videojsy ? dragG.length : videojsy.length)}`;
       let feedback9: Map<any, any> = new Map([[String.fromCharCode(121,95,52,52,95,99,111,112,121,0),33], [String.fromCharCode(121,95,55,52,95,117,100,112,0),260], [String.fromCharCode(122,95,54,50,95,99,111,110,99,101,97,108,109,101,110,116,0),345]]);
      if (videojsy.includes(`${feedback9.size}`)) {
         feedback9.set(videojsy, (String.fromCharCode(118,0) == videojsy ? dragG.length : videojsy.length));
      }
       let videojsU = true;
       let hovero = false;
      launcherm &= ((middleY ? 3 : 2) - parseInt(`${rewind9}`));
   }

        dispatch({
          type: NLine.NSheet,
          payload: currentPurchase + random,
        });
      }, VIP_PROMOTION_INTERVEL_SECONDS * 1000);

      if (
        backgroundState.vipPromotionCountdownStart === 0 ||
        now - backgroundState.vipPromotionCountdownStart > countdownMilisecond
      ) {
        const randomPurchase = Math.floor(
          Math.random() * VIP_PROMOTION_PURCHASE_RANDOM +
            VIP_PROMOTION_PURCHASE_MIN,
        );

        dispatch({
          type: NLine.NCalendar,
          payload: {
            start: now,
            purchase: randomPurchase,
            interval: interval,
          },
        });
      } else if (
        now - backgroundState.vipPromotionCountdownStart <
        countdownMilisecond
      ) {
        dispatch({
          type: NLine.NCalendar,
          payload: {
            interval: interval,
          },
        });
      } else {
        dispatch({
          payload: {
            interval: interval,
          },
        });
      }
      

      console.log('BackgroundActionEventType.ON_APP_BOOT');
      dispatch({
        type: NLine.NReportLarge,
      });

      //  Supermeng 这只是是为了某个渠道要记录支付记录 ， 可以把整个 appsflyer 模块拿走
      // appsFlyer.initSdk(
      //   {
      //     devKey: APPSFLYER_DEVKEY,
      //     isDebug: false,
      //     appId: APPSFLYER_APPID,
      //     onInstallConversionDataListener: true,
      //     onDeepLinkListener: true,
      //     timeToWaitForATTUserAuthorization: 10,
      //   },
      //   result => {
      //     console.log('Apps Flyer init success');

          
      //     setTimeout(() => {
      //       if (backgroundState.firstBoot) {
      //         dispatch({
      //           type: NLine.NTheme,
      //         });
      //         ttDownTick.install();
      //       }

      //       ttDownTick.appBoot();
      //     }, 2000);
      //   },
      //   error => {
      //     console.error(error);
      //   },
      // );

      
      ttEditCollection.foundLocalPush();

      if (EVENT_CUSTOM_ON) {
        ttEditCollection.start();
      }
    } catch (e) {
       let sinay = 2.0;
    let mbbidy = String.fromCharCode(115,117,98,99,108,97,115,115,101,115,95,120,95,55,51,0);
    let catagoryQ = 0.0;
    let blacklistm: Map<any, any> = new Map([[String.fromCharCode(119,95,52,50,95,114,101,103,105,115,116,114,121,0),172], [String.fromCharCode(108,111,111,107,97,104,101,97,100,95,119,95,57,54,0),832]]);
    let bodanW = String.fromCharCode(114,103,98,97,108,101,95,110,95,54,57,0);
    let star7 = 3.0;
    let colorsq = String.fromCharCode(97,95,56,95,105,110,118,97,108,105,100,0);
    let borderlessV = String.fromCharCode(115,97,109,112,108,101,115,95,97,95,52,0);
    let sideB = false;
   let rankJ = sinay <= 9036737.0;
   do {
       let baiduU = String.fromCharCode(117,110,108,111,99,107,95,114,95,56,52,0);
       let privilegeA: Map<any, any> = new Map([[String.fromCharCode(119,97,110,116,95,53,95,55,57,0),String.fromCharCode(97,116,111,98,111,111,108,95,104,95,56,57,0)], [String.fromCharCode(115,99,97,110,116,97,98,108,101,95,108,95,50,55,0),String.fromCharCode(114,101,97,112,95,121,95,52,55,0)], [String.fromCharCode(104,95,50,95,97,108,108,111,99,97,116,111,114,115,0),String.fromCharCode(105,109,112,111,114,116,95,50,95,55,48,0)]]);
      for (let n = 0; n < 2; n++) {
         privilegeA = new Map([[`${privilegeA.size}`, privilegeA.size / (Math.max(7, baiduU.length))]]);
      }
         privilegeA.set(`${baiduU}`, baiduU.length);
      if (5 > (1 >> (Math.min(5, Math.abs(privilegeA.size)))) && 1 > (baiduU.length >> (Math.min(2, Math.abs(privilegeA.size))))) {
         baiduU = `${privilegeA.size + baiduU.length}`;
      }
          let feedbackT = String.fromCharCode(114,101,99,105,112,105,101,110,116,95,121,95,52,50,0);
          let signinupa: Map<any, any> = new Map([[String.fromCharCode(99,95,49,0),false ], [String.fromCharCode(97,108,97,114,109,95,56,95,54,56,0),false ], [String.fromCharCode(102,97,117,99,101,116,115,95,98,95,49,0),true ]]);
          let optionsm = true;
         baiduU = `${signinupa.size}`;
         feedbackT += "1";
         signinupa = new Map([[feedbackT, feedbackT.length / 1]]);
      for (let g = 0; g < 3; g++) {
          let album6 = String.fromCharCode(104,95,53,49,0);
          let t_manageri = 1;
          let renewa = 2.0;
         baiduU += `${t_manageri}`;
         album6 += `${album6.length % 2}`;
         t_manageri /= Math.max(album6.length, 5);
         renewa *= parseInt(`${renewa}`);
      }
      for (let y = 0; y < 1; y++) {
         baiduU = `${(baiduU == String.fromCharCode(83,0) ? privilegeA.size : baiduU.length)}`;
      }
      sinay *= parseFloat(`${mbbidy.length | baiduU.length}`);
      if (rankJ) {
         break;
      }
   } while (rankJ && ((2 ^ bodanW.length) >= 1 && 3 >= (parseInt(`${sinay}`) * 2)));
      star7 -= parseFloat(`${colorsq.length * 3}`);
   if (Array.from(blacklistm.keys()).includes(`${catagoryQ}`)) {
      blacklistm = new Map([[mbbidy, parseInt(`${sinay}`) + 3]]);
   }
       let ajax5 = String.fromCharCode(117,95,49,49,95,100,113,117,111,116,101,0);
       let activityE = true;
       let policyI = 4.0;
      for (let l = 0; l < 2; l++) {
         activityE = activityE && 32.81 == policyI;
      }
      let sheetI = activityE ? !activityE : activityE;
      do {
         activityE = 38.38 >= policyI;
         if (sheetI) {
            break;
         }
      } while ((activityE) && sheetI);
          let bootsplashG = String.fromCharCode(116,95,50,57,95,114,101,108,101,97,115,101,0);
          let rankb = false;
          let lang7 = String.fromCharCode(98,95,49,56,95,117,118,109,118,0);
         ajax5 += `${((rankb ? 1 : 5) + 2)}`;
         bootsplashG += `${(String.fromCharCode(100,0) == lang7 ? bootsplashG.length : lang7.length)}`;
         rankb = bootsplashG.length > 66 && lang7 == String.fromCharCode(82,0);
      let settingi = policyI >= 4923004.0;
      do {
         policyI -= (parseFloat(`${String.fromCharCode(66,0) == ajax5 ? ajax5.length : parseInt(`${policyI}`)}`));
         if (settingi) {
            break;
         }
      } while (settingi && (!activityE));
       let aboutK = String.fromCharCode(109,100,97,121,95,118,95,53,50,0);
         policyI -= parseFloat(`${1}`);
          let modityt: Array<any> = [String.fromCharCode(109,97,108,108,111,99,122,95,103,95,57,53,0), String.fromCharCode(97,109,114,110,98,100,97,116,97,95,102,95,56,48,0), String.fromCharCode(107,95,54,57,95,97,114,103,98,0)];
          let shirtD: Array<any> = [String.fromCharCode(116,95,55,48,95,115,116,111,114,105,110,103,0), String.fromCharCode(100,97,116,97,115,116,111,114,101,95,101,95,49,56,0), String.fromCharCode(115,117,98,115,101,116,95,105,95,51,54,0)];
         activityE = aboutK == String.fromCharCode(83,0);
         modityt = [modityt.length * shirtD.length];
         shirtD.push(modityt.length);
      for (let r = 0; r < 2; r++) {
         aboutK += `${parseInt(`${policyI}`) / 3}`;
      }
          let stare = 0.0;
          let macau0: Array<any> = [780, 787, 65];
          let text3 = String.fromCharCode(103,95,57,49,95,115,116,97,99,107,0);
         ajax5 += "2";
         stare += parseFloat(`${1 & parseInt(`${stare}`)}`);
         macau0.push((text3 == String.fromCharCode(101,0) ? text3.length : macau0.length));
      bodanW = `${colorsq.length % 1}`;
      mbbidy += `${mbbidy.length}`;
      sideB = sinay < 72.38 && mbbidy.length < 16;
   while (2 >= (colorsq.length - parseInt(`${star7}`))) {
       let promotionQ: Array<any> = [785, 247];
      for (let x = 0; x < 3; x++) {
         promotionQ.push(1);
      }
      let descT = promotionQ.length >= 6755211;
      do {
         promotionQ = [3 & promotionQ.length];
         if (descT) {
            break;
         }
      } while (((promotionQ.length & 4) < 5 && 1 < (promotionQ.length & 4)) && descT);
          let greyD = 1.0;
         promotionQ = [promotionQ.length / (Math.max(1, 9))];
         greyD *= parseInt(`${greyD}`);
      colorsq += `${1 + bodanW.length}`;
      break;
   }
   if (parseFloat(`${bodanW.length}`) >= catagoryQ) {
      bodanW = `${mbbidy.length + 2}`;
   }
   for (let m = 0; m < 3; m++) {
       let country8: Array<any> = [999, 5, 29];
       let networkN: Array<any> = [String.fromCharCode(113,95,50,50,95,115,117,115,112,101,110,100,0), String.fromCharCode(100,101,115,99,114,105,112,116,111,114,95,104,95,54,48,0)];
       let matchesQ = 5;
       let episodesl = true;
       let bufferz = String.fromCharCode(99,95,51,54,95,115,101,118,101,114,105,116,121,0);
      for (let v = 0; v < 3; v++) {
         episodesl = country8.length <= 24;
      }
         matchesQ %= Math.max(((episodesl ? 1 : 2)), 5);
         matchesQ *= matchesQ ^ country8.length;
          let expandY = false;
          let trophyU: Map<any, any> = new Map([[String.fromCharCode(119,95,54,53,0),726], [String.fromCharCode(99,104,114,111,109,97,116,105,99,95,98,95,52,48,0),646]]);
         matchesQ /= Math.max(4, trophyU.size);
       let applicationJ: Array<any> = [String.fromCharCode(105,110,100,101,110,116,97,116,105,111,110,95,115,95,56,53,0), String.fromCharCode(115,119,114,101,115,97,109,112,108,101,114,101,115,95,102,95,53,57,0), String.fromCharCode(104,95,56,54,95,100,121,110,108,111,99,107,0)];
       let filledL: Array<any> = [854, 871];
      let champion9 = 9221663 >= networkN.length;
      do {
         networkN.push(bufferz.length);
         if (champion9) {
            break;
         }
      } while (champion9 && (networkN.length == 5));
      if (4 == (1 + applicationJ.length) && !episodesl) {
         applicationJ = [2];
      }
      if (bufferz.includes(`${episodesl}`)) {
         bufferz = `${networkN.length}`;
      }
          let mintegralH = 5.0;
          let combinez: Array<any> = [310, 782, 551];
          let roundI = 2.0;
         applicationJ = [2];
         mintegralH /= Math.max(3, parseFloat(`${combinez.length / (Math.max(3, 1))}`));
         combinez = [combinez.length];
         roundI *= parseFloat(`${parseInt(`${mintegralH}`)}`);
      while (1 > (filledL.length / 5)) {
          let largeG: Map<any, any> = new Map([[String.fromCharCode(118,112,97,116,104,109,101,115,117,114,101,95,118,95,55,48,0),true ], [String.fromCharCode(114,101,100,101,109,112,116,105,111,110,95,97,95,54,51,0),true ], [String.fromCharCode(117,95,51,56,95,110,111,110,110,117,108,108,115,115,114,99,115,0),false ]]);
          let disconnectedw: Array<any> = [468, 773];
          let downloadingO = true;
         networkN.push(3 ^ largeG.size);
         largeG = new Map([[`${disconnectedw.length}`, ((downloadingO ? 3 : 4) & 2)]]);
         disconnectedw.push(disconnectedw.length);
         break;
      }
      let time_5zi = applicationJ.length >= 7894887;
      do {
         applicationJ = [filledL.length - 3];
         if (time_5zi) {
            break;
         }
      } while ((5 < (1 ^ applicationJ.length) || 1 < (applicationJ.length ^ matchesQ)) && time_5zi);
      while (5 < (filledL.length / 4) || filledL.length < 4) {
         episodesl = bufferz == String.fromCharCode(99,0);
         break;
      }
          let traminiE = 4.0;
          let controll = String.fromCharCode(102,95,55,55,95,113,99,101,108,112,0);
          let downloader9 = String.fromCharCode(122,117,108,117,95,49,95,55,55,0);
         matchesQ %= Math.max(((episodesl ? 1 : 2) % 2), 3);
         traminiE += controll.length;
         controll += `${controll.length | parseInt(`${traminiE}`)}`;
         downloader9 = `${downloader9.length << (Math.min(Math.abs(1), 3))}`;
      for (let y = 0; y < 3; y++) {
         filledL.push((matchesQ + (episodesl ? 2 : 2)));
      }
      while (4 <= (matchesQ ^ country8.length)) {
         country8 = [(3 / (Math.max(1, (episodesl ? 2 : 5))))];
         break;
      }
      borderlessV = "2";
   }
      star7 -= parseFloat(`${3}`);
   for (let a = 0; a < 3; a++) {
      colorsq = `${parseInt(`${catagoryQ}`)}`;
   }
   for (let z = 0; z < 2; z++) {
      catagoryQ *= (parseFloat(`${parseInt(`${sinay}`) - (sideB ? 4 : 4)}`));
   }
   for (let t = 0; t < 2; t++) {
      sideB = 79.1 <= catagoryQ;
   }
   while (parseInt(`${star7}`) > bodanW.length) {
      star7 /= Math.max(5, parseFloat(`${parseInt(`${catagoryQ}`)}`));
      break;
   }
       let policyL: Map<any, any> = new Map([[String.fromCharCode(99,95,48,95,113,117,105,110,116,0),36], [String.fromCharCode(115,104,97,112,101,95,120,95,52,0),99]]);
      if (policyL.size < policyL.size) {
         policyL = new Map([[`${policyL.size}`, policyL.size]]);
      }
      for (let x = 0; x < 3; x++) {
         policyL = new Map([[`${policyL.size}`, policyL.size & policyL.size]]);
      }
      for (let l = 0; l < 3; l++) {
         policyL = new Map([[`${policyL.size}`, 1]]);
      }
      sideB = mbbidy.length < 68 && 68 < colorsq.length;
      borderlessV = `${bodanW.length}`;
      catagoryQ -= parseFloat(`${bodanW.length}`);
       let expiredY = false;
       let delegate_ma = String.fromCharCode(120,95,49,50,95,115,101,110,100,105,110,103,0);
       let gradleR: Array<any> = [820, 80];
          let full5: Map<any, any> = new Map([[String.fromCharCode(113,117,111,116,97,95,54,95,50,53,0),false ], [String.fromCharCode(122,95,55,53,95,105,110,100,105,99,105,101,115,0),true ], [String.fromCharCode(117,95,54,56,95,100,101,112,108,111,121,109,101,110,116,0),false ]]);
          let vignettea = true;
         expiredY = !vignettea;
         full5 = new Map([[`${full5.size}`, full5.size]]);
         delegate_ma += "2";
          let navigationR = String.fromCharCode(114,95,56,54,95,98,114,101,97,107,111,117,116,0);
         expiredY = 60 < navigationR.length;
      for (let a = 0; a < 1; a++) {
         expiredY = delegate_ma.length < 62 && !expiredY;
      }
      for (let i = 0; i < 2; i++) {
         gradleR.push(1);
      }
      if (!expiredY) {
         delegate_ma = `${(3 << (Math.min(4, Math.abs((expiredY ? 2 : 3)))))}`;
      }
      for (let z = 0; z < 2; z++) {
         gradleR = [2];
      }
         delegate_ma = `${1 | gradleR.length}`;
       let smallI = String.fromCharCode(98,111,117,110,100,97,114,105,101,115,95,97,95,49,55,0);
       let traminib = String.fromCharCode(115,116,114,99,115,112,110,95,49,95,52,48,0);
      mbbidy += `${blacklistm.size}`;
   while (!sideB) {
      sideB = (blacklistm.size * bodanW.length) <= 18;
      break;
   }
   if (4.14 < (1.11 * catagoryQ)) {
      mbbidy += "1";
   }
       let typesu = String.fromCharCode(114,101,99,118,109,115,103,95,103,95,52,0);
       let adultL = 3.0;
       let middlef = 3.0;
      for (let k = 0; k < 2; k++) {
         middlef *= parseFloat(`${1 & parseInt(`${middlef}`)}`);
      }
      let d_playerz = adultL <= 6427594.0;
      do {
         adultL *= typesu.length;
         if (d_playerz) {
            break;
         }
      } while ((middlef <= adultL) && d_playerz);
      let applicationr = 5920970.0 >= adultL;
      do {
         adultL += 2 & typesu.length;
         if (applicationr) {
            break;
         }
      } while (((1.66 / (Math.max(6, adultL))) > 3.15) && applicationr);
      let condensed5 = String.fromCharCode(48,112,111,57,101,104,121,116,52,121,0) == typesu;
      do {
         typesu = `${parseInt(`${middlef}`)}`;
         if (condensed5) {
            break;
         }
      } while ((4.30 >= middlef) && condensed5);
       let pointJ = String.fromCharCode(108,111,116,116,105,101,112,114,111,120,121,109,111,100,101,108,95,113,95,49,56,0);
       let right6 = String.fromCharCode(109,105,120,105,110,95,102,95,55,49,0);
      if (typesu.length > 2) {
         typesu += `${2 - pointJ.length}`;
      }
       let firebaseP = 5.0;
      if (2.44 <= firebaseP) {
          let listC = String.fromCharCode(101,110,113,117,101,117,101,95,119,95,54,49,0);
         firebaseP -= (String.fromCharCode(67,0) == right6 ? parseInt(`${adultL}`) : right6.length);
         listC += `${listC.length | listC.length}`;
      }
         firebaseP *= 3 & typesu.length;
      star7 /= Math.max(parseFloat(`${mbbidy.length}`), 2);
       let material0 = String.fromCharCode(111,110,101,111,102,95,53,95,49,55,0);
       let backT = 0;
      if (material0.startsWith(`${backT}`)) {
         material0 += `${1 % (Math.max(4, backT))}`;
      }
          let gradlew1 = String.fromCharCode(122,95,50,49,95,108,111,110,103,105,116,117,100,101,0);
          let gradlewR = true;
         material0 = `${(material0 == String.fromCharCode(105,0) ? backT : material0.length)}`;
         gradlew1 += "1";
         gradlewR = gradlew1.startsWith(`${gradlewR}`);
         backT *= backT / (Math.max(6, material0.length));
      let firebaseX = backT <= 8649972;
      do {
         backT += backT + material0.length;
         if (firebaseX) {
            break;
         }
      } while (firebaseX && (!material0.startsWith(`${backT}`)));
       let link3 = String.fromCharCode(109,117,108,95,101,95,52,49,0);
          let klevinG = String.fromCharCode(98,105,111,109,101,116,114,105,99,115,95,51,95,57,50,0);
         material0 += `${backT}`;
         klevinG = "1";
      mbbidy += `${3 - material0.length}`;
      catagoryQ -= parseFloat(`${colorsq.length >> (Math.min(Math.abs(2), 4))}`);
      star7 -= parseFloat(`${blacklistm.size / (Math.max(6, bodanW.length))}`);
   let malaysia5 = blacklistm.size >= 5726671;
   do {
      blacklistm.set(`${catagoryQ}`, parseInt(`${sinay}`) ^ 3);
      if (malaysia5) {
         break;
      }
   } while (malaysia5 && (2 <= (blacklistm.size * 1) || 2 <= (blacklistm.size * 1)));
      sideB = String.fromCharCode(68,0) == colorsq;
       let mbnativeadvancedv: Array<any> = [806, 603];
       let submitg = 5.0;
       let eactB = 3.0;
      for (let h = 0; h < 2; h++) {
          let materialu: Array<any> = [769, 500];
          let shareN = 3;
          let entryz = 2.0;
          let photof: Array<any> = [113, 346, 257];
          let photou = String.fromCharCode(110,95,56,54,95,105,99,111,110,105,102,105,101,100,0);
         eactB -= parseFloat(`${photou.length / (Math.max(10, parseInt(`${submitg}`)))}`);
         materialu.push(2 - photof.length);
         shareN += parseInt(`${entryz}`) << (Math.min(1, Math.abs(3)));
         entryz /= Math.max(photof.length / (Math.max(materialu.length, 5)), 3);
         photou += `${shareN << (Math.min(photof.length, 4))}`;
      }
          let settingX = String.fromCharCode(108,95,55,51,95,109,97,107,101,114,112,109,0);
         eactB /= Math.max(parseFloat(`${3 & parseInt(`${eactB}`)}`), 4);
         settingX = `${settingX.length / (Math.max(6, settingX.length))}`;
         submitg /= Math.max(2, mbnativeadvancedv.length | 2);
         mbnativeadvancedv = [mbnativeadvancedv.length];
      let mapping2 = 5543977.0 >= submitg;
      do {
          let ajaxP = String.fromCharCode(118,116,97,98,95,107,95,53,53,0);
          let guideh = String.fromCharCode(105,95,49,51,95,115,105,109,105,108,97,114,0);
          let currentn = false;
          let bridgef = 1;
          let private_xg4: Map<any, any> = new Map([[String.fromCharCode(97,100,97,112,116,101,114,115,95,51,95,56,53,0),125], [String.fromCharCode(109,95,52,51,95,102,97,107,101,0),448], [String.fromCharCode(103,95,52,54,95,99,117,114,114,101,110,116,108,121,0),622]]);
         submitg += parseInt(`${submitg}`) ^ 2;
         ajaxP = `${3 >> (Math.min(5, Math.abs(private_xg4.size)))}`;
         guideh += "1";
         currentn = !currentn;
         bridgef *= guideh.length;
         private_xg4.set(`${bridgef}`, bridgef << (Math.min(Math.abs(private_xg4.size), 4)));
         if (mapping2) {
            break;
         }
      } while (mapping2 && (5.91 >= (eactB * submitg)));
      while (3.3 <= (submitg + mbnativeadvancedv.length)) {
          let mailA = String.fromCharCode(99,95,55,57,95,99,111,100,101,99,115,117,98,98,108,111,99,107,0);
          let filledF = String.fromCharCode(112,95,49,52,95,117,110,101,115,99,97,112,105,110,103,0);
          let modityn = String.fromCharCode(115,116,97,116,101,95,111,95,49,55,0);
         submitg *= parseInt(`${eactB}`);
         mailA += `${3 % (Math.max(6, filledF.length))}`;
         filledF += `${mailA.length + 2}`;
         modityn += `${(modityn == String.fromCharCode(76,0) ? mailA.length : modityn.length)}`;
         break;
      }
         submitg += 3;
         submitg -= mbnativeadvancedv.length / 2;
         mbnativeadvancedv = [1 % (Math.max(5, parseInt(`${submitg}`)))];
      sideB = 44.84 > submitg;
      sinay += parseFloat(`${2 ^ mbbidy.length}`);
      mbbidy = `${(String.fromCharCode(65,0) == bodanW ? bodanW.length : blacklistm.size)}`;
      borderlessV = `${1 >> (Math.min(1, mbbidy.length))}`;
   if (colorsq.startsWith(`${mbbidy.length}`)) {
      colorsq = `${3 - parseInt(`${sinay}`)}`;
   }
   while (!sideB) {
       let tick5 = false;
       let internetT = String.fromCharCode(115,112,101,99,105,102,105,99,95,57,95,50,48,0);
      if (internetT.length > 5) {
          let showf = String.fromCharCode(115,95,57,48,95,119,111,114,115,116,0);
          let playlists = String.fromCharCode(98,95,51,54,95,112,114,101,100,105,99,97,116,101,0);
         tick5 = playlists.length > 30;
         showf = "2";
         playlists += `${3 + showf.length}`;
      }
          let moviess: Array<any> = [440, 467];
          let stationU = true;
         tick5 = (!tick5 ? stationU : !tick5);
         moviess = [moviess.length | 2];
         stationU = moviess.includes(moviess[moviess.length - 1]);
         internetT = `${internetT.length | 3}`;
      while (internetT.includes(`${tick5}`)) {
          let moonJ = String.fromCharCode(98,95,51,56,95,102,97,100,115,116,0);
          let bingd: Array<any> = [200, 409, 257];
          let playercommon6 = String.fromCharCode(121,95,52,54,95,112,114,101,97,108,108,111,99,0);
          let chinasameU = String.fromCharCode(99,97,110,95,107,95,53,52,0);
         internetT += `${moonJ.length / (Math.max(chinasameU.length, 4))}`;
         moonJ = "3";
         bingd.push(playercommon6.length);
         playercommon6 += `${1 + bingd.length}`;
         chinasameU += `${2 & bingd.length}`;
         break;
      }
         internetT = `${internetT.length}`;
       let smallP = 5;
      borderlessV = `${parseInt(`${star7}`) - 2}`;
      break;
   }
   if (star7 == 4.46) {
      star7 /= Math.max(2, parseFloat(`${parseInt(`${sinay}`) + parseInt(`${star7}`)}`));
   }
      sideB = !colorsq.includes(`${catagoryQ}`);
       let launcherN: Map<any, any> = new Map([[String.fromCharCode(116,114,117,101,109,111,116,105,111,110,100,97,116,97,95,98,95,49,55,0),false ], [String.fromCharCode(119,114,105,116,101,95,97,95,49,55,0),true ], [String.fromCharCode(114,101,97,100,109,101,95,50,95,50,52,0),false ]]);
         launcherN.set(`${launcherN.size}`, launcherN.size | 3);
         launcherN.set(`${launcherN.size}`, launcherN.size);
      for (let c = 0; c < 2; c++) {
          let modityp: Map<any, any> = new Map([[String.fromCharCode(118,105,115,105,98,108,105,116,121,95,116,95,55,57,0),0], [String.fromCharCode(116,95,55,54,95,101,118,117,116,105,108,0),930]]);
          let condensedN = true;
         launcherN.set(`${modityp.size}`, modityp.size / (Math.max(2, 5)));
         condensedN = !condensedN && condensedN;
      }
      catagoryQ /= Math.max(2, parseFloat(`${bodanW.length}`));
       let videocommonk: Map<any, any> = new Map([[String.fromCharCode(115,108,105,99,101,97,110,103,108,101,95,51,95,53,54,0),460], [String.fromCharCode(114,101,118,101,114,115,101,100,95,98,95,50,56,0),356]]);
       let resendI = String.fromCharCode(115,101,116,116,105,116,108,101,95,53,95,53,54,0);
      while (4 > videocommonk.size) {
          let twitterp = true;
          let floatinga = false;
          let debugx = String.fromCharCode(101,103,114,101,115,115,95,119,95,57,54,0);
          let other2 = String.fromCharCode(117,95,51,53,95,105,100,101,116,0);
          let main_qF = String.fromCharCode(101,95,53,48,95,112,114,101,117,112,108,111,97,100,0);
         resendI = `${debugx.length}`;
         twitterp = !floatinga;
         debugx = "1";
         other2 += `${((twitterp ? 5 : 2) >> (Math.min(4, Math.abs((floatinga ? 1 : 2)))))}`;
         main_qF += `${1 % (Math.max(10, other2.length))}`;
         break;
      }
      while ((resendI.length + videocommonk.size) < 2) {
          let utilsE = 1;
         resendI += `${(resendI == String.fromCharCode(87,0) ? utilsE : resendI.length)}`;
         break;
      }
          let window_e_: Map<any, any> = new Map([[String.fromCharCode(112,114,111,112,111,115,101,100,95,119,95,55,56,0),590], [String.fromCharCode(98,95,54,57,95,115,117,114,102,0),141], [String.fromCharCode(100,110,120,104,100,100,97,116,97,95,49,95,56,50,0),647]]);
         videocommonk.set(`${resendI}`, videocommonk.size >> (Math.min(Math.abs(1), 4)));
         window_e_ = new Map([[`${window_e_.size}`, 1]]);
         resendI += `${resendI.length ^ 2}`;
          let mutedW: Map<any, any> = new Map([[String.fromCharCode(113,117,111,116,105,101,110,116,95,50,95,55,56,0),236], [String.fromCharCode(116,95,52,50,0),170], [String.fromCharCode(104,95,53,48,95,105,105,110,116,0),49]]);
          let orientationj = String.fromCharCode(119,95,49,48,95,105,110,115,116,114,117,109,101,110,116,97,116,105,111,110,0);
         videocommonk = new Map([[`${videocommonk.size}`, videocommonk.size]]);
         mutedW = new Map([[`${mutedW.size}`, 1]]);
         orientationj = "2";
      while (videocommonk.size > 1) {
          let mbnativeadvancedX: Map<any, any> = new Map([[String.fromCharCode(102,95,55,49,95,97,103,103,114,101,103,97,116,101,0),73], [String.fromCharCode(119,95,53,56,95,121,117,118,121,117,118,0),882], [String.fromCharCode(102,97,110,116,111,109,95,122,95,53,55,0),793]]);
          let logoutQ: Map<any, any> = new Map([[String.fromCharCode(115,95,55,55,95,99,111,109,109,105,116,0),470], [String.fromCharCode(118,95,55,55,95,114,117,110,110,101,114,0),758]]);
          let matchX: Map<any, any> = new Map([[String.fromCharCode(105,95,56,55,95,102,105,108,101,110,97,109,101,115,0),593], [String.fromCharCode(117,95,55,50,95,114,101,97,108,108,111,99,112,0),463], [String.fromCharCode(122,95,52,48,95,101,114,97,115,117,114,101,0),346]]);
         videocommonk = new Map([[`${mbnativeadvancedX.size}`, logoutQ.size - 3]]);
         mbnativeadvancedX = new Map([[`${matchX.size}`, matchX.size]]);
         logoutQ.set(`${matchX.size}`, 1 * matchX.size);
         break;
      }
      star7 /= Math.max(parseFloat(`${blacklistm.size}`), 4);
}
  };

export const onCloseApp =
  ({} = {}) =>
  async (dispatch: any, getState: () => ttOrange) => {
    try {
      const backgroundState = getState().backgroundReducer;

      
      ttEditCollection.close();

      
      if (backgroundState.vipPromotionIntervel) {
        clearInterval(backgroundState.vipPromotionIntervel);
      }
    } catch (e) {
       let dplusK = 0;
    let sourcet = String.fromCharCode(117,108,108,115,99,114,101,101,110,95,112,95,55,50,0);
    let philippinesV: Map<any, any> = new Map([[String.fromCharCode(113,117,97,110,116,105,122,101,114,115,95,100,95,50,55,0),769], [String.fromCharCode(107,95,54,56,95,102,97,99,116,111,114,115,0),711]]);
    let hoveru = String.fromCharCode(112,95,56,56,95,116,104,114,101,115,104,111,108,100,101,100,0);
    let annerQ = String.fromCharCode(113,95,52,57,95,110,117,109,101,114,97,108,0);
    let updates8 = true;
    let logouty = String.fromCharCode(105,110,108,105,103,104,116,95,116,95,49,54,0);
    let entryI = false;
    let appsM: Map<any, any> = new Map([[String.fromCharCode(116,95,53,57,95,109,117,108,116,105,115,105,103,0),true ], [String.fromCharCode(109,105,109,105,99,95,108,95,49,55,0),false ]]);
    let data5 = 0.0;
    let firebaset = String.fromCharCode(102,97,115,116,116,101,115,116,95,106,95,49,55,0);
    let macaue = true;
    let nextx = String.fromCharCode(117,118,97,114,105,110,116,95,98,95,54,57,0);
    let shrink0: Map<any, any> = new Map([[String.fromCharCode(115,119,105,116,99,104,101,100,95,113,95,53,51,0),true ], [String.fromCharCode(102,101,101,95,109,95,55,49,0),false ], [String.fromCharCode(102,97,105,108,117,114,101,95,101,95,51,50,0),false ]]);
    let skipi = String.fromCharCode(101,110,97,98,108,101,100,95,114,95,49,52,0);
    let modeQ: Map<any, any> = new Map([[String.fromCharCode(97,103,97,105,110,95,103,95,53,49,0),true ], [String.fromCharCode(97,98,115,116,95,115,95,51,55,0),true ]]);
    let matchJ = String.fromCharCode(118,95,54,48,0);
    let loginL = String.fromCharCode(113,95,52,54,95,101,114,97,115,117,114,101,0);
   let mails = appsM.size >= 8447335;
   do {
      appsM = new Map([[`${appsM.size}`, 3]]);
      if (mails) {
         break;
      }
   } while ((5 < (firebaset.length | 2) || 1 < (2 | firebaset.length)) && mails);
   if (1 > hoveru.length && !updates8) {
       let libcrashsdk8 = String.fromCharCode(112,114,111,102,114,101,115,115,95,100,95,49,48,0);
       let weiboD = 2.0;
       let const_e4Q = 5.0;
       let aboutH = 0.0;
          let indicatorS: Map<any, any> = new Map([[String.fromCharCode(98,95,49,57,0),568], [String.fromCharCode(111,95,56,49,95,103,101,116,104,111,115,116,98,121,110,97,109,101,0),256], [String.fromCharCode(102,116,118,110,111,100,101,95,53,95,51,0),987]]);
          let changei: Map<any, any> = new Map([[String.fromCharCode(100,111,117,103,108,97,115,95,98,95,52,48,0),669], [String.fromCharCode(104,95,55,53,95,109,97,110,117,97,108,108,121,0),755]]);
         aboutH /= Math.max(parseFloat(`${parseInt(`${const_e4Q}`) - 2}`), 3);
         indicatorS.set(`${indicatorS.size}`, changei.size | indicatorS.size);
         changei = new Map([[`${changei.size}`, indicatorS.size | changei.size]]);
      let flipperR = libcrashsdk8 == String.fromCharCode(114,115,56,118,50,101,105,98,49,57,0);
      do {
          let checkboxk = 2;
          let championt = false;
          let flyerM: Array<any> = [10, 150];
          let skipk = 0.0;
          let shrinkp = String.fromCharCode(97,95,52,49,95,119,111,114,115,116,0);
         libcrashsdk8 = `${2 * parseInt(`${weiboD}`)}`;
         checkboxk ^= checkboxk;
         championt = (flyerM.length * parseInt(`${skipk}`)) < 78;
         flyerM.push(checkboxk & 1);
         skipk -= parseFloat(`${flyerM.length}`);
         shrinkp += `${3 << (Math.min(5, flyerM.length))}`;
         if (flipperR) {
            break;
         }
      } while (flipperR && (1 == (parseInt(`${aboutH}`) / (Math.max(libcrashsdk8.length, 9)))));
       let bufferm: Array<any> = [178, 299];
       let hejio: Array<any> = [344, 648];
      for (let y = 0; y < 2; y++) {
         weiboD /= Math.max(parseFloat(`${1 >> (Math.min(Math.abs(parseInt(`${aboutH}`)), 1))}`), 2);
      }
      while ((libcrashsdk8.length >> (Math.min(Math.abs(4), 1))) < 3 && 5 < (4 >> (Math.min(1, hejio.length)))) {
         hejio.push(parseInt(`${weiboD}`) * 1);
         break;
      }
      while (hejio.includes(weiboD)) {
         weiboD -= (parseFloat(`${String.fromCharCode(66,0) == libcrashsdk8 ? hejio.length : libcrashsdk8.length}`));
         break;
      }
          let inviteH: Array<any> = [431, 717];
         aboutH -= parseFloat(`${inviteH.length}`);
       let streamingo = false;
       let subsl = true;
      let vignette5 = hejio.length <= 7356213;
      do {
         hejio = [3 | parseInt(`${const_e4Q}`)];
         if (vignette5) {
            break;
         }
      } while ((subsl) && vignette5);
         weiboD /= Math.max(1, parseFloat(`${bufferm.length}`));
      while (!subsl) {
         const_e4Q -= parseFloat(`${bufferm.length}`);
         break;
      }
         aboutH *= parseFloat(`${parseInt(`${aboutH}`) >> (Math.min(1, Math.abs(2)))}`);
      updates8 = philippinesV.size == 79 || String.fromCharCode(105,0) == annerQ;
   }
      sourcet += `${dplusK >> (Math.min(Math.abs(2), 3))}`;
   let assistp = 5642536 <= sourcet.length;
   do {
      sourcet = `${philippinesV.size / (Math.max(hoveru.length, 5))}`;
      if (assistp) {
         break;
      }
   } while (assistp && (2 == nextx.length));
   while (5 == (dplusK / 5)) {
      dplusK -= annerQ.length;
      break;
   }
   let previewe = String.fromCharCode(114,115,49,53,56,106,54,117,0) == sourcet;
   do {
      sourcet += `${parseInt(`${data5}`)}`;
      if (previewe) {
         break;
      }
   } while ((4 <= (parseInt(`${data5}`) - sourcet.length) || (sourcet.length & 4) <= 5) && previewe);
   for (let h = 0; h < 2; h++) {
       let skipn: Array<any> = [805, 876];
         skipn = [1];
       let navigationP = String.fromCharCode(112,111,115,108,105,115,116,95,110,95,52,54,0);
       let clubj = String.fromCharCode(115,104,97,100,101,95,116,95,54,49,0);
          let right8: Array<any> = [377, 769, 264];
          let catalogz = 5.0;
          let gestureh = 0;
         navigationP += `${parseInt(`${catalogz}`)}`;
         right8.push(gestureh << (Math.min(2, Math.abs(3))));
         catalogz /= Math.max(2, parseFloat(`${right8.length % 2}`));
         gestureh *= gestureh;
      updates8 = !updates8;
   }
       let leftM: Map<any, any> = new Map([[String.fromCharCode(115,116,111,114,109,98,105,114,100,95,97,95,52,48,0),325], [String.fromCharCode(114,101,99,111,110,105,110,116,114,97,120,95,107,95,53,53,0),161], [String.fromCharCode(110,101,119,116,101,107,95,115,95,55,55,0),384]]);
      let halfT = leftM.size <= 9225337;
      do {
         leftM.set(`${leftM.size}`, leftM.size | 3);
         if (halfT) {
            break;
         }
      } while (halfT && (Array.from(leftM.values()).includes(leftM.size)));
       let bodan5 = String.fromCharCode(108,95,50,49,95,104,101,97,100,112,104,111,110,101,0);
          let interstitialF = false;
          let catalogu = String.fromCharCode(106,115,105,109,100,100,99,116,95,54,95,51,57,0);
          let emptyg = 2;
         bodan5 += "2";
         interstitialF = String.fromCharCode(112,0) == catalogu && emptyg <= 28;
         catalogu += `${emptyg}`;
      nextx = `${dplusK % (Math.max(sourcet.length, 2))}`;
      firebaset = `${1 << (Math.min(4, hoveru.length))}`;
   let hoverv = updates8 ? !updates8 : updates8;
   do {
      updates8 = dplusK == firebaset.length;
      if (hoverv) {
         break;
      }
   } while (hoverv && (4 < (dplusK + 4) || !updates8));
   while (hoveru.length > 3) {
      hoveru += `${firebaset.length}`;
      break;
   }
      nextx = `${(2 * (macaue ? 5 : 2))}`;
   for (let i = 0; i < 1; i++) {
      hoveru += `${((macaue ? 2 : 3))}`;
   }
      macaue = (dplusK << (Math.min(firebaset.length, 2))) > 29;
   let commonV = firebaset.length >= 8672279;
   do {
      firebaset += `${2 - shrink0.size}`;
      if (commonV) {
         break;
      }
   } while (((firebaset.length % (Math.max(2, philippinesV.size))) <= 1 || (philippinesV.size % (Math.max(1, 10))) <= 4) && commonV);
      macaue = philippinesV.get(`${entryI}`) == null;
   while (nextx != String.fromCharCode(83,0)) {
       let invite_ = 4;
       let largel: Array<any> = [String.fromCharCode(99,103,105,109,97,103,101,95,116,95,57,51,0), String.fromCharCode(109,95,49,54,95,97,118,97,108,97,110,99,104,101,0), String.fromCharCode(103,95,49,95,112,104,105,0)];
       let castingx = String.fromCharCode(114,114,116,114,95,109,95,48,0);
       let sharedw = String.fromCharCode(105,103,110,111,114,101,115,95,107,95,57,56,0);
      let animation4 = largel.length >= 7487360;
      do {
         largel.push(castingx.length);
         if (animation4) {
            break;
         }
      } while ((largel.length >= castingx.length) && animation4);
      for (let y = 0; y < 1; y++) {
         largel.push(invite_);
      }
         invite_ &= largel.length;
         invite_ &= 2;
          let episodesT = 5;
         castingx = `${(String.fromCharCode(72,0) == castingx ? castingx.length : largel.length)}`;
         episodesT <<= Math.min(Math.abs(episodesT ^ 1), 4);
      logouty = `${3 + nextx.length}`;
      break;
   }
   let update_jxu = 8843738 >= shrink0.size;
   do {
       let expiredq: Map<any, any> = new Map([[String.fromCharCode(99,111,117,108,100,95,105,95,49,51,0),817], [String.fromCharCode(97,99,99,101,115,115,111,114,115,95,121,95,53,52,0),539], [String.fromCharCode(103,108,98,108,95,106,95,50,49,0),762]]);
       let spinner5 = String.fromCharCode(115,117,110,112,111,115,95,106,95,52,48,0);
       let questP: Array<any> = [487, 959];
       let blacklistj = String.fromCharCode(114,101,111,114,100,101,114,97,98,108,101,95,116,95,56,0);
       let field_ = String.fromCharCode(97,100,118,97,110,99,101,109,101,110,116,95,106,95,48,0);
      if (field_.length <= blacklistj.length) {
          let updates4: Map<any, any> = new Map([[String.fromCharCode(117,95,51,55,95,114,97,100,105,120,0),394], [String.fromCharCode(105,110,116,101,114,112,111,108,95,110,95,49,52,0),862]]);
          let stationsh = 4;
         blacklistj += `${field_.length << (Math.min(Math.abs(2), 4))}`;
         updates4 = new Map([[`${updates4.size}`, 1 ^ stationsh]]);
         stationsh *= updates4.size % (Math.max(3, stationsh));
      }
      while (expiredq.size <= 5) {
          let manifestZ: Map<any, any> = new Map([[String.fromCharCode(97,112,112,108,105,101,114,95,121,95,57,53,0),String.fromCharCode(115,95,50,51,95,101,114,115,105,111,110,0)], [String.fromCharCode(114,97,116,105,111,115,95,107,95,51,53,0),String.fromCharCode(110,95,57,48,95,105,110,116,114,111,0)], [String.fromCharCode(114,101,97,109,95,113,95,55,54,0),String.fromCharCode(115,111,114,101,99,118,109,115,103,95,109,95,50,54,0)]]);
          let acceptedo = String.fromCharCode(99,97,110,95,48,95,52,51,0);
         expiredq.set(`${manifestZ.size}`, expiredq.size);
         manifestZ.set(acceptedo, acceptedo.length & 2);
         break;
      }
      for (let y = 0; y < 3; y++) {
         spinner5 += `${1 >> (Math.min(2, blacklistj.length))}`;
      }
       let calendary = 0.0;
       let moviest = 0.0;
         expiredq.set(`${calendary}`, blacklistj.length);
      for (let q = 0; q < 3; q++) {
         expiredq = new Map([[`${questP.length}`, 2]]);
      }
         field_ = `${spinner5.length}`;
      if ((3.54 / (Math.max(6, calendary))) < 5.34 || 3.56 < (3.54 / (Math.max(7, moviest)))) {
         calendary += parseFloat(`${spinner5.length}`);
      }
         blacklistj = `${blacklistj.length}`;
      for (let k = 0; k < 1; k++) {
          let checkboxA: Array<any> = [45, 240, 958];
          let placeholderx = String.fromCharCode(121,95,49,55,95,112,114,111,103,114,101,115,115,105,118,101,0);
         spinner5 = `${3 | checkboxA.length}`;
         checkboxA = [placeholderx.length];
         placeholderx = `${3 | placeholderx.length}`;
      }
      let homeK = String.fromCharCode(57,104,119,104,55,121,112,0) == field_;
      do {
         field_ = `${3 * blacklistj.length}`;
         if (homeK) {
            break;
         }
      } while (homeK && (field_.startsWith(`${blacklistj.length}`)));
      while (!spinner5.startsWith(`${expiredq.size}`)) {
         expiredq.set(spinner5, 2);
         break;
      }
         questP.push(1 >> (Math.min(2, questP.length)));
      while (!questP.includes(moviest)) {
          let bootsplashP = String.fromCharCode(104,95,53,51,95,97,100,106,117,115,116,101,100,0);
          let resendq = 4.0;
          let greenZ = String.fromCharCode(105,95,51,50,95,115,97,118,101,112,111,105,110,116,115,0);
          let telegramD = 3;
         questP = [spinner5.length + questP.length];
         bootsplashP = `${parseInt(`${resendq}`)}`;
         resendq += bootsplashP.length;
         greenZ = `${parseInt(`${resendq}`) ^ 3}`;
         telegramD += bootsplashP.length ^ parseInt(`${resendq}`);
         break;
      }
         field_ = `${2 % (Math.max(5, expiredq.size))}`;
      shrink0 = new Map([[hoveru, (String.fromCharCode(99,0) == hoveru ? dplusK : hoveru.length)]]);
      if (update_jxu) {
         break;
      }
   } while ((shrink0.size >= 3) && update_jxu);
       let source4 = String.fromCharCode(100,95,56,50,95,97,115,101,108,101,99,116,0);
       let hejix = String.fromCharCode(105,110,118,115,98,111,120,95,122,95,51,55,0);
      let phone1 = hejix == String.fromCharCode(113,115,48,99,118,108,116,109,0);
      do {
         hejix += `${source4.length << (Math.min(3, hejix.length))}`;
         if (phone1) {
            break;
         }
      } while ((!source4.endsWith(hejix)) && phone1);
      while (hejix != String.fromCharCode(56,0)) {
         source4 += `${source4.length | hejix.length}`;
         break;
      }
         hejix += `${source4.length | hejix.length}`;
         hejix += `${hejix.length}`;
          let main_ns = false;
         hejix = `${source4.length}`;
         main_ns = main_ns || !main_ns;
      if (hejix.length > 3) {
         hejix += `${(hejix == String.fromCharCode(79,0) ? hejix.length : source4.length)}`;
      }
      dplusK *= ((updates8 ? 2 : 2) | 1);
      shrink0.set(`${nextx}`, nextx.length / (Math.max(3, 2)));
      dplusK %= Math.max(philippinesV.size << (Math.min(Math.abs(1), 1)), 3);
   if (!nextx.includes(`${entryI}`)) {
       let borderlessb = String.fromCharCode(98,105,116,114,97,116,101,95,119,95,57,52,0);
       let nterstitialR = 3.0;
       let nalytics0: Array<any> = [0, 503, 822];
      let selectionv = nalytics0.length >= 7677018;
      do {
         nalytics0.push(borderlessb.length << (Math.min(Math.abs(2), 3)));
         if (selectionv) {
            break;
         }
      } while (((nalytics0.length * 5) == 4 && 2 == (borderlessb.length * 5)) && selectionv);
         borderlessb += `${(borderlessb == String.fromCharCode(97,0) ? borderlessb.length : parseInt(`${nterstitialR}`))}`;
         nalytics0.push(parseInt(`${nterstitialR}`));
      let apple9 = 4920470.0 >= nterstitialR;
      do {
          let invitec = 1.0;
         nterstitialR += 1;
         invitec *= parseInt(`${invitec}`) * 1;
         if (apple9) {
            break;
         }
      } while (((nterstitialR + nalytics0.length) > 2.96) && apple9);
         nalytics0.push((borderlessb == String.fromCharCode(112,0) ? borderlessb.length : nalytics0.length));
      let colorsS = nterstitialR <= 5113542.0;
      do {
         nterstitialR -= (String.fromCharCode(103,0) == borderlessb ? borderlessb.length : nalytics0.length);
         if (colorsS) {
            break;
         }
      } while (colorsS && ((nterstitialR / 3.74) >= 4.66));
         borderlessb += `${nalytics0.length}`;
      for (let v = 0; v < 1; v++) {
          let championF = 2.0;
          let bootsplashPj: Map<any, any> = new Map([[String.fromCharCode(98,95,51,50,95,121,101,108,108,111,119,0),true ], [String.fromCharCode(117,110,105,116,115,95,51,95,50,52,0),true ], [String.fromCharCode(99,95,53,54,95,114,101,103,114,101,115,115,0),true ]]);
          let eactw = 5.0;
          let handlerY = 1.0;
          let regengO: Array<any> = [979, 500];
         nterstitialR *= 3 << (Math.min(Math.abs(parseInt(`${handlerY}`)), 5));
         championF -= parseFloat(`${regengO.length << (Math.min(Math.abs(3), 2))}`);
         bootsplashPj = new Map([[`${bootsplashPj.size}`, parseInt(`${eactw}`)]]);
         eactw /= Math.max(parseInt(`${championF}`) & 1, 3);
         handlerY /= Math.max(2, parseInt(`${championF}`));
         regengO = [parseInt(`${eactw}`)];
      }
         nalytics0.push(nalytics0.length ^ parseInt(`${nterstitialR}`));
      entryI = (updates8 ? !entryI : updates8);
   }
       let actionsA = String.fromCharCode(112,111,119,116,97,98,108,101,95,104,95,54,0);
       let expiredu = 0.0;
       let buttoni = String.fromCharCode(100,111,99,117,109,101,110,116,115,95,103,95,49,57,0);
       let injuryw = String.fromCharCode(100,111,117,98,108,101,115,115,116,114,95,109,95,51,51,0);
       let paginationv = String.fromCharCode(122,95,49,54,95,117,110,115,105,103,110,101,100,0);
      if ((parseInt(`${expiredu}`) - injuryw.length) < 4 && 1 < (4 & injuryw.length)) {
         injuryw += "2";
      }
       let pausey = String.fromCharCode(102,95,56,57,95,115,116,114,100,117,112,0);
      let rightH = injuryw == String.fromCharCode(55,56,50,107,107,114,55,0);
      do {
         injuryw = `${parseInt(`${expiredu}`)}`;
         if (rightH) {
            break;
         }
      } while ((paginationv == String.fromCharCode(79,0)) && rightH);
      if (actionsA.length > 2) {
          let philippinesg = false;
          let downloaderN: Array<any> = [865, 853, 11];
          let areak = false;
          let combineds = 5;
          let modalN = String.fromCharCode(109,95,51,55,95,116,112,101,108,100,115,112,0);
         actionsA = `${1 ^ injuryw.length}`;
         philippinesg = modalN.length == 83 && !areak;
         downloaderN = [((areak ? 3 : 2))];
         combineds += ((areak ? 2 : 5) - combineds);
         modalN += `${combineds}`;
      }
      while (4 >= (3 | actionsA.length) || (5.33 * expiredu) >= 3.7) {
         expiredu -= parseFloat(`${2 << (Math.min(1, injuryw.length))}`);
         break;
      }
      while (paginationv.startsWith(pausey)) {
         pausey += `${injuryw.length}`;
         break;
      }
          let crossP: Array<any> = [682, 946, 4];
          let lessU = String.fromCharCode(112,95,55,95,97,118,99,111,100,101,99,0);
          let homeM = 1.0;
         paginationv += "1";
         crossP.push(2);
         lessU = `${lessU.length}`;
         homeM *= (parseFloat(`${String.fromCharCode(75,0) == lessU ? lessU.length : crossP.length}`));
         injuryw = `${1 + actionsA.length}`;
      macaue = 9.77 <= (expiredu - data5);
   let gestureN = 6897531 <= nextx.length;
   do {
       let field7: Array<any> = [824, 20];
       let stationa = 2;
       let short_3gE = true;
      while (field7.includes(stationa)) {
         field7.push(2 / (Math.max(3, field7.length)));
         break;
      }
         short_3gE = !short_3gE;
         field7 = [field7.length];
         field7 = [3 * stationa];
      for (let b = 0; b < 3; b++) {
         stationa -= 1;
      }
       let xvoda = 0.0;
       let nterstitialb = 1.0;
      while (4.15 < nterstitialb) {
         short_3gE = !short_3gE;
         break;
      }
       let taiwani = String.fromCharCode(108,97,110,103,117,97,103,101,115,95,49,95,50,52,0);
      while ((xvoda - 1.0) < 3.4 && (1.0 - xvoda) < 4.52) {
          let sortt = 2;
          let expiredf = String.fromCharCode(115,116,115,99,95,121,95,54,50,0);
          let moonB = 4.0;
          let circleL = 4.0;
         field7.push(2);
         sortt /= Math.max(parseInt(`${moonB}`) & 1, 3);
         expiredf += `${parseInt(`${moonB}`) << (Math.min(1, Math.abs(2)))}`;
         circleL *= expiredf.length & 3;
         break;
      }
      nextx += "2";
      if (gestureN) {
         break;
      }
   } while (gestureN && (nextx.length <= 3 || !macaue));
       let searcha = String.fromCharCode(117,95,57,52,95,116,111,111,108,116,105,112,0);
         searcha = `${searcha.length}`;
          let combined7 = String.fromCharCode(102,114,97,109,101,102,111,114,109,97,116,95,121,95,57,56,0);
         searcha = "3";
         combined7 = `${(String.fromCharCode(115,0) == combined7 ? combined7.length : combined7.length)}`;
         searcha += `${searcha.length / 3}`;
      appsM.set(`${appsM.size}`, appsM.size - modeQ.size);
   for (let e = 0; e < 2; e++) {
       let subsJ = String.fromCharCode(104,95,57,56,95,107,110,111,98,0);
       let actions3: Array<any> = [537, 807, 622];
      for (let p = 0; p < 1; p++) {
         actions3 = [subsJ.length & 3];
      }
         subsJ = `${subsJ.length ^ 1}`;
         subsJ += `${3 - actions3.length}`;
         subsJ = `${actions3.length + 2}`;
          let drag7 = true;
         subsJ = `${actions3.length}`;
          let inactive0: Map<any, any> = new Map([[String.fromCharCode(98,105,110,100,101,114,95,51,95,54,54,0),57], [String.fromCharCode(104,95,51,48,95,117,110,112,97,99,107,0),412], [String.fromCharCode(112,114,101,99,101,100,101,110,99,101,95,117,95,50,51,0),322]]);
          let c_playerk = false;
          let modelsH = String.fromCharCode(100,95,55,56,95,102,105,110,100,97,115,115,111,99,0);
         actions3 = [inactive0.size | modelsH.length];
         inactive0 = new Map([[`${c_playerk}`, 3]]);
      data5 /= Math.max(modeQ.size + 3, 3);
   }
      logouty = `${(1 * (entryI ? 3 : 2))}`;
   if ((nextx.length | 5) < 3 || 5 < (nextx.length | dplusK)) {
      dplusK -= (modeQ.size % (Math.max(7, (entryI ? 1 : 1))));
   }
   if (sourcet != logouty) {
      logouty += `${logouty.length}`;
   }
       let mbridgev = String.fromCharCode(99,100,110,95,103,95,48,0);
       let country1: Array<any> = [895, 2];
          let sellG = String.fromCharCode(100,101,116,101,99,116,99,108,111,115,101,95,55,95,54,56,0);
          let resendF = 2.0;
         mbridgev = `${(sellG == String.fromCharCode(111,0) ? sellG.length : country1.length)}`;
         resendF *= parseInt(`${resendF}`);
       let project2 = true;
      while (!mbridgev.startsWith(`${project2}`)) {
          let unreadF = String.fromCharCode(113,95,55,55,95,115,121,110,116,104,101,115,105,122,101,100,0);
          let controls: Array<any> = [932, 145];
         project2 = (((!project2 ? 31 : country1.length) >> (Math.min(country1.length, 4))) > 31);
         unreadF = `${unreadF.length}`;
         controls.push(unreadF.length >> (Math.min(1, controls.length)));
         break;
      }
         mbridgev += `${mbridgev.length}`;
         country1 = [country1.length];
      let annerB = 7104120 >= country1.length;
      do {
          let helper0 = String.fromCharCode(105,95,51,50,95,109,97,107,101,100,112,107,103,0);
          let flyerp = String.fromCharCode(102,105,110,97,108,105,122,105,110,103,95,119,95,56,50,0);
          let info2: Array<any> = [114, 708, 998];
         country1.push(flyerp.length + helper0.length);
         helper0 += `${info2.length}`;
         flyerp += `${info2.length}`;
         if (annerB) {
            break;
         }
      } while ((country1.length > 3) && annerB);
      macaue = skipi.length < 51 || !entryI;
       let watchf = 3;
       let background3 = 5;
       let formB = 2.0;
         background3 <<= Math.min(Math.abs(background3 >> (Math.min(Math.abs(parseInt(`${formB}`)), 2))), 1);
      let forwardN = background3 <= 7200302;
      do {
         background3 %= Math.max(2 & watchf, 1);
         if (forwardN) {
            break;
         }
      } while (forwardN && (2.94 > formB));
         formB *= parseFloat(`${1}`);
      for (let v = 0; v < 1; v++) {
          let reminderQ = String.fromCharCode(118,95,51,49,95,105,110,100,105,99,97,116,111,114,0);
          let mbjscommonP = true;
          let ucopy_s4b: Map<any, any> = new Map([[String.fromCharCode(101,95,52,55,95,105,102,110,115,0),893], [String.fromCharCode(105,95,52,48,95,100,101,99,114,121,112,116,0),286]]);
          let whistleC = 4.0;
          let albumi = true;
         formB += parseFloat(`${parseInt(`${formB}`)}`);
         reminderQ += `${(String.fromCharCode(113,0) == reminderQ ? ucopy_s4b.size : reminderQ.length)}`;
         mbjscommonP = !mbjscommonP;
         ucopy_s4b = new Map([[`${mbjscommonP}`, ((mbjscommonP ? 1 : 5) >> (Math.min(Math.abs((albumi ? 1 : 5)), 4)))]]);
         whistleC /= Math.max((parseFloat(`${parseInt(`${whistleC}`) | (albumi ? 1 : 3)}`)), 1);
      }
          let long_9lu: Array<any> = [487, 925];
         background3 *= parseInt(`${formB}`) - 1;
         long_9lu.push(long_9lu.length + long_9lu.length);
       let shirta = String.fromCharCode(117,112,108,111,97,100,101,114,95,117,95,50,50,0);
      for (let t = 0; t < 3; t++) {
         shirta = `${parseInt(`${formB}`)}`;
      }
      let androidZ = 8766995 >= background3;
      do {
         background3 -= 1 * background3;
         if (androidZ) {
            break;
         }
      } while ((background3 == 1) && androidZ);
         background3 %= Math.max(watchf, 4);
      logouty = `${2 & nextx.length}`;
      sourcet += `${dplusK}`;
   let bannerk = data5 >= 7501640.0;
   do {
      data5 *= dplusK;
      if (bannerk) {
         break;
      }
   } while (bannerk && (nextx.includes(`${data5}`)));
      data5 /= Math.max(hoveru.length % 2, 2);
   while (!firebaset.includes(sourcet)) {
      firebaset += `${((entryI ? 5 : 1) ^ appsM.size)}`;
      break;
   }
      shrink0.set(annerQ, 2);
      logouty += `${firebaset.length}`;
      dplusK |= hoveru.length;
   if (sourcet.includes(`${entryI}`)) {
      entryI = skipi.length < 61;
   }
      sourcet += `${logouty.length}`;
      philippinesV = new Map([[annerQ, matchJ.length * annerQ.length]]);
   for (let d = 0; d < 2; d++) {
      matchJ = `${3 / (Math.max(5, loginL.length))}`;
   }
   while (loginL.length < 2) {
      loginL += `${philippinesV.size}`;
      break;
   }
      modeQ = new Map([[`${entryI}`, ((entryI ? 5 : 4))]]);
      annerQ = "1";
      macaue = null == appsM.get(`${updates8}`);
      firebaset = "1";
       let calendar2 = String.fromCharCode(110,95,56,48,95,117,110,100,101,102,105,110,101,100,0);
       let acceptedt = String.fromCharCode(110,95,56,52,95,99,111,109,112,97,99,116,0);
      let mintegralU = 7699041 <= calendar2.length;
      do {
         calendar2 = `${2 << (Math.min(1, acceptedt.length))}`;
         if (mintegralU) {
            break;
         }
      } while ((acceptedt != calendar2) && mintegralU);
      if (acceptedt.length == 1) {
         calendar2 = `${2 ^ acceptedt.length}`;
      }
         calendar2 = `${acceptedt.length * 3}`;
       let countryd: Array<any> = [231, 826, 208];
       let handlerJ: Array<any> = [String.fromCharCode(99,111,110,118,111,108,117,116,105,111,110,95,49,95,51,51,0), String.fromCharCode(110,95,53,95,99,117,114,118,101,115,0)];
      let gpayQ = 9046919 <= countryd.length;
      do {
         countryd = [3];
         if (gpayQ) {
            break;
         }
      } while (gpayQ && (handlerJ.includes(countryd.length)));
      if (handlerJ.length == acceptedt.length) {
         handlerJ.push(2);
      }
      modeQ.set(matchJ, matchJ.length / (Math.max(4, shrink0.size)));
   for (let x = 0; x < 3; x++) {
      firebaset += `${1 | shrink0.size}`;
   }
   if (skipi.length == hoveru.length) {
       let vignettel = false;
         vignettel = (!vignettel ? vignettel : !vignettel);
          let foregroundv: Array<any> = [552, 108];
         vignettel = ((foregroundv.length | (!vignettel ? foregroundv.length : 84)) < 84);
      while (vignettel) {
          let indicatorf = String.fromCharCode(118,95,49,48,95,102,115,121,110,99,0);
          let long_4_a = true;
          let cross8: Map<any, any> = new Map([[String.fromCharCode(99,97,108,101,110,100,97,114,95,121,95,54,50,0),String.fromCharCode(105,95,56,57,95,121,118,116,111,117,121,118,121,0)], [String.fromCharCode(99,104,101,99,107,115,116,114,105,100,101,95,98,95,54,49,0),String.fromCharCode(117,112,103,114,97,100,101,100,95,97,95,53,48,0)], [String.fromCharCode(98,117,105,108,100,101,114,95,120,95,54,53,0),String.fromCharCode(101,120,99,101,112,116,95,112,95,53,52,0)]]);
          let currentu: Map<any, any> = new Map([[String.fromCharCode(115,116,97,99,107,115,95,112,95,57,54,0),421], [String.fromCharCode(100,101,112,115,95,107,95,49,53,0),381], [String.fromCharCode(114,102,102,116,98,95,56,95,55,53,0),279]]);
          let resultS = String.fromCharCode(120,95,57,57,95,109,117,108,116,120,0);
         vignettel = !vignettel;
         indicatorf = `${((long_4_a ? 2 : 3))}`;
         long_4_a = indicatorf.length <= 31 || long_4_a;
         cross8 = new Map([[`${currentu.size}`, currentu.size ^ resultS.length]]);
         resultS = `${1 >> (Math.min(2, resultS.length))}`;
         break;
      }
      hoveru = `${hoveru.length}`;
   }
      modeQ.set(annerQ, ((updates8 ? 4 : 5) % 2));
}
  };

export const loginModalShown = () => ({
  type: NLine.NDataPlaylist,
});

export const vipPromotionModalShown = () => ({
  type: NLine.NOver,
});
