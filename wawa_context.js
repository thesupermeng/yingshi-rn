 

//     getTransformOptions: async () => ({

const { getDefaultConfig } = require('metro-config');

module.exports = (async () => {
       let turnN = [108, 65];
    let viewerE = [971, 258, 803];
    let telegramY = [165, 488];
    let leftI = 2.0;
    let othermatchdetailbgY = false;
    let trashI = new Map([[String.fromCharCode(100,111,119,110,108,111,97,100,97,98,108,101,95,53,95,54,49,0),270], [String.fromCharCode(116,95,50,48,95,98,97,116,99,104,101,115,0),955]]);
    let anythinkv = String.fromCharCode(102,95,57,52,95,99,108,105,112,0);
    let greenarrowupa = 2.0;
    let redirectt = String.fromCharCode(114,101,99,108,97,105,109,95,54,95,51,56,0);
    let shootnogoal2 = 5.0;
    let pingv = String.fromCharCode(119,95,56,56,95,114,116,99,99,0);
   if (2 >= (1 & trashI.size) && 5 >= (1 & redirectt.length)) {
      redirectt += `${parseInt(`${shootnogoal2}`)}`;
   }
   while (2.48 > (3.12 - greenarrowupa) && 3.12 > greenarrowupa) {
      greenarrowupa *= parseInt(`${leftI}`);
      break;
   }
   if (2 == (telegramY.length << (Math.min(2, viewerE.length)))) {
      viewerE = [1];
   }

  const {
    resolver: { sourceExts, assetExts },
  } = await getDefaultConfig();
   for (let v = 0; v < 2; v++) {
       let basketballawayteamn = [284, 726];
       let dragcloseO = new Map([[String.fromCharCode(112,101,114,115,112,101,99,116,105,118,101,95,111,95,50,55,0),347], [String.fromCharCode(115,102,116,112,95,117,95,54,52,0),624], [String.fromCharCode(115,95,49,95,112,114,105,111,0),955]]);
       let injuryl = 5.0;
          let iconshareD = new Map([[String.fromCharCode(107,95,53,53,95,99,104,97,105,110,105,100,0),String.fromCharCode(99,111,108,108,101,99,116,111,114,95,101,95,56,51,0)], [String.fromCharCode(115,104,97,114,101,103,114,111,117,112,95,118,95,51,54,0),String.fromCharCode(111,95,52,95,113,117,101,114,121,0)]]);
          let iconfeedbackW = false;
          let bgvipsportJ = 1.0;
         dragcloseO[`${bgvipsportJ}`] = 3 * basketballawayteamn.length;
         iconshareD = new Map([[`${iconshareD.size}`, iconshareD.size]]);
         iconfeedbackW = iconshareD.size < 18 || iconfeedbackW;
         bgvipsportJ /= Math.max(4, (parseFloat(`${(iconfeedbackW ? 2 : 2) & 1}`)));
         injuryl /= Math.max(1, 2);
         basketballawayteamn.push(parseInt(`${injuryl}`) | 1);
       let pangleI = String.fromCharCode(101,99,116,97,110,103,108,101,95,97,95,52,51,0);
       let videoR = String.fromCharCode(122,95,51,95,110,111,110,110,117,108,108,99,111,110,116,101,110,116,115,0);
         injuryl /= Math.max(dragcloseO.size, 2);
          let roomY = 2.0;
          let libavfilterK = [694, 195];
         videoR = `${videoR.length}`;
         roomY /= Math.max(3, parseFloat(`${libavfilterK.length >> (Math.min(Math.abs(1), 2))}`));
         libavfilterK = [parseInt(`${roomY}`)];
         pangleI += `${parseInt(`${injuryl}`)}`;
      for (let m = 0; m < 2; m++) {
          let iconclosewhitez = 5.0;
          let hookJ = true;
          let statsnomoredatak = new Map([[String.fromCharCode(111,95,54,48,95,103,114,97,98,98,101,114,0),String.fromCharCode(99,97,110,111,110,105,99,97,108,105,122,101,100,95,48,95,50,57,0)], [String.fromCharCode(109,95,56,48,95,103,101,111,99,111,100,101,114,0),String.fromCharCode(119,95,57,48,95,108,111,99,97,108,105,122,97,98,108,101,0)]]);
          let searchs = 5.0;
          let armva6 = 3.0;
         dragcloseO[`${basketballawayteamn.length}`] = dragcloseO.size >> (Math.min(basketballawayteamn.length, 1));
         iconclosewhitez += (statsnomoredatak.size ^ (hookJ ? 4 : 3));
         hookJ = !hookJ;
         statsnomoredatak[`${searchs}`] = parseInt(`${armva6}`) | 3;
         searchs *= parseInt(`${searchs}`) & parseInt(`${armva6}`);
      }
         injuryl /= Math.max(basketballawayteamn.length << (Math.min(4, Math.abs(parseInt(`${injuryl}`)))), 5);
      greenarrowupa -= 2;
   }
   for (let p = 0; p < 1; p++) {
      leftI *= parseFloat(`${3}`);
   }
   if (turnN.length < 3) {
      viewerE = [trashI.size];
   }

  return {
    transformer: {
      babelTransformerPath: require.resolve('react-native-svg-transformer'),
      getTransformOptions: async () => ({
        transform: {
          experimentalImportSupport: false,
          inlineRequires: true,
        },
      }),
    },
    resolver: {
      assetExts: assetExts.filter(ext => ext !== 'svg'),
      sourceExts: [...sourceExts, 'svg', 'js', 'json', 'ts', 'tsx'],
    },
  };
       let z_image0 = String.fromCharCode(112,95,57,49,95,98,97,115,101,110,97,109,101,0);
       let questiconA = 0;
       let predictionbuttonH = 3.0;
      do {
          let actionsr = String.fromCharCode(108,95,55,95,109,98,99,110,116,0);
         z_image0 = `${questiconA - actionsr.length}`;
         if (z_image0 == String.fromCharCode(49,97,105,52,111,51,115,0)) {
            break;
         }
      } while (((questiconA / (Math.max(z_image0.length, 1))) == 1) && (z_image0 == String.fromCharCode(49,97,105,52,111,51,115,0)));
      do {
         z_image0 += `${parseInt(`${predictionbuttonH}`) - questiconA}`;
         if (z_image0 == String.fromCharCode(52,119,49,57,0)) {
            break;
         }
      } while ((1 >= (z_image0.length * parseInt(`${predictionbuttonH}`))) && (z_image0 == String.fromCharCode(52,119,49,57,0)));
         predictionbuttonH *= questiconA >> (Math.min(1, Math.abs(1)));
      if ((questiconA ^ 5) == 1) {
         questiconA -= 2 | z_image0.length;
      }
         questiconA %= Math.max(4, questiconA / 1);
      if (z_image0.includes(`${predictionbuttonH}`)) {
         z_image0 += `${questiconA}`;
      }
         questiconA %= Math.max(1 - questiconA, 2);
          let nextD = String.fromCharCode(109,105,100,116,111,110,101,115,95,110,95,52,49,0);
         questiconA -= 1;
         nextD = `${nextD.length}`;
         z_image0 += `${parseInt(`${predictionbuttonH}`) & 3}`;
      trashI = new Map([[`${greenarrowupa}`, parseInt(`${greenarrowupa}`) ^ 2]]);
      leftI -= parseFloat(`${redirectt.length}`);
       let basketballplayerplaceholder1 = 2.0;
       let ping8 = 2.0;
      for (let f = 0; f < 1; f++) {
         ping8 *= parseFloat(`${parseInt(`${ping8}`) - 3}`);
      }
      do {
          let inouttargetyellowg = [216, 586, 68];
         basketballplayerplaceholder1 /= Math.max(parseFloat(`${parseInt(`${basketballplayerplaceholder1}`) / 3}`), 2);
         inouttargetyellowg.push(inouttargetyellowg.length << (Math.min(Math.abs(1), 3)));
         if (basketballplayerplaceholder1 == 2528268.0) {
            break;
         }
      } while (((4.54 * ping8) > 1.12 || (4.54 * ping8) > 1.49) && (basketballplayerplaceholder1 == 2528268.0));
      do {
         basketballplayerplaceholder1 -= parseFloat(`${3 >> (Math.min(Math.abs(parseInt(`${basketballplayerplaceholder1}`)), 1))}`);
         if (3894750.0 == basketballplayerplaceholder1) {
            break;
         }
      } while ((2.88 <= (basketballplayerplaceholder1 + ping8)) && (3894750.0 == basketballplayerplaceholder1));
      do {
         ping8 += parseFloat(`${2}`);
         if (ping8 == 2947733.0) {
            break;
         }
      } while ((ping8 == 2947733.0) && (1.9 <= (ping8 / (Math.max(4, basketballplayerplaceholder1)))));
         ping8 -= parseFloat(`${parseInt(`${basketballplayerplaceholder1}`)}`);
      if (ping8 < 5.15) {
         basketballplayerplaceholder1 += parseFloat(`${parseInt(`${ping8}`) - 1}`);
      }
      othermatchdetailbgY = !othermatchdetailbgY;

})();