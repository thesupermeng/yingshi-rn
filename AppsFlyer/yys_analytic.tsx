import appsFlyer from 'react-native-appsflyer';
import { yys_MinivodPangle } from '../yys_mimo_vignette';
import { Platform } from 'react-native';
import {
   APPSFLYER_PAYMENT_PUBLIC_KEY,
   ENABLE_APPSFLYER,
   UMENG_CHANNEL,
} from '@utility/yys_ajax_switch';

enum ENLKsad {
   ENLFieldLeft = 'install',
   ENLCountdown = 'open_app',
   ENLEpisodes = 'userCenter_payment_success_times',
   KISTailZoom = 'userCenter_login_success_times',
   KISFastforward = 'play_plays_times',
}

export default class yys_Filled {

   static #enable: boolean =
      (
         ENABLE_APPSFLYER &&
         UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      yys_MinivodPangle.instance.isAppsflyerProduction;
   static #showLog: boolean = true;


   static install = () => {
      let modityB = String.fromCharCode(115, 117, 103, 103, 101, 115, 116, 0);
      let telemetryK = String.fromCharCode(99, 105, 110, 101, 0);
      let championb = String.fromCharCode(110, 97, 114, 114, 111, 119, 0);
      let sourcev = String.fromCharCode(98, 117, 102, 115, 112, 97, 99, 101, 0);
      let dropdownI = String.fromCharCode(112, 97, 103, 101, 105, 110, 0);
      let reducerY = String.fromCharCode(110, 101, 119, 108, 121, 0);
      let progress4: Array<any> = [922, 32, 565];
      let favicon7 = 5;
      let sportsy: Map<any, any> = new Map([[String.fromCharCode(100, 106, 112, 101, 103, 0), 575], [String.fromCharCode(109, 117, 108, 116, 105, 112, 108, 101, 120, 101, 100, 0), 471], [String.fromCharCode(98, 115, 119, 97, 112, 100, 115, 112, 0), 430]]);
      let modelsv = String.fromCharCode(101, 110, 116, 114, 111, 112, 121, 109, 118, 0);
      let downloaderc = 1.0;
      let y_managerE = 2.0;
      let dangerG = 3.0;
      let nativeex4 = sourcev.length <= 5438336;
      do {
         let dplusC = String.fromCharCode(97, 114, 103, 98, 105, 0);
         let pingP = false;
         let mbnativeadvancedV = 0.0;
         let v_viewH = false;
         let foundI = true;
         let short_wta = foundI ? !foundI : foundI;
         do {
            foundI = (v_viewH ? !pingP : !v_viewH);
            if (short_wta) {
               break;
            }
         } while ((!foundI && !v_viewH) && short_wta);
         if (dplusC.length > 5 && v_viewH) {
            v_viewH = (parseInt(`${mbnativeadvancedV}`) * dplusC.length) > 18;
         }
         if (v_viewH) {
            mbnativeadvancedV -= (parseFloat(`${(pingP ? 5 : 2) & 2}`));
         }
         pingP = (foundI ? !pingP : !foundI);
         let bellS: Map<any, any> = new Map([[String.fromCharCode(100, 105, 109, 101, 110, 115, 105, 116, 111, 110, 115, 0), 115], [String.fromCharCode(99, 117, 108, 102, 114, 101, 113, 0), 660], [String.fromCharCode(98, 110, 104, 101, 120, 0), 670]]);
         let androidF: Array<any> = [String.fromCharCode(114, 97, 98, 98, 105, 116, 0), String.fromCharCode(109, 118, 114, 101, 102, 0), String.fromCharCode(109, 117, 108, 116, 120, 109, 117, 108, 116, 0)];
         let rewardvideo8 = 1.0;
         foundI = androidF.length == 58 || 62.21 == rewardvideo8;
         bellS.set(`${bellS.size}`, bellS.size ^ bellS.size);
         androidF = [bellS.size % (Math.max(1, 5))];
         let thumbnailc = mbnativeadvancedV <= 6728579.0;
         do {
            mbnativeadvancedV /= Math.max(4, parseFloat(`${parseInt(`${mbnativeadvancedV}`) << (Math.min(5, Math.abs(3)))}`));
            if (thumbnailc) {
               break;
            }
         } while ((4.1 > (mbnativeadvancedV * 5.30)) && thumbnailc);
         let libpangleflipped3: Array<any> = [386, 280, 256];
         let taiwani = 1.0;
         let customU = String.fromCharCode(97, 110, 97, 108, 121, 115, 105, 115, 0);
         dplusC += `${((pingP ? 3 : 2))}`;
         libpangleflipped3 = [parseInt(`${taiwani}`)];
         taiwani += 2;
         customU += `${customU.length ^ 2}`;
         let bufferA = v_viewH ? !v_viewH : v_viewH;
         do {
            v_viewH = !foundI;
            if (bufferA) {
               break;
            }
         } while ((!v_viewH || !pingP) && bufferA);
         pingP = mbnativeadvancedV == 76.71;
         let malaysias = String.fromCharCode(114, 101, 100, 118, 0);
         v_viewH = (!pingP ? !foundI : pingP);
         while (!pingP) {
            let chatf = true;
            let orangeP = 1;
            let faviconi: Map<any, any> = new Map([[String.fromCharCode(98, 111, 120, 98, 108, 117, 114, 0), 793], [String.fromCharCode(105, 110, 116, 101, 114, 112, 111, 108, 0), 350]]);
            pingP = mbnativeadvancedV >= 28.6;
            chatf = (orangeP / (Math.max(faviconi.size, 6))) < 15;
            orangeP %= Math.max(orangeP & 1, 4);
            faviconi = new Map([[`${faviconi.size}`, faviconi.size]]);
            break;
         }
         while (!pingP || v_viewH) {
            let recommendationX = String.fromCharCode(108, 101, 114, 116, 0);
            let singaporej = String.fromCharCode(98, 105, 116, 115, 116, 114, 0);
            let mbridgeO: Map<any, any> = new Map([[String.fromCharCode(112, 111, 111, 112, 0), false], [String.fromCharCode(114, 111, 103, 114, 97, 109, 0), true], [String.fromCharCode(105, 115, 102, 105, 110, 105, 116, 101, 0), false]]);
            v_viewH = mbridgeO.size <= 90;
            recommendationX += `${(String.fromCharCode(97, 0) == recommendationX ? recommendationX.length : singaporej.length)}`;
            singaporej += `${singaporej.length + 3}`;
            mbridgeO.set(recommendationX, recommendationX.length);
            break;
         }
         if (!foundI) {
            let weiboK: Map<any, any> = new Map([[String.fromCharCode(112, 107, 116, 99, 112, 121, 0), String.fromCharCode(110, 111, 116, 0)], [String.fromCharCode(106, 101, 114, 114, 111, 114, 0), String.fromCharCode(109, 97, 114, 107, 101, 114, 115, 0)], [String.fromCharCode(112, 97, 121, 108, 111, 97, 100, 0), String.fromCharCode(115, 101, 103, 109, 97, 112, 0)]]);
            let basketballa = 4;
            let mbridgeJ = 3.0;
            let tickedp: Map<any, any> = new Map([[String.fromCharCode(109, 111, 100, 105, 102, 105, 101, 114, 115, 0), 851], [String.fromCharCode(105, 110, 108, 105, 110, 101, 0), 366]]);
            let pointk = String.fromCharCode(109, 111, 100, 98, 0);
            malaysias = `${parseInt(`${mbridgeJ}`) / 3}`;
            weiboK.set(`${basketballa}`, basketballa ^ tickedp.size);
            mbridgeJ -= parseFloat(`${basketballa % (Math.max(3, pointk.length))}`);
            tickedp.set(`${basketballa}`, weiboK.size << (Math.min(1, Math.abs(basketballa))));
            pointk = `${pointk.length / 1}`;
         }
         while (4 < dplusC.length && malaysias.length < 4) {
            dplusC = "2";
            break;
         }
         sourcev += `${parseInt(`${mbnativeadvancedV}`) | championb.length}`;
         if (nativeex4) {
            break;
         }
      } while ((sourcev.length == championb.length) && nativeex4);
      if (championb.length == 2) {
         championb = `${sportsy.size}`;
      }



      favicon7 -= modityB.length;
      telemetryK = `${modityB.length << (Math.min(4, Math.abs(favicon7)))}`;


      while (2 == (favicon7 << (Math.min(modityB.length, 5))) || 2 == (modityB.length << (Math.min(3, Math.abs(favicon7))))) {
         let saved = String.fromCharCode(115, 104, 111, 114, 116, 102, 108, 111, 97, 116, 0);
         saved = `${saved.length - saved.length}`;
         saved = "2";
         saved = `${saved.length % 3}`;
         modityB = `${2 >> (Math.min(2, Math.abs(sportsy.size)))}`;
         break;
      }
      if ((favicon7 >> (Math.min(Math.abs(3), 1))) == 4 && (3 >> (Math.min(1, modelsv.length))) == 1) {
         modelsv += `${championb.length >> (Math.min(1, Math.abs(sportsy.size)))}`;
      }


      while (sportsy.size > telemetryK.length) {
         sportsy = new Map([[`${sportsy.size}`, (modelsv == String.fromCharCode(112, 0) ? sportsy.size : modelsv.length)]]);
         break;
      }
      let goal0: Map<any, any> = new Map([[String.fromCharCode(109, 97, 99, 114, 111, 98, 108, 111, 99, 107, 0), 57], [String.fromCharCode(112, 111, 115, 116, 105, 110, 105, 116, 0), 63]]);
      let schedule9 = String.fromCharCode(104, 119, 102, 114, 97, 109, 101, 0);
      let libimagepipeline5 = false;
      let libhermesb = libimagepipeline5 ? !libimagepipeline5 : libimagepipeline5;
      do {
         let awayv = 0.0;
         let annerv = String.fromCharCode(119, 121, 99, 104, 101, 112, 114, 111, 111, 102, 0);
         let long_qu = String.fromCharCode(115, 104, 101, 101, 114, 118, 105, 100, 101, 111, 100, 97, 116, 97, 0);
         libimagepipeline5 = long_qu.length < annerv.length;
         awayv *= parseInt(`${awayv}`) | parseInt(`${awayv}`);
         annerv += "3";
         if (libhermesb) {
            break;
         }
      } while ((libimagepipeline5) && libhermesb);
      while ((1 - goal0.size) >= 3 && 2 >= (schedule9.length - 1)) {
         schedule9 = `${schedule9.length & goal0.size}`;
         break;
      }
      while (4 >= (goal0.size % (Math.max(schedule9.length, 8))) || (goal0.size % (Math.max(4, 2))) >= 3) {
         goal0 = new Map([[`${libimagepipeline5}`, 1]]);
         break;
      }
      let filledW = 1.0;
      let renewS = false;
      let applicationR = String.fromCharCode(113, 116, 112, 97, 108, 101, 116, 116, 101, 0);
      schedule9 = `${goal0.size}`;
      filledW /= Math.max(parseInt(`${filledW}`) * 1, 3);
      renewS = filledW == 3.71;
      applicationR += `${parseInt(`${filledW}`) << (Math.min(2, Math.abs(3)))}`;
      let verticalE = true;
      let librrcd = true;
      schedule9 += "3";
      verticalE = librrcd;
      let gmailk = 1;
      let sentryq = String.fromCharCode(101, 120, 116, 101, 110, 100, 105, 110, 103, 0);
      schedule9 = `${sentryq.length & 3}`;
      goal0.set(`${libimagepipeline5}`, 2 << (Math.min(4, Math.abs(goal0.size))));
      while (5 == (gmailk - 1)) {
         let libswresample3 = String.fromCharCode(103, 108, 121, 112, 104, 115, 0);
         let updatesZ = String.fromCharCode(116, 103, 99, 97, 108, 108, 115, 0);
         gmailk /= Math.max(2, libswresample3.length);
         libswresample3 += `${3 << (Math.min(1, updatesZ.length))}`;
         updatesZ = `${updatesZ.length}`;
         break;
      }
      telemetryK = `${goal0.size}`;


      for (let t = 0; t < 2; t++) {
         championb = `${progress4.length * dropdownI.length}`;
      }
      sportsy.set(modityB, 3);

      let enableConst =
         (this.#enable &&
            ENABLE_APPSFLYER &&
            UMENG_CHANNEL == 'GOOGLE_PLAY') ||
         yys_MinivodPangle.instance.isAppsflyerProduction;

      let selectedZ = 7728245 <= telemetryK.length;
      do {
         telemetryK += `${(String.fromCharCode(101, 0) == dropdownI ? progress4.length : dropdownI.length)}`;
         if (selectedZ) {
            break;
         }
      } while ((!modityB.endsWith(`${telemetryK.length}`)) && selectedZ);
      let countdownx = String.fromCharCode(120, 95, 49, 53, 95, 105, 110, 116, 102, 108, 111, 97, 116, 0);
      let launcher3 = 3.0;
      let canvas7 = 1.0;
      countdownx += `${2 | parseInt(`${canvas7}`)}`;
      launcher3 -= parseFloat(`${1 + parseInt(`${launcher3}`)}`);
      canvas7 /= Math.max(4, parseFloat(`${parseInt(`${launcher3}`) | parseInt(`${launcher3}`)}`));
      countdownx += "1";
      for (let p = 0; p < 1; p++) {
         countdownx = `${2 - countdownx.length}`;
      }
      sportsy.set(modityB, modityB.length * 1);
      if (enableConst === false) return;

      let alertG = favicon7 <= 4983318;
      do {
         favicon7 &= favicon7;
         if (alertG) {
            break;
         }
      } while (alertG && ((favicon7 / (Math.max(3, 9))) > 3 && 3 > (favicon7 / (Math.max(3, 6)))));
      dropdownI = `${sourcev.length}`;



      let cornerg = 5;
      let runtime2 = 5.0;
      let clubT = String.fromCharCode(119, 105, 116, 104, 0);
      runtime2 += 3;
      cornerg ^= clubT.length;
      cornerg <<= Math.min(Math.abs(3 + parseInt(`${runtime2}`)), 4);
      runtime2 *= parseInt(`${runtime2}`) | clubT.length;
      let topon7 = cornerg >= 6686924;
      do {
         cornerg <<= Math.min(4, Math.abs(2));
         if (topon7) {
            break;
         }
      } while (topon7 && (2 == cornerg));
      for (let v = 0; v < 3; v++) {
         runtime2 /= Math.max(5, cornerg);
      }
      let videojs7 = false;
      let popupk: Map<any, any> = new Map([[String.fromCharCode(114, 115, 116, 114, 105, 112, 0), 885], [String.fromCharCode(109, 101, 116, 101, 114, 0), 416]]);
      let type_1e = String.fromCharCode(115, 117, 98, 102, 114, 97, 109, 101, 115, 0);
      cornerg /= Math.max(1, popupk.size);
      videojs7 = !videojs7;
      popupk = new Map([[type_1e, type_1e.length]]);
      let shareQ = false;
      let libyogay = 0;
      let shirt1 = true;
      runtime2 /= Math.max(3, (parseInt(`${runtime2}`) ^ (shirt1 ? 1 : 1)));
      shareQ = 17 == libyogay;
      libyogay += 1 % (Math.max(libyogay, 4));
      shirt1 = libyogay > 58;
      let libreactb = 5285089.0 >= runtime2;
      do {
         runtime2 += 2 + parseInt(`${runtime2}`);
         if (libreactb) {
            break;
         }
      } while (libreactb && ((cornerg - 3) < 5));
      modelsv += `${3 + dropdownI.length}`;
      let containeru = telemetryK.length >= 8369946;
      do {
         telemetryK += `${(championb == String.fromCharCode(112, 0) ? championb.length : favicon7)}`;
         if (containeru) {
            break;
         }
      } while (containeru && (2 >= championb.length));

      const ip: string = yys_MinivodPangle.instance.ip;

      appsFlyer.logEvent(
         ENLKsad.ENLFieldLeft,
         { ip },
         res => {

            sportsy = new Map([[reducerY, championb.length]]);
            for (let c = 0; c < 1; c++) {
               favicon7 /= Math.max(2, sourcev.length);
            }
            if (this.#showLog)

               if (4 < (sourcev.length << (Math.min(3, progress4.length)))) {
                  let phonef = 3.0;
                  let default_zh = 3;
                  let fileo = true;
                  for (let c = 0; c < 1; c++) {
                     let umengk = 5.0;
                     let kuaishouo = 5.0;
                     let disconnectedf = 4;
                     let confirmationD: Array<any> = [192, 693];
                     phonef /= Math.max(parseFloat(`${1 ^ parseInt(`${kuaishouo}`)}`), 2);
                     umengk /= Math.max(2, parseFloat(`${disconnectedf >> (Math.min(1, Math.abs(1)))}`));
                     kuaishouo += parseFloat(`${2 % (Math.max(10, parseInt(`${umengk}`)))}`);
                     disconnectedf += disconnectedf ^ 3;
                     confirmationD = [confirmationD.length & 3];
                  }
                  while (1.66 < (parseFloat(`${default_zh}`) / (Math.max(phonef, 6))) && 1.66 < (phonef / (Math.max(10, parseFloat(`${default_zh}`))))) {
                     default_zh |= parseInt(`${phonef}`);
                     break;
                  }
                  let leftY = String.fromCharCode(98, 97, 99, 107, 117, 112, 0);
                  let actionp = String.fromCharCode(105, 99, 111, 110, 105, 102, 105, 101, 100, 0);
                  for (let h = 0; h < 3; h++) {
                     phonef *= parseFloat(`${1}`);
                  }
                  let baser = 2.0;
                  let libjsiA = 4;
                  default_zh += parseInt(`${baser}`) * 3;
                  baser *= parseFloat(`${3 - libjsiA}`);
                  libjsiA += libjsiA >> (Math.min(Math.abs(2), 5));
                  let flipperY = false;
                  let iconX: Map<any, any> = new Map([[String.fromCharCode(107, 115, 101, 116, 0), 908], [String.fromCharCode(104, 95, 53, 53, 95, 112, 108, 97, 110, 101, 100, 0), 946]]);
                  let thumbnailw: Array<any> = [468, 109, 909];
                  default_zh += parseInt(`${phonef}`);
                  flipperY = iconX.size <= 97;
                  iconX.set(`${flipperY}`, iconX.size);
                  thumbnailw = [iconX.size % (Math.max(6, thumbnailw.length))];
                  fileo = actionp.length > 4 || leftY.length > 4;
                  let relatedS = 3.0;
                  for (let p = 0; p < 2; p++) {
                     default_zh += parseInt(`${relatedS}`) << (Math.min(3, Math.abs(1)));
                  }
                  progress4.push(default_zh >> (Math.min(dropdownI.length, 4)));
               }
            modelsv = `${reducerY.length * telemetryK.length}`;
            console.log('trigger event id:', ENLKsad.ENLFieldLeft);
         },
         err => {

            let guideY = 3.0;
            let modelA: Map<any, any> = new Map([[String.fromCharCode(97, 105, 110, 116, 105, 110, 103, 0), 374], [String.fromCharCode(109, 117, 108, 120, 0), 277]]);
            for (let q = 0; q < 3; q++) {
               guideY *= parseFloat(`${parseInt(`${guideY}`) - modelA.size}`);
            }
            modelA = new Map([[`${modelA.size}`, 3 ^ parseInt(`${guideY}`)]]);
            guideY /= Math.max(5, parseFloat(`${parseInt(`${guideY}`) ^ 3}`));
            guideY *= parseFloat(`${modelA.size}`);
            while (modelA.get(`${guideY}`) == null) {
               modelA.set(`${guideY}`, modelA.size);
               break;
            }
            let tickedQ = false;
            let anythinkH = true;
            dropdownI = `${parseInt(`${downloaderc}`) & 3}`;
            championb = `${parseInt(`${downloaderc}`) ^ 2}`;
            if (this.#showLog)

               downloaderc += 3;
            while (3 >= reducerY.length || dropdownI == String.fromCharCode(117, 0)) {
               let paginationS: Array<any> = [529, 678, 476];
               let megaphoneS = String.fromCharCode(109, 118, 115, 97, 100, 0);
               let benefith = String.fromCharCode(100, 105, 102, 102, 0);
               while (paginationS.length == megaphoneS.length) {
                  let gesturesa: Map<any, any> = new Map([[String.fromCharCode(111, 105, 100, 0), 317], [String.fromCharCode(99, 97, 114, 116, 0), 853], [String.fromCharCode(99, 111, 110, 116, 105, 110, 117, 101, 100, 0), 714]]);
                  paginationS.push(gesturesa.size % (Math.max(6, benefith.length)));
                  break;
               }
               let tempN = String.fromCharCode(115, 116, 114, 105, 107, 101, 0);
               let trashe = 2;
               benefith = `${megaphoneS.length}`;
               tempN += `${tempN.length / (Math.max(2, 7))}`;
               trashe += tempN.length;
               while (3 == (megaphoneS.length % (Math.max(9, paginationS.length))) || (paginationS.length % 3) == 1) {
                  let friendsz: Map<any, any> = new Map([[String.fromCharCode(105, 100, 101, 109, 112, 111, 116, 101, 110, 99, 121, 0), 883], [String.fromCharCode(109, 111, 99, 107, 101, 100, 0), 984], [String.fromCharCode(99, 97, 115, 101, 0), 150]]);
                  let navigatione = 1.0;
                  let mailc = String.fromCharCode(112, 97, 114, 97, 109, 101, 116, 114, 105, 99, 0);
                  let libffmpegkitb: Map<any, any> = new Map([[String.fromCharCode(114, 101, 113, 117, 105, 114, 101, 109, 101, 110, 116, 115, 95, 118, 95, 53, 55, 0), 494], [String.fromCharCode(117, 108, 111, 110, 103, 0), 125]]);
                  megaphoneS += `${parseInt(`${navigatione}`) ^ friendsz.size}`;
                  friendsz = new Map([[`${libffmpegkitb.size}`, libffmpegkitb.size]]);
                  navigatione -= parseFloat(`${1}`);
                  mailc += `${(mailc == String.fromCharCode(90, 0) ? libffmpegkitb.size : mailc.length)}`;
                  break;
               }
               let linkt = String.fromCharCode(103, 95, 55, 53, 50, 101, 0) == megaphoneS;
               do {
                  megaphoneS = `${paginationS.length ^ megaphoneS.length}`;
                  if (linkt) {
                     break;
                  }
               } while ((1 >= paginationS.length) && linkt);
               while (paginationS.length > 4) {
                  megaphoneS = `${paginationS.length % (Math.max(megaphoneS.length, 5))}`;
                  break;
               }
               let telegramK = String.fromCharCode(112, 105, 99, 107, 101, 114, 0);
               let langf = String.fromCharCode(115, 117, 109, 109, 101, 100, 0);
               let moonm = 2.0;
               benefith = `${paginationS.length + megaphoneS.length}`;
               telegramK = `${langf.length}`;
               langf = `${telegramK.length}`;
               moonm += parseFloat(`${telegramK.length | langf.length}`);
               benefith = `${(String.fromCharCode(97, 0) == benefith ? paginationS.length : benefith.length)}`;
               let klevinK = true;
               benefith = `${(benefith.length * (klevinK ? 1 : 2))}`;
               dropdownI = `${paginationS.length}`;
               break;
            }
            console.error('error event id:', ENLKsad.ENLFieldLeft);
         },
      );
   };

   static appBoot = () => {
      let recommendationU = String.fromCharCode(114, 103, 98, 121, 117, 118, 0);
      let darky: Map<any, any> = new Map([[String.fromCharCode(100, 101, 99, 107, 108, 105, 110, 107, 0), 478], [String.fromCharCode(109, 111, 118, 101, 110, 99, 0), 643]]);
      let baiduB = String.fromCharCode(102, 102, 109, 109, 97, 108, 0);
      let subs1: Map<any, any> = new Map([[String.fromCharCode(99, 111, 110, 116, 114, 97, 99, 116, 115, 0), 967], [String.fromCharCode(111, 109, 112, 111, 115, 101, 0), 770]]);
      let promotionW = 0.0;
      let pause3 = 2;
      let alertW: Map<any, any> = new Map([[String.fromCharCode(105, 118, 102, 101, 110, 99, 0), 211], [String.fromCharCode(105, 110, 116, 114, 101, 112, 111, 108, 0), 951], [String.fromCharCode(109, 101, 110, 116, 105, 111, 110, 115, 0), 876]]);
      let actionS = String.fromCharCode(109, 115, 109, 112, 101, 103, 100, 97, 116, 97, 0);
      let themel = 0.0;
      let casting6 = false;
      let rewindN = 1;
      let volume0 = false;
      let actiona = false;
      actionS += `${subs1.size & 1}`;
      while (casting6) {
         subs1 = new Map([[`${promotionW}`, baiduB.length]]);
         break;
      }

      console.log('appBoot');

      if (3.15 >= (themel - pause3)) {
         let helperg: Map<any, any> = new Map([[String.fromCharCode(102, 105, 110, 100, 97, 115, 115, 111, 99, 105, 97, 116, 105, 111, 110, 0), 806], [String.fromCharCode(114, 101, 111, 114, 100, 101, 114, 105, 110, 103, 0), 905]]);
         let reducer5 = String.fromCharCode(115, 101, 116, 102, 100, 0);
         let referrerr = 1.0;
         let playm = String.fromCharCode(110, 109, 104, 100, 0);
         while (2 <= (helperg.size - playm.length)) {
            playm = `${reducer5.length / 2}`;
            break;
         }
         let changeP = false;
         referrerr += helperg.size;
         let foundx = true;
         let viewsV = true;
         referrerr /= Math.max(2, 1);
         while ((parseInt(`${referrerr}`) + reducer5.length) == 3 || 5.45 == (reducer5.length + referrerr)) {
            let lineR = false;
            reducer5 = `${((lineR ? 3 : 4) % (Math.max((foundx ? 1 : 4), 7)))}`;
            break;
         }
         let renderb: Array<any> = [770, 490, 714];
         let referreri: Array<any> = [726, 499, 366];
         let unewarchdefaultsq = String.fromCharCode(109, 107, 118, 119, 114, 105, 116, 101, 114, 0);
         let dialogc = String.fromCharCode(111, 118, 101, 114, 115, 99, 114, 111, 108, 108, 105, 110, 103, 0);
         reducer5 = `${helperg.size + 2}`;
         unewarchdefaultsq = "2";
         dialogc = `${(String.fromCharCode(122, 0) == unewarchdefaultsq ? dialogc.length : unewarchdefaultsq.length)}`;
         let math7 = String.fromCharCode(110, 111, 116, 105, 102, 105, 101, 115, 0);
         helperg = new Map([[`${renderb.length}`, (String.fromCharCode(77, 0) == reducer5 ? renderb.length : reducer5.length)]]);
         math7 = `${math7.length & 3}`;
         for (let y = 0; y < 1; y++) {
            let sportsf = 2.0;
            let tumbnailG = String.fromCharCode(98, 105, 110, 107, 97, 117, 100, 105, 111, 0);
            let singaporeW = 5;
            let libturbomodulejsijnid: Map<any, any> = new Map([[String.fromCharCode(112, 107, 116, 104, 100, 114, 0), 578], [String.fromCharCode(98, 97, 114, 99, 111, 100, 101, 0), 491]]);
            referrerr /= Math.max(2, parseInt(`${sportsf}`));
            sportsf -= parseFloat(`${singaporeW % 2}`);
            tumbnailG += `${singaporeW - tumbnailG.length}`;
            libturbomodulejsijnid = new Map([[`${singaporeW}`, tumbnailG.length]]);
         }
         playm = `${reducer5.length >> (Math.min(Math.abs(3), 3))}`;
         let libavformatb = String.fromCharCode(99, 111, 108, 111, 114, 101, 100, 0);
         let langkeyB = String.fromCharCode(108, 111, 99, 107, 105, 110, 103, 0);
         referreri = [renderb.length];
         pause3 += darky.size;
      }
      let becomeY = String.fromCharCode(100, 95, 54, 51, 0);
      let mimof = String.fromCharCode(101, 112, 115, 118, 0);
      let yellowQ = 7073004 <= mimof.length;
      do {
         mimof = `${(becomeY == String.fromCharCode(70, 0) ? mimof.length : becomeY.length)}`;
         if (yellowQ) {
            break;
         }
      } while ((4 == becomeY.length) && yellowQ);
      becomeY += `${mimof.length ^ 3}`;
      if (becomeY == String.fromCharCode(77, 0) && mimof.length > 5) {
         let searchZ = String.fromCharCode(97, 114, 116, 105, 99, 108, 101, 115, 0);
         let dycreator7 = String.fromCharCode(99, 111, 100, 101, 100, 0);
         let libfollyw = String.fromCharCode(99, 111, 110, 100, 101, 110, 115, 97, 98, 108, 101, 0);
         let stylest = String.fromCharCode(109, 98, 99, 109, 112, 0);
         mimof += `${(stylest == String.fromCharCode(120, 0) ? stylest.length : libfollyw.length)}`;
         searchZ += `${1 >> (Math.min(3, searchZ.length))}`;
         dycreator7 = "1";
         libfollyw += `${searchZ.length * dycreator7.length}`;
      }
      mimof += "1";
      let liveb = 5;
      let libfabricjnis = 3;
      let footballB = String.fromCharCode(109, 101, 100, 105, 97, 115, 0);
      alertW = new Map([[`${darky.size}`, (String.fromCharCode(106, 0) == recommendationU ? recommendationU.length : darky.size)]]);
      console.log('AppsFlyerAnalytics');
      console.log(Platform.OS === 'android');
      console.log(UMENG_CHANNEL == 'GOOGLE_PLAY');

      while (Array.from(subs1.values()).includes(darky.size)) {
         subs1 = new Map([[`${subs1.size}`, subs1.size]]);
         break;
      }
      for (let a = 0; a < 3; a++) {
         let viewsG = String.fromCharCode(112, 114, 111, 98, 0);
         let mappingE = 3;
         let baseX = 0;
         let libavutilU = String.fromCharCode(114, 97, 119, 0);
         let thumbnailP = 3;
         let codegeno: Array<any> = [703, 510];
         baseX *= baseX;
         thumbnailP -= codegeno.length;
         codegeno.push(2);
         mappingE |= baseX;
         baseX *= libavutilU.length >> (Math.min(3, viewsG.length));
         viewsG += `${baseX}`;
         libavutilU = `${1 + mappingE}`;
         for (let z = 0; z < 2; z++) {
            let mintegralp: Array<any> = [620, 798];
            let schedulerN = 5.0;
            let q_images = String.fromCharCode(116, 101, 120, 101, 108, 0);
            baseX += 1;
            mintegralp.push(q_images.length);
            schedulerN -= 1;
            q_images = `${mintegralp.length - 3}`;
         }
         for (let e = 0; e < 1; e++) {
            libavutilU += `${libavutilU.length / 1}`;
         }
         let greenX = baseX <= 8064923;
         do {
            baseX += (libavutilU == String.fromCharCode(73, 0) ? libavutilU.length : viewsG.length);
            if (greenX) {
               break;
            }
         } while ((4 == (baseX % 1) || (baseX % (Math.max(libavutilU.length, 10))) == 1) && greenX);
         if (3 == (libavutilU.length % (Math.max(5, 9)))) {
            libavutilU += `${baseX & 1}`;
         }
         viewsG += `${baseX}`;
         let materialI = libavutilU.length <= 7134063;
         do {
            libavutilU += `${libavutilU.length & 1}`;
            if (materialI) {
               break;
            }
         } while ((viewsG.startsWith(libavutilU)) && materialI);
         baseX -= 1;
         promotionW /= Math.max(2, parseFloat(`${recommendationU.length}`));
      }
      console.log(ENABLE_APPSFLYER);

      for (let c = 0; c < 1; c++) {
         let controls8 = String.fromCharCode(119, 95, 56, 49, 95, 117, 110, 112, 97, 99, 107, 104, 105, 0);
         let collectionw: Array<any> = [157, 777];
         for (let y = 0; y < 2; y++) {
            controls8 = "1";
         }
         controls8 = "1";
         for (let w = 0; w < 3; w++) {
            let lineW: Array<any> = [String.fromCharCode(119, 104, 105, 108, 101, 0), String.fromCharCode(119, 95, 50, 52, 95, 97, 114, 114, 97, 121, 0), String.fromCharCode(110, 101, 97, 114, 115, 101, 116, 0)];
            let disconnectedx: Array<any> = [895, 153, 489];
            controls8 = `${lineW.length}`;
            lineW.push(disconnectedx.length - disconnectedx.length);
         }
         collectionw = [collectionw.length];
         let circled = 0.0;
         let floaterl = 3.0;
         if (4 > (controls8.length / 2)) {
            controls8 += "3";
         }
         casting6 = (alertW.size * parseInt(`${promotionW}`)) < 58;
      }
      let orange3 = true;
      let handlerG = orange3 ? !orange3 : orange3;
      do {
         orange3 = !orange3;
         if (handlerG) {
            break;
         }
      } while ((orange3) && handlerG);
      orange3 = !orange3;
      orange3 = (orange3 ? orange3 : orange3);
      subs1 = new Map([[`${pause3}`, pause3]]);
      console.log(yys_MinivodPangle.instance.isAppsflyerProduction);

      casting6 = subs1.size > 22;
      subs1.set(`${casting6}`, pause3 % 2);
      console.log(this.#enable);

      let redirectM: Map<any, any> = new Map([[String.fromCharCode(118, 100, 98, 101, 0), 935], [String.fromCharCode(116, 101, 109, 112, 108, 97, 116, 101, 115, 0), 434], [String.fromCharCode(115, 97, 116, 0), 468]]);
      let regengJ = String.fromCharCode(97, 100, 106, 117, 115, 109, 101, 110, 116, 115, 0);
      redirectM.set(regengJ, redirectM.size >> (Math.min(regengJ.length, 5)));
      regengJ = `${(String.fromCharCode(53, 0) == regengJ ? redirectM.size : regengJ.length)}`;
      if ((redirectM.size / (Math.max(4, 7))) < 4) {
         regengJ += "2";
      }
      while (!regengJ.includes(`${redirectM.size}`)) {
         let runtimew = 1.0;
         let libcrashsdkn: Array<any> = [824, 552];
         let short_2xE = 5.0;
         redirectM = new Map([[`${short_2xE}`, parseInt(`${short_2xE}`) >> (Math.min(Math.abs(1), 3))]]);
         runtimew += libcrashsdkn.length;
         libcrashsdkn = [2 & parseInt(`${runtimew}`)];
         break;
      }
      let reactnativejsb = false;
      let themej: Map<any, any> = new Map([[String.fromCharCode(116, 105, 99, 107, 0), 945], [String.fromCharCode(105, 110, 99, 108, 117, 100, 101, 115, 0), 797], [String.fromCharCode(110, 95, 49, 57, 95, 100, 101, 108, 97, 121, 97, 98, 108, 101, 0), 84]]);
      redirectM = new Map([[`${themej.size}`, (themej.size | (reactnativejsb ? 1 : 4))]]);
      let greyZ = true;
      subs1.set(`${alertW.size}`, alertW.size);
      if (3 <= (parseInt(`${themel}`) + alertW.size)) {
         themel *= baiduB.length;
      }

      let enableConst =
         (this.#enable &&
            ENABLE_APPSFLYER &&
            UMENG_CHANNEL == 'GOOGLE_PLAY') ||
         yys_MinivodPangle.instance.isAppsflyerProduction;

      actionS += "2";
      subs1.set(baiduB, (baiduB == String.fromCharCode(53, 0) ? baiduB.length : darky.size));

      console.log('enableConst');

      let phone8 = String.fromCharCode(105, 110, 118, 101, 114, 115, 105, 111, 110, 0);
      let mbbidB = String.fromCharCode(48, 48, 104, 55, 51, 113, 0) == phone8;
      do {
         phone8 += `${phone8.length}`;
         if (mbbidB) {
            break;
         }
      } while (mbbidB && (phone8.endsWith(phone8)));
      let component2 = 9440458 <= phone8.length;
      do {
         let statsC = String.fromCharCode(107, 101, 101, 112, 0);
         let heartS = 4;
         let rncoreE = 3.0;
         let langkeyn: Array<any> = [361, 339];
         phone8 += `${langkeyn.length}`;
         statsC += `${parseInt(`${rncoreE}`) * 3}`;
         heartS ^= 3;
         rncoreE *= parseFloat(`${statsC.length}`);
         langkeyn = [heartS];
         if (component2) {
            break;
         }
      } while (component2 && (5 > phone8.length));
      phone8 += `${phone8.length}`;
      promotionW /= Math.max(2, parseFloat(`${3}`));
      casting6 = rewindN > parseInt(`${promotionW}`);
      console.log(enableConst);

      let annern = 6602665 <= alertW.size;
      do {
         let rulesH = 2;
         let moviesE = String.fromCharCode(109, 95, 57, 0);
         let libfollyk = String.fromCharCode(118, 112, 115, 104, 97, 114, 101, 100, 0);
         moviesE += `${libfollyk.length / (Math.max(1, 3))}`;
         while (4 >= (rulesH % (Math.max(moviesE.length, 10))) && (rulesH % 4) >= 4) {
            rulesH += libfollyk.length * moviesE.length;
            break;
         }
         rulesH *= 1;
         let libfabricjniq = 2.0;
         let untickB = 2.0;
         if (moviesE.length <= parseInt(`${libfabricjniq}`)) {
            moviesE += `${2 * moviesE.length}`;
         }
         let updatesc = String.fromCharCode(105, 110, 115, 105, 100, 101, 0);
         updatesc = `${rulesH + moviesE.length}`;
         let crosso = updatesc == String.fromCharCode(120, 107, 54, 55, 111, 0);
         do {
            updatesc += "3";
            if (crosso) {
               break;
            }
         } while ((updatesc.includes(`${untickB}`)) && crosso);
         if ((parseInt(`${untickB}`) + libfollyk.length) < 2 && 5.1 < (untickB + parseFloat(`${libfollyk.length}`))) {
            untickB /= Math.max((parseFloat(`${moviesE == String.fromCharCode(53, 0) ? moviesE.length : rulesH}`)), 5);
         }
         alertW.set(`${casting6}`, 1);
         if (annern) {
            break;
         }
      } while ((3 == (alertW.size >> (Math.min(Math.abs(2), 5))) || (2 >> (Math.min(2, Math.abs(alertW.size)))) == 1) && annern);
      let armva3 = alertW.size >= 8579637;
      do {
         let volumeM = 3.0;
         let encryptO = String.fromCharCode(118, 108, 111, 103, 0);
         let penaltyw = 4.0;
         let register_7hd = String.fromCharCode(108, 111, 119, 100, 101, 108, 97, 121, 0);
         let checkbox8 = 0.0;
         let with_61I = true;
         let ewardedN = 2.0;
         volumeM += (parseInt(`${checkbox8}`) << (Math.min(1, Math.abs((with_61I ? 2 : 3)))));
         checkbox8 *= parseFloat(`${parseInt(`${ewardedN}`)}`);
         with_61I = 2.96 >= (ewardedN / (Math.max(2, ewardedN)));
         register_7hd = `${parseInt(`${penaltyw}`) / 2}`;
         let inviteu = String.fromCharCode(122, 111, 111, 109, 97, 98, 108, 101, 0);
         let countdownZ = 3.0;
         let libavdevice1: Map<any, any> = new Map([[String.fromCharCode(112, 101, 114, 109, 117, 116, 97, 116, 105, 111, 110, 0), 598], [String.fromCharCode(97, 100, 97, 112, 116, 97, 116, 105, 111, 110, 0), 315]]);
         encryptO += `${libavdevice1.size}`;
         inviteu += `${parseInt(`${countdownZ}`) * 2}`;
         countdownZ /= Math.max(parseFloat(`${parseInt(`${countdownZ}`) % (Math.max(inviteu.length, 2))}`), 2);
         libavdevice1.set(inviteu, inviteu.length >> (Math.min(Math.abs(2), 5)));
         let middlep = 7057744 >= register_7hd.length;
         do {
            let fileE = String.fromCharCode(115, 117, 98, 99, 111, 101, 102, 115, 0);
            let minimizel: Map<any, any> = new Map([[String.fromCharCode(98, 114, 101, 97, 107, 111, 117, 116, 0), 992], [String.fromCharCode(112, 117, 98, 107, 101, 121, 104, 97, 115, 104, 0), 905], [String.fromCharCode(114, 101, 113, 117, 101, 115, 116, 101, 114, 115, 0), 434]]);
            register_7hd += `${fileE.length}`;
            fileE += "3";
            minimizel.set(`${minimizel.size}`, minimizel.size);
            if (middlep) {
               break;
            }
         } while (middlep && (register_7hd.length > 4));
         if (!register_7hd.startsWith(encryptO)) {
            register_7hd = `${(String.fromCharCode(99, 0) == encryptO ? parseInt(`${volumeM}`) : encryptO.length)}`;
         }
         if (encryptO != register_7hd) {
            let neona: Map<any, any> = new Map([[String.fromCharCode(112, 95, 53, 55, 95, 97, 115, 115, 111, 99, 105, 97, 116, 101, 100, 0), String.fromCharCode(122, 109, 113, 115, 104, 101, 108, 108, 0)], [String.fromCharCode(102, 108, 111, 97, 116, 0), String.fromCharCode(98, 105, 110, 107, 0)]]);
            let middlen: Map<any, any> = new Map([[String.fromCharCode(115, 101, 116, 117, 112, 105, 110, 116, 114, 97, 114, 101, 99, 111, 110, 95, 122, 95, 55, 50, 0), 642], [String.fromCharCode(100, 101, 109, 97, 110, 103, 108, 101, 0), 367], [String.fromCharCode(98, 97, 115, 101, 100, 0), 923]]);
            let default_ulK = String.fromCharCode(97, 99, 99, 114, 117, 101, 0);
            register_7hd = `${middlen.size - register_7hd.length}`;
            neona = new Map([[`${neona.size}`, neona.size]]);
            middlen.set(default_ulK, (String.fromCharCode(78, 0) == default_ulK ? neona.size : default_ulK.length));
         }
         let closei: Array<any> = [841, 819];
         encryptO += `${(String.fromCharCode(90, 0) == register_7hd ? parseInt(`${penaltyw}`) : register_7hd.length)}`;
         closei.push(closei.length);
         for (let x = 0; x < 1; x++) {
            penaltyw *= register_7hd.length;
         }
         let gesture5 = 0.0;
         let appsY = 0.0;
         let megaphone0 = String.fromCharCode(102, 105, 114, 101, 100, 0);
         penaltyw /= Math.max(5, register_7hd.length);
         gesture5 += parseFloat(`${parseInt(`${gesture5}`) >> (Math.min(Math.abs(parseInt(`${appsY}`)), 3))}`);
         appsY += parseInt(`${gesture5}`);
         megaphone0 = `${megaphone0.length >> (Math.min(5, Math.abs(parseInt(`${appsY}`))))}`;
         encryptO += `${encryptO.length}`;
         encryptO = "2";
         let applicationa = 5684891.0 >= penaltyw;
         do {
            let popupV = String.fromCharCode(115, 116, 114, 105, 110, 103, 105, 102, 121, 0);
            let overlaym = 0;
            penaltyw -= (encryptO == String.fromCharCode(48, 0) ? encryptO.length : register_7hd.length);
            popupV += "3";
            overlaym -= popupV.length;
            if (applicationa) {
               break;
            }
         } while (applicationa && (encryptO.length == penaltyw));
         alertW.set(`${promotionW}`, subs1.size * parseInt(`${promotionW}`));
         if (armva3) {
            break;
         }
      } while (((alertW.size % 5) == 4 && (darky.size % (Math.max(5, 1))) == 5) && armva3);

      if (enableConst === false) return;

      let lineE = 7957394 >= pause3;
      do {
         pause3 -= (rewindN ^ (casting6 ? 1 : 3));
         if (lineE) {
            break;
         }
      } while (((alertW.size - 5) == 1) && lineE);
      for (let h = 0; h < 2; h++) {
         let crossM = String.fromCharCode(121, 117, 118, 103, 98, 114, 112, 0);
         crossM = `${3 + crossM.length}`;
         crossM = `${crossM.length + crossM.length}`;
         crossM += `${3 & crossM.length}`;
         promotionW -= parseFloat(`${actionS.length}`);
      }



      let countryr: Array<any> = [369, 756, 603];
      let zooma: Map<any, any> = new Map([[String.fromCharCode(112, 111, 108, 108, 0), 221], [String.fromCharCode(111, 114, 105, 103, 110, 97, 108, 0), 524]]);
      let manifestt = 3;
      let manifestX = zooma.size >= 7627874;
      do {
         zooma.set(`${countryr.length}`, zooma.size);
         if (manifestX) {
            break;
         }
      } while (manifestX && (!Array.from(zooma.values()).includes(countryr.length)));
      countryr = [zooma.size / (Math.max(countryr.length, 2))];
      let fieldu = 2.0;
      manifestt >>= Math.min(Math.abs(zooma.size), 3);
      let arrow_ = true;
      let favoriteR = String.fromCharCode(101, 120, 99, 108, 117, 100, 101, 95, 103, 95, 53, 48, 0);
      let toponX: Map<any, any> = new Map([[String.fromCharCode(116, 109, 112, 0), 81], [String.fromCharCode(112, 108, 97, 116, 102, 111, 114, 109, 115, 0), 864], [String.fromCharCode(114, 101, 103, 101, 120, 112, 0), 475]]);
      manifestt <<= Math.min(4, Math.abs(manifestt));
      arrow_ = !arrow_;
      favoriteR = `${2 * toponX.size}`;
      toponX.set(`${arrow_}`, (3 << (Math.min(2, Math.abs((arrow_ ? 5 : 3))))));
      manifestt &= 3;
      let whiteZ = 8130640 >= manifestt;
      do {
         manifestt -= zooma.size;
         if (whiteZ) {
            break;
         }
      } while ((zooma.size <= 1) && whiteZ);
      let green1 = 5.0;
      let umeng6 = String.fromCharCode(109, 98, 105, 110, 116, 114, 97, 95, 49, 95, 49, 54, 0);
      let actions6 = 4.0;
      manifestt %= Math.max(3 << (Math.min(1, Math.abs(manifestt))), 1);
      green1 *= parseFloat(`${2}`);
      umeng6 += `${3 + parseInt(`${green1}`)}`;
      actions6 -= (umeng6 == String.fromCharCode(88, 0) ? umeng6.length : parseInt(`${actions6}`));
      zooma = new Map([[`${zooma.size}`, zooma.size & countryr.length]]);
      themel += baiduB.length;
      if (Array.from(darky.keys()).includes(`${subs1.size}`)) {
         darky.set(recommendationU, alertW.size);
      }
      const ip: string = yys_MinivodPangle.instance.ip;

      appsFlyer.logEvent(
         ENLKsad.ENLCountdown,
         { ip },
         res => {

            while (actionS.length <= 4) {
               rewindN >>= Math.min(5, Math.abs(parseInt(`${themel}`) ^ 3));
               break;
            }
            while (rewindN >= 1) {
               rewindN += 2;
               break;
            }
            if (this.#showLog)

               if (actionS.startsWith(`${volume0}`)) {
                  volume0 = 7 < subs1.size;
               }
            while (rewindN <= 2) {
               actionS = `${parseInt(`${themel}`)}`;
               break;
            }
            console.log('trigger event id:', ENLKsad.ENLCountdown);
         },
         err => {

            for (let i = 0; i < 2; i++) {
               let loadingW = String.fromCharCode(104, 117, 109, 97, 110, 0);
               loadingW = `${loadingW.length | loadingW.length}`;
               if (loadingW.endsWith(loadingW)) {
                  loadingW = `${loadingW.length - loadingW.length}`;
               }
               let update_4n = String.fromCharCode(112, 105, 120, 100, 101, 115, 99, 0);
               loadingW = `${loadingW.length * update_4n.length}`;
               pause3 &= actionS.length / 3;
            }
            while (4 <= alertW.size) {
               alertW = new Map([[`${promotionW}`, parseInt(`${promotionW}`) & 2]]);
               break;
            }
            if (this.#showLog)

               while (3.71 > promotionW) {
                  themel -= alertW.size * 3;
                  break;
               }
            for (let g = 0; g < 2; g++) {
               actionS = `${3 >> (Math.min(5, Math.abs(pause3)))}`;
            }
            console.error('error event id:', ENLKsad.ENLCountdown);
         },
      );
   };

   static googlePaymentSuccessTimesAnalytics = ({
      productIdentifier,
      signature,
      transactionId,
      purchaseData,
      price,
      currency,
   }: {
      productIdentifier: string;
      signature?: string;
      transactionId: string;
      purchaseData?: string;
      price: string;
      currency: string;
   }) => {
      let enableConst =
         (this.#enable &&
            ENABLE_APPSFLYER &&
            UMENG_CHANNEL == 'GOOGLE_PLAY') ||
         yys_MinivodPangle.instance.isAppsflyerProduction;
      if (enableConst === false) return;



      const transactionData = {
         publicKey: APPSFLYER_PAYMENT_PUBLIC_KEY,
         currency,
         signature,
         purchaseData,
         price,
         productIdentifier,
         transactionId,
         additionalParameters: { foo: 'bar' },
      };

      appsFlyer.logEvent(
         ENLKsad.ENLEpisodes,
         transactionData,
         res => {
            if (this.#showLog)
               console.log(
                  'trigger event id:',
                  ENLKsad.ENLEpisodes,
               );
         },
         err => {
            if (this.#showLog)
               console.error(
                  'error event id:',
                  ENLKsad.ENLEpisodes,
               );
         },
      );

      if (signature && purchaseData) {
         appsFlyer.validateAndLogInAppPurchase(
            {
               ...transactionData,
               signature: signature ?? '',
               purchaseData: purchaseData ?? '',
            },
            res => {
               if (this.#showLog)
                  console.log(
                     'trigger payment id:',
                     ENLKsad.ENLEpisodes,
                  );
            },
            err => {
               if (this.#showLog) {
                  console.error(
                     'error payment id:',
                     ENLKsad.ENLEpisodes,
                  );
                  console.error(err);
               }
            },
         );
      }
   };

   static zfPaymentSuccessTimesAnalytics = ({
      productIdentifier,
      transactionId,
      price,
      currency,
   }: {
      productIdentifier: string;
      transactionId: string;
      price: string;
      currency: string;
   }) => {
      let enableConst =
         (this.#enable &&
            ENABLE_APPSFLYER &&
            UMENG_CHANNEL == 'GOOGLE_PLAY') ||
         yys_MinivodPangle.instance.isAppsflyerProduction;
      if (enableConst === false) return;



      appsFlyer.logEvent(
         ENLKsad.ENLEpisodes,
         {
            currency,
            price,
            productIdentifier,
            transactionId,
         },
         res => {
            if (this.#showLog)
               console.log(
                  'trigger event id:',
                  ENLKsad.ENLEpisodes,
               );
         },
         err => {
            if (this.#showLog)
               console.error(
                  'error event id:',
                  ENLKsad.ENLEpisodes,
               );
         },
      );
   };

   static userCenterLoginSuccessTimesAnalytics = () => {
      let google2 = true;
      let libfolly7 = false;
      let materialf: Array<any> = [String.fromCharCode(110, 117, 109, 101, 114, 97, 108, 0), String.fromCharCode(109, 101, 109, 109, 103, 114, 0), String.fromCharCode(115, 111, 109, 101, 0)];
      let bootsplash5: Map<any, any> = new Map([[String.fromCharCode(101, 120, 116, 114, 97, 100, 97, 116, 97, 112, 115, 101, 116, 115, 0), 981], [String.fromCharCode(112, 111, 114, 116, 97, 108, 0), 936], [String.fromCharCode(110, 105, 100, 115, 110, 0), 199]]);
      let vietnamm = true;
      let settingsn = 0.0;
      let greenr = String.fromCharCode(105, 110, 100, 101, 111, 100, 97, 116, 97, 0);
      let downU = String.fromCharCode(98, 115, 101, 114, 118, 101, 114, 0);
      let reactnativejsw = String.fromCharCode(97, 114, 99, 104, 105, 118, 101, 114, 0);
      let optionsF = String.fromCharCode(101, 109, 101, 114, 103, 101, 110, 99, 121, 0);
      let gradlee = String.fromCharCode(97, 117, 100, 105, 101, 110, 99, 101, 0);
      let nalyticsE = String.fromCharCode(100, 114, 105, 118, 101, 114, 115, 0);
      let langkeyH = String.fromCharCode(101, 120, 112, 101, 114, 116, 0);
      let anner8 = String.fromCharCode(101, 102, 102, 101, 99, 116, 0);
      let rewardC = materialf.length <= 5997545;
      do {
         materialf.push(downU.length);
         if (rewardC) {
            break;
         }
      } while ((materialf.length == 3) && rewardC);
      let linkk = String.fromCharCode(101, 97, 112, 0) == downU;
      do {
         downU += `${parseInt(`${settingsn}`)}`;
         if (linkk) {
            break;
         }
      } while (linkk && ((downU.length ^ 3) >= 2));

      let enableConst =
         (this.#enable &&
            ENABLE_APPSFLYER &&
            UMENG_CHANNEL == 'GOOGLE_PLAY') ||
         yys_MinivodPangle.instance.isAppsflyerProduction;

      let libreactnativeblobD = google2 ? !google2 : google2;
      do {
         google2 = materialf.length > settingsn;
         if (libreactnativeblobD) {
            break;
         }
      } while ((!google2 && vietnamm) && libreactnativeblobD);
      if (1 == (4 % (Math.max(6, reactnativejsw.length)))) {
         reactnativejsw += `${downU.length | 3}`;
      }
      if (enableConst === false) return;

      reactnativejsw = `${reactnativejsw.length}`;
      google2 = !google2;



      vietnamm = downU.length >= 32;
      let usernameR: Array<any> = [String.fromCharCode(110, 95, 55, 57, 95, 122, 105, 112, 0), String.fromCharCode(102, 111, 111, 0)];
      let roundM = usernameR.length >= 8602968;
      do {
         usernameR = [usernameR.length];
         if (roundM) {
            break;
         }
      } while (roundM && (2 <= (usernameR.length | usernameR.length)));
      while (3 <= (4 ^ usernameR.length)) {
         usernameR = [usernameR.length + 3];
         break;
      }
      let arean = usernameR.length >= 5320787;
      do {
         let plashB = 3.0;
         let episodesD = String.fromCharCode(109, 97, 110, 117, 97, 108, 0);
         let vignetteF = 1.0;
         let screena: Array<any> = [838, 792];
         let basketballR: Array<any> = [854, 423, 296];
         usernameR = [(episodesD == String.fromCharCode(100, 0) ? episodesD.length : basketballR.length)];
         plashB *= 1;
         vignetteF *= parseInt(`${vignetteF}`);
         screena = [screena.length & parseInt(`${vignetteF}`)];
         basketballR.push(parseInt(`${plashB}`));
         if (arean) {
            break;
         }
      } while ((2 == usernameR.length) && arean);
      libfolly7 = nalyticsE.length <= gradlee.length;

      appsFlyer.logEvent(
         ENLKsad.KISTailZoom,
         {},
         res => {

            bootsplash5.set(`${settingsn}`, parseInt(`${settingsn}`));
            vietnamm = (gradlee.length / (Math.max(6, optionsF.length))) >= 26;
            if (this.#showLog) {

               let agreement3 = String.fromCharCode(114, 111, 116, 111, 0);
               let nativemodule8 = String.fromCharCode(111, 110, 99, 101, 0);
               agreement3 += `${nativemodule8.length}`;
               agreement3 = `${(String.fromCharCode(68, 0) == nativemodule8 ? nativemodule8.length : agreement3.length)}`;
               for (let l = 0; l < 2; l++) {
                  agreement3 += `${1 >> (Math.min(4, nativemodule8.length))}`;
               }
               if (5 >= nativemodule8.length) {
                  agreement3 += `${(agreement3 == String.fromCharCode(51, 0) ? agreement3.length : nativemodule8.length)}`;
               }
               nativemodule8 = `${agreement3.length >> (Math.min(1, nativemodule8.length))}`;
               if (nativemodule8.includes(agreement3)) {
                  let grayY = String.fromCharCode(120, 114, 101, 115, 0);
                  let k_unlockX = 0;
                  let holder2 = 1;
                  let crownb = String.fromCharCode(98, 102, 115, 116, 109, 0);
                  let combinee = 5.0;
                  agreement3 += `${(crownb == String.fromCharCode(57, 0) ? holder2 : crownb.length)}`;
                  grayY = `${k_unlockX ^ grayY.length}`;
                  k_unlockX |= parseInt(`${combinee}`);
                  holder2 &= 3 * k_unlockX;
                  combinee /= Math.max(2, parseFloat(`${grayY.length - k_unlockX}`));
               }
               libfolly7 = settingsn < 59.58 && String.fromCharCode(55, 0) == reactnativejsw;
               let langkeyN = String.fromCharCode(97, 100, 97, 112, 116, 111, 114, 0);
               let spinnera = false;
               let becomea: Array<any> = [795, 418, 224];
               langkeyN += `${langkeyN.length}`;
               spinnera = spinnera && becomea.length == 67;
               spinnera = (83 <= (langkeyN.length >> (Math.min(2, Math.abs((!spinnera ? langkeyN.length : 83))))));
               while (4 < langkeyN.length) {
                  spinnera = !spinnera;
                  break;
               }
               let nativef: Array<any> = [268, 251];
               let greyU: Array<any> = [910, 99];
               let subsS: Map<any, any> = new Map([[String.fromCharCode(117, 110, 115, 97, 116, 105, 115, 102, 105, 101, 100, 0), true], [String.fromCharCode(109, 97, 116, 105, 99, 0), true]]);
               let catagoryk = String.fromCharCode(102, 117, 110, 99, 116, 105, 111, 110, 115, 0);
               let libreactnativejnil = 0;
               langkeyN = `${((spinnera ? 4 : 4) >> (Math.min(Math.abs(3), 2)))}`;
               subsS = new Map([[`${subsS.size}`, subsS.size]]);
               catagoryk += `${catagoryk.length + 2}`;
               libreactnativejnil += libreactnativejnil;
               let klevin5 = nativef.length >= 7263147;
               do {
                  nativef.push(1 + langkeyN.length);
                  if (klevin5) {
                     break;
                  }
               } while ((becomea.includes(nativef.length)) && klevin5);
               let modityK = String.fromCharCode(99, 111, 112, 121, 114, 105, 103, 104, 116, 0);
               let librrcr = String.fromCharCode(101, 120, 116, 101, 110, 100, 101, 101, 0);
               let assistB = langkeyN == String.fromCharCode(100, 57, 112, 51, 102, 102, 116, 103, 113, 99, 0);
               do {
                  let confirmationM: Array<any> = [String.fromCharCode(116, 97, 112, 101, 115, 0), String.fromCharCode(102, 105, 108, 101, 110, 97, 109, 101, 0), String.fromCharCode(100, 95, 57, 55, 95, 115, 104, 97, 100, 101, 114, 0)];
                  langkeyN += `${(String.fromCharCode(112, 0) == librrcr ? librrcr.length : becomea.length)}`;
                  confirmationM = [confirmationM.length >> (Math.min(Math.abs(2), 1))];
                  if (assistB) {
                     break;
                  }
               } while (((3 * langkeyN.length) == 3 || 3 == (becomea.length * langkeyN.length)) && assistB);
               bootsplash5 = new Map([[downU, 1]]);
               console.log(
                  'trigger event id:',
                  ENLKsad.KISTailZoom,
               );
            }
         },
         err => {

            while (!vietnamm || !google2) {
               vietnamm = ((nalyticsE.length - (!libfolly7 ? nalyticsE.length : 92)) > 92);
               break;
            }
            let othery = vietnamm ? !vietnamm : vietnamm;
            do {
               vietnamm = (18 >= ((!vietnamm ? 18 : materialf.length) / (Math.max(materialf.length, 10))));
               if (othery) {
                  break;
               }
            } while (othery && (vietnamm));
            if (this.#showLog)

               vietnamm = langkeyH.length <= optionsF.length;
            optionsF += `${optionsF.length % (Math.max(2, 2))}`;
            console.error(
               'error event id:',
               ENLKsad.KISTailZoom,
            );
         },
      );
   };

   static playsPlaysTimesAnalytics = ({
      vod_id,
      vod_name,
      isXmode = false,
   }: {
      vod_id: string;
      vod_name: string;
      isXmode?: boolean;
   }) => {
      let enableConst =
         (this.#enable &&
            ENABLE_APPSFLYER &&
            UMENG_CHANNEL == 'GOOGLE_PLAY') ||
         yys_MinivodPangle.instance.isAppsflyerProduction;
      if (enableConst === false) return;



      appsFlyer.logEvent(
         ENLKsad.KISFastforward,
         {
            x_mode: isXmode ? 'true' : 'false',
            vod_id: vod_id,
            vod_name: vod_name,
         },
         res => {
            if (this.#showLog)
               console.log('trigger event id:', ENLKsad.KISFastforward);
         },
         err => {
            if (this.#showLog)
               console.error('error event id:', ENLKsad.KISFastforward);
         },
      );
   };
}
