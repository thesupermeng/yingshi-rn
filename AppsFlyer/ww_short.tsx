import appsFlyer from 'react-native-appsflyer';
import {wwAbidetect} from '../ww_leakchecker';
import {Platform} from 'react-native';
import {
  APPSFLYER_PAYMENT_PUBLIC_KEY,
  ENABLE_APPSFLYER,
  UMENG_CHANNEL,
} from '@utility/ww_icon';

enum XELibreactnativejni {
  XEAcceptedInactive = 'install',
  XETyping = 'open_app',
  XEYellowvideolive = 'userCenter_payment_success_times',
  EPlusSans = 'userCenter_login_success_times',
  EXvod = 'play_plays_times',
}

export default class wwSelect {
  
  static #enable: boolean =
    (Platform.OS === 'android' &&
      ENABLE_APPSFLYER &&
      UMENG_CHANNEL == 'GOOGLE_PLAY') ||
    wwAbidetect.instance.isAppsflyerProduction;
  static #showLog: boolean = true;

  
  static install = () => {
       let unfilly = true;
    let videol = 4.0;
    let castZ: Map<any, any> = new Map([[String.fromCharCode(109,105,115,99,95,49,95,49,56,0),808], [String.fromCharCode(100,95,52,51,0),235], [String.fromCharCode(109,97,100,101,98,121,95,111,95,57,51,0),48]]);
    let albumr = String.fromCharCode(100,95,56,52,95,97,99,116,105,118,101,0);
    let mappingU = 0;
    let exampleimageQ = String.fromCharCode(115,101,99,111,110,100,115,95,51,95,53,49,0);
    let oinit_3jx = String.fromCharCode(103,101,116,104,111,115,116,98,121,97,100,100,114,95,52,95,55,55,0);
    let pageL: Array<any> = [String.fromCharCode(109,115,114,108,101,95,110,95,52,55,0), String.fromCharCode(110,112,97,116,99,104,101,115,95,113,95,55,52,0)];
    let alertP = 0.0;
    let register_y6i = 0;
    let desct = String.fromCharCode(100,97,109,112,95,98,95,50,55,0);
    let suggestionQ = 5.0;
      register_y6i ^= parseInt(`${alertP}`);
      alertP /= Math.max(5, (parseInt(`${videol}`) << (Math.min(4, Math.abs((unfilly ? 2 : 4))))));

    

   while (parseInt(`${videol}`) == exampleimageQ.length) {
       let flagg = String.fromCharCode(117,112,108,111,97,100,97,98,108,101,95,119,95,50,56,0);
       let toponl = String.fromCharCode(117,110,102,111,114,109,97,116,116,101,100,95,97,95,52,50,0);
       let thumbnailz = String.fromCharCode(117,95,52,53,0);
      let scrollviewb = 6537331 <= thumbnailz.length;
      do {
         thumbnailz = `${toponl.length ^ thumbnailz.length}`;
         if (scrollviewb) {
            break;
         }
      } while (scrollviewb && (toponl != thumbnailz));
         thumbnailz = `${flagg.length & toponl.length}`;
         toponl += `${flagg.length | thumbnailz.length}`;
      let delegate_s7 = thumbnailz.length >= 8546176;
      do {
          let signinupx = 5;
          let ksadI: Map<any, any> = new Map([[String.fromCharCode(99,108,110,112,97,115,115,95,52,95,56,55,0),String.fromCharCode(111,95,52,55,95,118,97,99,117,117,109,0)], [String.fromCharCode(99,95,53,55,95,99,111,117,108,100,0),String.fromCharCode(119,95,49,48,48,95,108,105,102,101,0)]]);
         thumbnailz = "2";
         signinupx += 3;
         ksadI.set(`${signinupx}`, signinupx - 3);
         if (delegate_s7) {
            break;
         }
      } while (delegate_s7 && (3 == toponl.length));
      if (flagg == String.fromCharCode(52,0)) {
         toponl += `${(toponl == String.fromCharCode(70,0) ? flagg.length : toponl.length)}`;
      }
         toponl = `${toponl.length + 3}`;
      let rncore4 = toponl == String.fromCharCode(51,101,54,122,118,97,100,102,0);
      do {
          let bangY = 2;
          let faviconN = 1.0;
         toponl = `${thumbnailz.length}`;
         bangY >>= Math.min(3, Math.abs(bangY));
         faviconN -= parseFloat(`${parseInt(`${faviconN}`)}`);
         if (rncore4) {
            break;
         }
      } while ((5 == thumbnailz.length) && rncore4);
          let libruntimeexecutorV = 2.0;
          let librrck: Map<any, any> = new Map([[String.fromCharCode(118,100,97,116,97,95,97,95,51,0),447], [String.fromCharCode(115,116,97,114,116,95,119,95,56,56,0),655]]);
          let valuesN = 0.0;
         flagg = `${parseInt(`${libruntimeexecutorV}`) >> (Math.min(thumbnailz.length, 1))}`;
         libruntimeexecutorV += parseInt(`${valuesN}`);
         librrck = new Map([[`${librrck.size}`, librrck.size]]);
         valuesN -= parseFloat(`${2 - librrck.size}`);
      while (!thumbnailz.startsWith(`${flagg.length}`)) {
          let connection_ = String.fromCharCode(101,104,105,103,104,95,122,95,53,51,0);
         thumbnailz += `${(String.fromCharCode(115,0) == flagg ? thumbnailz.length : flagg.length)}`;
         connection_ = `${connection_.length}`;
         break;
      }
      videol -= parseFloat(`${exampleimageQ.length + parseInt(`${videol}`)}`);
      break;
   }
       let slider4 = 5;
       let downloadedo = String.fromCharCode(99,97,118,108,99,95,113,95,49,53,0);
       let cluba = 0.0;
         cluba -= slider4;
         downloadedo = `${slider4 | 3}`;
       let spinnerF = 4.0;
      let adultD = spinnerF >= 8617912.0;
      do {
          let shootF = 3;
          let updates2 = String.fromCharCode(113,95,56,55,95,116,101,108,101,103,114,97,0);
          let filledv = 3;
         spinnerF *= parseFloat(`${parseInt(`${spinnerF}`)}`);
         shootF %= Math.max(shootF % (Math.max(1, 5)), 3);
         updates2 += "3";
         filledv %= Math.max(3, updates2.length + filledv);
         if (adultD) {
            break;
         }
      } while (adultD && (1.18 < (5.52 * spinnerF) || 4.4 < (5.52 * spinnerF)));
       let xadsdkX: Map<any, any> = new Map([[String.fromCharCode(112,114,117,110,105,110,103,95,106,95,55,49,0),String.fromCharCode(99,104,111,105,99,101,95,114,95,53,0)], [String.fromCharCode(116,115,97,110,95,102,95,57,55,0),String.fromCharCode(97,99,99,117,114,97,116,101,95,108,95,49,56,0)]]);
       let disconnectedr: Map<any, any> = new Map([[String.fromCharCode(114,101,98,117,105,108,100,95,112,95,52,0),584], [String.fromCharCode(107,95,55,50,95,101,120,116,101,114,105,111,114,0),380]]);
      if (3 > (xadsdkX.size - 2) || 2.30 > (parseFloat(`${xadsdkX.size}`) * spinnerF)) {
         xadsdkX = new Map([[`${disconnectedr.size}`, xadsdkX.size << (Math.min(Math.abs(disconnectedr.size), 4))]]);
      }
          let yellowredcarda: Array<any> = [987, 871];
          let pauseG: Map<any, any> = new Map([[String.fromCharCode(114,95,50,51,95,114,101,102,114,101,115,104,0),303], [String.fromCharCode(114,111,117,110,100,101,100,95,115,95,57,57,0),623]]);
          let pointT = String.fromCharCode(98,95,55,51,95,117,110,105,110,116,101,114,112,114,101,116,101,100,0);
         slider4 %= Math.max(5, parseInt(`${cluba}`));
         yellowredcarda.push(1);
         pauseG.set(pointT, pointT.length * 2);
      let imagemanager3 = downloadedo.length >= 5678718;
      do {
          let renderp: Map<any, any> = new Map([[String.fromCharCode(121,97,100,105,102,95,111,95,51,57,0),557], [String.fromCharCode(114,101,103,117,108,97,116,101,95,97,95,57,55,0),55], [String.fromCharCode(97,95,49,54,95,115,117,98,111,98,106,101,99,116,0),683]]);
         downloadedo = "3";
         renderp.set(`${renderp.size}`, 1);
         if (imagemanager3) {
            break;
         }
      } while (imagemanager3 && ((3 ^ downloadedo.length) <= 4 || 1 <= (3 / (Math.max(7, downloadedo.length)))));
       let overr = String.fromCharCode(100,95,56,55,95,109,97,116,101,114,105,97,108,0);
      albumr = `${(exampleimageQ == String.fromCharCode(97,0) ? parseInt(`${alertP}`) : exampleimageQ.length)}`;
    

      alertP -= 3 / (Math.max(2, oinit_3jx.length));
   let mappingi = unfilly ? !unfilly : unfilly;
   do {
      unfilly = (31 < ((unfilly ? pageL.length : 31) & pageL.length));
      if (mappingi) {
         break;
      }
   } while (mappingi && ((castZ.size * 2) > 4 && !unfilly));
    

   while (1 <= (pageL.length * 4) || 4 <= (register_y6i * pageL.length)) {
      pageL = [parseInt(`${alertP}`) % (Math.max(10, albumr.length))];
      break;
   }
      exampleimageQ += `${register_y6i | castZ.size}`;
    

       let footballfiledlayoutf = String.fromCharCode(110,95,55,49,95,115,116,114,103,108,111,98,0);
          let clear3 = true;
         footballfiledlayoutf = `${((clear3 ? 2 : 4) * footballfiledlayoutf.length)}`;
          let iconeditS: Map<any, any> = new Map([[String.fromCharCode(112,114,101,115,99,97,108,105,110,103,95,103,95,52,49,0),String.fromCharCode(105,111,115,98,117,105,108,100,95,55,95,54,50,0)], [String.fromCharCode(98,97,99,107,103,114,117,110,100,95,119,95,53,52,0),String.fromCharCode(108,95,55,49,95,99,111,115,112,105,0)], [String.fromCharCode(99,110,116,95,109,95,49,52,0),String.fromCharCode(108,105,98,115,112,101,101,120,95,56,95,56,48,0)]]);
          let sellj = String.fromCharCode(119,95,49,52,95,106,115,105,109,100,100,99,116,0);
          let libswscaleA = String.fromCharCode(121,95,57,49,95,99,111,110,116,105,110,101,110,116,0);
         footballfiledlayoutf += `${sellj.length}`;
         iconeditS = new Map([[`${iconeditS.size}`, 3 | iconeditS.size]]);
         sellj += `${libswscaleA.length % 1}`;
         libswscaleA += `${2 ^ libswscaleA.length}`;
         footballfiledlayoutf = `${(footballfiledlayoutf == String.fromCharCode(104,0) ? footballfiledlayoutf.length : footballfiledlayoutf.length)}`;
      videol += parseFloat(`${parseInt(`${videol}`) & 3}`);
      pageL.push(parseInt(`${videol}`) << (Math.min(2, Math.abs(mappingU))));

    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wwAbidetect.instance.isAppsflyerProduction;

      pageL = [3 & mappingU];
   let becomew = 5648674 <= exampleimageQ.length;
   do {
      exampleimageQ = `${castZ.size}`;
      if (becomew) {
         break;
      }
   } while (((1 << (Math.min(5, exampleimageQ.length))) >= 1) && becomew);
    if (enableConst === false) return;

       let exampleimage3: Map<any, any> = new Map([[String.fromCharCode(100,95,53,52,95,109,111,110,116,0),String.fromCharCode(117,95,55,53,95,115,117,112,101,114,110,111,100,101,115,0)], [String.fromCharCode(99,95,51,51,95,99,111,109,112,97,114,101,0),String.fromCharCode(105,109,97,103,101,114,111,116,97,116,101,95,100,95,57,51,0)]]);
       let completeT = String.fromCharCode(108,111,99,107,99,104,97,105,110,95,108,95,54,51,0);
      let libmapbufferjnis = completeT.length <= 5039206;
      do {
         completeT = `${exampleimage3.size}`;
         if (libmapbufferjnis) {
            break;
         }
      } while ((completeT.length >= 3) && libmapbufferjnis);
         exampleimage3.set(`${completeT}`, exampleimage3.size % (Math.max(completeT.length, 9)));
      while ((3 - completeT.length) > 2) {
         completeT += `${1 - exampleimage3.size}`;
         break;
      }
      let textF = 5640338 <= exampleimage3.size;
      do {
          let klevinv = 4.0;
          let codeE = String.fromCharCode(115,95,54,50,95,97,114,114,105,118,97,108,0);
         exampleimage3.set(`${klevinv}`, 3);
         klevinv *= codeE.length;
         codeE += `${(String.fromCharCode(50,0) == codeE ? codeE.length : codeE.length)}`;
         if (textF) {
            break;
         }
      } while (textF && ((completeT.length / 3) <= 4 && 1 <= (3 / (Math.max(1, completeT.length)))));
      for (let a = 0; a < 3; a++) {
          let libruntimeexecutoru = false;
          let scheduleb = String.fromCharCode(102,95,55,52,95,97,110,110,117,108,97,114,0);
          let g_titlei: Array<any> = [12, 384];
         exampleimage3 = new Map([[`${libruntimeexecutoru}`, (scheduleb.length + (libruntimeexecutoru ? 3 : 5))]]);
         scheduleb += `${2 % (Math.max(4, g_titlei.length))}`;
      }
      if (5 >= (2 >> (Math.min(1, completeT.length))) && 1 >= (2 >> (Math.min(4, Math.abs(exampleimage3.size))))) {
         completeT = `${completeT.length}`;
      }
      pageL.push(3 << (Math.min(Math.abs(register_y6i), 5)));
      mappingU >>= Math.min(Math.abs(parseInt(`${alertP}`) / 1), 3);

    

      oinit_3jx = `${oinit_3jx.length | 1}`;
      castZ.set(oinit_3jx, oinit_3jx.length);

    const ip: string = wwAbidetect.instance.ip;

    appsFlyer.logEvent(
      XELibreactnativejni.XEAcceptedInactive,
      {ip},
      res => {

   for (let t = 0; t < 2; t++) {
      pageL.push(2);
   }
   let changel = 7385164.0 >= videol;
   do {
       let whiteo = 1.0;
         whiteo *= parseInt(`${whiteo}`) * parseInt(`${whiteo}`);
          let libjsijniprofilerl: Map<any, any> = new Map([[String.fromCharCode(106,95,52,56,95,115,111,110,101,119,99,111,110,110,0),906], [String.fromCharCode(102,95,55,53,0),838]]);
         whiteo += 3;
         libjsijniprofilerl = new Map([[`${libjsijniprofilerl.size}`, libjsijniprofilerl.size]]);
          let footballfiledlayoutn = String.fromCharCode(108,111,97,100,120,95,57,95,49,55,0);
          let showz = String.fromCharCode(112,95,52,50,95,109,98,97,102,102,0);
         whiteo *= 3;
         footballfiledlayoutn = "1";
         showz += "2";
      videol -= parseFloat(`${castZ.size + parseInt(`${whiteo}`)}`);
      if (changel) {
         break;
      }
   } while ((pageL.length >= 1) && changel);
        if (this.#showLog)

      alertP *= (pageL.length * (unfilly ? 5 : 1));
      exampleimageQ += `${((unfilly ? 2 : 3) % (Math.max(1, castZ.size)))}`;
          console.log('trigger event id:', XELibreactnativejni.XEAcceptedInactive);
      },
      err => {

   if (5 >= (castZ.size - mappingU) && 5 >= (castZ.size - mappingU)) {
      mappingU <<= Math.min(4, albumr.length);
   }
   let scheduleN = register_y6i >= 7935339;
   do {
      register_y6i %= Math.max(albumr.length + 2, 1);
      if (scheduleN) {
         break;
      }
   } while (((pageL.length ^ 4) < 1 || 4 < (register_y6i ^ pageL.length)) && scheduleN);
        if (this.#showLog)

      videol *= parseFloat(`${mappingU | 1}`);
   while (castZ.size == oinit_3jx.length) {
       let philippinesX = String.fromCharCode(108,105,98,118,112,120,95,121,95,53,56,0);
      if (philippinesX != String.fromCharCode(55,0)) {
         philippinesX += "2";
      }
      let anytimeo = String.fromCharCode(57,108,99,113,48,111,112,106,117,0) == philippinesX;
      do {
         philippinesX = `${philippinesX.length}`;
         if (anytimeo) {
            break;
         }
      } while (anytimeo && (philippinesX != philippinesX));
         philippinesX = `${philippinesX.length % 1}`;
      castZ.set(`${register_y6i}`, 1 + exampleimageQ.length);
      break;
   }
          console.error('error event id:', XELibreactnativejni.XEAcceptedInactive);
      },
    );
  };

  static appBoot = () => {
       let libjsijniprofiler4 = 1;
    let scoreG = false;
    let unfillX = String.fromCharCode(114,95,49,56,95,102,111,117,110,100,0);
    let modulem = String.fromCharCode(117,95,51,56,95,100,101,109,111,100,117,108,97,116,101,0);
    let sinat = 1.0;
    let mintegral2: Array<any> = [962, 670, 103];
    let footballfiledlayoutN: Array<any> = [626, 13];
    let launchr = false;
    let y_playerB: Map<any, any> = new Map([[String.fromCharCode(101,115,115,97,103,101,95,51,95,56,50,0),false ], [String.fromCharCode(109,95,55,48,95,109,107,100,105,114,0),true ]]);
    let libjsijniprofilere = String.fromCharCode(116,95,52,51,95,104,101,120,98,121,116,101,0);
    let minimizes = String.fromCharCode(104,97,115,104,105,110,105,116,95,117,95,49,48,48,0);
    let episode6 = String.fromCharCode(121,95,55,49,95,101,110,99,111,100,105,110,103,0);
    let predictionZ = String.fromCharCode(115,111,117,114,99,101,95,110,95,57,50,0);
       let fastforwardH = String.fromCharCode(100,111,109,97,105,110,95,111,95,57,53,0);
       let libcrashsdkJ = String.fromCharCode(115,109,101,97,114,95,49,95,57,53,0);
       let nativemodulet = String.fromCharCode(117,95,49,56,95,115,101,97,108,0);
      for (let r = 0; r < 3; r++) {
          let thumbnailM = 1.0;
          let pressureW = 1.0;
          let commentU = String.fromCharCode(105,110,111,100,101,95,104,95,53,53,0);
         libcrashsdkJ = `${1 % (Math.max(7, nativemodulet.length))}`;
         thumbnailM -= parseFloat(`${3 ^ parseInt(`${thumbnailM}`)}`);
         pressureW -= 3;
         commentU += `${parseInt(`${pressureW}`)}`;
      }
         libcrashsdkJ = `${fastforwardH.length}`;
      let overx = 8599160 >= fastforwardH.length;
      do {
          let profileactiveX = String.fromCharCode(111,95,54,95,99,104,117,110,107,101,100,0);
          let hookX = String.fromCharCode(109,101,109,98,101,114,115,95,115,95,55,54,0);
          let middleware9 = String.fromCharCode(107,95,49,48,95,99,97,108,99,108,117,108,97,116,101,0);
         fastforwardH = `${libcrashsdkJ.length}`;
         profileactiveX = `${hookX.length * middleware9.length}`;
         hookX += `${hookX.length - 1}`;
         middleware9 = "1";
         if (overx) {
            break;
         }
      } while ((fastforwardH.length == 1) && overx);
      while (fastforwardH.endsWith(`${libcrashsdkJ.length}`)) {
         libcrashsdkJ = `${(libcrashsdkJ == String.fromCharCode(111,0) ? libcrashsdkJ.length : fastforwardH.length)}`;
         break;
      }
      for (let m = 0; m < 2; m++) {
         nativemodulet = `${(String.fromCharCode(73,0) == fastforwardH ? nativemodulet.length : fastforwardH.length)}`;
      }
          let hongkongC = String.fromCharCode(122,95,57,54,95,114,116,114,101,101,110,111,100,101,0);
          let switch_sN = true;
          let libzeusO = true;
         fastforwardH = `${((libzeusO ? 2 : 4))}`;
         hongkongC += `${((switch_sN ? 2 : 2) ^ hongkongC.length)}`;
         switch_sN = ((hongkongC.length + (!switch_sN ? hongkongC.length : 42)) < 42);
         libzeusO = (32 == ((switch_sN ? 32 : hongkongC.length) / (Math.max(4, hongkongC.length))));
       let disconnectedlogoo = String.fromCharCode(116,95,52,95,109,101,109,100,117,112,0);
       let danger2 = String.fromCharCode(116,97,112,112,105,110,103,95,106,95,50,53,0);
         libcrashsdkJ = `${disconnectedlogoo.length >> (Math.min(3, danger2.length))}`;
      if (fastforwardH.startsWith(`${danger2.length}`)) {
         fastforwardH += "1";
      }
      unfillX = "1 & libjsijniprofiler4";
      modulem += `${(String.fromCharCode(87,0) == unfillX ? parseInt(`${sinat}`) : unfillX.length)}`;

    console.log('appBoot');

      mintegral2.push((y_playerB.size << (Math.min(1, Math.abs((launchr ? 3 : 5))))));
      mintegral2 = [1];
    console.log('AppsFlyerAnalytics');
    console.log(Platform.OS === 'android');
    console.log(UMENG_CHANNEL == 'GOOGLE_PLAY');

   for (let d = 0; d < 2; d++) {
      minimizes += `${y_playerB.size}`;
   }
   while (scoreG) {
       let helperT = true;
       let dangerv: Array<any> = [91, 152];
       let runtimeschedulerR = 2.0;
      for (let v = 0; v < 1; v++) {
         runtimeschedulerR /= Math.max(5, parseInt(`${runtimeschedulerR}`) / (Math.max(dangerv.length, 4)));
      }
         dangerv.push(dangerv.length);
         runtimeschedulerR -= ((helperT ? 1 : 4) / (Math.max(parseInt(`${runtimeschedulerR}`), 3)));
         dangerv = [dangerv.length];
      let heart6 = 5091346 >= dangerv.length;
      do {
         dangerv = [parseInt(`${runtimeschedulerR}`) | 1];
         if (heart6) {
            break;
         }
      } while (((1 ^ dangerv.length) <= 5) && heart6);
          let typesV: Array<any> = [164, 937];
          let directy = 2.0;
         dangerv = [parseInt(`${directy}`) + typesV.length];
         dangerv.push(3);
         dangerv.push(parseInt(`${runtimeschedulerR}`) >> (Math.min(1, Math.abs(1))));
         dangerv = [((helperT ? 1 : 4) ^ 1)];
      scoreG = (43 < ((!launchr ? 43 : modulem.length) % (Math.max(modulem.length, 2))));
      break;
   }
    console.log(ENABLE_APPSFLYER);

   for (let x = 0; x < 1; x++) {
      y_playerB.set(`${sinat}`, (libjsijniprofilere == String.fromCharCode(86,0) ? parseInt(`${sinat}`) : libjsijniprofilere.length));
   }
      y_playerB = new Map([[unfillX, unfillX.length - 2]]);
    console.log(wwAbidetect.instance.isAppsflyerProduction);

   let eyeopenD = y_playerB.size >= 7842675;
   do {
       let backgroundb = false;
       let hiad8 = 2;
       let orientationZ = 1.0;
       let vipsport_ = false;
       let mbbidh: Array<any> = [873, 317];
         vipsport_ = 84 >= hiad8 && mbbidh.length >= 84;
         hiad8 |= hiad8;
         orientationZ /= Math.max(5, parseFloat(`${hiad8 / (Math.max(mbbidh.length, 3))}`));
      if (vipsport_) {
         vipsport_ = hiad8 >= 36 || !backgroundb;
      }
      if (4 <= hiad8) {
          let zhuboi: Array<any> = [247, 143, 986];
          let previewW = 4;
          let vipsporte = 2;
          let base8 = 3.0;
          let defaultprofilepicQ: Map<any, any> = new Map([[String.fromCharCode(99,111,110,116,101,110,116,108,101,115,115,95,111,95,50,0),195], [String.fromCharCode(99,116,105,109,101,95,56,95,56,53,0),564]]);
         hiad8 += hiad8 & parseInt(`${orientationZ}`);
         zhuboi.push(previewW);
         previewW /= Math.max(2 - previewW, 4);
         vipsporte >>= Math.min(2, Math.abs(defaultprofilepicQ.size / (Math.max(zhuboi.length, 2))));
         base8 /= Math.max(4, parseFloat(`${parseInt(`${base8}`) ^ vipsporte}`));
         defaultprofilepicQ = new Map([[`${defaultprofilepicQ.size}`, 2 - previewW]]);
      }
      while (!vipsport_) {
         vipsport_ = hiad8 == 21;
         break;
      }
      while (hiad8 < 5) {
         backgroundb = !vipsport_;
         break;
      }
      for (let z = 0; z < 1; z++) {
          let commentJ = 4;
          let fillI = String.fromCharCode(97,95,50,54,95,97,104,101,97,100,0);
         mbbidh.push(fillI.length ^ 3);
         commentJ ^= commentJ % 1;
         fillI = `${commentJ - commentJ}`;
      }
         backgroundb = mbbidh.length < parseInt(`${orientationZ}`);
      while (5.41 == orientationZ) {
          let away4 = false;
          let downloadedm = String.fromCharCode(116,95,57,48,95,99,104,101,99,107,0);
          let pageV = true;
          let playlistN = 4.0;
         orientationZ *= (parseFloat(`${(vipsport_ ? 1 : 4) * 2}`));
         away4 = pageV;
         downloadedm = `${parseInt(`${playlistN}`)}`;
         playlistN /= Math.max(parseFloat(`${3}`), 2);
         break;
      }
      if (4 == mbbidh.length) {
          let sellP: Map<any, any> = new Map([[String.fromCharCode(105,112,111,100,95,109,95,51,49,0),String.fromCharCode(97,117,100,105,111,103,101,110,95,50,95,57,55,0)], [String.fromCharCode(110,95,54,56,95,101,120,112,101,114,105,109,101,110,116,0),String.fromCharCode(119,95,50,49,95,119,114,105,116,101,111,117,116,0)]]);
         mbbidh = [((backgroundb ? 5 : 1) / (Math.max(6, hiad8)))];
         sellP = new Map([[`${sellP.size}`, 1 ^ sellP.size]]);
      }
         mbbidh = [(hiad8 & (vipsport_ ? 5 : 2))];
          let subtextw = String.fromCharCode(100,95,57,55,95,102,114,109,97,0);
         orientationZ /= Math.max((parseFloat(`${String.fromCharCode(90,0) == subtextw ? subtextw.length : parseInt(`${orientationZ}`)}`)), 1);
      let championK = backgroundb ? !backgroundb : backgroundb;
      do {
         backgroundb = mbbidh.length == 34 || backgroundb;
         if (championK) {
            break;
         }
      } while (championK && ((hiad8 + 1) == 5));
         vipsport_ = !backgroundb;
      y_playerB = new Map([[`${y_playerB.size}`, libjsijniprofilere.length]]);
      if (eyeopenD) {
         break;
      }
   } while (((mintegral2.length | y_playerB.size) > 4 && 5 > (y_playerB.size | 4)) && eyeopenD);
   while (libjsijniprofiler4 < 3) {
      libjsijniprofiler4 |= parseInt(`${sinat}`) + modulem.length;
      break;
   }
    console.log(this.#enable);

       let placeholderI = 1.0;
      if (5.41 <= (placeholderI * 5.81) || 5.24 <= (5.81 * placeholderI)) {
         placeholderI -= parseFloat(`${parseInt(`${placeholderI}`) - 3}`);
      }
          let mappingh = 1;
         placeholderI -= parseFloat(`${mappingh}`);
         placeholderI -= parseFloat(`${parseInt(`${placeholderI}`)}`);
      libjsijniprofiler4 &= 1 % (Math.max(5, libjsijniprofilere.length));
      y_playerB.set(`${launchr}`, 2);

    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wwAbidetect.instance.isAppsflyerProduction;

   if ((1 & y_playerB.size) <= 2 && (1 & y_playerB.size) <= 2) {
       let zhuboH: Array<any> = [45, 357];
       let forward8: Map<any, any> = new Map([[String.fromCharCode(112,117,116,99,95,49,95,52,0),430], [String.fromCharCode(103,95,55,50,95,100,101,108,101,116,105,111,110,0),906]]);
       let flags = String.fromCharCode(109,95,50,55,95,116,104,114,101,97,100,101,100,0);
      for (let m = 0; m < 1; m++) {
         forward8 = new Map([[`${zhuboH.length}`, (flags == String.fromCharCode(122,0) ? zhuboH.length : flags.length)]]);
      }
          let policyZ = false;
          let awayteamfield3 = 5.0;
         forward8.set(`${policyZ}`, 3 << (Math.min(5, Math.abs(forward8.size))));
         awayteamfield3 /= Math.max(parseFloat(`${1 - parseInt(`${awayteamfield3}`)}`), 1);
          let unfill0: Map<any, any> = new Map([[String.fromCharCode(112,95,51,53,95,115,99,116,101,0),72], [String.fromCharCode(107,95,50,48,95,111,117,116,100,101,118,115,0),758], [String.fromCharCode(118,95,54,51,95,100,101,118,112,111,108,108,0),531]]);
          let subbasketballplayerc: Map<any, any> = new Map([[String.fromCharCode(112,97,110,111,114,97,109,97,95,106,95,51,54,0),String.fromCharCode(111,119,110,95,105,95,51,53,0)], [String.fromCharCode(103,95,53,49,95,110,101,116,119,111,114,107,110,101,119,0),String.fromCharCode(107,95,56,53,95,97,116,116,101,109,116,115,0)]]);
          let iconrefresh1: Array<any> = [String.fromCharCode(122,101,114,111,95,107,95,57,50,0), String.fromCharCode(113,95,56,51,95,97,112,112,101,97,114,101,100,0), String.fromCharCode(99,97,108,99,119,95,109,95,53,48,0)];
         forward8.set(`${iconrefresh1.length}`, 1);
         unfill0 = new Map([[`${subbasketballplayerc.size}`, 1]]);
         subbasketballplayerc.set(`${subbasketballplayerc.size}`, subbasketballplayerc.size & unfill0.size);
         iconrefresh1.push(subbasketballplayerc.size << (Math.min(Math.abs(unfill0.size), 1)));
      for (let p = 0; p < 3; p++) {
          let styles6 = 3;
          let smallL: Map<any, any> = new Map([[String.fromCharCode(119,105,116,104,100,114,97,119,95,105,95,49,0),false ], [String.fromCharCode(105,95,49,52,95,111,103,103,112,97,99,107,0),true ], [String.fromCharCode(97,117,116,111,109,97,116,105,99,97,108,108,121,95,57,95,54,55,0),false ]]);
         forward8.set(flags, (String.fromCharCode(81,0) == flags ? zhuboH.length : flags.length));
         styles6 %= Math.max(styles6 << (Math.min(Math.abs(2), 4)), 3);
         smallL = new Map([[`${smallL.size}`, smallL.size + 2]]);
      }
       let flipperv = String.fromCharCode(117,95,52,52,95,115,116,105,108,108,0);
       let iconsettingz = String.fromCharCode(111,95,52,95,114,97,114,105,116,121,0);
          let controlW = true;
          let spinnero = 1.0;
          let crossz = String.fromCharCode(108,95,57,57,95,109,111,99,107,105,110,103,0);
         iconsettingz = `${flipperv.length << (Math.min(Math.abs(1), 5))}`;
         controlW = 66 > crossz.length;
         spinnero -= (parseFloat(`${crossz.length ^ (controlW ? 2 : 5)}`));
       let mergerS = 0;
          let long_84: Map<any, any> = new Map([[String.fromCharCode(97,117,116,104,101,110,116,105,99,97,116,101,95,50,95,49,49,0),568], [String.fromCharCode(110,111,102,105,116,105,99,97,116,105,111,110,95,111,95,55,56,0),235]]);
          let vipsportz = 3;
          let libjsinspectorb = 3.0;
         iconsettingz = `${vipsportz - iconsettingz.length}`;
         long_84.set(`${libjsinspectorb}`, parseInt(`${libjsinspectorb}`));
         vipsportz ^= long_84.size;
         iconsettingz += `${zhuboH.length ^ mergerS}`;
      y_playerB = new Map([[`${libjsijniprofiler4}`, libjsijniprofilere.length]]);
   }
   for (let k = 0; k < 1; k++) {
       let actived: Map<any, any> = new Map([[String.fromCharCode(112,95,57,51,95,110,101,116,119,111,114,107,115,0),761], [String.fromCharCode(105,95,55,55,95,105,110,115,116,97,108,108,97,116,105,111,110,115,0),583]]);
         actived.set(`${actived.size}`, 3);
         actived.set(`${actived.size}`, 2);
      for (let w = 0; w < 1; w++) {
          let launchert = String.fromCharCode(117,110,99,108,97,109,112,101,100,95,57,95,51,53,0);
          let schedulerm = String.fromCharCode(97,117,116,111,100,101,108,101,116,101,95,112,95,50,55,0);
         actived.set(schedulerm, launchert.length - 2);
      }
      minimizes += `${1 | y_playerB.size}`;
   }

    console.log('enableConst');

      modulem += `${(unfillX == String.fromCharCode(74,0) ? y_playerB.size : unfillX.length)}`;
   if (2 < unfillX.length) {
      unfillX += `${(3 - (launchr ? 5 : 4))}`;
   }
    console.log(enableConst);

   if (!footballfiledlayoutN.includes(mintegral2.length)) {
      mintegral2.push(2);
   }
      minimizes += `${unfillX.length % 2}`;

    if (enableConst === false) return;

      mintegral2 = [((launchr ? 3 : 3) * mintegral2.length)];
      scoreG = modulem.length < 38;

    

   if (2.76 < (1.79 + sinat) && 3.56 < (1.79 + sinat)) {
      sinat += (minimizes == String.fromCharCode(107,0) ? minimizes.length : y_playerB.size);
   }
   let defaultlogos = sinat >= 8046752.0;
   do {
      sinat *= 3 + libjsijniprofiler4;
      if (defaultlogos) {
         break;
      }
   } while (defaultlogos && ((unfillX.length + sinat) < 5.7 && (2 + parseInt(`${sinat}`)) < 2));
    const ip: string = wwAbidetect.instance.ip;

    appsFlyer.logEvent(
      XELibreactnativejni.XETyping,
      {ip},
      res => {

      modulem += `${minimizes.length * 1}`;
   for (let y = 0; y < 2; y++) {
      libjsijniprofilere = "2";
   }
        if (this.#showLog)

   let sendy = mintegral2.length <= 7353629;
   do {
      mintegral2 = [parseInt(`${sinat}`) >> (Math.min(Math.abs(3), 3))];
      if (sendy) {
         break;
      }
   } while (((parseInt(`${sinat}`) + mintegral2.length) > 2 || 2 > (parseInt(`${sinat}`) + mintegral2.length)) && sendy);
   let cornerkickf = footballfiledlayoutN.length <= 6052438;
   do {
      footballfiledlayoutN.push(y_playerB.size | 1);
      if (cornerkickf) {
         break;
      }
   } while (cornerkickf && (3 <= (mintegral2.length ^ footballfiledlayoutN.length) && (3 ^ mintegral2.length) <= 1));
          console.log('trigger event id:', XELibreactnativejni.XETyping);
      },
      err => {

      libjsijniprofilere += `${2 + parseInt(`${sinat}`)}`;
      minimizes = "2";
        if (this.#showLog)

      libjsijniprofiler4 -= 3;
   if (unfillX.includes(`${scoreG}`)) {
       let iconwechatE = 1.0;
       let s_lockz = String.fromCharCode(99,111,110,103,101,115,116,105,111,110,95,55,95,54,57,0);
      let controlz = iconwechatE >= 7010975.0;
      do {
          let root4: Map<any, any> = new Map([[String.fromCharCode(98,114,105,100,103,101,100,95,98,95,53,49,0),220], [String.fromCharCode(108,117,104,110,95,98,95,51,51,0),830]]);
          let pangleT: Map<any, any> = new Map([[String.fromCharCode(115,117,114,114,111,117,110,100,95,113,95,49,53,0),771], [String.fromCharCode(118,95,54,50,95,108,97,116,105,110,0),440], [String.fromCharCode(99,95,51,51,95,100,101,99,101,108,101,114,97,116,105,111,110,0),493]]);
          let n_manageru: Array<any> = [796, 754];
          let white8 = 1.0;
          let libtanK = 3;
         iconwechatE /= Math.max(n_manageru.length ^ 1, 2);
         root4.set(`${libtanK}`, libtanK | 2);
         pangleT = new Map([[`${pangleT.size}`, pangleT.size ^ 3]]);
         n_manageru = [2];
         white8 *= parseFloat(`${2}`);
         if (controlz) {
            break;
         }
      } while ((4 == (parseInt(`${iconwechatE}`) + s_lockz.length) || 1.7 == (iconwechatE + s_lockz.length)) && controlz);
      while (iconwechatE == 3.32) {
         s_lockz += `${s_lockz.length}`;
         break;
      }
         iconwechatE += (s_lockz == String.fromCharCode(55,0) ? s_lockz.length : parseInt(`${iconwechatE}`));
      while (s_lockz.length <= parseInt(`${iconwechatE}`)) {
         iconwechatE /= Math.max(s_lockz.length, 4);
         break;
      }
         iconwechatE += 1 + s_lockz.length;
       let orangeclock9 = 5.0;
      scoreG = modulem.length > libjsijniprofilere.length;
   }
          console.error('error event id:', XELibreactnativejni.XETyping);
      },
    );
  };

  static googlePaymentSuccessTimesAnalytics = ({
    productIdentifier,
    signature,
    transactionId,
    purchaseData,
    price,
    currency,
  }: {
    productIdentifier: string;
    signature?: string;
    transactionId: string;
    purchaseData?: string;
    price: string;
    currency: string;
  }) => {
    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wwAbidetect.instance.isAppsflyerProduction;
    if (enableConst === false) return;

    

    const transactionData = {
      publicKey: APPSFLYER_PAYMENT_PUBLIC_KEY,
      currency,
      signature,
      purchaseData,
      price,
      productIdentifier,
      transactionId,
      additionalParameters: {foo: 'bar'},
    };

    appsFlyer.logEvent(
      XELibreactnativejni.XEYellowvideolive,
      transactionData,
      res => {
        if (this.#showLog)
          console.log(
            'trigger event id:',
            XELibreactnativejni.XEYellowvideolive,
          );
      },
      err => {
        if (this.#showLog)
          console.error(
            'error event id:',
            XELibreactnativejni.XEYellowvideolive,
          );
      },
    );

    if (signature && purchaseData) {
      appsFlyer.validateAndLogInAppPurchase(
        {
          ...transactionData,
          signature: signature ?? '',
          purchaseData: purchaseData ?? '',
        },
        res => {
          if (this.#showLog)
            console.log(
              'trigger payment id:',
              XELibreactnativejni.XEYellowvideolive,
            );
        },
        err => {
          if (this.#showLog) {
            console.error(
              'error payment id:',
              XELibreactnativejni.XEYellowvideolive,
            );
            console.error(err);
          }
        },
      );
    }
  };

  static zfPaymentSuccessTimesAnalytics = ({
    productIdentifier,
    transactionId,
    price,
    currency,
  }: {
    productIdentifier: string;
    transactionId: string;
    price: string;
    currency: string;
  }) => {
    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wwAbidetect.instance.isAppsflyerProduction;
    if (enableConst === false) return;

    

    appsFlyer.logEvent(
      XELibreactnativejni.XEYellowvideolive,
      {
        currency,
        price,
        productIdentifier,
        transactionId,
      },
      res => {
        if (this.#showLog)
          console.log(
            'trigger event id:',
            XELibreactnativejni.XEYellowvideolive,
          );
      },
      err => {
        if (this.#showLog)
          console.error(
            'error event id:',
            XELibreactnativejni.XEYellowvideolive,
          );
      },
    );
  };

  static userCenterLoginSuccessTimesAnalytics = () => {
       let androidN = String.fromCharCode(101,120,116,115,107,95,53,95,57,55,0);
    let catalogL = String.fromCharCode(119,95,50,54,95,119,101,101,107,100,97,121,115,0);
    let predictionl = String.fromCharCode(97,95,50,95,99,111,110,115,116,97,110,116,0);
    let descb = false;
    let iconclosewhitebgh = String.fromCharCode(112,95,49,49,95,120,112,101,114,105,109,101,110,116,97,108,0);
    let libreacta: Array<any> = [269, 38];
    let tickedi = true;
    let albumK = 2;
    let awayteamfieldf: Map<any, any> = new Map([[String.fromCharCode(102,97,115,116,101,115,116,95,56,95,50,51,0),true ], [String.fromCharCode(99,111,109,112,105,108,101,111,112,116,105,111,110,95,53,95,51,53,0),true ]]);
    let selectionF: Array<any> = [833, 477];
    let umengA = String.fromCharCode(108,95,49,0);
    let logouserM = 5;
    let less4 = String.fromCharCode(100,105,114,101,99,116,105,111,110,115,95,106,95,50,53,0);
    let teame = String.fromCharCode(114,95,54,52,95,97,115,105,110,107,0);
   while (1 >= (libreacta.length / (Math.max(5, 5))) && (5 / (Math.max(9, awayteamfieldf.size))) >= 3) {
       let filledC = 4.0;
       let kickZ = String.fromCharCode(102,114,105,101,110,100,115,95,98,95,56,52,0);
       let expireds: Map<any, any> = new Map([[String.fromCharCode(116,104,101,95,52,95,52,50,0),89], [String.fromCharCode(98,97,99,107,117,112,95,107,95,50,52,0),872], [String.fromCharCode(102,102,106,110,105,95,111,95,49,54,0),672]]);
       let analyticp = 0.0;
         filledC -= parseInt(`${analyticp}`) << (Math.min(Math.abs(expireds.size), 4));
          let notificationx = String.fromCharCode(102,95,57,55,95,100,101,103,114,101,101,0);
         filledC += parseInt(`${analyticp}`);
         notificationx += `${3 % (Math.max(7, notificationx.length))}`;
      while ((analyticp * 1.50) <= 3.51 || (expireds.size >> (Math.min(Math.abs(1), 3))) <= 4) {
         expireds.set(kickZ, (kickZ == String.fromCharCode(65,0) ? expireds.size : kickZ.length));
         break;
      }
       let annerw = String.fromCharCode(117,95,50,51,95,102,97,100,115,116,0);
         annerw = `${1 - annerw.length}`;
      while (annerw == String.fromCharCode(88,0) || 1 < kickZ.length) {
         kickZ = `${(annerw == String.fromCharCode(98,0) ? annerw.length : parseInt(`${analyticp}`))}`;
         break;
      }
      for (let u = 0; u < 3; u++) {
         expireds = new Map([[`${expireds.size}`, 1]]);
      }
      let dependency1 = 6656344.0 <= analyticp;
      do {
         analyticp += kickZ.length;
         if (dependency1) {
            break;
         }
      } while (dependency1 && ((analyticp + 5.41) == 1.20));
       let iconscheduleu = 5.0;
      for (let p = 0; p < 3; p++) {
          let middlebrightnessc = String.fromCharCode(107,95,49,53,95,118,97,108,117,101,0);
          let mountingD = String.fromCharCode(105,116,117,110,101,115,95,120,95,52,55,0);
          let favoriteO = String.fromCharCode(101,95,52,95,97,110,105,109,97,116,105,110,103,0);
          let canvasC = String.fromCharCode(122,95,52,53,95,104,111,108,100,101,114,115,0);
          let predictionactivej = 0;
         kickZ += `${1 >> (Math.min(1, kickZ.length))}`;
         middlebrightnessc += "3";
         mountingD += `${favoriteO.length >> (Math.min(1, Math.abs(predictionactivej)))}`;
         favoriteO = `${(canvasC == String.fromCharCode(75,0) ? canvasC.length : predictionactivej)}`;
      }
      let becomeO = String.fromCharCode(117,53,99,120,57,101,0) == kickZ;
      do {
         kickZ = `${(kickZ == String.fromCharCode(54,0) ? kickZ.length : parseInt(`${iconscheduleu}`))}`;
         if (becomeO) {
            break;
         }
      } while ((1 == (4 + kickZ.length) && 1 == (kickZ.length + 4)) && becomeO);
         iconscheduleu -= expireds.size % 2;
      libreacta.push(albumK + androidN.length);
      break;
   }
   if (!descb) {
       let baselineW: Array<any> = [823, 619, 892];
       let private_tL = 2.0;
         baselineW.push(2);
       let filterb: Array<any> = [221, 820];
       let topic7 = false;
         topic7 = baselineW.length > 63;
          let soundA: Array<any> = [597, 620];
         baselineW = [3 + parseInt(`${private_tL}`)];
         soundA.push(soundA.length);
      while (topic7) {
          let zhuboW = 2.0;
          let iconfeedbackA = 5;
         baselineW = [(baselineW.length & (topic7 ? 4 : 2))];
         zhuboW -= parseInt(`${zhuboW}`);
         iconfeedbackA &= iconfeedbackA;
         break;
      }
      descb = 14 < libreacta.length && private_tL < 72.32;
   }

    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wwAbidetect.instance.isAppsflyerProduction;

   if (!iconclosewhitebgh.includes(`${descb}`)) {
       let subbasketballplayerw = 3.0;
       let darko: Map<any, any> = new Map([[String.fromCharCode(98,102,114,97,109,101,115,95,121,95,57,57,0),false ], [String.fromCharCode(115,110,97,112,112,101,100,95,97,95,49,49,0),false ], [String.fromCharCode(105,95,56,57,95,116,101,114,109,105,110,97,116,101,0),false ]]);
          let hongkongs = String.fromCharCode(111,95,51,54,95,105,110,116,101,114,115,101,99,116,0);
          let sharemodald = 4.0;
          let tickb = String.fromCharCode(108,95,53,95,114,101,115,116,114,105,99,116,101,100,0);
         subbasketballplayerw -= parseFloat(`${hongkongs.length - 3}`);
         hongkongs += `${(String.fromCharCode(65,0) == tickb ? parseInt(`${sharemodald}`) : tickb.length)}`;
         sharemodald -= tickb.length;
          let storeU = String.fromCharCode(112,114,101,112,114,111,99,101,115,115,95,122,95,54,53,0);
          let config4 = String.fromCharCode(117,110,100,111,95,104,95,50,50,0);
         subbasketballplayerw -= parseFloat(`${3}`);
         storeU = `${storeU.length}`;
         config4 += "3";
      if (5 <= darko.size) {
         darko = new Map([[`${darko.size}`, darko.size / (Math.max(10, parseInt(`${subbasketballplayerw}`)))]]);
      }
      let confirmationT = darko.size >= 8725274;
      do {
         darko = new Map([[`${darko.size}`, darko.size]]);
         if (confirmationT) {
            break;
         }
      } while ((!Array.from(darko.values()).includes(subbasketballplayerw)) && confirmationT);
      let utilsJ = 5564194.0 <= subbasketballplayerw;
      do {
         subbasketballplayerw -= parseFloat(`${parseInt(`${subbasketballplayerw}`)}`);
         if (utilsJ) {
            break;
         }
      } while (utilsJ && ((5.61 / (Math.max(3, subbasketballplayerw))) == 5.11 || (subbasketballplayerw / (Math.max(9, parseFloat(`${darko.size}`)))) == 5.61));
      let libpangleflippedB = darko.size >= 7059434;
      do {
          let directH = String.fromCharCode(121,111,103,97,95,104,95,50,0);
          let m_image9: Array<any> = [471, 749];
         darko = new Map([[`${darko.size}`, parseInt(`${subbasketballplayerw}`)]]);
         directH = `${3 | directH.length}`;
         m_image9 = [3];
         if (libpangleflippedB) {
            break;
         }
      } while (libpangleflippedB && (!Array.from(darko.keys()).includes(`${subbasketballplayerw}`)));
      iconclosewhitebgh += "3";
   }
   while (!descb) {
      descb = (awayteamfieldf.size | libreacta.length) <= 49;
      break;
   }
    if (enableConst === false) return;

   while (!descb) {
       let mintegralg = String.fromCharCode(121,95,53,55,95,105,99,99,112,0);
       let tailR = String.fromCharCode(97,95,56,53,95,97,110,103,108,101,100,0);
       let bootx: Map<any, any> = new Map([[String.fromCharCode(113,95,53,95,115,97,105,122,0),417], [String.fromCharCode(108,105,98,99,111,100,101,99,95,110,95,54,57,0),594], [String.fromCharCode(116,95,54,53,95,117,110,104,97,110,100,108,101,100,0),685]]);
       let panglex = String.fromCharCode(112,97,114,116,110,101,114,95,57,95,49,55,0);
       let calendaro: Map<any, any> = new Map([[String.fromCharCode(105,95,55,50,95,109,97,105,110,108,105,115,116,0),990], [String.fromCharCode(98,108,97,109,101,100,95,122,95,51,54,0),216], [String.fromCharCode(106,95,54,50,95,112,97,103,101,104,97,115,104,0),384]]);
      while (2 == tailR.length) {
          let libjsijniprofiler1 = 0;
         mintegralg = `${panglex.length << (Math.min(Math.abs(2), 5))}`;
         libjsijniprofiler1 |= libjsijniprofiler1 >> (Math.min(2, Math.abs(libjsijniprofiler1)));
         break;
      }
         bootx.set(`${tailR}`, calendaro.size);
       let inouttargetredt = 5.0;
          let turnt = 4;
         calendaro.set(`${inouttargetredt}`, turnt * 1);
      while (!panglex.startsWith(`${bootx.size}`)) {
         bootx = new Map([[`${bootx.size}`, parseInt(`${inouttargetredt}`)]]);
         break;
      }
          let canvasF = String.fromCharCode(115,111,108,105,100,95,98,95,55,56,0);
         bootx.set(`${mintegralg}`, (String.fromCharCode(77,0) == mintegralg ? mintegralg.length : bootx.size));
         canvasF += `${(canvasF == String.fromCharCode(66,0) ? canvasF.length : canvasF.length)}`;
         mintegralg = `${bootx.size >> (Math.min(3, Math.abs(parseInt(`${inouttargetredt}`))))}`;
          let typesl = String.fromCharCode(109,95,53,49,95,103,114,97,98,0);
          let combineG = String.fromCharCode(115,117,98,108,101,110,103,116,104,95,99,95,54,54,0);
         calendaro = new Map([[`${calendaro.size}`, 2 + calendaro.size]]);
         typesl += `${3 | typesl.length}`;
         combineG += `${typesl.length * combineG.length}`;
         panglex += `${tailR.length}`;
         calendaro = new Map([[tailR, parseInt(`${inouttargetredt}`) << (Math.min(Math.abs(3), 1))]]);
       let hongkongx = String.fromCharCode(99,114,111,110,111,115,95,105,95,53,48,0);
       let yellowanimationlivei = String.fromCharCode(105,109,112,108,105,99,105,116,95,98,95,56,49,0);
      for (let f = 0; f < 1; f++) {
         tailR += `${parseInt(`${inouttargetredt}`) | 2}`;
      }
      let main_kI = hongkongx.length >= 7088535;
      do {
          let infoO: Map<any, any> = new Map([[String.fromCharCode(105,95,51,55,95,117,108,112,105,110,102,111,0),String.fromCharCode(97,95,50,50,95,115,116,111,112,112,101,100,0)], [String.fromCharCode(98,95,52,51,95,118,114,108,101,0),String.fromCharCode(115,95,48,95,116,114,97,110,115,112,97,114,101,110,99,121,0)]]);
          let iconuserM: Map<any, any> = new Map([[String.fromCharCode(117,95,53,50,95,109,101,97,115,117,114,101,115,0),false ], [String.fromCharCode(110,95,52,57,95,100,117,109,112,105,110,102,111,0),true ], [String.fromCharCode(112,114,101,97,108,108,111,99,95,53,95,55,57,0),false ]]);
          let q_countJ = String.fromCharCode(112,114,111,100,117,99,116,115,95,49,95,49,53,0);
          let cancel3 = String.fromCharCode(97,108,116,95,99,95,53,48,0);
         hongkongx += `${panglex.length}`;
         infoO.set(`${iconuserM.size}`, iconuserM.size);
         q_countJ = `${cancel3.length % (Math.max(8, infoO.size))}`;
         cancel3 = `${cancel3.length + 1}`;
         if (main_kI) {
            break;
         }
      } while (main_kI && (panglex == hongkongx));
         hongkongx = `${tailR.length % 3}`;
      let eventsplashC = String.fromCharCode(53,120,117,50,52,99,0) == mintegralg;
      do {
          let configurey = String.fromCharCode(116,95,49,95,105,97,100,115,116,0);
          let downarrowZ: Array<any> = [String.fromCharCode(112,101,114,109,97,110,101,110,116,95,52,95,57,53,0), String.fromCharCode(105,95,51,54,95,114,101,108,111,97,100,101,114,0), String.fromCharCode(98,95,55,57,95,99,104,101,99,107,115,0)];
          let filedR = String.fromCharCode(110,101,97,114,101,115,116,95,119,95,51,57,0);
          let stationsX: Array<any> = [780, 640, 214];
         mintegralg += `${1 - mintegralg.length}`;
         configurey += "2";
         downarrowZ = [downarrowZ.length ^ filedR.length];
         filedR += `${1 * configurey.length}`;
         stationsX.push(filedR.length | stationsX.length);
         if (eventsplashC) {
            break;
         }
      } while ((5 <= mintegralg.length) && eventsplashC);
      descb = descb || tickedi;
      break;
   }
       let iconrefreshb: Map<any, any> = new Map([[String.fromCharCode(114,101,115,112,111,110,100,95,115,95,49,57,0),false ], [String.fromCharCode(97,114,101,116,104,101,114,101,95,110,95,57,50,0),false ]]);
      while ((iconrefreshb.size ^ 4) <= 4) {
         iconrefreshb.set(`${iconrefreshb.size}`, iconrefreshb.size & 3);
         break;
      }
      let hover5 = iconrefreshb.size <= 9626876;
      do {
         iconrefreshb.set(`${iconrefreshb.size}`, 2 << (Math.min(4, Math.abs(iconrefreshb.size))));
         if (hover5) {
            break;
         }
      } while (hover5 && (Array.from(iconrefreshb.keys()).includes(`${iconrefreshb.size}`)));
      while ((iconrefreshb.size - 4) < 1 && (4 - iconrefreshb.size) < 1) {
          let iconsubssuccessf = String.fromCharCode(110,95,57,55,95,105,100,99,116,120,100,99,0);
          let libfbjniR = 4;
          let layoutG = false;
         iconrefreshb.set(`${libfbjniR}`, 3);
         iconsubssuccessf = `${((layoutG ? 2 : 4))}`;
         libfbjniR >>= Math.min(3, iconsubssuccessf.length);
         break;
      }
      descb = 45 == catalogL.length;

    

      tickedi = androidN.length < catalogL.length;
      albumK *= libreacta.length | 2;

    appsFlyer.logEvent(
      XELibreactnativejni.EPlusSans,
      {},
      res => {

      catalogL += `${((tickedi ? 4 : 2) & albumK)}`;
      awayteamfieldf = new Map([[androidN, iconclosewhitebgh.length - androidN.length]]);
        if (this.#showLog)

   for (let h = 0; h < 3; h++) {
      predictionl += "3";
   }
      tickedi = iconclosewhitebgh == androidN;
          console.log(
            'trigger event id:',
            XELibreactnativejni.EPlusSans,
          );
      },
      err => {

      androidN = `${awayteamfieldf.size}`;
   while (1 <= iconclosewhitebgh.length) {
      androidN += `${libreacta.length}`;
      break;
   }
        if (this.#showLog)

   while (tickedi) {
      catalogL = `${androidN.length & 3}`;
      break;
   }
       let libreactnativejnij = String.fromCharCode(102,95,50,54,95,115,105,109,117,108,97,116,111,114,0);
         libreactnativejnij += `${libreactnativejnij.length % 2}`;
      for (let k = 0; k < 1; k++) {
         libreactnativejnij += `${libreactnativejnij.length % (Math.max(5, libreactnativejnij.length))}`;
      }
      for (let e = 0; e < 1; e++) {
         libreactnativejnij += `${(String.fromCharCode(67,0) == libreactnativejnij ? libreactnativejnij.length : libreactnativejnij.length)}`;
      }
      catalogL = `${(catalogL.length - (tickedi ? 2 : 4))}`;
          console.error(
            'error event id:',
            XELibreactnativejni.EPlusSans,
          );
      },
    );
  };

  static playsPlaysTimesAnalytics = ({
    vod_id,
    vod_name,
    isXmode = false,
  }: {
    vod_id: string;
    vod_name: string;
    isXmode?: boolean;
  }) => {
    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wwAbidetect.instance.isAppsflyerProduction;
    if (enableConst === false) return;

    

    appsFlyer.logEvent(
      XELibreactnativejni.EXvod,
      {
        x_mode: isXmode ? 'true' : 'false',
        vod_id: vod_id,
        vod_name: vod_name,
      },
      res => {
        if (this.#showLog)
          console.log('trigger event id:', XELibreactnativejni.EXvod);
      },
      err => {
        if (this.#showLog)
          console.error('error event id:', XELibreactnativejni.EXvod);
      },
    );
  };
}
