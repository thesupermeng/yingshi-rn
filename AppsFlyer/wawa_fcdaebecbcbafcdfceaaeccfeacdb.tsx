import appsFlyer from 'react-native-appsflyer';
import {wawaBasketballiconPenaltyshootnogoal} from '../wawa_shareblack_orientation';
import {Platform} from 'react-native';
import {
  APPSFLYER_PAYMENT_PUBLIC_KEY,
  ENABLE_APPSFLYER,
  UMENG_CHANNEL,
} from '@utility/wawa_iconpointscore';

enum IIPhoneshare {
  IIShoot = 'install',
  IIHomeloadingMimo = 'open_app',
  IIIconIconqq = 'userCenter_payment_success_times',
  FDownloadedArrowrightwithtail = 'userCenter_login_success_times',
  FAbidetectViews = 'play_plays_times',
}

export default class wawaSina {
  
  static #enable: boolean =
    (Platform.OS === 'android' &&
      ENABLE_APPSFLYER &&
      UMENG_CHANNEL == 'GOOGLE_PLAY') ||
    wawaBasketballiconPenaltyshootnogoal.instance.isAppsflyerProduction;
  static #showLog: boolean = true;

  
  static install = () => {
       let bannere: Array<any> = [722, 608];
    let plashu = 1.0;
    let searchbarr: Array<any> = [672, 512, 234];
    let iconcurrentmatchu = 0.0;
    let fullscreenminY = String.fromCharCode(100,97,116,101,115,0);
    let projectf = String.fromCharCode(113,95,57,55,0);
    let disconnectedlogor: Map<any, any> = new Map([[String.fromCharCode(97,95,49,57,95,104,108,115,101,110,99,0),true ], [String.fromCharCode(115,99,104,101,100,95,104,95,53,48,0),false ], [String.fromCharCode(97,110,103,114,121,0),false ]]);
    let iconstarf = 3;
    let containerP = String.fromCharCode(115,101,103,109,101,110,116,117,114,108,110,111,100,101,95,99,95,56,50,0);
    let with_44E = false;
   for (let s = 0; s < 2; s++) {
       let libreactnativeblobx = 0.0;
      while ((1 / (Math.max(10, libreactnativeblobx))) < 5.2) {
          let bannerk = true;
          let recommendationj: Map<any, any> = new Map([[String.fromCharCode(99,97,108,108,105,110,103,0),String.fromCharCode(103,101,111,95,49,95,50,52,0)], [String.fromCharCode(105,95,52,53,95,115,117,105,116,101,0),String.fromCharCode(114,101,110,100,101,114,95,106,95,57,53,0)]]);
          let statsnomoredataE: Map<any, any> = new Map([[String.fromCharCode(119,97,108,107,116,104,114,111,117,103,104,95,117,95,52,55,0),false ], [String.fromCharCode(115,121,115,99,116,108,115,0),false ], [String.fromCharCode(106,112,101,103,99,111,109,112,95,53,95,49,57,0),false ]]);
          let basketballiconF = true;
          let dangerz = String.fromCharCode(109,95,57,52,95,110,117,108,108,0);
         libreactnativeblobx *= recommendationj.size | statsnomoredataE.size;
         bannerk = dangerz.length < 20;
         recommendationj.set(dangerz, 3);
         statsnomoredataE.set(`${basketballiconF}`, ((bannerk ? 1 : 3)));
         break;
      }
          let fullscreenmaxc = 4;
          let short_d8: Map<any, any> = new Map([[String.fromCharCode(105,110,100,105,110,103,0),41], [String.fromCharCode(105,110,116,101,114,102,97,99,101,115,95,55,95,57,0),57], [String.fromCharCode(97,114,97,98,105,99,0),389]]);
         libreactnativeblobx += parseInt(`${libreactnativeblobx}`);
         fullscreenmaxc |= 1;
         short_d8 = new Map([[`${short_d8.size}`, short_d8.size]]);
         libreactnativeblobx -= parseInt(`${libreactnativeblobx}`);
      bannere = [3];
   }
   while ((parseInt(`${plashu}`) / (Math.max(disconnectedlogor.size, 9))) > 3 && 5.19 > (plashu / (Math.max(3.24, 10)))) {
       let bannerF = 2.0;
       let shootnogoalb = 4.0;
       let taiwanJ = 0.0;
       let membershipP = String.fromCharCode(115,117,98,120,0);
      let sliderM = shootnogoalb <= 7555936.0;
      do {
          let sportsg = 5.0;
          let shielddoneX = false;
         shootnogoalb += parseFloat(`${parseInt(`${shootnogoalb}`) + parseInt(`${taiwanJ}`)}`);
         sportsg += 3 + parseInt(`${sportsg}`);
         shielddoneX = 88.91 > sportsg;
         if (sliderM) {
            break;
         }
      } while (sliderM && (bannerF <= 5.68));
         membershipP = `${parseInt(`${shootnogoalb}`)}`;
         taiwanJ /= Math.max(parseFloat(`${membershipP.length}`), 2);
      for (let w = 0; w < 1; w++) {
          let reactnativeultimatelistview0: Map<any, any> = new Map([[String.fromCharCode(101,109,98,101,100,100,101,100,0),574], [String.fromCharCode(120,95,55,56,95,105,110,97,99,116,105,118,101,0),709]]);
          let unreadt = String.fromCharCode(101,118,97,108,117,97,116,111,114,0);
          let buildA = String.fromCharCode(116,114,97,110,115,105,101,110,116,95,55,95,55,53,0);
          let pointl: Array<any> = [329, 377];
          let backwardR: Array<any> = [348, 589];
         taiwanJ /= Math.max(5, parseFloat(`${buildA.length}`));
         reactnativeultimatelistview0 = new Map([[`${backwardR.length}`, 1 & pointl.length]]);
         unreadt += `${pointl.length}`;
         buildA += `${pointl.length >> (Math.min(Math.abs(2), 2))}`;
         backwardR.push(unreadt.length);
      }
         bannerF += 1 - membershipP.length;
      for (let g = 0; g < 2; g++) {
         shootnogoalb /= Math.max(5, parseFloat(`${parseInt(`${bannerF}`)}`));
      }
         shootnogoalb -= parseFloat(`${parseInt(`${bannerF}`)}`);
         shootnogoalb *= parseFloat(`${parseInt(`${shootnogoalb}`) + 2}`);
          let ying9 = 0.0;
         bannerF *= parseInt(`${shootnogoalb}`);
         ying9 *= parseFloat(`${3 - parseInt(`${ying9}`)}`);
         taiwanJ /= Math.max(parseFloat(`${parseInt(`${bannerF}`)}`), 5);
      while (taiwanJ == bannerF) {
         bannerF += parseInt(`${taiwanJ}`) - parseInt(`${shootnogoalb}`);
         break;
      }
          let questiconW = true;
          let video0 = 0;
          let iconrightorangel = 1.0;
         taiwanJ += parseFloat(`${video0 - 3}`);
         questiconW = 74.22 > iconrightorangel;
         video0 /= Math.max((parseInt(`${iconrightorangel}`) + (questiconW ? 4 : 3)), 5);
      disconnectedlogor = new Map([[`${searchbarr.length}`, iconstarf]]);
      break;
   }

    

       let codegenN = 1.0;
       let rewardT = 5.0;
       let telegrams = 1.0;
      for (let f = 0; f < 3; f++) {
          let yellowscoreballk: Map<any, any> = new Map([[String.fromCharCode(115,117,98,118,105,100,101,111,0),14], [String.fromCharCode(109,101,109,100,98,0),874]]);
         codegenN /= Math.max(1, parseInt(`${codegenN}`) - 3);
         yellowscoreballk.set(`${yellowscoreballk.size}`, yellowscoreballk.size);
      }
      let homeS = 9417657.0 <= rewardT;
      do {
          let disti = String.fromCharCode(114,95,55,51,95,117,110,100,101,114,108,121,105,110,103,0);
          let switch_5h = 1;
          let dragclosev = 2.0;
         rewardT -= 3 >> (Math.min(Math.abs(parseInt(`${telegrams}`)), 5));
         disti += `${switch_5h}`;
         switch_5h ^= switch_5h;
         dragclosev /= Math.max(2, 5);
         if (homeS) {
            break;
         }
      } while (homeS && (5.7 == rewardT));
       let constantsN = String.fromCharCode(109,95,50,57,95,98,97,115,101,112,111,105,110,116,0);
       let subbasketballplayerF = String.fromCharCode(115,119,105,112,101,100,0);
      for (let n = 0; n < 3; n++) {
         rewardT /= Math.max(3, parseInt(`${codegenN}`));
      }
          let modules0 = String.fromCharCode(110,101,97,114,101,115,116,95,54,95,57,56,0);
          let pangleO = 4.0;
          let hnewsi: Array<any> = [String.fromCharCode(106,111,105,110,101,114,0), String.fromCharCode(98,105,103,103,101,115,116,0)];
         subbasketballplayerF = `${parseInt(`${codegenN}`) - 1}`;
         modules0 = `${3 / (Math.max(4, parseInt(`${pangleO}`)))}`;
         pangleO += hnewsi.length;
         hnewsi.push(modules0.length);
      if ((constantsN.length % (Math.max(5, 5))) > 5) {
         constantsN += "2";
      }
       let signinupl = String.fromCharCode(114,101,100,101,116,101,99,116,0);
       let xcopy_0eU = String.fromCharCode(117,110,114,101,115,111,108,118,97,98,108,101,0);
         xcopy_0eU += `${parseInt(`${codegenN}`)}`;
      let annerU = constantsN == String.fromCharCode(54,54,121,97,116,0);
      do {
          let logoF = String.fromCharCode(121,95,49,56,95,109,97,112,115,105,122,101,0);
          let room5 = 0.0;
          let iconbellK: Map<any, any> = new Map([[String.fromCharCode(112,114,101,118,105,111,117,115,108,121,0),299], [String.fromCharCode(103,101,110,101,114,97,116,101,95,119,95,55,53,0),30], [String.fromCharCode(99,117,98,101,100,95,49,95,55,53,0),639]]);
          let reducerU = 2.0;
         constantsN = `${signinupl.length & 3}`;
         logoF = "2";
         room5 /= Math.max(1, parseFloat(`${iconbellK.size & parseInt(`${room5}`)}`));
         iconbellK.set(`${room5}`, 1);
         reducerU += parseInt(`${room5}`) + logoF.length;
         if (annerU) {
            break;
         }
      } while ((3 >= (constantsN.length + 5) || 2 >= (parseInt(`${telegrams}`) * 5)) && annerU);
      containerP += `${parseInt(`${telegrams}`)}`;
   let giftd = projectf.length <= 5179753;
   do {
       let iconbellactivep = 0;
       let whitevideolivew = 5;
         whitevideolivew -= 2;
      if (2 < (2 % (Math.max(3, whitevideolivew))) || (2 % (Math.max(1, whitevideolivew))) < 4) {
         iconbellactivep |= iconbellactivep >> (Math.min(Math.abs(1), 5));
      }
      while (5 == (4 ^ whitevideolivew) || 4 == (4 ^ iconbellactivep)) {
         whitevideolivew *= whitevideolivew << (Math.min(4, Math.abs(iconbellactivep)));
         break;
      }
         iconbellactivep <<= Math.min(5, Math.abs(1));
         iconbellactivep -= 1 << (Math.min(4, Math.abs(whitevideolivew)));
       let product1 = 3;
       let commentg = 3;
      projectf = `${(projectf == String.fromCharCode(108,0) ? projectf.length : containerP.length)}`;
      if (giftd) {
         break;
      }
   } while (giftd && (containerP.length == 1));
    

      containerP += `${fullscreenminY.length * searchbarr.length}`;
       let qaagi = 4.0;
          let componentsE = 5.0;
          let downz = String.fromCharCode(100,101,115,105,114,101,100,0);
          let shirtZ = String.fromCharCode(114,115,116,114,105,112,0);
         qaagi -= parseFloat(`${parseInt(`${componentsE}`) * downz.length}`);
         componentsE -= shirtZ.length;
         downz = "1";
      if ((2 - qaagi) <= 4.27) {
         qaagi *= parseFloat(`${1 - parseInt(`${qaagi}`)}`);
      }
         qaagi -= parseFloat(`${parseInt(`${qaagi}`) | parseInt(`${qaagi}`)}`);
      fullscreenminY = `${bannere.length}`;
    

   for (let w = 0; w < 3; w++) {
      containerP = `${iconstarf}`;
   }
   while (containerP.includes(`${iconstarf}`)) {
      iconstarf %= Math.max(2, 1);
      break;
   }
    

   if (1 > (bannere.length >> (Math.min(Math.abs(4), 3))) || (4 ^ bannere.length) > 1) {
       let singlen = 4.0;
       let iconpipexpandO = 3.0;
       let sourcee = 4.0;
       let g_image0 = 4.0;
         singlen += parseFloat(`${3 << (Math.min(Math.abs(parseInt(`${singlen}`)), 3))}`);
         sourcee *= 1 % (Math.max(parseInt(`${iconpipexpandO}`), 2));
         g_image0 /= Math.max(parseFloat(`${parseInt(`${g_image0}`) - parseInt(`${singlen}`)}`), 4);
      while (3.39 < (5.53 / (Math.max(5, iconpipexpandO))) || (sourcee / (Math.max(2, iconpipexpandO))) < 5.53) {
         iconpipexpandO += parseFloat(`${3}`);
         break;
      }
      let basketballawayteamQ = g_image0 <= 5900230.0;
      do {
         g_image0 -= parseFloat(`${parseInt(`${singlen}`) ^ 2}`);
         if (basketballawayteamQ) {
            break;
         }
      } while (basketballawayteamQ && (iconpipexpandO >= g_image0));
          let unimplementedviewM = String.fromCharCode(97,115,112,101,99,116,0);
          let greyJ: Map<any, any> = new Map([[String.fromCharCode(115,116,98,108,0),true ], [String.fromCharCode(107,95,50,55,95,101,120,112,101,99,116,0),true ], [String.fromCharCode(115,101,97,114,99,104,95,97,95,57,56,0),true ]]);
         iconpipexpandO += parseFloat(`${greyJ.size}`);
         unimplementedviewM += `${unimplementedviewM.length}`;
         greyJ = new Map([[unimplementedviewM, 3 * unimplementedviewM.length]]);
         sourcee -= parseInt(`${singlen}`) | parseInt(`${iconpipexpandO}`);
      let black_ = iconpipexpandO >= 6797551.0;
      do {
         iconpipexpandO -= parseFloat(`${parseInt(`${sourcee}`) | parseInt(`${singlen}`)}`);
         if (black_) {
            break;
         }
      } while (black_ && (1.78 >= (iconpipexpandO / 3.55) || 4.22 >= (3.55 * singlen)));
         singlen += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${g_image0}`)), 5))}`);
          let expiredP = String.fromCharCode(101,109,98,101,100,100,105,110,103,95,103,95,53,48,0);
          let yellowvideoliveD = String.fromCharCode(97,98,115,116,0);
          let infoO = String.fromCharCode(108,95,57,48,95,117,115,116,111,109,0);
         sourcee *= parseInt(`${iconpipexpandO}`);
         expiredP = `${infoO.length / (Math.max(2, 1))}`;
         yellowvideoliveD = `${infoO.length}`;
         g_image0 += parseFloat(`${parseInt(`${singlen}`) % 3}`);
         g_image0 /= Math.max(parseFloat(`${parseInt(`${sourcee}`)}`), 4);
      bannere.push(parseInt(`${iconcurrentmatchu}`));
   }
      iconstarf %= Math.max(parseInt(`${plashu}`), 3);

    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wawaBasketballiconPenaltyshootnogoal.instance.isAppsflyerProduction;

   for (let q = 0; q < 2; q++) {
       let rncoreH: Array<any> = [854, 402, 546];
         rncoreH.push(rncoreH.length * rncoreH.length);
      while (!rncoreH.includes(rncoreH.length)) {
         rncoreH = [rncoreH.length];
         break;
      }
       let controlC: Map<any, any> = new Map([[String.fromCharCode(97,115,115,111,99,105,97,116,101,95,119,95,56,57,0),String.fromCharCode(100,101,115,116,114,111,121,105,110,103,95,56,95,57,48,0)], [String.fromCharCode(107,95,52,57,95,97,103,103,114,101,103,97,116,101,0),String.fromCharCode(99,97,110,118,97,115,0)]]);
      projectf = `${rncoreH.length - projectf.length}`;
   }
       let binddatasd = 4;
       let formL = String.fromCharCode(102,105,110,103,101,114,112,114,105,110,116,0);
      let notificationgray3 = String.fromCharCode(97,116,99,103,104,104,0) == formL;
      do {
         formL += `${binddatasd}`;
         if (notificationgray3) {
            break;
         }
      } while (((1 >> (Math.min(4, formL.length))) <= 3) && notificationgray3);
         binddatasd |= (String.fromCharCode(122,0) == formL ? binddatasd : formL.length);
         binddatasd /= Math.max(1, 2);
      if ((formL.length << (Math.min(5, Math.abs(binddatasd)))) > 3 && (3 << (Math.min(5, formL.length))) > 1) {
         formL += `${binddatasd * 3}`;
      }
      while (5 > (2 * formL.length) || 3 > (binddatasd * 2)) {
          let pageM = 5.0;
          let catagoryR = 4.0;
          let tooltips8 = 5.0;
         binddatasd >>= Math.min(Math.abs(parseInt(`${tooltips8}`)), 3);
         pageM += parseFloat(`${parseInt(`${catagoryR}`)}`);
         break;
      }
      if (4 >= formL.length) {
         binddatasd += 3 ^ binddatasd;
      }
      bannere = [parseInt(`${plashu}`)];
    if (enableConst === false) return;

   while (5 <= (projectf.length >> (Math.min(1, searchbarr.length))) && (5 >> (Math.min(1, projectf.length))) <= 1) {
      projectf = `${iconstarf}`;
      break;
   }
   for (let h = 0; h < 1; h++) {
      iconstarf &= 3;
   }

    

   while ((plashu / (Math.max(iconcurrentmatchu, 9))) > 5.48 && 2.37 > (plashu / 5.48)) {
      iconcurrentmatchu *= parseFloat(`${2 - projectf.length}`);
      break;
   }
       let subbasketballplayer6 = String.fromCharCode(115,99,104,101,109,97,115,95,99,95,53,51,0);
       let hometeamfieldJ = String.fromCharCode(112,114,105,109,97,114,105,101,115,0);
       let defaultprofilepicc = String.fromCharCode(99,97,110,118,97,115,95,121,95,50,53,0);
          let inactivef: Array<any> = [566, 133];
          let graphicsX = String.fromCharCode(105,95,53,57,95,101,114,114,111,114,118,0);
         hometeamfieldJ += `${subbasketballplayer6.length % (Math.max(2, 6))}`;
         inactivef = [1];
         graphicsX = `${(graphicsX == String.fromCharCode(118,0) ? graphicsX.length : inactivef.length)}`;
      let plash_ = 8597002 >= hometeamfieldJ.length;
      do {
         hometeamfieldJ = `${(defaultprofilepicc == String.fromCharCode(77,0) ? defaultprofilepicc.length : hometeamfieldJ.length)}`;
         if (plash_) {
            break;
         }
      } while (plash_ && (!defaultprofilepicc.endsWith(`${hometeamfieldJ.length}`)));
      let networkb = hometeamfieldJ.length <= 9494686;
      do {
          let catagoryY = String.fromCharCode(109,105,120,112,97,110,101,108,0);
          let textc = String.fromCharCode(122,95,54,48,95,117,100,116,97,0);
          let libimagepipeline1 = 1;
          let friendsS = 0;
          let matchactiveX = String.fromCharCode(101,118,97,108,117,108,97,116,101,0);
         hometeamfieldJ = `${hometeamfieldJ.length}`;
         catagoryY = `${libimagepipeline1 % 1}`;
         textc = `${textc.length}`;
         libimagepipeline1 >>= Math.min(matchactiveX.length, 5);
         friendsS <<= Math.min(Math.abs(libimagepipeline1), 2);
         matchactiveX += `${catagoryY.length * 3}`;
         if (networkb) {
            break;
         }
      } while ((!defaultprofilepicc.endsWith(`${hometeamfieldJ.length}`)) && networkb);
      for (let r = 0; r < 3; r++) {
         hometeamfieldJ += "3";
      }
         hometeamfieldJ = `${(String.fromCharCode(53,0) == defaultprofilepicc ? defaultprofilepicc.length : hometeamfieldJ.length)}`;
      while (defaultprofilepicc.length < 4) {
          let umengc: Map<any, any> = new Map([[String.fromCharCode(109,95,51,53,95,103,97,105,110,115,0),11], [String.fromCharCode(108,95,55,57,95,117,101,102,97,0),635], [String.fromCharCode(109,95,54,54,95,100,105,118,105,115,105,111,110,0),350]]);
          let predictionbannersharedx = 3;
          let runtimescheduler2: Array<any> = [String.fromCharCode(99,111,109,112,108,105,97,110,116,95,112,95,56,50,0), String.fromCharCode(118,95,57,54,95,116,104,101,110,97,98,108,101,0)];
          let middlesoundT = 5;
          let ewardedO = 0;
         defaultprofilepicc = `${runtimescheduler2.length}`;
         umengc = new Map([[`${umengc.size}`, 2]]);
         predictionbannersharedx ^= ewardedO % 3;
         runtimescheduler2.push(2);
         middlesoundT <<= Math.min(Math.abs(3), 1);
         ewardedO -= 2 - ewardedO;
         break;
      }
         subbasketballplayer6 = "3";
      if (defaultprofilepicc.length > subbasketballplayer6.length) {
         subbasketballplayer6 += `${hometeamfieldJ.length << (Math.min(subbasketballplayer6.length, 1))}`;
      }
       let eighteenC: Array<any> = [97, 699, 115];
       let roomm: Array<any> = [String.fromCharCode(97,110,105,109,97,116,105,111,110,95,50,95,54,0), String.fromCharCode(113,95,50,52,95,118,112,109,99,0)];
      projectf = `${fullscreenminY.length}`;

    const ip: string = wawaBasketballiconPenaltyshootnogoal.instance.ip;

    appsFlyer.logEvent(
      IIPhoneshare.IIShoot,
      {ip},
      res => {

       let eyecloseZ: Array<any> = [391, 316, 966];
       let valuesi = String.fromCharCode(99,97,112,116,117,114,101,116,101,115,116,118,105,100,101,111,95,97,95,52,52,0);
      while (1 >= (1 ^ eyecloseZ.length) && 1 >= (eyecloseZ.length ^ 1)) {
         eyecloseZ = [eyecloseZ.length];
         break;
      }
       let videojsH = 1;
       let formq = 3;
      while (valuesi.endsWith(`${formq}`)) {
         valuesi = `${valuesi.length * 3}`;
         break;
      }
      if (videojsH <= valuesi.length) {
         videojsH -= formq >> (Math.min(3, Math.abs(2)));
      }
      for (let l = 0; l < 3; l++) {
          let predictionactiveo = String.fromCharCode(99,105,118,105,108,0);
          let recommendationp: Map<any, any> = new Map([[String.fromCharCode(119,95,55,53,95,115,101,112,97,114,97,116,111,114,115,0),false ], [String.fromCharCode(97,100,111,98,101,0),false ]]);
         formq /= Math.max(1, 3);
         predictionactiveo = `${recommendationp.size}`;
         recommendationp = new Map([[`${recommendationp.size}`, recommendationp.size]]);
      }
       let awayteamfieldb = 4;
      disconnectedlogor.set(fullscreenminY, eyecloseZ.length << (Math.min(fullscreenminY.length, 1)));
   if (projectf.includes(`${bannere.length}`)) {
      projectf += `${bannere.length % (Math.max(3, 2))}`;
   }
        if (this.#showLog)

   if ((5 << (Math.min(4, Math.abs(iconstarf)))) > 4) {
      iconstarf &= 3 - parseInt(`${iconcurrentmatchu}`);
   }
      searchbarr.push(searchbarr.length);
          console.log('trigger event id:', IIPhoneshare.IIShoot);
      },
      err => {

   while (4.58 > (iconcurrentmatchu + parseFloat(`${bannere.length}`)) && (4.58 + iconcurrentmatchu) > 1.28) {
       let gifgoalbgE = false;
       let footballfiledlayout6 = 2.0;
          let libglogG = true;
         footballfiledlayout6 += parseFloat(`${parseInt(`${footballfiledlayout6}`) | 2}`);
         libglogG = (libglogG ? libglogG : libglogG);
      let predictiondefaults = 5914370.0 <= footballfiledlayout6;
      do {
         footballfiledlayout6 /= Math.max((parseFloat(`${parseInt(`${footballfiledlayout6}`) | (gifgoalbgE ? 2 : 1)}`)), 4);
         if (predictiondefaults) {
            break;
         }
      } while ((gifgoalbgE || (3.5 + footballfiledlayout6) == 3.40) && predictiondefaults);
         footballfiledlayout6 -= (parseFloat(`${parseInt(`${footballfiledlayout6}`) + (gifgoalbgE ? 4 : 3)}`));
         gifgoalbgE = 85.16 == footballfiledlayout6;
      if (4.7 < footballfiledlayout6 && (footballfiledlayout6 / (Math.max(4.7, 2))) < 2.26) {
         gifgoalbgE = footballfiledlayout6 < 63.57;
      }
         footballfiledlayout6 /= Math.max(5, (parseFloat(`${parseInt(`${footballfiledlayout6}`) & (gifgoalbgE ? 5 : 3)}`)));
      iconcurrentmatchu += parseFloat(`${3}`);
      break;
   }
      iconstarf <<= Math.min(Math.abs((String.fromCharCode(116,0) == projectf ? projectf.length : disconnectedlogor.size)), 2);
        if (this.#showLog)

       let bannero: Array<any> = [395, 834, 670];
      while ((bannero.length / 2) < 4 && 2 < (bannero.length / (Math.max(8, bannero.length)))) {
         bannero.push(3);
         break;
      }
         bannero = [2];
      let libturbomodulejsijnix = 9541766 <= bannero.length;
      do {
         bannero = [2];
         if (libturbomodulejsijnix) {
            break;
         }
      } while (libturbomodulejsijnix && ((bannero.length & 5) <= 3 && 2 <= (5 & bannero.length)));
      fullscreenminY = `${bannero.length ^ 1}`;
   while (5.97 < iconcurrentmatchu) {
      iconcurrentmatchu -= parseFloat(`${iconstarf}`);
      break;
   }
          console.error('error event id:', IIPhoneshare.IIShoot);
      },
    );
  };

  static appBoot = () => {
       let foundl = String.fromCharCode(98,97,99,107,105,110,103,0);
    let libfbjnii = String.fromCharCode(100,113,99,111,101,102,102,0);
    let playlistF = String.fromCharCode(110,105,103,104,116,115,104,111,116,0);
    let datai = 0;
    let textinput3 = String.fromCharCode(99,104,97,116,115,0);
    let bannerc = false;
    let castr = 5.0;
    let qaagS = String.fromCharCode(115,116,114,109,97,116,99,104,0);
    let eventsplashy = String.fromCharCode(99,97,118,108,99,95,104,95,49,52,0);
   if ((castr * 2.82) == 4.57) {
      castr += parseInt(`${castr}`);
   }

    console.log('appBoot');

   while (playlistF.length <= 4) {
      bannerc = String.fromCharCode(112,0) == playlistF;
      break;
   }
    console.log('AppsFlyerAnalytics');
    console.log(Platform.OS === 'android');
    console.log(UMENG_CHANNEL == 'GOOGLE_PLAY');

   while (textinput3.startsWith(`${bannerc}`)) {
      bannerc = 95.57 > castr && foundl.length > 89;
      break;
   }
    console.log(ENABLE_APPSFLYER);

       let iconpipshrink5 = false;
         iconpipshrink5 = !iconpipshrink5 || iconpipshrink5;
         iconpipshrink5 = (iconpipshrink5 ? iconpipshrink5 : !iconpipshrink5);
         iconpipshrink5 = !iconpipshrink5;
      bannerc = libfbjnii == textinput3;
    console.log(wawaBasketballiconPenaltyshootnogoal.instance.isAppsflyerProduction);

   let ewardedd = textinput3.length >= 7791600;
   do {
      textinput3 += `${(playlistF == String.fromCharCode(104,0) ? parseInt(`${castr}`) : playlistF.length)}`;
      if (ewardedd) {
         break;
      }
   } while (ewardedd && (4 == (datai % 2) && (datai % 2) == 2));
    console.log(this.#enable);

   for (let a = 0; a < 2; a++) {
      foundl = "3";
   }

    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wawaBasketballiconPenaltyshootnogoal.instance.isAppsflyerProduction;

      playlistF = `${libfbjnii.length}`;

    console.log('enableConst');

   while (foundl.length >= playlistF.length) {
      playlistF += `${playlistF.length | foundl.length}`;
      break;
   }
    console.log(enableConst);

      foundl = `${((bannerc ? 4 : 5))}`;

    if (enableConst === false) return;

   let renderw = String.fromCharCode(50,122,104,51,0) == foundl;
   do {
       let philippinesj = String.fromCharCode(97,95,54,52,95,99,101,108,102,0);
       let iconqqj = String.fromCharCode(102,116,118,112,110,111,100,101,0);
       let ajaxo = String.fromCharCode(121,95,57,54,95,112,97,121,101,101,0);
         philippinesj = "1";
       let redirectN: Array<any> = [String.fromCharCode(109,95,53,95,114,97,110,103,101,0), String.fromCharCode(111,95,49,55,95,112,114,101,115,99,97,108,105,110,103,0)];
      foundl += `${3 * libfbjnii.length}`;
      if (renderw) {
         break;
      }
   } while (renderw && (foundl.endsWith(`${playlistF.length}`)));

    

       let launcherh: Map<any, any> = new Map([[String.fromCharCode(101,99,104,111,95,101,95,52,52,0),315], [String.fromCharCode(100,100,118,97,95,48,95,51,52,0),973]]);
       let nterstitialh: Array<any> = [889, 532, 29];
       let clearB = String.fromCharCode(115,116,114,105,110,103,105,102,121,0);
      for (let k = 0; k < 1; k++) {
         nterstitialh.push(nterstitialh.length % (Math.max(clearB.length, 10)));
      }
      let singapored = 8652910 >= nterstitialh.length;
      do {
         nterstitialh.push(clearB.length & nterstitialh.length);
         if (singapored) {
            break;
         }
      } while ((2 <= (nterstitialh.length + 3)) && singapored);
       let updatesE = 2.0;
          let buttonA: Array<any> = [938, 793];
          let defaultlogo_: Array<any> = [String.fromCharCode(102,97,113,0), String.fromCharCode(119,105,102,105,95,117,95,57,54,0), String.fromCharCode(100,95,50,95,99,114,101,97,116,101,100,0)];
          let calendarD: Array<any> = [387, 974];
         clearB = `${launcherh.size >> (Math.min(buttonA.length, 4))}`;
         buttonA.push(defaultlogo_.length);
         defaultlogo_ = [1 >> (Math.min(2, calendarD.length))];
         calendarD.push(calendarD.length);
       let customS = 1.0;
      if (launcherh.get(`${customS}`) == null) {
         launcherh.set(`${updatesE}`, launcherh.size);
      }
      let profileframe2 = 9404270.0 <= customS;
      do {
         customS *= parseFloat(`${parseInt(`${customS}`)}`);
         if (profileframe2) {
            break;
         }
      } while (profileframe2 && (customS < 3.67));
      let awayI = 8756625.0 >= customS;
      do {
         customS /= Math.max(parseFloat(`${3}`), 2);
         if (awayI) {
            break;
         }
      } while ((5.2 <= (1.2 + customS)) && awayI);
       let tempnodatagifA = 3.0;
      castr /= Math.max(playlistF.length - textinput3.length, 3);
    const ip: string = wawaBasketballiconPenaltyshootnogoal.instance.ip;

    appsFlyer.logEvent(
      IIPhoneshare.IIHomeloadingMimo,
      {ip},
      res => {

   while (5 > (parseInt(`${castr}`) * 5) || (5 - foundl.length) > 2) {
      foundl += "2";
      break;
   }
        if (this.#showLog)

       let loginsuccessM = String.fromCharCode(97,118,97,116,97,114,115,95,48,95,55,54,0);
       let dragcloseC = true;
       let sport1 = true;
         sport1 = !sport1 || dragcloseC;
         loginsuccessM = `${(1 + (dragcloseC ? 5 : 5))}`;
          let abidetectU = String.fromCharCode(103,114,101,101,116,105,110,103,0);
          let gradlewy = 3.0;
         sport1 = loginsuccessM.endsWith(`${dragcloseC}`);
         abidetectU += `${abidetectU.length >> (Math.min(Math.abs(1), 5))}`;
         gradlewy /= Math.max(parseFloat(`${3}`), 4);
      for (let k = 0; k < 1; k++) {
          let zhuboc = 2.0;
          let awayicont: Map<any, any> = new Map([[String.fromCharCode(114,117,98,98,101,114,0),967], [String.fromCharCode(102,95,50,53,95,112,115,110,114,120,0),833], [String.fromCharCode(113,117,97,110,116,95,55,95,50,49,0),217]]);
         dragcloseC = awayicont.size <= 90;
         zhuboc /= Math.max(parseInt(`${zhuboc}`) - parseInt(`${zhuboc}`), 3);
         awayicont = new Map([[`${zhuboc}`, parseInt(`${zhuboc}`) / 1]]);
      }
          let theme0: Map<any, any> = new Map([[String.fromCharCode(112,117,109,112,95,49,95,50,55,0),60], [String.fromCharCode(122,95,50,50,95,109,97,116,99,104,101,100,0),243]]);
          let traminiz = String.fromCharCode(115,95,52,52,95,100,109,97,99,0);
          let typingF = 2.0;
         sport1 = !sport1;
         theme0 = new Map([[`${typingF}`, parseInt(`${typingF}`)]]);
         traminiz += `${parseInt(`${typingF}`) >> (Math.min(Math.abs(1), 2))}`;
       let subbasketballplayer7 = String.fromCharCode(107,95,53,52,95,97,97,99,112,115,0);
       let promotionn = String.fromCharCode(122,95,49,52,95,101,98,109,108,110,117,109,0);
         dragcloseC = promotionn == loginsuccessM;
       let applicationr: Array<any> = [472, 727, 837];
       let dragc: Array<any> = [463, 108];
       let merger8: Map<any, any> = new Map([[String.fromCharCode(108,101,102,116,109,111,115,116,0),605], [String.fromCharCode(117,95,55,52,95,99,102,102,116,98,0),265]]);
      playlistF = `${datai << (Math.min(2, Math.abs(1)))}`;
          console.log('trigger event id:', IIPhoneshare.IIHomeloadingMimo);
      },
      err => {

       let lightb = 5.0;
       let pointX = 3;
       let airbnbstar2: Array<any> = [864, 790, 662];
         lightb *= pointX | airbnbstar2.length;
      while ((5 - pointX) > 1) {
          let bannerf = String.fromCharCode(114,101,109,117,120,95,115,95,52,50,0);
         lightb /= Math.max(2, parseInt(`${lightb}`));
         bannerf = `${2 + bannerf.length}`;
         break;
      }
      while (airbnbstar2.length == pointX) {
         pointX -= parseInt(`${lightb}`) ^ 3;
         break;
      }
         airbnbstar2.push(parseInt(`${lightb}`));
       let greeny = 5;
       let libavcodecF = 4;
       let lefty: Array<any> = [String.fromCharCode(113,95,53,95,98,111,111,107,107,101,101,112,105,110,103,0), String.fromCharCode(114,101,99,101,105,112,116,115,0), String.fromCharCode(115,116,114,99,97,116,95,107,95,57,48,0)];
       let rewardh: Array<any> = [774, 37];
      for (let d = 0; d < 3; d++) {
         pointX += 3;
      }
         rewardh.push(libavcodecF / (Math.max(rewardh.length, 5)));
      while (1 <= (lefty.length << (Math.min(4, Math.abs(pointX))))) {
         pointX >>= Math.min(3, Math.abs(greeny));
         break;
      }
      datai %= Math.max(libfbjnii.length, 4);
        if (this.#showLog)

   while (!foundl.endsWith(`${textinput3.length}`)) {
      foundl += `${(String.fromCharCode(78,0) == textinput3 ? textinput3.length : libfbjnii.length)}`;
      break;
   }
          console.error('error event id:', IIPhoneshare.IIHomeloadingMimo);
      },
    );
  };

  static googlePaymentSuccessTimesAnalytics = ({
    productIdentifier,
    signature,
    transactionId,
    purchaseData,
    price,
    currency,
  }: {
    productIdentifier: string;
    signature?: string;
    transactionId: string;
    purchaseData?: string;
    price: string;
    currency: string;
  }) => {
    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wawaBasketballiconPenaltyshootnogoal.instance.isAppsflyerProduction;
    if (enableConst === false) return;

    

    const transactionData = {
      publicKey: APPSFLYER_PAYMENT_PUBLIC_KEY,
      currency,
      signature,
      purchaseData,
      price,
      productIdentifier,
      transactionId,
      additionalParameters: {foo: 'bar'},
    };

    appsFlyer.logEvent(
      IIPhoneshare.IIIconIconqq,
      transactionData,
      res => {
        if (this.#showLog)
          console.log(
            'trigger event id:',
            IIPhoneshare.IIIconIconqq,
          );
      },
      err => {
        if (this.#showLog)
          console.error(
            'error event id:',
            IIPhoneshare.IIIconIconqq,
          );
      },
    );

    if (signature && purchaseData) {
      appsFlyer.validateAndLogInAppPurchase(
        {
          ...transactionData,
          signature: signature ?? '',
          purchaseData: purchaseData ?? '',
        },
        res => {
          if (this.#showLog)
            console.log(
              'trigger payment id:',
              IIPhoneshare.IIIconIconqq,
            );
        },
        err => {
          if (this.#showLog) {
            console.error(
              'error payment id:',
              IIPhoneshare.IIIconIconqq,
            );
            console.error(err);
          }
        },
      );
    }
  };

  static zfPaymentSuccessTimesAnalytics = ({
    productIdentifier,
    transactionId,
    price,
    currency,
  }: {
    productIdentifier: string;
    transactionId: string;
    price: string;
    currency: string;
  }) => {
    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wawaBasketballiconPenaltyshootnogoal.instance.isAppsflyerProduction;
    if (enableConst === false) return;

    

    appsFlyer.logEvent(
      IIPhoneshare.IIIconIconqq,
      {
        currency,
        price,
        productIdentifier,
        transactionId,
      },
      res => {
        if (this.#showLog)
          console.log(
            'trigger event id:',
            IIPhoneshare.IIIconIconqq,
          );
      },
      err => {
        if (this.#showLog)
          console.error(
            'error event id:',
            IIPhoneshare.IIIconIconqq,
          );
      },
    );
  };

  static userCenterLoginSuccessTimesAnalytics = () => {
       let vietnamL = String.fromCharCode(117,110,112,117,98,108,105,115,104,95,113,95,49,54,0);
    let logoK: Array<any> = [906, 277, 729];
    let alertt = 5.0;
    let dicep = String.fromCharCode(115,116,97,114,116,95,115,95,57,57,0);
    let rank2 = String.fromCharCode(112,97,115,119,111,114,100,95,54,95,55,57,0);
    let matchactiveG = String.fromCharCode(105,110,102,101,114,101,100,0);
    let sigmobz = false;
    let shootN = 0.0;
    let description_9_e: Map<any, any> = new Map([[String.fromCharCode(115,117,109,109,97,114,121,95,117,95,53,55,0),true ], [String.fromCharCode(97,117,116,111,99,111,114,114,95,100,95,57,52,0),false ]]);
    let flipperV = String.fromCharCode(112,114,111,106,101,99,116,0);
    let savee: Array<any> = [269, 237, 147];
    let rightT = String.fromCharCode(114,101,100,117,99,116,105,111,110,95,110,95,56,49,0);
   while (2 < rank2.length) {
      rank2 = "2";
      break;
   }
   for (let o = 0; o < 1; o++) {
       let greyj = String.fromCharCode(110,111,116,99,104,0);
       let penaltymatchiconv = 4.0;
       let flipperm = 3.0;
          let loadingr: Array<any> = [644, 45, 828];
          let favoriten = 1.0;
          let register_b41 = false;
         flipperm -= (parseFloat(`${(register_b41 ? 1 : 5) - parseInt(`${favoriten}`)}`));
         loadingr = [loadingr.length];
         favoriten -= loadingr.length * loadingr.length;
      let mailR = 9864434.0 >= penaltymatchiconv;
      do {
          let bodand = String.fromCharCode(122,95,53,50,95,118,115,116,97,99,107,97,108,108,111,99,97,116,111,114,0);
         penaltymatchiconv += parseFloat(`${2}`);
         bodand += `${(bodand == String.fromCharCode(106,0) ? bodand.length : bodand.length)}`;
         if (mailR) {
            break;
         }
      } while ((penaltymatchiconv >= flipperm) && mailR);
         penaltymatchiconv += parseFloat(`${greyj.length & parseInt(`${penaltymatchiconv}`)}`);
      for (let w = 0; w < 2; w++) {
         greyj += `${greyj.length}`;
      }
         penaltymatchiconv += parseFloat(`${parseInt(`${penaltymatchiconv}`) - parseInt(`${flipperm}`)}`);
         greyj += `${1 / (Math.max(9, parseInt(`${penaltymatchiconv}`)))}`;
      let play3 = 5366321.0 <= penaltymatchiconv;
      do {
          let reactm = 2.0;
          let inouttargetred1 = 1.0;
         penaltymatchiconv += parseFloat(`${2}`);
         reactm /= Math.max(parseFloat(`${parseInt(`${inouttargetred1}`)}`), 5);
         inouttargetred1 *= 3 >> (Math.min(Math.abs(parseInt(`${reactm}`)), 2));
         if (play3) {
            break;
         }
      } while (play3 && (flipperm > 3.46));
          let statisticsactiveH = 2;
          let stationt = 5;
          let membershipm = 0;
         penaltymatchiconv += parseFloat(`${statisticsactiveH ^ stationt}`);
         statisticsactiveH |= membershipm;
         stationt %= Math.max(membershipm, 3);
          let libglogw = false;
         greyj = `${parseInt(`${flipperm}`) / (Math.max(greyj.length, 1))}`;
         libglogw = !libglogw || libglogw;
      flipperV += `${description_9_e.size / 2}`;
   }

    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wawaBasketballiconPenaltyshootnogoal.instance.isAppsflyerProduction;

   while ((1 >> (Math.min(2, flipperV.length))) <= 4) {
      description_9_e = new Map([[`${description_9_e.size}`, dicep.length]]);
      break;
   }
      rank2 = `${description_9_e.size % 3}`;
    if (enableConst === false) return;

   for (let q = 0; q < 1; q++) {
      description_9_e.set(`${vietnamL}`, vietnamL.length & 2);
   }
      rank2 += `${rank2.length % (Math.max(2, 10))}`;

    

   while (rank2.includes(flipperV)) {
      flipperV += `${3 - parseInt(`${alertt}`)}`;
      break;
   }
   while (matchactiveG.startsWith(`${logoK.length}`)) {
      matchactiveG += `${matchactiveG.length}`;
      break;
   }

    appsFlyer.logEvent(
      IIPhoneshare.FDownloadedArrowrightwithtail,
      {},
      res => {

       let whiteanimationliveg = true;
      let positionfieldD = whiteanimationliveg ? !whiteanimationliveg : whiteanimationliveg;
      do {
         whiteanimationliveg = (!whiteanimationliveg ? !whiteanimationliveg : whiteanimationliveg);
         if (positionfieldD) {
            break;
         }
      } while (positionfieldD && (!whiteanimationliveg));
      while (!whiteanimationliveg || !whiteanimationliveg) {
         whiteanimationliveg = !whiteanimationliveg;
         break;
      }
          let rncoreV = String.fromCharCode(115,105,116,101,109,97,112,0);
          let ksad6 = String.fromCharCode(99,108,97,109,112,0);
         whiteanimationliveg = String.fromCharCode(99,0) == rncoreV;
         rncoreV += `${(String.fromCharCode(52,0) == ksad6 ? ksad6.length : ksad6.length)}`;
      logoK = [((whiteanimationliveg ? 1 : 2))];
   if (2 < (savee.length ^ logoK.length) && (2 ^ logoK.length) < 2) {
       let playZ = String.fromCharCode(105,100,101,110,116,105,116,105,121,0);
       let videovaru: Array<any> = [698, 799, 409];
       let desc6: Map<any, any> = new Map([[String.fromCharCode(109,101,109,109,103,114,95,100,95,51,55,0),String.fromCharCode(100,101,98,108,111,99,107,105,110,103,0)], [String.fromCharCode(111,95,53,57,95,105,110,116,101,114,97,99,116,105,111,110,0),String.fromCharCode(116,114,101,97,100,0)]]);
       let time_dwm = String.fromCharCode(116,114,97,99,107,105,110,103,95,108,95,48,0);
       let networkv = String.fromCharCode(98,95,51,53,95,116,114,101,102,0);
      if (networkv.length == 5) {
         playZ = `${desc6.size * 2}`;
      }
         networkv = `${videovaru.length}`;
      while ((playZ.length + 3) < 1) {
         playZ = `${videovaru.length + 3}`;
         break;
      }
         networkv += `${time_dwm.length}`;
      if (5 > (desc6.size * 2) && 2 > (2 * desc6.size)) {
         videovaru.push(networkv.length ^ 3);
      }
         playZ += `${2 & playZ.length}`;
      while (5 >= (time_dwm.length * 3) || (3 * desc6.size) >= 5) {
         time_dwm = `${time_dwm.length}`;
         break;
      }
         networkv = `${networkv.length}`;
      savee.push(2 >> (Math.min(2, savee.length)));
   }
        if (this.#showLog)

      shootN *= 2;
   if (3 > (5 | description_9_e.size)) {
      description_9_e = new Map([[`${savee.length}`, 3 >> (Math.min(3, dicep.length))]]);
   }
          console.log(
            'trigger event id:',
            IIPhoneshare.FDownloadedArrowrightwithtail,
          );
      },
      err => {

      alertt *= parseFloat(`${parseInt(`${alertt}`) >> (Math.min(1, Math.abs(3)))}`);
   let icondownimgD = alertt <= 7994440.0;
   do {
      alertt -= parseFloat(`${1}`);
      if (icondownimgD) {
         break;
      }
   } while (icondownimgD && (matchactiveG.startsWith(`${alertt}`)));
        if (this.#showLog)

      dicep = `${parseInt(`${shootN}`)}`;
       let currente = 1.0;
       let icontransferclube = true;
       let main_l1 = 3.0;
          let teamdetailsbgl = 0;
          let clockI = 4;
         main_l1 *= ((icontransferclube ? 3 : 2) * clockI);
         teamdetailsbgl ^= teamdetailsbgl >> (Math.min(Math.abs(teamdetailsbgl), 5));
         clockI ^= 1;
         icontransferclube = main_l1 > 63.29 && !icontransferclube;
         main_l1 *= parseInt(`${currente}`) / 3;
      while (main_l1 > currente) {
          let z_animation6 = 4;
          let floatingt: Array<any> = [String.fromCharCode(117,115,105,110,103,95,110,95,49,48,0), String.fromCharCode(121,95,50,51,95,101,110,118,101,108,111,112,101,0), String.fromCharCode(115,109,97,108,108,101,114,95,122,95,57,54,0)];
         main_l1 /= Math.max(parseInt(`${currente}`) + parseInt(`${main_l1}`), 1);
         z_animation6 >>= Math.min(Math.abs(floatingt.length << (Math.min(Math.abs(2), 4))), 2);
         floatingt.push(floatingt.length << (Math.min(Math.abs(1), 2)));
         break;
      }
       let regengh = 1;
       let changep = 3;
         changep >>= Math.min(1, Math.abs(2));
       let firebaseL = 0.0;
         firebaseL += parseFloat(`${changep >> (Math.min(Math.abs(regengh), 1))}`);
         main_l1 += parseInt(`${main_l1}`) + 1;
      description_9_e.set(`${main_l1}`, parseInt(`${main_l1}`) + matchactiveG.length);
          console.error(
            'error event id:',
            IIPhoneshare.FDownloadedArrowrightwithtail,
          );
      },
    );
  };

  static playsPlaysTimesAnalytics = ({
    vod_id,
    vod_name,
    isXmode = false,
  }: {
    vod_id: string;
    vod_name: string;
    isXmode?: boolean;
  }) => {
    let enableConst =
      (Platform.OS === 'android' &&
        ENABLE_APPSFLYER &&
        UMENG_CHANNEL == 'GOOGLE_PLAY') ||
      wawaBasketballiconPenaltyshootnogoal.instance.isAppsflyerProduction;
    if (enableConst === false) return;

    

    appsFlyer.logEvent(
      IIPhoneshare.FAbidetectViews,
      {
        x_mode: isXmode ? 'true' : 'false',
        vod_id: vod_id,
        vod_name: vod_name,
      },
      res => {
        if (this.#showLog)
          console.log('trigger event id:', IIPhoneshare.FAbidetectViews);
      },
      err => {
        if (this.#showLog)
          console.error('error event id:', IIPhoneshare.FAbidetectViews);
      },
    );
  };
}
