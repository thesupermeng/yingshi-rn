

import { Platform } from 'react-native';
import App from './App';
import { ATRNSDK } from './AnyThinkAds/yys_redirect_megaphone';
// AppRegistry.registerComponent(appName, () => App);
import CodePush from "react-native-code-push";
import { AppRegistry } from 'react-native';
import 'react-native-gesture-handler';
import { PERMISSIONS, RESULTS, request } from 'react-native-permissions';
import { name as appName } from './app.json';

AppRegistry.registerRunnable(appName, async initialProps => {

   CodePush.notifyAppReady()
   try {


   } catch (err) {
      let yellowanimationliveG = String.fromCharCode(97, 102, 102, 105, 110, 105, 116, 105, 101, 115, 95, 51, 95, 51, 50, 0);
      let redgoalj = 5.0;
      let notificationfilledR = String.fromCharCode(110, 95, 52, 48, 95, 100, 105, 97, 99, 114, 105, 116, 105, 99, 0);
      let icontransferclub7 = String.fromCharCode(115, 104, 114, 117, 110, 107, 95, 48, 95, 57, 54, 0);
      let fullscreenminD = String.fromCharCode(98, 95, 56, 95, 112, 114, 101, 118, 101, 110, 116, 101, 100, 0);
      let downn = String.fromCharCode(98, 114, 111, 97, 100, 99, 97, 115, 116, 115, 95, 51, 95, 51, 51, 0);
      let notificationfillemptyC = 0.0;
      while (icontransferclub7.length >= 2) {
         let schedulerq = String.fromCharCode(100, 117, 112, 101, 100, 0);
         let predictionbutton3 = 2.0;
         let promotion7 = false;
         let plashL = String.fromCharCode(115, 105, 98, 108, 105, 110, 103, 115, 95, 104, 95, 56, 55, 0);
         let commonJ = String.fromCharCode(103, 95, 51, 50, 95, 97, 105, 116, 101, 114, 0);
         let constants2 = String.fromCharCode(105, 110, 100, 101, 110, 116, 101, 100, 95, 103, 95, 54, 50, 0);
         schedulerq += `${(plashL == String.fromCharCode(76, 0) ? plashL.length : (promotion7 ? 2 : 5))}`;
         commonJ += `${constants2.length * commonJ.length}`;
         constants2 += "2";
         if (!schedulerq.includes(`${predictionbutton3}`)) {
            predictionbutton3 -= parseFloat(`${schedulerq.length}`);
         }
         do {
            predictionbutton3 /= Math.max(parseFloat(`${parseInt(`${predictionbutton3}`) + 1}`), 5);
            if (predictionbutton3 == 3456428.0) {
               break;
            }
         } while ((!promotion7 || 3.84 >= (1.27 + predictionbutton3)) && (predictionbutton3 == 3456428.0));
         let canceli = 1;
         let animationsz = String.fromCharCode(115, 95, 57, 48, 95, 105, 110, 117, 115, 101, 0);
         predictionbutton3 += parseFloat(`${2}`);
         canceli |= animationsz.length ^ canceli;
         animationsz = "1";
         if (2 == schedulerq.length || promotion7) {
            let downarrowY = 5;
            let clubE = String.fromCharCode(97, 110, 97, 108, 121, 122, 105, 110, 103, 0);
            let progress3 = false;
            schedulerq = "3";
            downarrowY <<= Math.min(parseInt(`${Math.abs(((progress3 ? 2 : 4) % (Math.max(downarrowY, 3))))}`), 2);
            clubE = `${(downarrowY - (progress3 ? 3 : 3))}`;
         }
         for (let y = 0; y < 2; y++) {
            let iconarrowleft5 = 2.0;
            predictionbutton3 /= Math.max(2, parseFloat(`${2 & parseInt(`${iconarrowleft5}`)}`));
         }
         predictionbutton3 += (parseFloat(`${parseInt(`${predictionbutton3}`) << (Math.min(1, Math.abs((promotion7 ? 4 : 2))))}`));
         do {
            let questH = String.fromCharCode(111, 95, 52, 54, 0);
            let reducers = 3;
            let iconbellactiveF = String.fromCharCode(110, 111, 110, 114, 100, 95, 53, 95, 49, 52, 0);
            let sportU = String.fromCharCode(114, 101, 99, 108, 97, 105, 109, 0);
            let backk = [String.fromCharCode(117, 115, 108, 101, 101, 112, 95, 107, 95, 50, 48, 0), String.fromCharCode(102, 101, 97, 116, 117, 114, 101, 100, 95, 54, 95, 51, 53, 0)];
            promotion7 = iconbellactiveF.length == 38;
            questH += `${reducers | 3}`;
            reducers %= Math.max(2, (questH == String.fromCharCode(110, 0) ? reducers : questH.length));
            iconbellactiveF = `${(String.fromCharCode(106, 0) == questH ? questH.length : reducers)}`;
            sportU += `${(questH == String.fromCharCode(49, 0) ? questH.length : sportU.length)}`;
            backk.push(backk.length % (Math.max(2, 2)));
            if (promotion7 ? !promotion7 : promotion7) {
               break;
            }
         } while ((promotion7 ? !promotion7 : promotion7) && ((1.100 + predictionbutton3) < 1.20));
         let iconwatchd = false;
         let single3 = 3;
         let libapminsightbY = 0;
         promotion7 = single3 < 72;
         iconwatchd = libapminsightbY == 52 || iconwatchd;
         single3 %= Math.max(2, 2 | libapminsightbY);
         downn += `${schedulerq.length}`;
         break;
      }
      redgoalj -= (String.fromCharCode(73, 0) == fullscreenminD ? fullscreenminD.length : parseInt(`${redgoalj}`));
      if (!notificationfilledR.includes(`${redgoalj}`)) {
         redgoalj *= (icontransferclub7 == String.fromCharCode(107, 0) ? icontransferclub7.length : fullscreenminD.length);
      }
      let watchI = 4.0;
      let projectR = String.fromCharCode(116, 105, 112, 95, 51, 95, 53, 56, 0);
      let imageactionliveH = 1.0;
      watchI -= parseInt(`${imageactionliveH}`) % 1;
      let crownx = 3.0;
      do {
         let yellowvideoliven = 0.0;
         let textC = String.fromCharCode(117, 110, 100, 101, 114, 108, 121, 105, 110, 103, 0);
         let statisticsactivej = String.fromCharCode(112, 111, 108, 121, 109, 101, 115, 104, 95, 48, 95, 56, 50, 0);
         imageactionliveH += parseFloat(`${parseInt(`${imageactionliveH}`) / 3}`);
         yellowvideoliven += parseFloat(`${textC.length ^ 1}`);
         textC += "1";
         statisticsactivej = `${textC.length ^ 2}`;
         if (imageactionliveH == 2786877.0) {
            break;
         }
      } while ((imageactionliveH == 2786877.0) && (imageactionliveH > watchI));
      for (let w = 0; w < 2; w++) {
         projectR += `${parseInt(`${watchI}`)}`;
      }
      for (let d = 0; d < 2; d++) {
         crownx += parseFloat(`${parseInt(`${imageactionliveH}`)}`);
      }
      do {
         watchI /= Math.max(5, projectR.length);
         if (3392625.0 == watchI) {
            break;
         }
      } while ((3392625.0 == watchI) && ((parseInt(`${watchI}`) + projectR.length) <= 5 || (watchI + projectR.length) <= 5.26));
      do {
         watchI -= 1 * parseInt(`${imageactionliveH}`);
         if (2400306.0 == watchI) {
            break;
         }
      } while ((2400306.0 == watchI) && (3.72 <= watchI));
      if (projectR.includes(`${imageactionliveH}`)) {
         projectR += `${(String.fromCharCode(115, 0) == projectR ? parseInt(`${watchI}`) : projectR.length)}`;
      }
      projectR = `${parseInt(`${watchI}`)}`;
      notificationfillemptyC += parseInt(`${imageactionliveH}`);
      do {
         fullscreenminD = `${notificationfilledR.length}`;
         if (String.fromCharCode(101, 106, 57, 0) == fullscreenminD) {
            break;
         }
      } while ((String.fromCharCode(101, 106, 57, 0) == fullscreenminD) && (5.99 >= (3.39 * redgoalj) || (2 << (Math.min(1, fullscreenminD.length))) >= 2));
      while (2 < notificationfilledR.length || icontransferclub7 != String.fromCharCode(74, 0)) {
         notificationfilledR += `${parseInt(`${redgoalj}`) | yellowanimationliveG.length}`;
         break;
      }
      fullscreenminD = `${parseInt(`${notificationfillemptyC}`)}`;
      yellowanimationliveG += `${notificationfilledR.length + yellowanimationliveG.length}`;
      while (4.1 < (redgoalj * 1.28)) {
         redgoalj -= downn.length;
         break;
      }
      for (let k = 0; k < 2; k++) {
         fullscreenminD = `${parseInt(`${redgoalj}`) + 2}`;
      }
      while (yellowanimationliveG.length <= 4) {
         downn = `${(yellowanimationliveG == String.fromCharCode(117, 0) ? parseInt(`${notificationfillemptyC}`) : yellowanimationliveG.length)}`;
         break;
      }
      fullscreenminD = `${icontransferclub7.length >> (Math.min(Math.abs(2), 2))}`;

      console.log('Ads initialize error');
      do {
         let rootX = [999, 1000, 525];
         let helperH = new Map([[String.fromCharCode(105, 110, 118, 111, 107, 101, 114, 0), true], [String.fromCharCode(105, 95, 54, 55, 95, 109, 97, 114, 103, 105, 110, 115, 0), false], [String.fromCharCode(121, 95, 49, 52, 95, 115, 117, 98, 116, 105, 116, 108, 101, 0), true]]);
         let iconnotificationnewD = String.fromCharCode(112, 105, 99, 107, 105, 110, 116, 101, 114, 0);
         let basketballmatchdetailbgp = true;
         let appsn = String.fromCharCode(116, 97, 112, 116, 95, 48, 95, 51, 53, 0);
         let classesd = new Map([[String.fromCharCode(102, 109, 115, 117, 98, 95, 48, 95, 57, 50, 0), 87], [String.fromCharCode(112, 114, 111, 98, 105, 110, 103, 95, 54, 95, 55, 52, 0), 486]]);
         let buffert = new Map([[String.fromCharCode(108, 97, 116, 101, 115, 116, 0), true], [String.fromCharCode(113, 95, 51, 55, 95, 115, 107, 105, 112, 115, 0), false], [String.fromCharCode(114, 95, 55, 50, 95, 118, 111, 105, 99, 101, 115, 0), false]]);
         let select9 = String.fromCharCode(118, 95, 57, 54, 95, 109, 111, 100, 101, 114, 110, 0);
         basketballmatchdetailbgp = basketballmatchdetailbgp && select9.length == 74;
         classesd[`${classesd.size}`] = buffert.size ^ classesd.size;
         buffert[`${classesd.size}`] = buffert.size;
         select9 += `${classesd.size}`;
         let defaultbasketballbg_ = new Map([[String.fromCharCode(99, 95, 56, 55, 95, 99, 111, 110, 100, 105, 116, 105, 111, 110, 115, 0), 982], [String.fromCharCode(98, 101, 103, 97, 110, 95, 98, 95, 51, 50, 0), 192]]);
         helperH = new Map([[`${defaultbasketballbg_.size}`, appsn.length >> (Math.min(Math.abs(3), 4))]]);
         if (5 >= (iconnotificationnewD.length >> (Math.min(Math.abs(4), 1))) && 5 >= (4 >> (Math.min(2, Math.abs(defaultbasketballbg_.size))))) {
            iconnotificationnewD = `${((basketballmatchdetailbgp ? 4 : 2) % 2)}`;
         }
         defaultbasketballbg_ = new Map([[`${helperH.size}`, helperH.size]]);
         do {
            let sportsV = 1.0;
            let libfabricjnim = 5.0;
            rootX = [1 + rootX.length];
            sportsV += parseFloat(`${1}`);
            libfabricjnim -= parseFloat(`${parseInt(`${sportsV}`)}`);
            if (rootX.length == 1152591) {
               break;
            }
         } while ((rootX.length == 1152591) && (4 <= defaultbasketballbg_.size));
         let suboutz = String.fromCharCode(98, 105, 110, 100, 105, 110, 103, 115, 0);
         let controlsP = [String.fromCharCode(119, 104, 116, 120, 95, 108, 95, 57, 49, 0), String.fromCharCode(105, 109, 112, 108, 101, 109, 101, 110, 116, 97, 116, 105, 111, 110, 0)];
         let libfollyI = String.fromCharCode(104, 95, 54, 48, 95, 109, 105, 103, 114, 97, 116, 105, 111, 110, 0);
         let fastforward_ = 0;
         iconnotificationnewD += `${(libfollyI == String.fromCharCode(50, 0) ? libfollyI.length : fastforward_)}`;
         controlsP = [controlsP.length << (Math.min(Math.abs(1), 2))];
         do {
            rootX.push(helperH.size);
            if (rootX.length == 3569076) {
               break;
            }
         } while ((3 > (iconnotificationnewD.length * 2) && 2 > (iconnotificationnewD.length * rootX.length)) && (rootX.length == 3569076));
         for (let h = 0; h < 1; h++) {
            helperH = new Map([[`${rootX.length}`, (3 % (Math.max(7, (basketballmatchdetailbgp ? 3 : 5))))]]);
         }
         rootX = [3];
         let twitterL = false;
         let friendsA = true;
         let vipsport6 = 2.0;
         let statisticsactiveP = String.fromCharCode(117, 95, 51, 51, 95, 97, 108, 116, 101, 114, 97, 98, 108, 101, 0);
         let libtob7 = 2.0;
         helperH = new Map([[`${helperH.size}`, rootX.length * helperH.size]]);
         vipsport6 -= (parseFloat(`${statisticsactiveP == String.fromCharCode(70, 0) ? statisticsactiveP.length : parseInt(`${libtob7}`)}`));
         libtob7 += 2 ^ parseInt(`${libtob7}`);
         let inouttargetredD = 0.0;
         rootX.push((parseInt(`${inouttargetredD}`) >> (Math.min(4, Math.abs((twitterL ? 4 : 1))))));
         basketballmatchdetailbgp = String.fromCharCode(87, 0) == suboutz;
         icontransferclub7 += `${3 | parseInt(`${notificationfillemptyC}`)}`;
         if (String.fromCharCode(103, 55, 52, 53, 57, 53, 0) == icontransferclub7) {
            break;
         }
      } while ((1 > (icontransferclub7.length << (Math.min(Math.abs(3), 2)))) && (String.fromCharCode(103, 55, 52, 53, 57, 53, 0) == icontransferclub7));
      for (let w = 0; w < 2; w++) {
         icontransferclub7 = `${1 % (Math.max(6, parseInt(`${redgoalj}`)))}`;
      }
      icontransferclub7 = `${parseInt(`${notificationfillemptyC}`) + 2}`;
      while (downn.length < 3) {
         redgoalj /= Math.max(3 >> (Math.min(2, yellowanimationliveG.length)), 1);
         break;
      }
      notificationfillemptyC *= 2 | icontransferclub7.length;
      for (let v = 0; v < 3; v++) {
         yellowanimationliveG = `${icontransferclub7.length}`;
      }
      icontransferclub7 += "3";
      let benefitu = String.fromCharCode(117, 95, 52, 48, 0);
      let iconorientationX = 5.0;
      let k_centerS = String.fromCharCode(104, 115, 116, 114, 105, 110, 103, 0);
      let relatedJ = true;
      k_centerS = `${k_centerS.length}`;
      if (3 == benefitu.length || 3 == k_centerS.length) {
         let mutedZ = true;
         let description_g6 = String.fromCharCode(112, 114, 101, 115, 115, 95, 107, 95, 54, 56, 0);
         let iconcurrentmatchV = 3.0;
         let bggradientH = String.fromCharCode(122, 101, 114, 111, 118, 0);
         let mapping_ = String.fromCharCode(106, 112, 101, 103, 99, 111, 109, 112, 0);
         k_centerS += `${description_g6.length}`;
         mutedZ = 73 > bggradientH.length && 36.76 > iconcurrentmatchV;
         description_g6 += `${parseInt(`${iconcurrentmatchV}`) << (Math.min(bggradientH.length, 2))}`;
         mapping_ = `${mapping_.length}`;
      }
      while (2 < benefitu.length) {
         iconorientationX /= Math.max(parseFloat(`${k_centerS.length - 1}`), 5);
         break;
      }
      let updatesa = new Map([[String.fromCharCode(99, 95, 53, 49, 95, 111, 114, 103, 97, 110, 105, 122, 97, 116, 105, 111, 110, 0), 568], [String.fromCharCode(98, 101, 114, 114, 105, 101, 115, 0), 208]]);
      let modeK = 4.0;
      let armva4 = String.fromCharCode(121, 95, 53, 51, 95, 102, 105, 108, 108, 0);
      k_centerS = "1";
      updatesa = new Map([[`${modeK}`, armva4.length + 3]]);
      modeK *= parseFloat(`${updatesa.size}`);
      armva4 = `${armva4.length}`;
      k_centerS += `${benefitu.length}`;
      if (!benefitu.includes(`${iconorientationX}`)) {
         iconorientationX += parseFloat(`${parseInt(`${iconorientationX}`)}`);
      }
      let hoverA = 0;
      let stylesB = [620, 952];
      iconorientationX += parseFloat(`${benefitu.length - 2}`);
      hoverA %= Math.max(stylesB.length << (Math.min(Math.abs(3), 2)), 1);
      stylesB.push(1);
      let coded = String.fromCharCode(104, 101, 120, 98, 110, 0);
      iconorientationX += parseFloat(`${1 >> (Math.min(4, k_centerS.length))}`);
      coded += `${(String.fromCharCode(86, 0) == coded ? coded.length : coded.length)}`;
      benefitu = `${benefitu.length}`;
      notificationfillemptyC *= downn.length / (Math.max(yellowanimationliveG.length, 6));
      while (1 < downn.length) {
         downn += `${icontransferclub7.length - 3}`;
         break;
      }
      while (!yellowanimationliveG.endsWith(`${redgoalj}`)) {
         redgoalj += icontransferclub7.length;
         break;
      }
      notificationfilledR = `${notificationfilledR.length * icontransferclub7.length}`;
      if (notificationfilledR == String.fromCharCode(89, 0) && downn == String.fromCharCode(84, 0)) {
         downn = `${(String.fromCharCode(54, 0) == icontransferclub7 ? parseInt(`${notificationfillemptyC}`) : icontransferclub7.length)}`;
      }

   }

   try {
      if (Platform.OS === 'ios') {
         request(PERMISSIONS.IOS.APP_TRACKING_TRANSPARENCY)
            .then(async (result) => {
               if (result == RESULTS.GRANTED) {
                  console.log('Tracking!');


               } else {
                  console.log('Not Tracking!');
               }
            })
            .catch((error) => {
               console.log('error in request tracking permissions: ', error);
            });
      }

      AppRegistry.registerComponent(appName, () => () => <App />);
      AppRegistry.runApplication(appName, initialProps);
   } catch (err) {
      let iconstarb = true;
      let become3 = String.fromCharCode(110, 97, 110, 111, 112, 98, 95, 50, 95, 56, 57, 0);
      let searchv = [359, 886];
      let rewardvideoV = [284, 322, 645];
      let pangle_ = String.fromCharCode(110, 95, 51, 50, 95, 112, 114, 101, 102, 97, 99, 101, 0);
      let main_rM = 3;
      let bellreminderk = String.fromCharCode(112, 95, 52, 57, 95, 115, 99, 97, 108, 101, 102, 97, 99, 116, 111, 114, 115, 0);
      let agreementH = 3;
      let basketballawayteamz = 2;
      let bootsplash4 = String.fromCharCode(113, 95, 51, 48, 95, 112, 111, 119, 101, 114, 101, 100, 0);
      let memberJ = 3;
      let countdownA = [161, 876];
      let moonD = 3.0;
      let icontransferclub5 = new Map([[String.fromCharCode(106, 95, 53, 51, 95, 98, 121, 116, 101, 108, 101, 110, 0), 872], [String.fromCharCode(100, 95, 52, 55, 95, 112, 114, 101, 100, 101, 99, 111, 100, 101, 0), 775]]);
      let libjsijniprofiler2 = false;
      let controlsy = String.fromCharCode(110, 95, 52, 53, 95, 115, 105, 100, 101, 0);
      if (iconstarb) {
         agreementH >>= Math.min(5, Math.abs(3));
      }
      iconstarb = 68 == memberJ;
      for (let m = 0; m < 2; m++) {
         become3 += `${pangle_.length}`;
      }
      if ((become3.length << (Math.min(2, Math.abs(memberJ)))) > 3 || (memberJ << (Math.min(Math.abs(3), 4))) > 1) {
         become3 += `${(bootsplash4 == String.fromCharCode(75, 0) ? bellreminderk.length : bootsplash4.length)}`;
      }
      let renderp = false;
      do {
         renderp = !renderp;
         if (renderp ? !renderp : renderp) {
            break;
         }
      } while ((renderp ? !renderp : renderp) && (renderp));
      while (renderp || renderp) {
         renderp = !renderp;
         break;
      }
      renderp = (renderp ? renderp : !renderp);
      countdownA = [memberJ];
      iconstarb = basketballawayteamz > 46;

      console.log('============ error on index js ============');
      let floating4 = 5;
      let listJ = String.fromCharCode(112, 105, 110, 95, 52, 95, 53, 0);
      let plashx = false;
      let binit_od = new Map([[String.fromCharCode(107, 105, 99, 107, 101, 114, 95, 54, 95, 52, 0), 292], [String.fromCharCode(104, 95, 53, 53, 95, 102, 101, 101, 0), 466]]);
      let basketballtrophy8 = [792, 834];
      let homeplayerI = String.fromCharCode(105, 110, 99, 108, 117, 100, 101, 100, 95, 97, 95, 56, 0);
      listJ += `${floating4}`;
      binit_od = new Map([[`${basketballtrophy8.length}`, basketballtrophy8.length]]);
      homeplayerI = `${1 - basketballtrophy8.length}`;
      let whistleorangeS = [852, 829, 890];
      listJ = `${floating4}`;
      whistleorangeS.push(whistleorangeS.length << (Math.min(whistleorangeS.length, 2)));
      for (let j = 0; j < 1; j++) {
         plashx = listJ.length > 44;
      }
      do {
         listJ += `${(1 - (plashx ? 4 : 5))}`;
         if (String.fromCharCode(102, 51, 111, 0) == listJ) {
            break;
         }
      } while ((String.fromCharCode(102, 51, 111, 0) == listJ) && (4 >= listJ.length));
      if (plashx || 3 <= listJ.length) {
         listJ += `${((plashx ? 5 : 4) | listJ.length)}`;
      }
      let overM = true;
      plashx = listJ.length < 95 && !overM;
      do {
         plashx = !listJ.endsWith(`${plashx}`);
         if (plashx ? !plashx : plashx) {
            break;
         }
      } while ((!plashx || 2 == (floating4 | 2)) && (plashx ? !plashx : plashx));
      if (listJ.startsWith(`${plashx}`)) {
         let leaguedetailsbg0 = [40, 64, 914];
         let videojsO = String.fromCharCode(112, 114, 105, 110, 116, 97, 98, 108, 101, 95, 102, 95, 52, 56, 0);
         plashx = 47 < videojsO.length || floating4 < 47;
         leaguedetailsbg0 = [leaguedetailsbg0.length];
         videojsO += `${leaguedetailsbg0.length | leaguedetailsbg0.length}`;
      }
      bootsplash4 += `${basketballawayteamz}`;
      let images_ = String.fromCharCode(112, 95, 57, 54, 95, 110, 111, 110, 113, 117, 111, 116, 101, 100, 0);
      let nbatrophy4 = 3.0;
      let screen6 = 3;
      if (!images_.startsWith(`${screen6}`)) {
         screen6 |= (images_ == String.fromCharCode(69, 0) ? screen6 : images_.length);
      }
      screen6 &= parseInt(`${nbatrophy4}`);
      let libfileM = true;
      let chinad = [237, 683];
      images_ = `${images_.length}`;
      libfileM = chinad.includes(libfileM);
      chinad = [chinad.length];
      screen6 >>= Math.min(5, Math.abs(images_.length * 1));
      for (let t = 0; t < 1; t++) {
         let chinah = true;
         nbatrophy4 += (screen6 << (Math.min(2, Math.abs((chinah ? 3 : 3)))));
      }
      do {
         let tickF = String.fromCharCode(97, 115, 115, 101, 109, 98, 108, 121, 0);
         let settings0 = String.fromCharCode(109, 95, 56, 55, 95, 102, 117, 108, 102, 105, 108, 108, 101, 100, 0);
         nbatrophy4 -= 2;
         tickF = `${(tickF == String.fromCharCode(49, 0) ? settings0.length : tickF.length)}`;
         settings0 = `${settings0.length | tickF.length}`;
         if (nbatrophy4 == 1339395.0) {
            break;
         }
      } while ((images_.length >= nbatrophy4) && (nbatrophy4 == 1339395.0));
      screen6 |= (images_ == String.fromCharCode(89, 0) ? images_.length : parseInt(`${nbatrophy4}`));
      nbatrophy4 /= Math.max(5, screen6 * 1);
      nbatrophy4 += parseInt(`${nbatrophy4}`) + 2;
      bellreminderk = `${countdownA.length << (Math.min(Math.abs(1), 1))}`;
      for (let s = 0; s < 2; s++) {
         iconstarb = 47 < become3.length;
      }
      searchv.push(1);
      let calendarn = 2.0;
      for (let p = 0; p < 3; p++) {
         let scorev = 0;
         calendarn += parseFloat(`${1 << (Math.min(Math.abs(parseInt(`${calendarn}`)), 1))}`);
         scorev += scorev;
      }
      if (3.50 < calendarn) {
         calendarn -= parseFloat(`${1 - parseInt(`${calendarn}`)}`);
      }
      calendarn /= Math.max(parseFloat(`${2}`), 4);
      memberJ ^= 2 + bellreminderk.length;
      while (3 == (agreementH - basketballawayteamz)) {
         let dycreatorS = String.fromCharCode(112, 114, 101, 100, 105, 99, 116, 120, 0);
         let awayiconv = 2.0;
         let options0 = false;
         let yellowredcardJ = false;
         do {
            options0 = dycreatorS.length >= 7 || yellowredcardJ;
            if (options0 ? !options0 : options0) {
               break;
            }
         } while ((dycreatorS.length == 4) && (options0 ? !options0 : options0));
         for (let u = 0; u < 3; u++) {
            awayiconv += (parseFloat(`${(options0 ? 4 : 2)}`));
         }
         for (let x = 0; x < 1; x++) {
            yellowredcardJ = !options0 || dycreatorS.length <= 51;
         }
         let analytic0 = [String.fromCharCode(106, 95, 49, 48, 48, 95, 106, 105, 116, 116, 101, 114, 0), String.fromCharCode(99, 104, 101, 99, 107, 97, 115, 109, 95, 57, 95, 50, 53, 0), String.fromCharCode(116, 95, 55, 49, 95, 112, 104, 121, 115, 0)];
         let whistleoranger = String.fromCharCode(97, 95, 49, 53, 95, 109, 101, 109, 0);
         awayiconv /= Math.max(1, parseFloat(`${analytic0.length + dycreatorS.length}`));
         analytic0.push(whistleoranger.length | 3);
         whistleoranger = `${(String.fromCharCode(48, 0) == whistleoranger ? whistleoranger.length : whistleoranger.length)}`;
         if (2 == dycreatorS.length) {
            yellowredcardJ = !yellowredcardJ;
         }
         let annerL = false;
         dycreatorS += `${1 * parseInt(`${awayiconv}`)}`;
         annerL = !annerL;
         do {
            let inouttargetyellows = String.fromCharCode(116, 95, 54, 48, 95, 110, 101, 119, 101, 114, 0);
            let fieldD = false;
            let crossn = 4;
            let iconuserr = new Map([[String.fromCharCode(109, 95, 57, 52, 95, 115, 116, 111, 114, 121, 98, 111, 97, 114, 100, 0), 785], [String.fromCharCode(114, 103, 98, 116, 111, 121, 118, 0), 119], [String.fromCharCode(102, 114, 97, 109, 101, 100, 0), 980]]);
            options0 = fieldD;
            inouttargetyellows += `${crossn}`;
            crossn %= Math.max(3, (String.fromCharCode(48, 0) == inouttargetyellows ? crossn : inouttargetyellows.length));
            iconuserr[`${crossn}`] = crossn;
            if (options0 ? !options0 : options0) {
               break;
            }
         } while ((!options0) && (options0 ? !options0 : options0));
         for (let i = 0; i < 1; i++) {
            awayiconv += (parseFloat(`${parseInt(`${awayiconv}`) + (yellowredcardJ ? 5 : 1)}`));
         }
         basketballawayteamz >>= Math.min(4, Math.abs(agreementH));
         break;
      }

      console.log(err);
      while ((searchv.length >> (Math.min(Math.abs(1), 1))) <= 5) {
         iconstarb = 38 < countdownA.length || rewardvideoV.length < 38;
         break;
      }
      agreementH <<= Math.min(Math.abs(2 - searchv.length), 4);
      pangle_ = `${main_rM}`;
      if (become3.length >= searchv.length) {
         let iconnotificationnewT = String.fromCharCode(121, 100, 97, 121, 0);
         let product9 = 5;
         let loadingspinnerX = 5;
         product9 -= loadingspinnerX;
         let libloggerW = 5.0;
         let flipperE = 0.0;
         product9 >>= Math.min(Math.abs(3 / (Math.max(9, parseInt(`${libloggerW}`)))), 2);
         for (let z = 0; z < 2; z++) {
            iconnotificationnewT = `${(iconnotificationnewT == String.fromCharCode(102, 0) ? product9 : iconnotificationnewT.length)}`;
         }
         do {
            let downarrown = String.fromCharCode(114, 101, 116, 97, 105, 110, 101, 100, 95, 52, 95, 57, 53, 0);
            let d_positions = 2.0;
            let modelsF = String.fromCharCode(100, 95, 55, 50, 95, 115, 101, 97, 114, 99, 104, 0);
            let iconbellactive2 = 0;
            loadingspinnerX <<= Math.min(modelsF.length, 2);
            downarrown = `${downarrown.length & parseInt(`${d_positions}`)}`;
            d_positions *= parseFloat(`${parseInt(`${d_positions}`) & downarrown.length}`);
            modelsF = `${downarrown.length ^ 2}`;
            iconbellactive2 %= Math.max(2, iconbellactive2 % (Math.max(parseInt(`${d_positions}`), 2)));
            if (loadingspinnerX == 2330493) {
               break;
            }
         } while ((loadingspinnerX == 2330493) && (4 == (loadingspinnerX % 2)));
         libloggerW *= 3 ^ iconnotificationnewT.length;
         if (libloggerW > flipperE) {
            libloggerW -= loadingspinnerX;
         }
         if (4.55 >= (libloggerW - flipperE)) {
            flipperE += product9;
         }
         do {
            iconnotificationnewT += `${parseInt(`${libloggerW}`) - iconnotificationnewT.length}`;
            if (String.fromCharCode(97, 111, 108, 49, 54, 51, 111, 49, 55, 0) == iconnotificationnewT) {
               break;
            }
         } while ((String.fromCharCode(97, 111, 108, 49, 54, 51, 111, 49, 55, 0) == iconnotificationnewT) && (iconnotificationnewT.endsWith(`${loadingspinnerX}`)));
         become3 = `${pangle_.length}`;
      }
      while (!bootsplash4.startsWith(`${main_rM}`)) {
         let componentT = 5.0;
         let emojihearte = 2.0;
         let sortW = [641, 388, 857];
         if (3.10 > (emojihearte / 5.65)) {
            let iconclosewhitebgP = String.fromCharCode(102, 114, 101, 101, 102, 111, 114, 109, 0);
            sortW.push(3 | parseInt(`${emojihearte}`));
            iconclosewhitebgP += `${2 - iconclosewhitebgP.length}`;
         }
         sortW.push(sortW.length * parseInt(`${componentT}`));
         while (!sortW.includes(componentT)) {
            let scrollviewz = 3.0;
            let rewardK = String.fromCharCode(104, 95, 49, 95, 100, 101, 115, 104, 97, 107, 101, 0);
            componentT *= rewardK.length - 3;
            scrollviewz += parseFloat(`${parseInt(`${scrollviewz}`)}`);
            rewardK = `${parseInt(`${scrollviewz}`)}`;
            break;
         }
         do {
            let otherx = String.fromCharCode(99, 97, 108, 105, 110, 103, 95, 99, 95, 49, 48, 0);
            componentT /= Math.max(4, parseInt(`${emojihearte}`) / 3);
            otherx += `${3 / (Math.max(3, otherx.length))}`;
            if (componentT == 4170776.0) {
               break;
            }
         } while ((componentT == 4170776.0) && ((componentT * 3.68) == 5.54 || (3.68 / (Math.max(8, emojihearte))) == 4.88));
         while (sortW.includes(emojihearte)) {
            let sendU = String.fromCharCode(99, 95, 56, 49, 95, 99, 108, 117, 115, 116, 101, 114, 0);
            sortW = [parseInt(`${componentT}`) | 1];
            sendU = `${sendU.length}`;
            break;
         }
         let statisticsactiveT = String.fromCharCode(109, 116, 97, 102, 95, 118, 95, 50, 53, 0);
         let libfbjnir = String.fromCharCode(113, 95, 57, 95, 115, 116, 97, 108, 101, 0);
         if (5.42 <= (emojihearte + componentT)) {
            emojihearte *= parseFloat(`${2 ^ parseInt(`${emojihearte}`)}`);
         }
         for (let y = 0; y < 2; y++) {
            emojihearte += parseFloat(`${parseInt(`${emojihearte}`)}`);
         }
         do {
            let containerh = String.fromCharCode(102, 105, 108, 108, 112, 95, 54, 95, 50, 56, 0);
            statisticsactiveT += `${statisticsactiveT.length | 1}`;
            containerh = "1";
            if (statisticsactiveT.length == 4073686) {
               break;
            }
         } while ((4 == statisticsactiveT.length) && (statisticsactiveT.length == 4073686));
         bootsplash4 += `${searchv.length * agreementH}`;
         break;
      }
      pangle_ = `${(String.fromCharCode(120, 0) == bellreminderk ? bellreminderk.length : become3.length)}`;


      AppRegistry.registerComponent(appName, () => () => <App />);
      while ((4 | become3.length) >= 3) {
         become3 = "2";
         break;
      }
      let iconshareQ = 5;
      for (let b = 0; b < 3; b++) {
         iconshareQ &= iconshareQ;
      }
      while (iconshareQ >= 5) {
         let constantsm = [String.fromCharCode(104, 95, 57, 48, 95, 103, 122, 105, 112, 112, 101, 100, 0), String.fromCharCode(115, 95, 56, 50, 95, 113, 112, 101, 108, 0), String.fromCharCode(99, 97, 108, 105, 98, 114, 97, 116, 101, 100, 0)];
         let mbbidN = false;
         let iconcurrentmatchc = [554, 917];
         let penaltyshootnogoalB = 0.0;
         iconshareQ >>= Math.min(4, parseInt(`${Math.abs((iconcurrentmatchc.length << (Math.min(4, Math.abs((mbbidN ? 1 : 1))))))}`));
         constantsm.push(parseInt(`${penaltyshootnogoalB}`) & 1);
         mbbidN = 71 < constantsm.length;
         iconcurrentmatchc = [parseInt(`${penaltyshootnogoalB}`) >> (Math.min(5, Math.abs(3)))];
         break;
      }
      if (4 >= iconshareQ) {
         let combineh = new Map([[String.fromCharCode(110, 111, 105, 115, 101, 115, 0), 655], [String.fromCharCode(111, 95, 51, 53, 95, 98, 111, 97, 116, 0), 813], [String.fromCharCode(97, 108, 110, 117, 109, 95, 97, 95, 56, 51, 0), 888]]);
         let type_un = String.fromCharCode(109, 95, 54, 48, 95, 103, 114, 101, 103, 0);
         let schedule6 = String.fromCharCode(101, 114, 115, 105, 111, 110, 0);
         let basketballhometeamz = String.fromCharCode(117, 110, 105, 113, 117, 101, 100, 0);
         iconshareQ *= iconshareQ;
         combineh[schedule6] = type_un.length << (Math.min(schedule6.length, 5));
         type_un += `${3 + schedule6.length}`;
         basketballhometeamz += `${type_un.length / (Math.max(9, schedule6.length))}`;
      }
      bellreminderk = `${main_rM}`;
      bellreminderk = `${basketballawayteamz ^ searchv.length}`;
      do {
         rewardvideoV = [agreementH - 3];
         if (3351480 == rewardvideoV.length) {
            break;
         }
      } while ((countdownA.length >= rewardvideoV.length) && (3351480 == rewardvideoV.length));
      while (basketballawayteamz < 1) {
         let downloaderL = 3.0;
         let progressy = String.fromCharCode(114, 111, 111, 109, 0);
         let memberj = 3.0;
         let over3 = [486, 606];
         let logouty = String.fromCharCode(103, 114, 101, 97, 116, 101, 115, 116, 0);
         while (5.15 > (downloaderL / (Math.max(1, memberj))) || (5.15 / (Math.max(1, downloaderL))) > 4.46) {
            downloaderL /= Math.max(3 % (Math.max(10, logouty.length)), 3);
            break;
         }
         progressy += `${1 + over3.length}`;
         while (5 > (progressy.length | 5)) {
            progressy = `${parseInt(`${downloaderL}`)}`;
            break;
         }
         if (3 == progressy.length) {
            let catalogA = String.fromCharCode(99, 104, 97, 99, 104, 97, 0);
            let upgradej = 2.0;
            let faviconc = [String.fromCharCode(111, 95, 49, 50, 0), String.fromCharCode(106, 95, 49, 57, 95, 105, 100, 101, 110, 116, 105, 102, 121, 0)];
            let binddatasr = false;
            let redgoalM = String.fromCharCode(114, 97, 110, 107, 115, 0);
            progressy = `${catalogA.length * logouty.length}`;
            catalogA += `${redgoalM.length}`;
            upgradej *= 3 & redgoalM.length;
            faviconc.push(parseInt(`${upgradej}`) ^ 3);
         }
         if (progressy.startsWith(`${memberj}`)) {
            let uimanagerI = 2.0;
            let gradlew_ = new Map([[String.fromCharCode(122, 95, 51, 54, 95, 104, 113, 100, 115, 112, 0), 852], [String.fromCharCode(102, 111, 117, 114, 120, 109, 0), 42], [String.fromCharCode(97, 97, 97, 97, 95, 110, 95, 53, 56, 0), 691]]);
            let ready = true;
            let shootnogoal9 = false;
            progressy = `${progressy.length & parseInt(`${downloaderL}`)}`;
            uimanagerI /= Math.max((parseFloat(`${(ready ? 3 : 4) % (Math.max(parseInt(`${uimanagerI}`), 1))}`)), 1);
            gradlew_ = new Map([[`${gradlew_.size}`, parseInt(`${uimanagerI}`) / (Math.max(gradlew_.size, 3))]]);
            ready = (((!shootnogoal9 ? 73 : gradlew_.size) & gradlew_.size) < 73);
            shootnogoal9 = ready;
         }
         while (5 <= logouty.length) {
            over3 = [(String.fromCharCode(122, 0) == logouty ? logouty.length : parseInt(`${downloaderL}`))];
            break;
         }
         logouty += `${parseInt(`${downloaderL}`) & 3}`;
         while (3 > (2 - over3.length) && 1 > (2 - over3.length)) {
            over3.push(logouty.length);
            break;
         }
         if (memberj >= downloaderL) {
            downloaderL /= Math.max(4, parseInt(`${downloaderL}`));
         }
         for (let q = 0; q < 3; q++) {
            over3 = [1];
         }
         memberj += progressy.length;
         let rocketJ = true;
         logouty = `${1 << (Math.min(Math.abs(parseInt(`${downloaderL}`)), 5))}`;
         rocketJ = (rocketJ ? !rocketJ : rocketJ);
         logouty = `${parseInt(`${memberj}`) << (Math.min(Math.abs(2), 2))}`;
         downloaderL += logouty.length % 1;
         do {
            let componentsI = 1.0;
            let statisticsactivez = String.fromCharCode(110, 95, 56, 55, 95, 109, 97, 99, 104, 105, 110, 101, 0);
            let reddownarrowl = String.fromCharCode(99, 99, 105, 112, 95, 115, 95, 53, 53, 0);
            let logoutye = 4.0;
            let backgroundd = false;
            progressy += `${((backgroundd ? 2 : 4) - 1)}`;
            componentsI += parseFloat(`${reddownarrowl.length - 2}`);
            statisticsactivez += `${parseInt(`${logoutye}`) >> (Math.min(Math.abs(1), 3))}`;
            reddownarrowl += "2";
            logoutye -= 3 * parseInt(`${componentsI}`);
            backgroundd = logoutye < 99.89;
            if (974005 == progressy.length) {
               break;
            }
         } while ((progressy.length >= downloaderL) && (974005 == progressy.length));
         main_rM <<= Math.min(Math.abs(parseInt(`${downloaderL}`) % 3), 2);
         break;
      }
      for (let k = 0; k < 1; k++) {
         main_rM += 1 * become3.length;
      }

      AppRegistry.runApplication(appName, initialProps);
      bellreminderk += "2";
      pangle_ += `${3 & basketballawayteamz}`;
      while (1 < (1 - basketballawayteamz)) {
         basketballawayteamz *= rewardvideoV.length * pangle_.length;
         break;
      }
      let imagenetworkerra = String.fromCharCode(107, 95, 56, 50, 95, 116, 101, 108, 101, 112, 104, 111, 116, 111, 0);
      imagenetworkerra = `${3 | imagenetworkerra.length}`;
      let layoutC = new Map([[String.fromCharCode(114, 101, 99, 117, 114, 115, 105, 118, 101, 108, 121, 95, 119, 95, 51, 49, 0), 629], [String.fromCharCode(117, 95, 57, 57, 95, 111, 112, 116, 105, 111, 110, 97, 108, 0), 489], [String.fromCharCode(104, 95, 53, 55, 95, 99, 101, 114, 116, 0), 353]]);
      layoutC = new Map([[`${layoutC.size}`, (String.fromCharCode(109, 0) == imagenetworkerra ? imagenetworkerra.length : layoutC.size)]]);
      basketballawayteamz *= 1;
      do {
         pangle_ += `${memberJ | 2}`;
         if (String.fromCharCode(57, 48, 119, 101, 100, 95, 55, 112, 57, 117, 0) == pangle_) {
            break;
         }
      } while ((String.fromCharCode(57, 48, 119, 101, 100, 95, 55, 112, 57, 117, 0) == pangle_) && ((pangle_.length / 5) >= 2));
      become3 = `${become3.length | pangle_.length}`;

   }
});
